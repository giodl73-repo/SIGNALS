out = open('simulations/quest/rubrics/topic-plan-rubric-v7-2026-03-15.md', 'w', encoding='utf-8')
W = out.write

W("# topic-plan rubric v7\n\n---\n\n")
W("## What changed v6 -> v7\n\n")
W("Three new aspirational criteria, all sourced from R6 variate excellence signals:\n\n")

W("**C-25 -- Gate 1 names enforcement note as named sub-requirement** (behavior)\n")
W("C-24 requires Gate 1 Check [2] to verify the VERBATIM BLOCK is present with delimiters\n")
W("and Source element. C-25 requires Check [2] to explicitly name the enforcement note\n")
W("(C-23) as a required field: VERBATIM BLOCK has delimiters + Source dimension in D-N\n")
W("format + enforcement note present. A Gate 1 Check [2] that says only VERBATIM BLOCK\n")
W("present or VERBATIM BLOCK has delimiters + Source satisfies C-24's structural\n")
W("requirement but fails C-25 if the enforcement note is not named as a required check\n")
W("target. Gate 1 then audits not only structure (C-24) but also the self-auditing\n")
W("property of the Source element (C-23). Sources: V-04 and V-05.\n\n")

W("**C-26 -- PROPOSAL SCOPE reproduced at Phase 5 enforcement point** (behavior)\n")
W("C-22 requires the violation condition to appear in the PROPOSAL SCOPE block. C-26\n")
W("requires the full PROPOSAL SCOPE block -- all three lines including the violation\n")
W("condition -- to be reproduced at the start of Phase 5 (the proposal phase), making the\n")
W("filter locally visible at the exact point where it constrains proposal selection. A single\n")
W("upfront PROPOSAL SCOPE block passes C-22 but fails C-26 if it is not reproduced at Phase\n")
W("5. A reader inspecting a specific proposal must be able to verify the filter was active at\n")
W("that phase without scrolling to an earlier section of the output. Sources: V-03 and V-05.\n\n")

W("**C-27 -- Scope gate audits violation condition presence** (behavior)\n")
W("C-22 requires the violation condition to appear in the PROPOSAL SCOPE block; C-26\n")
W("requires the full block to be reproduced at Phase 5. C-27 requires a named gate to\n")
W("explicitly confirm that the violation condition line is present in the PROPOSAL SCOPE\n")
W("block. A PROPOSAL SCOPE block with a violation condition -- even one reproduced at Phase\n")
W("5 -- passes C-22 and C-26 but fails C-27 if no gate names violation condition present\n")
W("as an explicit confirmed check. The gate audit closes the loop: the constraint is\n")
W("declared (C-22), enforced at the phase boundary (C-26), and gate-verified (C-27).\n")
W("Source: V-05's Gate 2 confirms violation condition present.\n\n")

W("**Formula**: denominator /16 -> /19. Max composite 109. Golden threshold (>= 80) unchanged.\n\n")
W("---\n\n")

W("## What changed v5 -> v6\n\n")
W("Three new aspirational criteria, all sourced from R5 variate excellence signals:\n\n")
W("**C-22 -- SCOPE violation as named output category** (behavior)\n")
W("Parallel to C-21. C-20 installs the PROPOSAL SCOPE filter; C-22 requires the block to\n")
W("name what violating it looks like: A proposal row citing any excluded namespace is a\n")
W("SCOPE violation and must be dropped. A PROPOSAL SCOPE block without the violation\n")
W("condition passes C-20 but fails C-22. Source: V-03.\n\n")
W("**C-23 -- Source enforcement note** (correctness)\n")
W("C-19 requires a D-N tagged Source element; C-23 requires the Source element to include an\n")
W("explicit enforcement note: A Source dimension field that does not match a D-N label fails\n")
W("this block. Makes C-19 self-auditable -- the test is embedded in the output, not in\n")
W("external documentation. Source: V-02.\n\n")
W("**C-24 -- Commitment-phase compound gate** (behavior)\n")
W("C-14 requires each gate to name its condition; C-24 requires Gate 1 specifically to\n")
W("validate all commitment-phase artifacts as a compound: Schema A filled + VERBATIM BLOCK\n")
W("present with delimiters and Source + SEALED BLOCK with all required fields. A Gate 1 that\n")
W("checks only Schema A filled passes C-14 but fails C-24 -- partial completion is\n")
W("invisible to a single-condition gate. Source: V-01's Gate 1 compound condition.\n\n")
W("**Formula**: denominator /13 -> /16. Max composite 106. Golden threshold (>= 80) unchanged.\n\n")
W("---\n\n")

W("## What changed v4 -> v5\n\n")
W("Three new aspirational criteria from R4 excellence signals:\n\n")
W("**C-19 -- Source-anchored verbatim quotation** (correctness)\n")
W("C-16 requires a verbatim quote; C-19 requires it tagged to a named dimension (D-N row)\n")
W("via a Source element. This makes C-13 independently auditable: a Before value can be\n")
W("cross-checked against the quote at its Source dimension without re-reading strategy.md.\n\n")
W("**C-20 -- Change-pressure proposal filter** (behavior)\n")
W("C-17 computes pressure per namespace; C-20 requires those ratings to gate Phase 5.\n")
W("HIGH-pressure namespaces listed after Schema C; proposals restricted to HIGH-pressure\n")
W("namespaces only. A skill that computes pressure and then ignores it at proposal time\n")
W("fails C-20.\n\n")
W("**C-21 -- SEAL violation as named output category** (correctness)\n")
W("C-18 requires a re-read prohibition marker; C-21 requires the marker to name the\n")
W("specific violation form in the output. A Before value containing text not present in\n")
W("Schema A at seal time is a SEAL violation and must be dropped. A reader inspecting a\n")
W("suspicious Before value needs a concrete test to apply, not just a prohibition statement.\n\n")
W("**Formula**: denominator /10 -> /13. Max composite 103. Golden threshold (>= 80) unchanged.\n\n")
W("---\n\n")

W("## What changed v3 -> v4\n\n")
W("Three new aspirational criteria, all sourced from Round 3 scorecard patterns:\n\n")
W("**C-16 -- Verbatim strategy.md quotation** (correctness)\n")
W("All four R3 variates independently required a verbatim quote for C-01 evidence. C-01\n")
W("only requires at least one concrete reference -- a paraphrase qualifies. A verbatim\n")
W("quote cannot be confabulated from memory and anchors C-13: the Before value in each\n")
W("proposal row can be checked against the quoted text without re-reading the file.\n\n")
W("**C-17 -- Per-namespace quantified inventory** (coverage)\n")
W("V-03's scout: [total] | [new] format. C-02 requires all 9 namespaces assessed\n")
W("(present/absent); C-17 requires total + new counts per row. A namespace with 15 prior\n")
W("artifacts and 0 new carries no change pressure -- you cannot see that from\n")
W("presence/absence alone. Absence becomes scout: 0 total | 0 new.\n\n")
W("**C-18 -- Re-read prohibition marker** (correctness)\n")
W("V-03's After this block is written, strategy.md may not be re-opened. C-12 proves the\n")
W("commitment was made first; C-13 proves proposals respect it; C-18 makes the constraint a\n")
W("visible output artifact so a reader can distinguish correct ordering from lucky ordering\n")
W("without auditing the execution trace.\n\n")
W("Formula update: denominator /7 -> /10.\n\n---\n\n")

W("## What changed v2 -> v3\n\n")
W("Three aspirational criteria extracted from Round 2 excellence signals:\n\n")
W("**C-13 -- Commitment reference integrity** (correctness)\n")
W("C-12 locks the commitment before the scan. C-13 requires proposals to draw their Before\n")
W("state from that lock, not from a subsequent strategy.md re-read. Source: V-02's\n")
W("[END COMMITMENT -- LOCKED] marker + V-04 reproducing the same enforcement. A skill that\n")
W("passes C-12 but fails C-13 has locked the door and then unlocked it at proposal time.\n\n")
W("**C-14 -- Self-documenting gate conditions** (behavior)\n")
W("Extends C-11. A gate that emits PASS without naming the condition checked provides no\n")
W("audit value. Source: V-01's GATE BLOCK template and V-04's\n")
W("[PHASE N GATE: PASS -- all 9 namespace rows filled] format. A gate that names its\n")
W("condition is an audit trail; an unlabeled PASS is just a label.\n\n")
W("**C-15 -- Temporal boundary attestation** (correctness)\n")
W("An explicit sentence at the commitment->scan boundary: Commitment table complete. No\n")
W("signal artifacts read yet. Source: V-03. Distinct from C-11/C-14 (a phase gate): this\n")
W("is a human-readable claim that either appears or does not, with no way to silently omit\n")
W("it.\n\n")
W("Formula update: denominator /4 -> /7.\n\n---\n\n")

W("## Essential Criteria\n\n")
W("Must all pass. If any essential criterion fails, the output is not useful\n")
W("regardless of score.\n\n")
W("| ID | Criterion | Weight | Category | Pass Condition |\n")
W("|----|-----------|--------|----------|----------------|\n")
W("| C-01 | **Read strategy.md** | essential | correctness | Output explicitly references content from the current strategy.md (dimensions, scope, sequencing, or completion criteria) -- not a generic placeholder. Reading is evidenced by at least one concrete reference to the strategy's existing structure. |\n")
W("| C-02 | **Signal inventory** | essential | coverage | Output produces a list of gathered signal artifacts for the topic, with artifact filenames and dates, drawn from simulations/. All 9 namespaces are explicitly assessed (even if absent). |\n")
W("| C-03 | **Delta detection** | essential | correctness | Output distinguishes NEW artifacts (artifact date > strategy.md last-modified date) from PRIOR artifacts. Only NEW artifacts drive change proposals. PRIOR artifacts are not used as evidence for change. The strategy date is named explicitly. |\n")
W("| C-04 | **Typed change proposals** | essential | behavior | Proposals are categorized as ADD, REMOVE, or REPRIORITIZE (not prose observations or generic updates). If no changes are warranted, the output says so explicitly -- the null is not silence. |\n")
W("| C-05 | **Confirmation gate** | essential | behavior | Output stops before modifying strategy.md and presents proposals for user review. The output contains a visible gate asking for YES/NO/REVISED confirmation. strategy.md is not modified until the user replies. |\n\n")
W("---\n\n")

W("## Recommended Criteria\n\n")
W("Output is better with these. At least 2 of 3 are needed to reach the\n")
W("golden threshold alongside all essential criteria passing.\n\n")
W("| ID | Criterion | Weight | Category | Pass Condition |\n")
W("|----|-----------|--------|----------|----------------|\n")
W("| C-06 | **Evidence citation** | recommended | depth | Each proposal row names the specific signal artifact(s) that motivate the change -- not generic reasoning. An artifact filename appears in every non-null proposal. |\n")
W("| C-07 | **Before/after diff** | recommended | format | For each proposed change, output shows the current strategy state (Before) and the proposed new state (After), making the delta concrete and auditable. A diff table or equivalent structure is present. |\n")
W("| C-08 | **Inertia justification** | recommended | depth | Each proposal addresses why NO CHANGE is not sufficient. The burden of proof rests on change, not on stability. A vs. NO CHANGE column or equivalent justification is present in every non-null proposal row. |\n\n")
W("---\n\n")

W("## Aspirational Criteria\n\n")
W("Raises the bar once essential and recommended are stable. Nineteen criteria;\n")
W("denominator /19.\n\n")
W("| ID | Criterion | Weight | Category | Pass Condition |\n")
W("|----|-----------|--------|----------|----------------|\n")
W("| C-09 | **Type-labeled null declarations** | aspirational | format | When a change type has no proposals (e.g., no removals warranted), the output declares it explicitly with a type label: REMOVALS: none -- inertia holds. Silence or a single unlabeled no changes covering multiple types does not pass. |\n")
W("| C-10 | **Conflict detection** | aspirational | depth | Output identifies any NEW artifacts that contradict each other on the same strategy dimension before finalizing proposals. A conflict audit table or equivalent is present, even if it contains no conflicts (null declared explicitly). |\n")
W("| C-11 | **Phase-gated lifecycle** | aspirational | behavior | Each stage of the skill (read, inventory, delta, propose, confirm) is structured as a named phase with an explicit PASS/STOP exit gate. Output cannot advance to the next phase without meeting the gate condition. The phase structure is visible in the output, not just in the skill definition. |\n")
W("| C-12 | **Pre-scan commitment** | aspirational | correctness | The skill registers what the current strategy says -- dimensions, scope, completion criteria -- before scanning any signal artifacts. This commitment is written down before artifact dates are examined. Prevents rationalization: the before state cannot be rewritten to match the evidence. |\n")
W("| C-13 | **Commitment reference integrity** | aspirational | correctness | Each proposal row's Before state is sourced from the locked commitment block written in the pre-scan phase. The skill explicitly prohibits re-opening strategy.md during the proposal phase. A Before value that could only have come from a re-read of strategy.md (i.e., contains content not present in the commitment block) fails this criterion. |\n")
W("| C-14 | **Self-documenting gate conditions** | aspirational | behavior | Each phase gate in the output names the specific condition that was evaluated, not just the binary PASS/STOP result. Example: [PHASE 2 GATE: PASS -- all 9 namespace rows filled with artifact counts]. A gate that emits only PASS or STOP without the condition does not pass. |\n")
W("| C-15 | **Temporal boundary attestation** | aspirational | correctness | Output contains an explicit attestation sentence at the commitment->scan boundary confirming: (1) the commitment phase is complete and (2) no signal artifacts have been examined yet. A phase gate alone (C-11/C-14) is not sufficient; the attestation must be a separate human-readable sentence, e.g., Commitment table complete. No signal artifacts read yet. |\n")
W("| C-16 | **Verbatim strategy.md quotation** | aspirational | correctness | Output contains a verbatim quoted excerpt from strategy.md in the commitment phase -- surrounded by quotation marks or in a quote block. A paraphrase or summary does not pass. The quote must appear before artifact scanning begins, not appended after the fact. |\n")
W("| C-17 | **Per-namespace quantified inventory** | aspirational | coverage | The namespace inventory includes both a total artifact count and a new-artifact count for each of the 9 namespace rows (e.g., scout: 12 total | 1 new). Absence is expressed as scout: 0 total | 0 new, not as ABSENT without counts. Presence/absence alone (C-02) does not pass. |\n")
W("| C-18 | **Re-read prohibition marker** | aspirational | correctness | Output contains an explicit statement at or after the commitment lock point that strategy.md may not be re-opened or re-read until after the user applies changes. The prohibition must be a visible output artifact, not merely an implied ordering from phase structure. A phase gate (C-11) or boundary attestation (C-15) is not sufficient; the prohibition must name the constraint directly. |\n")
W("| C-19 | **Source-anchored verbatim quotation** | aspirational | correctness | The verbatim quote from strategy.md (C-16) includes a Source element that maps the quoted sentence to a named strategy dimension (e.g., D-3: sequencing). The Source element enables cross-check of any proposal Before value against the verbatim text at the cited dimension without re-reading strategy.md. A verbatim quote without a Source element passes C-16 but fails C-19. |\n")
W("| C-20 | **Change-pressure proposal filter** | aspirational | behavior | After the namespace inventory, HIGH-pressure namespaces (those with new artifacts) are listed explicitly, and the proposal phase restricts evidence to HIGH-pressure namespaces only. A namespace with new artifacts but no HIGH-pressure designation does not contribute to proposals. A reader can independently verify proposal volume is proportionate to HIGH-pressure namespace count. Computing pressure (C-17) without gating the proposal phase on it does not pass. |\n")
W("| C-21 | **SEAL violation as named output category** | aspirational | correctness | The re-read prohibition marker (C-18) explicitly names the violation condition in the output: what specific form of Before-value content constitutes a SEAL violation (e.g., A Before value containing text not present in Schema A at seal time is a SEAL violation and must be dropped). The violation must be a named category in the output, not only a rule in skill instructions. A prohibition marker that states only may not re-open without naming the violation form passes C-18 but fails C-21. |\n")
W("| C-22 | **SCOPE violation as named output category** | aspirational | behavior | The PROPOSAL SCOPE block (C-20) explicitly names the violation condition: what constitutes a SCOPE violation (e.g., A proposal row citing any excluded namespace is a SCOPE violation and must be dropped). The violation condition must appear in the PROPOSAL SCOPE block itself, not only in skill instructions. A PROPOSAL SCOPE block that lists HIGH-pressure namespaces but does not name the violation form passes C-20 but fails C-22. Parallel structure to C-21 for the proposal-filter domain. |\n")
W("| C-23 | **Source enforcement note** | aspirational | correctness | The Source element in the verbatim quote block (C-19) carries an explicit enforcement note stating what invalidates it (e.g., A Source dimension field that does not match a D-N label fails this block). The enforcement note makes C-19 self-auditable: a reader verifying the Source element has an explicit test without knowing the expected format in advance. A D-N tagged Source element (C-19) without an enforcement note passes C-19 but fails C-23. |\n")
W("| C-24 | **Commitment-phase compound gate** | aspirational | behavior | Gate 1 (or the equivalent commitment-phase gate) validates all commitment-phase artifacts as structurally separate named check lines with a single PASS/STOP result: Check [1] Schema A filled, Check [2] VERBATIM BLOCK present with delimiters and Source element, Check [3] SEALED BLOCK present with all required fields. A Gate 1 that checks only one condition (e.g., Schema A filled) satisfies C-14 but fails C-24. The compound condition must be visible as named checks in the gate output. |\n")
W("| C-25 | **Gate 1 audits enforcement note as named sub-requirement** | aspirational | behavior | Gate 1 Check [2] (VERBATIM BLOCK verification) explicitly names the Source enforcement note (C-23) as a required field: e.g., VERBATIM BLOCK has delimiters + Source dimension in D-N format + enforcement note present. A Check [2] that verifies the VERBATIM BLOCK is present with delimiters and Source element satisfies C-24 but fails C-25 if it does not name the enforcement note as a required check target. Gate 1 then audits not only structure (C-24) but also the self-auditing property of the Source element (C-23). |\n")
W("| C-26 | **PROPOSAL SCOPE reproduced at Phase 5 enforcement point** | aspirational | behavior | The full PROPOSAL SCOPE block -- all three lines including the violation condition (C-22) -- is reproduced at the start of the proposal phase (Phase 5), making the filter locally visible at the exact point where it constrains proposal selection. A single upfront PROPOSAL SCOPE block passes C-22 but fails C-26 if it is not reproduced at Phase 5. A reader inspecting a specific proposal must be able to verify the scope filter was active at that phase without scrolling to an earlier section. |\n")
W("| C-27 | **Scope gate audits violation condition presence** | aspirational | behavior | A named gate explicitly confirms that the violation condition line is present in the PROPOSAL SCOPE block. A PROPOSAL SCOPE block with a violation condition -- even one reproduced at Phase 5 (C-26) -- passes C-22 and C-26 but fails C-27 if no gate names violation condition present as an explicit confirmed check. The gate closes the constraint loop: declared (C-22), reproduced at enforcement point (C-26), gate-verified (C-27). |\n\n")
W("---\n\n")

W("## Scoring Reference\n\n")
W("Formula: 60 + (rec_passed x 10) + asp_passed\n")
W("(when all 5 essential pass; max when essential fail: composite <= 72)\n\n")
W("| All essential pass | Recommended pass | Aspirational pass | Composite | Golden? |\n")
W("|--------------------|-----------------|-------------------|-----------|----------|\n")
W("| Yes | 3/3 | 19/19 | 109 | Yes |\n")
W("| Yes | 3/3 | 16/19 | 106 | Yes |\n")
W("| Yes | 3/3 | 13/19 | 103 | Yes |\n")
W("| Yes | 3/3 | 10/19 | 100 | Yes |\n")
W("| Yes | 3/3 | 7/19 | 97 | Yes |\n")
W("| Yes | 3/3 | 0/19 | 90 | Yes |\n")
W("| Yes | 2/3 | 19/19 | 99 | Yes |\n")
W("| Yes | 2/3 | 10/19 | 90 | Yes |\n")
W("| Yes | 2/3 | 7/19 | 87 | Yes |\n")
W("| Yes | 2/3 | 0/19 | 80 | Yes (exactly) |\n")
W("| Yes | 1/3 | 19/19 | 89 | Yes |\n")
W("| Yes | 1/3 | 10/19 | 80 | Yes (exactly) |\n")
W("| Yes | 1/3 | 7/19 | 77 | No |\n")
W("| Yes | 1/3 | 0/19 | 70 | No |\n")
W("| Yes | 0/3 | 19/19 | 79 | No |\n")
W("| No (4/5) | any | any | max 72 | No |\n\n")
W("---\n\n")

W("## Criterion Notes\n\n")
W("**C-03 (Delta detection)** is the criterion that separates a strategy\n")
W("revision from a strategy rewrite. An output that treats all signals\n")
W("equally -- ignoring the strategy date -- will propose changes based on\n")
W("evidence already incorporated when the strategy was written. This produces\n")
W("noise, not signal.\n\n")
W("**C-05 (Confirmation gate)** is non-negotiable: the skill's contract with\n")
W("the user is that strategy.md will not change without explicit confirmation.\n")
W("An output that silently applies changes or omits the gate entirely fails\n")
W("regardless of the quality of its proposals.\n\n")
W("**C-08 (Inertia justification)** distinguishes preferences from decisions.\n")
W("A proposal that cannot name a concrete consequence of leaving strategy.md\n")
W("unchanged is a preference, not a decision. The vs. NO CHANGE test is the\n")
W("core judgment required of this skill.\n\n")
W("**C-11 (Phase-gated lifecycle)** emerged from Round 1 scoring: V-03's\n")
W("named phases with PASS/STOP gates were the only mechanism that\n")
W("simultaneously enforced C-01, C-02, C-03, and C-05. A lifecycle visible\n")
W("in the output is structurally harder to skip than one embedded only in the\n")
W("skill prompt.\n\n")
W("**C-12 (Pre-scan commitment)** emerged from V-04's pre-signal defense\n")
W("register pattern. Registering strategy content before examining artifact\n")
W("dates makes the before state immutable to rationalization. An output that\n")
W("states what strategy currently says only after it has read the signals\n")
W("cannot be trusted to have read the strategy first.\n\n")
W("**C-13 (Commitment reference integrity)** closes the loop C-12 opens.\n")
W("C-12 locks the commitment before the scan; C-13 requires proposals to\n")
W("draw their Before state from that lock, not from a subsequent read.\n")
W("A skill that passes C-12 but fails C-13 has done the work of locking and\n")
W("then unlocked it anyway at proposal time.\n\n")
W("**C-14 (Self-documenting gate conditions)** extends C-11. A gate that\n")
W("emits PASS without naming the condition that was checked provides no\n")
W("audit value -- it could be a copy-paste from a template. A gate that\n")
W("names the condition (all 9 rows filled, commitment block present)\n")
W("proves the evaluation actually occurred.\n\n")
W("**C-15 (Temporal boundary attestation)** is the most direct enforcement\n")
W("of the ordering contract between C-12 and the scan phase. The attestation\n")
W("sentence -- Commitment table complete. No signal artifacts read yet. --\n")
W("is a one-line claim that either appears or does not. It cannot be\n")
W("rationalized retroactively and cannot be silently omitted without the\n")
W("absence being obvious. Note: All five R4 variations failed C-15. The SEALED\n")
W("block (C-18/C-21) covers the commitment-complete claim but omits the no\n")
W("artifacts read yet half. These are two distinct constructs.\n\n")
W("**C-16 (Verbatim strategy.md quotation)** extends C-01 from reference to\n")
W("proof. Every R3 variate independently converged on requiring a verbatim\n")
W("quote; the pattern is not a coincidence. A paraphrase is consistent with\n")
W("having read the document, but a verbatim quote cannot be produced without\n")
W("it. The quote also anchors C-13: a proposal's Before value can be\n")
W("checked against the verbatim text, making commitment reference integrity\n")
W("auditable without re-reading the file.\n\n")
W("**C-17 (Per-namespace quantified inventory)** extends C-02 from coverage\n")
W("to signal pressure. Presence/absence tells you whether a namespace has\n")
W("artifacts; counts tell you how many are new. A namespace with 15 prior\n")
W("artifacts and 0 new carries no change pressure regardless of its richness.\n")
W("Quantification enables the reader to independently assess whether the\n")
W("skill's proposal volume is proportionate to the signal volume.\n\n")
W("**C-18 (Re-read prohibition marker)** makes C-12 and C-13's temporal\n")
W("contract explicit and self-enforcing. C-12 proves ordering; C-13 proves\n")
W("proposals respect it; C-18 eliminates ambiguity by stating the constraint\n")
W("directly in the output. A skill that satisfies C-12 + C-13 but omits\n")
W("C-18 has the right behavior but leaves the reader unable to distinguish\n")
W("correct ordering from lucky ordering.\n\n")
W("**C-19 (Source-anchored verbatim quotation)** extends C-16 from proof of\n")
W("reading to dimension-level traceability. C-16 proves strategy.md was read;\n")
W("C-19 proves which dimension was read and maps the evidence to the commitment\n")
W("block. The Source element is the link that makes C-13 independently auditable:\n")
W("without it, a reviewer must know what Schema A D-N row a Before value came\n")
W("from; with it, the quote at the Source element either matches or does not.\n")
W("A delimiter-enclosed verbatim quote plus a Source element is the\n")
W("strongest achievable C-16+C-19 combination.\n\n")
W("**C-20 (Change-pressure proposal filter)** extends C-17 from inventory\n")
W("to scope enforcement. C-17 computes pressure; C-20 requires pressure to\n")
W("gate what enters Phase 5. The two-step filter -- inventory first, proposals\n")
W("only from HIGH-pressure namespaces -- decouples richness from relevance.\n")
W("A namespace with 20 prior artifacts and 0 new is rich but carries zero\n")
W("change pressure; filtering it out is the correct result, not an oversight.\n\n")
W("**C-21 (SEAL violation as named output category)** extends C-18 from\n")
W("prohibition to enforcement definition. C-18 states the constraint; C-21\n")
W("states what violating it looks like. The distinction matters because a\n")
W("reader inspecting a suspicious Before value needs a concrete test to\n")
W("apply, not just a rule that says re-reading was prohibited. Naming the\n")
W("violation form (Before value containing text not present in Schema A at\n")
W("seal time) gives any reader the rubric they need to identify violations\n")
W("without access to the execution trace.\n\n")
W("**C-22 (SCOPE violation as named output category)** mirrors C-21 for the\n")
W("proposal-filter domain. C-20 installs the filter; C-22 makes the filter\n")
W("self-enforcing by naming what a violation looks like. The parallel is\n")
W("exact: C-18 prohibits re-reading, C-21 names what re-reading produces;\n")
W("C-20 prohibits out-of-scope proposals, C-22 names what an out-of-scope\n")
W("proposal looks like. A reader who finds a proposal citing a namespace with\n")
W("zero new artifacts needs the same kind of concrete test that C-21 provides\n")
W("for Before-value traceability failures.\n\n")
W("**C-23 (Source enforcement note)** extends C-19 from D-N tagging to\n")
W("self-auditable tagging. C-19 requires the Source element to reference a\n")
W("D-N row label; C-23 requires the Source block to state what makes a Source\n")
W("element invalid. Without the enforcement note, a reader verifying a Source\n")
W("element must already know the expected format (D-N labels) to recognize a\n")
W("mismatch. With the enforcement note, the test is embedded in the output\n")
W("itself. This is the same architectural principle as C-21 and C-22: naming\n")
W("the violation condition inside the output eliminates the need to consult\n")
W("external documentation to audit the artifact.\n\n")
W("**C-24 (Commitment-phase compound gate)** extends C-14 from single-condition\n")
W("gates to full-artifact-set validation at Gate 1. C-14 requires each gate to\n")
W("name its condition; C-24 requires the commitment-phase gate to validate all\n")
W("commitment artifacts in one compound PASS/STOP. The distinction matters\n")
W("because a Gate 1 that checks only Schema A filled can PASS even when the\n")
W("VERBATIM BLOCK is absent or the SEALED BLOCK is missing fields -- partial\n")
W("completion is invisible to a single-condition gate. A compound gate closes\n")
W("this gap: it cannot PASS unless Schema A, the VERBATIM BLOCK, and the SEALED\n")
W("BLOCK all meet their structural requirements simultaneously.\n\n")
W("**C-25 (Gate 1 audits enforcement note as named sub-requirement)** extends\n")
W("C-24 from structural compound verification to content-level verification. C-24\n")
W("requires Gate 1 to check that the VERBATIM BLOCK is present with delimiters and\n")
W("Source element; C-25 requires Check [2] to name the enforcement note (C-23) as a\n")
W("required sub-field of that check. Without C-25, Gate 1 can pass a VERBATIM BLOCK\n")
W("that has a Source element but lacks the enforcement note -- a C-23 failure that\n")
W("Gate 1 does not catch. With C-25, the commitment-phase gate audits the full\n")
W("structural and self-auditing requirements of the VERBATIM BLOCK simultaneously.\n")
W("Sources: V-04 and V-05.\n\n")
W("**C-26 (PROPOSAL SCOPE reproduced at Phase 5 enforcement point)** extends C-22\n")
W("from declaration to local enforcement. C-22 ensures the violation condition exists\n")
W("somewhere in the output; C-26 ensures it is visible at the exact phase where it\n")
W("constrains behavior. The parallel to C-15 is structural: C-15 requires the temporal\n")
W("boundary attestation at the commitment->scan boundary, not just once anywhere; C-26\n")
W("requires the PROPOSAL SCOPE block at the proposal-phase boundary, not just once\n")
W("at the top. A reader auditing a specific proposal should not need to scroll\n")
W("backward to verify the filter was active. Sources: V-03 and V-05.\n\n")
W("**C-27 (Scope gate audits violation condition presence)** closes the scope\n")
W("enforcement loop that C-22 opens and C-26 extends. C-22 declares the violation\n")
W("condition; C-26 reproduces it at Phase 5; C-27 requires a gate to confirm its\n")
W("presence. The architectural pattern is the same as C-21->C-22->(gate audit):\n")
W("the constraint is named in an output block, reproduced at the enforcement point,\n")
W("and then gate-verified so that its absence from any specific run is caught rather\n")
W("than silently missing. A PROPOSAL SCOPE block that is declared, reproduced, but\n")
W("never gate-checked can be copy-pasted without the violation condition and the gap\n")
W("remains invisible until a reader audits the block manually. Source: V-05.\n")

out.close()
print('done')
