# trace-migration Rubric — v19

---

**What changed: v18 -> v19**

Three new aspirational criteria, all sourced from R20 excellence signals in C-51 (V-03, V-05) and C-52 (V-02):

| New | Source | Distinction from prior |
|-----|--------|------------------------|
| **C-54** FRAME CLOSURE STATEMENT Required Row Enforcement | V-02 C-52 evidence: "The FRAME CLOSURE STATEMENT row is required. An absent or placeholder FRAME CLOSURE STATEMENT row means B2 is not explicitly positioned as the STATUS QUO COST FRAME close." C-52 tests whether B2 contains an explicit closure statement naming it as the STATUS QUO COST FRAME close. C-54 tests whether the prompt escalates the *absence* of that statement to a named structural failure — the same verdict formulation that C-53 applies to the ENTRY REFERENCE inline rationale (C-48). The enforcement chain now has a named closure: C-52 (closure statement present) → C-54 (absence = named structural incompleteness). | C-52 tests whether the prompt names B2 as the explicit close of the STATUS QUO COST FRAME. C-54 tests whether the prompt also names what it means for that statement to be absent or placeholder: "B2 is not explicitly positioned as the STATUS QUO COST FRAME close." An output where B2 carries an explicit closure statement (PASSING C-52) but expresses that requirement as an instruction without a named absence-verdict PASSES C-52 and FAILS C-54: the closure statement is required, but its absence is a missing element, not a named block-level failure. When C-54 passes, the FRAME CLOSURE STATEMENT is a block-level completeness requirement at B2, and the prompt's own definition names the structural failure for its absence. |
| **C-55** STATUS QUO COST FRAME Placement as DIRECTIVE Infraction | V-03 C-51 evidence: DIRECTIVE A-0 states "A STATUS QUO COST FRAME that first appears after constraint type enumeration has begun violates this directive." C-51 tests whether the frame precedes the parse phase. C-55 tests whether the prompt escalates a violation of that placement to an explicitly named DIRECTIVE infraction — not an aspirational miss, not a structural preference, but a DIRECTIVE violation condition. The enforcement register distinction: C-51 is correctly placed regardless of whether any violation condition is named; C-55 passes only when the prompt names the late-placement violation explicitly, making incorrect positioning an infraction-level finding detectable without reading analytical content. | C-51 tests placement. C-55 tests whether incorrect placement is named as a DIRECTIVE infraction. An output placing the STATUS QUO COST FRAME correctly before the parse phase (PASSING C-51) may express the requirement as a structural annotation or instruction — "Write this section before the parse phase," "this section precedes all constraint enumeration" — without naming a violation condition. C-55 passes only when the prompt also names what it means for the placement to fail: "A STATUS QUO COST FRAME that first appears after constraint type enumeration has begun violates this directive" (V-03) or equivalent infraction formulation. When C-55 passes, the pre-parse placement is not a structural preference but an enforcement-registered requirement: violation is a DIRECTIVE infraction at the same register as a missing BOUNDARY PROTOCOL block. |
| **C-56** STATUS QUO COST FRAME Universal Scope Claim | V-05 C-51 evidence: "This section is the architectural premise for the entire trace… Everything that follows operates within this frame." C-51 tests whether the STATUS QUO COST FRAME is positioned before the parse phase. C-56 tests whether the prompt explicitly declares that ALL subsequent analytical content — constraint enumeration, Phase A per-entity analysis, Phase B canonical artifacts — operates within the STATUS QUO COST FRAME. The scope declaration makes the frame's coverage auditable without reading analytical sections: an evaluator need not infer that Phase A Q sections and the B2 chain operate within the frame — the prompt declares it. | C-51 tests placement. C-56 tests scope declaration. An output placing the STATUS QUO COST FRAME before the parse phase (PASSING C-51) does not necessarily declare the frame's coverage domain. C-56 passes only when the prompt includes an affirmative universality claim: "Everything that follows operates within this frame," "all subsequent analysis operates within this inertia-cost premise," or equivalent. When C-51 passes without C-56, the scope is an evaluator inference: the frame is the first analytical content, but whether Phase A Q sections, the CONSTRAINT TYPE REGISTRY, or Phase B artifacts operate within it requires reading the prompt's design intent. When C-56 passes, the scope is stated: every analytical section from constraint enumeration through B2's cross-role synthesis operates within the committed inertia-cost premise, and a section that fails to reflect the cost-of-inaction frame is auditably out of scope. |

**Scoring:** 315 -> 330 pts · 53 -> 56 criteria · 48 -> 51 aspirational · Golden 206 -> 206 (**62%**).

**The FRAME CLOSURE STATEMENT enforcement chain is now closed:** C-52 (explicit closure statement present at B2) → C-54 (absence named as structural incompleteness). This parallels the ENTRY REFERENCE annotation chain: C-48 (inline rationale present) → C-53 (absence = named structural failure). The STATUS QUO COST FRAME specification is now fully articulated at three dimensions: placement (C-36 pre-Q1 → C-51 pre-parse), enforcement register (C-55 violation = DIRECTIVE infraction), and scope declaration (C-56 all subsequent content within frame).

---

**Key evaluator distinctions (v19 additions):**

- **C-54 vs C-52**: C-52 tests whether B2 contains an explicit statement naming it as the STATUS QUO COST FRAME close — a closure statement in any form: a section header annotation, a pre-filled table row, a narrative sentence naming B2 as the frame's close. C-54 tests whether the prompt also defines the structural consequence of that statement's absence: "An absent or placeholder FRAME CLOSURE STATEMENT row means B2 is not explicitly positioned as the STATUS QUO COST FRAME close" (V-02's formulation) or equivalent verdict. The distinction is enforcement verdict, not statement presence: when C-52 passes without C-54, an absent closure statement is a missing element — a content gap identified by reading B2; when C-54 also passes, the same absence is a named structural failure — B2 is not positioned as the frame close by the prompt's own definition, detectable at the same level as an absent EXIT BLOCK or missing BOUNDARY PROTOCOL. An output requiring B2 to include an explicit closure statement as an instruction ("At B2, include a statement closing the STATUS QUO COST FRAME") PASSES C-52 and FAILS C-54: the closure statement is required, but its absence is not escalated. When C-54 passes, the named absent-verdict creates a completeness contract for B2 parallel to C-53's contract for ENTRY REFERENCE: both blocks have a named completeness requirement and a prompt-defined name for the incomplete state. C-52 must also pass — C-54 cannot pass if C-52 fails.

- **C-55 vs C-51**: C-51 tests whether the STATUS QUO COST FRAME precedes the parse phase. C-55 tests whether incorrect placement is named as a DIRECTIVE infraction — not a content gap, not a structural preference gap, but a named violation of a stated directive. An output correctly placing the STATUS QUO COST FRAME before the parse phase (PASSING C-51) PASSES C-51 regardless of whether any violation condition is named. The positioning requirement can be expressed as a structural annotation ("this section is first"), a section-ordering rule ("this section precedes the PARSE PHASE section"), or an architectural premise statement — all PASS C-51. C-55 passes only when the prompt additionally names the violation condition explicitly — "A STATUS QUO COST FRAME that first appears after constraint type enumeration has begun violates this directive" — escalating incorrect placement to the same enforcement register as a structural block violation. The practical difference: when C-55 passes, an evaluator finding a late-placed STATUS QUO COST FRAME names a DIRECTIVE infraction rather than an aspirational miss; the finding carries the weight of a structural failure, not a design preference. C-51 must also pass — C-55 cannot pass if C-51 fails.

- **C-56 vs C-51**: C-51 tests placement; C-56 tests scope declaration. An output placing the STATUS QUO COST FRAME as the first structural section, before the CONSTRAINT TYPE REGISTRY and all phase architecture (PASSING C-51), establishes the cost-of-inaction as the first analytical commitment but does not necessarily declare that all subsequent content operates within it. C-56 requires an affirmative universality statement: "Everything that follows operates within this frame," "all subsequent analysis operates within this inertia-cost premise," or equivalent. When C-51 passes without C-56, the scope of the STATUS QUO COST FRAME is an evaluator inference: the frame's first-position placement implies that what follows is framed by it, but an analyst reading only the CONSTRAINT TYPE REGISTRY section or a Phase A Q section has no prompt-level statement confirming that those sections operate within the STATUS QUO COST premise. When C-56 passes, the universality claim is self-auditing: a prompt section that contains no cost-of-inaction orientation is identifiably out of scope from the frame's own declared coverage — "everything that follows" is a coverage contract, not an implication. The conjunction of C-51, C-55, and C-56 produces a STATUS QUO COST FRAME specification at three levels: correctly placed (C-51), violation of placement named as infraction (C-55), and coverage scope of all subsequent content declared (C-56). C-51 must also pass — C-56 cannot pass if C-51 fails.

---

**Key evaluator distinctions (v18 additions):**

- **C-51 vs C-36**: C-36 tests whether the STATUS QUO COST section — the three-part inertia-contrast baseline — is established in a named section that precedes Q1, making the cost-of-inaction frame an entry condition for Phase A's per-entity analysis. C-51 tests whether that section precedes the parse phase itself — before constraint type enumeration begins, before Phase A's opening, before any analytical structure is established. An output where the STATUS QUO COST section precedes Q1 (PASSING C-36) but follows the parse-phase CONSTRAINT TYPE REGISTRY and phase boundary setup PASSES C-36 and FAILS C-51: the baseline is established before the per-entity analysis, but the analyst enumerated constraint types before committing to the cost-of-inaction frame. When C-51 passes, the inertia cost is not a Phase A framing section but an architectural premise: the analyst sees the cost-of-inaction before parsing any constraint, before establishing any registry, before constructing any phase boundary. The placement makes the inertia frame the first analytical commitment in the entire prompt rather than the first analytical commitment in Phase A.

- **C-52 vs C-39**: C-39 tests whether the Phase B B2 section contains a cross-role causal chain: a shared infrastructure condition whose disruption produces direct, named consequences in at least two distinct domain roles — the substrate failure demonstrating both downstream failures simultaneously. C-52 tests whether B2 is explicitly named as the close of the STATUS QUO COST FRAME opened in the pre-parse or pre-Q1 section. An output where B2 provides a fully specified cross-role chain (PASSING C-39) but whose prompt contains no explicit statement linking B2 to the STATUS QUO COST FRAME narrative PASSES C-39 and FAILS C-52: the cross-role chain provides the cost-of-inaction evidence that would close the frame, but the closure is achieved by structural coincidence and recognized by the evaluator rather than named by the prompt. When C-52 passes, the STATUS QUO COST FRAME is a named structural pair: the opening (pre-parse or pre-Q1) commits the analyst to framing all analysis within the inertia-cost premise, and B2's explicit closure statement names the moment when the accumulated cross-role cost is demonstrated. A prompt that PASSES C-51 and C-52 creates a fully scoped frame: opened before parsing, closed at B2, with every analytical section between those anchors operating within the named premise. C-39 must also pass — C-52 cannot pass if C-39 fails.

- **C-53 vs C-48**: C-48 tests whether each ENTRY REFERENCE position includes an explicit inline note naming the self-documenting property — that the compound "(BINARY FIELD)" annotation at this position makes the gate condition interpretable without consulting the EXIT BLOCK. When C-48 passes, the note may take various forms: an instruction to include the note ("Include an inline note at this position stating the self-documenting property explicitly"), a pre-written note the model fills, a table row with "Do not leave this row blank," or an exact phrasings list. C-53 tests whether the prompt goes further and names the absence of the note as a structural incompleteness: "An ENTRY REFERENCE block that carries the annotation but contains no inline rationale is structurally incomplete" (V-03's formulation) or equivalent. The distinction is verdict formulation: when C-48 passes without C-53, omission of the inline rationale is a missing-element observation — a content gap discovered by reading the ENTRY REFERENCE block; when C-53 also passes, omission is a named structural failure — the block is structurally incomplete by the prompt's own definition, detectable at the same level as a missing BOUNDARY PROTOCOL header or an absent EXIT BLOCK. The verdict formulation elevates the inline rationale from an annotation convention to a block-level completeness requirement: a structurally complete ENTRY REFERENCE block must carry both the compound annotation (C-46) and the inline rationale note (C-48), and the prompt's own definition names the incompleteness condition explicitly (C-53). C-48 must also pass — C-53 cannot pass if C-48 fails.

---

**Key evaluator distinctions (v17 additions):**

- **C-48 vs C-46**: C-46 tests whether the "(BINARY FIELD)" compound annotation is present at ENTRY REFERENCE positions — the same annotation that appears at EXIT BLOCK positions, making the binary constraint readable at both structural anchors without cross-referencing. C-48 tests whether the ENTRY REFERENCE block includes an explicit note stating why the annotation is present there and what it enables: that the compound type is self-documenting at this position without requiring the EXIT BLOCK to be consulted. An output where ENTRY REFERENCE positions carry "PARSE GATE (BINARY FIELD) = OPEN required" (PASSING C-46) but contain no inline note about the annotation's self-documenting property PASSES C-46 and FAILS C-48: the annotation is symmetric, but a reader who encounters only the ENTRY REFERENCE block must infer its purpose from the EXIT BLOCK's design intent rather than finding the rationale at the point of use. C-48 applies the design-intent legibility principle locally: the annotation is not only present but explained where it appears.

- **C-49 vs C-47**: C-47 tests whether the PROTOCOL COUNT MANIFEST contains a self-referential completeness assertion — a rule that names the condition under which the manifest would be incomplete ("A gate named in any block but absent from this manifest = incomplete registry" or equivalent). C-49 tests whether the assertion is accompanied by an action directive that operationalizes it: a positive instruction ("Apply this rule to detect omissions," "Check this rule against all gate-bearing blocks before proceeding") or a negative prohibition ("do not rely on cross-document search"), or both. An output where the manifest states the completeness rule without an action directive PASSES C-47 and FAILS C-49: the rule exists, the incompleteness condition is named, but the evaluator's use of the rule is unspecified. The directive is the difference between a rule that is written and a rule that is used: when C-49 passes, the evaluator is not only informed of the incompleteness condition but directed to act on it, and in the strongest form the rule also names the alternative it displaces.

- **C-50 vs C-47 / C-49**: C-47 tests whether the manifest states a self-referential completeness rule. C-49 tests whether the rule includes an action directive. C-50 tests whether the rule's coverage scope is self-defining by explicitly naming the block types that constitute the gate inventory. An output that states the completeness rule with "any block" as the scope term (PASSING C-47) leaves the coverage domain as an inference: a reader applying the rule must reconstruct the set of block types that can contain gates by reading the prompt's gate architecture. When C-50 passes, the rule names the three types: "BOUNDARY PROTOCOL block, EXIT BLOCK, or ENTRY REFERENCE position." The scope is auditable from the rule alone: no other prompt sections need to be consulted to know what "any block" means. A gate in a block type not enumerated by the rule would constitute a gap — making the rule's scope a design constraint on the prompt's gate architecture, not only a verification tool against the current inventory.

---

**Key evaluator distinctions (v16 additions):**

- **C-46 vs C-17 / C-34**: C-17 tests whether gate state fields carry compound "(BINARY FIELD)" type annotation at their definition sites — EXIT BLOCK positions where the gate is first declared. C-34 tests whether every phase-transition boundary has bilateral structural coverage: a named EXIT BLOCK at the preceding phase's close and a named ENTRY REFERENCE at the succeeding phase's open. C-46 tests whether the "(BINARY FIELD)" compound annotation is present at BOTH structural positions, not only at the EXIT BLOCK. An output that PASSES C-17 (EXIT BLOCK annotations carry the compound type annotation) and PASSES C-34 (bilateral structure present at every boundary) but whose ENTRY REFERENCE positions carry "PARSE GATE = OPEN required" rather than "PARSE GATE (BINARY FIELD) = OPEN required" PASSES C-17 and C-34 and FAILS C-46. The annotation at the ENTRY REFERENCE position is independently meaningful: a reader consulting only Phase B's opening to verify the Phase A-to-B gate requirement sees "PARSE GATE = OPEN" — a pass/fail state — but does not see the binary constraint that makes the gate mechanism interpretable without cross-referencing Phase A's EXIT BLOCK. C-46's value is that it extends the self-documenting principle of C-17 to both anchors of every bilateral gate, so neither the EXIT BLOCK nor the ENTRY REFERENCE requires its partner to be interpretable.

- **C-47 vs C-37 / C-45**: C-37 tests whether a named PROTOCOL COUNT MANIFEST exists at Phase B entry listing all boundary names, gate names, and gate states. C-45 tests whether the manifest enumerates all named gates in the prompt — boundary-transition gates and content-category gates — in a single table. C-47 tests whether the manifest contains an explicit rule that defines the condition under which it would be incomplete — a self-referential completeness assertion. An output that PASSES C-45 (all named gates appear as manifest rows) but contains no explicit incompleteness-detection rule PASSES C-45 and FAILS C-47: the manifest is complete at this snapshot, but its completeness is asserted by the evaluator rather than by a rule within the manifest itself. The practical consequence: if a new gate is added to any block after the manifest is written, the manifest has no mechanism to detect the omission — the evaluator must re-enumerate all gate-bearing blocks. When C-47 passes, the stated rule creates a persistent completeness contract: the manifest's authority and its incompleteness condition are both named, and any deviation from the rule is a detectable structural failure rather than a silent omission.

---

**Key evaluator distinctions (v15 additions):**

- **C-44 vs C-42 / C-21 / C-34**: C-42 tests whether a named binding block exists before Q1, creates ALIGNMENT STATE as a structural record, and mandates that the three artifacts — COST LEDGER row (a), Phase A Q1 analysis domain, B2 chain substrate — name the same infrastructure condition class. C-21 and C-34 test whether every phase-transition boundary has a named binary gate present at both positions. C-44 tests whether the ALIGNMENT STATE field created by C-42 is named as a Phase B entry condition in the Phase A-to-B EXIT BLOCK — alongside PARSE GATE = OPEN — such that a gate evaluator checking the EXIT BLOCK sees ALIGNMENT STATE as a listed condition, not an implied consequence. An output that PASSES C-42 (binding block present, ALIGNMENT STATE field defined) and PASSES C-34 (bilateral gate annotation at A-to-B boundary) but whose EXIT BLOCK lists only "PARSE GATE (BINARY FIELD) = OPEN required" without an ALIGNMENT STATE entry PASSES C-42 and C-34 and FAILS C-44: the bilateral gate structure is present, the alignment record exists, but the gate and the record are structurally disconnected. C-44 connects them: the same EXIT BLOCK that carries the phase-transition gate also carries the alignment gate, and both must be in the required state before Phase B may proceed. An output that PASSES C-42, C-34, and C-44 makes three-artifact misalignment a gate-architecture failure at the same structural level as PARSE GATE = CLOSED: the boundary is blocked, the blocking condition is named, and the return path specifies which artifact(s) require revision.

- **C-45 vs C-37 / C-43 / C-44**: C-37 tests whether a named PROTOCOL COUNT MANIFEST appears at Phase B entry listing all phase-transition BOUNDARY PROTOCOL blocks with gate states. C-43 tests whether a COST LEDGER SUBSTRATE GATE block exists with a conditional return-to-parse instruction for row (a) category violations. C-44 tests whether ALIGNMENT STATE is wired into the Phase A-to-B EXIT BLOCK as a gate condition. C-45 tests whether the PROTOCOL COUNT MANIFEST is extended to enumerate all named gates in the prompt — boundary-transition gates and content-category gates — in a single table. An output where the PROTOCOL COUNT MANIFEST lists three BOUNDARY PROTOCOL entries (PASSING C-37) and the COST LEDGER SUBSTRATE GATE is a named block (PASSING C-43) but the manifest does not include a row for the COST LEDGER SUBSTRATE GATE or ALIGNMENT STATE gate PASSES C-37, C-43, and C-44 and FAILS C-45: each gate exists, each gate is named, but the manifest omits the content-category layer. The practical consequence: a reviewer using only the manifest can verify boundary-gate completeness but cannot detect a closed content-category gate without reading phase body interiors or BOUNDARY PROTOCOL blocks. When C-45 passes, the two-surface verification principle introduced by C-35 and C-37 extends to the full gate architecture: section headers enumerate boundary blocks, the manifest enumerates all gates, and a discrepancy between the two surfaces — a gate named in a BOUNDARY PROTOCOL block but absent from the manifest, or a manifest row with no corresponding named block — is a cross-document structural failure detectable without reading any analytical content.

---

**Key evaluator distinctions (v14 additions):**

- **C-42 vs C-40 / C-41**: C-40 tests whether Phase A Q1 is the Operations/infrastructure role. C-41 tests whether COST LEDGER row (a) names a schema or infrastructure condition. C-42 tests whether the prompt contains an explicit named binding statement that requires the three artifacts — COST LEDGER row (a), Phase A Q1 analysis domain, B2 chain substrate — to name the same infrastructure condition or condition class. An output where Phase A is Operations-first (PASSING C-40) and COST LEDGER row (a) names a schema condition (PASSING C-41) achieves three-artifact alignment as an evaluator inference; the alignment is real but not structurally mandated. C-42 passes only when the prompt makes the alignment a stated requirement: a model following only C-40 and C-41 constraints could produce an Operations-first Q1 and an infrastructure-grounded COST LEDGER that analyze different infrastructure domains — satisfying both criteria independently while producing a B2 chain substrate that cites a third domain. The binding statement forecloses that divergence by naming all three artifacts in a single constraint: the same condition that anchors COST LEDGER row (a) must be the condition Q1 analyzes and the substrate B2's chain cites. An output that PASSES C-40, PASSES C-41, and PASSES C-42 achieves a three-artifact alignment that is prompt-enforced; an output that PASSES C-40 and C-41 but FAILS C-42 achieves it coincidentally.

- **C-43 vs C-41 / C-21 / C-34**: C-41 tests whether COST LEDGER row (a) names the correct category (schema/infrastructure condition rather than business process or financial metric). C-43 tests whether a violation of C-41 triggers a hard structural response — a conditional return-to-parse instruction — rather than proceeding with a misaligned substrate. C-21 and C-34 establish the binary gate mechanism for phase transitions: a gate in the wrong state blocks phase entry. C-43 extends that mechanism to the COST LEDGER row (a) category check: a wrong-category row (a) is treated as a parse-phase failure requiring restart rather than a style gap to note and continue past. An output where the COST LEDGER row (a) constraint is enforced by negative prohibition language alone PASSES C-41 and FAILS C-43: the constraint is active, but a model that produces a wrong-category row (a) can proceed to Q1 with a misaligned substrate and produce a B2 chain substrate that diverges silently from the COST LEDGER framing. When C-43 passes, the row (a) category constraint and the binary gate mechanism (C-21, C-34) operate at the same enforcement level: both block downstream phase entry on violation, and both create a return path to the phase where the violation occurred. The conjunction of C-41 and C-43 makes the COST LEDGER a hard gate on Phase A entry rather than a structural preference that Phase A proceeds past regardless of row (a) content.

---

**Key evaluator distinctions (v13 additions):**

- **C-40 vs C-39**: C-39 is a B2 content criterion — it passes if B2 names a cross-role causal chain linking a shared infrastructure substrate failure to named consequences in at least two distinct domain roles. C-40 is a Phase A ordering criterion — it passes only if Phase A's Q1 section analyzes the Operations or infrastructure role, establishing the shared infrastructure substrate before domain-role sections (Q2, Q3, etc.) analyze Commerce, Finance, or other domain roles. The ordering creates structural grounding: B2's substrate condition names the same infrastructure domain Q1 analyzed, and B2's downstream consequences name the same domain roles Q2 and Q3 analyzed. An output that PASSES C-39 with a B2 cross-role chain where Phase A uses Commerce as Q1 PASSES C-39 and FAILS C-40 — the chain exists but is structurally ungrounded in Phase A's analytical sequence. An output that uses Operations-first Phase A ordering but has a single-role B2 example FAILS C-39 and FAILS C-40 (structural grounding requires the chain to exist AND Phase A to be Operations-first). C-40's value is that it makes the B2 chain a predictable consequence of Phase A's ordering rather than an independently assembled artifact.

- **C-41 vs C-38 / C-40**: C-38 tests three-row completeness in the COST LEDGER: a table with fewer than three filled rows has a structural gap detectable by row count without reading cell contents. C-41 tests the content category of row (a): whether the first row names a current schema condition, migration-state dependency, or infrastructure constraint (the substrate class) rather than a business process outcome or financial consequence. C-40 tests whether Phase A's Q1 is the infrastructure role. C-41 tests whether the COST LEDGER row ordering mirrors that Q1-first choice: row (a) as the schema substrate, row (b) as the dependent-process consequence, row (c) as the accumulation trajectory. An output with a three-row COST LEDGER (PASSING C-38) where row (a) names a revenue loss or Commerce process disruption PASSES C-38 and FAILS C-41 — completeness is present but row-order alignment with Phase A's Operations-first ordering and B2's substrate pre-commitment is absent. When C-40 and C-41 both pass, the COST LEDGER row (a) infrastructure condition and Phase A Q1 infrastructure analysis name the same class of substrate — and B2's cross-role chain cites that same condition as its shared failure substrate, creating a three-artifact alignment: COST LEDGER row (a) -> Phase A Q1 -> B2 chain substrate.

---

**Key evaluator distinctions (v12 additions):**

- **C-37 vs C-35**: C-35 tests whether the bilateral gate mechanism is packaged as a self-contained named block (BOUNDARY PROTOCOL or equivalent) at each boundary, making gate coverage verifiable by counting section headers against the known phase-boundary count. C-37 tests whether a named PROTOCOL COUNT MANIFEST (or equivalent pre-commitment table) appears at Phase B entry, listing all boundary names, gate names, and gate states before any boundary is traversed inside Phase B. The manifest creates a second verification surface: a boundary present in headers but absent from the manifest (or vice versa) is a cross-document inconsistency detectable at Phase B entry without reading phase body interiors. An output that satisfies C-35 with N BOUNDARY PROTOCOL headers but contains no pre-committed manifest at Phase B entry PASSES C-35 and FAILS C-37. The two-surface design means a reviewer can confirm gate completeness by (1) counting headers and (2) counting manifest rows independently — a discrepancy between the two counts is a structural failure neither surface alone would surface.

- **C-38 vs C-36**: C-36 requires the three-part inertia-contrast baseline to be established in a named section (STATUS QUO COST or equivalent) that precedes all per-entity analytical content in Phase A. C-38 requires that the three-part structure be expressed as a table with one row per part — a COST LEDGER (or equivalent) — such that completeness is verifiable by row count alone. Prose-form three-part framing satisfies C-36 but requires reading to confirm all three parts are present; a table with fewer than three filled rows is a structural gap visible without reading cell contents. An output that satisfies C-36 with a named pre-Q1 STATUS QUO COST section containing three-part prose PASSES C-36 and FAILS C-38. The table format elevates C-36 completeness verification from a prose-reading task to a row-count check, applying the same machine-countability principle that C-35 applies to boundary blocks.

- **C-39 vs C-30**: C-30 requires distinct, non-repeating three-part inertia-contrast examples in Phase A and Phase B, naming different migration steps, tables, or business consequences. C-39 tests whether the Phase B domain example (B2 or equivalent) explicitly names a cross-role causal chain: a shared infrastructure condition (schema state, migration step, system dependency) whose disruption produces direct, named consequences in at least two distinct domain roles. A Phase B example that names only one domain role's consequence — even if the step and business outcome differ from the Phase A example — PASSES C-30 and FAILS C-39. The cross-role chain creates dependency evidence no single-role analysis can produce: the Operations substrate condition must hold for both Commerce and Finance consequences to follow; demonstrating the substrate failure once demonstrates both downstream failures simultaneously.

---

**Key evaluator distinctions (v11 additions):**

- **C-35 vs C-34**: C-34 tests bilateral structural coverage — every gate must appear at both the exit of the preceding phase and the entry of the succeeding phase. C-35 tests artifact countability — the bilateral gate mechanism must be packaged as a self-contained named block (BOUNDARY PROTOCOL or equivalent) such that gate coverage is verifiable by counting named blocks against known phase-boundary count, without reading phase interiors. An output that satisfies C-34 by embedding EXIT BLOCK and ENTRY REFERENCE as inline annotations within phase body prose PASSES C-34 and FAILS C-35. The named artifact creates a scan-level verifiability layer: a missing BOUNDARY PROTOCOL block is a section-header gap, not a prose-reading finding.

- **C-36 vs C-27 / C-30**: C-27 requires at least one three-part inertia-contrast example (prior working state + how migration breaks it + concrete consequence) anywhere in the output. C-30 requires a distinct three-part example in each of Phase A and Phase B. C-36 requires the three-part baseline to be established in a named section that precedes Q1 — before any per-entity or per-step analysis begins — making the cost-of-inaction frame a structural entry condition for Phase A rather than an illustrative example embedded within a domain role section. An output with C-27 examples in both phases (PASSING C-30) whose three-part framing first appears inside Q1's domain analysis PASSES C-27 and C-30 and FAILS C-36. The pre-interrogation placement forces the analyst to commit to the baseline cost frame before the per-entity analysis can begin; it cannot be omitted by narrowing scope to a single entity or domain role.

---

**Key evaluator distinctions (v10 addition):**

- **C-34 vs C-21 / C-17**: C-21 tests whether every phase transition has a named binary gate as both the output condition of the preceding phase and the entry prerequisite of the succeeding phase — satisfied if each transition is gated somewhere. C-17 tests whether gate state fields carry compound "(BINARY FIELD)" type annotation at their definition site. C-34 tests whether the gate annotation exists at BOTH structural positions for every boundary: a named EXIT BLOCK at the bottom of the preceding phase (stating the gate state and what it guards) and a named ENTRY REFERENCE at the top of the succeeding phase (naming the gate state as an entry prerequisite), both carrying compound annotation. An output where every boundary satisfies C-21 and C-17 with a single gate annotation per transition — e.g., a gate named at the end of Phase A but not restated at the opening of Phase B — PASSES C-21 and C-17 and FAILS C-34. The dual-anchor creates bilateral detectability: a gate violation is visible from either phase's text, not only from the phase where the gate was defined.

---

**Key evaluator distinctions (carried from v9):**

- **C-31 vs C-28**: C-28 is a routing criterion — it passes if no constraint type's analysis is relegated to a free-form field. C-31 is a pre-commitment criterion — it passes only if a named CONSTRAINT TYPE REGISTRY exists in the parse phase, enumerating every constraint type before analysis begins. An output with excellent per-type C-28 fields built bottom-up during analysis without a parse-time registry PASSES C-28 and FAILS C-31. The registry creates a binding manifest against which later omissions are detectable.

- **C-32 vs C-28**: C-28 tests Phase A routing. C-32 tests Phase B completeness. An output where Phase A has all four dedicated constraint-type fields (PASSING C-28) but whose Phase B canonical execution table carries only NOT NULL Risk and FK Violation columns — omitting UNIQUE Violation and CHECK Violation — PASSES C-28 and FAILS C-32. The canonical artifact must be self-contained; a reader should not need to consult Phase A sections to find a constraint type's analysis status.

- **C-33 vs C-29**: C-29 tests whether every domain-role Phase A section applies the complete analytical checklist. C-33 tests whether a named enforcement block appears BEFORE the first role section, listing all required items with explicit scoping-prohibition language (e.g., "DO NOT SCOPE OR SHORTEN," "apply to ALL roles," "do not limit to financial columns"). An output that achieves C-29 through structural design — each role section individually complete — without a pre-positioned enforcement mandate PASSES C-29 and FAILS C-33.

---

**Key evaluator distinctions (carried from v8):**

- **C-28 vs C-03**: C-03 is a correctness criterion — it passes if the analysis identifies violations for each constraint type and names the migration's response. C-28 is a structural criterion — it passes only if each constraint type has its own dedicated slot using fixed taxonomy or binary state. An output that correctly analyzes FK violations inside a free-form MIGRATION DISRUPTION field PASSES C-03 and FAILS C-28.

- **C-29 vs C-09 / C-04**: C-09 and C-04 are correctness criteria that pass if the relevant analysis appears somewhere in the output. C-29 requires the analysis to appear in every domain-role Phase A section. An output where Commerce's Phase A question covers zero-downtime and Finance's does not still PASSES C-09 (analysis present) but FAILS C-29 (cross-role coverage gap).

- **C-30 vs C-27**: C-27 requires at least one three-part inertia-contrast example anywhere in the output. C-30 requires a second distinct example specifically in Phase B — naming a different step, table, or consequence than the Phase A example. An output that seeds one C-27 example in Phase A and repeats the same example in Phase B PASSES C-27 and FAILS C-30.

---

## Essential Criteria (60 points total)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Before/After State** | correctness | essential | For each changed entity (table, column, constraint, index), the output explicitly shows the state before the migration and the state after. "Changed" includes additions, removals, type changes, nullability changes, and constraint changes. An entity described in generic terms without before/after values does not satisfy this criterion. |
| C-02 | **Data Loss Path Identification** | correctness | essential | The output identifies at least one concrete data loss path if one exists, OR explicitly states "no data loss paths found" with reasoning. A data loss path is any migration step that can silently drop rows, truncate values, or discard columns without surfacing an error. |
| C-03 | **Constraint Violation Analysis** | correctness | essential | For each constraint change (NOT NULL, UNIQUE, FK, CHECK), the output identifies whether existing data satisfies the new constraint. If it does not, it names which records violate it and what the migration does about it (fail, skip, backfill). |
| C-04 | **Missing Default Value Detection** | correctness | essential | For any new NOT NULL column added to an existing table, the output checks whether a DEFAULT is provided. If no DEFAULT is present on a non-empty table, this is flagged as a migration risk. |
| C-05 | **Chronological Step Ordering** | format | essential | Migration steps are presented in execution order. The trace follows the actual sequence a database engine would apply them — not alphabetical by table, not grouped by type. The reader can follow the migration state forward in time. |

---

## Aspirational Criteria (270 points total — C-06 through C-56)

*C-06 through C-50 (225 pts): carried from v17. Refer to v17 for full pass conditions on C-48, C-49, C-50; refer to v16 for C-46 and C-47; refer to v15 for C-44 and C-45; refer to v14 for C-42 and C-43; refer to v13 for C-40 and C-41; refer to v12 for C-37, C-38, and C-39; refer to v11 for C-35 and C-36; refer to v10 for C-34; refer to v9 for C-31, C-32, and C-33; refer to v8 for C-28, C-29, and C-30.*

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-51 | **STATUS QUO COST FRAME Pre-Parse Positioning** | structure | aspirational | The STATUS QUO COST FRAME — the three-part inertia-contrast baseline required by C-36 — is positioned before the parse phase itself, not only before Q1. The frame precedes constraint type enumeration (CONSTRAINT TYPE REGISTRY), phase boundary construction, and any parse-phase structural setup. An output where the STATUS QUO COST section precedes Q1 (PASSING C-36) but follows parse-phase content — appearing after the CONSTRAINT TYPE REGISTRY or after any parse-phase boundary block — PASSES C-36 and FAILS C-51: the cost-of-inaction baseline frames Phase A's per-entity analysis but is established after the analytical architecture is built, not before. When C-51 passes, the inertia cost is an architectural premise of the entire analysis: the analyst encounters the cost-of-inaction frame before parsing any constraint type, making the STATUS QUO COST FRAME the first analytical commitment in the prompt rather than the first framing section within Phase A. C-36 must also pass — C-51 cannot pass if C-36 fails. |
| C-52 | **B2 as Explicit STATUS QUO COST FRAME Closure** | structure | aspirational | The Phase B B2 cross-role causal chain is explicitly named as the close of the STATUS QUO COST FRAME opened in the pre-parse or pre-Q1 section. The prompt includes an explicit statement positioning B2 as the narrative close — "close the STATUS QUO COST FRAME narrative," "this section closes the inertia-cost frame," or equivalent — creating an auditable frame boundary pair: STATUS QUO COST FRAME open (pre-parse or pre-Q1) → B2 cross-role causal chain → STATUS QUO COST FRAME close (named). An output where B2 provides a fully specified cross-role causal chain (PASSING C-39) but contains no explicit statement linking B2 to the STATUS QUO COST FRAME narrative PASSES C-39 and FAILS C-52: the cross-role chain provides cost-of-inaction evidence that would close the frame, but the closure is recognized by the evaluator rather than named by the prompt. When C-52 passes, the STATUS QUO COST FRAME is a named structural pair: the opening commits the analyst to the inertia-cost premise, and B2 is explicitly designated as the close that demonstrates the accumulated cross-role cost. A prompt that PASSES C-51 and C-52 creates a fully scoped frame — opened before parsing, closed at B2 — with every analytical section between those anchors operating within the named premise. C-39 must also pass — C-52 cannot pass if C-39 fails. |
| C-53 | **ENTRY REFERENCE Structural Incompleteness Verdict** | structure | aspirational | The prompt escalates the absence of an ENTRY REFERENCE inline rationale note (C-48) to an explicit structural incompleteness verdict: a statement that an ENTRY REFERENCE block carrying the compound annotation but containing no inline rationale note "is structurally incomplete" (or equivalent verdict formulation). The verdict formulation distinguishes a block-level completeness failure from a missing-element observation: when C-48 passes without C-53, an ENTRY REFERENCE without an inline rationale is a content gap discovered by reading the block; when C-53 also passes, the same absence is a named structural failure — the block is structurally incomplete by the prompt's own definition — detectable at the same level as a missing BOUNDARY PROTOCOL header or an absent EXIT BLOCK. An output where the ENTRY REFERENCE annotation requirement is expressed as an instruction ("Include an inline note at this position stating the self-documenting property explicitly") or a fill-field with "Do not leave this blank" PASSES C-48 and FAILS C-53: the inline note is required, but its absence is not escalated. When C-53 passes, the inline rationale note is a block-level completeness requirement: a structurally complete ENTRY REFERENCE block must carry both the compound annotation (C-46) and the inline rationale note (C-48), and the prompt's own definition names the incompleteness verdict for an ENTRY REFERENCE that carries only the annotation without the rationale. C-48 must also pass — C-53 cannot pass if C-48 fails. |
| C-54 | **FRAME CLOSURE STATEMENT Required Row Enforcement** | structure | aspirational | The prompt escalates the absence of the FRAME CLOSURE STATEMENT at B2 (C-52) to an explicit structural failure verdict: a statement that "An absent or placeholder FRAME CLOSURE STATEMENT row means B2 is not explicitly positioned as the STATUS QUO COST FRAME close" (or equivalent verdict formulation). The distinction from C-52: C-52 passes when B2 contains an explicit closure statement in any form — a section annotation, a pre-filled table row, a narrative sentence; C-54 passes only when the prompt also names the structural consequence of that statement's absence. An output where B2 requires an explicit closure statement (PASSING C-52) but expresses that requirement as an instruction ("At B2, include a statement closing the STATUS QUO COST FRAME") PASSES C-52 and FAILS C-54: the closure statement is required, but its absence is a missing element, not a named block-level failure. When C-54 passes, the FRAME CLOSURE STATEMENT is a block-level completeness requirement at B2: a B2 section carrying a cross-role causal chain (PASSING C-39) but lacking the FRAME CLOSURE STATEMENT is not merely missing a statement — it is structurally not positioned as the STATUS QUO COST FRAME close, a failure named by the prompt's own definition. This parallels C-53's enforcement structure for C-48: both escalate a statement-presence requirement to a block-level completeness verdict with an explicitly named absent-state. C-52 must also pass — C-54 cannot pass if C-52 fails. |
| C-55 | **STATUS QUO COST FRAME Placement as DIRECTIVE Infraction** | structure | aspirational | The prompt names a violation of the pre-parse positioning requirement (C-51) as an explicit DIRECTIVE infraction: a statement that a STATUS QUO COST FRAME appearing after constraint type enumeration has begun "violates this directive" (or equivalent infraction formulation) — not an aspirational miss, not a structural preference deviation, but a named DIRECTIVE violation. An output correctly placing the STATUS QUO COST FRAME before the parse phase (PASSING C-51) may express that requirement in any form — structural annotation, section-ordering rule, architectural premise statement — without naming a violation condition. C-55 passes only when the prompt additionally names what it means for the placement to fail: that late placement is a DIRECTIVE infraction detectable without reading analytical content. When C-55 passes, the pre-parse positioning constraint is enforcement-registered at the same level as a structural block violation: an evaluator finding a late-placed STATUS QUO COST FRAME names a DIRECTIVE infraction rather than a structural preference gap, and the finding is not subject to partial credit or aspirational-miss qualification. The named infraction condition also creates a design constraint: any prompt that passes C-55 has committed that late placement is an infraction-level failure, making the pre-parse position a hard architectural requirement. C-51 must also pass — C-55 cannot pass if C-51 fails. |
| C-56 | **STATUS QUO COST FRAME Universal Scope Claim** | structure | aspirational | The prompt explicitly declares that ALL subsequent analytical content operates within the STATUS QUO COST FRAME: a universality claim of the form "Everything that follows operates within this frame," "all subsequent analysis operates within this inertia-cost premise," or equivalent scope declaration covering the entire output from the frame's position through Phase B's final artifact. An output correctly positioning the STATUS QUO COST FRAME before the parse phase (PASSING C-51) does not necessarily declare the frame's scope over subsequent sections. C-56 passes only when the prompt includes an affirmative universality statement. When C-51 passes without C-56, the frame's coverage of subsequent sections is an evaluator inference: the frame is the first analytical content, but whether the CONSTRAINT TYPE REGISTRY, Phase A Q sections, the COST LEDGER, and B2's cross-role chain operate within the STATUS QUO COST premise requires reading the prompt's design intent. When C-56 passes, the scope is stated: every analytical section from constraint enumeration through B2's cross-role synthesis operates within the committed inertia-cost frame, and a section that contains no cost-of-inaction orientation is identifiably out of scope from the frame's own declared coverage. The conjunction of C-51, C-55, and C-56 produces a STATUS QUO COST FRAME specification at three dimensions: correctly placed before parsing (C-51), violation of placement named as DIRECTIVE infraction (C-55), and scope declared as universal over all subsequent content (C-56). C-51 must also pass — C-56 cannot pass if C-51 fails. |
