---
skill: quest-rubric
skill_target: quest-rubric
date: 2026-03-15
version: 20
round: R19
changes: Added C-55 (when two aspirational criteria co-extend the same structural clause in different information-embedding dimensions — C-51+C-53 both constrain the Phase 9 PREREQUISITE sufficiency clause, C-52+C-54 both constrain the Architecture-labeled STOP consequence clause — the rubric's baseline section provides a combined-form example for each co-extension pair that satisfies both criteria simultaneously in a single clause expression, converting structural independence between paired criteria from an empirical observation to a declared rubric property, enabling a builder to produce a jointly-compliant combined form without trial-and-error) from R19 ES-R19-1/V-04/V-05; Added C-56 (each aspirational criterion requiring a specific information element to be embedded within a structural clause — C-53 requiring verbatim gate identifier, C-54 requiring absent element name — has a dedicated named verification step in the Dual Auditor Track 2 that identifies the criterion by ID and discriminates the compliant form from the non-compliant form by name, creating a second structural forcing function for form compliance independent of the instruction-level form requirement and closing the gap between instruction-level compliance and audit-verified compliance) from R19 ES-R19-2/V-05; Added C-57 (the emit manifest check includes at least one dedicated named row per form-embedding criterion — C-53, C-54 — verifying that the specific embedded form is present, converting form-requirement verification from an inline instruction to a pre-emit gate and creating a third independent verification level so compliant-form production is checkable at instruction level, Dual Auditor Track 2, and emit manifest pre-emit gate; a variation satisfying instruction-level form requirements and C-56 but omitting emit manifest form rows fails C-57) from R19 ES-R19-3/V-05
---

# Rubric: quest-rubric (v20)

Evaluates output from the `quest-rubric` skill, which produces a scoring rubric for a named
Signal skill. The rubric must function as a working objective function: each criterion must be
independently checkable, the tiers must follow the structural contract, and the essential
criteria must be grounded in the target skill's actual non-negotiable behaviors — not generic
quality observations that would apply to any skill.

**v2 additions**: Three aspirational criteria extracted from R1 excellence signals — the inertia
test (V-05), the concrete discrimination example (V-04), and phase-gate enforcement (V-01).

**v3 additions**: Five aspirational criteria extracted from R2 excellence signals — calibration
pair lifecycle position (ES-2/V-03), sub-step output sequencing (ES-1/V-03), multi-level gate
architecture (ES-3/V-03), audit role separation (ES-4/V-05), and audit trail captures reasoning
(ES-5/V-05).

**v4 additions**: Four aspirational criteria extracted from R3 excellence signals — named-block
artifact architecture (ES-R3-1/V-02/V-04), gate-blocking vs retroactive-repair distinction
(ES-R3-2/V-02), cross-criterion audit consolidation (ES-R3-3/V-03/V-05), and structural
enforcement paired with behavioral anti-deferral instruction (ES-R3-4/V-01/V-04/V-05).

**v5 additions**: Three aspirational criteria extracted from R4 excellence signals — structural
verification phase as Auditor entry gate (ES-R4-1/V-05), three-level verification chain with
non-overlapping defect classes (ES-R4-2/V-05), and anti-deferral prohibition co-located at
sub-step level rather than phase header (ES-R4-3/V-05).

**v6 additions**: Three aspirational criteria extracted from R5 excellence signals — phase-level
workflow transitions marked by named section headings (ES-R5-1), cross-criterion verification
produces a named artifact with a forward-blocking gate (ES-R5-2), and scoring formula
denominators explicitly stated and matched to current criterion count (ES-R5-3).

**v7 additions**: Two aspirational criteria extracted from R6 excellence signals — mechanism
overview declares structural output format requirement alongside behavioral requirement
(ES-R6-1), and structural verification explicitly declares its phase-bounded coverage scope and
names the deferral destination for out-of-scope heading checks (ES-R6-2).

**v8 additions**: One aspirational criterion extracted from R7 excellence signal — pre-phase
output skeleton declared from MECHANISMS alone and cross-referenced in Structural Verification
(ES-R7-1). The skeleton step serves as an executable test of C-29's textual sufficiency claim
and creates a second independent source of truth for Structural Verification: the
instruction-derived heading patterns and the builder-declared skeleton are checked jointly,
making builder-commitment failures (declared but not produced) distinguishable from
heading-pattern misses (produced but wrong format).

**v9 additions**: Three aspirational criteria extracted from R8 excellence signals — mechanism
overview declares the pre-phase skeleton step itself as a named mechanism with its heading
pattern, extending C-29's orientation-time principle to the pre-phase boundary (ES-R8-1/V-05);
Structural Verification opens with a two-source preamble naming both sources of truth and the
non-overlapping failure class each catches before the scans begin, making the two-source
architecture explicit at SV orientation rather than implicit in scan structure (ES-R8-2/V-05);
and the pre-phase skeleton step is labeled as Phase 0 (or equivalent positional phase marker)
rather than as a free-floating task step, integrating it into the C-26 scannable phase sequence
so the complete workflow from Phase 0 through Auditor Phase is verifiable by heading scan alone
(ES-R8-3/V-04/V-05).

**v10 additions**: Two aspirational criteria extracted from R9 excellence signals — competitor-
defeat annotations co-located at mechanism introduction points in the MECHANISMS section and in
the SV preamble, extending C-32/C-33's structural declaration requirement to include provenance
declaration (which competing protocol's structural gap each mechanism closes), enabling
maintainers to reason about minimality not just completeness (ES-R9-1/V-01); and the pre-phase
skeleton formatted as an operational tick-off checklist with an explicit live-construction
tracking instruction, extending C-31's declare-and-cross-reference contract to declare-track-
and-cross-reference by converting the skeleton into a production-state artifact whose item
ticks accumulate during the Author Phase, adding a third omission-detection mechanism
independent of both SV scans (ES-R9-2/V-02).

**v11 additions**: Two aspirational criteria extracted from R10 excellence signals — Competing
Implementations Preamble structured as a tabular mapping (M-NN x competing protocol x failure
class) enabling independent completeness verification by row count against the mechanism list
and independent claim verification by row content alone, extending C-35's naming requirement
from prose provenance to audit-table self-sufficiency (ES-R10-1/V-01); and format-compliance
STOP conditions co-located at the declaration step for compliance-critical format distinctions,
so that a format requirement where two visually similar formats have non-equivalent compliance
value (e.g., `- [ ]` tracked vs `- ` untracked) is gate-enforced at declaration time rather
than detected at SV time, extending C-36/C-22/C-25's enforcement pattern to format-level
requirements (ES-R10-2/V-02).

**v12 additions**: Two aspirational criteria extracted from R11 excellence signals — Competing
Implementations table gate enforcement via two co-located STOP conditions (one blocking on
completeness by row count == mechanism count, one blocking on minimality by failure-class
uniqueness), converting C-37's passive audit-enabling table into an active construction
checkpoint (ES-R11-1/V-01); and format-compliance STOP consequence statement traces the causal
chain to the specific audit mechanism defeated by the non-compliant format (non-compliant format
→ mechanism dependency → detection failure), extending C-38's proximate consequence requirement to
a three-link causal chain that makes the compliance failure legible as an audit integrity defeat
rather than a cosmetic issue (ES-R11-2/V-02).

**v13 additions**: Two aspirational criteria extracted from R12 excellence signals — named audit
roles bound to named phase spans with explicit role-to-phase ownership declarations, extending
C-17's role/phase separation requirement to phase-anchored role assignment so that role
compliance is verifiable by phase-label inspection without reading per-step instructions
(ES-R12-1/V-03/V-04); and per-STOP-instance consequence chain artifacts required as independent
named section blocks with a sequential-instance gate preventing STOP N+1 from being written
until STOP N's named block appears in the heading sequence, extending C-15/C-19's named-block
artifact independence from per-criterion level to per-STOP-instance level with a sequential
omission-detection guarantee that precedes Structural Verification (ES-R12-2/V-03/V-04).

**v14 additions**: Two aspirational criteria extracted from R13 excellence signals — emit phase
closes with a per-criterion evaluability declaration mapping each structural criterion (those
satisfying C-15/C-19) to the named artifact that enables its independent verification and the
scan method required, extending C-29's orientation-time format declaration to emit-time
criterion-to-artifact mapping so a reader navigating directly to the emit phase can identify
the verification path for each structural criterion without reading phase instructions
(ES-R13-1/V-04/V-05); and each per-criterion evaluability declaration includes an explicit
independence guarantee naming the instruction content that does NOT need to be read to verify
compliance, extending C-43's criterion-to-artifact mapping from a positive claim ("scan X to
verify Y") to a falsifiable independence claim ("scan X to verify Y — Z reading not required"),
making the independence guarantee checkable from the declaration alone (ES-R13-1/V-04/V-05).

**v15 additions**: Two aspirational criteria extracted from R14 excellence signals — emit-phase
entry condition quotes the evaluability gate identifier verbatim, making evaluability sequencing
independently auditable from Phase 9 entry alone without reading the EVALUABILITY AUDITOR role
body, extending C-41's independent-audit principle to the evaluability workflow so the phase-entry
dependency on the gate is auditable from the Phase 9 header without reading the role that
implements it (ES-R14-2/V-05); and evaluability gate and emit-phase terminal table declared as
non-redundant enforcement points where each is independently necessary, with the gate enforcing
C-44 as a pre-Phase-9 structural condition and the terminal table enforcing C-43 as an emit-close
structural requirement (ES-R14-1/V-05).

**v16 additions**: Two aspirational criteria extracted from R15 excellence signals — non-redundancy
failure-mode arguments employ Architecture-labeled experiment anchors (STOP-condition enforced)
that resolve to named competitor entries in the EVALUABILITY ARCHITECTURE COMPETITOR section,
enabling failure-mode claim verification by label-lookup without re-reading inline prose arguments,
extending C-46's positive declaration from prose argumentation to cross-referenceable label-anchor
architecture (ES-R15-2/V-05); and emit-phase verbatim gate-ID PREREQUISITE includes an explicit
auditability boundary declaration co-located with the gate-ID quote naming the role body excluded
from sequencing verification, making the PREREQUISITE both a workflow condition and a
self-declaring independence claim verifiable from Phase 9's entry header alone without reading the
gate-implementing role body, extending C-45's verbatim-quote requirement by the same pattern C-44
extends C-43: positive claim to positive claim plus independence boundary declaration (ES-R15-1/V-05).

**v17 additions**: Two aspirational criteria extracted from R16 excellence signals — Phase 9
PREREQUISITE independence boundary declaration includes a co-located positive audit-sufficiency
claim explicitly stating that reading the PREREQUISITE line alone is sufficient to confirm gate
completion ("a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE
line alone, without reading the body of ROLE: EVALUABILITY AUDITOR"), converting the independence
boundary from a negative-exclusion claim to a combined negative-exclusion plus positive-sufficiency
declaration and extending C-48 by the same pattern C-43 is extended by C-44: the named-artifact
claim is joined by a falsifiable independence claim (ES-R16-1/V-01); and each Architecture-labeled
STOP in the NON-REDUNDANCY DECLARATION explicitly names the target resolution section in the STOP
predicate (e.g., "does not resolve to a named entry in the EVALUABILITY ARCHITECTURE COMPETITOR
section"), making the STOP condition independently checkable from its text alone without reading
declaration context — a STOP that enforces the Architecture-label structure without naming the
resolution target fails C-50 even if C-47 passes, extending C-47's enforcement requirement by
requiring the STOP predicate to embed the resolution-target knowledge that a maintainer currently
must infer from surrounding context (ES-R16-2/V-02).

**v18 additions**: Two aspirational criteria extracted from R17 excellence signals — positive
audit-sufficiency claim in the Phase 9 PREREQUISITE embeds the excluded role body name within the
sufficiency clause itself (e.g., "a reader can confirm that Phase 9 was properly gated by reading
this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR"), making the
sufficiency clause a self-contained exclusion+sufficiency statement that does not require reading
the independence boundary sentence to know what is excluded, extending C-49 by the same embedding
principle C-44 applies to per-criterion evaluability declarations: the excluded content must appear
within the clause that makes the sufficiency assertion, not only in a co-located but logically
separate sentence (ES-R17-1/V-01); and each Architecture-labeled STOP consequence clause in the
NON-REDUNDANCY DECLARATION names the target declaration by its exact section heading name ("the
NON-REDUNDANCY DECLARATION is incomplete" rather than "the declaration is incomplete"), making the
consequence independently anchored without reading the enclosing section context and extending
C-50's self-sufficiency requirement from the resolution-target predicate to the consequence clause,
completing the triple-self-sufficiency of each Architecture-labeled STOP: which label is enforced,
which section the label must resolve to, and which declaration is rendered incomplete if resolution
fails (ES-R17-2/V-02).

**v19 additions**: Two aspirational criteria extracted from R18 excellence signals — positive
audit-sufficiency claim in the Phase 9 PREREQUISITE embeds the verbatim evaluability gate
identifier within the sufficiency clause itself (e.g., "a reader can confirm that EVALUABILITY
GATE is complete by reading this PREREQUISITE line alone, without reading the body of ROLE:
EVALUABILITY AUDITOR"), making the sufficiency clause a complete standalone audit certificate
naming both the specific gate completed and the excluded role body — extending C-51 by the same
embedding principle C-51 applies to C-49: just as C-51 requires the excluded role name to appear
within the sufficiency clause rather than only in the co-located independence boundary sentence,
C-53 requires the verbatim gate identifier to appear within the sufficiency clause rather than
only in the co-located PREREQUISITE gate-ID annotation, so a reader of the sufficiency clause
alone can confirm which gate was completed and what reading is excluded without consulting any
other PREREQUISITE element (ES-R18-1/V-01); and each Architecture-labeled STOP consequence
clause in the NON-REDUNDANCY DECLARATION names the specific structural element within the
affected declaration that the predicate failure leaves absent (e.g., "the Architecture-A
competitor entry in the NON-REDUNDANCY DECLARATION is absent" rather than "the NON-REDUNDANCY
DECLARATION is incomplete"), extending C-52's declaration-naming requirement from
declaration-level to element-level and completing the quadruple-self-sufficiency of each
Architecture-labeled STOP: which Architecture label, which resolution section, which named
declaration, and which specific element within the declaration is absent if resolution fails
(ES-R18-2/V-02).

**v20 additions**: Three aspirational criteria extracted from R19 excellence signals — when two
aspirational criteria co-extend the same structural clause in different information-embedding
dimensions (C-51+C-53 on the sufficiency clause, C-52+C-54 on the STOP consequence clause), the
rubric's baseline section provides a combined-form example for each co-extension pair satisfying
both criteria simultaneously in a single clause expression, converting structural independence
between paired criteria from an empirical observation to a declared rubric property so a builder
can produce the jointly-compliant combined form without trial-and-error (ES-R19-1/V-04/V-05);
each form-embedding criterion (C-53, C-54) has a dedicated named Dual Auditor Track 2 verification
step identifying the criterion by ID and discriminating compliant from non-compliant form by name,
creating a second structural forcing function for form compliance independent of the instruction-
level form requirement and closing the gap between instruction-level compliance and audit-verified
compliance (ES-R19-2/V-05); and the emit manifest check includes at least one dedicated named row
per form-embedding criterion verifying that the specific embedded form is present before emission,
creating a third independent verification level — instruction, Dual Auditor Track 2, and emit
manifest pre-emit gate — so compliant-form production is verifiable at all three levels and a
criterion absent from the emit manifest cannot slip through to emission even if instruction-level
and Dual Auditor Track 2 checks pass (ES-R19-3/V-05).

---

## Criteria

### Essential (must all pass — without these the output is not useful)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Field completeness** — Every criterion entry contains all five required fields: ID (C-NN), text with a bold label, weight (essential/recommended/aspirational), category (correctness/depth/format/coverage/behavior), and a pass condition. | format | essential | Count the fields present for each criterion. Pass if no criterion is missing any of the five fields. A criterion with "see above" or an empty pass condition is a fail. |
| C-02 | **Tier count compliance** — The rubric contains 3-5 essential criteria, 2-3 recommended criteria, and 1-2 aspirational criteria — no more, no fewer. | correctness | essential | Count criteria in each tier. Pass only if all three counts fall within the specified ranges. A rubric with 2 essential criteria or 4 recommended criteria fails immediately. |
| C-03 | **Essential criteria grounded in target skill** — Essential criteria cover behaviors that are specific and non-negotiable for the named target skill, not generic output-quality criteria that would apply to any skill. | correctness | essential | Read the essential criteria and ask: could these criteria be copy-pasted unchanged onto a rubric for a completely different skill? Pass if fewer than 2 of the essential criteria are purely generic (e.g., "output is well-formatted" with no connection to the skill contract). |
| C-04 | **Testable pass conditions** — Every pass condition names a specific, observable outcome that an evaluator can verify without subjective judgment. | correctness | essential | For each pass condition, ask: can two independent evaluators reach the same pass/fail verdict without discussion? Pass conditions containing only vague language ("good", "sufficient", "appropriate") with no concrete anchor are a fail. |
| C-05 | **Composite score formula and golden threshold present** — The rubric includes the composite scoring formula using the standard weights (essential 60, recommended 30, aspirational 10) and states the golden threshold condition. | format | essential | Check that the score section contains a formula matching the standard composite structure and a golden threshold statement. Missing formula or threshold is a fail. |

### Recommended (output is better with these)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-06 | **Category diversity** — Criteria span at least three distinct categories across the full rubric, avoiding clustering in a single dimension. | coverage | recommended | Count distinct categories used. Pass if >= 3 of the 5 canonical categories (correctness/depth/format/coverage/behavior) appear across all criteria. |
| C-07 | **Criteria ordered by severity within tiers** — Within the essential tier, criteria are listed from most structurally critical (output unusable if this fails) to least. Same ordering principle applied to recommended and aspirational tiers. | depth | recommended | Read the essential criteria in order. If swapping C-01 and C-04 would produce a more logical triage sequence, the ordering fails. Pass if the first essential criterion represents the single highest-stakes failure mode for the target skill. |
| C-08 | **Quick checklist present** — The rubric includes a compact checklist of all criteria IDs and short labels (one line per criterion), enabling rapid manual scoring without re-reading the full criterion text. | format | recommended | A quick checklist section exists. It contains one entry per criterion with the criterion ID and a short recognizable label. Pass if a scorer using only the checklist could locate and score the corresponding full criterion without searching. |

### Aspirational (raise the bar once essential/recommended are stable)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-09 | **Criteria enable iteration** — When a criterion fails, its pass condition gives enough specificity that the output author knows exactly what to fix without running the skill again to ask. Each criterion's failure mode is self-describing. | behavior | aspirational | For each criterion, ask: if this criterion failed, does the pass condition text tell the author the minimum change needed to achieve a pass? Pass if >= 80% of criteria have self-describing failure modes (not just "does not pass"). |
| C-10 | **No redundancy or contradiction** — No two criteria overlap such that passing one guarantees passing the other, and no two criteria impose logically contradictory requirements on the output. | depth | aspirational | For each pair of criteria, ask: is there a rubric output that passes one but fails the other for a non-trivial reason? If two criteria always agree, they are redundant and one should be folded in. Pass if all criteria add unique signal with no fully-overlapping pair. |
| C-11 | **Discriminating pass conditions** — Each pass condition tests a behavior that a generic rubric ("the output is clear and comprehensive") would fail to catch. Distinct from C-04 (observability): a condition can be observable but still redundant with a vague rubric if it checks only structural presence rather than skill-specific contract fulfillment. | correctness | aspirational | For each pass condition, ask the inertia test: "Could a rubric that says 'the output is clear and comprehensive' still pass this condition?" If yes for any pass condition, that condition has not been sharpened enough. Pass if every pass condition names at least one count, named field, structural pattern, or enumeration that only the target skill's contract could supply — not shared structural minimums. |
| C-12 | **Calibration example present** — The rubric includes at least one concrete positive/negative pair showing the boundary between acceptable and failing output for the most critical essential criterion. The pair must name the skill — e.g., "generic: 'output is low quality' / grounded: 'the output has no formula so quest-golden cannot compute a composite score'" — so a new evaluator can self-calibrate without a training session. | behavior | aspirational | A calibration example block exists containing one labeled generic example (shows what NOT to write) and one labeled grounded example (shows what to write) for at least one essential criterion. Both examples must reference the target skill by name or reference its output contract. Pass if a reader unfamiliar with the rubric could use the pair to correctly classify two additional borderline cases. |
| C-13 | **Phase-gate enforcement present** — The rubric's structure includes at least one hard gate that blocks forward progress until a structural requirement is verified. A gate must be explicit (e.g., "DO NOT proceed to the next step until...") and must reference a checkable condition, not an aspiration. | behavior | aspirational | A gate statement is present using explicit blocking language ("DO NOT proceed", "stop here", "must complete before advancing"). The gate condition is checkable: it names a count, a field, or a structural property, not a quality judgment. Pass if violating the gate would result in a detectably incomplete output (the gated step is one that earlier steps feed). |
| C-14 | **Calibration pair produced at point of drafting** — The calibration pair required by C-12 is written while the pass condition is being drafted, not retrospectively after all criteria are sealed. Retrospective calibration pairs lose context and risk being rationalized from the final condition rather than derived alongside it. | behavior | aspirational | The rubric's workflow positions the calibration pair as a step immediately following pass-condition drafting for each criterion, before the criterion row is sealed. A workflow that permits all calibration pairs to be written at the end, after all criteria rows are finalized, does not pass. The ordering must be: draft pass condition → write calibration pair → seal criterion — not: draft all pass conditions → seal all criteria → write all calibration pairs. |
| C-15 | **Sub-step output sequencing makes omission detectable** — When the rubric requires checking mechanisms (inertia test, calibration pair), each mechanism's output appears as an independent artifact in the output stream before the criterion is recorded in the summary table. A builder who skips a check produces a table row with no preceding artifact — a structural gap detectable by inspection, not a quality judgment. | behavior | aspirational | For each mandatory check step (e.g., inertia test, calibration pair), the rubric's instructions require that step's output to appear in the output before the criterion is entered into the table (not embedded in the table cell or omitted silently). A rubric where all checking can be done mentally with only the final table as output does not satisfy this criterion. Pass if a reviewer scanning the output would see a detectable structural gap — not just a short table cell — wherever a required check was skipped. |
| C-16 | **Multi-level gate architecture** — The rubric enforces gating at two distinct granularities: a phase gate ensuring criterion-building begins from a sufficient structural foundation, and at least one per-criterion gate ensuring each criterion is complete before the next begins. A single gate at either level alone leaves the other level unprotected. | behavior | aspirational | Identify the phase gate (a condition that must pass before any criterion-building begins) and at least one per-criterion gate (a condition that must pass before moving from one criterion to the next). Both must use explicit blocking language. Pass if the rubric has both levels of gating and neither relies solely on the author's self-discipline to enforce. |
| C-17 | **Audit role or phase separation** — The rubric's workflow separates the criterion-authoring concern (achieving coverage: do the criteria address the right behaviors?) from the criterion-auditing concern (achieving specificity: are the pass conditions discriminating?). When both concerns are addressed in a single pass, the author optimizes for completion rather than for quality of each concern. | depth | aspirational | The rubric's instructions include two structurally distinct phases or roles: one focused on criterion construction (ensuring all required behaviors are covered) and one focused on criterion quality verification (ensuring each pass condition is discriminating, non-redundant, and calibrated). A rubric where both concerns are addressed in a single pass through the criteria table does not satisfy this criterion. Pass if the workflow explicitly sequences authoring before auditing, with a gate or role boundary between them. |
| C-18 | **Audit trail captures reasoning, not only verdicts** — The audit phase produces a structured artifact (table or equivalent) recording not just whether each pass condition passed the inertia test, but which specific skill-contract element made it discriminating. Future rubric maintainers can trace which part of the skill's contract grounds each criterion, making the grounding visible without re-running the evaluation. | behavior | aspirational | The audit artifact (inertia-test table or equivalent) includes a column or field for each criterion recording the discriminating element — the specific skill-contract behavior, count, named field, or structural pattern that grounds the pass condition. An audit table with only "PASS/FAIL" columns does not satisfy this criterion. Pass if a maintainer reading the audit artifact could identify which part of the target skill's contract would need to change before each criterion became invalid. |
| C-19 | **Named-block artifact architecture** — Mandatory check outputs (inertia test records, calibration pairs) are required as named markdown section blocks (e.g., `### INERTIA-RECORD [C-NN]`, `### CALIBRATION-PAIR [C-NN]`) rather than inline prose within the drafting flow. Named blocks create heading-scan detectability: a missing block corresponds to a missing heading, verifiable without reading content. | format | aspirational | The rubric's instructions require at least two categories of check outputs (e.g., inertia-test records and calibration pairs) to appear as named markdown section headings — not as inline paragraphs or table cells. Pass if a reviewer could detect a missing check artifact by scanning section headings alone, without reading the content of any section. A rubric where all check outputs are embedded as text within a single drafting flow does not satisfy this criterion. |
| C-20 | **Gate must block forward progress, not repair backward omissions** — A retroactive repair step ("if the block is missing, write it now") is not a gate. Gates prevent a builder from proceeding until a condition is satisfied; repair steps detect a past omission after criterion rows are already sealed. | behavior | aspirational | For each gate in the rubric, verify that it is positioned before the gated work (not after it). A gate that says "if you have not yet written the calibration pair, write it now before continuing" is a retroactive repair masquerading as a gate: the criterion row may already have been sealed. Pass if every gate is positioned at the entry of the step it protects, with explicit blocking language that prevents the protected step from beginning rather than detecting its omission. |
| C-21 | **Audit table consolidates all criteria into a single cross-criterion view** — The audit phase produces a single table where each row corresponds to one criterion, enabling a maintainer to scan all criteria simultaneously and detect systemic specificity failures (e.g., every criterion anchors to the same skill-contract element, leaving other elements ungrounded). Cross-criterion visibility is not achievable by reading per-criterion records sequentially. | depth | aspirational | The audit phase output is structured as a single table where rows correspond to criteria (not to individual inertia-test records scattered through the output). The table must include at least: criterion ID, pass/fail verdict, and the discriminating element (per C-18). Pass if a maintainer reviewing the table could identify a systemic pattern (e.g., all criteria ground in output format, none in behavioral contract) without reading any other output section. |
| C-22 | **Structural sequencing enforced by both ordering constraint and explicit anti-deferral instruction** — Structural ordering constraints (e.g., requiring named blocks in sequence: CALIBRATION-PAIR before CRITERION) are necessary but not sufficient to guarantee per-criterion production. A builder who understands the sequence can still defer all calibration pairs to the end by producing empty placeholder headings. An explicit behavioral instruction paired with the structural constraint closes this gap. | behavior | aspirational | The rubric includes both: (a) a structural ordering constraint (block X must appear before block Y in the output) and (b) an explicit behavioral instruction prohibiting deferral (e.g., "produce this block now, before sealing the criterion row — do not defer to end of output"). A rubric with structural ordering alone, or with a behavioral instruction absent from the operative step, does not satisfy this criterion. Pass if the two mechanisms are co-located at the operative sub-step (not split between a phase header and a sub-step body). |
| C-23 | **Structural verification phase gates Auditor entry** — C-20 requires per-criterion gates to block forward progress rather than repair backward omissions. C-23 extends this principle one level up: the structural heading-scan (or equivalent structural completeness check) must itself be a hard gate that blocks Auditor Phase entry. A structural scan that allows the Auditor phase to begin despite detected heading-pattern violations is a detection step, not a gate. | behavior | aspirational | The Structural Verification step includes explicit blocking language that prevents the Auditor Phase from beginning until all structural checks pass. Language such as "if any scan fails, do not proceed to the Auditor Phase" is required. A structural scan that reports findings as observations — allowing the Auditor Phase to proceed regardless of result — does not satisfy this criterion. Pass if violating the structural check produces a hard stop that the workflow cannot route around without ignoring the gate language. |
| C-24 | **Three-level verification chain covers non-overlapping defect classes** — C-16 requires two gate levels (phase and per-criterion). C-24 requires the full verification architecture to cover three distinct defect classes in sequence, each addressed by a mechanism the prior level cannot handle: (1) per-criterion omission prevention at write time, (2) structural pattern bypass detection post-phase, and (3) cross-criterion specificity analysis at audit time. | depth | aspirational | Identify the three verification mechanisms and the defect class each addresses. The defect classes must be non-overlapping: a defect caught by mechanism 2 must be structurally undetectable by mechanism 1 (because it requires a completed phase to scan), and a defect caught by mechanism 3 must require cross-criterion comparison (not per-criterion checking). Pass if the three mechanisms address: (1) builder skipping a required check mid-criterion, (2) builder completing all per-criterion checks but producing the wrong structural pattern, and (3) builder producing correct patterns but with redundant or non-discriminating pass conditions across criteria. |
| C-25 | **Anti-deferral prohibition co-located at the operative sub-step, not only in phase header** — C-22 requires an explicit anti-deferral behavioral instruction paired with structural sequencing. C-25 specifies where that instruction must appear: inside the sub-step body it governs (e.g., directly inside the calibration-pair sub-step instruction), not only in a phase preamble or section header that a builder could skip while following the sub-steps. | behavior | aspirational | Locate the anti-deferral instruction required by C-22. Verify that it appears inside the sub-step body where the gated action is performed (not only in a phase header or preamble above the sub-step). A rubric where the anti-deferral instruction appears only in a phase introduction paragraph — with no restatement inside the operative sub-step instruction — does not satisfy this criterion. Pass if a builder following only the sub-step instructions (skipping all preamble text) would still encounter the anti-deferral prohibition at the point where deferral is possible. |
| C-26 | **Phase-level workflow transitions marked by named section headings** — C-19 requires per-criterion artifact outputs to appear as named markdown section blocks, making their absence detectable by heading scan without reading content. C-26 extends this principle one level up: each workflow phase transition is marked by a named section heading (e.g., `### AUTHOR PHASE`, `### STRUCTURAL VERIFICATION`, `### AUDITOR PHASE`), making the position of structural verification between Author and Auditor phases verifiable by heading scan. | format | aspirational | The workflow's phase transitions are marked by named section headings that appear in the output. A reviewer scanning only section headings (not reading prose) can confirm: (a) the Author Phase precedes Structural Verification, (b) Structural Verification precedes the Auditor Phase, and (c) the Auditor Phase is the final phase. A workflow where phase transitions are conveyed only through numbered steps or prose instructions — with no heading-level markers in the output — does not satisfy this criterion. Pass if the phase sequence is verifiable by heading scan without reading any instruction or prose content. |
| C-27 | **Cross-criterion verification produces a named artifact with a forward-blocking gate** — C-15 requires each mandatory per-criterion check to emit an independent output artifact before the criterion is sealed, making omission detectable by structural gap. C-19 requires those artifacts to be named markdown section blocks. C-27 extends both to cross-criterion verification: any mechanism that checks properties across multiple criteria simultaneously (e.g., redundancy check, category coverage check) must produce a named section block and carry a forward-blocking gate that prevents subsequent steps from beginning until the cross-criterion check is complete. | behavior | aspirational | The rubric includes at least one cross-criterion verification mechanism (operating on the set of criteria together, not on individual criteria). This mechanism produces a named section block (e.g., `### REDUNDANCY-CHECK-TABLE`) and carries explicit blocking language preventing forward progress (e.g., "DO NOT proceed to Structural Verification until this table is complete and all pairs are confirmed non-redundant"). A cross-criterion check that produces only a prose summary embedded in a phase header, without a named block and a gate, does not satisfy this criterion. Pass if the cross-criterion check artifact is detectable by heading scan and the gate is explicit. |
| C-28 | **Scoring formula denominators explicitly annotated with the criterion ID range they cover** — The scoring formula uses a fixed denominator for each tier (5 for essential, 3 for recommended, N for aspirational). As aspirational criteria are added across rubric versions, the aspirational denominator changes. Without an annotation, a scorer must count the aspirational criteria in the table to verify the denominator is current — a manual step that is skipped in practice, allowing stale denominators to persist undetected. | format | aspirational | The scoring formula in the rubric includes an inline comment or annotation (e.g., `/* C-09 through C-54 */`) stating the criterion ID range covered by the aspirational denominator. The annotation must be co-located with the formula (not in a separate note), so a scorer verifying the denominator can do so by reading the formula line alone. A formula with a numeric denominator and no ID-range annotation does not satisfy this criterion. Pass if a scorer could verify the aspirational denominator's correctness without counting rows in the criteria table. |
| C-29 | **Mechanism overview declares structural output format requirement alongside behavioral requirement** — C-27 requires cross-criterion verification mechanisms to produce named section blocks; C-19 requires per-criterion check mechanisms to produce named section blocks. Both criteria govern what must be produced. C-29 requires the mechanism overview (or equivalent orientation section) to declare the structural format (named `###` heading) each mechanism will produce, so that a builder reading the orientation section can derive the complete expected heading structure before any phase begins. Without this declaration, the structural contract is discoverable only by reading the operative phase instructions — the builder cannot build an output skeleton from the orientation section alone. | behavior | aspirational | The rubric's MECHANISMS section (or equivalent named orientation block) lists each mechanism governed by C-19 and C-27 and names: (a) the mechanism's function (what it checks), and (b) the structural output format it produces (the named `###` heading it will emit, e.g., `### INERTIA-RECORD [C-NN]`). A MECHANISMS section that lists mechanism names and functions without naming the output heading pattern does not satisfy this criterion. Pass if a builder reading only the MECHANISMS section could construct the complete expected section heading sequence for a full rubric output, without reading any phase-level instructions. |
| C-30 | **Structural Verification explicitly declares its phase-bounded coverage scope and names the deferral destination for out-of-scope checks** — C-24's three-level verification chain assigns each level a distinct defect class. C-23 requires Structural Verification to gate Auditor entry. Neither criterion requires SV to declare what it does not check. Without an explicit scope declaration, a builder using SV may believe it verifies heading patterns from all phases — including Auditor Phase headings — when it covers only Author Phase output. This silence creates a false sense of completeness: heading-pattern errors in the Auditor Phase output are not caught until a re-run, if at all. | behavior | aspirational | The Structural Verification step's instructions include an explicit scope statement identifying: (a) the phase whose output it covers (Author Phase only, or equivalent), (b) at least one named category of heading check that is out of scope (e.g., Auditor Phase heading patterns), and (c) the mechanism or phase where the deferred check will be performed. A Structural Verification step with no scope declaration — leaving coverage extent implicit — does not satisfy this criterion. Pass if a builder reading the SV scope statement could enumerate both what SV checks and what it explicitly defers, with the deferral destination named. |
| C-31 | **Pre-phase output skeleton declared from MECHANISMS alone and cross-referenced in Structural Verification** — C-29 requires the mechanism overview to declare the structural format (named headings) each mechanism will produce, making the structural contract visible at orientation time. C-31 extends that declared contract into an executable artifact: the workflow includes a pre-phase step where the builder constructs the complete heading skeleton of the expected output from the mechanism overview alone — before any phase begins. This skeleton step serves as an executable test of C-29's textual sufficiency claim: if the mechanism overview omits a heading pattern, the builder cannot include it in the skeleton, and the gap appears at skeleton time rather than at output time. The skeleton also creates a second independent source of truth alongside the instruction-derived heading patterns: Structural Verification cross-references the skeleton against actual output, catching headings the builder declared but failed to produce — a builder-commitment failure structurally distinct from a heading-pattern miss (which SV's instruction-based scan catches). Without the skeleton cross-reference, SV catches only one class of failure; with it, SV catches both. Distinct from C-29 (which requires the mechanism overview to be sufficiently complete to support skeleton derivation): C-31 requires the skeleton derivation step itself to exist in the workflow and be integrated into SV's cross-reference check. | behavior | aspirational | The rubric's workflow includes a named pre-phase step (positioned before the Author Phase begins) that instructs the builder to construct the complete output heading skeleton from the MECHANISMS section alone. Structural Verification cross-references this builder-declared skeleton against actual output in a dedicated scan step, distinct from the instruction-derived heading-pattern scan. A workflow where Structural Verification checks only the instruction-derived heading patterns — with no reference to a pre-declared skeleton — does not satisfy this criterion. Pass if both (a) the pre-phase skeleton step exists with an instruction to derive the heading list from MECHANISMS alone, and (b) Structural Verification includes a dedicated scan step that compares actual output against the builder-declared skeleton (not just against the instruction-derived patterns). |
| C-32 | **Mechanism overview declares the pre-phase skeleton step as a named mechanism with its heading pattern** — C-29 requires the mechanism overview to declare the structural output format (named `###` heading) for every mechanism governed by C-19 and C-27. C-31 requires the workflow to include a pre-phase skeleton step that exercises the declared formats. C-32 extends C-29 to cover the skeleton step itself: the mechanism overview must include an entry for the pre-phase skeleton step naming its section heading (e.g., `### PHASE 0 — OUTPUT SKELETON` or `### OUTPUT SKELETON`) and describing its role, so that a builder reading only the orientation section can derive the complete output heading skeleton — including the pre-phase section — before any phase begins. Without this entry, the skeleton step's heading is discoverable only by reading into the phase instructions. A mechanism overview that lists inertia-record, calibration-pair, criterion, and redundancy-check mechanisms while omitting the skeleton step leaves the pre-phase heading invisible at orientation time: the builder must read ahead to discover that a `### OUTPUT SKELETON` heading exists, which is the very dependency C-29 was designed to eliminate. With this entry, the mechanism overview becomes a complete description of all heading-producing steps including the pre-phase boundary, and the skeleton derivable from the overview covers the full output. Distinct from C-29 (which extends orientation-time declaration to per-phase and cross-phase mechanisms): C-32 closes the remaining orientation gap by including the pre-phase skeleton step in the mechanism overview's structural declaration. Distinct from C-31 (which requires the skeleton step to exist and be cross-referenced): C-32 requires the skeleton step to be declared in the mechanism overview before any builder reaches the operative step. | behavior | aspirational | The rubric's mechanism overview (or equivalent opening orientation section) contains an entry for the pre-phase skeleton step naming: (a) its section heading pattern (e.g., `### PHASE 0 — OUTPUT SKELETON` or `### OUTPUT SKELETON`), and (b) its role in the workflow (e.g., "constructs the complete heading skeleton from the mechanism overview alone before any phase begins"). A mechanism overview that lists only the inertia-record, calibration-pair, criterion, and redundancy-check mechanisms — without an entry for the skeleton step — does not satisfy this criterion, even if the skeleton step is fully specified in its operative phase-instruction section. Pass if a builder reading only the mechanism overview — without reading any phase instructions — could include the pre-phase skeleton heading in their output structure plan and understand that the skeleton must be derivable from the overview alone. |
| C-33 | **Structural Verification opens with a two-source preamble naming both sources of truth and the non-overlapping failure classes before the scans begin** — C-31 requires Structural Verification to cross-reference both the instruction-derived heading patterns and the builder-declared skeleton when checking structural completeness. Cross-referencing two sources in the scan steps (Scan A, Scan B) satisfies C-31's mechanical requirement but leaves the two-source architecture implicit: a builder reading SV for the first time learns that two sources exist by encountering the two scans, not from an architectural declaration. C-33 requires SV to open with an explicit preamble that (1) names both sources of truth, (2) names the distinct failure class each source catches — heading-pattern miss (instruction scan, Scan A) and builder-commitment failure (skeleton comparison, Scan B) — and (3) states that these failure classes are non-overlapping, positioned before any scan step begins. This preamble makes the two-source architecture a declared design principle, not a structural inference from scan count. Distinct from C-30 (which requires SV to declare its phase-bounded coverage scope and deferral destinations): C-33 requires SV to declare its internal two-source architecture and failure taxonomy before executing the scans, not only to bound its external scope relative to other phases. Distinct from C-31 (which requires the two-source cross-reference to exist): C-33 requires the architecture behind that cross-reference to be stated as an orientation principle, not inferred from the scan structure. | behavior | aspirational | The Structural Verification step's instructions include a preamble (positioned before any scan step or scan-heading) that explicitly states: (a) the names of both sources of truth being checked (instruction-derived heading patterns and builder-declared skeleton, or equivalent labels), (b) the distinct failure class each source catches (e.g., "heading-pattern miss" for the instruction scan, "builder-commitment failure" for the skeleton comparison), and (c) that these failure classes are non-overlapping (a failure caught by one source cannot be caught by the other). A Structural Verification step whose first content is a scan step or scan heading — with no architectural preamble — does not satisfy this criterion, even if both scans are present and correctly cross-reference both sources. Pass if a builder reading only the SV preamble — without reading the scan step instructions — could state why two sources are needed, which failure class each catches, and why collapsing to a single scan would leave one failure class undetected. |
| C-34 | **Pre-phase skeleton step labeled as Phase 0 (or equivalent positional phase marker), integrating it into the scannable phase sequence** — C-26 requires each workflow phase transition to be marked by a named section heading, making the position of Structural Verification between Author and Auditor phases verifiable by heading scan. C-31 requires a named pre-phase skeleton step. The combination of these two criteria is necessary but not sufficient to integrate the skeleton step into the C-26 phase sequence: a skeleton step named `### OUTPUT SKELETON` is a visible named block but is not positionally anchored as a phase. A skeleton step labeled `### PHASE 0 — OUTPUT SKELETON` (or equivalent: `### PRE-PHASE — OUTPUT SKELETON`) is positionally anchored as a phase: the complete workflow sequence Phase 0 → Author Phase → Structural Verification → Auditor Phase is readable from headings alone. This matters because C-26's scan-verifiability guarantee applies to phases: a reviewer confirming that structural verification appears between Author and Auditor phases by heading scan alone cannot simultaneously confirm that the skeleton step is a phase rather than a sub-step without reading content. Phase-0 labeling extends C-26's scan-verifiability guarantee to cover the full workflow including the pre-phase boundary. Distinct from C-31 (which requires the skeleton to exist and be cross-referenced at SV time): C-34 requires the skeleton step's heading to be phase-labeled, integrating it into the C-26 scannable phase sequence. Distinct from C-26 (which requires phase transitions to be marked by named headings): C-34 requires the pre-phase skeleton step specifically to use a phase-positional label, so the heading scan produces a complete four-phase sequence rather than three phases plus a floating task block. | format | aspirational | The pre-phase skeleton step (required by C-31) uses a section heading that includes a positional phase label (e.g., `### PHASE 0 — OUTPUT SKELETON`, `### PRE-PHASE: OUTPUT SKELETON`, or equivalent positional marker). A skeleton step with a heading such as `### OUTPUT SKELETON` — lacking any positional phase indicator — does not satisfy this criterion, even if it is positioned correctly in the output stream. Pass if a reviewer scanning only section headings would see the skeleton step as a phase in the phase sequence (Phase 0 or equivalent) rather than as a floating task block. |
| C-35 | **Competitor-defeat annotations co-located at mechanism introduction points and in the Structural Verification preamble** — C-32 requires the mechanism overview to declare each mechanism's heading pattern and role. C-33 requires the SV preamble to declare both sources of truth and their non-overlapping failure classes. Both criteria require structural completeness declarations — they do not require provenance declarations. C-35 extends both: each MECHANISMS entry includes a named annotation identifying which structural gap in a known competing protocol the mechanism closes; and the SV preamble names at least one competing SV design by label and identifies the failure class that alternative would miss. Without provenance annotations, the mechanism set satisfies completeness (C-32) but is silent on minimality: a maintainer cannot assess whether removing a mechanism collapses a non-overlapping failure class that a named alternative also catches, or merely removes redundant coverage. This is architecturally distinct from C-11 (which requires pass conditions to fail the inertia test) and C-10 (which requires no redundancy between criteria): C-35 requires the rubric itself to carry the provenance argument that justifies its own mechanism set, making the design auditable without re-deriving the competitor landscape from scratch. | depth | aspirational | (a) Each entry in the MECHANISMS section (or equivalent orientation block) includes a statement — either inline or as a labeled annotation — identifying the competing protocol or design approach whose structural gap this mechanism closes. Entries for mechanisms with no named competing design may omit this annotation only if the entry states that no competing design omits this mechanism. (b) The Structural Verification preamble (required by C-33) names at least one competing SV design by label (e.g., "Single-Source SV Protocol") and identifies the failure class that alternative would miss. Pass if a maintainer reading the MECHANISMS section and SV preamble could state which competing designs were considered and why each mechanism is necessary given those alternatives. |
| C-36 | **Pre-phase skeleton formatted as operational tick-off checklist with live-construction tracking instruction** — C-31 requires the pre-phase skeleton step to (a) construct the complete expected heading structure from the mechanism overview alone, and (b) be cross-referenced in Structural Verification — creating two independent sources of truth (instructions and skeleton) that SV checks jointly. C-36 extends C-31 by requiring the skeleton to function as a live production-tracking artifact during the Author Phase, not only as a pre-construction declaration: the skeleton is formatted as a `- [ ]` checklist where each item corresponds to a required output heading, and the skeleton step includes an explicit instruction to tick each item as the corresponding output block is produced during the Author Phase. This creates a third failure-detection mechanism operating at Author Phase time rather than SV time: a builder who silently omits a heading leaves the corresponding item unticked in the skeleton checklist — a visible omission record that precedes SV and is independent of both SV scan sources. Distinct from C-31 (which requires the skeleton to exist and be cross-referenced at SV time): C-36 requires the skeleton to be instrumentalized as a live tracker that accumulates evidence of builder-commitment fidelity during construction, not only as a pre-phase declaration that SV audits post-construction. Distinct from C-34 (which requires the skeleton step to be phase-labeled): C-36 requires the skeleton's content format to convert structural declaration into production-state tracking. | behavior | aspirational | The pre-phase skeleton step (a) formats the expected heading list as a `- [ ]` checklist (one checkbox per required heading, not a plain bullet or numbered list), and (b) includes an explicit instruction directing the builder to tick each checkbox as the corresponding output block is produced during the Author Phase (not retrospectively as a review step after all blocks are produced). A skeleton formatted as a `- ` plain list without tracking checkboxes does not satisfy condition (a). A skeleton using `- [ ]` format but with a tick instruction only at SV time (not during Author Phase construction) does not satisfy condition (b). Pass if a builder following the skeleton step instructions would have a production-state tick record at each point during Author Phase construction — not only a completed vs. not-completed binary at SV time. |
| C-37 | **Competing Implementations Preamble structured as a tabular mapping enabling independent completeness and minimality verification** — C-35 requires the SV preamble to name at least one competing SV architecture by label and identify the failure class it would miss. Naming one competitor satisfies C-35(b) but does not enable completeness verification: a maintainer cannot determine from a prose naming whether every mechanism has a corresponding competitor justification, or whether any mechanism lacks a provenance argument. C-37 extends C-35(b) from prose naming to tabular mapping: the Competing Implementations Preamble (or equivalent SV anchor section) is structured as a table with one row per mechanism, where each row independently carries the mechanism ID (M-NN), the competing protocol name, and the failure class that protocol misses. This structure enables two independent verifications that prose naming cannot support: (1) completeness — row count is checkable against the mechanism list, and (2) minimality — each row's failure class is readable in isolation, making it possible to check whether any two rows name the same failure class. An evaluator reading only the table can verify both properties without navigating to the aspirational criteria section or the MECHANISMS overview. Distinct from C-35 (which requires competitor naming at two structural positions): C-37 requires the SV preamble's competitor content to be structured for independent audit, not only for orientation. Distinct from C-21 (which requires the audit table to consolidate all criteria for cross-criterion analysis): C-37 requires the SV preamble's competitor table to consolidate all mechanism provenance claims for independent completeness and minimality verification. | depth | aspirational | The Structural Verification preamble (required by C-33 and C-35) includes a table — not prose alone — with one row per mechanism in the MECHANISMS section. Each row contains at minimum: (a) mechanism identifier (M-NN or equivalent), (b) the competing protocol or design approach whose gap this mechanism closes, and (c) the failure class that competing approach would miss. Pass if a reviewer reading only the table (without reading the MECHANISMS section or aspirational criteria) could (1) verify completeness by confirming row count equals mechanism count, and (2) detect any minimality violations by scanning the failure-class column for duplicates. |
| C-38 | **Format-compliance STOP conditions co-located at the declaration step for compliance-critical format distinctions** — C-22 requires both a structural ordering constraint and an explicit anti-deferral behavioral instruction for calibration-pair production. C-25 requires the anti-deferral instruction to appear inside the operative sub-step body. C-36 requires the pre-phase skeleton to use `- [ ]` format with a live-construction tick instruction. These criteria establish structural and behavioral enforcement patterns. C-38 extends the enforcement pattern to format-level compliance: when two visually similar formats have non-equivalent compliance value — one satisfies the structural requirement, the other does not (e.g., `- [ ]` creates a tracked entry; `- ` creates an untracked entry that degrades the skeleton to a plain list) — a STOP condition must appear at the declaration step identifying the non-compliant variant by name and blocking forward progress if it is used. A declarative format instruction alone ("use `- [ ]` format") fails at enforcement because it states the correct form without identifying the failure mode or triggering a stop. The STOP condition closes this gap by naming the non-compliant variant explicitly and making the distinction enforcement-backed rather than aspirational. The STOP condition must be co-located at the declaration step (not only in an SV scan), so a builder who does not read SV encounters the enforcement before any phase begins — the same co-location principle C-25 requires for anti-deferral instructions. Distinct from C-22 (which requires structural ordering constraint + behavioral anti-deferral instruction for calibration-pair production): C-38 requires a format-distinction STOP condition for compliance-critical format requirements, not only a behavioral anti-deferral instruction. Distinct from C-25 (which requires anti-deferral instructions to be co-located at the operative sub-step): C-38 requires format-compliance STOP conditions to be co-located at the format declaration step, not only anti-deferral instructions. | behavior | aspirational | For each compliance-critical format distinction in the rubric's instructions (a distinction where two visually similar formats have non-equivalent compliance value), a STOP condition is present at the format declaration step that: (a) names the non-compliant variant explicitly (e.g., "STOP: if you used `- ` instead of `- [ ]`"), (b) states the compliance consequence (e.g., "the item is untracked"), and (c) blocks forward progress (uses blocking language — "STOP", "do not proceed"). The STOP condition must be co-located at the declaration step, not deferred to an SV scan. A format instruction that states the correct form without a STOP condition identifying the non-compliant variant does not satisfy this criterion. Pass if a builder who reads only the declaration step (not SV) would encounter the STOP before any phase begins. |
| C-39 | **Competing Implementations table STOP conditions enforce completeness and minimality as active gates at the declaration point** — C-37 requires the SV preamble to include a tabular Competing Implementations Preamble with one row per mechanism, enabling independent completeness verification (row count == mechanism count) and minimality verification (failure classes unique). Enabling verification is a passive property: a table without co-located STOP conditions makes both properties checkable by an evaluator but does not block construction from proceeding if either property fails. C-39 extends C-37 by requiring that both verification properties be gate-enforced at the declaration point via two co-located STOP conditions: (1) a completeness STOP blocking forward progress if row count does not equal mechanism count, and (2) a minimality STOP blocking forward progress if any two rows share a failure class. The two STOPs test orthogonal properties: a table can satisfy completeness while failing minimality, or satisfy minimality while failing completeness. Co-locating both STOPs at the table declaration converts both dimensions from passively auditable to actively gated. Distinct from C-37 (which establishes the table structure enabling passive completeness and minimality verification): C-39 requires active gate enforcement converting the table from a passive audit surface into a construction checkpoint. Distinct from C-38 (which requires format-compliance STOPs at declaration for format-level compliance distinctions): C-39 requires property-verification STOPs at declaration for the two structural properties the Competing Implementations table is designed to enforce. | depth | aspirational | The Competing Implementations table in the SV preamble (required by C-37) is accompanied by two co-located STOP conditions: (1) a completeness STOP that blocks forward progress if the table row count does not equal the MECHANISMS section mechanism count, naming both the verification property (completeness) and the check operand (row count vs. mechanism count); and (2) a minimality STOP that blocks forward progress if any two rows in the table share the same failure class, naming both the verification property (minimality) and the check operand (failure-class column uniqueness). Both STOPs must be co-located at the table declaration (not in the SV scan steps). Pass if a builder completing the Competing Implementations table would encounter both STOPs before proceeding to any subsequent step, regardless of whether the table satisfies completeness or minimality. |
| C-40 | **Format-compliance STOP consequence statement traces the causal chain to the specific audit mechanism that the non-compliant format defeats** — C-38 requires a format-compliance STOP condition to include a consequence statement identifying the compliance failure when the non-compliant format variant is used. A consequence that names the proximate failure mode satisfies C-38 but leaves the severity of the format choice opaque: "items will be untracked" describes a property of the artifact without explaining why that property matters for audit integrity. C-40 extends C-38's consequence statement requirement: the consequence must trace the causal chain from the non-compliant format to the specific downstream audit mechanism that is defeated. The chain has three links: (1) the proximate non-compliance, (2) the audit mechanism dependency, and (3) the detection failure. This tracing makes the compliance failure legible as an audit integrity defeat. Distinct from C-38 (which requires the consequence statement to name the proximate compliance failure and block forward progress): C-40 requires the consequence to trace causally through to the specific audit mechanism defeated, naming the mechanism, its dependency on the compliant format, and the defect class that escapes detection when the non-compliant variant is used. | behavior | aspirational | For each format-compliance STOP condition (required by C-38), the consequence statement names not only the proximate non-compliance but also traces forward through at least three causal links: (a) the non-compliant format property (e.g., "this item will not be tracked"), (b) the audit mechanism that depends on the compliant format property (e.g., "the Structural Verification omission scan operates on tracked sections"), and (c) the specific defect class that escapes detection when the non-compliant format is used (e.g., "SV omission detection cannot fire on this item — heading-pattern misses for this block escape SV detection"). Pass if a maintainer reading only the STOP consequence could state which audit mechanism is defeated, why it depends on the compliant format, and what defect class becomes undetectable as a result. |
| C-41 | **Named audit roles bound to named phase spans with role-to-phase ownership declarations** — C-17 requires the workflow to include two structurally distinct phases or roles: one for criterion construction and one for criterion quality verification. A workflow satisfying C-17 may name roles without anchoring them to named phase spans — the role assignment remains implicit in per-step instructions, requiring a reader to trace all steps to determine which role performs which operations. C-41 extends C-17: each named audit role must be explicitly coupled to a named phase span it owns, so that role compliance is verifiable by phase-label inspection alone without reading per-step instructions. Without phase-binding, a rubric satisfying C-17 may declare two roles whose operational boundaries are ambiguous. With phase-binding, the role-to-phase coupling makes role-assignment ambiguity structurally impossible: every phase has a declared owner, and every role has a declared phase. This property is distinct from C-17 (which requires role/phase separation) and from C-26 (which requires phase transitions to be marked by named headings): C-41 requires each named phase heading to declare its role owner, so that the heading scan produces not only a phase sequence but a role-assignment map verifiable by heading inspection alone. | depth | aspirational | The rubric's workflow (a) assigns each named role to a named phase span via an explicit ownership declaration (e.g., "Table Gatekeeper — owns the Competing Implementations Construction Phase", "Chain Enforcer — owns the STOP Consequence Audit Phase"), and (b) co-locates the ownership declaration with the phase heading (not only in a prose preamble separate from the phase structure). A workflow that names roles in a preamble but does not bind each role to a named phase heading does not satisfy condition (b). A workflow with distinct phases but no named roles does not satisfy condition (a). Pass if a reviewer scanning only section headings would see each phase heading as both a phase label and a role-ownership declaration, without needing to read the phase body to determine which role owns it. |
| C-42 | **Per-STOP-instance consequence chain artifacts are independent named section blocks with sequential-instance gate** — C-15 requires mandatory check outputs to be independent artifacts whose absence creates a detectable structural gap before the criterion is sealed. C-19 requires those artifacts to be named markdown section blocks detectable by heading scan. Together, C-15 and C-19 apply at the per-criterion level: each criterion check produces a named block. C-42 extends the same requirement to the per-STOP-instance level within the consequence-chain construction process: each STOP's consequence chain verification must produce an independent named section block (not inline annotations embedded within the STOP declaration), and the block for STOP N must appear before STOP N+1 is written — creating a sequential-instance gate. The discriminating property is heading-independence: an inline annotation is present-or-absent without creating a heading-scan gap; a named section block is absent-as-gap — its non-appearance in the heading sequence is detectable without reading content. Without per-STOP named blocks, a builder can silently skip consequence chain verification for a STOP and proceed to STOP N+1 without a visible omission record; the gap is discoverable only at SV time after the full Author Phase is complete. With per-STOP named blocks and a sequential gate, each skipped STOP leaves a heading-scan gap detectable before the next STOP construction begins. Distinct from C-15 (which requires independent artifacts at the per-criterion level): C-42 requires the same named-block independence at the per-STOP-instance level, with a sequential gate (STOP N artifact before STOP N+1). Distinct from C-19 (which requires per-criterion check outputs to be named markdown section blocks): C-42 requires per-STOP-instance consequence chain artifacts to be named markdown section blocks whose sequential position creates an instance-by-instance omission record. | behavior | aspirational | The rubric's consequence-chain construction instructions require each STOP instance's consequence chain artifact to appear as an independent named section block (e.g., `### STOP-CONSEQUENCE [STOP-NN]`) before the next STOP instance is written. A sequential-instance gate explicitly states that STOP N+1 may not be constructed until STOP N's named block appears in the heading sequence. A workflow where all STOP consequence chain artifacts can be produced inline, or all deferred to a single SV scan, does not satisfy this criterion. Pass if (a) each STOP instance produces a named section block that would show as a heading-scan gap if absent, and (b) explicit blocking language prevents STOP N+1 construction until STOP N's block is present. |
| C-43 | **Emit phase closes with a per-criterion evaluability declaration mapping each structural criterion to its named verification artifact and scan method** — C-15 and C-19 ensure check artifacts exist and are named blocks. C-29 ensures the mechanism overview declares each mechanism's output format at orientation time. These three criteria do not ensure that a reader navigating directly to the emit phase can identify which named artifact enables which criterion's independent verification. C-43 extends C-29's declaration requirement from mechanism-overview time to emit-phase time: the emit phase (Phase 9 or equivalent final section) closes with an explicit per-criterion evaluability map that, for each structural criterion whose compliance is verifiable via heading scan or named artifact inspection (those satisfying C-15/C-19), states the criterion ID, the named artifact that enables independent verification, and the scan method. Without C-43, a reader must infer the criterion→artifact mapping by reading phase instructions; with C-43, the mapping is declared at emit time so a reader navigating only to the emit phase can identify the verification path for each structural criterion. Distinct from C-29 (which requires the mechanism overview to declare output format): C-43 requires the emit phase to declare the criterion→artifact mapping as evaluability guidance, not only output format specification. Distinct from C-15/C-19 (which require artifact existence and named-block format): C-43 requires the emit phase to declare which artifact verifies which criterion, converting a runtime property (artifact exists, therefore verification is possible) into a declared property (artifact X verifies criterion Y, explicitly stated). | behavior | aspirational | The rubric's emit phase (Phase 9 or equivalent final section) includes a per-criterion evaluability declaration that, for each structural criterion whose compliance is verifiable via heading scan or named artifact inspection, states: (a) the criterion ID, (b) the named artifact (by its `###` heading name) that enables independent verification, and (c) the scan method (e.g., "heading-scan", "table-row-count", "checkbox-state inspection"). A declaration that uses generic references ("see the structural artifacts") without naming specific criterion IDs and artifacts does not satisfy this criterion. Pass if a reader navigating only to the emit phase could identify, for each listed criterion, exactly which named section to inspect and what scan to perform — without reading any phase instructions. |
| C-44 | **Per-criterion evaluability declarations include an explicit independence guarantee naming the instruction content excluded from the verification path** — C-43 requires the emit phase to include a per-criterion evaluability declaration naming the artifact and scan method for each structural criterion. A declaration that says "scan the Role-Phase Index to verify C-41" is a positive claim: it names what the evaluator should do. It does not say what the evaluator is freed from doing. C-44 extends C-43's positive claim to a falsifiable independence claim: each per-criterion evaluability declaration must additionally name the instruction content that does NOT need to be read to verify compliance. A declaration that says "scan X to verify Y" without naming the excluded content does not satisfy C-44 even if the independence claim is true at runtime; the criterion requires the declaration itself to make the independence claim falsifiable by stating the exclusion. Distinct from C-43 (which requires the criterion→artifact mapping to exist in the emit phase): C-44 requires the mapping to additionally name the instruction content excluded from the verification path, converting the independence claim from implicit to explicit. The excluded content must be named specifically — a generic "independently verifiable" claim without naming what the evaluator is freed from reading does not satisfy this criterion. Distinct from C-15/C-19 (which establish artifact independence at the structural level): C-44 establishes independence declaration at the evaluability-statement level, so an evaluator consulting only the emit phase's evaluability declarations can state both what to read and what not to read. | behavior | aspirational | Each per-criterion evaluability declaration (required by C-43) includes, co-located with the artifact and scan method, an explicit statement naming the instruction content that does NOT need to be read to verify compliance (e.g., "without reading role instruction bodies", "without reading phase-step instructions", "without reading the MECHANISMS section"). The excluded content must be named specifically: "independently verifiable" without naming the excluded content does not satisfy this criterion. Pass if an evaluator reading only the per-criterion declaration could state both (a) what to inspect and (b) what reading is not required — with (b) checkable: the stated excluded content must actually not be required to reach the verification verdict. |
| C-45 | **Emit-phase entry condition quotes the evaluability gate identifier verbatim, making evaluability sequencing independently auditable from Phase 9 entry alone** — C-41 requires each named audit role to be bound to a named phase span via an ownership declaration co-located with the phase heading, so that role compliance is verifiable by heading scan without reading per-step instructions. C-43 requires the emit phase to close with a per-criterion evaluability declaration. C-44 requires each declaration to name the excluded instruction content. These criteria together ensure the emit phase body is evaluability-complete. What they do not ensure is that the emit phase's prerequisite relationship to the evaluability gate is auditable from the phase's entry point. C-45 extends C-41's independent-audit principle to the evaluability workflow: the emit-phase entry condition (Phase 9 PREREQUISITE, gate annotation, or equivalent header-level declaration) must quote the evaluability gate identifier verbatim (e.g., "PREREQUISITE: EVALUABILITY GATE complete" or "Requires: Phase 8.5 EVALUABILITY GATE"), so that a reader examining only the Phase 9 section header can confirm the evaluability sequencing requirement without reading the EVALUABILITY AUDITOR role body or any prior phase instructions. Distinct from C-41 (which requires role-to-phase ownership declarations at phase headings): C-45 requires the emit phase's entry condition to embed the evaluability gate identifier, making the dependency auditable from Phase 9 entry alone without reading the gate-implementing role body or any prior phase. | behavior | aspirational | The emit-phase entry condition (Phase 9 section header, PREREQUISITE annotation, or equivalent header-level element) quotes the evaluability gate identifier verbatim — the exact name used for the gate in the EVALUABILITY AUDITOR role or equivalent gating mechanism (e.g., "EVALUABILITY GATE", "Phase 8.5 Gate: EVALUABILITY GATE"). A phase entry condition that references the gate by description ("after evaluability is confirmed") without the verbatim identifier does not satisfy this criterion. Pass if a reader examining only the Phase 9 section header — without reading the EVALUABILITY AUDITOR role body, any prior phase instructions, or any section below the Phase 9 header — could confirm: (a) that a gate is required before Phase 9 begins, and (b) the exact name of the gate that must be satisfied. |
| C-46 | **Evaluability gate and emit-phase terminal table declared as non-redundant enforcement points with explicitly stated independent necessity** — C-43 requires the emit phase to close with a per-criterion evaluability declaration (terminal table). C-44 requires each declaration to name the excluded instruction content. Both criteria specify what each enforcement point must produce but do not require the two mechanisms to be declared as non-redundant. The isolation results from R14 establish why the declaration is necessary: terminal-table-alone fails C-44; gate-alone leaves per-criterion evaluability declarations unverified. C-46 requires the evaluability architecture to include an explicit non-redundancy declaration stating that the pre-emit gate and the emit-close terminal table are independently necessary enforcement points, with per-mechanism failure-mode arguments identifying what fails when each is removed. Distinct from C-43/C-44 (which require the terminal table and independence guarantees to exist): C-46 requires the architecture to declare that neither mechanism alone is sufficient, specifying which failure mode each mechanism independently prevents. | depth | aspirational | The rubric's evaluability architecture (or equivalent section describing the relationship between the pre-emit gate and the emit-close terminal table) includes an explicit declaration that the two mechanisms are non-redundant — stating specifically: (a) what fails if the gate is removed (which evaluability property becomes unenforced at Phase 9 entry), and (b) what fails if the terminal table is removed (which evaluability property becomes unenforced at Phase 9 close). A declaration that states the two mechanisms exist without addressing their non-redundancy does not satisfy this criterion. Pass if a maintainer reading the non-redundancy declaration could state, for each mechanism, the specific evaluability failure that the other mechanism cannot prevent. |
| C-47 | **Non-redundancy failure-mode arguments employ Architecture-labeled experiment anchors with STOP-condition enforcement** — C-46 requires the evaluability architecture to include an explicit non-redundancy declaration stating that the pre-emit gate and emit-close terminal table are independently necessary, with per-mechanism failure-mode arguments identifying what fails when each is removed. Prose-based failure-mode arguments satisfy C-46 but leave the claim verification path internal to the declaration block: a maintainer confirming the claim must re-read the prose argument rather than locating an independently auditable description. C-47 extends C-46 by requiring the failure-mode arguments to employ Architecture-labeled experiment anchors — named labels such as "Architecture A — gate-only failure" and "Architecture B — terminal-table-only failure" — where each label resolves to a named competitor entry in the EVALUABILITY ARCHITECTURE COMPETITOR section (per C-35/C-37), and a STOP condition enforces the Architecture label structure at the NON-REDUNDANCY DECLARATION point. Labels create a cross-reference path: a maintainer confirming "Architecture B fails C-44" can navigate to the Architecture B competitor entry by label lookup without re-reading the inline failure-mode argument. The STOP condition converts the label-anchor architecture from a style choice to a structural enforcement requirement. Distinct from C-46 (which requires the failure-mode arguments to exist and identify the failure mode): C-47 requires the arguments to cite named Architecture labels that resolve to COMPETITOR section entries, enabling independent verification of each failure-mode claim by label lookup. Distinct from C-35/C-37 (which require the COMPETITOR section to exist and be tabular): C-47 requires the NON-REDUNDANCY DECLARATION to cross-reference the COMPETITOR section via Architecture labels, closing the referential gap between the declaration block and the competitor descriptions. | behavior | aspirational | The NON-REDUNDANCY DECLARATION (or equivalent evaluability architecture block satisfying C-46) employs Architecture-labeled anchors — labels of the form "Architecture A — [mechanism configuration]" and "Architecture B — [mechanism configuration]" (or equivalent named architecture identifiers) — for each failure-mode argument, where each label used in the declaration appears as a named competitor entry in the EVALUABILITY ARCHITECTURE COMPETITOR section (verifiable by label-lookup without reading the declaration prose). A STOP condition co-located at the NON-REDUNDANCY DECLARATION point enforces the Architecture label structure. Pass if a maintainer reading the NON-REDUNDANCY DECLARATION can: (a) identify the Architecture label cited for each failure-mode argument, (b) locate the corresponding COMPETITOR section entry by label lookup, and (c) verify the failure-mode claim from the entry without re-reading the declaration prose. |
| C-48 | **Emit-phase verbatim gate-ID PREREQUISITE includes an explicit auditability boundary declaration naming the excluded role body or phase content** — C-45 requires the emit phase's entry condition to quote the evaluability gate identifier verbatim at the section-header level, making evaluability sequencing independently auditable from Phase 9's entry header without reading the gate-implementing role body. A verbatim gate-ID quote confirms that the gate was a structural prerequisite for Phase 9 entry — the sequencing requirement is auditable from the PREREQUISITE line. It does not declare what a verifier is freed from reading: the auditability boundary is implied by the gate structure rather than stated alongside the quote. C-48 extends C-45's verbatim-quote requirement from gate-ID presence to gate-ID presence plus an independence boundary declaration co-located with the verbatim quote: the emit-phase PREREQUISITE must additionally name the specific role body or phase content that a verifier does NOT need to read to confirm the sequencing requirement. This is the same extension pattern as C-44 extending C-43: C-43 requires the positive claim (criterion → artifact + scan method); C-44 requires the positive claim plus the independence guarantee (names excluded instruction content). C-48 applies the identical pattern to the Phase 9 entry condition: C-45 requires the positive claim (gate-ID quoted at entry); C-48 requires the positive claim plus the independence boundary declaration (names role body excluded from sequencing verification). Distinct from C-44 (which requires per-criterion evaluability declarations in the terminal table to name excluded content): C-48 requires the Phase 9 entry condition — at the PREREQUISITE / section-header level, not in the terminal table body — to name the role body or phase content excluded from evaluability sequencing verification. Distinct from C-45 (which requires the verbatim gate-ID quote): C-48 requires the verbatim quote to be accompanied by an independence boundary declaration, making the auditability property self-asserting at the entry condition rather than inferred from the gate structure. | behavior | aspirational | The emit-phase entry condition (PREREQUISITE annotation satisfying C-45) includes — co-located with the verbatim gate-ID quote, at the section-header level — an explicit auditability boundary declaration naming the specific role body or phase content that a verifier does NOT need to read to confirm the evaluability sequencing requirement (e.g., "an evaluator auditing evaluability sequencing can confirm this PREREQUISITE was satisfied by reading Phase 9's header alone, without reading ROLE: EVALUABILITY AUDITOR's body"). The declaration must be co-located at the header level, not buried in phase-body prose. Pass if a reader examining only the Phase 9 section header — without reading the EVALUABILITY AUDITOR role body or any prior phase — can confirm not only what gate is required (C-45) but also what reading is excluded from sequencing verification. |
| C-49 | **Phase 9 PREREQUISITE independence boundary declaration includes a co-located positive audit-sufficiency claim** — C-48 requires the emit-phase PREREQUISITE to co-locate an independence boundary declaration (naming the excluded role body) alongside the verbatim gate-ID quote, making the auditability boundary self-asserting at Phase 9 entry. A boundary declaration states what the verifier is freed from reading — a negative exclusion claim. It does not state what the verifier CAN accomplish from the PREREQUISITE alone. C-49 extends C-48 by requiring the PREREQUISITE to also include a positive audit-sufficiency claim explicitly stating that reading the PREREQUISITE line alone is sufficient to confirm gate completion — for example: "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR." A PREREQUISITE satisfying C-48 (names excluded role body) but not including the positive sufficiency claim fails C-49 — the excluded-content naming is necessary but not sufficient. This is the same extension pattern as C-43→C-44: C-43 requires the positive claim (criterion → artifact + scan method); C-44 requires the positive claim plus the independence boundary (names excluded instruction content). C-49 applies the reverse: C-48 requires the negative exclusion claim (names excluded role body); C-49 requires the negative exclusion claim plus the positive sufficiency claim (explicitly states what can be confirmed from the PREREQUISITE alone). Together, C-48 and C-49 make the PREREQUISITE a combined exclusion-plus-sufficiency declaration — the most informative form of a self-asserting auditability claim at Phase 9 entry. Distinct from C-48 (which requires the independence boundary declaration naming excluded content): C-49 requires the boundary declaration to be paired with an affirmative sufficiency claim, converting the PREREQUISITE from a negative-only to a bidirectional independence declaration. Distinct from C-44 (which requires the terminal table's per-criterion declarations to name excluded instruction content): C-49 requires the Phase 9 entry condition to make the positive sufficiency claim at the header level, not only in the terminal table body. | behavior | aspirational | The emit-phase entry condition (PREREQUISITE annotation satisfying C-48) includes — co-located with the verbatim gate-ID quote and the independence boundary declaration — an explicit positive audit-sufficiency claim stating that reading the PREREQUISITE line alone is sufficient to confirm gate completion (e.g., "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone", "this PREREQUISITE line alone confirms gate completion"). The sufficiency claim must be affirmative ("reading this line is sufficient") not only negative ("reading ROLE: EVALUABILITY AUDITOR is not required"). A PREREQUISITE with the independence boundary declaration (C-48 pass) but no affirmative sufficiency claim fails C-49. Pass if a reader encountering only the PREREQUISITE annotation would understand both: (a) that gate completion can be confirmed from this line alone (positive claim), and (b) what reading is excluded (negative boundary). |
| C-50 | **Each Architecture-labeled STOP in the NON-REDUNDANCY DECLARATION explicitly names the target resolution section in the STOP predicate** — C-47 requires the NON-REDUNDANCY DECLARATION to employ Architecture-labeled anchors (Architecture A, Architecture B) with co-located STOP conditions enforcing label resolution against the EVALUABILITY ARCHITECTURE COMPETITOR section. A STOP condition satisfying C-47 must block a declaration that omits Architecture labels — its predicate may be as minimal as "STOP: if Architecture A label is absent." Such a STOP enforces the label-structure requirement but does not embed the knowledge of which section the label must resolve to: a maintainer reading the STOP in isolation must infer the resolution target from context. C-50 extends C-47 by requiring each Architecture-labeled STOP predicate to name the resolution target section explicitly (e.g., "STOP: if the Architecture A label above does not resolve to a named entry in the EVALUABILITY ARCHITECTURE COMPETITOR section, the NON-REDUNDANCY DECLARATION is incomplete"), making the STOP condition independently verifiable from the STOP text alone without reading adjacent declaration prose or navigating to the COMPETITOR section to learn its name. A STOP predicate that enforces label presence without naming the resolution target section fails C-50 even if C-47 passes. This extends C-47 following the same self-sufficiency principle C-44 applies to per-criterion evaluability declarations: the declaration must embed the information needed for verification, not require the reader to infer it from context. Distinct from C-47 (which requires Architecture-labeled STOP enforcement at the NON-REDUNDANCY DECLARATION): C-50 requires the STOP predicate to name the resolution target section explicitly, making the STOP a self-contained verification instruction. | behavior | aspirational | Each Architecture-labeled STOP condition co-located at the NON-REDUNDANCY DECLARATION (required by C-47) includes in its predicate an explicit naming of the target resolution section — the section a maintainer must consult to verify that the label resolves to a named entry (e.g., "does not resolve to a named entry in the EVALUABILITY ARCHITECTURE COMPETITOR section", or equivalent section-naming form). A STOP predicate that enforces the Architecture-label structure (satisfying C-47) without naming the resolution target section fails C-50. Pass if a maintainer reading only the STOP predicate text — without reading any surrounding prose, section headings, or other declaration context — could identify: (a) which Architecture label is being enforced, (b) which section that label must resolve to, and (c) that failure to resolve constitutes an incomplete declaration. |
| C-51 | **Positive audit-sufficiency claim in the Phase 9 PREREQUISITE embeds the excluded role body name within the sufficiency clause itself** — C-49 requires the PREREQUISITE to include a positive audit-sufficiency claim co-located with the independence boundary declaration (satisfying C-48). The positive sufficiency claim may be expressed as "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone" — a statement of what CAN be confirmed. The independence boundary sentence (C-48) separately names the excluded role body. These two sentences together satisfy C-49, but a reader encountering only the positive sufficiency clause — in isolation from the independence boundary sentence — cannot identify what is excluded from the verification path: the exclusion knowledge resides in the other sentence. C-51 extends C-49 by requiring the positive sufficiency claim to embed the excluded role body name within the sufficiency clause itself (e.g., "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR"), making the sufficiency clause a self-contained exclusion+sufficiency statement. A PREREQUISITE satisfying C-49 (positive sufficiency claim present, co-located with independence boundary) but expressing the exclusion only in the independence boundary sentence without embedding it in the sufficiency clause fails C-51 — the sufficiency clause alone is incomplete: it states what can be confirmed but not what need not be read. This applies the same embedding principle C-44 requires for per-criterion evaluability declarations (excluded content co-located within the declaration clause, not only in an adjacent sentence) to the Phase 9 positive sufficiency clause: the exclusion must appear within the clause that makes the sufficiency assertion, creating a dual-presence of the exclusion at both the independence boundary sentence level (C-48) and within the positive sufficiency clause level (C-51). Distinct from C-49 (which requires the positive sufficiency claim to be co-located with the independence boundary declaration): C-51 requires the positive sufficiency claim to embed the excluded content name within its own clause body, making the sufficiency clause self-sufficient without the independence boundary sentence. | behavior | aspirational | The positive audit-sufficiency claim (required by C-49) in the emit-phase PREREQUISITE embeds the excluded role body name within the sufficiency clause text — not only in the co-located independence boundary sentence (satisfying C-48). A sufficiency clause that reads "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone" without embedding the excluded role body name fails C-51, even if the independence boundary sentence co-located with it names the excluded role. Pass if a reader encountering only the sufficiency clause text — in isolation from the independence boundary sentence — can identify both (a) what can be confirmed (gate completion) and (b) what reading is excluded (the specific role body or phase content named within the clause). |
| C-52 | **Each Architecture-labeled STOP consequence clause in the NON-REDUNDANCY DECLARATION names the target declaration by its exact section heading name** — C-50 requires each Architecture-labeled STOP predicate to name the resolution target section explicitly and to state a consequence of resolution failure. A consequence satisfying C-50 may be as minimal as "the declaration is incomplete" — this states a consequence using a generic reference ("the declaration") that requires the reader to infer which declaration is meant from the enclosing section context. A maintainer reading the STOP text in isolation cannot identify which declaration is rendered incomplete without reading the surrounding section heading. C-52 extends C-50 by requiring the consequence clause to name the target declaration by its exact section heading name (e.g., "the NON-REDUNDANCY DECLARATION is incomplete" rather than "the declaration is incomplete"), making the consequence independently anchored without reading the enclosing section heading or surrounding declaration prose. Together, C-50 and C-52 make each Architecture-labeled STOP a triple-self-sufficient enforcement point: a maintainer reading only the STOP text can identify (a) which Architecture label is being enforced (C-47), (b) which section the label must resolve to (C-50), and (c) which named declaration is rendered incomplete if resolution fails (C-52). Distinct from C-50 (which requires the STOP predicate to name the resolution target section): C-52 requires the STOP consequence clause to name the affected declaration by its section heading name, extending the self-sufficiency requirement from the predicate to the consequence. Distinct from C-47 (which requires Architecture-labeled STOP enforcement at the NON-REDUNDANCY DECLARATION): C-52 requires the STOP's consequence clause to be declaration-specific, not only predicate-specific, so the consequence is independently interpretable without knowing the enclosing section. | behavior | aspirational | Each Architecture-labeled STOP condition co-located at the NON-REDUNDANCY DECLARATION (required by C-47 and C-50) includes a consequence clause that names the affected declaration by its exact section heading name (e.g., "the NON-REDUNDANCY DECLARATION is incomplete", not "the declaration is incomplete" or "this section is incomplete"). A consequence clause using a generic declarative reference ("the declaration", "this section") that requires the reader to infer the declaration identity from the enclosing section heading fails C-52. Pass if a maintainer reading only the STOP text — without reading any surrounding section heading, adjacent declaration prose, or enclosing section context — could identify which specific named declaration is rendered incomplete by the predicate failure. |
| C-53 | **Positive audit-sufficiency claim in the Phase 9 PREREQUISITE embeds the verbatim evaluability gate identifier within the sufficiency clause itself** — C-51 requires the positive sufficiency clause to embed the excluded role body name within the clause text, making the clause a self-contained exclusion+sufficiency statement. A sufficiency clause satisfying C-51 states "a reader can confirm that Phase 9 was properly gated by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR" — naming the role exclusion within the sufficiency clause. The verbatim gate identifier (e.g., "EVALUABILITY GATE") appears separately in the co-located PREREQUISITE gate-ID quote (required by C-45), not within the sufficiency clause itself. A reader encountering only the sufficiency clause can identify what is excluded from reading (the role body, C-51) but cannot confirm which specific gate was completed — "Phase 9 was properly gated" is a generic confirmation that does not embed the gate identity. C-53 extends C-51 by requiring the verbatim evaluability gate identifier also to be embedded within the sufficiency clause, so the clause names both WHAT gate was completed (by verbatim identifier, resolving the gate definitively without reading the PREREQUISITE gate-ID quote) and WHY role body reading is unnecessary (by excluded role name). V-01 (R18) satisfies C-53: the sufficiency clause reads "a reader can confirm that EVALUABILITY GATE is complete by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR" — both the verbatim gate identifier and the excluded role name are embedded, making the clause a complete standalone audit certificate. A sufficiency clause that embeds the role name (C-51 pass) but refers to the gate generically ("Phase 9 was properly gated") or by phase number ("Phase 8.5 is complete") without embedding the verbatim gate identifier fails C-53, because a reader of the sufficiency clause alone cannot confirm which named gate was satisfied without consulting the co-located PREREQUISITE gate-ID quote. This extends C-51 by the same embedding principle C-51 applies to C-49: just as C-51 requires the excluded role name to appear within the sufficiency clause rather than only in the co-located independence boundary sentence, C-53 requires the verbatim gate identifier to appear within the sufficiency clause rather than only in the co-located PREREQUISITE gate-ID annotation — each extension closes one remaining information gap in the sufficiency clause, until the clause is a complete standalone certificate. Together, C-45 + C-48 + C-49 + C-51 + C-53 achieve five-element PREREQUISITE completeness: (C-45) verbatim gate identifier in the entry condition, (C-48) excluded role body named in the boundary declaration, (C-49) positive sufficiency claim co-located with the boundary declaration, (C-51) excluded role body embedded within the sufficiency clause, and (C-53) verbatim gate identifier embedded within the sufficiency clause — the sufficiency clause is now a complete standalone certificate whose reader needs no other PREREQUISITE element to confirm gate completion and understand the independence property. Distinct from C-51 (which requires the excluded role body name to be embedded in the sufficiency clause): C-53 requires the verbatim gate identifier to additionally be embedded in the sufficiency clause, closing the final information gap so a reader of the sufficiency clause alone can identify every element needed to confirm evaluability gating without reading any other PREREQUISITE component. Distinct from C-45 (which requires the verbatim gate identifier to appear in the Phase 9 entry condition): C-53 requires the verbatim gate identifier to appear within the sufficiency clause text, not only in the co-located PREREQUISITE annotation. | behavior | aspirational | The positive audit-sufficiency claim (required by C-49 and satisfying C-51) in the emit-phase PREREQUISITE embeds both (a) the verbatim evaluability gate identifier (as used in the C-45 PREREQUISITE annotation, e.g., "EVALUABILITY GATE") and (b) the excluded role body name (as used in the C-51 embedding, e.g., "ROLE: EVALUABILITY AUDITOR") within the text of the sufficiency clause itself — not only in the co-located gate-ID annotation or the co-located independence boundary sentence. A sufficiency clause that embeds the role name (C-51 pass) but refers to the gate by phase number, by description, or by generic confirmation ("Phase 9 was properly gated") without embedding the verbatim gate identifier fails C-53. Pass if a reader encountering only the sufficiency clause text — in complete isolation from the gate-ID annotation, the independence boundary sentence, and all other PREREQUISITE elements — can identify: (a) the specific gate completed (by verbatim gate identifier), and (b) the specific role body excluded from sequencing verification (by role body name), both from the clause text alone. |
| C-54 | **Each Architecture-labeled STOP consequence clause in the NON-REDUNDANCY DECLARATION names the specific structural element within the affected declaration that the predicate failure leaves absent** — C-52 requires the consequence clause to name the affected declaration by its exact section heading name (e.g., "the NON-REDUNDANCY DECLARATION is incomplete" rather than "the declaration is incomplete"), making the consequence independently anchored without reading the enclosing section. A consequence satisfying C-52 names the declaration but leaves implicit which element of that declaration is rendered absent or incomplete by the resolution failure. A maintainer reading the consequence "the NON-REDUNDANCY DECLARATION is incomplete" can identify the affected declaration but must read the STOP predicate and the declaration body to determine what is missing: the consequence does not itself embed the element-level information. C-54 extends C-52 by requiring the consequence clause to additionally name the specific structural element within the declaration that the predicate failure leaves absent (e.g., "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent" or "the Architecture-A-to-EVALUABILITY ARCHITECTURE COMPETITOR mapping in the NON-REDUNDANCY DECLARATION is incomplete") — so the consequence clause specifies both the incomplete declaration (by heading name, C-52) and the specific missing element (by element name, C-54). V-02 (R18) satisfies C-54: the STOP consequence reads "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent" — naming both the declaration by its heading name and the specific missing element by its label. A consequence satisfying C-52 (declaration named by heading) but stating only "the NON-REDUNDANCY DECLARATION is incomplete" without naming the specific absent element fails C-54, because a maintainer cannot identify what to add to remediate the incompleteness without reading the predicate or the declaration body. This extends C-52 following the same specificity principle C-52 extends C-50: C-50 requires the predicate to name the resolution section, C-52 requires the consequence to name the affected declaration, and C-54 requires the consequence to name the specific absent element within the declaration — each extension makes one more information element of the STOP independently readable from the STOP text, reducing external context needed for interpretation. Together, C-47 + C-50 + C-52 + C-54 make each Architecture-labeled STOP a quadruple-self-sufficient enforcement point: a maintainer reading only the STOP text can identify (a) which Architecture label is being enforced (C-47), (b) which section the label must resolve to (C-50), (c) which named declaration is rendered incomplete if resolution fails (C-52), and (d) which specific element within the declaration is absent if resolution fails (C-54). Distinct from C-52 (which requires the consequence clause to name the affected declaration by section heading): C-54 requires the consequence clause to additionally name the specific structural element within the declaration that is left absent by the predicate failure, completing the consequence's causal specificity at the element level. Distinct from C-50 (which requires the STOP predicate to name the resolution target section): C-54 operates on the consequence clause and extends C-52's declaration-level naming to element-level naming, not the predicate. | behavior | aspirational | Each Architecture-labeled STOP condition co-located at the NON-REDUNDANCY DECLARATION (required by C-47, C-50, and C-52) includes a consequence clause that names both the affected declaration (by section heading name, satisfying C-52) and the specific structural element within that declaration that the predicate failure leaves absent or incomplete (e.g., "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent", "the Architecture-A resolution mapping in the NON-REDUNDANCY DECLARATION is incomplete"). A consequence clause naming the declaration by heading name (C-52 pass) but using generic incompleteness language ("the NON-REDUNDANCY DECLARATION is incomplete") without identifying the specific absent element does not satisfy C-54. Pass if a maintainer reading only the STOP consequence clause — without reading the STOP predicate, the NON-REDUNDANCY DECLARATION body, the EVALUABILITY ARCHITECTURE COMPETITOR section, or any surrounding prose — can state both (a) which named declaration is affected and (b) which specific element within that declaration is absent or incomplete. |
| C-55 | **Paired same-clause extension criteria include a combined-form example demonstrating joint satisfiability** — When two aspirational criteria co-extend the same structural clause in different information-embedding dimensions, each extension adding a distinct required element to the same clause text (e.g., C-51 and C-53 both constrain the Phase 9 PREREQUISITE sufficiency clause — C-51 requiring the excluded role body name to be embedded within the clause, C-53 requiring the verbatim gate identifier to also be embedded within the clause; C-52 and C-54 both constrain the Architecture-labeled STOP consequence clause — C-52 requiring the affected declaration name to be embedded by exact heading, C-54 requiring the specific absent element name to additionally be embedded), the rubric's baseline section provides a combined-form example for each co-extension pair that satisfies both criteria simultaneously in a single clause expression. Without a combined-form example, a builder implementing a ceiling variation must discover by trial-and-error whether the two criteria can coexist in one clause or whether one extension displaces the other; the combined-form example converts structural independence between paired criteria from an empirical observation (discovered only by running both simultaneously) to a declared property of the rubric that can be verified before construction. V-05 (R19) satisfies C-55: the baseline provides the combined sufficiency clause "a reader can confirm that EVALUABILITY GATE is complete by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR" — satisfying C-53 (verbatim gate identifier embedded) and C-51 (excluded role body name embedded) simultaneously in a single expression; and the combined consequence clause "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent" — satisfying C-52 (declaration named by exact heading) and C-54 (absent element named) simultaneously. A rubric whose baseline provides individual examples for each criterion of a co-extension pair but no single combined clause expression demonstrating both satisfied together fails C-55, because structural independence is asserted but not demonstrated. Distinct from C-53 and C-51 individually (each of which requires a specific element embedded in the sufficiency clause): C-55 requires the rubric itself to provide a combined quotation showing C-51+C-53 jointly satisfied in one clause and C-52+C-54 jointly satisfied in one clause, making joint satisfiability an auditable rubric property rather than an assumed one. | behavior | aspirational | The rubric's baseline section (or the co-extension pair criteria descriptions) provides a combined-form example for each pair of aspirational criteria that co-extend the same structural clause in different embedding dimensions. For the sufficiency clause pair (C-51 + C-53): a single clause expression must be quoted that simultaneously embeds both the verbatim gate identifier (C-53) and the excluded role body name (C-51), e.g., "a reader can confirm that EVALUABILITY GATE is complete by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR." For the STOP consequence pair (C-52 + C-54): a single clause expression must be quoted that simultaneously names both the affected declaration by exact heading (C-52) and the specific absent element (C-54), e.g., "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent." A rubric that provides examples for C-53 and C-51 separately but does not quote a single combined expression satisfying both fails C-55. Pass if a builder reading only the baseline section — without reading the individual criterion descriptions — can identify a single combined clause expression to use for each co-extension pair that would satisfy both criteria in the pair simultaneously. |
| C-56 | **Each form-embedding criterion has a dedicated named Dual Auditor Track 2 verification step that identifies the criterion by ID and discriminates compliant from non-compliant form by name** — Aspirational criteria that require a specific information element to be embedded within a structural clause (e.g., C-53 requiring the verbatim gate identifier embedded in the sufficiency clause; C-54 requiring the absent element name embedded in the STOP consequence clause) impose form requirements that may be satisfied at instruction level — the rubric instructs the builder to produce a specific form — without being satisfied at audit level: the Dual Auditor may verify general form compliance but miss the per-criterion discrimination between compliant and non-compliant forms. A Dual Auditor Track 2 that checks form compliance generically (e.g., "verify PREREQUISITE form is correct") provides some forcing function but does not discriminate by criterion ID: an auditor performing a generic check cannot distinguish a C-53 failure ("EVALUABILITY GATE is missing from the sufficiency clause") from a C-51 failure ("role body name is missing from the sufficiency clause") because the generic step does not name either criterion. C-56 requires the rubric to include, for each form-embedding criterion, a dedicated named verification step in the Dual Auditor Track 2 that (a) identifies the criterion by ID (e.g., "C-53 verification"), (b) quotes or names the compliant form required (e.g., "verbatim gate identifier within sufficiency clause text"), and (c) explicitly states the non-compliant form that would cause a FAIL (e.g., "flag FAIL if sufficiency clause uses generic confirmation without verbatim gate identifier"). V-05 (R19) satisfies C-56: Dual Auditor Track 2 includes step 7 ("C-53: flag FAIL if sufficiency clause uses only generic language without verbatim gate identifier") and step 6 ("C-54: each STOP consequence names absent ELEMENT, NOT only declaration heading"). A variation satisfying instruction-level form requirements (C-53 and C-54 form specified in rubric text) but implementing only a generic Dual Auditor Track 2 step without per-criterion ID discrimination fails C-56, because the second structural forcing function is absent: audit-verified compliance cannot be distinguished from instruction-level compliance by the Track 2 output alone. Distinct from C-53 and C-54 (which require specific embedded forms in the rubric's structural elements): C-56 requires dedicated per-criterion Dual Auditor verification steps that close the gap between instruction-level compliance (the rubric says to produce form X) and audit-verified compliance (the Dual Auditor confirms form X was actually produced for criterion C-NN specifically). | behavior | aspirational | For each aspirational criterion requiring a specific information element to be embedded within a structural clause (at minimum C-53 and C-54), the Dual Auditor Track 2 contains a dedicated named verification step that (a) identifies the criterion by ID (e.g., "C-53 verification" or "Track 2 Step N — C-53"), (b) states the specific compliant form required — the form that would produce a PASS verdict for that criterion, and (c) explicitly names the non-compliant form that would cause a FAIL (e.g., "flag FAIL if sufficiency clause uses generic gate confirmation without verbatim gate identifier such as 'EVALUABILITY GATE'"). A Dual Auditor Track 2 that checks form compliance as part of a combined step covering C-53 and C-54 together without per-criterion discrimination, or that names the criterion in step text without naming both the compliant and non-compliant forms, fails C-56. Pass if a reader navigating only to the Track 2 step labeled for C-53 — without reading any other Track 2 step, instruction body, or criterion description — can state exactly what form would produce a PASS verdict and what form would produce a FAIL verdict for C-53 specifically. |
| C-57 | **Emit manifest check includes at least one dedicated per-criterion form-verification row for each form-embedding criterion, creating a third independent verification level** — C-56 requires a dedicated Dual Auditor Track 2 step for each form-embedding criterion, creating a second structural forcing function at audit time. A rubric satisfying C-56 has form compliance verified at two levels: (1) instruction level (builder told to produce specific form X), and (2) Dual Auditor Track 2 (auditor explicitly checks form X per criterion by ID). However, both levels operate within the Author-through-Auditor phase sequence: a form requirement absent from the emit manifest can pass instruction-level and Dual Auditor checks but be omitted from the pre-emit structural gate — the emit manifest, if it includes only general completeness rows, does not block emission on form-criterion-specific compliance. C-57 requires the emit manifest check to include at least one dedicated named row for each form-embedding criterion (e.g., one row for C-53: "PREREQUISITE sufficiency clause contains verbatim gate identifier [EVALUABILITY GATE] per C-53"; one row for C-54: "each Architecture-labeled STOP consequence names specific absent element per C-54"), converting form-requirement verification from a dual-level check (instruction + Dual Auditor) to a triple-level check with a pre-emit gate. Together, the three levels are structurally independent: instruction-level compliance can be present while Dual Auditor Track 2 is absent (fails C-56); Dual Auditor Track 2 can be present while emit manifest rows are absent (fails C-57); emit manifest rows can be present while instruction-level form requirements are absent (the criterion is gated but the form is undefined). V-05 (R19) satisfies C-57: the emit manifest includes two rows for C-53 form verification and one row for C-54 form verification — three dedicated form rows in addition to general completeness rows. A variation satisfying C-56 (dedicated Dual Auditor Track 2 steps) but omitting emit manifest form rows fails C-57, because the form requirement lacks pre-emit gate enforcement: compliant-form production is audited but not gated before emission. Distinct from C-56 (which requires dedicated Dual Auditor Track 2 steps): C-57 requires the emit manifest itself to contain criterion-specific form verification rows, operating at pre-emit gate time rather than at Dual Auditor time, creating a third structurally independent check that is verifiable from the manifest alone without reading Dual Auditor instructions or instruction bodies. | behavior | aspirational | The emit manifest check includes at least one dedicated named row for each aspirational criterion requiring a specific embedded form (at minimum C-53 and C-54). Each manifest row must (a) name the criterion it verifies by ID (e.g., "C-53 form check" or "Row: C-54 element naming"), (b) state the specific form condition being verified (e.g., "verbatim gate identifier present in sufficiency clause text" for C-53; "specific absent element named within STOP consequence clause" for C-54), and (c) function as a pre-emit gate — its absence from the manifest is a blocking condition, not a deferred check. An emit manifest that includes general completeness rows ("all STOP conditions present", "all PREREQUISITE elements present") but no criterion-specific form-verification rows named by criterion ID fails C-57, even if C-56 (Dual Auditor Track 2 dedicated steps) passes. Pass if a reviewer checking only the emit manifest — without reading Dual Auditor instructions, instruction bodies, or criterion descriptions — can identify that C-53 form compliance (verbatim gate identifier in sufficiency clause) and C-54 form compliance (absent element name in STOP consequence) are each gated as named conditions before emission. |

---

## Scoring

```
composite = (essential_pass / 5 * 60)
          + (recommended_pass / 3 * 30)
          + (aspirational_pass / 49 * 10)  /* C-09 through C-57 */
```

**Note**: aspirational denominator is 49 (C-09 through C-57).

**Golden threshold**: all 5 essential criteria pass AND composite >= 80.

| Band | Score | Interpretation |
|------|-------|----------------|
| Golden | all essential + >= 80 | Ship-ready rubric — use as objective function |
| Acceptable | all essential + 70-79 | Minor gaps, usable with reviewer awareness |
| Marginal | all essential + < 70 | Coverage or ordering gaps, iterate before use |
| Fail | any essential fails | Not a valid objective function regardless of total score |

---

## Baseline for R20

A variation satisfying all R19 criteria (C-53 PASS, C-54 PASS) but lacking the combined-form
joint satisfiability example (C-55 FAIL), or lacking dedicated per-criterion Dual Auditor Track 2
steps for C-53 and C-54 (C-56 FAIL), or lacking emit manifest form-verification rows for C-53
and C-54 (C-57 FAIL) scores 46/49 x 10 = 9.388, composite <= 99.388. The new discrimination
targets for R20 are C-55 (rubric baseline provides a combined-form example for each co-extension
pair — C-51+C-53 satisfied simultaneously in one sufficiency clause expression, C-52+C-54
satisfied simultaneously in one STOP consequence clause expression — converting joint
satisfiability from an empirical observation to a declared rubric property verifiable before
construction), C-56 (each form-embedding criterion C-53 and C-54 has a dedicated named Dual
Auditor Track 2 verification step identifying the criterion by ID and naming both the compliant
form and the non-compliant form that causes a FAIL, creating a second structural forcing function
independent of instruction-level form requirements), and C-57 (emit manifest check includes at
least one dedicated named row per form-embedding criterion verifying the specific embedded form
as a pre-emit gate condition, creating a third independent verification level so compliant-form
production is checkable at instruction, Dual Auditor Track 2, and emit manifest levels).

---

## Quick Checklist (for manual scoring)

- [ ] C-01: Every criterion has all five required fields (ID, text, weight, category, pass condition)
- [ ] C-02: Exactly 3-5 essential, 2-3 recommended, 1-2 aspirational
- [ ] C-03: Essential criteria are specific to the target skill, not generic quality observations
- [ ] C-04: Every pass condition is testable by two independent evaluators without discussion
- [ ] C-05: Composite score formula and golden threshold are present
- [ ] C-06: At least 3 distinct categories represented across all criteria
- [ ] C-07: Essential criteria ordered from most to least structurally critical
- [ ] C-08: Quick checklist present with all criterion IDs
- [ ] C-09: Each failing criterion tells the author what to fix without re-running the skill
- [ ] C-10: No two criteria are redundant or contradictory
- [ ] C-11: Every pass condition fails the inertia test (a "clear and comprehensive" rubric would miss it)
- [ ] C-12: At least one concrete generic/grounded example pair present for an essential criterion
- [ ] C-13: At least one hard phase gate with explicit blocking language and a checkable condition
- [ ] C-14: Calibration pair is written immediately after pass-condition drafting, not retrospectively
- [ ] C-15: Each mandatory check step emits an independent output artifact before the criterion is sealed — skipping leaves a detectable gap
- [ ] C-16: Two gate levels present — one phase gate and at least one per-criterion gate
- [ ] C-17: Authoring phase and auditing phase are separated, producing independently readable outputs
- [ ] C-18: Audit artifact captures the discriminating element for each condition, not only pass/fail verdicts
- [ ] C-19: Mandatory check outputs are named markdown section blocks — a missing block = a missing heading, detectable by section scan without reading content
- [ ] C-20: Per-criterion gates block forward progress before the criterion row is written — not retroactive repair steps that request missing content after the fact
- [ ] C-21: Audit phase produces a single consolidated table (all criteria as rows) enabling cross-criterion pattern analysis without manual aggregation
- [ ] C-22: Calibration pair instruction includes both a structural ordering constraint AND an explicit anti-deferral behavioral instruction — neither alone is sufficient
- [ ] C-23: Structural verification step (heading-scan) carries blocking language that prevents the Auditor phase from starting until all required patterns are confirmed present
- [ ] C-24: Workflow contains three verification mechanisms targeting non-overlapping defect classes: omission prevention at write time, structural bypass detection post-phase, cross-criterion specificity analysis at audit time
- [ ] C-25: Anti-deferral prohibition appears inside the sub-step body it governs — not only in a phase header or preamble — so a builder skipping preamble still encounters it
- [ ] C-26: Phase transitions are marked by named section headings — the position of structural verification between Author and Auditor phases is verifiable by heading scan, not by reading instruction prose
- [ ] C-27: Any cross-criterion verification mechanism (e.g., redundancy check) produces a named section block and carries a forward-blocking gate — its omission is detectable by heading scan
- [ ] C-28: Scoring formula's aspirational denominator is annotated with the criterion ID range it covers (e.g., "C-09 through C-54") — correctness verifiable without counting criteria in the table
- [ ] C-29: Mechanism overview names the structural output format (e.g., named `###` heading) required for each mechanism governed by C-19/C-27 — structural contract visible at orientation time, not only at execution time
- [ ] C-30: Structural Verification step explicitly states its phase-bounded coverage scope, names out-of-scope heading patterns by name, and identifies where each deferred check will be performed
- [ ] C-31: Workflow includes a named pre-phase OUTPUT SKELETON step (built from MECHANISMS alone, before any phase) and Structural Verification cross-references it — catching builder-declared-but-not-produced headings as a failure mode distinct from heading-pattern misses
- [ ] C-32: Mechanism overview includes an entry for the pre-phase skeleton step naming its heading pattern and role — so a builder reading only the overview can include the skeleton heading in their output plan and derive a complete heading skeleton without reading phase instructions
- [ ] C-33: Structural Verification opens with a preamble (before any scan step) naming both sources of truth, the distinct failure class each catches, and that the failure classes are non-overlapping — two-source architecture declared as design principle, not inferred from scan count
- [ ] C-34: Pre-phase skeleton step heading uses a phase-positional label (e.g., `### PHASE 0 — OUTPUT SKELETON`) that anchors it as a distinct phase in the scannable sequence — complete workflow Phase 0 → Author → SV → Auditor verifiable from headings alone
- [ ] C-35: Each MECHANISMS entry names the competing protocol whose structural gap the mechanism closes; SV preamble names at least one competing SV design and the failure class it would miss — provenance argument embedded in the rubric, not inferred externally
- [ ] C-36: Pre-phase skeleton uses `- [ ]` checklist format with an explicit instruction to tick each item during Author Phase construction — skeleton is a live production tracker, not only a pre-construction declaration audited at SV time
- [ ] C-37: Competing Implementations Preamble is a table (M-NN | competing protocol | failure class) with one row per mechanism — row count verifies completeness without reading MECHANISMS, row content verifies each claim without reading aspirational criteria, distinct failure classes across rows enable redundancy detection
- [ ] C-38: For each compliance-critical format distinction (e.g., `- [ ]` vs `- `), a STOP condition at the declaration step names the non-compliant variant, states the compliance consequence, and blocks forward progress — format enforcement is gate-backed at declaration time, not only detectable at SV time
- [ ] C-39: Competing Implementations table has two co-located STOP conditions — one blocking on completeness (row count != mechanism count) and one blocking on minimality (duplicate failure class) — co-located at the table declaration, not deferred to SV scan
- [ ] C-40: Each format-compliance STOP consequence traces three causal links: non-compliant format → named audit mechanism dependency → specific defect class that escapes detection
- [ ] C-41: Each named audit role is bound to a named phase span via an explicit ownership declaration co-located with the phase heading — role compliance verifiable by heading scan without reading per-step instructions, no role ambiguity across phases
- [ ] C-42: Each STOP's consequence chain produces an independent named section block (not inline annotations) with a sequential-instance gate — STOP N named block must appear before STOP N+1 is written, absence detectable as a heading-scan gap during Author Phase construction
- [ ] C-43: Emit phase closes with a per-criterion evaluability declaration naming the artifact and scan method for each structural criterion (those satisfying C-15/C-19) — a reader navigating only to the emit phase can identify the verification path for each criterion without reading phase instructions
- [ ] C-44: Each per-criterion evaluability declaration names the instruction content excluded from the verification path (e.g., "without reading role instruction bodies") — independence claim is falsifiable from the declaration alone, not merely implied by the scan method
- [ ] C-45: Emit-phase entry condition quotes the evaluability gate identifier verbatim at the section-header level (e.g., "PREREQUISITE: EVALUABILITY GATE complete") — evaluability sequencing independently auditable from Phase 9 entry alone without reading the gate-implementing role body or any prior phase
- [ ] C-46: Evaluability architecture includes an explicit declaration that the pre-emit gate and emit-close terminal table are non-redundant enforcement points, each independently necessary — gate failure mode (C-44 unenforced at entry) and terminal-table failure mode (C-43 partial without independent production) both stated, so maintainers can verify non-redundancy without running isolation experiments
- [ ] C-47: NON-REDUNDANCY DECLARATION uses Architecture A/B labeled anchors resolving to named COMPETITOR section entries, STOP-enforced — failure-mode claims verifiable by label-lookup without re-reading inline prose arguments
- [ ] C-48: Emit-phase PREREQUISITE co-locates an independence boundary declaration with the verbatim gate-ID quote, naming the excluded role body — auditability property self-declared at Phase 9 entry, not inferred from gate structure
- [ ] C-49: Phase 9 PREREQUISITE includes both the independence boundary declaration (C-48, names excluded role body) AND a co-located positive audit-sufficiency claim explicitly stating that reading the PREREQUISITE line alone is sufficient to confirm gate completion — negative exclusion plus positive sufficiency, PREREQUISITE is a bidirectional independence declaration
- [ ] C-50: Each Architecture-labeled STOP predicate in the NON-REDUNDANCY DECLARATION explicitly names the resolution target section (e.g., "does not resolve to a named entry in the EVALUABILITY ARCHITECTURE COMPETITOR section") — STOP condition independently checkable from its text alone, resolution target not inferrable only from context
- [ ] C-51: Positive sufficiency clause in the Phase 9 PREREQUISITE embeds the excluded role body name within the sufficiency clause itself (e.g., "without reading the body of ROLE: EVALUABILITY AUDITOR" appears within the sufficiency clause, not only in the independence boundary sentence) — sufficiency clause is self-contained as both a positive claim and a negative exclusion, readable independently of the independence boundary sentence
- [ ] C-52: Each Architecture-labeled STOP consequence clause names the affected declaration by its exact section heading name (e.g., "the NON-REDUNDANCY DECLARATION is incomplete" not "the declaration is incomplete") — consequence independently anchored from STOP text alone without reading the enclosing section heading, completing triple-self-sufficiency: which label, which section, which named declaration
- [ ] C-53: Positive sufficiency clause in the Phase 9 PREREQUISITE embeds the verbatim evaluability gate identifier within the sufficiency clause itself (e.g., "a reader can confirm that EVALUABILITY GATE is complete by reading this PREREQUISITE line alone, without reading the body of ROLE: EVALUABILITY AUDITOR") — sufficiency clause is a complete standalone audit certificate naming both the specific gate completed and the excluded role body, readable without consulting the co-located gate-ID annotation or independence boundary sentence
- [ ] C-54: Each Architecture-labeled STOP consequence clause names the specific structural element within the affected declaration that the predicate failure leaves absent (e.g., "the Architecture-A competitor entry in the NON-REDUNDANCY DECLARATION is absent" not "the NON-REDUNDANCY DECLARATION is incomplete") — consequence independently anchored at element level from STOP text alone, completing quadruple-self-sufficiency: which label, which section, which named declaration, which specific absent element
- [ ] C-55: Baseline section provides a combined-form example for each co-extension pair (C-51+C-53 simultaneously in one sufficiency clause; C-52+C-54 simultaneously in one STOP consequence clause) — joint satisfiability declared as a rubric property with a quoted combined expression, not merely assumed from individual criterion examples
- [ ] C-56: Each form-embedding criterion (C-53, C-54) has a dedicated named Dual Auditor Track 2 verification step that identifies the criterion by ID and names both the compliant form and the non-compliant form that causes a FAIL — second structural forcing function independent of instruction-level form requirement
- [ ] C-57: Emit manifest check includes at least one dedicated named row per form-embedding criterion (C-53, C-54) verifying the specific embedded form as a pre-emit gate — third independent verification level enabling compliant-form production to be checked at instruction, Dual Auditor Track 2, and emit manifest levels
