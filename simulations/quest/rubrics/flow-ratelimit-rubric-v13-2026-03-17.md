Reading the scorecard carefully to extract the new pattern before writing C-34.

Key R12 signal: V-02 proves C-33 is reachable without C-32 — but at the cost of making terminal reconciler check (a) a **mixed correction+verification** operation. V-01/V-05 achieve 120/120 precisely because C-32's prior sweep makes check (a) **pure verification**. The pre-reconciler's `CURRENCY SWEEP FINDINGS` record becomes the independent prior that check (a) confirms against rather than replaces. That "pure-verification mode" property is not captured by C-32 (which concerns the sweep role itself) or C-33 (which concerns the four checks existing) — it's a property of how check (a) executes when all three layers are present simultaneously.

**C-34 — Pure-Verification Reconciler Check (a)**

---

# flow-ratelimit — Quest Rubric v13

**Version summary:**
- Aspirational pool: 25 → 26
- Scoring denominator: `/26`
- C-34 added to Aspirational table; Evaluator Notes updated with disambiguation guidance against C-32 and C-33

---

## Essential (60 points total — must all pass)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Rate Limit Scope Identification** | coverage | essential | Output identifies at least one concrete rate limit by component name, numeric threshold, interval, and scope (connector-level, action-level, platform-level, or environment-level). Vague statements such as "there are rate limits" do not pass. |
| C-02 | **Connector-Level vs Platform-Level Differentiation** | correctness | essential | Output explicitly distinguishes between connector-level rate limits (enforced by the external service or connector SDK) and platform-level rate limits (enforced by the workflow platform, e.g., Power Automate run concurrency or action throughput limits). Treating them as a single undifferentiated limit category does not pass. |
| C-03 | **Observable UX Per Throttle Tier** | coverage | essential | Output describes observable user-facing behavior at each distinct throttle tier reached (e.g., 429 with Retry-After shown in connector error, flow run queued and delayed, flow run failed with ActionThrottled, silent exponential backoff invisible to user). At least two distinct tiers described if the scenario spans multiple tiers. |
| C-04 | **Unprotected Burst Path Identification** | correctness | essential | Output identifies at least one structurally unprotected burst path — a trigger or action that can generate concurrent requests exceeding a rate limit with no concurrency cap, no retry policy, and no queue buffer between the source and the rate-limited endpoint. A path that has throttle controls but at a higher tier does not qualify. |
| C-05 | **Retry-After Handling Gap Check** | coverage | essential | Output explicitly evaluates whether the flow or connector handles Retry-After headers (or equivalent backoff signals such as `x-ms-ratelimit-remaining`, `Retry-After-Ms`) from throttled endpoints. Missing handling must be flagged as a finding with a description of the failure mode it causes (e.g., immediate retry storm, permanent failure after N retries). |

---

## Recommended (30 points total — output is better with these)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-06 | **Cascading Throttle Failure Scenario** | depth | recommended | Output constructs or identifies a specific scenario where throttling at one tier triggers a second distinct throttle event at a different tier — and describes the compounding effect on throughput or error rate. Two independent limits both hit under load do not constitute a cascade; the second throttle must be causally triggered by the first. |
| C-07 | **Numeric Rate Limit Specificity** | depth | recommended | At least one rate limit is cited with a concrete numeric threshold (e.g., "600 API calls per 60 seconds per connection", "5 concurrent runs per environment per flow") rather than described only in abstract or relative terms. Estimates are acceptable if labeled as such. |
| C-08 | **Volume-to-Behavior Mapping** | format | recommended | Output includes a structured mapping — table, tiered list, or equivalent — that shows which throttle behavior activates at which request volume range, enabling a reader to determine expected system behavior for a given load without re-reading the full analysis. |

---

## Aspirational (30 points total — raise the bar once essential/recommended are stable)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-09 | **Per-bottleneck Mitigation Prescriptions** | depth | aspirational | For each identified bottleneck or unprotected burst path, output proposes a concrete remediation specific to the component and limit type (e.g., "enable concurrency control on the For Each action capped at 5 parallel branches", "add exponential backoff with Retry-After header parsing to the HTTP action", "split batch trigger into N sequential calls with 1s delay"). Generic advice ("add retry logic") does not pass — the prescription must name the specific action, setting, or pattern. |
| C-10 | **Quantified Impact at Load Spike** | depth | aspirational | Output provides numeric estimates of degradation at a specific load multiplier (e.g., "at 5x normal volume with a 600 req/min limit, 42% of requests are queued beyond 30s, 11% fail after exhausting 3 retry attempts") using the rate limits cited in C-07 as the arithmetic basis. The estimate must be derivable from the stated limits — not asserted as a generic percentage. |
| C-11 | **Burst Gap Classification (Structural vs. Incidental)** | depth | aspirational | *R1 excellence signal: V-05 was the only variation with an explicit structural-vs-incidental sub-test, identified as the sharpest C-04 enforcement across all variations.* Beyond identifying the unprotected burst path (C-04), output explicitly classifies whether the gap is structural (no mechanism exists on this path) vs. incidental (mechanism exists but is misconfigured, bypassable, or absent only under specific conditions). Classification must be stated and justified — not merely implied by framing. Requires C-04 to pass. |
| C-12 | **Dual-state Volume Mapping (Baseline vs. Protected)** | depth | aspirational | *R1 excellence signal: V-05 produced unique dual-state volume mapping (inertia vs. protected) across volume tiers, identified as satisfying C-08 more richly than any other variation.* The volume-to-behavior mapping extends C-08 to compare two states at each volume level: the current/unprotected baseline and the mitigated/protected state. Each volume tier shows what changes when protections are applied — not just what happens at that tier in isolation. Requires C-08 to pass. |
| C-13 | **Verdict-before-Evidence Structure** | format | aspirational | *R1 excellence signal: V-04's commitment-before-evidence (VERDICT-first) structure produced the clearest binding-order statements of any variation.* Output states the binding constraint or primary gap as an explicit labeled finding — a verdict or claim — before the supporting evidence section. The pre-commitment forces an analytical position rather than narration toward a conclusion. The evidence section must then confirm or explicitly revise the verdict; evidence that drifts without revisiting the verdict does not pass. |
| C-14 | **Baseline-delta Mitigation** | depth | aspirational | *R1 excellence signal: V-05's "Improvement over inertia" column dual-enforced that mitigations move from baseline state, rejecting both generic advice and improvements that don't change the baseline condition.* Each mitigation explicitly states the before-state (baseline behavior at the bottleneck) and the after-state (system behavior with the mitigation applied). Mitigations that could apply to any system without referencing the specific baseline condition do not pass. Requires C-09 to pass. |
| C-15 | **Document-head Global Verdict** | format | aspirational | *R2 excellence signal: Both V-01 and V-02 scored PARTIAL on C-13 — V-01 placed the binding constraint label in Section 3 (after Sections 1-2 audit); V-02 placed it within a mid-document analysis section. Neither enforced a standalone verdict block before any evidence or inventory. C-13 as written permits section-local verdicts that satisfy the label requirement without anchoring the analysis at document head.* Output opens with a standalone Verdict block — appearing before any rate limit inventory, burst path audit table, or volume mapping section — that states: (a) the binding constraint by component name and numeric threshold, (b) the primary gap (e.g., unprotected burst path at a named action, or Retry-After not handled at a named connector), and (c) the current system status at the stated load (safe / degraded / failing). The Verdict block must be self-contained: a reader who reads only it knows the core finding without proceeding to the evidence. Evidence sections must confirm or explicitly revise each Verdict claim; a Verdict that no evidence section references does not pass. Requires C-01 and C-04 to pass. |
| C-16 | **Format Contract Preamble** | format | aspirational | *R2 excellence signal: V-02's axis — "format contract preamble — BASELINE / PROTECTED dual-column on every table and mitigation" — produced the strongest C-12 enforcement of either variation. Declaring the dual-column structure as a document-wide contract before sections removes per-section negotiation about whether the dual-column applies and enforces consistency across all comparative tables simultaneously. Neither V-01 nor any prior variation captured this as an explicit structural discipline.* Output includes a Format Contract section placed before the first analysis section that declares: (a) the column structure that applies to every comparative table in the document (e.g., BASELINE \| PROTECTED \| Delta), (b) what BASELINE and PROTECTED mean in the specific scenario context — not generic definitions, but names tied to the scenario's components or states, and (c) a rejection clause stating that tables or mitigation rows omitting the declared structure are flagged as incomplete. At least two distinct tables elsewhere in the document must comply with the declared format; a format contract with only one compliant table does not pass. Requires C-08 and C-12 to pass. |
| C-17 | **Cascading Section Gate Enforcement** | format | aspirational | *R3 excellence signal: V-01's role sequence gating was scored "mechanically hard" and "most structurally enforced of all five variations" because every section transition carried explicit "Do not begin Role N until [specific deliverables from Role N-1] are written" language — extending the gate chain across all nine roles, not only the verdict-first and format-contract-first transitions captured by C-15 and C-16.* Beyond placing the Verdict and Format Contract as preambles (C-15, C-16), the document structure includes explicit per-transition gate language for at least three additional section transitions beyond the two preambles. Each gate must name the prior section's specific deliverables — not a generic "do not skip ahead" instruction — so that omission of any deliverable from section N-1 structurally blocks section N. A document with gate language only on the preamble transitions does not pass. Requires C-15 and C-16 to pass. |
| C-18 | **Bidirectional Verdict Revision Marking** | format | aspirational | *R3 excellence signal: V-03's phase lifecycle introduced "If Phase 1 revises, Phase 0 marked REVISED" — a bidirectional revision property absent from all prior variations. C-13 and C-15 require evidence sections to confirm or revise the Verdict, but neither requires the Verdict section itself to be updated when revised. V-03's pattern makes the Verdict block self-annotating: a reader consulting only the Verdict section knows immediately whether it was revised and which section to consult, without reading every evidence section.* When any evidence section revises a claim in the Verdict block, the Verdict section itself contains an inline revision marker — e.g., "REVISED — see [Section N]" — for each revised claim, with a forward reference to the revising section. A Verdict block revised by evidence but not updated with markers does not pass. A Verdict block with markers but no corresponding revision in a named evidence section does not pass. Requires C-15 to pass. |
| C-19 | **Four-Field UX Tier Template** | format | aspirational | *R3 excellence signal: V-02's Section 4 "User Experience Per Tier" and V-03's Phase 4 "UX PER THROTTLE TIER" both pass C-03 using a structured four-field template per tier; V-01 fails C-03 outright because its nine-role structure contains no dedicated UX role and "Role 4 terminates at one user-observable effect." C-03 passes with observable behavior at two tiers in any form; C-19 requires a structured template enforcing all four fields at every tier described.* The UX-per-tier section applies a structured template to each throttle tier with all four fields: (a) error code or platform signal (e.g., "HTTP 429 with Retry-After header", "ActionThrottled event in run history"), (b) user-visible behavior (what the user observes or experiences at that tier), (c) visibility level (user-visible and explicit vs. silent or background), (d) recovery path available to the user (e.g., manual retry, automatic exponential backoff, permanent failure with no recovery). A tier described with fewer than all four fields does not pass the template requirement. A tier described in free prose without the template structure does not pass even if all four topics are mentioned. At least two tiers must be described using the full four-field template. Requires C-03 to pass. |
| C-20 | **Arithmetic Trace Explicitness** | depth | aspirational | *R4 excellence signal: V-01's ROLE 8 mandates step-by-step arithmetic for the 5x load cell with each computation step shown inline and traceable to the ROLE 3 numeric registry — "Qualitative-only cell flagged INCOMPLETE." C-10 requires that quantified impact estimates be "derivable" from stated limits; a conclusion stated without its arithmetic chain satisfies the letter of C-10. V-01's R4 implementation showed the stronger form: the Format Contract rejection clause flags under-derived cells inline as INCOMPLETE, making arithmetic gaps visible without reading the analysis, and the derivation steps appear in the cell itself rather than being reconstructible by an attentive reader.* The quantified impact section shows step-by-step arithmetic for at least the primary load spike cell — not only the conclusion but the computation chain: requests per interval x load multiplier = peak load, peak load - rate limit = overflow volume, overflow x retry factor = failed request volume, failed / peak = failure percentage. Each step must reference the specific numeric values from the rate limit registry (C-07). A cell that states a conclusion (e.g., "42% fail") without the intermediate computation steps does not pass, even if the conclusion is arithmetically correct. Requires C-10 to pass. |
| C-21 | **Full Gate Chain Closure** | format | aspirational | *R4 excellence signal: V-01 gates all 8 remaining role transitions (Roles 3-10) beyond the two preambles, producing a closed chain where no section can be reached without all prior sections' deliverables present. C-17 requires at least three additional gated transitions; V-01's R4 implementation demonstrates that full closure — zero ungated boundaries — creates qualitatively stronger structural enforcement than partial coverage, because any partial gate chain still permits selective skipping of ungated sections. A document with three gated transitions and two ungated transitions in the analysis body satisfies C-17 but retains bypass paths that C-21 eliminates.* Every section transition in the document is gated — no transition in the analysis body remains ungated beyond the two preamble transitions already required by C-15 and C-16. The count of gated transitions in the analysis body equals the total number of section boundaries in the analysis body. Each gate must name the prior section's specific deliverables (as required by C-17); a gate that says only "do not proceed until the previous section is complete" does not count toward the closure count. Requires C-17 to pass. |
| C-22 | **Gate-checkpoint Verdict Currency** | format | aspirational | *R4 excellence signal: V-04's axis combines V-01's complete gate chain (C-17/C-21 target) with V-03's bidirectional Verdict revision (C-18 target). The combination reveals a pattern neither criterion alone requires: when gates and revision marking are both present, each gated transition can carry an immediate Verdict-currency check — inserting the "REVISED — see Role N" marker at the boundary of the revising section rather than deferring all revision marking to a terminal reconciliation role. C-18 is satisfied by any mechanism that results in markers appearing in the Verdict block; a document can earn C-18 via a single closing reconciliation pass while leaving the Verdict stale across every intermediate gate. C-22 closes that gap: the currency check is embedded in the gate condition itself.* Each gated section transition beyond the two preambles includes an explicit Verdict-currency instruction: the producing section must check whether its findings revise any Verdict claim, and if so, must insert the inline revision marker before the next section's gate condition is unblocked. A document that satisfies C-18 solely through a terminal reconciliation role — performing all marker insertions after the final analysis section — does not pass C-22. Revision marking must occur at the gate boundary of the revising section, not deferred to document close. Requires C-17 and C-18 to pass. |
| C-23 | **Schema-column Arithmetic Enforcement** | format | aspirational | *R5 excellence signal: V-02 declared DERIVATION CHAIN as a mandatory 4th schema column in the Format Contract alongside BASELINE and PROTECTED. This embeds arithmetic enforcement structurally: a blank DERIVATION CHAIN cell is a structural omission visible at scan time — no prose reading required — whereas V-01's Rejection Clause B operates as a content check requiring an evaluator to read and assess whether the prose-level derivation steps are present. The schema-column form of C-20 enforcement creates observable completeness gaps; the rejection-clause form creates detectable content violations. Both satisfy C-20 through distinct mechanisms; C-23 captures the schema-column form as the stronger structural discipline because omissions are self-evident without interpretation.* The Format Contract schema declares a DERIVATION CHAIN column (or equivalently named column) as a mandatory structural field alongside BASELINE and PROTECTED in every comparative table containing quantified estimates. Each row's derivation chain cell must contain at least the primary computation steps (not a conclusion alone). A Format Contract that names the column but does not declare it mandatory via a rejection clause does not pass. A table with the column header present but cells left as qualitative conclusions does not pass. At least two distinct quantified tables must include the schema column with populated derivation cells. Requires C-16 and C-20 to pass. |
| C-24 | **Terminal Document-Close Reconciler** | format | aspirational | *R7 excellence signal: V-01's Role 11 is a dedicated terminal role appearing after all analysis sections, whose sole purpose is to audit the complete document state — scanning for missing REVISED markers in the Verdict block, confirming each gated deliverable was produced, and flagging DERIVATION CHAIN cells that contain only conclusions. C-18 requires markers to appear in the Verdict block; C-22 requires currency checks embedded at gate boundaries. V-01 demonstrates a third, orthogonal enforcement mechanism: retroactive audit over the full document once production is complete. V-01 passes C-18 via the reconciler even with only partial per-gate currency coverage, showing the reconciler catches violations that incomplete forward enforcement misses. C-22 requires currency checks at every gate boundary (forward enforcement); C-24 requires a reconciler that verifies the complete document state at close (backward enforcement). Neither subsumes the other: a document with full C-22 coverage but no reconciler has no audit of whether markers were correctly inserted; a document with a reconciler but incomplete gate coverage satisfies C-24 but not C-22.* Output includes a dedicated terminal reconciler section — the last named section in the document, after all analysis sections — that performs three checks: (a) scans the Verdict block for claims that were revised during analysis and confirms each carries an inline REVISED marker with a forward reference to the revising section; (b) enumerates each gated transition and confirms the named deliverables from the prior section are present; (c) identifies any DERIVATION CHAIN cells in quantified tables that contain only conclusions without computation steps and flags them as incomplete. The reconciler must produce a named output (e.g., "Reconciler Findings: 0 violations" or an itemized violation list) — a section that scans without producing a finding record does not pass. A document whose reconciler findings are never referenced by any prior gate does not pass. Requires C-18 and C-21 to pass. |
| C-25 | **Two-Tier Violation Taxonomy in Format Contract** | format | aspirational | *R7 excellence signal: V-02 distinguishes STRUCTURAL REJECTION CLAUSE (schema violation — a required column is absent from the table, detectable at scan time without reading cell content, remediation: add the column) from CONTENT REJECTION CLAUSE (content violation — the required column is present but a cell is populated with a qualitative conclusion only, detectable only by reading cell content, remediation: deepen the cell). C-16 requires "a rejection clause stating that tables or mitigation rows omitting the declared structure are flagged as incomplete" — a single, undifferentiated clause. C-23 adds a DERIVATION CHAIN rejection clause but does not require the clause to distinguish structural from content violations. V-02's taxonomy makes violation type, detection method, and remediation path explicit as separate named clause types. This matters for evaluation: a structural violation requires no interpretation to detect (the column header is present or absent); a content violation requires a judgment about whether the cell content is sufficient. Combining them into one clause obscures which remediation applies and conflates scan-time detection with read-time assessment.* The Format Contract declares two named rejection clause types as distinct entries: a STRUCTURAL clause covering schema-level violations (required column absent from table structure) and a CONTENT clause covering cell-level violations (required column present but cell contains only a conclusion without the mandated derivation or comparison content). Each clause must: (a) be named as a distinct clause type, (b) state the detection method (scan-time vs. read-time), and (c) state the remediation action specific to that violation type. A Format Contract with a single combined rejection clause covering both violation types does not pass, even if both violation types are mentioned within it. A Format Contract that names both types but provides identical remediation for both does not pass. Requires C-16 and C-23 to pass. |
| C-26 | **Explicit UX Role with Gated Four-Field Enforcement** | format | aspirational | *R8 excellence signal: V-01's 11-role sequence achieves full gate chain closure (C-21) and a terminal reconciler that checks all gated deliverables (C-24), but fails C-19 because "implied ROLE 10 may cover UX but four-field template structure is not enforced." C-17 and C-21 require gate language at every section transition naming the prior section's specific deliverables; this enforcement chain cannot extend to the UX tier section unless the four required fields are explicitly enumerated as gate deliverables for that role. C-19 requires the four-field template to be applied in the UX section's content; C-26 requires the four-field constraint to be embedded in the gate condition itself — so that C-24's reconciler check (b), which enumerates each gated transition and confirms named deliverables are present, automatically audits UX template completeness at document close without a separate UX-specific reconciler check.* The UX-per-tier section is implemented as an explicitly numbered role in the role sequence with a gate condition that names all four required fields — (a) error code or platform signal, (b) user-visible behavior, (c) visibility level, (d) recovery path — as individually enumerated deliverables. The gate must block the next role unless all four fields are present for every tier described in the document, not merely for the two-tier minimum required by C-19. An implied role present in a numbered list but whose gate condition does not name the four fields individually does not pass. A gate that names the four fields but scopes them only to the minimum two tiers while leaving additional tiers ungated at field level does not pass. A role whose gate condition reads "UX complete" or similar without enumerating the four fields by name does not pass — the gate must name each field as a distinct checkable deliverable. Requires C-19 and C-21 to pass. |
| C-27 | **UX Template as Named Format Contract Schema** | format | aspirational | *R9 excellence signal: V-02. C-26 closes the gate-enumeration gap (four fields as individually blocking gate conditions). C-27 closes the Format Contract-level scan-time detection gap: Schema B is declared as a named structural schema in the Format Contract with its own STRUCTURAL REJECTION CLAUSE, making missing UX fields detectable at scan time without reading prose — parallel to Schema A's column-level enforcement. Terminal reconciler must include an explicit Schema B audit as check (d), distinct from the gate audit.* The Format Contract declares Schema B as a named structural schema — not a formatting suggestion — with its own STRUCTURAL REJECTION CLAUSE stating: scan-time detection method (count field labels present in the tier block) and remediation (add the missing field labels). A Format Contract that applies rejection clauses only to Schema A (the comparative table) without an equivalent structural clause for Schema B does not pass — the UX template must be a first-class schema in the Format Contract, not a prose instruction. The Terminal Reconciler section extends its audit to include check (d): an independent Schema B structural scan confirming that every tier block contains all four field-label targets. Check (d) must be named separately from the gate audit check (b) — a reconciler whose check (b) covers all gated deliverables including UX fields but has no dedicated Schema B structural scan does not pass. Requires C-16, C-19, C-26. |
| C-28 | **UX Completeness as Named Verdict Claim** | format | aspirational | *R9 excellence signal: V-03 signal A. Claim (d) in the Verdict block commits to UX template completeness and tier count. This makes UX coverage automatically subject to C-18 bidirectional revision marking and C-22 gate-checkpoint currency — a tier added mid-analysis that lacks full four-field coverage triggers a REVISED marker on Claim (d) at the gate boundary, not only at terminal reconciliation. Without Claim (d), a Verdict-only reader has no visibility into UX commitments or their revisions.* The Verdict block contains a named Claim (d) committing to the total number of tiers described and confirming that all tiers use the four-field Schema B template. The revision rule for Claim (d) must be explicitly stated: a tier added mid-analysis that modifies the tier count or reveals incomplete four-field coverage requires a REVISED marker on Claim (d) inserted at the gate boundary of the role where the new tier was discovered — deferral to terminal reconciliation does not satisfy the currency requirement. A Verdict block that mentions UX coverage within one of the other claims (a-c) without a dedicated Claim (d) does not pass — the claim must be separately enumerable so that C-18 and C-22 can apply to it independently. Requires C-15, C-18, C-22, C-26. |
| C-29 | **Six-Item UX Gate with Tier-Count and Structure-Compliance Items** | format | aspirational | *R9 excellence signal: V-03 signal B. Extends C-26's four-item gate to six explicitly enumerated blocking conditions: the four FIELD conditions plus (e) tier-count verification against the burst path audit section (not merely the two-tier minimum) and (f) structure-compliance confirmation (template structure used, not free prose) as a separately named blocking condition. Eliminates two residual bypass paths the four-item gate leaves open: undercounting tiers and prose-formatting compliant-field tiers.* The UX role gate names six separately enumerated blocking conditions: (a) FIELD-A present and substantively populated for every tier, (b) FIELD-B present and substantively populated for every tier, (c) FIELD-C present and substantively populated for every tier, (d) FIELD-D present and substantively populated for every tier, (e) tier count in this section verified against the burst path audit section (the role or section where unprotected burst paths are enumerated) — not merely confirmed against the two-tier minimum and not resolved by reference to the Verdict block, (f) structure-compliance confirmed: each tier uses the named template structure, not free prose. Items (e) and (f) must each appear as separately named blocking conditions — collapsing them into a compound condition such as "tier count and structure verified" fails both. A gate with items (a)-(e) that omits (f) as a named item does not pass. A gate with six items in which (e) cites Verdict Claim (d) instead of the analysis section does not pass. Requires C-26. |
| C-30 | **Schema B Content Rejection Clause** | format | aspirational | *R10 excellence signal: V-02. C-27 requires Schema B to carry a STRUCTURAL REJECTION CLAUSE (scan-time: are the four field labels present?). V-02's Format Contract declares Schema B with both a STRUCTURAL and a CONTENT rejection clause — making four named rejection clauses total: Schema A STRUCTURAL, Schema A CONTENT, Schema B STRUCTURAL, Schema B CONTENT. Without a Schema B CONTENT clause, a tier with all four field labels present but filled with conclusion-only text or boilerplate passes the structural scan while delivering no analytical value — the same bypass path that necessitated Schema A's CONTENT REJECTION CLAUSE. C-30 closes this parallel gap: the content quality of UX tier fields is subject to the same read-time enforcement as Schema A cells.* The Format Contract declares a CONTENT REJECTION CLAUSE for Schema B that is distinct from the STRUCTURAL REJECTION CLAUSE: the CONTENT clause fires at read-time (field labels present but cell content is conclusion-only, generic, or non-specific to this tier's behavior) and names its own detection method and remediation separate from the structural scan. The document must contain four named rejection clauses in total — Schema A STRUCTURAL, Schema A CONTENT, Schema B STRUCTURAL, Schema B CONTENT — each with its own (a) clause name, (b) detection method, and (c) remediation. A Format Contract that applies both clause types to Schema A but only STRUCTURAL to Schema B does not pass. The Terminal Reconciler's Schema B audit (check d from C-27) must cover both the structural scan and the content scan, producing findings against either clause type. Requires C-27. |
| C-31 | **Gate Item (e) Analytical Independence from Verdict** | format | aspirational | *R10 excellence signal: V-01 C-29 FAIL evidence revealed "Item (e) checks against Verdict Claim (d), not the Role 4 burst path audit count directly." When gate item (e) — tier-count verification — cites Verdict Claim (d) instead of the burst path audit section, the gate has a circular dependency on the Verdict block: the gate verifier cannot confirm the correct tier count without consulting the Verdict, which may itself be stale or incorrect. C-29 requires item (e) to verify against "the burst path audit section (not merely the two-tier minimum)" but does not explicitly prohibit citing the Verdict as the resolution source. C-31 closes this gap: item (e) must be analytically independent of the Verdict — its resolution path must lead to the analysis section where burst paths are enumerated, not to a Verdict claim about that count. This parallels the motivation behind C-22 (gate-checkpoint Verdict currency): a mechanism that depends on the Verdict to verify itself cannot catch Verdict staleness.* Gate item (e) names the specific section, role, or label in the analysis body where burst paths are enumerated (e.g., "verify tier count against Role 4 burst path audit" or "verify against Section 3 burst path table") — not Verdict Claim (d) or any other Verdict-block item. A gate item (e) that reads "tier count matches Verdict Claim (d)" or "consistent with stated tier count in Verdict" does not pass, even if the Verdict itself is current. The gate verifier must be able to confirm tier-count correctness by examining the analytical section without consulting the Verdict block. A gate that phrases item (e) in terms of the Verdict and also provides a cross-reference to the analysis section satisfies independence only if the analysis-section cross-reference is the stated resolution path — a Verdict reference that appears alongside the analysis reference as an alternative source does not pass. Requires C-28, C-29. |
| C-32 | **Pre-reconciler Verdict Currency Role** | format | aspirational | *R11 excellence signal: V-01 Signal 1. C-22 requires revision markers to be inserted at gate boundaries (forward enforcement). C-24 requires the terminal reconciler to verify markers are present (backward enforcement). V-01's Role 9 is a dedicated named role — "VERDICT CURRENCY CHECK" — positioned between the last analysis section and the terminal reconciler. Its sole function is to retroactively insert any REVISED markers that were deferred rather than inserted at the originating gate boundary, annotating each deferred insertion distinctly. This converts the terminal reconciler's check (a) from a mixed correction-and-verification operation into a pure verification operation: the reconciler confirms that markers are correctly placed rather than discovering omissions and performing insertions. The pre-reconciler sweep and the reconciler audit are complementary, not redundant: forward gate-currency checks (C-22) insert markers during analysis; the pre-reconciler role (C-32) sweeps for any that slipped past gate enforcement; the terminal reconciler (C-24) confirms the sweep was complete. A document that relies on the terminal reconciler's check (a) to perform deferred insertions has no independent verification that check (a) itself executed correctly.* The document contains a dedicated named role positioned after the last analysis section and before the terminal reconciler whose explicit function is to sweep all prior sections for REVISED-marker gaps. The role must: (a) be explicitly named as a currency check, pre-reconciler sweep, or equivalent — an analysis role that incidentally corrects markers does not pass; (b) annotate each deferred marker insertion distinctly from markers inserted at gate boundaries (e.g., "deferred from Role N"); (c) produce a named output recording the count of deferred insertions (e.g., "0 deferred markers inserted" or an itemized list). A document that satisfies C-18 and C-22 solely via the terminal reconciler's check (a) — performing all deferred insertions there — does not pass C-32. Requires C-22, C-24. |
| C-33 | **Four-Check Reconciler Completeness** | format | aspirational | *R11 PARTIAL gap: V-01 C-24 PARTIAL (check (c) absent) and C-27 PARTIAL (check (d) absent) both trace to Role 10 ending at check (b). C-24 requires three checks — (a) Verdict marker audit, (b) gate deliverable audit, (c) DERIVATION CHAIN arithmetic cell audit. C-27 requires a fourth — (d) Schema B structural scan — as a separately named check distinct from (b). V-01's Role 10 demonstrates that a terminal reconciler satisfying C-18 and C-21's individual requirements can still leave two entire audit surfaces — arithmetic completeness and UX template structure — without retroactive coverage. C-33 requires all four checks present as separately enumerated audit items, closing both PARTIAL gaps simultaneously. The distinction from C-24 and C-27 individually: C-24 is satisfied by any reconciler with checks (a), (b), (c) regardless of whether check (d) is present; C-27 is satisfied by any reconciler with check (d) regardless of whether checks (a)-(c) are complete; C-33 is satisfied only when all four checks are present and separately named.* The terminal reconciler section contains all four checks as separately enumerated named audit items: (a) Verdict revision marker scan — each revised claim carries an inline REVISED marker with forward reference to the revising section; (b) gate deliverable audit — each gated transition's named prior-section deliverables are confirmed present; (c) DERIVATION CHAIN cell audit — every DERIVATION CHAIN cell in all Schema A tables is scanned and any cell containing only a conclusion without computation steps is flagged as a Schema A CONTENT violation; (d) Schema B structural scan — every UX tier block is scanned and any block missing one or more of the four field labels is flagged as a Schema B STRUCTURAL violation. A reconciler with only checks (a) and (b) does not pass. A reconciler that combines checks (c) and (d) into a single composite audit item does not pass — they must appear as separately enumerated items with separate findings outputs. The reconciler must produce a named finding record for each of the four checks. A reconciler that passes C-24 and C-27 individually but combines (c) and (d) into one item fails C-33. Requires C-24, C-27. |
| C-34 | **Pure-Verification Reconciler Check (a)** | format | aspirational | *R12 excellence signal: V-01 and V-05 are the first variations to achieve 120/120, both implementing the full three-layer currency stack: C-22 (gate-checkpoint insertion) → C-32 (pre-reconciler deferred sweep) → C-33 check (a) (terminal audit). V-02 demonstrates that C-33 is satisfiable without C-32 — but at the cost of making terminal reconciler check (a) a mixed correction+verification operation: the reconciler discovers omissions and performs insertions in the same pass, then records those insertions as its findings output. The problem this creates: when check (a) both corrects and records, there is no independent prior record against which to verify the reconciler's own work. If the reconciler misses a marker gap — performing an incomplete sweep — its findings output reflects only what it found, with no external reference to confirm completeness. When C-32 precedes the reconciler, the pre-reconciler's CURRENCY SWEEP FINDINGS record is an independent prior artifact: it was produced before the reconciler ran, by a separate named role. Terminal reconciler check (a) can then operate as pure verification — cross-referencing the pre-reconciler's record against the current Verdict block state — rather than as a discovery-and-correction agent. This creates an audit trail where two independent passes (C-32 sweep and C-33 check (a)) would both have to fail simultaneously to leave a marker gap undetected. C-32 and C-33 are individually satisfiable without this property; C-34 requires all three layers to be simultaneously active and for check (a) to explicitly operate in verification-only mode, making the three-layer defense-in-depth observable in the reconciler's findings record rather than merely structurally implied.* The terminal reconciler's named output for check (a) explicitly distinguishes verification mode from correction mode and declares that no marker insertions were performed during the reconciler pass — all REVISED markers were already present in the Verdict block before the reconciler ran. The check (a) finding record must: (i) reference the pre-reconciler role's CURRENCY SWEEP FINDINGS record by name as the prior sweep being confirmed; (ii) enumerate each REVISED marker found in the Verdict block and confirm the forward reference to the revising section is present and correct; (iii) state explicitly the count of new insertions performed during the reconciler pass as zero. A check (a) finding that records "inserted N markers" or "discovered and corrected N missing markers" during the reconciler pass does not pass, even if N is small. A check (a) finding that records "0 violations" without (a) referencing the pre-reconciler sweep record and (b) explicitly stating insertion count zero does not pass — the verification-only mode must be explicitly declared, not inferred from a zero-violation count alone. A document that satisfies C-32 and C-33 individually but whose check (a) makes no reference to the pre-reconciler sweep record does not pass C-34: the independent prior must be cited, not merely implied by structural ordering. Requires C-22, C-32, C-33. |

---

## Scoring

```
composite = (essential_pass / 5 * 60)
          + (recommended_pass / 3 * 30)
          + (aspirational_pass / 26 * 30)
```
