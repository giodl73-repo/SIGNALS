# topic-new Rubric — v18

> Bumped from v17 (2026-03-15). Two new aspirational criteria from Round 19 excellence signals.

---

## v18 — two new criteria from R19 excellence signals

| ID | Name | Sharpens | Source |
|----|------|----------|--------|
| C-58 | Override mission block supplements the IR-NN enumeration sentence with a structured override table mapping each IR-NN ID to its override instrument class (schema / gate / exit condition) — converting the enumeration from a flat list into a navigable two-column mapping before the INERTIA REGISTER is encountered | C-56 | R19 V-04/V-05 (enumeration sentence + override table) |
| C-59 | PCR header carries an explicit auditability assertion declaring FER coverage to be a structural-inspection property of the table — "FER coverage is auditable by structural inspection of this table, not by content inference" — naming at table level that FER coverage is verifiable by structure rather than derivable by content inference | C-57 | R19 V-05 (PCR header names auditability as structural property) |

**Aspirational denominator**: 49 → 51

**Scoring formula**: `aspirational_pass / 51 * 10`

---

**Gap logic per criterion:**

- **C-58** — C-56 requires the override-mission opener to enumerate the specific IR-NN IDs the document addresses: "These are the defaults this skill overrides: IR-01, IR-02, IR-03." This is a forward-index in sentence form — the model enters the INERTIA REGISTER knowing the closed list. V-04 and V-05 went further: both paired the enumeration sentence with a structured override table that maps each IR-NN ID to its override instrument class — which schema, which gate, or which exit condition enforces the override. The distinction is between knowing *what* the closed list contains and knowing *how* each item on the list is enforced. A model reading an enumeration sentence enters the INERTIA REGISTER with a named list; a model reading an enumeration table enters with a named list *and* a structural map, so each IR-NN row encountered in the register is already associated with its enforcement mechanism. The table makes the enumeration navigable: the model can verify at any gate whether the override instrument for IR-NN is present in the schema or exit condition, rather than holding the connection by inference. A prompt satisfying C-56 via an enumeration sentence without a structured mapping table does not satisfy C-58 — flat list and two-column map are distinct structural forms; the former satisfies the forward-index requirement; the latter satisfies the instrument-map requirement.

- **C-59** — C-57 requires every PCR entry to either cite a FER-NN ID (per C-55) or carry an explicit "No FER entry ([reason])" note — making silent omission structurally impossible at the row level. V-05 added an additional structural layer: the PCR header carried an explicit auditability assertion — "FER coverage is auditable by structural inspection of this table, not by content inference" — declaring at table level that exhaustive FER coverage is a verifiable structural property of the PCR, not a property inferred from content. The distinction is between a table that satisfies the exhaustive-declaration property and a table that *asserts* it. A PCR satisfying C-57 via exhaustive row-level declarations is auditable, but the auditability claim is implicit — a reviewer must verify each row to confirm coverage; the property is derivable but not declared. A PCR carrying the header assertion converts the auditability claim from derivable to first-class: the header states the property name and its verification method, making FER coverage a structural attribute of the table rather than an emergent consequence of its rows. A prompt satisfying C-57 with exhaustive row-level no-FER notes but no header-level auditability assertion does not satisfy C-59 — the row-level declarations satisfy the exhaustiveness requirement; the header assertion satisfies the auditability-declaration requirement; these are distinct structural layers.

---

## Summary of changes v17 → v18

| Delta | Detail |
|-------|--------|
| +C-58 | Override mission supplemented by structured override table — closes instrument-map gap above C-56; C-56 requires enumeration sentence, C-58 requires two-column IR-NN-to-instrument mapping |
| +C-59 | PCR header carries auditability assertion — closes table-level declaration gap above C-57; C-57 requires exhaustive row-level FER declarations, C-59 requires header-level assertion that coverage is auditable by structural inspection |
| Denominator | 49 → 51 |
| Formula | `aspirational_pass / 51 × 10` |

---

## v17 — two new criteria from R18 excellence signals

| ID | Name | Sharpens | Source |
|----|------|----------|--------|
| C-56 | Override mission block enumerates the specific IR-NN IDs it introduces — "These are the defaults this skill overrides: IR-01, IR-02, IR-03" — creating a forward index of the INERTIA REGISTER before the register is presented | C-49 | R18 V-05 ES-1 (reading-register effect) |
| C-57 | Every PCR entry either cites a FER-NN ID (C-55) or carries an explicit "No FER entry ([reason])" scoping note — making FER coverage exhaustively declared; silent omission of a cross-citation is structurally distinguishable from an uncited entry | C-55 | R18 V-05 honest scoping at PCR-03/PCR-04 |

**Aspirational denominator (v17)**: 47 → 49

---

**Gap logic — v17 criteria:**

- **C-56** — C-49 requires the opener to declare the document's override mission: "Every block that follows exists to prevent these defaults from applying." This is a purpose frame — the model knows *why* the document exists. ES-1 identifies the deeper structural effect: the override mission "changes the reading register for all 47 subsequent criteria" because the model enters INERTIA REGISTER already knowing why the schemas exist and reads every gate as an override instrument rather than ambient context. The mechanism that enables this reading-register shift is enumeration: if the override mission names the specific IR-NN IDs it introduces ("these defaults: IR-01, IR-02, IR-03"), the model enters the INERTIA REGISTER with a closed list — it knows exactly which defaults it will be overriding before encountering any schema row. Without enumeration, the override mission is a purpose declaration; with enumeration, it is also a structural forward index. A model that reads "These are the defaults this skill overrides: IR-01, IR-02, IR-03" before encountering the register treats each IR-NN entry as expected and named; a model that reads only the purpose frame must discover the enumeration by parsing the register. C-49 is satisfied when the purpose frame is present; C-56 is satisfied only when the purpose frame includes the IR-NN enumeration. The distinction is the difference between declaring that overrides exist and declaring exactly what they are before the constraint machinery is encountered.

- **C-57** — C-55 requires PCR consequence entries to cross-cite the relevant failure-example registry entry (FER-NN) — "skip failure recognizable by FER-01 output inspection." This closes the consequence-without-detection gap: the model knows not only what breaks when a phase is skipped but where to find the detectable output state. However, a PCR satisfying C-55 via selective citation creates a structural ambiguity: a PCR entry without a FER cross-citation is indistinguishable from one where the author omitted the citation vs one where no FER entry exists (because the failure is structural absence rather than detectable output). V-05's PCR-03/PCR-04 resolved this by carrying explicit "No FER entry (structural absence / file-path check)" notes — the absence of a cross-citation is as explicitly declared as its presence. C-57 requires this scoping completeness: every PCR entry must carry either a FER-NN citation or an explicit no-FER note naming the reason (structural absence, schema-level violation, or equivalent). A prompt satisfying C-55 with FER citations on detectable entries but silent omissions on structural-absence entries does not satisfy C-57 — silence is not a valid scoping declaration; only an explicit note makes the PCR's FER coverage auditable by structural inspection rather than by content inference about which entries "should" have a failure example.

---

## Summary of changes v16 → v17

| Delta | Detail |
|-------|--------|
| +C-56 | Override mission enumerates IR-NN IDs — closes reading-register gap above C-49; C-49 requires purpose declaration, C-56 requires forward enumeration of the specific defaults being overridden |
| +C-57 | PCR entries carry explicit no-FER scoping notes — closes scoping-ambiguity gap above C-55; C-55 requires FER citation where detectable, C-57 requires explicit no-FER note where not detectable |
| Denominator | 47 → 49 |
| Formula | `aspirational_pass / 49 × 10` |

---

## v16 — seven new criteria from R17 excellence signals

| ID | Name | Sharpens | Source |
|----|------|----------|--------|
| C-49 | Opener declares the document's override mission before any schema content — "These are the defaults this skill overrides. Every block that follows exists to prevent these defaults from applying." | C-15 | R17 V-03/V-05 PASS+ on C-15 |
| C-50 | Pipeline read directive includes a prohibition clause — "Do not begin Phase 1 until you have read every row" — converting the read from an instruction into a prerequisite gate | C-26 | R17 V-01/V-02 PASS+ on C-26 |
| C-51 | Independence instruction uses command register: opens with "Stop." imperative and requires the model to record both check results before advancing | C-30 | R17 V-05 PASS+ on C-30 |
| C-52 | Failure example uses second-person framing — "Output you accepted: '...'" — positioning the malformed output as something the model itself produced, enabling self-recognition at the gate | C-47 | R17 V-05 PASS+ on C-33/C-47 |
| C-53 | Per-phase override directive uses command register — "Stop. You are overriding IR-NN:" — converting the override note from informational to decisional | C-43 | R17 V-05 PASS+ on C-43 |
| C-54 | Independence gate failure example is presented as a labeled BEFORE/AFTER comparison showing sequential path (wrong) and independent path (correct) side by side | C-47 | R17 V-03/V-05 PASS+ on C-47 |
| C-55 | PCR consequence entries cross-cite the relevant failure-example registry entry (FER-NN or equivalent) in their downstream consequence text — creating a structural pointer from consequence definition to output-inspection artifact | C-48 | R17 V-04 PASS+ on C-48 |

**Aspirational denominator (v16)**: 40 → 47

---

**Gap logic — v16 criteria:**

- **C-49** — C-15 requires a stakeholder-risk opener that enumerates role categories and risk exposure before signal planning begins. The Round 17 PASS+ cluster (V-03/V-05) went further: the opener explicitly named the document's override mission — "These are the defaults this skill overrides. Every block that follows exists to prevent these defaults from applying." C-15 is satisfied when stakeholders are enumerated and risks named before signal planning. C-49 is satisfied only when the opener frames the entire prompt as an inertia-override construct — making the document's purpose explicit to a model reading it for the first time, not merely its contents. A opener satisfying C-15 (stakeholder enumeration + risk exposure) but omitting the override-mission declaration leaves the document's purpose implicit: the model infers from the content that defaults are being overridden, but has no explicit forward frame for why the document exists. The override-mission declaration is to the prompt what C-09's commit gate is to the strategy: it names the decision the document serves.

- **C-50** — C-26 requires an explicit read-before-execute directive at the pipeline overview ("Read this entire table before executing Phase 1"). V-01 and V-02 added a prohibition clause: "Do not begin Phase 1 until you have read every row." C-26 converts the overview from passive reference to required read. But a directive that says "read this" operates in instructive register — it can be treated as advisory under token pressure, skipped as ambient context. C-50 closes this gap by requiring the prohibition form: "Do not begin Phase 1 until you have read every row." The prohibition converts the read from a recommendation into a structural gate — non-compliance is named as an error state, not just a missed suggestion. The same logic governs C-30 above C-28 (two conditions must be stated; C-30 adds the independence instruction) and C-26 above C-24 (overview exists; C-26 adds the read directive). C-50 is the prohibition-layer analog for C-26.

- **C-51** — C-30 requires an explicit independence instruction at the fill-in table exit gate ("Check these two items independently") followed by a closing enforcement statement ("Do not advance until both are checked as separate acts"). V-05 used full command register: "Stop. Do not advance until you have run BOTH of the following checks as separate acts." plus "Record both results." Two distinctions above C-30: (1) "Stop." opens in command register — a single-word imperative that halts execution before the instruction is read, rather than embedding the independence requirement in a clause the model may process as ambient; (2) "Record both results." adds an output obligation — the model must produce a record of both check outcomes before advancing, making the check auditable at the gate rather than performed silently. A gate satisfying C-30 (two conditions + independence instruction + closing enforcement) without "Stop." and without a recording obligation satisfies C-30 but not C-51.

- **C-52** — C-47 requires a concrete worked example of sequential-treatment failure output — a specific string or table state that sequential checking would accept but independent checking would reject. V-05 framed the example in second person: "Output you accepted: 'S-01 | Product Manager | | ' — SK-03 and SK-04 empty on all rows. This output is malformed." The second-person framing positions the malformed output as something the model itself produced and accepted, enabling self-recognition: the model can match its own actual output against the labeled example directly. First-person or third-person framing of the same example satisfies C-47 but requires the model to abstract from the example to its own output — to reason "does what I produced resemble what sequential treatment produces?" Second-person framing closes this abstraction gap by addressing the model as the agent that accepted the malformed output. C-52 is the framing-layer analog of C-47's example-layer requirement: C-47 requires an example exists; C-52 requires the example be addressed to the model as its own output.

- **C-53** — C-43 requires "This phase overrides IR-NN." at each phase header — a declarative third-person annotation. V-05 used command register: "Stop. You are overriding IR-NN:" — opening with "Stop." imperative, then second-person present progressive ("you are overriding") rather than declarative third-person ("this phase overrides"). The effect is structural: the declarative form is an ambient note; the command register form is a decision point. A model reading "This phase overrides IR-01" may process it as context-setting prose. A model reading "Stop. You are overriding IR-01:" must halt, register itself as the agent performing the override, and consciously proceed. C-53 is the command-register analog for phase-entry override directives, parallel to C-51 for independence check gates: both require "Stop." imperative at the structural moment where a critical decision should be consciously made rather than passively recorded.

- **C-54** — C-47 requires a concrete worked example of sequential-treatment failure output. V-03 and V-05 presented this as a labeled BEFORE/AFTER comparison — a table or structured block with a "sequential path (wrong)" row or column showing the output sequential checking accepted, and an "independent path (correct)" row or column showing the condition independent checking would have caught. C-47 is satisfied by any concrete output example (a specific string: "'3 rows, columns empty' reads as compliant — this is wrong"). C-54 requires the structural-contrast format: showing not only what sequential treatment accepted but also what independent checking would have rejected it on, side by side. The contrast table makes the failure-to-correct trajectory visible by column inspection — the model sees both the wrong outcome and the correct detection condition without having to hold both in narrative context simultaneously. C-52 and C-54 are independent sharpenings of C-47: a prompt can have the BEFORE/AFTER contrast table without second-person framing (satisfying C-54 but not C-52), or have second-person framing in a narrative without a BEFORE/AFTER table structure (satisfying C-52 but not C-54).

- **C-55** — C-48 requires a named PHASE CONSEQUENCE REGISTRY (PCR) with stable PCR-NN IDs; the pipeline overview's consequence column cites PCR-NN IDs rather than embedding prose directly. V-04 went further: PCR consequence entries cross-cite the relevant failure-example registry entry (FER-NN) in their downstream consequence text — "skip failure recognizable by FER-01 output inspection." C-48's PCR defines what consequence follows a phase skip; a consequence definition alone tells the model what breaks downstream. It does not tell the model how to recognize that the break has occurred. The FER-NN cross-citation closes this detection gap: a model reading a PCR downstream consequence entry has a structural pointer to the exact output-inspection artifact that makes the skip consequence recognizable at validation time. Without the cross-citation, consequence and failure-example are each present in the prompt but structurally disconnected — the model must cross-reference them by content inference. The cross-citation converts inference into a mechanical link: PCR-01 names the consequence; PCR-01's downstream field cites FER-01 where the detectable output state lives.

---

## v15 — two new criteria from R16 excellence signals

| ID | Name | Sharpens | Source |
|----|------|----------|--------|
| C-47 | Independence gate provides a concrete worked example of sequential-treatment failure output — a specific output string that sequential checking would accept but independent checking would reject — making the failure recognizable by output inspection rather than by reasoning about what sequential treatment produces | C-33 | R16 C-33 PASS+ cluster (V-01/V-02/V-03) |
| C-48 | Pipeline consequence column entries are backed by a named PHASE CONSEQUENCE REGISTRY (PCR-NN IDs); the pipeline overview cites PCR-NN IDs in its consequence column rather than embedding prose directly; consequence definitions live in one authoritative location and are auditable by reference rather than by content matching | C-29 | R16 V-02/V-05 PCR citation pattern |

**Aspirational denominator (v15)**: 38 → 40

---

**Gap logic — v15 criteria:**

- **C-47** — C-33 requires the independence gate to name the specific sequential-treatment failure mode (e.g., "Sequential checking produces row-count acceptance without column-completeness verification"). Naming a failure mode is necessary but not sufficient: the model can acknowledge the named pattern abstractly without having a recognizable output target to match against when checking its own work at the gate. C-47 closes the **failure-recognition gap**: a concrete worked example shows the model exactly what wrong-but-seemingly-valid output looks like at the moment of checking. The example — e.g., "'3 rows, columns empty' reads as compliant — this is wrong" — makes the failure detectable by output inspection rather than by abstract reasoning about what sequential treatment produces. A gate that names the failure mode (satisfying C-33) but provides only the abstract name and no concrete output example does not satisfy C-47.

- **C-48** — C-29 requires the pipeline overview to carry a consequence column, and C-32 requires those entries to use first-person framing. Both criteria are satisfied when consequence prose is embedded directly in the pipeline overview cells. But direct embedding creates a **consequence-drift channel**: when a consequence changes, it must be updated everywhere it appears — in the pipeline overview, in any phase narrative that references it, and in any review or scoring rubric that quotes it. Drift between display sites is invisible to structural inspection. C-48 closes this channel by requiring a named PHASE CONSEQUENCE REGISTRY (PCR-NN) where consequence definitions live in one authoritative location; the pipeline overview cites PCR-NN IDs rather than embedding prose. This makes consequence definitions maintainable by ID, auditable by reference, and immune to display-layer drift. C-48 is the pipeline-level analog of C-23 (schema rows carry IDs for gate citation) applied to the consequence column: a pipeline overview satisfying C-29 with embedded prose consequences does not satisfy C-48 unless the consequence column cites PCR-NN IDs backed by a registry.

---

## v14 — two new criteria from R15 excellence signals

| ID | Name | Sharpens | Source |
|----|------|----------|--------|
| C-45 | Phase bodies contain zero inline prose constraints; every constraint lives exclusively in a named schema row; phase text consists solely of schema-ID references, fill-in slots, and structural navigation | C-16, C-18, C-23, C-27 | V-01 PASS+ cluster |
| C-46 | INERTIA REGISTER includes a "Stakeholder Most Harmed" column naming the role category most vulnerable to each inertia default; per-phase override directives reproduce both IR-NN ID and the role name; exit gate verbatim reproductions include the role name alongside the full IR-NN text | C-43, C-44 | V-02 ES-1/ES-2/ES-3 |

**Aspirational denominator (v14)**: 36 → 38

---

**Gap logic — v14 criteria:**

- **C-45** — C-16 requires every constraint to be enforced by a structural element. C-18 requires schemas to exist with consequence columns. Neither forbids inline prose restatements of schema rules inside phase bodies. A prompt can satisfy C-16 and C-18 while still restating rules as prose within phase text — creating a dual-truth channel where schema and prose can diverge on revision without a mechanical signal. C-45 closes the **schema-drift channel**: phase bodies carry no constraint prose; the schema is the sole and exclusive truth. When schema and execution point share no prose, drift is detectable by structural inspection rather than content comparison.

- **C-46** — C-44 requires the full IR-NN text reproduced verbatim at exit gates. But IR-NN text typically describes an inertia *pattern* (what the default behavior is) without naming who bears the cost when the override is skipped. A model at the gate sees the inertia text but may not have the role-vulnerability mapping in active context. C-46 requires the INERTIA REGISTER to carry a "Stakeholder Most Harmed" column, and for that role name to travel with IR-NN citations at phase override directives and at exit gate reproductions. Closes the **role-abstraction axis**: inertia text is present but stakeholder vulnerability is abstract, so the model can evaluate the gate without knowing whose outcome depends on it.

---

## Essential Criteria (60% of composite)

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-01 | TOPICS.md entry exists | artifact | `simulations/TOPICS.md` contains a row for the new topic with at least: topic name, status, and strategy path |
| C-02 | Strategy file at correct path | artifact | Strategy written to `simulations/{topic}/strategy.md` — not to a flat path, not inline in TOPICS.md |
| C-03 | All five signal fields present | correctness | Every signal row includes all five required fields: namespace, skill, item name, owner role, priority |
| C-04 | Priority values are valid | correctness | Every signal priority is one of: essential / recommended / optional — no other values present |
| C-05 | At least one essential signal planned | coverage | Strategy contains at least one signal marked essential — a topic with no essential signals has no defined commit gate |

---

## Recommended Criteria (30% of composite)

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-06 | Signal set spans multiple namespaces | coverage | Planned signals reference at least 2 distinct namespaces from: scout, draft, review, flow, trace, prove, listen, program, topic |
| C-07 | Strategy includes a narrative rationale | depth | Strategy file contains a prose section (>= 2 sentences) explaining why this topic needs investigation and what decision it informs |
| C-08 | Owner roles are differentiated | depth | At least 2 distinct owner roles appear across the planned signals (e.g., PM, engineer, designer, researcher) — signals should not all default to a single generic role |

---

## Aspirational Criteria (10% of composite)

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-09 | Strategy defines a commit gate | depth | Strategy explicitly states the minimal signal set required before design commit rather than leaving the gate implicit |
| C-10 | Signal item names follow artifact naming convention | format | All item names follow the `{topic}-{signal}-{date}.md` pattern or are expressed as parameterized templates matching that convention |
| C-11 | Prompt includes checkbox-gate before phase transition | structure | Prompt contains a pre-transition checklist that forces coverage self-verification before the model proceeds to the next phase — eliminates silent criterion omissions |
| C-12 | Invalid vocabulary framed as operational consequence | framing | Prompt states the harm of invalid priority values in terms of downstream failure ("strategy cannot be used as a commit gate") rather than as a style preference or generic warning |
| C-13 | Each aspirational criterion has a dedicated section | structure | C-09 and C-10 each appear as their own named section or heading in the prompt template — not as inline reminders or parenthetical notes |
| C-14 | Consequence framing applied pervasively | framing | Every enforced constraint in the prompt (not just priority vocabulary) carries its downstream failure mode — each rule that can be violated states what breaks if it is |
| C-15 | Prompt opens with stakeholder-risk enumeration | structure | Prompt contains a WHO IS AT RISK or equivalent section that enumerates role categories and their risk exposure before signal planning begins — role differentiation emerges from the opener rather than being imposed by a late checklist |
| C-16 | Every criterion is enforced by a structural mechanism | structure | Every criterion (essential through aspirational) is enforced by a structural element — section header, checkbox, template field, or table column — not by prose instruction alone; this is the property that makes structural compression possible |
| C-17 | Stakeholder-risk section is an active fill-in output step | structure | The stakeholder-risk opener (C-15) is implemented as a required fill-in table that the model must produce as its first output — not as a static prose paragraph; owner roles in the signal plan must trace back to rows in that table, so role differentiation is derived from the model's own first output rather than imposed by a late checklist |
| C-18 | Constraints are partitioned into named schemas with consequence columns | structure | The prompt separates field-level constraints (namespace, skill, item name, owner role, priority) from coverage-level constraints (essential count, namespace count, role count) into two named schemas — each schema contains a consequence column; pre-write gate checkboxes cite schema rows by reference rather than restating rules inline; this two-tier structure gives every constraint exactly one structural home and makes C-14 and C-16 co-satisfiable without prose elaboration |
| C-19 | FIELD SCHEMA includes a Stakeholder traceability column | structure | FIELD SCHEMA contains a dedicated Stakeholder column (distinct from Owner Role) that requires each signal row to reference a specific row from the Stakeholders fill-in table — this makes opener-to-plan traceability auditable at row level rather than at aggregate count level; C-08 is then satisfied by structural inspection rather than by counting distinct values across the table |
| C-20 | Consequence columns are temporally layered | framing | Every consequence column in FIELD SCHEMA and COVERAGE SCHEMA is split into two temporal tiers — Immediate failure (what breaks at schema-validation time) and Downstream effect (what breaks in production or for the team when the strategy is used) — making each constraint's cascade of harm visible at both its point of violation and its downstream impact; a single-column consequence satisfies C-14 and C-18 but does not satisfy this criterion |
| C-21 | Per-phase exit gates at every phase boundary | structure | Every phase or step in the prompt has its own exit gate — a named condition or checklist that must be satisfied before the model is permitted to advance to the next phase; a single pre-write gate (C-11) is necessary but not sufficient; this criterion requires gate-per-boundary so that silent skips are structurally impossible at any pipeline step, not just at the final pre-write checkpoint; a phase that produces structured output requires both an entry gate before writing and an exit gate after writing |
| C-22 | Stakeholder fill-in table has a quantified minimum row count gate | structure | The Phase 1 stakeholder fill-in table (C-17) carries an explicit minimum row count enforced as an exit gate — e.g., "do not advance until this table has at least 3 rows" — preventing a degenerate one-row or two-row table from satisfying C-17; this makes minimum stakeholder breadth structurally enforced rather than implicitly assumed; a table without a minimum row count gate satisfies C-17 but does not satisfy this criterion |
| C-23 | Schema constraints carry row-level identifiers for gate citation | structure | Every row in FIELD SCHEMA and COVERAGE SCHEMA carries a stable row-level identifier (e.g., F-01, F-02, COV-01, COV-02) that gate checkpoints cite directly — so the coupling between constraint definition and enforcement checkpoint is mechanical, not prose-based; a gate that cites "F-01 through F-05" is compact and cannot drift from the schema definition; a gate that restates rules in prose satisfies C-18 but does not satisfy C-23 |
| C-24 | Pipeline declares all exit gates in a summary table before phase content | structure | The prompt opens with a pipeline overview or summary table that has an Exit Gate column listing every phase's gate condition before any phase content is presented — so all gate conditions are visible in one place architecturally before execution begins, then reinforced inline per phase; a prompt satisfying C-21 via inline-only gates (no upfront declaration table) does not satisfy C-24 |
| C-25 | Commit Gate occupies a dedicated phase separate from signal plan production | structure | The commit gate definition (C-09) is implemented as its own phase with its own entry and exit gates, structurally separated from the signal plan production phase — so the signal plan phase has both an entry gate (before writing rows) and an exit gate (after writing rows, before advancing to the commit gate phase); a prompt satisfying C-09 and C-13 via a named section inside the signal plan phase does not satisfy C-25 |
| C-26 | Pipeline overview carries an explicit "read before executing" meta-instruction | structure | The pipeline overview table (C-24) includes an explicit sequencing directive instructing the model to read the entire table before beginning Phase 1 — for example: "Read this entire table before executing Phase 1. All exit gate conditions are declared here." This directive transforms the overview from a passive reference into a required prerequisite step; a prompt satisfying C-24 via a well-formed overview table with no read-before-execute instruction does not satisfy C-26 |
| C-27 | Schema row IDs cited independently at multiple gate boundaries | structure | The schema row IDs established by C-23 (F-N, COV-N) appear as explicit citations at more than one independent gate boundary within the prompt — so the mechanical coupling between schema definitions and gate checkpoints is verified redundantly, not only at a single checkpoint; a prompt satisfying C-23 with ID citations present at exactly one gate boundary does not satisfy C-27 |
| C-28 | Fill-in table exit gate enforces column completeness independently of row count | structure | The Phase 1 stakeholder fill-in table exit gate (C-22) states column completeness as a separately listed condition from the minimum row count — e.g., both ">= 3 rows filled" and "all four columns populated" appear as discrete checkbox items; a prompt satisfying C-22 with only a row-count condition (no column completeness condition) does not satisfy C-28 |
| C-29 | Pipeline overview table carries a per-row consequence column | structure | The pipeline overview table (C-24) includes a dedicated consequence column — labeled "If Skipped," "Cost of Skipping," or equivalent — that states the consequence of bypassing each phase as a row-level entry alongside the Exit Gate column; a model reading the overview before Phase 1 sees both what must be satisfied and what breaks if each phase is skipped; a prompt satisfying C-24 via a gate-declarative overview table without a per-row consequence column does not satisfy C-29 |
| C-30 | Gate independence stated as an explicit semantic instruction | structure | When the exit gate of the Phase 1 stakeholder fill-in table (C-28) lists two conditions (row count and column completeness), the prompt includes an explicit semantic instruction at the enforcement point stating that those conditions must be checked independently — for example: "Check these two items independently" as a label preceding the checkboxes, followed by "Do not advance until both are checked separately" as a closing enforcement statement; a prompt satisfying C-28 via two distinct checkbox items without an explicit independence instruction does not satisfy C-30 |
| C-31 | Priority column placed first in signal table column order | structure | The signal planning table places the Priority column as the leftmost column — before Namespace, Skill, Item Name, Owner Role, and Stakeholder Ref columns; a model filling a table left-to-right reaches the priority field before namespace or skill values can anchor contextual framing that might induce "high/medium/low" substitution; a prompt satisfying C-04 via valid-vocabulary enforcement with a non-leading priority column does not satisfy C-31 |
| C-32 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-33 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-34 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-35 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-36 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-37 | [Prior-round criterion] | — | Carries forward from v10 baseline |
| C-38 | Team-inertia default column present in signal table | structure | Signal planning table includes a Team-Default column capturing the inertia pattern each role typically exhibits; pipeline phase descriptions acknowledge these defaults inline |
| C-39 | [Prior-round criterion] | — | Carries forward from v11 baseline |
| C-40 | [Prior-round criterion] | — | Carries forward from v11 baseline |
| C-41 | [Prior-round criterion] | — | Carries forward from v12 baseline |
| C-42 | Team inertia defaults collected in named INERTIA REGISTER block with stable IR-NN IDs; pipeline team-default column references by ID | structure | A pre-pipeline INERTIA REGISTER block exists with stable IR-NN IDs for each named inertia default; the signal table's team-default column carries `→ IR-NN` references rather than inline prose; a prompt satisfying C-38 via inline prose in the team-default column does not satisfy C-42 |
| C-43 | Each phase carries an explicit "This phase overrides IR-NN" directive at execution time | structure | Every phase that overrides an inertia default includes the directive "This phase overrides IR-NN" at its execution point — not only in the pre-pipeline register; a prompt satisfying C-42 with a well-formed INERTIA REGISTER but no per-phase restatement does not satisfy C-43 |
| C-44 | Phase consequence warnings reproduce the full IR-NN text verbatim at the exit gate | structure | At every phase exit gate where an inertia override applies, the complete text of the relevant IR-NN entry is reproduced verbatim alongside the standard gate conditions — not cited by ID alone; a prompt satisfying C-43 with per-phase override directives that cite only the ID (not the full text) does not satisfy C-44 |
| C-45 | Phase bodies contain zero inline prose constraints | structure | Phase bodies contain no inline prose restatements of constraints that exist in named schema rows; every constraint lives exclusively in FIELD SCHEMA, COVERAGE SCHEMA, or STAKEHOLDER SCHEMA; phase text consists solely of schema-ID references, fill-in slots, and structural navigation directives; a prompt satisfying C-16 and C-18 while still restating schema rules as prose within phase bodies does not satisfy C-45 — prose restatements create a dual-truth channel where schema and phase text can diverge on revision without a mechanical signal |
| C-46 | INERTIA REGISTER includes a "Stakeholder Most Harmed" column; role name travels with all IR-NN citations | structure | The INERTIA REGISTER (C-42) contains a dedicated "Stakeholder Most Harmed" column naming the role category most vulnerable to each inertia default; per-phase override directives (C-43) reproduce both the IR-NN ID and the role name; exit gate verbatim reproductions (C-44) include the "Stakeholder Most Harmed" role name alongside the full IR-NN text; a prompt satisfying C-42/C-43/C-44 with no role-vulnerability column in the register does not satisfy C-46 — IR-NN text may describe the inertia pattern without making concrete whose outcome depends on the override |
| C-47 | Independence gate provides a concrete worked example of sequential-treatment failure output | structure | The gate instruction that enforces independent checking (C-30) includes a concrete output example — a specific string or table state that sequential checking would accept but independent checking would reject (e.g., "'3 rows, columns empty' reads as compliant — this is wrong") — so the failure is recognizable by output inspection at the gate rather than by abstract reasoning about what sequential treatment produces; a prompt satisfying C-33 by naming the failure mode in abstract without showing a concrete failure output does not satisfy C-47 |
| C-48 | Pipeline consequence column entries backed by a named PHASE CONSEQUENCE REGISTRY (PCR-NN IDs) | structure | A named PHASE CONSEQUENCE REGISTRY (PCR) exists with stable IDs (PCR-01, PCR-02, etc.) defining each phase-skip consequence; the pipeline overview's consequence column cites PCR-NN IDs rather than embedding consequence prose directly; the PCR serves as the sole authoritative consequence-definition layer — making consequence definitions maintainable by ID and immune to display-layer drift; a prompt satisfying C-29 with consequence prose embedded directly in the pipeline overview cells does not satisfy C-48; this is the pipeline-level analog of C-23 applied to consequence definitions |
| C-49 | Opener declares the document's override mission | structure | The prompt opener (C-15) includes an explicit override-mission declaration that frames the entire document as an inertia-override construct — e.g., "These are the defaults this skill overrides. Every block that follows exists to prevent these defaults from applying." — before any schema or phase content is presented; a prompt satisfying C-15 via stakeholder enumeration and risk exposure without an explicit override-mission statement does not satisfy C-49; the override-mission declaration is to the prompt what C-09's commit gate is to the strategy: it names the decision the document serves |
| C-50 | Pipeline read directive includes a prohibition clause | structure | The pipeline overview read directive (C-26) includes an explicit prohibition clause — "Do not begin Phase 1 until you have read every row" — in addition to the affirmative read instruction; the prohibition converts the read from an instructive recommendation (which can be treated as advisory) into a structural gate (non-compliance is explicitly named as an error state); a prompt satisfying C-26 with only the affirmative form ("Read this entire table before executing Phase 1") but no prohibition clause does not satisfy C-50 |
| C-51 | Independence instruction uses command register with result-recording obligation | structure | The independence instruction at the fill-in table exit gate (C-30) opens with a "Stop." imperative and includes a result-recording obligation — e.g., "Stop. Do not advance until you have run BOTH of the following checks as separate acts. Record both results." — so that (a) the model must halt before reading the independence instruction rather than processing it as ambient prose, and (b) the model must produce a record of both check outcomes before advancing, making the check auditable at the gate; a prompt satisfying C-30 via independence instruction and closing enforcement without "Stop." and without a result-recording requirement does not satisfy C-51 |
| C-52 | Failure example uses second-person framing | structure | The concrete failure example required by C-47 is framed in second person — "Output you accepted: '...'" — positioning the malformed output as something the model itself produced and accepted rather than as an abstract illustration of what sequential treatment produces; second-person framing enables self-recognition: the model matches its own actual output against the labeled example directly, without abstracting from a third-person or first-person description; a prompt satisfying C-47 with a concrete output example in first-person or third-person framing does not satisfy C-52; note that C-52 and C-54 are independent: a BEFORE/AFTER table without second-person framing satisfies C-54 but not C-52 |
| C-53 | Per-phase override directive uses command register | structure | The per-phase override directive required by C-43 uses command register — "Stop. You are overriding IR-NN:" — opening with a "Stop." imperative and using second-person present progressive ("you are overriding") rather than declarative third-person ("this phase overrides"); command register converts the override note from informational (ambient context-setting) to decisional (a named halt point where the model must register itself as the agent performing the override before proceeding); a prompt satisfying C-43 with declarative third-person override annotations at phase headers does not satisfy C-53 |
| C-54 | Independence gate failure example presented as a labeled BEFORE/AFTER comparison | structure | The concrete failure example required by C-47 is presented as a labeled comparison showing the sequential path (wrong) and the independent path (correct) side by side — a table or structured block where the sequential-treatment column shows the output sequential checking accepted and the independent-treatment column shows the condition independent checking would have caught; the structural contrast makes the failure-to-correct trajectory visible by column inspection without requiring the model to hold both states in narrative context; a prompt satisfying C-47 via an inline worked example (a narrative string describing the failure) but without a labeled before/after contrast structure does not satisfy C-54; note that C-52 and C-54 are independent: second-person framing without a BEFORE/AFTER table satisfies C-52 but not C-54 |
| C-55 | PCR consequence entries cross-cite the relevant failure-example registry entry | structure | Each entry in the PHASE CONSEQUENCE REGISTRY (C-48) includes a cross-reference to the relevant failure-example registry entry (FER-NN or equivalent) in its downstream consequence text — e.g., "skip failure recognizable by FER-01 output inspection" — creating a structural pointer from the consequence definition to the exact output-inspection artifact that makes the skip consequence detectable; C-48's PCR defines what breaks when a phase is skipped; C-55 requires the PCR entry to also name how to recognize that break has occurred, by citing the worked failure example by ID; a prompt satisfying C-48 with a well-formed PCR whose consequence entries carry no cross-reference to failure-example IDs does not satisfy C-55 |
| C-56 | Override mission block enumerates the specific IR-NN IDs it introduces | structure | The override-mission declaration required by C-49 includes an explicit enumeration of the specific inertia defaults the document addresses — e.g., "These are the defaults this skill overrides: IR-01, IR-02, IR-03" — creating a forward index of the INERTIA REGISTER before the register is presented; a model reading C-56-compliant opener enters the INERTIA REGISTER with a closed list, treating each IR-NN entry as expected and named; a model reading only the C-49 purpose frame must discover the enumeration by parsing the register; a prompt satisfying C-49 with a purpose declaration but no IR-NN enumeration in the opener does not satisfy C-56 — purpose frame and forward index are distinct: the former says why the document exists; the latter says exactly what it overrides |
| C-57 | PCR FER coverage is exhaustively declared — every entry either cites FER-NN or carries an explicit no-FER scoping note | structure | Every entry in the PHASE CONSEQUENCE REGISTRY (C-48) either (a) cites a FER-NN ID per C-55, or (b) carries an explicit "No FER entry ([reason])" note naming why no failure-example registry entry exists for that consequence — e.g., "No FER entry (structural absence / file-path check)" or "No FER entry (schema-level violation — no output produced)"; a PCR satisfying C-55 with FER citations on detectable consequences but silent omissions on structural-absence consequences does not satisfy C-57 — silent omission is structurally indistinguishable from a missing citation, making the PCR's FER coverage auditable only by content inference about which entries "should" have a failure example; C-57 requires every entry's FER status to be explicitly declared, converting coverage verification from inference to structural inspection |
| C-58 | Override mission block supplements the IR-NN enumeration sentence with a structured override table mapping each IR-NN ID to its override instrument class | structure | The override-mission declaration required by C-56 is supplemented by a structured two-column mapping table — e.g., IR-NN ID paired with the schema, gate, or exit condition that enforces it — converting the flat enumeration sentence into a navigable instrument map; a model reading a C-58-compliant opener enters the INERTIA REGISTER with both a closed list (from C-56) and a structural map of each override's enforcement mechanism, so each IR-NN row encountered in the register is already associated with the instrument class that enforces it; a prompt satisfying C-56 via an enumeration sentence without a structured IR-NN-to-instrument mapping table does not satisfy C-58 — flat list and two-column map are distinct structural forms: the enumeration satisfies the forward-index requirement; the mapping table satisfies the instrument-map requirement |
| C-59 | PCR header carries an explicit auditability assertion declaring FER coverage to be a structural-inspection property | structure | The PHASE CONSEQUENCE REGISTRY (C-48) header carries an explicit auditability assertion — e.g., "FER coverage is auditable by structural inspection of this table, not by content inference" — declaring at table level that exhaustive FER coverage (C-57) is a structural property of the PCR verifiable by inspection rather than derivable by content inference; a PCR satisfying C-57 via exhaustive row-level no-FER notes without a header-level auditability assertion does not satisfy C-59 — row-level declarations satisfy the exhaustiveness requirement; the header assertion satisfies the auditability-declaration requirement; these are distinct structural layers: one makes FER coverage true at the row level, the other names it as a table-level structural property and specifies its verification method |
