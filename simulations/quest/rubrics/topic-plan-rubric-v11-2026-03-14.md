v11 rubric written to `simulations/quest/rubrics/topic-plan-rubric-v11-2026-03-15.md`.

---

**Two new criteria extracted from Round 11:**

**C-38 — Pass/fail example symmetry** (`depth`)

The discriminating signal: V-01/V-02 are PARTIAL on C-37 because their pass examples describe the passing mechanism structurally ("a phrase from strategy.md") rather than showing a concrete quotable value. V-03/V-04/V-05 supply labeled quoted pass examples traceable to strategy.md format — "teams run scout before draft" / "signal count threshold never defined" / "the phrase 'gather evidence first'" — symmetric in concreteness and specificity with the fail examples. The gap C-38 closes: C-37 says "explicit pass/fail examples" but does not define what makes a pass example explicit. C-38 defines it: pass examples must be quotable concrete values a reader could recognize as actual cell output, labeled PASS, and traceable to the target document format — the same standard applied to fail examples. An asymmetric exhibit (concrete FAIL, structural PASS) cannot verify that the test's pass criterion is equally well-defined.

**C-39 — Step-level activation cross-reference** (`enforcement`)

The discriminating signal: V-04 passes C-37 via the procedure embedded in the column definition and adds a third activation point — Step 2 explicitly directs the model to "apply the PASS/FAIL exhibit from COLUMN CONTRACT before populating each cell." C-36 fires the CONTRACT at one reading point (before schema); C-37 fires the test at a second reading point (column definition under schema review); both occur at prompt-reading time. C-39 requires a third firing at execution time — the extraction step itself cites the CONTRACT by name. The gap: a test procedure that is visible at declaration time may be deprioritized under context pressure once execution is underway; a step-level citation re-activates the test precisely when it is needed and creates a verifiable cross-reference from cell population back to the governing contract.

**Updated totals:** 39 criteria, 245 pts. Formula: `(essential_pass/5 × 60) + (recommended_pass/3 × 30) + (aspirational_pass/31 × 155)`. Golden threshold bumped to 145.

**Phase 9 added to the arc:** *test exhibit quality and activation timing* — pass examples must be symmetric with fail examples at concrete quotable specificity; the governing test must be re-activated at execution time by a step-level cross-reference, not only at declaration time.
