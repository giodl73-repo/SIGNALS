Reading the scorecard evidence carefully to extract patterns before writing.

Key signals from R9:

1. **V-01, C-09 PASS evidence**: "FORMAT CONTRACT schema (5) declares `[element name] (cat-N) —` prefix + DISQUALIFY-IF; **re-read instruction at PRODUCE**" — V-01 emitted an explicit back-reference directive at the PRODUCE command, not just in FORMAT CONTRACT. This is a new temporal mechanism: BEFORE-PRODUCE re-read distinct from both C-23 (pre-generation) and C-18 (post-produce VERIFY). Three-point chain.

2. **V-02, C-21 PASS evidence**: "CHECKs at diagram, **headcount Key Roles**, DRI/Owner (with ROLE-NAME LOCK reference), charter Membership/Decides, and embedded in REBUTTAL FORM at ROLE UNDER PRESSURE — **all four required sites covered**" — C-21 lists only four sites. V-02 used five, adding `Key Roles` column of the headcount table. C-21 needs a fifth mandatory site.

3. **V-02, C-25 PASS evidence**: "Every value must appear in the ROLE-NAME LOCK list — no new role names are introduced in this column" + **per-cell CHECK** — C-25 requires mandatory column-level language. V-02 went one granularity level deeper: a per-cell verification directive that fires row-by-row, not just once at column-header instruction time.

These become **C-26, C-27, C-28**.

---

```markdown
# org-chart Rubric — v9

## Essential Criteria

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | ROLE-TYPE-CLASSIFICATION with all four domain types | correctness | Output contains a ROLE-TYPE-CLASSIFICATION block listing every role from ROLES-LOADED, each annotated with exactly one domain type from the closed vocabulary: (strategic), (operational), (advisory), (governance). No role is missing. No unlisted domain type is introduced. |
| C-02 | ASCII diagram with hierarchy and committees as distinct nodes | correctness | Output contains an ASCII box/tree diagram showing at minimum two levels of hierarchy. Committees appear as distinct labeled nodes — not embedded inside an area box. No flat list of names without parent-child connectors. Role names in diagram must match those in ROLES-LOADED or ROLES-NOTE. |
| C-03 | Headcount table with Decides and Escalates columns | correctness | Output contains a headcount table with columns `Area`, `Headcount`, `Key Roles`, `Decides`, and `Escalates` — five columns, not four. `Decides` and `Escalates` are separate columns (no merged `Decision Scope`). Table includes at minimum two area rows plus a `**Total**` row with the sum. Key Roles cells annotated as `[role name] ([domain type])`. |
| C-04 | Operating rhythm table with minimum three distinct rows | coverage | Output contains an operating rhythm table with columns `Cadence`, `Frequency`, `DRI / Owner`, `Purpose`. Table contains at minimum three rows covering at minimum: (a) ROB, (b) a shiproom or gate meeting, and (c) a governance meeting. Two meetings must not be merged into one row. |
| C-05 | Committee charters with all five required fields | correctness | For every governance meeting in the rhythm table, a charter is written with all five fields: `Purpose`, `Membership`, `Decides`, `Escalates`, `Quorum`. Quorum field uses the fraction format `Quorum: [N] of [M] member roles required for binding decisions`. Membership lists at minimum two roles annotated as `[role name] ([domain type])`. Escalates names a destination — not `everything not listed under Decides`. |

---

## Recommended Criteria

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | Inertia assessment with verdict and pressure rating | depth | Output contains an Inertia Assessment with all four sub-sections in order: (1) Case for Staying Flat with a mechanism table (>=2 rows, Type values from closed vocabulary), (2) How We Coordinate Today, (3) Rebuttal naming a specific observable failure mode, (4) VERDICT opening with `FLAT-CASE-PRESSURE: [rating] — [justification]` where rating is from the closed set (Strong / Moderate / Weak / Negligible / Insufficient). Verdict declares exactly one of `CAN-OPERATE-FLAT` or `STRUCTURE-WARRANTED`. Re-assessment trigger names a concrete threshold (not "revisit as the team grows"). |
| C-07 | ORG-ELEMENT REGISTER with all four categories | coverage | Output contains an `ORG-ELEMENT REGISTER` block after the charters phase gate with all four categories populated: `cat-1 (Areas)` with headcount per area, `cat-2 (Committees/Cadences)`, `cat-3 (Headcount)` with total, `cat-4 (DRI Roles)`. No category may be empty or missing. |
| C-08 | Section order with all phase gates present | behavior | Output contains all four phase gate lines in correct sequence: (1) `=== [PHASE GATE: ROLES COMPLETE — INERTIA ASSESSMENT BEGINS] ===`, (2) `=== [PHASE GATE: INERTIA COMPLETE — STRUCTURE SECTION BEGINS] ===`, (3) `=== [PHASE GATE: STRUCTURE COMPLETE — HEADCOUNT AND RHYTHM BEGIN] ===`, (4) `=== [PHASE GATE: CHARTERS COMPLETE — REGISTER AND ANALYSIS BEGIN] ===`. No section appears before its gate. |

---

## Aspirational Criteria

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | Anti-Pattern Watch with typed cat-N citations | depth | Output contains an Anti-Pattern Watch section after the Org Evolution Roadmap with columns `Anti-Pattern`, `Why It Applies Here`, `Mitigation`. Contains at minimum two rows. Every `Why It Applies Here` cell opens with `[element name] (cat-N) —` using a valid cat-N code from the ORG-ELEMENT REGISTER. No cell names an org element without the typed citation syntax. |
| C-10 | Org Evolution Roadmap with two distinct trigger categories | depth | Output contains an Org Evolution Roadmap trigger table with columns `Trigger`, `Structural Change`, `Type`. Contains at minimum two rows where Row 1 is a headcount threshold trigger and Row 2 comes from a different category (workload signal, structural symptom, or milestone event). Two headcount thresholds do not satisfy this criterion. |
| C-11 | Inertia Rebuttal is role-grounded with concrete re-assessment trigger | depth | The Rebuttal sub-section of the Inertia Assessment names at least one specific role from ROLES-LOADED as the coordination failure point. The failure described is an observable breakdown (e.g., "the [role] has no escalation path for cross-area dependency disputes") — not a growth projection ("as we scale, coordination will become harder"). The re-assessment trigger in the VERDICT names a concrete measurable threshold: a specific hire count, a named milestone event, or a structural symptom (e.g., "two or more missed ship dates attributable to cross-area misalignment") rather than a generic signal. |
| C-12 | Role-name coherence across all document sections | correctness | Every named role reference throughout the document — ASCII diagram, rhythm table DRI/Owner column, committee charter Membership and Decides fields, and Inertia Assessment sub-sections — matches a role listed in ROLES-LOADED or ROLES-NOTE. No new role names are introduced in any section after the roles block. This extends C-02 (diagram-only) to the full document. |
| C-13 | ROLE-NAME LOCK block emitted after classification | behavior | Output contains a visible `ROLE-NAME LOCK` block immediately after the ROLE-TYPE-CLASSIFICATION that explicitly enumerates every role from ROLES-LOADED as the complete set of permitted references for the remainder of the document. The block appears before the first phase gate. Its presence converts role-coherence from a prose-governed expectation into a mechanical per-section check: downstream sections (diagram, rhythm table DRI/Owner, charter Membership/Decides, Inertia sub-sections) can be verified against a single named list without rereading the roles block. |
| C-14 | Inertia Rebuttal uses mandatory four-field case form | depth | The Rebuttal sub-section of the Inertia Assessment is structured as a four-field form with explicit field labels in this order: `ROLE UNDER PRESSURE`, `OBSERVABLE BREAKDOWN`, `WHY EXISTING MECHANISMS FAIL`, `RE-ASSESSMENT TRIGGER`. Each field is present and populated. The `ROLE UNDER PRESSURE` field names exactly one role from ROLES-LOADED. The `OBSERVABLE BREAKDOWN` field describes a current coordination failure — not a future growth risk ("as we scale..." does not satisfy this field). This form structure makes role selection a syntactic precondition before the breakdown prose can begin, eliminating the compliance gap that exists when role-grounding is requested only via prose instruction. |
| C-15 | Anti-Pattern Watch uses schema-enforced CAT-N citation prefix | behavior | The Anti-Pattern Watch section is introduced with a `CAT-N-CITATION-SCHEMA` block (or equivalent column header template) that explicitly enumerates the valid cat-N codes drawn from the ORG-ELEMENT REGISTER and states the mandatory `[element name] (cat-N) —` prefix format for `Why It Applies Here` cells. The schema block appears immediately before the Anti-Pattern Watch table. Its presence converts the typed-citation requirement from a prose expectation into a mechanical per-cell check — each cell can be verified against the schema without rereading the register. This is the structural-mechanism parallel to C-09, using the same principle as C-13 (ROLE-NAME LOCK for role coherence) and C-14 (four-field form for Rebuttal role-grounding): a named contract that makes compliance syntactically checkable rather than prose-discipline-dependent. |
| C-16 | Org Evolution Roadmap uses TRIGGER-TYPE VOCABULARY block to enforce category diversity | behavior | The Org Evolution Roadmap section is introduced with a `TRIGGER-TYPE VOCABULARY` block that explicitly enumerates the closed set of permitted Type values (headcount threshold / workload signal / structural symptom / milestone event) and states that no two consecutive rows may share the same Type value. The block appears immediately before the trigger table. Its presence converts the category-diversity requirement of C-10 from a prose expectation into a mechanical per-row check — each row's Type value can be verified against the vocabulary and the consecutive-type constraint can be verified without rereading the section narrative. This is the structural-mechanism parallel to C-10, applying the same principle as C-13 (ROLE-NAME LOCK), C-15 (CAT-N-CITATION-SCHEMA): a named contract block makes compliance syntactically checkable. |
| C-17 | Anti-Pattern Watch section has a dedicated phase gate separating it from Org Evolution Roadmap | behavior | Output contains a fifth phase gate line: `=== [PHASE GATE: ORG-EVOLUTION COMPLETE — ANTI-PATTERN WATCH BEGINS] ===` appearing after the Org Evolution Roadmap and before the Anti-Pattern Watch table. The gate's presence makes Anti-Pattern Watch a structurally required section: its absence is a visible omission at the phase-gate level rather than a silent skip of an optional addendum. This is the architectural precondition for C-09 and C-15 — a prompt that defines only four phase gates cannot reliably produce both Org Evolution Roadmap and Anti-Pattern Watch as separate required sections. |
| C-18 | Every structural requirement with a known evasion mode has a corresponding VERIFY block | behavior | For every `PRODUCE:` command that has a documented failure mode, a `VERIFY:` block appears immediately after the command naming the exact disqualifying output variant. The VERIFY block uses present-tense disqualification syntax ("`[specific output]` does not satisfy this step") rather than affirmative restatement of the pass condition. At minimum, VERIFY blocks must be present for: the headcount table (disqualifying a merged `Decision Scope` column), the Org Evolution Roadmap (disqualifying two headcount threshold rows), and the Inertia Rebuttal (disqualifying future-tense breakdowns beginning with "as we scale..."). This mechanism is distinct from vocabulary-lock blocks (which enforce what must appear): VERIFY blocks enforce what must *not* appear, closing known compliance gaps that named vocabularies alone cannot close. |
| C-19 | Inertia Mechanism table uses MECHANISM-TYPE VOCABULARY block to enforce type coherence | behavior | The Inertia Assessment sub-section 2a (Case for Staying Flat) is introduced with a `MECHANISM-TYPE VOCABULARY` block that explicitly enumerates the closed set of permitted Type values for the mechanism table (e.g., span-of-control / direct-comms / shared-context / single-DRI / low-coordination-cost) and states that no row may introduce a Type value outside this vocabulary. The block appears immediately before the mechanism table. Its presence converts the mechanism-type constraint from a prose-inlined expectation into a mechanical per-row check — each row's Type value can be verified against the vocabulary without rereading the section narrative. This is the third inner-table application of the vocabulary-lock principle (after C-13 for roles and C-16 for trigger types), extending named-enumeration enforcement into the Inertia sub-section and making the mechanism table type column structurally checkable in the same way as role references and trigger categories. |
| C-20 | UPFRONT FORMAT CONTRACT block pre-declares all five table schemas before the first phase gate | behavior | Before the first phase gate or section-producing STEP, an `UPFRONT FORMAT CONTRACT` (or equivalent titled block) consolidates all five output schemas in one reference location: (1) headcount table column headers (`Area \| Headcount \| Key Roles \| Decides \| Escalates`), (2) operating rhythm table column headers (`Cadence \| Frequency \| DRI / Owner \| Purpose`), (3) committee charter field labels (Purpose / Membership / Decides / Escalates / Quorum including fraction format), (4) ORG-ELEMENT REGISTER categories (cat-1 through cat-4 with required content per category), (5) Anti-Pattern Watch table column headers (`Anti-Pattern \| Why It Applies Here \| Mitigation`). Each schema names required columns or fields exactly. The block appears before the first section that produces any of these outputs. Its presence converts schema compliance from a recall requirement — where the model must remember the schema from 1,500+ tokens earlier — into a lookup requirement where the model references the block in immediate context. |
| C-21 | ROLE-NAME LOCK with inline CHECK reminders embedded at every downstream section | behavior | The ROLE-NAME LOCK block (required by C-13) is extended with inline `CHECK:` annotations embedded at each downstream PRODUCE command that consumes role names: (1) immediately before the ASCII diagram command, (2) in the Operating Rhythm Table DRI/Owner column instruction, (3) in the Committee Charter Membership and Decides field instructions, (4) in each Inertia Assessment sub-section that references roles (including the `ROLE UNDER PRESSURE` field of the four-field form), and (5) in the headcount table Key Roles column instruction. Each CHECK explicitly states that every role name used in that section must appear in the ROLE-NAME LOCK list. The presence of inline CHECKs converts role-coherence from a one-time enumeration — read once and relied on across 2,000+ tokens — into an active per-section mechanical check that eliminates the memory-decay failure mode. This is the activation layer for C-13: the LOCK declares the contract; the inline CHECKs enforce it at each use site. |
| C-22 | Vocabulary-lock and VERIFY blocks co-deployed as complementary pairs for every governed table | behavior | For every inner table governed by a vocabulary-lock block, a VERIFY block targeting the complementary failure mode is present immediately after that table's PRODUCE command. Required pairings: (a) MECHANISM-TYPE VOCABULARY lock (C-19) paired with a VERIFY block disqualifying a row whose Type value falls outside the enumerated set; (b) TRIGGER-TYPE VOCABULARY lock (C-16) paired with a VERIFY block disqualifying two consecutive rows sharing the same Type value; (c) CAT-N-CITATION-SCHEMA lock (C-15) paired with a VERIFY block disqualifying a `Why It Applies Here` cell that names an org element without the `(cat-N)` typed citation prefix. Additionally, the committee charter Quorum field (C-05) must receive a VERIFY block disqualifying the short form that omits the denominator M (e.g., "N members required" without "of M member roles"). The co-deployment principle: vocabulary locks prevent wrong-type values; VERIFY blocks prevent right-type-but-wrong-structure evasions. Both mechanisms target distinct, non-overlapping failure modes and must be present together — a vocabulary lock without a paired VERIFY block leaves the complementary failure mode open, and vice versa. |
| C-23 | FORMAT CONTRACT includes per-schema DISQUALIFY-IF annotations for all schemas with documented failure modes | behavior | The UPFRONT FORMAT CONTRACT (C-20) is extended so that each schema with a known evasion mode includes an inline `DISQUALIFY-IF:` annotation immediately following the field list for that schema. At minimum: (1) headcount table schema: `DISQUALIFY-IF: Decides and Escalates merged into a single column (e.g., "Decision Scope")`; (2) committee charter Quorum field: `DISQUALIFY-IF: fraction omits denominator M (e.g., "3 members required" rather than "3 of 5 member roles required")`; (3) Anti-Pattern Watch table schema: `DISQUALIFY-IF: Why It Applies Here cell names an org element without the (cat-N) typed citation prefix`. The DISQUALIFY-IF annotations appear inside the FORMAT CONTRACT block at schema-declaration time — before any section-producing command — providing a pre-generation disqualification signal. This complements C-18 (VERIFY blocks at PRODUCE commands) without replacing it: FORMAT CONTRACT DISQUALIFY-IF fires before generation begins; C-18 VERIFY fires at each PRODUCE command. Both mechanisms must be present for full two-phase disqualification coverage. This pattern was observed in V-01 where FORMAT CONTRACT NOTE-style flags partially achieved disqualification at schema-declaration time (earning C-05 PASS via "NOTE disqualifies denominator-free form") despite the absence of VERIFY blocks at PRODUCE commands. |
| C-24 | Four-field form and ROLE-NAME LOCK CHECK co-deployed as activation pair at ROLE UNDER PRESSURE | behavior | The Inertia Rebuttal sub-section deploys three co-dependent mechanisms as an activation triad: (a) four-field form (C-14) with `ROLE UNDER PRESSURE` as the first labeled field, providing the structural anchor for role selection; (b) a ROLE-NAME LOCK CHECK embedded immediately before the `ROLE UNDER PRESSURE` field instruction, explicitly stating that the value must be drawn from the ROLE-NAME LOCK list (e.g., `CHECK: ROLE UNDER PRESSURE must name exactly one role from the ROLE-NAME LOCK list — no new role names may be introduced`); (c) a VERIFY block after the Rebuttal PRODUCE command (as required by C-18) disqualifying future-tense breakdowns beginning with "as we scale...". All three mechanisms must be present together: the four-field form creates the structural hook; the ROLE-NAME LOCK CHECK attaches role-governance to that hook; the VERIFY block closes the future-tense evasion mode. Without C-14, the CHECK required by C-21 for the Inertia sub-section has no structural anchor — role-selection devolves into a prose-governed expectation. Without the CHECK, the four-field form enforces field presence but does not enforce that the named role comes from the locked set. Without the VERIFY, the observable-breakdown requirement can be satisfied with growth projections. This is the structural-form + CHECK + VERIFY triad, extending C-22's vocabulary-lock + VERIFY co-deployment principle to the Rebuttal's structural-form governance layer. V-02's C-21 PARTIAL revealed the gap: CHECKs present at Rebuttal but without the four-field form left the ROLE UNDER PRESSURE field unanchored, preventing PASS. |
| C-25 | DRI/Owner column designated as strict ROLE-LOCK enforcement site with no softening qualifiers | behavior | The Operating Rhythm Table DRI/Owner column instruction contains an explicit role-coherence enforcement statement that: (a) explicitly references the ROLE-NAME LOCK list as the governing constraint, (b) uses mandatory present-tense language ("every DRI/Owner value must appear in the ROLE-NAME LOCK list — no new role names are introduced in this column") rather than advisory or hedged language, and (c) appears in the same instruction block as other DRI/Owner formatting requirements so the role-lock cannot be omitted during section generation. Softening qualifiers such as "where possible," "prefer roles from," "try to use," or "where a role is known" do not satisfy this criterion. The DRI/Owner column must be treated as a first-class role-coherence enforcement site with the same mandatory language as charter Membership and Decides fields — not as a best-effort site where novel role names may be introduced when convenient. This criterion closes the recurring C-12 partial-compliance pattern observed across V-01 ("DRI/Owner says 'where possible' — not strict for all sections") and V-03 ("DRI/Owner not explicitly locked"), where vocabulary-lock and VERIFY mechanisms covered inner table types but left the DRI/Owner column under-governed because it carries role references rather than categorical vocabulary terms. |
| C-26 | PRODUCE-time FORMAT CONTRACT back-reference directive at every governed PRODUCE command | behavior | Each PRODUCE command that generates an output governed by the FORMAT CONTRACT (C-20) contains an explicit back-reference directive immediately before the generation instruction, naming the specific FORMAT CONTRACT schema by number and/or label (e.g., `BEFORE PRODUCING: re-read FORMAT CONTRACT schema (1) — headcount table — and confirm the output satisfies the column headers and DISQUALIFY-IF annotation`). The directive appears as a labeled instruction (`BEFORE PRODUCING:`, `FORMAT CHECK:`, or equivalent) rather than inline prose. Its presence converts FORMAT CONTRACT compliance from a recall requirement — the FORMAT CONTRACT is read once at document head and then relied on across 1,000+ tokens — into a per-step active reference requirement that fires immediately before each governed output is generated. This is the production-time bridge between C-23's FORMAT CONTRACT DISQUALIFY-IF (pre-generation signal, fires before any section begins) and C-18's VERIFY blocks (post-produce signal, fires immediately after generation). Together, the three mechanisms form a three-point temporal governance chain: FORMAT CONTRACT DISQUALIFY-IF → BEFORE-PRODUCE re-read → post-produce VERIFY. A prompt with C-23 and C-18 but without C-26 has a temporal gap: the model reads the schema at document head, proceeds through thousands of tokens of generation, and arrives at the PRODUCE command having potentially overwritten the FORMAT CONTRACT constraints. The BEFORE-PRODUCE directive closes this gap. V-01's C-09 PASS evidence named this mechanism explicitly: "re-read instruction at PRODUCE." |
| C-27 | Headcount table Key Roles column designated as explicit ROLE-NAME LOCK CHECK site | behavior | The ROLE-NAME LOCK CHECK coverage (required by C-21) is extended to include the `Key Roles` column of the headcount table as a mandatory fifth CHECK site. A ROLE-NAME LOCK CHECK appears immediately before or within the headcount table PRODUCE command, explicitly stating that every role name in the Key Roles column must appear in the ROLE-NAME LOCK list and that no new role names may be introduced in this column. C-21 requires CHECKs at four sites: (1) ASCII diagram, (2) Operating Rhythm Table DRI/Owner column, (3) Committee Charter Membership and Decides fields, and (4) Inertia Assessment sub-sections. The Key Roles column carries the same role-coherence risk as all four existing sites — a novel role name introduced in a Key Roles cell propagates downstream just as a novel role in the diagram or charter Membership field would — and therefore requires the same mandatory CHECK enforcement. Without an explicit Key Roles CHECK, the column functions as an ungoverned insertion point: the ROLE-NAME LOCK is active everywhere except the one headcount table column that also carries role references. V-02's C-21 PASS evidence enumerated all five sites: "CHECKs at diagram, headcount Key Roles, DRI/Owner, charter Membership/Decides, and embedded in REBUTTAL FORM at ROLE UNDER PRESSURE." |
| C-28 | Per-cell role-lock CHECK at role-consuming columns | behavior | For the two role-consuming columns with highest novel-role-introduction risk (Operating Rhythm Table DRI/Owner and headcount table Key Roles), the ROLE-NAME LOCK enforcement is extended from a column-level instruction (required by C-25 for DRI/Owner and C-27 for Key Roles) to a per-cell CHECK directive: the column instruction explicitly states that each cell is individually verified against the ROLE-NAME LOCK list as it is populated (e.g., "for each cell in this column, verify the value appears in the ROLE-NAME LOCK list before populating the next row — do not complete the table and verify after"). The per-cell CHECK appears as a structural directive within the column definition rather than as a one-time column-header instruction. Its presence converts role-lock enforcement from a column-governing statement — applied once at instruction time, potentially forgotten row-by-row across a multi-row table — into a row-by-row mechanical check that eliminates the compliance-drift failure mode where early rows comply but later rows introduce novel names. Column-level instructions alone (C-25, C-27) are subject to memory-decay across rows in the same way that section-level CHECKs are subject to memory-decay across sections; the per-cell mechanism is the row-granularity parallel to inline CHECKs (C-21) at section granularity. V-02's C-25 PASS evidence explicitly identified "per-cell CHECK" as a mechanism beyond mandatory column-level language. |

---

## Design Logic

| Criterion pair | What the mechanism does |
|----------------|------------------------|
| C-12 + C-13 | Role coherence enforced by ROLE-NAME LOCK (named enumeration, not prose rule) |
| C-13 + C-21 | ROLE-NAME LOCK (one-time declaration) extended by inline CHECK reminders (per-section enforcement at every use site) |
| C-21 + C-27 | ROLE-NAME LOCK CHECK coverage extended to headcount table Key Roles column as mandatory fifth site — closes the gap where the Key Roles column is a role-consuming cell not covered by the original four-site enumeration |
| C-11 + C-14 | Rebuttal role-grounding enforced by four-field form (syntax forces field order) |
| C-09 + C-15 | Cat-N citation enforced by CAT-N-CITATION-SCHEMA block (named contract before table) |
| C-10 + C-16 | Trigger category diversity enforced by TRIGGER-TYPE VOCABULARY block (closed set before table) |
| C-06 + C-19 | Mechanism table Type coherence enforced by MECHANISM-TYPE VOCABULARY block (third inner-table vocabulary-lock application) |
| C-09 + C-15 + C-17 | Anti-Pattern Watch presence enforced by dedicated phase gate (section cannot be silently omitted) |
| C-03 + C-10 + C-14 + C-18 | Known evasion modes closed by VERIFY blocks (merged columns, duplicate trigger types, future-tense breakdowns) |
| C-15 + C-16 + C-19 + C-22 | Every vocabulary-lock block paired with a VERIFY block covering the complementary failure mode (wrong-type vs. right-type-wrong-structure) |
| C-03 + C-04 + C-05 + C-07 + C-09 + C-20 | All five table schemas pre-declared in UPFRONT FORMAT CONTRACT — schema compliance becomes lookup, not recall |
| C-20 + C-23 | FORMAT CONTRACT schema declarations extended with inline DISQUALIFY-IF annotations — adds pre-generation disqualification signal layer at schema-declaration time |
| C-18 + C-23 + C-26 | Three-point temporal governance chain: FORMAT CONTRACT DISQUALIFY-IF fires before any section begins (pre-generation); BEFORE-PRODUCE re-read directive fires immediately before each governed PRODUCE command (production-time bridge); C-18 VERIFY fires immediately after generation (post-produce). Each mechanism closes the temporal gap that the other two leave open. |
| C-14 + C-21 + C-24 | Four-field form (structural anchor) + ROLE-NAME LOCK CHECK (role-governance at ROLE UNDER PRESSURE) + VERIFY (future-tense disqualification) — three-mechanism activation triad for Rebuttal; C-14 is the prerequisite that gives C-21's Inertia CHECK a structural field to attach to |
| C-12 + C-21 + C-25 | DRI/Owner column designated as strict first-class role-coherence site — closes the recurring partial-compliance pattern where hedged language ("where possible") or unlocked columns left DRI/Owner as a role-coherence gap despite ROLE-NAME LOCK presence elsewhere |
| C-25 + C-27 + C-28 | Per-cell role-lock CHECK extends column-level enforcement (C-25 for DRI/Owner, C-27 for Key Roles) to row-by-row granularity — eliminates compliance-drift where early rows comply but later rows introduce novel role names; per-cell is the row-level parallel to inline CHECKs at section level |
```
