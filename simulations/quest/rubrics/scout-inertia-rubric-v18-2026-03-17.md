Reading the R17 scorecard carefully. Two excellence signals remain unformal after C-54 was extracted:

**Signal 1** (V-05): `IF Q3 SHOWS N, RETURN TO THE Q3 SECTION (FM->ACTOR BRIDGE) AND COMPLETE THE ACTOR MAPPING` -- the clause carries three elements: destination, artifact class, and completion task. C-54 minimum (`Return to Section 1A (FM->ACTOR BRIDGE) if Q3 shows N`) carries only destination and class. The `AND COMPLETE THE ACTOR MAPPING` clause names the work to be done on arrival -- making the routing instruction self-contained with no external lookup required. One new criterion: **C-55**. Extends C-54.

**Signal 2** (V-05): `THE Q3 SECTION` as the return target. V-01/V-03 route `Q3 -> Section 1A`; V-02/V-04 route `Q3 -> Stage 2A`. V-05 routes `Q3 -> THE Q3 SECTION` -- the artifact ID itself is the section reference. Zero mapping overhead: the same identifier appears in the gate table row, the conditional key (`IF Q3 SHOWS N`), and the return target (`THE Q3 SECTION`). One new criterion: **C-56**. Extends C-53 along a naming-form dimension orthogonal to C-54/C-55. Applicability condition: requires bridge artifacts to be standalone sections whose primary label is the artifact ID.

Two new criteria. Denominator 46 -> 48.

---

