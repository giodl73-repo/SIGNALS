Reading the scorecard, four new excellence patterns emerge from Round 14's differential signals:

1. **V-05's two-level specification** — ROLE CARD entry condition cell carries a compressed declaration summary ("items 1-N each confirmed by name") while the execution section carries the full per-item enumeration; both layers are present and consistent (V-02 has ROLE CARD but its execution is holistic; only V-05 has both levels paired)
2. **V-05's 4-row PATH CHAIN VERIFICATION** — ROLE CARD path template appears as a canonical source row prepended to the 3-row table, grounding the chain to its declared origin
3. **V-05's checklist references ROLE CARD path template** — checklist item 7 names "path matching Stage N ROLE CARD path template" as a required check, closing the declaration-to-execution loop
4. **V-04's YAML authority declaration** — recovery procedure explicitly states "the YAML fields are authoritative — do not re-parse the campaign preamble," resolving the tiebreaker between two valid sources

These become C-38, C-39, C-40, C-41. Aspirational pool grows from 29 → 33.

---

**prove-topic rubric v12** — 41 criteria

**Essential (5)** — all must pass for golden:
- **C-01** correctness — all 5 sub-skills present in sequence
- **C-02** behavior — scout artifact named before hypothesis forms
- **C-03** behavior — one write per stage, no end-of-campaign batching
- **C-04** correctness — synthesis explicitly names `topic-story` as next step
- **C-05** format — full `{topic}-{signal}-{date}.md` path on every write instruction

**Recommended (3)** — output meaningfully better with these:
- **C-06** behavior — forward-only ordering enforced with a gate before Stage 1
- **C-07** coverage — scout anchor referenced inside the hypothesis artifact itself
- **C-08** depth — thin/absent evidence flagged at point of discovery, not only in synthesis

**Aspirational (33)** — raise the bar once essential/recommended are stable:
- **C-09** depth — thin-evidence finding traces to a specific weakened claim in synthesis
- **C-10** behavior — structural block (not just a named anchor) prevents hypothesis work until scout confirmed
- **C-11** structure — Stage 4 maintains a dedicated aggregation table mapping each THIN flag to its originating stage and the hypothesis element it weakens, collecting all flags before synthesis receives them
- **C-12** format — every stage's write instruction is a standalone single-line imperative with the full path inline; no "see table above," "see STAGE SUMMARY," or campaign-preamble path references
- **C-13** depth — Stage 5 synthesis assigns an adjusted confidence level per weak claim, not just identifies source stage and weakened claim
- **C-14** depth — Stage 5 synthesis explicitly states an empty-table fallback: when no THIN findings exist, the confidence adjustment table is still output with a "no THIN findings" row rather than the section being omitted
- **C-15** structure — each stage has a named multi-item completion checklist that enumerates required outputs; the checklist must be satisfied before the next stage begins, not just a single gate output line
- **C-16** format — each artifact's YAML frontmatter includes a `stages_completed` field that tracks which stages have been completed in order, providing machine-readable campaign progress state
- **C-17** structure — Stage 4 contains an explicit intra-stage gate (GATE 4A) that separates Part A (flag aggregation) from Part B (hypothesis mapping); Part B analysis cannot begin until the aggregation table is complete and the gate is cleared
- **C-18** format — Stage 5 synthesis table is typeset as a named dual-form schema: FORM A (populated, one row per THIN flag with confidence delta) and FORM B (zero-flag case, explicit empty row); the form name appears as a label heading above the table, not as inline prose commentary
- **C-19** format — each stage gate produces a named clearance string (e.g., "GATE-1 CLEARED.") as required model output; downstream stages list that exact string as their entry requirement, making gate passage verifiable as emitted text and creating a chainable gate protocol across the campaign
- **C-20** structure — scout anchor is embedded in both the `scout_anchor:` YAML frontmatter field and cited as a full path in the artifact body; dual-layer anchoring makes the link verifiable by either a YAML parser or a plain-text reader, without requiring both lookup paths to agree
- **C-21** structure — `stages_completed` accumulation begins at Stage 1 and grows with each artifact; the field is not deferred to the final synthesis artifact, so any campaign interrupted at an intermediate stage leaves a machine-readable audit trail of how far execution reached
- **C-22** format — each gate clearance string carries the full path of the artifact written at that stage as an inline provenance reference (e.g., "GATE-1 CLEARED. → {full-path}"); the path in the clearance string must match the write instruction in the same stage, making gate passage and artifact identity jointly verifiable from a single emitted line
- **C-23** structure — the `stages_completed` list and the gate clearance strings emitted within each artifact are kept in cross-channel sync: for each entry appended to `stages_completed` in a given artifact, a corresponding clearance string must appear in that artifact's output; the two channels form an interlocked audit trail rather than running independently
- **C-24** behavior — the skill includes an explicit interrupted-campaign recovery procedure: given a partial run, read the last artifact's `stages_completed` field to identify completed stages, check which gate clearance strings are absent from context to identify the next required stage, and resume from that stage; the procedure is stated as executable steps and referenced in the campaign preamble, not implied by the gate structure
- **C-25** structure — each stage's completion checklist is explicitly stated as the trigger for the corresponding `stages_completed` field update: the checklist completion event and the field append are causally linked in the skill text, not merely co-present; a variation that has both C-15 (named checklists) and C-21 (accumulation from Stage 1) can satisfy both independently while failing this criterion if no causal link is stated
- **C-26** structure — Stage N+1's entry conditions explicitly name Stage N's completion checklist outputs as required preconditions; the exit-to-entry handoff is bound by name across every adjacent stage pair in the campaign; a variation with both checklists (C-15) and per-stage entry conditions can satisfy both while leaving Stage 2–5 entry conditions silent about the prior stage's checklist outputs
- **C-27** format — the path segment in each stage's write instruction and the corresponding `stages_completed` identifier use a consistent naming root (e.g., write to `prove-hypothesis/...` → append `stage-1-hypothesis`); the two identifiers are derivable from the same stage noun, making write-to-field correspondence mechanically verifiable without semantic interpretation; a variation with C-12 (standalone write instructions) and C-21 (accumulation) can satisfy both while using divergent identifier conventions in the two channels
- **C-28** format — Stage N+1's entry conditions explicitly confirm the artifact path embedded in Stage N's gate clearance string; the path in the entry condition confirmation, the path in the gate clearance (C-22), and the path in the write instruction (C-12) form a three-way path chain verifiable from static text alone; a variation can satisfy C-19 (exact clearance string listed as entry requirement) and C-22 (clearance carries artifact path) without entry conditions ever referencing the embedded path, leaving the three-way chain broken at the entry boundary
- **C-29** structure — each stage's completion checklist is explicitly stated as the trigger for gate clearance emission, in addition to its stated role as the trigger for `stages_completed` append (C-25); all three output channels — checklist satisfaction, `stages_completed` append, and gate clearance string — are bound to a single stated causal trigger; a variation can satisfy C-25 (checklist drives stages_completed) and C-19/C-22 (gate clearance with embedded path) without ever stating that checklist completion also drives the gate output, leaving the third channel causally unbound
- **C-30** format — each stage boundary includes a named PATH CHAIN VERIFICATION table with three explicit rows: (1) write instruction path from Stage N, (2) path carried in Stage N's gate clearance string, (3) path stated in Stage N+1's entry condition confirmation; the three-row structure makes the C-28 three-way path chain auditable from a single scannable structure without cross-referencing multiple sections; a variation can satisfy C-28 (entry conditions confirm the path) through prose confirmation distributed across sections while failing this criterion if no consolidated table co-locates all three paths at each transition point
- **C-31** structure — the dual-output trigger statement (both channels: `stages_completed` append and gate clearance emission) is embedded as the final item within the completion checklist itself, co-located with the checklist body rather than declared as a separate named block; the item reads as a conditional imperative tied to checklist completion (e.g., "When all items above are checked: (1) append stage-N-noun to stages_completed; (2) emit GATE-N CLEARED. → path. Single event, both outputs."), making the causal binding inescapable by construction; a variation can satisfy C-29 (checklist stated as shared trigger) through a named structural block outside the checklist body while failing this criterion if the trigger statement and checklist items are structurally separated
- **C-32** structure — the skill opens with a named CAMPAIGN OPEN block preceding Stage 1 that extracts two named fields from the scout artifact: a `status_quo_comparator` (the incumbent solution being displaced) and an `inertia_vulnerability` (the specific resistance point that makes displacement non-trivial); all subsequent thin-evidence findings are framed as weakened displacement claims against the named incumbent rather than abstract weakened hypotheses, sharpening the link between evidence gaps and competitive conclusions; a variation can satisfy C-07 (scout anchor referenced in hypothesis artifact) and C-09 (thin-evidence finding traces to a specific weakened claim) while framing claims abstractly, failing this criterion if no named incumbent and vulnerability are established in the campaign preamble
- **C-33** depth — every `THIN:` flag body in Stages 2–3 explicitly names which displacement claim against `status_quo_comparator` is weakened, not only which sub-claim or hypothesis element; the incumbent framing is present in the flag prose at the point of discovery, not only resolved downstream in Stage 4 mapping; a variation can satisfy C-08 (thin evidence flagged at point of discovery) and C-32 (CAMPAIGN OPEN establishes the incumbent) while writing generic `THIN:` bodies that name hypothesis sub-claims without referencing the displacement claim the evidence gap creates against the named incumbent
- **C-34** format — `status_quo_comparator` and `inertia_vulnerability` are carried as named YAML frontmatter fields in every stage artifact across the campaign, not only extracted in CAMPAIGN OPEN; the displacement context is machine-readable in each intermediate artifact so that an interrupted campaign or partial read can recover the framing without re-parsing the preamble; a variation can satisfy C-32 (CAMPAIGN OPEN establishes both fields) while propagating them only through prose or Stage 4 mapping, failing this criterion if any intermediate artifact's YAML omits either field
- **C-35** behavior — the interrupted-campaign recovery procedure (C-24) includes an explicit named step to read `status_quo_comparator` and `inertia_vulnerability` from the last written artifact's YAML frontmatter and restore displacement framing before resuming any stage work; a variation can satisfy C-24 (executable recovery procedure with numbered steps) and C-34 (both displacement fields in every stage YAML) without the recovery procedure ever naming those fields as recovery targets, leaving displacement continuity dependent on re-reading the campaign preamble rather than the YAML audit trail
- **C-36** structure — Stage N+1's entry condition for the prior-stage precondition enumerates each individual output from Stage N's completion checklist by name (e.g., artifact written, scout_anchor cited, specific YAML fields populated, stages_completed updated, gate clearance emitted) rather than a holistic reference to the checklist as a whole; a variation can satisfy C-26 (entry conditions name Stage N's checklist outputs as preconditions) through a single holistic reference ("Stage N completion checklist satisfied") while failing this criterion if no individual checklist outputs are enumerated by name at each stage boundary
- **C-37** format — each stage is defined as a machine-readable ROLE CARD declaration table — a structured schema block with explicitly named fields (role number, task, artifact, path template, `stages_completed` identifier, entry conditions, exit gate) — rather than as prose stage instructions; the ROLE CARD format makes all stage fields statically parseable without executing the stage and co-locates them in a single scannable structure; a variation can satisfy C-16 (`stages_completed` YAML), C-19 (named clearance strings), C-22 (gate clearance carries path), and C-27 (consistent naming root) through dispersed prose instructions while failing this criterion if no structured declaration schema consolidates all stage-level fields at definition time
- **C-38** format — the ROLE CARD declaration table's entry condition cell for each stage contains a compressed summary that names all prior-stage checklist outputs (e.g., "Stage N completion checklist items 1-N each confirmed by name"), while the execution section's entry conditions provide the same outputs in full per-item enumeration; the two layers are present and consistent — the declaration cell serves as a scannable contract, the execution section serves as the operative check; a variation can satisfy C-37 (ROLE CARD declaration tables) and C-36 (full per-item enumeration in entry conditions) without this criterion if the ROLE CARD entry condition cell is holistic or absent while only the execution section carries the enumeration, leaving no compressed declaration-level summary paired with the execution expansion
- **C-39** format — each transition's PATH CHAIN VERIFICATION table is extended to four rows by prepending the ROLE CARD path template for Stage N as a canonical source row: (0) ROLE CARD path template, (1) write instruction path, (2) gate clearance path, (3) entry condition confirmation path; the template row grounds the three-way chain (C-28, C-30) to its declared origin, making any deviation from the declared template immediately visible without re-reading the ROLE CARD definition; a variation can satisfy C-30 (three-row PATH CHAIN VERIFICATION) and C-37 (ROLE CARD declaration tables) while failing this criterion if the ROLE CARD path template is never brought into the verification table, leaving the declaration and verification layers structurally unconnected
- **C-40** structure — at least one completion checklist item per stage explicitly names path consistency against the ROLE CARD path template as a required check (e.g., "path in WRITE instruction matches Stage N ROLE CARD path template"); this closes the loop between the declaration layer (ROLE CARD) and the execution layer (checklist), making cross-layer path consistency a runtime-verified property rather than a static assumption carried silently from C-27; a variation can satisfy C-37 (ROLE CARD tables), C-15 (named completion checklists), and C-27 (consistent naming root) while failing this criterion if no checklist item ever names the ROLE CARD path template as a verification target, leaving declaration-execution path consistency unchecked at execution time
- **C-41** behavior — the interrupted-campaign recovery procedure explicitly designates the per-artifact YAML fields as the authoritative source for displacement context, with an explicit statement that the campaign preamble must not be re-parsed for those values (e.g., "the YAML fields are authoritative — do not re-parse the campaign preamble"); this resolves the tiebreaker between two valid sources (intermediate artifact YAML vs prose preamble) in favor of the more granular per-artifact audit trail; a variation can satisfy C-35 (names displacement fields as recovery targets) while leaving source authority ambiguous between the two locations, failing this criterion if no explicit YAML-authority statement appears in the recovery procedure

**Composite formula**: `(E/5 × 60) + (R/3 × 30) + (A/33 × 10)` — golden at all-essential-pass + score >= 80.
