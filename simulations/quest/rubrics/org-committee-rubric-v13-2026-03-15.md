Written to `simulations/quest/rubrics/org-committee-rubric-v13-2026-03-16.md`.

---

**What changed v12 → v13**

**C-34 — Phase 1 commit manifest entries carry content-anchor suffixes** *(new)*

Signal: V-01/V-02/V-03 all fail C-33 with "not individual label enumeration with content anchors." C-33 requires the manifest to enumerate labels; C-34 requires each entry to include an identifying anchor — title or keyword — so CITE: validity is verifiable against the manifest without reading Phase 1 prose. A bare-label manifest (`INERTIA-FINDING-A, INERTIA-FINDING-B`) passes C-33, fails C-34. This applies the same specificity upgrade C-21 applied to C-19: not just "name the thing" but "include identifying content alongside the name."

**C-35 — Phase 3 commit enumerates locked stance tokens as a vote-anchor manifest** *(new)*

Signal: V-04's "Combined: Lifecycle + Inertia Framing" axis and the explicit generalization hint in C-33's v12 description. C-33 established the manifest pattern at Phase 1 (investigation findings); C-35 generalizes it to Phase 3 (participant stances). Each participant's role name and stance token are enumerated individually (`[Architect] BLOCK, [PM Lead] CONDITION, ...`) so the Phase 4 tally is derivable from the commit declaration rather than re-parsed from prose. Together C-33 and C-35 establish the principle: any phase producing discrete named outputs must commit a named manifest of those outputs.

**Scoring**: aspirational pool 25 → 27; aspirational max 50 → 54; composite max **140 → 144**.
