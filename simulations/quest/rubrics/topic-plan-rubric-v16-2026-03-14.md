Good — the threshold is present as `>= 210` in bold markup, not as a plain string. All six criteria and all key values are in place.

---

Written to `simulations/quest/rubrics/topic-plan-rubric-v16-2026-03-15.md`.

---

## Changes v15 → v16

Six new aspirational criteria extracted from three R16 variation axes.

**C-50 — Challenge strength column in the Defender Challenge Table** (`depth`)
Extends C-48 and C-36. Adds `Challenge strength: Strong / Moderate / Weak` as a COLUMN CONTRACT Rule 1 column. C-48 makes *when* each defense was constructed auditable; C-50 adds *how strong* the challenge was. Source: V-02 Challenge strength column.

**C-51 — Co-equal inertia framing in the preamble** (`behavior`)
Extends C-32 and C-31. The preamble explicitly names inertia as a co-equal outcome ("The result may be zero changes…; inertia is a co-equal option, not a fallback") before any step executes. Proposal-level inertia columns (C-25/C-31/C-32) operate per-row; the preamble framing sets a structural prior that biases every downstream table before any evidence is read. Source: V-02 co-equal inertia framing.

**C-52 — Calibration check after the Defender Challenge Table** (`depth`)
Extends C-50 and C-47. After the table is complete, a one-sentence distribution audit declares "rubber-stamp risk" (all Weak), "over-zealous risk" (all Strong), or "balanced." Forces table-level quality assessment before the confirmation gate. Source: V-02 calibration check at Step 7b.

**C-53 — Analytical narrative before each table at the five analytical steps** (`format`)
Extends C-07 and C-27. A 2–3 sentence narrative precedes each table at Steps 2, 3b, 4, 5, 6 — stating what was found and why it matters, not describing the step. Step 2b exempt. Narrative-completeness rows in Phase 1 and Phase 3 gates enforce compliance structurally. Source: V-03 narrative-forward execution.

**C-54 — PURPOSE clause on each CONTRACT rule** (`enforcement`)
Extends C-36 and C-46. Each CONTRACT rule opens with "This rule exists because…" before the constraint text. Makes rules argument-anchored rather than instruction-only: recoverable under compression. Source: V-05 explanatory register (rule purpose clauses).

**C-55 — Why clause at the opening of each execution step** (`enforcement`)
Extends C-54 and C-39. Each step opens with "This step exists so that…" before the output specification. Rationale-anchored steps degrade gracefully under compression; specification-only steps degrade to format-matching. Together C-54/C-55 form a two-level why architecture mirroring the C-36/C-41/C-39 constraint activation chain. Source: V-05 explanatory register (step-level why clauses).

**Updated totals**: 55 criteria, 325 pts | Formula: `(essential/5×60) + (recommended/3×30) + (aspirational/47×235)` | Golden threshold: **210**
