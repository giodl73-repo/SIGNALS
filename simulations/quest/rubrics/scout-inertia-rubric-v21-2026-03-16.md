File written. Here's what changed in v21:

---

**v20 → v21 summary**

| # | Criterion | Source | Key distinction |
|---|-----------|--------|-----------------|
| A-48 | **Adversarial-role frame in FAIL-FIRST heading subtitle** | R20 V-02: `THE STATUS QUO AS UNNAMED COMPETITOR` | A-26 requires axis vocabulary; A-48 requires an "AS [COMPETITIVE ROLE]" construction assigning the axis subject an active adversarial identity |
| A-49 | **Diagnostic-framing claim in FAIL-FIRST declaration body** | R20 V-02: `advocacy, not analysis` | A-31 requires a named structural rule; A-49 requires a separately identifiable diagnostic claim naming the axis subject's structural bias — WHY FAIL-FIRST ordering is analytically necessary |
| A-50 | **BRIDGE STAGE as dedicated parent container in nested schema** | R20 V-03: Q3/Q4/gate inside `STAGE 2 BRIDGE STAGE` | A-37 requires schema consistency of position indexes; A-50 requires Q3, Q4, and the gate to share a single named parent STAGE in the hierarchy — the bridge phase as a first-class structural unit |
| A-51 | **VERIFICATION as normalized structural noun in command directive label** | R20 V-04: `[BRIDGE VERIFICATION COMMAND]` not `[BRIDGE CONFIRMATION COMMAND]` | A-45 allows label or body route; A-51 requires VERIFICATION specifically as the label noun — the command-directive analogue of A-47's RULE requirement |

**Ceiling: 290 → 310**

**New implication chains:**
- `A-51 → A-45 → A-43 → A-39 → A-33 → A-27 → A-22 → A-12`
- `A-50 → A-37 → A-36 → A-27 → A-22 → A-12`
- `A-49 → A-31 → A-10`
- `A-48 → A-26 → A-10 and A-14`

**Full FAIL-FIRST normalization** (when A-26 + A-48 + A-31 + A-49 all pass): heading names the axis subject, heading assigns it an adversarial competitive role, body states the ordering rule with criterion ID, body carries the diagnostic bias label.

**Full command directive normalization** (when A-45 + A-51 both pass): label uses `[BRIDGE VERIFICATION COMMAND]` with VERIFICATION as the normalized noun; body uses a verification-action imperative verb (CONFIRM, VERIFY, VALIDATE) that may differ from the label noun — label/body register complementarity.
