Written to `simulations/quest/rubrics/quest-rubric-rubric-v9-2026-03-15.md`.

---

**What changed v8 → v9:**

**Two new aspirational criteria from R8 excellence signals:**

**C-29** (`evolution-hook-notes-subsection-names-trigger-event`) — from the V-02/V-04/V-01 isolation. All three variations attempted the C-10 notes-path, but only V-02 passed: it named the trigger event explicitly ("this rubric grows when quest-golden discovers patterns"). V-04 labeled its section "the evolution hook" with no mechanism — PARTIAL. V-01 mentioned growth generally — FAIL. The distinction: a section label is not a mechanism description; a mechanism description must name the specific trigger event.

**C-30** (`failure-analyst-phase-gate-provides-structural-c04-c08-guarantee`) — promoted from the v9 candidates list, confirmed by R8 V-04. Phase 1 (enumerate failure modes before writing any criterion) is the "strongest C-04 mechanism in the round" because the phase gate cannot be skipped. Phase 2 (derive criteria from those failure modes) is the "strongest C-08 mechanism in the round" because overlap is prevented by construction, not instruction.

**Structural updates:**
- Required Sections row 7 adds `C-30` to the N/A scope list
- Scoring N/A handling paragraph adds C-30 to the example list
- Notes adds a `**C-30 N/A scope**` labeled block
- v10 Candidates: `failure-analyst-skeleton-as-canonical-path` retired (promoted to C-30); `rejection-log-minimum-count-scales-with-aspirational-depth` updated to 22 aspirational criteria; `evolution-hook-dual-path-confirmation-prevents-single-point-of-failure` added as new candidate
- Self-applies: C-29 PASS (Notes version line names trigger), C-30 N/A (no phase gates in this document)
