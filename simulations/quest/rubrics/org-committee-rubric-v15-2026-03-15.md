Written to `simulations/quest/rubrics/org-committee-rubric-v15-2026-03-16.md`.

---

**What changed v14 → v15**

**C-38 — Commit blocks reference their manifest sections by named heading** *(new)*

Signal: V-01's C-33 commit reads "citation-anchor manifest declared in ## INERTIA RECORD above; findings A–D locked." V-01's C-35 commit reads "vote-anchor manifest declared in ## STANCE MANIFEST above; all stances locked." Both commits delegate to their named sections by exact heading rather than re-enumerating content inline. C-37 requires the named sections to exist; no criterion required the commit to reference the section by name. Without the cross-reference, the section and commit are parallel but unlinked — they can diverge. With it, the commit becomes a pointer into a first-class artifact, creating a bidirectional link where any update to either element must be reflected in the other.

**C-39 — VALIDATE rules with bilateral examples extend to essential-criterion declaration slots** *(new)*

Signal: V-01's C-01 evidence notes "VALIDATE with ACCEPTABLE/FAILS pairs anchors C-01 structurally" — bilateral VALIDATE applied to the committee-type PRINT declaration (an essential criterion's output slot), not only to the four content-fill slots C-36 governs. C-36 establishes the bilateral VALIDATE standard for structural content-requirement criteria; C-39 requires that standard to propagate to essential-criterion declaration checkpoints (Phase 0 forward) so coverage is uniform across the skeleton rather than restricted to mid-phase content fills.

**Scoring**: aspirational pool 29 → 31; aspirational max 58 → 62; composite max **148 → 152**.
