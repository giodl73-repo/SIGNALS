**flow-trigger-rubric-v11** written to `simulations/quest/rubrics/flow-trigger-rubric-v11-2026-03-16.md`.

---

**Four new aspirational criteria (C-34–C-37), N_aspirational: 25 → 29, max (raw): 127.5 → 137.5:**

| ID | Source | What it captures | Extends |
|----|--------|-----------------|---------|
| C-34 | V-05 Pattern 1 — "anonymous [X]" / "invisible [X]" naming convention | Failure mode label-format constraint: the noun in the label must be a structural property name, not a consequence state. One-step derivation is testable by noun inspection alone ("anonymous" / "invisible" / "absent" + structural property = pass; consequence noun = fail). | C-33 (which requires constraint propagation as a semantic property; C-34 requires it as a named syntactic form) |
| C-35 | V-05 Pattern 2 — embedded DERIVATION TEST table in INERTIA CONTRAST | Formal proof of constraint propagation completeness: a 3-column table (Failure Mode Label → Absent Structural Property → Derived Minimum Implementation) plus an explicit verification statement confirming the Failure Mode column alone reconstructs all mechanism requirements. Converts INERTIA CONTRAST from explanatory to provably prescriptive. | C-33 (requires the property), C-26 (requires rationale survives rubric-version changes) — C-35 requires the author to prove sufficiency rather than the reader to verify it |
| C-36 | V-05 Pattern 3 — explicit N/A-with-reason per cell in unified registry | Every N/A cell in the Phase 0 obligation registry carries a parenthetical explanation specific to the obligation-column pair, distinguishing intentional inapplicability from omission at the cell level. | C-32 (requires explicit N/A marking vs. silent emptiness; C-36 requires explanation vs. bare N/A mark) |
| C-37 | V-05 Pattern 4 — role-attributed artifact production counters in CLOSURE CHECK | Artifact production counters name the producing role inline: "ART-02 — Auditor, Phase 0: ABSENT" rather than "ART-02: not found." Role attribution co-located with gap detection, not only with gap declaration. | C-31 (manifest-layer attribution), C-20 (counter existence) — C-37 adds counter-layer attribution so remediation target is determinable from the closure check alone |

**Structural relationships:** C-34 is the syntactic form of C-33 — it makes C-33's pass condition testable by label-noun inspection without semantic evaluation. C-35 is the completeness proof for C-34/C-33 — it requires the author to demonstrate that the full Failure Mode column collectively specifies all mechanism requirements. C-36 closes the last ambiguity in C-32's unified registry: C-32 disallows silent empty cells; C-36 disallows unexplained N/A markers, making every cell either populated or self-documented. C-37 is the temporal counterpart to C-31: C-31 attributes roles at declaration time (manifest); C-37 attributes roles at detection time (closure check), eliminating the manifest cross-reference required to answer "who is accountable for this gap?"
