Written to `simulations/quest/rubrics/signal-setup-rubric-v18-2026-03-17.md`.

---

**What changed in v18:**

Three new criteria extracted from R19 non-trivial evidence:

| ID | Pattern | Source | Extension over |
|----|---------|--------|----------------|
| C-43 | Parallel inline detection paths carry functionally identical promotion rationale | V-02 C-30 PASS — both `6-Copilot` and `6-Code` carry identical rationale text | C-30 (annotation per gate) → consistency *across* parallel gates sharing the same form |
| C-44 | Tool-specific decline anchors name the artifact produced under inertia's control | V-01 C-38 PASS — "through Copilot: code that assumes adoption gets generated…" | C-21/C-38 (path-specific + force as winner) → artifact the tool emits under inertia |
| C-45 | Secondary already-configured affirmations place Signal in concurrent operation with the tool | V-01 C-24 PASS — "While Copilot is suggesting implementation code, the inertia question is already in Copilot's context" | C-24 (tool-local consequence) → concurrent-moment framing, Signal shown active not installed |

**Denominator: A=42 → A=45**

The trio forms a consistency–specificity–presence chain over the existing tool-gate criteria. C-43 closes the multi-path annotation gap. C-44 extends the adversary chain one link further (force → context → artifact). C-45 elevates already-configured affirmations from archival description to operational co-presence.
