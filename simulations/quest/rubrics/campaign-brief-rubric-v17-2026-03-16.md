Written to `simulations/quest/rubrics/quest-score-rubric-v17-2026-03-17.md`.

---

## Quest Rubric — v17

`37 criteria, 370 pts max`

---

### What changed: v16 → v17

**R17 investigation candidate resolved.** R17 constructs five variations spanning the C-36 band structure. V-01–V-03 confirm the three C-36 PARTIAL bands (Band 1: authority claim only; Band 2: "structurally valid" architectural claim; Band 3: conditioned "operative" declaration). V-04 achieves C-36 PASS minimum-sufficient — both clause bodies carry the unconditional absent-state operability declaration without execution instruction. V-05 achieves C-36 PASS and extends the absent-state rule to include an unconditional execution mandate.

**Resolution:** C-36 PASS (V-04) closes the mandate operability gap but leaves the execution-directive gap open — the model knows the companion mandate is active but receives no instruction to execute it. C-37 captures V-05's extension that closes the execution-directive gap.

---

**New criterion C-37 — Unconditional absent-companion mandate execution directive** *(R17 Pattern — V-05)*

| Axis | C-36 PASS (V-04) | C-37 PASS (V-05) |
|---|---|---|
| What absent-state rule declares | Companion mandate is independently operative | Companion mandate is independently operative **and must be executed** as if its execution note were present |
| Model behavior under double-elision | Executes present clause; acknowledges companion mandate as active | Executes present clause; **generates companion-domain output** |
| Gap closed | Mandate operability gap | Execution-directive gap |

**C-37 PARTIAL band structure (two confirmed, one aspirational):**
- **Band 1** (= C-36 PASS territory): operability declared; no execution instruction. Execution-directive gap entirely open.
- **Band 2** (aspirational): conditioned execution instruction present — inference burden reintroduced at scope-determination step.
- **PASS**: unconditional execution mandate in both clause bodies — "must be executed as if its execution note were present in this context."

**Compression-immune consolidation chain (extended):** … → C-35 → C-36 → C-37 (companion-activation instruction → mandate operability declaration → execution-directive closing actual inferred two-mechanism output generation)

**R18 investigation candidate:** Whether a C-37-compliant clause body under double-elision produces complete-parity two-mechanism output — companion mandate executed at depth equivalent to direct execution-note presence — or depth-asymmetric output (companion executed but abbreviated). If asymmetry is observed, whether an explicit parity constraint ("at full depth, at parity with direct execution-note presence") closes the output-depth gap and constitutes a C-38 candidate.
