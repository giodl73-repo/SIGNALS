v20 rubric written to `simulations/quest/rubrics/prove-prototype-rubric-v20-2026-03-15.md`.

---

**Three new aspirational criteria extracted from R20:**

**C-53** (per-phase lifecycle state + active sub-role callout lines, 5 pts) — V-03 demonstrated a per-phase callout line at every phase entry: `Phase callout: BUILD Phase 7 | Competitor lifecycle: IDENTIFIED AND COMMITTED | Active sub-role: IMPLEMENTER`. C-43 annotates lifecycle state at container headers; C-53 extends that annotation to every phase entry within each container, creating phase-granularity state tracking. Requires C-43 and C-48.

**C-54** (CALIBRATE sub-transitions as distinct table rows, 5 pts) — V-05 expressed each CALIBRATE sub-transition (NOT YET IDENTIFIED → IDENTIFIED and IDENTIFIED → IDENTIFIED AND COMMITTED) as a separate row in the framing table, each with independent Gate, Contamination Risk, and Isolation Purpose columns. C-43 requires narrating both sub-transitions; C-46 requires rationale triples per boundary; C-54 requires them as distinct table rows so the two-event commitment is irreversible at schema level, not just text level. Requires C-43 and C-46.

**C-55** (extended competitor surfaces 6 + 7, 5 pts) — V-05 tracked the inertia competitor at seven surfaces. The two new surfaces: Phase 8 measurement results table B-00 reference row labeled with the competitor name (BUILD interior anchor — the gap between C-32 at CALIBRATE COMPLETE and C-34 at CLOSE column label); and a document preamble competitor investigation declaration before the manifest (pre-manifest-space anchor — before C-40's NOT YET IDENTIFIED opening). Requires C-34 and C-40.

**Updated ceiling**: 367 → **382** (Aspirational 267 → 282). R21 seed: combine C-53 + C-54 + C-55 on the R20 base.
