Done. `simulations/quest/rubrics/flow-ratelimit-rubric-v20-2026-03-17.md` written.

**What changed:**

Aspirational pool: 40 → 43. New scoring formula: `aspirational_pass / 43 * 30` (≈ 0.698 pts each). A variation passing all 40 prior criteria but missing all three new ones scores `117.907/120`.

---

**C-49 — Registry-Plane Cross-Class Confusion Prohibition** (requires C-47)
- C-47 requires REGISTRY GAP and ORPHANED SCHEMA to be "separately named and audited" — satisfied by enumeration.
- C-49 closes the residual gap: the Plane 1 CHECK instruction must name the cross-class confusion form and declare it non-compliant (e.g., "a report that flags an ORPHANED SCHEMA entry as a REGISTRY GAP does not satisfy this requirement").
- Applies Excellence Signal 1 (named prohibited form) to the registry plane, exactly as C-46 applied it to the cell plane.

**C-50 — BURST PATH TABLE Orphaned-Row Violation Class** (requires C-48)
- C-48 names the downstream bypass direction: BASELINE cell skips the BURST PATH TABLE.
- C-50 adds the symmetric upstream orphan: a BURST PATH TABLE row that exists but is never referenced by any downstream SCHEMA-A BASELINE definition — **ORPHANED BURST PATH ROW**, detectable by scanning each row against all downstream SCHEMA-A BASELINE definitions.
- Applies Excellence Signal 2 (symmetric counterpart) to the BURST PATH TABLE plane, exactly as C-47 applied it to the registry plane.

**C-51 — Cell-Plane Violation Classes as Format Contract Rejection Clauses** (requires C-46)
- C-46 places the cell-plane merge prohibition in the CHECK instruction. CHECK is downstream; the Format Contract is the declaration site.
- C-51 promotes both cell-plane violation classes (SCHEMA-A CELL ANCHOR VIOLATION, SCHEMA-A UNRESOLVED REFERENCE VIOLATION) into the Format Contract's rejection clause list, making them scan-time citable without consulting CHECK.
- Applies Excellence Signal 3 (Format Contract rejection clause as scan-time authority) to the cell plane, exactly as C-48 applied it to the BURST PATH TABLE plane.

**Derivation symmetry:** Each new criterion applies one R20 excellence signal to the plane it hadn't yet reached — Signal 1 → registry plane (C-49), Signal 2 → BURST PATH TABLE plane (C-50), Signal 3 → cell plane (C-51).
esent, no matching Finding ID) — making them independently citable from the Format Contract without consulting the CHECK instruction.
- Follows the same escalation as C-48 (Format Contract rejection clause applied to C-45): whenever a compliance requirement is enforced only in CHECK, add the named violation class to the Format Contract's rejection clause list so it is scan-time citable at the declaration site.

---

## Essential (60 points total)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Rate Limit Tier Distinction** | correctness | essential | Output distinguishes at least three structurally distinct rate limit tiers relevant to the scenario (e.g., connector-level, environment-level, tenant-level, endpoint/API-level). Each tier must be identified by its scope, not just its numeric threshold. Listing multiple limits at the same scope level as separate tiers does not pass. |
| C-02 | **No Single-Category Collapsing** | correctness | essential | Output does not treat all rate limits as a single undifferentiated category. Each distinct throttle tier must be described with its own scope, enforcement mechanism, and failure mode. An output that names multiple tiers but describes them as a single undifferentiated limit category does not pass. |
| C-03 | **Observable UX Per Throttle Tier** | coverage | essential | Output describes observable user-facing behavior at each distinct throttle tier reached (e.g., 429 with Retry-After shown in connector error, flow run queued and delayed, flow run failed with ActionThrottled, silent exponential backoff invisible to user). At least two distinct tiers described if the scenario spans multiple tiers. |
| C-04 | **Unprotected Burst Path Identification** | correctness | essential | Output identifies at least one structurally unprotected burst path — a trigger or action that can generate concurrent requests exceeding a rate limit with no concurrency cap, no retry policy, and no queue buffer between the source and the rate-limited endpoint. A path that has throttle controls but at a higher tier does not qualify. |
| C-05 | **Retry-After Handling Gap Check** | coverage | essential | Output explicitly evaluates whether the flow or connector handles Retry-After headers (or equivalent backoff signals such as `x-ms-ratelimit-remaining`, `Retry-After-Ms`) from throttled endpoints. Missing handling must be flagged as a finding with a description of the failure mode it causes (e.g., immediate retry storm, permanent failure after N retries). |

---

## Recommended (30 points total)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-06 | **Cascading Throttle Failure Scenario** | depth | recommended | Output constructs or identifies a specific scenario where throttling at one tier triggers a second distinct throttle event at a different tier — and describes the compounding effect on throughput or error rate. Two independent limits both hit under load do not constitute a cascade; the second throttle must be causally triggered by the first. |
| C-07 | **Numeric Rate Limit Specificity** | depth | recommended | At least one rate limit is cited with a concrete numeric threshold (e.g., "600 API calls per 60 seconds per connection", "5 concurrent runs per environment per flow") rather than described only in abstract or relative terms. Estimates are acceptable if labeled as such. |
| C-08 | **Volume-to-Behavior Mapping** | format | recommended | Output includes a structured mapping — table, tiered list, or equivalent — that shows which throttle behavior activates at which request volume range, enabling a reader to determine expected system behavior for a given load without re-reading the full analysis. |

---

## Aspirational (30 points total — 43 criteria, ≈ 0.698 pts each)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-09 | **Per-bottleneck Mitigation Prescriptions** | depth | aspirational | For each identified bottleneck or unprotected burst path, output proposes a concrete remediation specific to the component and limit type (e.g., "enable concurrency control on the For Each action capped at 5 parallel branches", "add exponential backoff with Retry-After header parsing to the HTTP action", "split batch trigger into N sequential calls with 1s delay"). Generic advice ("add retry logic") does not pass — the prescription must name the specific action, setting, or pattern. |
| C-10 | **Quantified Impact at Load Spike** | depth | aspirational | Output provides numeric estimates of degradation at a specific load multiplier (e.g., "at 5x normal volume with a 600 req/min limit, 42% of requests are queued beyond 30s, 11% fail after exhausting 3 retry attempts") using the rate limits cited in C-07 as the arithmetic basis. The estimate must be derivable from the stated limits — not asserted as a generic percentage. |
| C-11 | **Burst Gap Classification (Structural vs. Incidental)** | depth | aspirational | *R1 excellence signal: V-05 was the only variation with an explicit structural-vs-incidental sub-test, identified as the sharpest C-04 enforcement across all variations.* Beyond identifying the unprotected burst path (C-04), output explicitly classifies whether the gap is structural (no mechanism exists on this path) vs. incidental (mechanism exists but is misconfigured, bypassable, or absent only under specific conditions). Classification must be stated and justified — not merely implied by framing. Requires C-04 to pass. |
| C-12 | **Dual-state Volume Mapping (Baseline vs. Protected)** | depth | aspirational | *R1 excellence signal: V-05 produced unique dual-state volume mapping (inertia vs. protected) across volume tiers, identified as satisfying C-08 more richly than any other variation.* The volume-to-behavior mapping extends C-08 to compare two states at each volume level: the current/unprotected baseline and the mitigated/protected state. Each volume tier shows what changes when protections are applied — not just what happens at that tier in isolation. Requires C-08 to pass. |
| C-13 | **Verdict-before-Evidence Structure** | format | aspirational | *R1 excellence signal: V-04's commitment-before-evidence (VERDICT-first) structure produced the clearest binding-order statements of any variation.* Output states the binding constraint or primary gap as an explicit labeled finding — a verdict or claim — before the supporting evidence section. The pre-commitment forces an analytical position rather than narration toward a conclusion. The evidence section must then confirm or explicitly revise the verdict; evidence that drifts without revisiting the verdict does not pass. |
| C-14 | **Baseline-delta Mitigation** | depth | aspirational | *R1 excellence signal: V-05's "Improvement over inertia" column dual-enforced that mitigations move from baseline state, rejecting both generic advice and improvements that don't change the baseline condition.* Each mitigation explicitly states the before-state (baseline behavior at the bottleneck) and the after-state (system behavior with the mitigation applied). Mitigations that could apply to any system without referencing the specific baseline condition do not pass. Requires C-09 to pass. |
| C-15 | **Document-head Global Verdict** | format | aspirational | *R2 excellence signal: Both V-01 and V-02 scored PARTIAL on C-13 — V-01 placed the binding constraint label in Section 3 (after Sections 1-2 audit); V-02 placed it within a mid-document analysis section. Neither enforced a standalone verdict block before any evidence or inventory. C-13 as written permits section-local verdicts that satisfy the label requirement without anchoring the analysis at document head.* Output opens with a standalone Verdict block — appearing before any rate limit inventory, burst path audit table, or volume mapping section — that states: (a) the binding constraint by component name and numeric threshold, (b) the primary gap (e.g., unprotected burst path at a named action, or Retry-After not handled at a named connector), and (c) the current system status at the stated load (safe / degraded / failing). The Verdict block must be self-contained: a reader who reads only it knows the core finding without proceeding to the evidence. Evidence sections must confirm or explicitly revise each Verdict claim; a Verdict that no evidence section references does not pass. Requires C-01 and C-04 to pass. |
| C-16 | **Format Contract Preamble** | format | aspirational | *R2 excellence signal: V-02's axis — "format contract preamble — BASELINE / PROTECTED dual-column on every table and mitigation" — produced the strongest C-12 enforcement of either variation. Declaring the dual-column structure as a document-wide contract before sections removes per-section negotiation about whether the dual-column applies and enforces consistency across all comparative tables simultaneously. Neither V-01 nor any prior variation captured this as an explicit structural discipline.* Output includes a Format Contract section placed before the first analysis section that declares: (a) the column structure that applies to every comparative table in the document (e.g., BASELINE \| PROTECTED \| Delta), (b) what BASELINE and PROTECTED mean in the specific scenario context — not generic definitions, but names tied to the scenario's components or states, and (c) a rejection clause stating that tables or mitigation rows omitting the declared structure are flagged as incomplete. At least two distinct tables elsewhere in the document must comply with the declared format; a format contract with only one compliant table does not pass. Requires C-08 and C-12 to pass. |
| C-17 | **Cascading Section Gate Enforcement** | format | aspirational | *R3 excellence signal: V-01's role sequence gating was scored "mechanically hard" and "most structurally enforced of all five variations" because every section transition carried explicit "Do not begin Role N until [specific deliverables from Role N-1] are written" language — extending the gate chain across all nine roles, not only the verdict-first and format-contract-first transitions captured by C-15 and C-16.* Beyond placing the Verdict and Format Contract as preambles (C-15, C-16), the document structure includes explicit per-transition gate language for at least three additional section transitions beyond the two preambles. Each gate must name the prior section's specific deliverables — not a generic "do not skip ahead" instruction — so that omission of any deliverable from section N-1 structurally blocks section N. A document with gate language only on the preamble transitions does not pass. Requires C-15 and C-16 to pass. |
| C-18–C-35 | *(18 criteria from R4–R14 — not reproduced here)* | various | aspirational | — |
| C-36 | **FNMI as Standalone Labeled Section** | format | aspirational | *R15 excellence signal: V-01's FNMI-R15 was positioned as a labeled section with its own section header between Role 8 and Role 9, giving it scan-time detectability without reading either surrounding role's content.* C-35 requires the invariant to be "declared before the terminal reconciler section opens, not inside it" — this permits the invariant to appear as a closing paragraph within the final analysis role or an introductory note in the reconciler's preamble, as long as it precedes the reconciler's check list. Beyond C-35 placement, the FNMI must appear as a standalone labeled section with its own section header — not embedded as a paragraph within a surrounding role — so that it is findable by scanning section headers alone. This parallels C-16 (Format Contract as a named section) and C-15 (Verdict block as a standalone block): both are findable by scanning section headers, not by reading prose. |
| C-37 | **FNMI Four-Field Single-Block Completeness** | format | aspirational | *R15 excellence signal: V-01's FNMI-R15 labeled each field as a distinct item so an evaluator can verify all four requirements by reading one block without cross-referencing.* C-35 requires all four fields (a)–(d) to be present in the invariant; it does not require them to be co-located as individually labeled items in one contiguous block. A conformant C-35 implementation could satisfy field (b) in one paragraph, field (c) implicitly via field (b)'s logic, and field (d) in a closing sentence. Beyond C-35 field presence, the four fields must appear as individually labeled items in a single contiguous block, so that an evaluator can verify all four requirements by reading one block without cross-referencing. This mirrors C-25's motivation: distinguishing checklist satisfaction (all items present somewhere) from atomic single-block satisfaction (all items verifiable as a unit). |
| C-38 | **Reconciler Compliance-Verification Framing** | format | aspirational | *R15 excellence signal: V-01's pattern converts the reconciler from self-describing to compliance-verifying: check (a) explicitly names FNMI-R15 and frames its output as a compliance verdict against the named external constraint, not as a description of the reconciler's own behavior.* C-34 requires the reconciler to produce three behavioral declarations; C-35 requires the constraint to be pre-declared. Neither criterion specifies that the reconciler must cite the FNMI by name or frame its output as a compliance verdict against an external constraint — a document satisfying both could produce behavioral self-assertions that match the FNMI's constraints without naming the invariant. Beyond C-34 and C-35, the reconciler's primary check must explicitly name the FNMI by its label and frame its output as a compliance verdict: `"[FNMI label]: COMPLIANT"` or `"[FNMI label]: VIOLATION at [Finding ID] — [description]"`. A reconciler that produces behavioral self-assertions matching the FNMI's constraints without citing the invariant by name does not pass. |
| C-39 | **Format Contract Scenario-Anchored Column Definitions** | format | aspirational | *R16 excellence signal: V-02 received PARTIAL on C-16 because "column definitions in (a) use generic schema labels, not scenario-specific component names" and "V-02 defers definition to wherever the scenario is introduced." C-16 requires declaring what BASELINE and PROTECTED mean "in the specific scenario context — not generic definitions, but names tied to the scenario's components or states," but does not require these definitions to be self-contained within the Format Contract section itself. V-01's Format Contract defines BASELINE and PROTECTED using the scenario's actual component names and states at the point of declaration in the Format Contract section, making it self-interpreting without requiring the reader to locate the scenario introduction.* Beyond C-16, the Format Contract's BASELINE and PROTECTED definitions must name the scenario's actual components, states, or configurations within the Format Contract section itself — not generic placeholders (e.g., "BASELINE = current state") or deferred references ("BASELINE = as defined in Role 3"). The definitions must be self-contained within the Format Contract section; a reader who reads only the Format Contract must be able to interpret what BASELINE and PROTECTED refer to without reading any other section. This parallels C-15's self-containment requirement for the Verdict block. Requires C-16 to pass. |
| C-40 | **Format Contract Per-Table-Type Schema Decomposition** | format | aspirational | *R16 excellence signal: V-02's Format Contract declared three distinct schema entries — primary table, volume mapping, and mitigation — each with its own column structure specification and a unified rejection clause. C-16 requires declaring "the column structure that applies to every comparative table in the document" and at least two compliant tables, but does not require separate schema entries for each table type. The per-table-type decomposition eliminates ambiguity about which column structure governs which table type and prevents format drift where a volume mapping table omits a column required only on inventory tables. This parallels C-37's motivation: distinguishing pool-level satisfaction (all items present somewhere) from per-type atomic satisfaction (each table type's schema verifiable as a unit).* Beyond C-16, the Format Contract must declare a separate named schema entry for each distinct table type used in the document — at minimum: rate limit inventory table, volume mapping table, and mitigation table. Each entry names the table type it governs and specifies its own column structure. A Format Contract with a single universal schema applied to all table types, or with fewer named schema entries than distinct table types in the document, does not pass. A universal schema that happens to satisfy all table types is not equivalent to per-type decomposition: per-type entries allow an evaluator to verify each table type's compliance independently. Requires C-16 to pass. |
| C-41 | **Format Contract Cross-Role ID Anchoring in Column Definitions** | format | aspirational | *R17 excellence signal: V-01's PASS on C-39 showed BASELINE defined as "the connector API call volume directed at the burst-path source endpoint identified by Finding ID in Role 3 (BP-xx), measured at the point of burst onset," with the explicit note "This definition refers to the specific connector endpoint and Finding ID produced in Role 3 — not a generic 'before' state." C-39 requires scenario-anchored definitions (not generic placeholders, not deferred references). V-01 goes further: the definition binds BASELINE to a specific upstream Finding ID artifact (BP-xx from Role 3) so that the definition is falsifiable against a prior-role deliverable, not merely contextually meaningful. A scenario-anchored name such as "the connector API call volume at burst onset" satisfies C-39 but is verifiable only by semantic interpretation; a Finding-ID-anchored definition is verifiable by cross-referencing the upstream role's output.* Beyond C-39, the BASELINE and PROTECTED definitions in the Format Contract must each reference at least one specific Finding ID produced by a named prior role (e.g., "BP-xx from Role 3", "RH-xx from Role 8c"), so that the definition is verifiable by locating that Finding ID in the upstream role's output. A definition that names scenario components or states without binding them to a specific upstream Finding ID satisfies C-39 but does not achieve falsifiability against a prior-role artifact. This parallels C-37's atomicity motivation: moving from field presence (all definitions exist and are scenario-named) to artifact traceability (each definition verifiable against a specific upstream artifact). Requires C-39 to pass. |
| C-42 | **Format Contract Role Governance Annotation** | format | aspirational | *R17 excellence signal: V-01's PASS on C-40 showed each schema entry declaring "Applies to: Role 8a cascade failure analysis; Role 7 quantified impact tables." C-40 requires a separate named schema entry for each distinct table type, each with its own column structure. V-01 goes further: each schema entry explicitly names the role(s) responsible for producing schema-conformant tables of that type. Without role governance annotation, a reader verifying a table in Role 8a must infer the applicable schema by identifying the table type; with annotation, the schema entry governing Role 8a is directly named, making compliance verification O(1) per role. A Format Contract with per-type schema entries that omit role assignments leaves production responsibility unassigned — the decomposition disambiguates column structure per type but does not bind each type to its producing roles.* Beyond C-40, each named schema entry in the Format Contract must include a role governance annotation identifying the specific role(s) that must produce schema-conformant tables of that type (e.g., "Applies to: Role 8a cascade failure analysis; Role 7 quantified impact tables"). A Format Contract with per-type schema entries that omit role governance annotations does not pass. Per-type decomposition without role assignment does not achieve the same property: a reader must perform type inference per table, whereas role-annotated entries allow direct lookup by role number. This mirrors C-38's reconciler-names-invariant requirement: just as the Reconciler must name the FNMI by label, each schema entry must name the roles it governs by reference. Requires C-40 to pass. |
| C-43 | **Schema Cell-Level ID Anchor** | format | aspirational | *R18 excellence signal: V-01's SCHEMA-A CELL ANCHOR pattern requires every SCHEMA-A cell to open with "BP-xx: [value]", making each cell independently verifiable without reading the Format Contract column definition. V-05 extends this by distinguishing two violation classes: a cell missing the prefix is a CELL ANCHOR violation (format violation, scan-time detectable); a cell with a syntactically valid prefix that has no matching Finding ID in Role 3 is an UNRESOLVED REFERENCE violation (format-compliant but semantically invalid). V-05's three-plane enforcement chain includes the cell plane as independently auditable: a Role 9 CHECK (e) auditing e.4–e.6 detects both violation classes in one pass. This advances C-41's artifact-traceability from column-definition level to per-cell level, closing the gap between format compliance and semantic validity.* C-41 requires BASELINE and PROTECTED column definitions to reference specific Finding IDs, making the column's semantic meaning artifact-falsifiable at declaration level. It does not require each data cell in a schema-conformant table to carry a resolved prefix linking back to the Finding ID the cell's value was measured from. Beyond C-41, every data cell in a SCHEMA-A-governed column must carry a resolved Finding ID prefix (e.g., "BP-xx: [value]") naming the specific upstream Finding ID the cell's value was measured from. A cell missing the prefix is a CELL ANCHOR violation (scan-time detectable). A cell whose prefix names a Finding ID that does not exist in the specified prior role is an UNRESOLVED REFERENCE violation (distinct from a missing prefix; both violation classes must be explicitly distinguished in any compliance check). A document that enforces cell-level anchoring without distinguishing the two violation classes does not fully pass. Requires C-41 to pass. |
| C-44 | **Schema Production Registry** | format | aspirational | *R18 excellence signal: V-02's Schema Production Registry provides a single aggregate table placed at the top of the Format Contract with rows mapping schema to producing role(s), table type, and gate condition. This enables O(1) role-to-schema lookup (find the row for the role, read its schema) and makes coverage verifiable by row count: row count must equal producing role count; a missing row is a REGISTRY GAP violation detectable without scanning schema body entries. V-04's CHECK (e) audits registry completeness (e.1–e.3); V-05's Plane 1 of the three-plane CHECK (e) adds the completeness rule explicitly. This mirrors C-38: just as the reconciler must name the FNMI by label rather than produce behavior that matches it, the Format Contract must aggregate role-schema mappings in a named registry rather than distribute them across per-entry annotations requiring aggregation to verify.* C-42 requires each schema entry to include a role governance annotation identifying the producing role(s), enabling per-role compliance lookup by reading the schema entry that names that role. It does not require a single aggregate table that makes total coverage verifiable by row count without scanning all schema entries. Beyond C-42, the Format Contract must include a Schema Production Registry — a dedicated named table placed before individual schema body entries — that aggregates all schema → producing role(s) → table type → gate condition mappings in one location. Registry completeness rule: every table-producing role must have exactly one registry entry; a missing row is a REGISTRY GAP violation detectable by comparing registry row count against producing role count. The per-entry role governance annotations required by C-42 may reference the registry but do not replace it; the registry's aggregate coverage function is distinct from per-entry annotation. Requires C-42 to pass. |
| C-45 | **BURST PATH TABLE as Authoritative Single-Source Anchor** | format | aspirational | *R18 excellence signal: V-03's BURST PATH TABLE concentrates all burst-path endpoint identification in one named table in Role 3 with columns including ENDPOINT NAME and SCHEMA-A BASELINE SOURCE. Every downstream BASELINE definition references this table by name and column rather than independently citing Finding IDs. Bidirectional traceability: each Format Contract BASELINE definition names the BURST PATH TABLE column it draws from (forward); at least one BURST PATH TABLE column is named for the schema it feeds (backward). V-05 combines BURST PATH TABLE anchoring with cell-level enforcement (C-43): every SCHEMA-A cell carries a resolved BP-xx prefix locating the specific BURST PATH TABLE row, making traceability a table lookup at every granularity. A Format Contract citing individual Finding IDs from Role 3 satisfies C-41 but not C-45: the definitions are artifact-falsifiable but the endpoint identity authority is dispersed rather than concentrated.* C-41 requires BASELINE and PROTECTED definitions to reference specific Finding IDs from named prior roles. This permits dispersed citation where each BASELINE definition independently locates its anchor artifact, creating potential endpoint identity ambiguity when multiple downstream roles define BASELINE independently. Beyond C-41, Role 3 must produce a dedicated named table (e.g., BURST PATH TABLE) that serves as the single authoritative source for all downstream BASELINE definitions. The table must have named columns that include the burst-path endpoint identifier (e.g., ENDPOINT NAME) and the BASELINE source reference for at least one downstream schema (e.g., SCHEMA-A BASELINE SOURCE). Every Format Contract BASELINE definition must reference this table by name and column — not independently cite Finding IDs. Bidirectional traceability must be present: each BASELINE definition names the BURST PATH TABLE column it draws from (forward), and at least one BURST PATH TABLE column is named for the schema it feeds (backward). A Format Contract that cites individual Finding IDs from Role 3 without routing through a consolidated named table satisfies C-41 but not C-45. Requires C-41 to pass. |
| C-46 | **Cell-Plane Violation Merge Prohibition** | format | aspirational | *R19 excellence signal: V-05's three-plane CHECK (e) includes an explicit prohibition clause on C-43 audit output: "A report that merges them as 'missing or invalid prefix' does not satisfy this requirement." C-43 requires CELL ANCHOR and UNRESOLVED REFERENCE violation classes to be "explicitly distinguished in any compliance check," which is satisfied by enumerating both classes separately — but a document can enumerate them without explicitly prohibiting the merged form. V-05 closes this residual gap: the CHECK instruction itself names the merged form and declares it non-compliant, converting the distinction from a structural property into a named output obligation. This parallels C-38 (reconciler must name FNMI by label, not merely produce matching behavior) and C-48 (Format Contract must name the violation class, not merely imply the prohibition by omission): each criterion adds a labeled prohibition where the prior criterion implied the requirement by structure alone.* C-43 requires the two violation classes to be explicitly distinguished in any compliance check. It does not require the CHECK instruction to name the merged-class form and declare it non-compliant. Beyond C-43, the compliance check instruction for SCHEMA-A cell auditing must include an explicit prohibition clause that names the merged violation form (e.g., "missing or invalid prefix") and states that a report merging the two classes under that label does not satisfy this requirement. An audit that enumerates CELL ANCHOR and UNRESOLVED REFERENCE violations separately without the named prohibition clause passes C-43 but not C-46. Requires C-43 to pass. |
| C-47 | **Registry Orphaned-Schema Violation Class** | format | aspirational | *R19 excellence signal: V-05's Plane 1 of the three-plane CHECK (e) adds ORPHANED SCHEMA as a second distinct violation class at the registry plane, separate from REGISTRY GAP. C-44 defines one detectable violation class: REGISTRY GAP (a table-producing role without a registry entry), detectable by comparing registry row count to producing role count. REGISTRY GAP catches undercount — the registry is smaller than it should be. V-05 introduces the symmetric class: ORPHANED SCHEMA (a schema body entry present in the Format Contract that has no corresponding registry entry). ORPHANED SCHEMA catches misalignment — a schema documented in the body was never registered. The two classes are logically independent: a Format Contract can have a REGISTRY GAP without any ORPHANED SCHEMA (missing a producing role's entry, body entries all registered), an ORPHANED SCHEMA without any REGISTRY GAP (all producing roles registered, a stale body entry unregistered), or both simultaneously. V-05 requires Plane 1 to name and separately audit both classes: REGISTRY GAP by comparing row counts, ORPHANED SCHEMA by scanning body entries against registry rows.* C-44 defines REGISTRY GAP as the single violation class detectable at the registry plane. It does not require detection of the symmetric case where a body entry exists without a registry entry. Beyond C-44, CHECK (e) Plane 1 must separately name and audit both violation classes: REGISTRY GAP (producing role without registry entry, detectable by row count comparison) and ORPHANED SCHEMA (schema body entry without registry entry, detectable by scanning body entries against registry rows). A compliance check that audits only REGISTRY GAP without naming ORPHANED SCHEMA as a distinct auditable class does not pass. Requires C-44 to pass. |
| C-48 | **BURST PATH TABLE Named Rejection Clause** | format | aspirational | *R19 excellence signal: V-05's Format Contract adds "SCHEMA-A BASELINE SOURCE VIOLATION" as a named rejection clause with the condition "BASELINE cell cites a Finding ID directly instead of routing through the BURST PATH TABLE." C-45 requires BASELINE definitions to route through the BURST PATH TABLE by name and column — this defines the required structure and implies that direct Finding ID citation is non-compliant. But C-45's prohibition is implied by what it requires: a document satisfying C-45 structurally cannot have direct-citation BASELINE cells, yet no document that fails C-45 can be flagged by a named violation class for the specific failure mode of bypassing the table. V-05 converts the implied prohibition into a named, labeled rejection clause in the Format Contract itself, making the failure mode detectable by class name rather than by structural inference. This follows the same escalation pattern as C-38 (Reconciler must name FNMI by label, not merely produce behavior that matches), C-43 (violation classes must be explicitly distinguished, not merely implied by structure), and C-46 (merge prohibition must be named, not merely implied by the distinction requirement): each criterion converts an implied constraint into a named obligation that can be cited by label.* C-45 requires every BASELINE definition to route through the BURST PATH TABLE by name and column. It does not require the Format Contract to name the violation that occurs when a cell bypasses this routing. Beyond C-45, the Format Contract must include a named rejection clause — a labeled violation class (e.g., "SCHEMA-A BASELINE SOURCE VIOLATION") — with an explicit rejection condition stating that a BASELINE cell citing a Finding ID directly instead of routing through the BURST PATH TABLE is non-compliant. The rejection clause must appear within the Format Contract section (not only in the CHECK instructions) and must name the violation class by label so that a non-compliant cell can be flagged by name. A document in which the routing requirement is structurally enforced but the bypass failure mode is not named does not pass. Requires C-45 to pass. |
| C-49 | **Registry-Plane Cross-Class Confusion Prohibition** | format | aspirational | *R20 excellence signal: V-05's three-plane CHECK (e) confirms that REGISTRY GAP and ORPHANED SCHEMA are separately named and independently auditable. C-47 requires both classes to be "separately named and audited" — satisfied by enumerating them — but a document can enumerate REGISTRY GAP and ORPHANED SCHEMA without prohibiting the case where one class is reported under the other's label (e.g., an ORPHANED SCHEMA entry flagged as a REGISTRY GAP, confusing the detection direction). This residual gap parallels the one C-46 closed for C-43: just as C-43's "explicitly distinguished" requirement permitted the merged form, C-47's "separately named and audited" requirement permits cross-class confusion at output time. Signal 1 pattern: whenever a criterion requires two classes to be separately named, escalate by naming the case where one class is reported under the other's label and declaring it non-compliant.* C-47 requires REGISTRY GAP and ORPHANED SCHEMA to be separately named and audited in CHECK (e) Plane 1. It does not require the CHECK instruction to name and prohibit the cross-class confusion form — where an ORPHANED SCHEMA entry is reported as a REGISTRY GAP (or vice versa), confusing the detection direction. Beyond C-47, the Plane 1 CHECK instruction must include an explicit cross-class confusion prohibition clause that names the confusion form (e.g., "a report that flags an ORPHANED SCHEMA entry as a REGISTRY GAP does not satisfy this requirement") and declares it non-compliant. An audit that produces separate REGISTRY GAP and ORPHANED SCHEMA findings without the named cross-class confusion prohibition passes C-47 but not C-49. Requires C-47 to pass. |
| C-50 | **BURST PATH TABLE Orphaned-Row Violation Class** | format | aspirational | *R20 excellence signal: V-05's BURST PATH TABLE enforcement addresses the downstream direction: C-48 names the rejection class for BASELINE cells that bypass the table (consumer skips the authority). The scorecard's "symmetric violation class" signal identifies the reverse direction as the next gap: a BURST PATH TABLE row that exists but is never referenced by any downstream SCHEMA-A BASELINE definition is an orphaned authority row — a burst path documented in Role 3 that no downstream schema consumed. This parallels C-47's addition of ORPHANED SCHEMA as the symmetric counterpart to REGISTRY GAP: C-44 caught undercount (missing registry entry), C-47 caught misalignment (body entry without registry row), and here C-48 catches consumer bypass (BASELINE cell skips the table) while C-50 catches producer orphan (BURST PATH TABLE row not consumed). Signal 2 pattern: any single-direction completeness check should be paired with its symmetric counterpart.* C-48 names the rejection class for the downstream bypass direction: a BASELINE cell citing a Finding ID directly instead of routing through the BURST PATH TABLE. It does not name or require detection of the symmetric upstream orphan direction: a BURST PATH TABLE row that exists but is never referenced by any downstream SCHEMA-A BASELINE definition. Beyond C-48, CHECK (e) Plane 2 must separately name and audit both violation classes: SCHEMA-A BASELINE SOURCE VIOLATION (downstream bypass — BASELINE cell skips the BURST PATH TABLE, named per C-48) and ORPHANED BURST PATH ROW (upstream orphan — a BURST PATH TABLE row has no corresponding SCHEMA-A BASELINE reference, detectable by scanning each row against all downstream SCHEMA-A BASELINE definitions). A compliance check that audits only the downstream bypass direction without naming ORPHANED BURST PATH ROW as a distinct auditable class does not pass. Requires C-48 to pass. |
| C-51 | **Cell-Plane Violation Classes as Format Contract Rejection Clauses** | format | aspirational | *R20 excellence signal: V-05's three-plane enforcement places the cell-plane violation classes (CELL ANCHOR, UNRESOLVED REFERENCE) in the CHECK instruction (C-46's prohibition clause in CHECK (e) Plane 3). C-48's pattern shows that the Format Contract itself is the stronger declaration site: rejection clauses placed in the Format Contract are scan-time citable without consulting the CHECK instruction, because the Format Contract is the authority document for schema compliance. C-46 promotes cell-plane enforcement from structural enumeration (C-43) to named prohibition in CHECK — but CHECK is a downstream instruction, not the declaration site. Signal 3 pattern: whenever a compliance requirement is enforced only in CHECK, add the named violation class to the Format Contract's rejection clause list so it is independently citable at the declaration site.* C-46 requires the compliance check instruction to include an explicit prohibition clause naming the merged cell-plane violation form. It does not require the Format Contract itself to carry named rejection clauses for the cell-plane violation classes, leaving those classes citable only via the CHECK instruction. Beyond C-46, the Format Contract must include two named rejection clauses for the cell-plane violation classes: (a) SCHEMA-A CELL ANCHOR VIOLATION — condition: a SCHEMA-A cell is missing the BP-xx prefix, and (b) SCHEMA-A UNRESOLVED REFERENCE VIOLATION — condition: a SCHEMA-A cell carries a BP-xx prefix that has no matching Finding ID in the specified prior role. Each clause must appear within the Format Contract section by name and condition, so that a non-compliant cell can be flagged by class label without consulting the CHECK instruction. A document whose cell-plane violation classes appear only in the CHECK instruction and not in the Format Contract rejection clause list does not pass. Requires C-46 to pass. |
