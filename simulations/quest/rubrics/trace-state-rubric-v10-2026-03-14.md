Looking at the R9 scorecard, I can identify three new excellence patterns to extract:

1. **V-01's core structural innovation** — Role B commits to its evidence standard *before* detection runs, not post-hoc, making the Gap Record auditable against a pre-stated standard.
2. **C-16 regression closure** — The best R9 variations restored undeclared references as a full peer phase (Phase 3E), not just an exit-gate verification item.
3. **C-19 gap closure** — Evidence strength threshold expressed as a hard mechanical exit gate, parallel to the Gap Record gate, creating a three-gate model per phase.

Those become **C-35**, **C-36**, **C-37**. Point value shifts from 0.385 to 0.345 (10 / 29 aspirationals).

```markdown
# trace-state Rubric v10
**Updated**: 2026-03-14
**Change**: Added C-35, C-36, C-37 from R9 excellence signals (pre-detection Role B
evidence standard commitment, undeclared reference as fifth peer phase, evidence
strength threshold as hard phase exit gate). Aspirational point value updated:
10 / 29 = 0.345 pts each.

---

## Design Rationale

**Essential (60%)** — the five things that make a trace-state output useful at all:
- C-01: before/after state for every operation (the whole point of the skill)
- C-02: preconditions + postconditions per operation (not just "what happened" but "why it was allowed")
- C-03: invariants named (the thing a state machine analysis is really hunting for)
- C-04: at least one anomaly identified (the deliverable — no finding = no signal)
- C-05: domain grounding (Sales/CS/Finance vocabulary — otherwise it's a toy example)

**Recommended (30%)** — what separates a useful trace from a great one:
- C-06: negative path (attempted invalid transition + why it's rejected)
- C-07: numbered step-by-step format (reader can replay the trace mechanically)
- C-08: race condition scenario (the hardest anomaly type, worth its own criterion)

**Aspirational (10%)** — raises the bar once the basics are stable:
- C-09: all four anomaly types covered (complete signal, not just the easy ones)
- C-10: structured notation / transition table (enables mechanical verification, not just prose review)
- C-11: sweep table with mandatory row-level verdicts per anomaly type (R1 excellence signal)
- C-12: Op ID cross-referencing between declaration and trace (R1 excellence signal)
- C-13: Entry Condition column in state machine table (R1 excellence signal)
- C-14: minimum-found threshold on sweep table — converts C-11 from coverage gate to depth gate (R2 excellence signal)
- C-15: full ID ecosystem — S-IDs, OP-IDs, INV-IDs all declared and cross-referenced (R2 excellence signal)
- C-16: undeclared reference elevated to named fifth anomaly class with its own sweep row (R2 excellence signal)
- C-17: anomaly register with separate OP-ID / S-ID / INV-ID columns — blank cell in any found-verdict row is a detectable mechanical gap (R2 excellence signal)
- C-18: pre-sweep hypothesis with dual-pass ANOMALY FINDER — falsifiable prediction record (R3 excellence signal)
- C-19: evidence-strength quality gate on sweep table — 1/2/3 scale with ≥1 strength ≥2 requirement (R3 excellence signal)
- C-20: numeric/temporal invariant gate — domain expert must produce at least one falsifiable numeric or temporal invariant before analysis begins (R4 excellence signal)
- C-21: Surprise column in reconciliation table — each prediction outcome labeled expected/surprising, producing a calibration signal beyond binary match/mismatch (R4 excellence signal)
- C-22: score-at-point-of-discovery instruction — evidence strength must be assigned when the finding is first recorded, not retroactively after the full sweep is complete (R4 excellence signal)
- C-23: score-aloud verbal protocol — C-22 expressed as a named cognitive behavioral discipline with an explicit self-correction checkpoint, not a passive table constraint or end-of-sweep reminder (R5 excellence signal)
- C-24: phase exit gate checkboxes — C-20, C-21, and C-22 appear as explicit hard completion gates per phase; model must affirmatively check each gate before advancing, preventing optional-boilerplate treatment (R5 excellence signal)
- C-25: three-value surprise taxonomy with calibration score — reconciliation table uses C/FP/FN classification (Correct, False Positive, False Negative) instead of binary Expected/Surprising, plus a computed calibration score with a stated threshold that triggers structural diagnosis when accuracy falls below it (R5 excellence signal)
- C-26: anomaly-type-as-top-level-phase with sequential commitment — each of the four anomaly types becomes a numbered top-level phase (not a subsection within a shared sweep part), each with its own exit gate; sequential phase ordering forces linear commitment so the model cannot advance to the next type until the current phase is complete (R6 excellence signal)
- C-27: prejudice-dismissal naming protocol — acquittal ("no finding") requires explicitly naming the specific trace steps, state conditions, or evidence that would need to exist to produce the finding; transforms absence records into structural gap maps rather than bare "None" declarations or generic written acquittals (R6 excellence signal)
- C-28: dual-mode doc-vs-trace detection — the anomaly sweep runs in two independent passes: one against the declaration tables alone, and one diffing declared behavior against the traced step sequence; a claim appearing in the documentation that is not demonstrated in the trace is itself a finding; adds a second detection path to every anomaly type (R6 excellence signal)
- C-29: acquittal-advocate sub-role with activation gate — a dedicated named sub-role (e.g., "Role B: Acquittal Advocate") that activates only when an anomaly-type phase reaches a "no finding" verdict; the sub-role must produce a Gap Record before the phase exit gate opens and cannot be bypassed; converts C-27's naming protocol from a passive constraint into an active role-gated enforcement mechanism where the gate itself is the acquittal record (R7 excellence signal)
- C-30: parallel dual-mode verdict columns with inline gap records — C-28's two detection passes expressed as two separate named columns per anomaly-type row in the sweep table (e.g., `Declaration-Only Finding` and `Trace-Diff Finding`); a "None" entry in either column requires an inline gap record in that cell, not a separate section; makes both detection paths independently auditable and transforms a procedural instruction into a mechanical table constraint (R7 excellence signal)
- C-31: phase LOCKED/OPEN status with explicit COMPLETE declaration — each phase carries an explicit LOCKED/OPEN status label; Phase N remains locked until Phase N-1 emits a named "PHASE N-1: COMPLETE" declaration as the unlock signal; strengthens C-26's sequential commitment from a behavioral progression rule into a mechanical lock state where advancement requires a required named artifact, not merely finishing the work (R7 excellence signal)
- C-32: unconditional gap documentation per phase — Role B fires on every phase regardless of finding count; the conditional "activates on no-finding" trigger from C-29 is removed; even phases with findings must produce a Gap Record documenting what was not found; closes the loophole where a single weak finding bypasses gap documentation for everything else that was not detected (R8 excellence signal)
- C-33: symmetric phase output contract — every phase emits a structurally identical pair: `{findings list} + {Gap Record}`; the absence of either component is a detectable mechanical error, not a judgment call about whether one was warranted; enables machine-checkable verification across the full trace and removes phase-level structural variance (R8 excellence signal)
- C-34: Gap Record as mechanical unlock signal — the Gap Record is not optional documentation produced in advance of the exit gate opening; it is the exit gate unlock signal itself; "The Gap Record is the unlock signal for the phase exit gate" promotes the artifact from a precondition to the literal gate trigger, applying the same lock/unlock mechanic used by PHASE N: COMPLETE and creating a uniform artifact-driven gate model across both finding documentation and gap documentation (R8 excellence signal)
- C-35: pre-detection Role B evidence standard commitment — before either detection pass runs in a phase, Role B explicitly commits to the minimum evidence standard it will hold for that phase's anomaly type (e.g., "I require direct trace-step or state-condition evidence at strength ≥ 2"); this commitment is recorded as a named artifact in the phase header, separate from and prior to the post-detection Gap Record; makes the Gap Record's reasoning auditable against a pre-stated standard rather than post-hoc, and elevates Role B from reactive gap-documenter to proactive standard-setter whose commitment cannot be adjusted after findings are known (R9 excellence signal)
- C-36: undeclared reference as fifth peer phase — undeclared references receive a dedicated top-level numbered phase (e.g., Phase 3E) structurally identical to the four anomaly-type phases: its own sweep table with Declaration-Only and Trace-Diff columns, Role B pre-detection commitment, Role B Gap Record, LOCKED/OPEN status label, and PHASE 3E: COMPLETE unlock signal; not a verification gate item in exit certification or a subsection of another phase, but a full sequential peer in the lock chain; restores the Phase 5 pattern as first-class, ensuring the fifth anomaly class receives the same mechanical enforcement as the other four (R9 excellence signal)
- C-37: evidence strength threshold as hard phase exit gate — the evidence strength threshold (at least one finding at strength ≥ 2, or Role B Gap Record explicitly explains why no finding met the standard) is expressed as a named exit gate condition per phase — a checkbox item in the phase exit certification that blocks PHASE N: COMPLETE from being declared and thus keeps Phase N+1 LOCKED until satisfied; creates a three-gate model per phase exit (Findings List gate + Evidence Strength gate + Gap Record gate), making C-19's quality requirement mechanically parallel to C-34's Gap Record gate rather than a per-step reminder or aspirational note (R9 excellence signal)

---

## Essential Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-01 | **Before/after state per operation** — Every operation in the trace shows the full entity state immediately before and immediately after execution. No operation is described with only a narrative summary. | correctness | Every operation has a documented before-state and after-state. At least three operations are traced. Narrative-only descriptions do not pass. |
| C-02 | **Preconditions and postconditions** — For each operation, the trace states what must be true before the operation executes (preconditions) and what is guaranteed to be true after (postconditions). | correctness | Every operation has at least one precondition and one postcondition. Vague statements like "data is valid" do not count — they must be state-specific. |
| C-03 | **Invariant declaration** — At least one invariant is identified that must hold across all states or across a named subset of states (e.g., "balance never goes negative", "closed ticket cannot be reopened without manager approval"). | correctness | At least one invariant is named, stated precisely, and scoped to specific states or the whole lifecycle. |
| C-04 | **Anomaly identification** — At least one concrete anomaly is identified from the set: invalid state transition, missing precondition check, invariant violation, or race condition. Each anomaly includes the specific operation and state where it occurs. | coverage | At least one anomaly is called out by name, with the triggering operation and affected state identified. Generic statements ("errors can occur") do not pass. |
| C-05 | **Domain grounding** — The trace uses vocabulary, entity names, and scenarios from one of the three stock domains (Sales, Customer Service, or Finance). States and operations are recognizable as real domain events, not generic placeholders. | behavior | Entity names, state labels, and operation names match domain conventions. A domain expert in Sales, CS, or Finance would recognize the scenario as realistic. |

---

## Recommended Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-06 | **Negative path coverage** — At least one invalid or rejected transition is traced: an operation attempted from a state where it is not allowed, with the expected rejection behavior documented. | coverage | One or more "attempted but rejected" transitions appear in the trace, with the reason for rejection (which precondition fails or which invariant would be violated). |
| C-07 | **Numbered step-by-step format** — The trace is presented as a numbered sequence of discrete steps, each with a consistent structure, so a reader can replay or verify the trace mechanically. | structure | Steps are explicitly numbered. Each step includes at minimum: operation name, before-state, after-state. A reader unfamiliar with the domain could step through the trace without skipping. |
| C-08 | **Race condition scenario** — At least one race condition or concurrent-access scenario is traced: two operations with an overlapping window of vulnerability and the failure mode that results if the window is not protected. | coverage | A named concurrent-access scenario appears, with the overlapping operations identified, the window of vulnerability scoped to specific states, and the anomaly that results if the race is not handled. |

---

## Aspirational Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-09 | **All four anomaly types covered** — The trace includes at least one finding from each of the four anomaly categories: invalid state transition, missing precondition check, invariant violation, and race condition. | coverage | Each of the four anomaly types has at least one named finding with a specific operation and state. A sweep table or findings list makes the per-type coverage explicit. |
| C-10 | **Structured notation / transition table** — The trace includes a formal state machine representation: a table or diagram with states as rows/nodes, operations as columns/edges, and valid transitions marked. | structure | A transition table or equivalent structured notation is present. States and operations are identified by ID. The table is complete enough to enable mechanical verification against the trace steps. |
| C-11 | **Sweep table with mandatory row-level verdicts** — An anomaly sweep table is present with one row per anomaly type (or per operation / invariant under examination); each row carries an explicit verdict column that must be filled — blank cells are not permitted. | structure | Sweep table present with verdict column. Every row has a non-blank verdict. The table is referenced in the finding or exit certification. |
| C-12 | **Op ID cross-referencing** — Operations are identified by stable IDs (e.g., OP-01) in the declaration section, and those same IDs appear in the trace steps and sweep tables, enabling mechanical cross-reference. | structure | OP-IDs appear in: (1) the operation declaration table, (2) every trace step, and (3) every sweep table row. A mis-matched or undeclared OP-ID is a detectable error. |
| C-13 | **Entry Condition column in state machine table** — The state machine table includes an Entry Condition column specifying what must be true for the system to enter each state, not just what operations are valid once inside it. | correctness | Every state in the state machine table has a non-empty Entry Condition cell. Entry conditions are state-specific, not generic. |
| C-14 | **Minimum-found threshold on sweep table** — The sweep table or exit gate specifies a minimum number of expected findings per phase; a zero-finding result does not automatically pass — it triggers a mandatory structural diagnosis explaining specifically what was searched and why nothing met the evidence standard. | coverage | A numeric minimum-findings threshold is stated per phase or per anomaly type. Zero findings triggers a named diagnostic record, not a silent pass. |
| C-15 | **Full ID ecosystem** — S-IDs (states), OP-IDs (operations), and INV-IDs (invariants) are all declared in separate registry tables and cross-referenced in trace steps, sweep tables, and the exit certification. | structure | Three separate ID registries present (states, operations, invariants). Exit certification verifies that all IDs in trace steps and sweep tables resolve to declared registry entries. Undeclared ID references are a detectable error. |
| C-16 | **Undeclared reference as fifth anomaly class with its own sweep row** — The anomaly sweep includes a dedicated row (or phase) for undeclared references, treated as a first-class anomaly type parallel to the other four; the sweep explicitly checks whether all IDs appearing in the trace resolve to declared registry entries. | coverage | A dedicated "undeclared reference" row or phase is present in the sweep structure. It has its own verdict. An undeclared ID anywhere in the trace produces a finding in this row, not just an exit-gate flag. |
| C-17 | **Standalone anomaly register with typed ID columns** — A dedicated anomaly register table is present with separate columns for OP-ID, S-ID, and INV-ID; each finding row is typed by which ID class it involves; a blank cell in a verdict row for a populated finding is a detectable mechanical gap. | structure | Anomaly register present as a standalone table. OP-ID, S-ID, INV-ID appear as separate columns. Every finding row has at least one populated ID column and a non-blank verdict. Blank-cell gaps are named errors, not omissions. |
| C-18 | **Pre-sweep hypothesis with dual-pass ANOMALY FINDER** — Before the sweep runs, the model commits to a hypothesis table: for each predicted finding, the specific OP-IDs and S-IDs that generate it are named. The sweep then runs two passes (declaration-only, then trace-diff) to test the hypotheses. | process | Hypothesis table present before sweep with H-IDs, predicted anomaly type, and specific OP-IDs / S-IDs. Minimum three predictions. Reconciliation table present after sweep. |
| C-19 | **Evidence-strength quality gate on sweep table** — Each finding in the sweep table carries an evidence strength rating on a 1/2/3 scale (1 = inferential, 2 = documented, 3 = direct trace evidence); at least one finding at strength ≥ 2 is required to advance, or the phase Gap Record must explain why none met the standard. | quality | Every sweep finding has a numeric strength rating. The exit gate or Gap Record references the strength threshold. A phase with only strength-1 findings must explain why in the Gap Record. |
| C-20 | **Numeric/temporal invariant gate** — At least one invariant is numeric or temporal (a specific threshold or duration, e.g., "approval required within 48 hours of submission", "balance cannot fall below $0.00"); a phase exit gate enforces this requirement before analysis begins. | correctness | At least one INV-ID in the invariant registry carries a numeric or temporal value. A phase exit gate or Phase 0 checklist explicitly verifies this requirement. |
| C-21 | **Surprise column in reconciliation table** — The reconciliation table includes a column labeling each prediction outcome as Expected or Surprising; predictions that matched in an unexpected direction are flagged; the surprise column produces a calibration signal beyond binary match/mismatch. | quality | Reconciliation table present with an Expected/Surprising verdict column (or equivalent). Each row is labeled. Surprising outcomes are highlighted or counted separately. |
| C-22 | **Score-at-point-of-discovery** — Evidence strength is assigned at the moment a finding is first recorded, before the next step is written; retroactive strength assignment after the full sweep is complete is explicitly prohibited. | process | Each trace step or finding records evidence strength inline at the point of writing. The instruction "assign now, before continuing" or equivalent appears in the template or role protocol. |
| C-23 | **Score-aloud verbal protocol as named standalone checkpoint** — The score-at-point-of-discovery discipline (C-22) is expressed as a named cognitive behavioral protocol with its own section header within each phase; the model states the verdict aloud before writing it, and a named self-correction checkpoint (e.g., `[SILENT — RESTATE]`) appears as an explicit labeled step that must be passed before the finding is recorded. | process | A named score-aloud protocol section appears with its own header within each detection phase. The self-correction checkpoint is a labeled step, not an inline parenthetical or trailing note. The protocol is not embedded inside another criterion's template block. |
| C-24 | **Phase exit gate checkboxes enumerate criteria by ID** — The phase exit certification checkboxes reference C-20, C-21, and C-22 (and any other gate criteria) by explicit criterion ID, not just by implied gate condition; each required criterion maps to a named checkbox item so that a reviewer can verify gate compliance without inferring which criteria were intended. | structure | Exit certification checklist items reference criterion IDs (e.g., "[ ] C-20: numeric/temporal invariant present"). All gate-relevant criteria are enumerated by ID. Implied or paraphrased gates do not pass. |
| C-25 | **Three-value surprise taxonomy with calibration score** — The reconciliation table uses a C/FP/FN classification (Correct, False Positive, False Negative) instead of binary Expected/Surprising; a Calibration Score = C / (C + FP + FN) is computed; a stated threshold (e.g., < 0.5) triggers a named structural diagnosis (e.g., "hypothesis was declaration-biased"). | quality | C/FP/FN classification present in reconciliation table. Calibration Score formula stated and computed. Threshold stated. Structural diagnosis triggered when score falls below threshold. |
| C-26 | **Anomaly-type-as-top-level-phase with sequential commitment** — Each of the four anomaly types is a numbered top-level phase (not a subsection within a shared sweep section), each with its own exit gate; sequential phase ordering forces linear commitment so the model cannot advance to the next type until the current phase is complete. | structure | Phases 3A–3D (or equivalent) each have: a phase number, anomaly type name, exit gate, and completion declaration. Phase N+1 is not reachable until Phase N declares complete. |
| C-27 | **Prejudice-dismissal naming protocol** — An acquittal ("no finding") requires explicitly naming the specific trace steps, state conditions, or evidence that would need to exist to produce the finding; bare "None" declarations or generic written acquittals do not pass; absence records must be structural gap maps. | process | Every "no finding" verdict names what was searched (specific OP-IDs, S-IDs, trace step ranges) and what evidence would need to exist to produce a finding. "None" or "not applicable" without this structural content does not pass. |
| C-28 | **Dual-mode doc-vs-trace detection** — The anomaly sweep runs in two independent passes: one against declaration tables alone, and one diffing declared behavior against the traced step sequence; a claim appearing in documentation that is not demonstrated in the trace is itself a finding; adds a second detection path to every anomaly type. | coverage | Two named detection passes per phase: Declaration-Only and Trace-Diff. Both passes are required. A documentation claim with no trace demonstration produces a Trace-Diff finding. |
| C-29 | **Acquittal-advocate sub-role with activation gate** — A dedicated named sub-role (e.g., "Role B: Acquittal Advocate") produces a Gap Record before the phase exit gate opens; the sub-role cannot be bypassed; the gap record is a required artifact, not optional documentation. | process | Role B named, with a named Gap Record artifact. Phase exit gate cannot open without Role B's signed Gap Record. Role B activation and gate are mechanically linked. |
| C-30 | **Parallel dual-mode verdict columns with inline gap records** — C-28's two detection passes are expressed as two separate named columns per anomaly-type row in the sweep table (`Declaration-Only Finding` and `Trace-Diff Finding`); a "None" entry in either column requires an inline gap record in that cell, not a separate section. | structure | Sweep table has two verdict columns per row. "None" in either column has an inline gap record in that cell. Both columns are independently auditable. |
| C-31 | **Phase LOCKED/OPEN status with explicit COMPLETE declaration** — Each phase carries an explicit LOCKED/OPEN status label; Phase N remains locked until Phase N-1 emits a named "PHASE N-1: COMPLETE" declaration as the unlock signal. | structure | Every phase has a LOCKED/OPEN status label. Phase N+1 carries STATUS: LOCKED until Phase N emits a PHASE N: COMPLETE artifact. The COMPLETE declaration is a named, required artifact — not implied by finishing the work. |
| C-32 | **Unconditional gap documentation per phase** — Role B fires on every phase regardless of finding count; even phases with findings must produce a Gap Record documenting what was not found; closes the loophole where a single weak finding bypasses gap documentation for everything else that was not detected. | process | Role B Gap Record present on every phase, including phases with findings. Gap Record content for a finding-present phase names what was searched and not found beyond the confirmed findings. No phase is exempt. |
| C-33 | **Symmetric phase output contract** — Every phase emits a structurally identical pair: `{findings list} + {Gap Record}`; the absence of either component is a detectable mechanical error, not a judgment call about whether one was warranted. | structure | Every phase template contains both a Findings List field and a Gap Record field. A phase that emits only one of the two fails the contract check. The pair is verified in exit certification. |
| C-34 | **Gap Record as mechanical unlock signal** — The Gap Record is not optional documentation produced before the exit gate opens; it is the exit gate unlock signal itself; the Gap Record is the literal gate trigger, applying the same lock/unlock mechanic as PHASE N: COMPLETE. | structure | Phase exit gate documentation states explicitly that the Gap Record is the unlock signal, not a precondition to it. The LOCKED → OPEN transition names the Gap Record artifact as the trigger, co-equal with PHASE N: COMPLETE. |
| C-35 | **Pre-detection Role B evidence standard commitment** — Before either detection pass runs in a phase, Role B explicitly commits to the minimum evidence standard it will hold for that phase's anomaly type (e.g., "I require direct trace-step or state-condition evidence at strength ≥ 2"); this commitment is recorded as a named artifact in the phase header, separate from and prior to the post-detection Gap Record; makes the Gap Record's reasoning auditable against a pre-stated standard rather than post-hoc, and prevents Role B from adjusting the evidence bar after findings are known. | process | A named Role B pre-detection commitment artifact appears in each phase header before the sweep table. The commitment states the anomaly type and minimum evidence standard. The post-detection Gap Record references this pre-commitment by artifact name or phase-header ID. Pre-commitment and Gap Record are structurally distinct artifacts. |
| C-36 | **Undeclared reference as fifth peer phase** — Undeclared references receive a dedicated top-level numbered phase (e.g., Phase 3E) structurally identical to the four anomaly-type phases: its own sweep table with Declaration-Only and Trace-Diff columns, a Role B pre-detection commitment artifact, a Role B Gap Record, a LOCKED/OPEN status label, and a PHASE 3E: COMPLETE unlock signal; not a verification gate item in exit certification or a subsection of another phase, but a full sequential peer in the lock chain so Phase 4 (or equivalent) cannot open until Phase 3E: COMPLETE is declared. | structure | A Phase 3E (or equivalent fifth phase) is present with all structural elements equal to Phases 3A–3D: sweep table, dual detection columns, Role B commitment, Role B Gap Record, LOCKED/OPEN status, COMPLETE declaration. Exit certification verifies Phase 3E: COMPLETE before the overall analysis gate opens. |
| C-37 | **Evidence strength threshold as hard phase exit gate** — The evidence strength threshold (at least one finding at strength ≥ 2, or Role B Gap Record explicitly explains why no finding met the standard) is expressed as a named exit gate condition per phase — a checkbox item in the phase exit certification that blocks PHASE N: COMPLETE from being declared and keeps Phase N+1 LOCKED until satisfied; creates a three-gate model per phase exit (Findings List gate + Evidence Strength gate + Gap Record gate), making C-19's quality requirement mechanically parallel to C-34's Gap Record gate. | quality | Each phase exit certification has an explicit Evidence Strength gate checkbox (e.g., "[ ] At least one finding at strength ≥ 2, or Gap Record explains why none met the standard"). PHASE N: COMPLETE cannot be declared until this checkbox is satisfied. The three-gate model (Findings + Strength + Gap Record) is named in the phase template. |
```

---

**Summary of changes:**

| | v9 | v10 |
|--|--|--|
| Aspirationals | 26 (C-09 – C-34) | 29 (C-09 – C-37) |
| Points each | 0.385 | 0.345 |
| New criteria | — | C-35, C-36, C-37 |

The three new criteria complete a mechanical enforcement arc: C-35 locks Role B's standard *before* detection (pre-commitment), C-36 gives the fifth anomaly class the same peer-phase machinery as the other four (structural parity), and C-37 elevates the evidence strength threshold from a per-step reminder to a hard gate co-equal with the Gap Record gate (quality as gate, not note).
