Written to `simulations/quest/rubrics/topic-echo-rubric-v18-2026-03-16.md`.

---

**v18 summary -- three criteria added from R17 excellence signals:**

| ID | Gate | Pattern | Structural move |
|----|------|---------|-----------------|
| C-55 | C-52+C-53+C-54 | `TOKEN-FORMAT-SELF-CERTIFYING` | All gate tokens carry inline specifics sufficient to audit closure from token text alone |
| C-56 | C-53+C-54 | `GATE-INDEPENDENCE-ENFORCED` | Combined tokens explicitly prohibited at co-located gate boundaries; each gate emits independently |
| C-57 | C-52+C-54 | `MANIFEST-ROW-INLINE-EVIDENCE` | TYPE-C manifest rows carry confirming artifact inline -- manifest is a terminal evidence layer |

**Score model:** 320 -> 335 max, 46 -> 49 aspirational (5 pts each).

**Gate chain:** R10 auditable -> ... -> R16 citation exhaustively manifested, bidirectionally indexed, and chain-grounded -> R17 tokens self-certifying, gates independent, manifest rows inline-evidenced.

---

**R17 projected scoring under v18:**

| Variation | C-55 | C-56 | C-57 | v17 | v18 | delta |
|-----------|:----:|:----:|:----:|:---:|:---:|:-----:|
| V-01 (Axis A) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-02 (Axis B) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-03 (Axis C) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-04 (A+B) | FAIL | FAIL | FAIL | 315 | 315 | 0 |
| **V-05 (A+B+C)** | PASS | PASS | PASS | 320 | **335** | **+15** |

**Scoring rationale:** V-05 is the only variation that satisfies all three gate prerequisites simultaneously. C-55 requires C-52+C-53+C-54 (all three token types present); C-56 requires C-53+C-54 (the Step-7 dual-gate case); C-57 requires C-52+C-54 (manifest + extended Belief-Ref). V-01 through V-04 each lack at least one prerequisite: V-01/V-03 lack C-53, V-02/V-03 lack C-52, V-01/V-02/V-04 lack C-54.

---

**Pattern relationships:**

The three R17 patterns form a coherent set operating on the closure layer established by R16:

- **C-55** applies to all three R16 gate tokens and requires each to be self-certifying -- the token is the audit record, not a pointer to the audit record. This closes the token-content gap: a token that merely asserts closure without naming what it confirmed cannot be verified without traversal.

- **C-56** applies at the Step-7 boundary where C-53 and C-54 co-locate. It closes the gate-composition gap: when independent criteria share a phase boundary, combined tokens erase individual falsifiability. The explicit prohibition requirement ensures the constraint survives future variations that might be tempted to merge tokens for brevity.

- **C-57** applies the C-54 terminal-evidence format to the C-52 manifest layer. C-54 made Belief-Ref cells terminal at evidence; the manifest was built before C-54 existed and its TYPE-C rows were left at RESOLVED-only. C-57 closes the manifest-evidence gap by mirroring the C-54 extension one layer up.
elief-Ref cells in the STILL-LIVE FILTER terminal at evidence (belief ID + confirming artifact inline). C-57 applies the same terminal-evidence standard to TYPE-C rows in the CITATION-COMPLETENESS-MANIFEST: the RESOLVED status cell carries the confirming artifact attribution so the manifest row alone is a complete citation record. TYPE-A and TYPE-B rows are unaffected -- their RESOLVED status reflects structural/procedural resolution, not belief falsification, and inline evidence attribution is not applicable to them.

---

The three new criteria complete the closure-layer audit established by R16 across three dimensions: C-55 closes the token-content gap (tokens are now fully self-certifying), C-56 closes the gate-composition gap (independent gates cannot be merged at shared boundaries), and C-57 closes the manifest-evidence gap (manifest rows for belief-grounded citations carry the falsifying artifact inline).

Gate chain: R10 output auditable -> R11 output non-defeatable -> R12 enforcement legible -> R13 structure navigable -> R14 element addressable -> R15 address citation-activated -> R16 citation exhaustively manifested, bidirectionally indexed, and chain-grounded -> R17 tokens self-certifying, gates independent, manifest rows inline-evidenced.

---

**Score model:**

| Tier | Points | Criteria | Per-criterion |
|------|--------|----------|---------------|
| Essential | 60 | 5 | 12 |
| Recommended | 30 | 3 | 10 |
| Aspirational | **245** | **49** | 5 |
| **Max composite** | **335** | | |

**R17 scoring under v18:**

| Variation | C-55 | C-56 | C-57 | v17 | v18 | delta |
|-----------|:----:|:----:|:----:|:---:|:---:|:-----:|
| V-01 (Axis A) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-02 (Axis B) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-03 (Axis C) | FAIL | FAIL | FAIL | 310 | 310 | 0 |
| V-04 (A+B) | FAIL | FAIL | FAIL | 315 | 315 | 0 |
| **V-05 (A+B+C)** | PASS | PASS | PASS | 320 | **335** | **+15** |

---

## Essential Criteria (weight: 60 points total, 12 pts each)

Five criteria. All must pass to qualify as golden or passing.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Named surprise with departure framing** | correctness | The artifact names at least one finding explicitly framed as a departure from expectation. The framing must be structural -- the surprise is labeled or marked as such, not merely described. A finding presented as a neutral observation that happens to be unexpected does not pass. Minimum acceptable framing: "We expected X to be negligible; it turned out to dominate" (surprise). |
| C-02 | **Signal tracing per surprise** | correctness | Every named surprise is traced to a specific source signal or artifact by name. Generic attribution is insufficient. Each surprise must name the artifact that produced it. A surprise without a traceable source cannot be audited by the next team and fails. |
| C-03 | **Design impact assessed per surprise** | depth | Every named surprise carries an explicit assessment of its impact on design direction. Impact may be confirming, redirecting, or destabilizing. An impact statement that does not identify which design decision is affected fails. "This changes things" without naming what changes is insufficient. |
| C-04 | **Synthesis not summary** | behavior | The artifact contains only surprises and their analysis. Expected findings are either absent or explicitly marked as context-only. An artifact that enumerates findings and labels some "interesting" fails -- the surprise/expected partition must be explicit and structural. |
| C-05 | **Surprise specificity** | correctness | Each surprise is specific to this investigation's signal set, not a generic observation that could appear in any investigation. "APIs are harder to design than expected" fails -- it applies universally. "The scout:competitors signal revealed that all three competitors treat X as a solved problem, contrary to our assumption that the space was unsolved" passes -- it is falsifiable against this investigation's artifacts. |

---

## Recommended Criteria (weight: 30 points total, 10 pts each)

Output is better with these. Golden typically requires at least 2 of 3.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | **Expectation counterfactual** | depth | Each surprise explicitly names what was expected versus what was found. The counterfactual structure is stated, not implied. A reader new to the topic can reconstruct the original assumption from the artifact alone without consulting any prior document. |
| C-07 | **Institutional framing** | behavior | The artifact is explicitly addressed to a future team. At least one element signals forward-facing intent: surprises framed as things the next team would need to know, a forward statement naming the specific avoided failure, or an explicit handoff to the consuming phase. |
| C-08 | **Cross-signal pattern identification** | coverage | If two or more surprises share an underlying cause or implication, that relationship is named with a statement of what they have in common. A list of independent surprises that happen to share a root cause fails -- the relationship must be explicitly identified. |

---

## Aspirational Criteria (weight: 245 points total, 5 pts each)

Raise the bar once essential and recommended criteria are stable.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | **Surprise hierarchy by design impact** | depth | Surprises are ranked by design impact with an explicit rationale that is argued, not asserted. A ranked list without argument is insufficient. The rationale must name the design dimension on which each surprise is ordered. |
| C-10 | **Riskiest surprise flagged** | behavior | The single surprise most likely to invalidate a core assumption is explicitly flagged. The flag names the assumption at risk, the signal that revealed the risk, and the consequence if the team proceeds without addressing it. |
| C-11 | **Pre-committed prior belief inventory** | correctness | The artifact contains a Prior Belief Inventory produced before signal reading -- a discrete, labeled section predating surprise entries that lists the team's going-in assumptions. The PBI is frozen: no belief may be added after signal reading begins. A PBI that lists beliefs discovered during signal reading fails. |
| C-12 | **Named surprise handles** | behavior | Each surprise carries a short named handle (2-5 words) specific enough to be cited by future signal artifacts without re-reading the echo. Test: could a future artifact cite "COMPETITORS-TREAT-X-AS-SOLVED" unambiguously? A handle that duplicates another handle in the same artifact fails. |
| C-13 | **Dual phase-locked pre-commitment integrity** | correctness | The PBI and Handle Ledger are demonstrably independent -- PBI entries use belief language; Handle Ledger titles use finding language. An artifact where PBI entries anticipate specific findings fails. |
| C-14 | **Single-entry audit trail completeness** | behavior | Each surprise entry contains all three pointers enabling provenance verification without consulting other sections: (1) Handle Ledger title, (2) PBI-Ref naming the prior belief the surprise corrects, (3) source signal name. A surprise entry missing any pointer fails. |
| C-15 | **Pre-commitment enforcement mechanism declaration** | correctness | The artifact declares how its PBI was isolated from signal knowledge. A timestamped freeze declaration provides temporal provenance. A declaration that the PBI "was written before reading signals" without a mechanism fails -- mechanism means the structural or procedural constraint that made contamination impossible, not an assertion that it didn't happen. |
| C-16 | **Production-time per-entry verification attestation** | behavior | Each surprise entry includes an explicit statement confirming all three audit pointers were verified at production time: (1) PBI-Ref cites a real PBI entry; (2) Handle Ledger title is unique; (3) source signal name resolves to an artifact in the signal set. A generic "all entries verified" footer fails -- attestation must be per-entry. |
| C-17 | **Enforcement mechanism strength classification with calibration rationale** | correctness | The artifact classifies its declared enforcement mechanism on the strength hierarchy (structural > procedural > attestation-only) with an explicit rationale explaining why the chosen mechanism is sufficient given this investigation's risk profile. A classification without rationale fails. |
| C-18 | **Per-entry attestation with evidentiary quotation** | behavior | Each surprise entry's production-time attestation quotes the actual text of the PBI entry and source finding being attested -- not paraphrases, not summaries. The quote is sufficient for a reviewer to verify the attestation without locating the source documents. A paraphrase or summary fails. |
| C-19 | **Post-production chain integrity audit with visible tokens** | behavior | After the artifact is written, a discrete Chain Integrity Audit re-verifies all four chain elements [PBI-Ref, Handle, source signal, impact assessment] per entry and emits a per-entry CHAIN-FLAG token: CHAIN-FLAG:PASS if all four elements verified, CHAIN-FLAG:FAIL:[reason] if any element fails. The audit section is in the artifact output, not in a separate document. |
| C-20 | **Impact-anchored distillation layer with traceability verification** | behavior | The artifact includes a Rules of Thumb section encoding the highest-impact surprises in <=20-word quotable form with a tier annotation ([HIGH], [MEDIUM]) and a traceability check confirming each rule anchors to a named surprise entry. A rule without a surprise anchor fails the traceability check. |
| C-21 | **Enforcement mechanism declaration in independently navigable section** | behavior | The C-17 enforcement mechanism classification is placed in a discrete headed section or titled table that is reachable by heading navigation alone -- no scrolling through body text required. A classification buried in prose within a longer section fails. |
| C-22 | **Chain-flag progression gate** | behavior | The CHAIN-FLAG token produced by the Chain Integrity Audit (C-19) functions as a hard production gate: any CHAIN-FLAG on any entry blocks further artifact production until the flag is resolved. The artifact explicitly states this gate condition -- not as a recommendation but as a blocking rule. |
| C-23 | **Distillation phase blocking condition with named gate** | behavior | The RULES-ANCHORED traceability check (C-20) names an explicit blocking condition: production cannot proceed to the next phase until every rule passes the traceability check. The blocking condition is stated with a named gate token (e.g., RULES-ANCHORED) that is visible in the output. |
| C-24 | **Enforcement mechanism section at artifact head position** | behavior | The C-21 independently navigable section containing the enforcement mechanism declaration is placed at position 1 of the artifact -- before all surprise entries, before the PBI, before the Handle Ledger. A reviewer reading from position 1 encounters the enforcement mechanism before any claim the artifact makes. |
| C-25 | **CHAIN-FLAG resolution protocol with named repair action per flag type** | behavior | The CHAIN-FLAG production gate (C-22) specifies not only that production stops but how it restarts. For each flag type (PBI-Ref missing, Handle collision, source signal unresolvable, impact assessment absent), the resolution protocol names the specific repair action that clears the flag. A gate without a resolution protocol fails. |
| C-26 | **NO-ECHO COST declaration in head-position enforcement section** | behavior | The C-24 head-position enforcement section includes a NO-ECHO COST declaration: a statement naming the institutional cost of not producing this artifact -- what the next team loses if the echo is skipped. The declaration is a concrete loss, not a generic "reduces quality" statement. |
| C-27 | **Forward Statement names the specific avoided failure** | behavior | The terminal Forward Statement (or equivalent closing section addressed to the next team) names the specific failure that this echo prevents -- not a general benefit but the concrete wrong turn a future team would make if they did not have this artifact. |
| C-28 | **RULES-ANCHORED-COMPLETE closure token in distillation phase** | behavior | The RULES-ANCHORED check that gates distillation-phase exit (C-23) emits a named RULES-ANCHORED-COMPLETE token when every rule passes the traceability check. The token is present in the artifact output. Its absence is a detectable signal that distillation is incomplete. |
| C-29 | **RESOLUTION PROTOCOL as peer-level standalone declaration block** | behavior | The RESOLUTION PROTOCOL containing the per-flag-type repair actions (C-25) is formatted as a standalone ALL-CAPS headed block, not as a sub-item within the gate condition. A reviewer navigating the artifact by headings can locate the RESOLUTION PROTOCOL independently. |
| C-30 | **EF role-boundary protection for NO-ECHO COST declaration** | behavior | The NO-ECHO COST declaration required by C-26 is derived by a role-scoped function (Enforcement Framer, EF) at Step 0 before any signal exposure. EF's invocation at Step 0 is the structural guarantee that the cost declaration is not contaminated by signal knowledge. |
| C-31 | **RESOLUTION PROTOCOL with named verifier role per flag type** | behavior | The RESOLUTION PROTOCOL (C-29) names, for each flag type, a verifier role: the specific role whose attestation is required to clear that flag type. A resolution protocol that specifies repair actions without naming verifiers fails. |
| C-32 | **Evidentiary tier quotation in RULES-ANCHORED-COMPLETE token** | behavior | The RULES-ANCHORED-COMPLETE closure token (C-28) quotes the actual tier annotation string ([HIGH] or [MEDIUM]) for each rule anchored in the distillation layer, confirming tier assignments are present and verifiable from the token alone. A token that states "all rules anchored" without quoting tier annotations fails. |
| C-33 | **EF-INVOCATION-RECORD at Step 0** | behavior | The EF role-boundary established by C-30 produces an EF-INVOCATION-RECORD: a discrete block immediately following the ENFORCEMENT MECHANISM DECLARATION that records what EF saw at Step 0 (investigation topic, signal set size, declared mechanism, and NO-ECHO COST output). The record makes EF's invocation auditable after the fact. |
| C-34 | **BC-COVERAGE-RECORD** | behavior | The EF-INVOCATION-RECORD established by C-33 makes EF's scope observable from output. C-34 applies the identical standard to BC (Belief Compiler): BC produces a BC-COVERAGE-RECORD as part of its output, recording the full set of PBI entries examined during belief compilation and the disposition of each (accepted, deferred, or discarded). The record makes BC's coverage auditable. |
| C-35 | **PHASE-HANDOVER tokens at EF-exit and BC-exit** | behavior | C-33 makes role entry auditable at invocation time (EF-INVOCATION-RECORD records what EF saw before acting). C-35 makes role exit auditable: EF and BC each emit a PHASE-HANDOVER token at role exit naming the output produced, the consuming phase, and any unresolved items passed forward. |
| C-36 | **DISCARD LOG with typed entries and DISCARD-LOG-COMPLETE** | behavior | C-20 requires the distillation layer to extract the highest-impact surprises into Rules of Thumb with tier annotations. C-36 requires the distillation layer to also log every candidate surprise that was considered but not accepted into the Rules of Thumb, with a typed disposition entry: DISCARD-[N]: [handle] -- [reason]. DISCARD-LOG-COMPLETE is emitted only when every non-accepted candidate has a typed entry. |
| C-37 | **BC Step 1 sub-phase decomposition** | structure | C-34 makes BC's coverage record auditable as an output. C-37 makes BC's internal process observable: Step 1 is decomposed into three named sub-phases (inventory, evaluation, compilation) each with a visible output boundary. A BC process that produces a coverage record without sub-phase boundaries fails. |
| C-38 | **PHASE-HANDOVER table schema** | structure | C-35 makes role exits auditable via PHASE-HANDOVER tokens. C-38 standardizes token format: each PHASE-HANDOVER token is rendered as a 5-row markdown table with fixed column headers (Step, Role, Output, Consuming-Phase, Unresolved). A PHASE-HANDOVER token in free-form prose fails. |
| C-39 | **STILL-LIVE FILTER as unconditional imperative commands** | structure | C-36 requires typed DISCARD-[N] entries for every non-accepted candidate. C-39 makes that completeness structurally enforceable: the STILL-LIVE FILTER is written as unconditional imperative commands (MUST, MUST NOT) with no conditional branching. A filter written as guidelines or recommendations fails. |
| C-40 | **BC-STEP1-PROTOCOL pre-execution declaration** | structure | C-37 decomposes BC's Step 1 into named sub-phases with observable boundaries. C-40 makes the decomposition positionally legible: the BC-STEP1-PROTOCOL is declared as a standalone block before Step 1 execution begins, not discovered within Step 1 output. A reader encounters the protocol before the output it governs. |
| C-41 | **PHASE-HANDOVER universal coverage** | structure | C-38 standardizes the PHASE-HANDOVER schema to a 5-row table format applicable at EF and BC role exits. C-41 extends that schema to all step transitions in the artifact, making PHASE-HANDOVER universal: every step boundary emits a PHASE-HANDOVER table, not only role-exit boundaries. |
| C-42 | **MUST-clause scope declaration** | structure | C-39 requires the STILL-LIVE FILTER to be written as unconditional imperative commands, eliminating conditional branching. C-42 makes each clause self-describing: every MUST or MUST NOT clause includes an explicit universal quantifier naming the governed entity class (e.g., "Every PBI entry", "All DISCARD-LOG entries"). A clause without a scope declaration fails. |
| C-43 | **BC-STEP1-PROTOCOL heading sovereignty** | structure | C-40 requires BC-STEP1-PROTOCOL to be positioned as a standalone declaration before Step 1 begins. C-43 requires that standalone declaration to appear as a named heading at first-class document level, heading-navigable without entering any enclosing section. A BC-STEP1-PROTOCOL nested as a sub-item within a step section fails. |
| C-44 | **PHASE-HANDOVER inventory pre-declaration** | structure | C-41 extends PHASE-HANDOVER tables to all step transitions, making universality verifiable by cross-table column inspection. C-44 requires an explicit inventory of expected PHASE-HANDOVER transitions to be declared before Step 0, listing all transitions by identity (Step X -> Step Y: T-ID). A reader can verify universal coverage by matching the inventory against emitted tables. |
| C-45 | **MUST-clause scope-field separation** | structure | C-42 requires each MUST-clause to include an explicit universal quantifier naming the governed entity class. C-45 requires that quantifier to appear as a named structural field (Scope) in a separate column from the imperative action (Action), not embedded in the Action clause prose. A MUST-clause with a single prose cell containing both scope and action fails. |
| C-46 | **BC-COVERAGE-RECORD schema heading sovereignty** | structure | C-43 requires BC-STEP1-PROTOCOL to appear as a named heading at first-class document level, heading-navigable without entering any enclosing section. C-46 applies the identical heading-sovereignty standard to the BC-COVERAGE-RECORD Schema: the schema appears as a named sub-heading under BC-STEP1-PROTOCOL, making the schema independently navigable from the document's heading hierarchy without reading BC-STEP1-PROTOCOL body prose. |
| C-47 | **PHASE-HANDOVER inventory governance collocation** | structure | C-44 requires a PHASE-HANDOVER inventory to be declared before Step 0, listing all expected transitions by identity. C-47 requires that inventory to be co-located within the Roles section rather than appearing as a standalone pre-Step-0 block, making governance scope and transition inventory available in a single navigable unit. A PHASE-HANDOVER inventory placed outside the Roles section fails. |
| C-48 | **MUST-clause ID addressability** | structure | C-45 requires each MUST-clause to declare Scope as a named structural field separate from Action, making each clause self-describing from field labels alone. C-48 requires each MUST-clause to carry a named MUST-ID (e.g., MUST-1, MUST-2) as a dedicated identifier field, enabling citation of a specific clause by identifier without reading any field content. A MUST-clause without a stable MUST-ID fails. |
| C-49 | **BC-COVERAGE-RECORD schema field-ID citation** | structure | C-46 requires the BC-COVERAGE-RECORD Schema to appear as a named sub-heading under BC-STEP1-PROTOCOL, making the schema independently navigable from the document's heading hierarchy. C-49 requires that schema to carry a named field-ID column (F-BCR-1 through F-BCR-N), and requires the BC-COVERAGE-RECORD output produced during [COVERAGE AUDIT] to prefix each field header with its schema ID. Without field-ID citation, C-46 makes the schema navigable as a sub-heading but does not link the schema declaration to its output: a reviewer confirming a BC-COVERAGE-RECORD entry must navigate to the schema sub-heading and identify the corresponding field by column-name matching. With field-ID citation, schema-to-output matching is address-based: the ID in the output header points directly to the ID in the schema table, and the reviewer can verify correspondence from cell content alone without locating the field by column name or reading surrounding prose in either section. The distinction is between passive addressability (the schema exists at a navigable address -- C-46) and active citation (the consuming output carries the schema address, making the link bidirectional and traversal-free -- C-49). Pass requires: (1) the BC-COVERAGE-RECORD Schema table includes a named field-ID column with stable identifiers (F-BCR-1..F-BCR-N), and (2) the BC-COVERAGE-RECORD output table headers prefix each field with its schema ID. A schema with field-IDs whose output headers omit the IDs fails; an output with prefixed IDs whose schema has no named field-ID column fails. Applicable only when C-46 is satisfied. |
| C-50 | **PHASE-HANDOVER registry transition-ID citation** | structure | C-47 requires the Phase Transition Registry to be co-located within the Roles section, making governance scope and transition inventory available in a single navigable unit. C-50 requires that registry to include a Transition-ID column assigning stable identifiers (e.g., T-00 through T-06) to each registry row, and requires each inline PHASE-HANDOVER-[N] table to include a `Registry-Ref` row citing the corresponding T-ID. Without transition-ID citation, C-47 makes the registry co-located with governance but does not link each inline PHASE-HANDOVER table to its registry entry by address: a reviewer confirming that an inline table satisfies its registry entry must locate the registry row by step-name matching, which requires reading both the table and the registry row and confirming the step name matches. With transition-ID citation, each inline table carries its registry address: "Registry-Ref: T-02" in an inline table unambiguously identifies registry row T-02, and the reviewer can confirm the match from cell content alone without scanning the registry for a matching step name. The distinction mirrors the schema field-ID pattern: C-47 co-locates governance and inventory in one navigable section (passive addressability); C-50 activates the inventory address in each consuming table (active citation). Pass requires: (1) the Phase Transition Registry includes a Transition-ID column with stable identifiers per row, and (2) each PHASE-HANDOVER-[N] table includes a `Registry-Ref` row citing its T-ID. A registry with T-IDs whose inline tables carry no Registry-Ref row fails; an inline table with a Registry-Ref row whose registry has no T-ID column fails. Applicable only when C-47 is satisfied. |
| C-51 | **MUST-clause belief-ref citation** | structure | C-48 requires each MUST-clause to carry a named MUST-ID, enabling citation by identifier without reading any field. C-51 requires the STILL-LIVE FILTER table to include a `Belief-Ref` column (fifth column or equivalent) where each MUST-clause row names the prior belief entry (PB-[NN]) that motivated the clause. Without belief-ref citation, C-48 makes each clause independently citable by MUST-ID but does not link the clause to the prior belief that grounds its enforcement logic: a reviewer asking "which belief does MUST-3 enforce?" must read the Scope and Action columns and infer the motivating belief from content. With belief-ref citation, the link is explicit and bidirectional from table cells alone: a reviewer reading MUST-3 can immediately identify the PB-[NN] entry being guarded; a reviewer reading PB-03 in the PBI can immediately identify the MUST-clause that enforces it -- without consulting surrounding prose, reading the full PBI, or parsing the Scope or Action columns. The belief-ref column makes the MUST-clause address active: the clause identifier (MUST-ID) is linked to the prior-belief identifier (PB-[NN]), creating a cross-table citation path within the artifact. This parallels the epistemic structure established by the chain audit (C-14/C-19): each correction entry cites its PBI entry via PBI-Ref; each MUST-clause now cites its motivating PBI entry via Belief-Ref. The distinction from C-48 is the same as the distinction between naming an address (addressability) and citing it in a consuming context (citation activation). Pass requires each MUST-clause row in the STILL-LIVE FILTER table to include a `Belief-Ref` column with a stable PB-[NN] identifier naming the prior belief that motivated the clause. A MUST-clause with a Belief-Ref field of "N/A" or no content fails; a Belief-Ref citing a PBI identifier that does not exist in the artifact's PBI section fails. Applicable only when C-48 is satisfied. |
| C-52 | **Citation-completeness manifest exhaustive** | structure | C-49 requires the BC-COVERAGE-RECORD output to prefix each field header with its schema field-ID, making individual citations address-linked to their schema declarations. C-52 requires the artifact to include a post-artifact CITATION-COMPLETENESS-MANIFEST table enumerating all citations produced across the full artifact -- TYPE-A (primary structured citations, e.g., CIT-01..04), TYPE-B (secondary/supporting citations, e.g., CIT-05..11), and TYPE-C (supplementary citations, CIT-12+) -- with a RESOLVED or DANGLING status per row, and a MANIFEST-COMPLETE token emitted only when every row shows RESOLVED. Without the manifest, C-49 makes individual citations address-linked but provides no accounting of the complete citation set: a reviewer cannot determine from output alone how many citations exist, which were produced, or which could not be resolved -- completeness requires document traversal. With the manifest, citation accounting is exhaustive and status-visible: a reviewer auditing citation-completeness reads the manifest table alone without traversing the full artifact. The manifest functions as the citation-layer analog of DISCARD-LOG-COMPLETE (C-36): DISCARD-LOG-COMPLETE accounts for all filter candidates by status; the CITATION-COMPLETENESS-MANIFEST accounts for all citations by status. MANIFEST-COMPLETE is emitted only when every row is RESOLVED -- its absence is a detectable signal that citation accounting is incomplete, applying the same success-visible / failure-visible contract as CHAIN-COMPLETE (C-19) and RULES-ANCHORED-COMPLETE (C-28). Pass requires: (1) a CITATION-COMPLETENESS-MANIFEST table enumerating all citations by type (TYPE-A, TYPE-B, TYPE-C) with RESOLVED/DANGLING status per row, and (2) a MANIFEST-COMPLETE token emitted only when all rows show RESOLVED. Applicable only when C-49 is satisfied. |
| C-53 | **Declaring-section consumer index populated** | structure | C-49, C-50, and C-51 activate the citation layer in the consuming direction: BC-COVERAGE-RECORD output headers carry their schema field-IDs (C-49), inline PHASE-HANDOVER tables carry their registry T-IDs (C-50), and MUST-clause rows carry their motivating PBI identifiers (C-51). Each activation is unidirectional -- a reviewer at a consuming location can look up the declaring entry by ID, but a reviewer at a declaring entry cannot identify its consuming contexts without document traversal. C-53 adds the inverse direction to all three declaring tables simultaneously: the BC-COVERAGE-RECORD Schema gains a Consumer-Ref column where each F-BCR row names its consuming output step (e.g., F-BCR-2 -> "[COVERAGE AUDIT] Step 1 output header"); the Phase Transition Registry gains a Consumer-Ref column where each T-ID row names its consuming PHASE-HANDOVER-[N] table (e.g., T-03 -> "PHASE-HANDOVER-3"); the STILL-LIVE FILTER gains a Consumer-Ref column where each MUST-ID row names its consuming enforcement token. With Consumer-Ref populated in all three declaring tables, navigability is bidirectional in all three citation axes from cell content alone: declaring -> consuming via Consumer-Ref; consuming -> declaring via the citation ID established by C-49/C-50/C-51. Without Consumer-Ref, each citation axis is traversable in one direction only; with Consumer-Ref, every citation axis is traversable in both directions without document scan in either direction. Pass requires Consumer-Ref columns populated in all three declaring tables (BC-COVERAGE-RECORD Schema, Phase Transition Registry, and STILL-LIVE FILTER), each row naming its consuming context by identity. A partial implementation that populates Consumer-Ref in only one or two of the three declaring tables fails -- all three axes must be bidirectionally indexed. Applicable only when C-49, C-50, and C-51 are all satisfied. |
| C-54 | **Citation chain PBI grounding verified** | structure | C-51 requires each MUST-clause row in the STILL-LIVE FILTER to include a Belief-Ref column naming the PBI entry (PB-[NN]) that motivated the clause, making the clause-to-belief link bidirectional from table cells. However, the PBI entry asserts an expectation without naming the signal artifact that confirmed or falsified it: C-51 links clause to belief, but does not link belief to evidence. C-54 extends the Belief-Ref cell from a PBI identifier alone to a full citation chain entry: `PB-[NN] (confirmed false by: [artifact name, namespace, date])`. The extension makes the chain terminal at evidence: a reviewer reading MUST-3's Belief-Ref cell walks to PB-03 (via PBI identifier) and then immediately locates the falsifying signal artifact (via the inline attribution) -- the complete clause-to-belief-to-evidence chain is traversable from the table cell alone, without consulting the PBI section body, the signal artifact list, or any surrounding prose. C-54 additionally requires the BC-COVERAGE-RECORD F-BCR-4 output (Coverage field) to name specific PBI entries with material attribution (e.g., "PB-01 through PB-04 (from [material name])") rather than a summary sentence, making coverage entries traceable to individual belief entries rather than to belief-sets. Together the two extensions make the full citation chain traversable from table cell content alone across a five-hop path: MUST-ID -> Scope/Action (same row) -> Belief-Ref PBI identifier -> PBI entry (by ID lookup) -> confirming artifact (via inline attribution in Belief-Ref cell). No prose consultation is required at any hop. Pass requires: (1) each STILL-LIVE FILTER Belief-Ref cell to carry the form `PB-[NN] (confirmed false by: [artifact name, namespace, date])` rather than `PB-[NN]` alone, and (2) the BC-COVERAGE-RECORD F-BCR-4 output to name specific PBI entries with material attribution rather than a summary sentence. A Belief-Ref cell that names the PBI entry but omits the confirming artifact fails; a Belief-Ref that names a confirming artifact not present in the signal set fails. Applicable only when C-51 is satisfied. |
| C-55 | **Token-format self-certifying** | structure | C-52 requires a MANIFEST-COMPLETE token, C-53 requires a CONSUMER-INDEX-COMPLETE token, and C-54 requires a CHAIN-GROUNDING-COMPLETE token -- but none of these criteria specify what the token must contain. A token that states "MANIFEST-COMPLETE" or "all citations resolved" confirms the closure condition by assertion; a reviewer verifying the assertion must consult the manifest table to confirm per-type counts, consult the declaring tables to confirm Consumer-Ref coverage, or consult the STILL-LIVE FILTER to confirm Belief-Ref extension. C-55 requires each gate token to carry inline specifics sufficient to verify its pass condition without consulting the underlying artifact sections: (1) MANIFEST-COMPLETE must include per-type ID ranges and counts for all three citation types in the form `CIT-[N1]..CIT-[N2] [K] TYPE-A RESOLVED, CIT-[M1]..CIT-[M2] [L] TYPE-B RESOLVED, CIT-[P1]..CIT-[P2] [Q] TYPE-C RESOLVED` -- a reviewer reading only the token can confirm citation count and type distribution without traversing the manifest table; (2) CONSUMER-INDEX-COMPLETE must name all three declaring tables by heading identity and confirm "POPULATED" per table individually (e.g., "BC-COVERAGE-RECORD Schema Consumer-Ref POPULATED, Phase Transition Registry Consumer-Ref POPULATED, STILL-LIVE FILTER Consumer-Ref POPULATED") -- a reviewer reading only the token can confirm all three axes without consulting the tables; (3) CHAIN-GROUNDING-COMPLETE must name both required conditions by label (e.g., "Belief-Ref cells EXTENDED (all MUST-[N] carry confirming artifact reference), F-BCR-4 SPECIFIC (names PB-[NN] through PB-[NN])") -- a reviewer reading only the token can confirm both conditions without consulting the STILL-LIVE FILTER or BC-COVERAGE-RECORD. The criterion is satisfied when a reviewer can verify all three tokens' pass conditions from token text alone, treating each token as a self-contained audit record. Pass requires all three gate tokens (MANIFEST-COMPLETE, CONSUMER-INDEX-COMPLETE, CHAIN-GROUNDING-COMPLETE) to carry inline specifics meeting the per-token format requirements above. A MANIFEST-COMPLETE token without per-type counts fails; a CONSUMER-INDEX-COMPLETE token naming "all three tables" generically without individual table headings fails; a CHAIN-GROUNDING-COMPLETE token without both condition labels fails. Applicable only when C-52, C-53, and C-54 are all satisfied. |
| C-56 | **Gate-independence enforced** | structure | C-53 (CONSUMER-INDEX-COMPLETE) and C-54 (CHAIN-GROUNDING-COMPLETE) are independent criteria that both close at Step 7. When two or more gate tokens are emitted at the same step boundary, a combined token -- e.g., "CONSUMER-INDEX-AND-CHAIN-GROUNDING-COMPLETE: [conditions]" -- merges two independent pass conditions into a single token. A combined token obscures individual satisfiability: a reviewer cannot determine from a combined token whether each condition was independently verified and independently satisfiable, or whether the conditions were verified jointly in a way that could mask partial satisfaction. Each gate criterion is falsifiable independently; combined tokens undermine that independence by making the token's pass/fail state depend on the conjunction. C-56 requires the artifact to explicitly prohibit combined tokens at co-located gate boundaries and to emit each gate token independently. The prohibition must appear as an explicit artifact statement (e.g., "combined tokens are non-conforming when multiple gates close at the same step; each gate emits a separate token with its own inline certification") -- the mere presence of two separate tokens does not satisfy this criterion, because absence of a prohibition leaves the door open for future variations to combine them. With gate independence enforced, each criterion's token is individually falsifiable: a variation that passes C-53 but not C-54 emits CONSUMER-INDEX-COMPLETE and omits CHAIN-GROUNDING-COMPLETE; the token set is unambiguous. Pass requires: (1) an explicit prohibition statement in the artifact naming combined tokens as non-conforming when multiple gates close at the same step, and (2) each gate token emitted independently with its own inline certification. A variation that emits a combined token fails; a variation that emits separate tokens without an explicit prohibition statement fails. Applicable only when C-53 and C-54 are both satisfied (the canonical Step-7 dual-gate case). |
| C-57 | **Manifest-row inline evidence** | structure | C-52 requires each row in the CITATION-COMPLETENESS-MANIFEST to carry RESOLVED or DANGLING status, providing citation accounting. C-54 extends Belief-Ref cells in the STILL-LIVE FILTER to carry the confirming artifact inline (`PB-[NN] (confirmed false by: [artifact name, namespace, date])`), making each Belief-Ref cell a terminal evidence reference. The combination reveals a residual gap at the manifest layer: TYPE-C manifest rows carry RESOLVED status but do not identify the confirming artifact inline. A reviewer auditing TYPE-C citation completeness reads the manifest row, sees RESOLVED, and must then consult the Belief-Ref column (to find the PBI entry) and the PBI section body (to find the inline attribution added by C-54) before reaching the falsifying artifact -- the manifest row is not a terminal evidence reference. C-57 extends each TYPE-C row in the CITATION-COMPLETENESS-MANIFEST to carry the confirming artifact inline in the status cell, using the same format established by C-54: `RESOLVED (confirmed false by: [artifact name, namespace, date])`. With this extension, the manifest row for a TYPE-C citation is itself a complete citation record: citation ID, type, status, and the falsifying artifact are all present in the row. No consultation of the Belief-Ref column, the PBI section, the STILL-LIVE FILTER, or the signal artifact list is required to verify the citation. The manifest becomes a terminal evidence layer for TYPE-C citations, applying the same traversal-free standard that C-54 applies to Belief-Ref cells. TYPE-A and TYPE-B rows are not governed by this criterion: their RESOLVED status reflects structural or procedural resolution, not belief falsification, and inline evidence attribution is not applicable to them. Pass requires each TYPE-C row in the CITATION-COMPLETENESS-MANIFEST to carry the form `RESOLVED (confirmed false by: [artifact name, namespace, date])` in the status cell rather than bare `RESOLVED`. A TYPE-C row with bare RESOLVED status fails; a TYPE-C row whose inline attribution names an artifact not present in the signal set fails. TYPE-A and TYPE-B rows are not evaluated. Applicable only when C-52 and C-54 are both satisfied. |
