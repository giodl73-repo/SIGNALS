The complete v18 rubric is written. Here's a summary of what changed:

---

**v18 additions** — two new aspirational criteria from R17 excellence signals, denominator /38 → /40:

**C-47 — Per-note failure-mode argument names specific criterion ID**
Extends C-45. The failure-mode clause in each Phase 8.5 in-gate note must name the criterion ID that would be unenforced if the enforcement path were absent — e.g., `"Absent path 1, C-43 is unenforced at Phase 8.5 entry"` rather than a prose gap description. V-01 of R17 demonstrates PASS C-45 / FAIL C-47 (failure-mode argument present, no criterion ID), establishing independence.

**C-48 — Pass/fail criterion-pair framing in STATUS QUO COMPETITOR foil items**
Extends C-46. Each foil item must name both the criteria the STATUS QUO PASSES and the criterion it FAILS in the same item — e.g., `"passes C-41 and C-42 while failing C-43"` rather than `"fails C-43"`. Converts the foil item from a single-axis failure claim to a bidirectional discrimination statement locating the STATUS QUO within the criterion lattice. V-02 of R17 demonstrates PASS C-46 / FAIL C-48 (failing criterion named, no passing criteria paired), establishing independence.

**Formula:** `(E/5 × 60) + (R/3 × 30) + (A/40 × 10)` — denominator /38 → /40 (C-09 through C-48)

The file is at `simulations/quest/golden/quest-rubric-variate-v18-R17-2026-03-16.md`.
