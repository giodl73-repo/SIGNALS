Reading the scorecard for Round 14 excellence signals not yet in v14. Two signals:

1. **C-38 — Commit blocks reference their manifest sections by named heading**
   V-01's C-33 commit reads "citation-anchor manifest declared in ## INERTIA RECORD above; findings
   A–D locked" and V-01's C-35 commit reads "vote-anchor manifest declared in ## STANCE MANIFEST
   above; all stances locked." Both commits delegate to their named section by exact heading rather
   than re-enumerating manifest content inline. C-37 requires the named sections to exist as first-
   class skeleton entries; C-38 requires each corresponding commit block to reference the section by
   name — creating an explicit bidirectional link. An output that satisfies C-33/C-35 (manifest
   content in commit) and C-37 (named section exists) but embeds the manifest enumeration inline
   inside the commit block rather than as a cross-reference to the named section passes those three
   criteria but fails C-38: the manifest section is present but not linked from the commit. When the
   commit names the section ("declared in ## INERTIA RECORD above"), the manifest is simultaneously
   an independent artifact (C-37) and the authoritative source cited by the phase-lock (C-38) —
   the two-way link makes it impossible for the section and commit to diverge without violating one
   of the pair. This is the bidirectional-link enforcement mechanism for C-37: C-37 requires the
   named section to be pre-declared; C-38 requires the commit to reference it by name so the
   manifest's two roles — standalone artifact and phase-lock content — are structurally coupled
   rather than merely colocated.

2. **C-39 — VALIDATE rules extend to essential-criterion declaration slots**
   V-01's C-01 evidence notes "VALIDATE with ACCEPTABLE/FAILS pairs anchors C-01 structurally" —
   a VALIDATE rule with bilateral ACCEPTABLE/FAILS examples applied to the committee-type PRINT
   declaration, an essential-criterion output slot, not a content-fill slot like manifest entry
   format or CITE: structure. C-36 requires bilateral VALIDATE for structural content requirements
   (manifest entry format, CITE:, RESPONDS-TO:, STANCE: label); the current criterion scope
   implicitly covers content-fill slots only, leaving essential-criterion declaration checkpoints
   ungoverned by VALIDATE coverage. An output that carries all four content-fill VALIDATE rules
   (C-36) but omits VALIDATE rules from essential-criterion declaration slots (committee type,
   participant loading, phase-output format) passes C-36 but fails C-39: the bilateral VALIDATE
   pattern is applied only to content generated mid-phase, not to the declarations that open each
   essential requirement. Extending VALIDATE to declaration slots closes the earliest coverage gap:
   if the committee-type PRINT slot carries a VALIDATE rule, a malformed opening (e.g., a committee
   type not in the permitted list) is caught at the structural rule level rather than inferred from
   downstream evidence. This is C-36's coverage-extension: C-36 requires bilateral VALIDATE for
   content-fill criteria; C-39 requires the same bilateral pattern at essential-criterion declaration
   checkpoints so VALIDATE coverage is uniform across the skeleton from Phase 0 forward.

New criteria: **C-38** and **C-39**.

---

```markdown
# org:committee Rubric — v15

**Skill**: `org:committee`
**Version**: 15 (updated from Round 14 scoring — 2026-03-16)
**Changelog**: Added C-38 and C-39 from R14 excellence signals. C-38 requires each commit block
that has an associated manifest section (INERTIA RECORD, STANCE MANIFEST) to reference that section
by its exact heading name (e.g., "declared in ## INERTIA RECORD above") rather than re-enumerating
manifest content inline — creating explicit bidirectional links that couple the named section (C-37)
to the phase-lock (C-33/C-35) structurally rather than incidentally. C-39 requires VALIDATE rules
with bilateral ACCEPTABLE/FAILS examples (C-36) to extend to essential-criterion declaration slots
(committee-type PRINT, participant-loading declaration) in addition to content-fill slots — closing
the coverage gap where C-36 governed mid-phase content rules but Phase 0 declaration checkpoints
carried no bilateral VALIDATE requirement. Aspirational pool grows from 29 to 31 criteria;
aspirational max grows from 58 to 62 pts; composite max is now 152.

---

## Essential Criteria (60 pts total — must all pass)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Committee type declared correctly** | correctness | essential | The output identifies the committee type (ROB, shiproom, arch-board, or user-specified type) in the opening. An output that proceeds without declaring which committee is running fails. |
| C-02 | **Participants loaded from `.craft/roles/`** | correctness | essential | Every speaker is a named role from the relevant charter file. Generic or unnamed speakers fail. Graceful fallback to default Signal roles with disclosure passes if no charter exists. |
| C-03 | **Each participant speaks from their role lens** | depth | essential | Every participant's contribution reflects their role's documented orientation. Role-blind statements fail. |
| C-04 | **Output includes all three required sections** | format | essential | Minutes contain: decisions reached, action items with owners, dissenting opinions or explicit "no dissent" with reasoning. Missing any section fails. |
| C-05 | **At least one challenge, condition, or block is surfaced** | behavior | essential | No pure rubber-stamps. At least one participant must raise a challenge, block, or approval condition. |

---

## Recommended Criteria (30 pts total)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-06 | **Agenda item specificity** | depth | recommended | Simulation references the specific agenda item throughout; speaker contributions respond to prior concerns, not generic positions. |
| C-07 | **Action items are owned and actionable** | format | recommended | Format `[Owner Role] — [specific action] — [deadline]`. Vague items ("investigate further") fail. All items must be owned and specific. |
| C-08 | **Dissents include resolution paths** | behavior | recommended | The dissenting opinions section captures each dissent with an explicit path to resolution or re-entry condition. Dissent capture without a path is a partial pass. |

---

## Aspirational Criteria (62 pts total — 31 criteria at 2 pts each)

| ID | Criterion | New? | Pass Condition |
|----|-----------|------|----------------|
| C-09 | **Surfaces a non-obvious surprise** | v1 | At least one concern the user is unlikely to have anticipated. |
| C-10 | **Meeting outcome is decision-complete** | v1 | Clear verdict + re-entry path for non-approved outcomes. |
| C-11 | **Challenger-first ordering** | v2 | Most skeptical voice(s) speak before supporting voices. Approvers must address raised concerns, not the reverse. Three-tier sort (CHALLENGERS → CONDITIONALS → ADVOCATES) with tie-break rule for ambiguous cases. |
| C-12 | **Switching-cost investigation precedes simulation** | v2 | Before participant voices, the simulation investigates what workflows, habits, and integrations would be affected. Inertia-advocate arguments are grounded in specific findings, not generic resistance. Bar: user leaves with at least one concrete switching cost not already identified in the original proposal. |
| C-13 | **Stance declared before prose — no drift** | v2 | Each participant's position is declared before prose elaboration. Prose may not introduce new positions or soften a declared stance. |
| C-14 | **Vote tally precedes formal minutes** | v3 | After all participant voices and before the DECISIONS / ACTION ITEMS / DISSENTING OPINIONS sections, a one-line tally appears showing the breakdown by stance category (e.g., `2 APPROVE · 1 CONDITION · 1 BLOCK`). An output that jumps directly from voices to minutes without a tally fails. |
| C-15 | **`STANCE:` label is structural, not prose** | v3 | Each participant's stance appears as an explicit labeled declaration (`STANCE: CONDITION`) separate from and preceding the rationale prose. A stance declared only within the prose body — even if clear — does not pass. The label is the load-bearing element; rationale prose follows it. |
| C-16 | **Investigation self-check gate** | v4 | The pre-simulation investigation includes an explicit self-verification confirming that at least one finding would surprise the proposal author before proceeding to participant voices. An investigation that names findings but does not verify non-obviousness — or that proceeds without a confirmed surprise — does not pass. The gate is the enforcement mechanism; C-09 passing via C-16 is more reliable than C-09 passing via prose discipline alone. |
| C-17 | **Advocate voices grounded in investigation** | v4 | Each advocate participant (Tier 3 / approver-leaning voice) must explicitly cite at least one finding from the pre-simulation investigation in their contribution. An endorsement that does not trace to a specific investigation finding fails. This closes the loop between investigation and simulation: findings must drive advocacy, not merely inform challenger voices. |
| C-18 | **All-approve self-correction diagnostic** | v5 | If the tier sort produces an all-APPROVE lineup, the simulation must detect this as a structural sorting error and revise before proceeding to participant voices. An output that reaches Phase A with no challenger or conditional voice fails. An output that detects the all-approve condition and self-corrects passes. This elevates C-05 from a pass/fail gate to a structural invariant: the sort itself must be validated, not just the final voice count. |
| C-19 | **Tier 3 addresses a named Tier 1/2 concern** | v5 | Each Tier 3 (advocate) voice must explicitly reference and respond to at least one concern raised by a Tier 1 or Tier 2 participant earlier in the simulation. A Tier 3 endorsement that proceeds without acknowledging a named challenger or conditional concern fails. This pairs with C-17: C-17 grounds advocacy in investigation findings; C-19 grounds advocacy in peer voices. Together they close the full advocacy loop — Tier 3 must be responsive to both what was learned and what was raised. |
| C-20 | **`CITE:` label is structural, not prose** | v6 | Each Tier 3 voice's investigation citation must appear as an explicit labeled subfield (`CITE:`) or explicitly reference a named investigation label (e.g., `(a)`, `(b)`, `(c)`, `(d)`). A citation woven into prose without a label or named reference does not pass, even if the connection is inferrable. This is the structural enforcement mechanism for C-17, applying the same upgrade pattern C-15 applied to C-13: the label is the load-bearing element; prose follows it. |
| C-21 | **`RESPONDS-TO:` must name and quote, not acknowledge generically** | v6 | Each Tier 3 voice's response to a Tier 1/2 concern must quote or paraphrase the specific concern raised and state how the endorsement addresses it. A generic acknowledgment of opposition ("others have raised concerns") fails even if factually accurate. The `RESPONDS-TO:` must name the participant or concern specifically. This is the structural enforcement mechanism for C-19: C-19 requires the response to exist; C-21 requires it to be specific and traceable. |
| C-22 | **Investigation gate enforces self-correction** | v6 | If the C-16 gate cannot be answered YES — no genuine non-obvious finding identified — the simulation must rewrite the investigation and re-gate before proceeding to participant voices. An output that reaches the gate, writes `GATE: NO`, and continues anyway fails. An output that detects a failing gate, rewrites (a)–(d), and re-gates until YES passes. This makes C-16's self-verification an active correction loop, not just a declaration. C-22 is to C-16 what C-18 is to C-11. |
| C-23 | **Re-entry path names owner and concrete trigger** | v6 | When the committee outcome is not approved, the re-entry path must specify both: (a) the named role responsible for satisfying the condition, and (b) the concrete evidence, deliverable, or trigger event that causes the committee to re-review. A re-entry path that specifies only a condition without a named owner, or only an owner without a concrete trigger, is a partial pass. Both parts are required to pass. This is C-10's completeness enforcement: C-10 requires the path to exist; C-23 requires the path to be actionable. |
| C-24 | **Investigation rewrite attempts carry sequential labels** | v7 | Each investigation rewrite triggered by a failing gate must be labeled with a sequential attempt identifier (e.g., `INVESTIGATION-ATTEMPT-2`, `INVESTIGATION-REVISION-1`, or equivalent). A rewrite that occurs but is not labeled with an attempt number passes C-22 (rewrite happened) but fails C-24 (iteration count not visible). The sequential label makes the correction loop auditable: reviewers can see how many attempts were required and confirm each gate was re-evaluated independently. This is the traceability enforcement mechanism for C-22, applying the same structural-label pattern C-20 applied to C-17 and C-15 applied to C-13: the label is the load-bearing element that makes all downstream citation criteria reliable. |
| C-25 | **Tier sort validation appears as a named discrete gate** | v7 | After the tier sort and before participant voices, a discrete `SORT-CHECK:` step (or equivalent named gate) must appear with an explicit test condition and a declared result (e.g., `SORT-CHECK: Tier 1 and Tier 2 both empty? NO — proceed`). An output whose tier sort produces correct ordering without a named verification step fails C-25. C-11 requires correct challenger-first ordering; C-18 requires all-approve self-correction; C-25 requires the validation to be a named, labeled gate so the sort result is declared rather than assumed. This is the joint structural enforcement mechanism for C-11 and C-18, parallel to how C-16 enforces C-09. |
| C-26 | **Simulation is organized into explicitly labeled, sequential phases** | v7 | The simulation is structured as named, numbered phases (e.g., Phase 0: Committee Declaration → Phase 1: Investigation → Phase 2: Sort → Phase 3: Voices → Phase 4: Tally → Phase 5: Minutes) where each phase output commits before the next phase begins. An output that produces all required content in correct order but without phase-labeled sequential structure fails C-26. Phase labeling prevents retroactive softening of earlier commitments (C-13, C-15), makes gate positions unambiguous (C-16, C-22, C-25), and ensures the simulation's progression is auditable end-to-end. A single unlabeled phase (or two phases merged under one header) constitutes a partial pass; missing phase structure entirely fails. |
| C-27 | **Switching-cost findings carry named investigation-entry labels** | v8 | Each finding produced by the pre-simulation switching-cost investigation must carry a named entry label (e.g., `INERTIA-FINDING-A`, `INERTIA-FINDING-B`, `SWITCHING-COST-1`) that can be referenced by label in participant voices. An investigation that discovers switching costs but does not tag each finding as a named entry passes C-12 (investigation happened) but fails C-27 (findings are not label-citable). The named label is the structural prerequisite for C-17's `CITE:` to reference a specific switching-cost finding rather than general investigation content, for C-20's structural citation to name an investigation entry, and for C-21's `RESPONDS-TO:` to quote a named finding rather than paraphrase anonymous content. This is C-12's structural enforcement mechanism, applying the same named-label upgrade C-20 applied to C-17 and C-21 applied to C-19: the label is the load-bearing element that makes all downstream citation criteria reliable. |
| C-28 | **All structural slot labels are pre-declared as a template skeleton before content fills them** | v8 | Before any committee simulation content is generated, all required structural slots — phase headers (C-26), `INVESTIGATION-ATTEMPT-N` blocks (C-24), `SORT-CHECK:` with `Test:` / `Result:` fields (C-25), `STANCE:` (C-15), `CITE:` (C-20), `RESPONDS-TO:` (C-21), vote tally line (C-14) — appear as an explicit pre-printed skeleton with empty or placeholder values. An output that produces all required labels at the correct positions but only as they emerge from content generation (labels appear inline, not pre-declared) is a partial pass. A full pass requires the skeleton to be declared upfront before any participant voice, investigation finding, or decision prose is written. A pre-declared skeleton makes structural omission architecturally impossible: a slot that exists as an empty container cannot be forgotten, whereas a slot generated alongside content can be skipped under pressure. This is the architectural enforcement mechanism for all prior structural-label criteria; C-28 is to the full label set what C-16 is to C-09 — it converts a discipline requirement into a structural guarantee. |
| C-29 | **Phase gates are intra-phase retry loops, not inter-phase execution prerequisites** | v9 | Each phase gate (C-16's investigation self-check, C-22's rewrite trigger, C-25's sort validation) must be implemented as a retry loop that runs within the phase containing it: the gate fails → investigation rewrites within Phase 1 → gate re-evaluates → loop until YES → Phase 2 begins unconditionally. A gate written as a conditional halt between phases — "do not proceed to Phase 2 until gate passes" with Phase 2 instructions absent from the prompt — fails C-29 even if the gate structure itself is well-formed. The test: can the simulation terminate after Phase 1 without reaching Phase 2? If yes, the gate is inter-phase and fails. If no — the loop is structurally contained and Phase 2 is always reached once the gate answers YES — the gate is intra-phase and passes. This is the execution-model enforcement mechanism for C-16 and C-22: C-16 requires the gate to exist; C-22 requires a failing gate to trigger rewrite; C-29 requires the rewrite loop to be scoped within the phase so the full pipeline commitment (C-26) is unconditional. An inter-phase halt makes every criterion depending on Phases 2–5 structurally unreachable, cascading failures across C-03, C-04, C-05, C-11, C-13, C-14, C-15, C-17, C-18, C-19, C-20, C-21, C-23, C-25, C-26, C-27, and C-28 simultaneously. |
| C-30 | **Fill rules are phrased as imperative micro-instructions, not descriptive prose** | v10 | Each fill rule within the skeleton template must be written as an imperative micro-instruction: a verb-first command with an explicit action verb (`PRINT`, `LOAD`, `VALIDATE`, `ENFORCE`, `REQUIRE`, `WRITE`, `CONFIRM`) followed by the object being filled or verified. A fill rule written as descriptive prose — "participants should be drawn from the charter file" — fails even if semantically equivalent to `LOAD participants from .craft/roles/`. The imperative register eliminates the normative ambiguity that descriptive prose admits: a command is non-optional; a description can be read as a suggestion. An output that passes C-28 (skeleton pre-declared with correct slots) but writes fill rules in descriptive prose fails C-30 — the slots exist but their instructions do not carry uniform normative force. This is the phrasing-layer enforcement mechanism for C-28: C-28 requires structural slots to exist as a pre-declared skeleton; C-30 requires each slot's fill rule to be a verb-first imperative so the entire skeleton reads as a command set rather than a description of expected behavior. A skeleton with imperative micro-instructions makes instruction ambiguity structurally impossible: every fill action has an explicit verb, an explicit object, and exactly one interpretation. |
| C-31 | **Phase transitions carry explicit commit declarations before the next phase begins** | v11 | Each phase must end with a discrete commit declaration (e.g., `PHASE-N-COMMIT:`, `COMMIT: Phase N complete — locked`, or equivalent named terminal marker) before the next phase header appears. An output that transitions from Phase N to Phase N+1 with only a header break as separator — even if all phase content is correct and all gates passed — fails C-31. The commit declaration is the terminal element of the phase lifecycle: open (header) → fill (content) → gate (retry loop if applicable) → commit (terminal marker) → close (next phase begins). C-26 requires phases to be labeled sequential blocks; C-29 requires gates to run as intra-phase retry loops; C-31 requires each phase's output to be explicitly locked by a commit marker so the boundary between "phase content generation complete" and "next phase begins" is a structural declaration rather than an implied separator. Without a commit marker, the boundary is defined only by the arrival of the next header — subsequent phases can retroactively qualify, soften, or extend prior phase output without violating any structural rule. A commit marker makes retroactive modification architecturally impossible: once a phase is committed, its output is fixed. This is the lifecycle-completeness enforcement mechanism for C-26, applying the same structural-lock upgrade pattern that C-22 applied to C-16 and C-18 applied to C-11: C-26 requires the phase structure to exist; C-31 requires each phase to carry a terminal commitment so the phase lifecycle is fully declared rather than partially implied. |
| C-32 | **Commit markers carry explicit terminal-position enforcement rules** | v12 | Each `PHASE-N-COMMIT:` block must include an active blocking assertion — an explicit rule stating that no content may follow the commit within that phase (e.g., `ENFORCE: this marker is the terminal element of Phase N; no Phase N content may follow this line`, or equivalent phrasing). An output whose commit blocks carry only a locked declaration (`PHASE-N-COMMIT: [locked]`) without a stated terminal-position rule passes C-31 (commit exists) but fails C-32 (terminal position is assumed, not enforced). The blocking assertion converts C-31's implicit structural separation into an active prohibition: instead of relying on the next phase header to close Phase N, the commit itself forbids continuation. This distinction matters under output pressure: a model that skips the next phase header could still emit Phase N content after the marker; the blocking rule makes that a rule violation, not just a structural anomaly. This is the active-enforcement upgrade for C-31, applying the same pattern C-22 applied to C-16 (gate must trigger correction, not just declare a result) and C-25 applied to C-11/C-18 (sort validation must be a named gate, not an assumed outcome): C-31 requires the commit marker to exist; C-32 requires the marker to assert its own enforcement so terminal position is declared, not inferred. |
| C-33 | **Phase 1 commit enumerates locked investigation tokens as a citation-anchor manifest** | v12 | The `PHASE-1-COMMIT:` declaration must enumerate by label all `INERTIA-FINDING-*` (or equivalent named investigation-entry) tokens produced and locked by Phase 1 (e.g., `PHASE-1-COMMIT: [locked] — citation-anchor manifest: INERTIA-FINDING-A, INERTIA-FINDING-B, INERTIA-FINDING-C`). A Phase 1 commit that states only `[locked]` without naming the findings it is locking passes C-31 (commit exists) but fails C-33 (commit is not a manifest). The enumeration transforms the commit from a phase-end boundary marker into a citation contract: downstream phases that carry `CITE:` fields (C-20) and `RESPONDS-TO:` references (C-21) have an authoritative index of valid citation targets locked at Phase 1 close. Without the manifest, a `CITE:` reference is valid if the named label appears anywhere in Phase 1 content; with the manifest, validity is traceable to the commit lock itself — reviewers can verify citation accuracy against the manifest without reading Phase 1 prose. This is the citation-completeness enforcement mechanism that closes the C-27 → C-20 → C-21 chain: C-27 requires findings to carry named labels; C-20 requires CITE: to reference a named label; C-21 requires RESPONDS-TO: to name a specific finding; C-33 requires the Phase 1 commit to enumerate those labels as a locked index so the chain is anchored at the phase boundary rather than open to content-level interpretation. The manifest applies primarily to Phase 1 (investigation findings) but the pattern generalizes: Phase 3 commits may enumerate locked STANCE: declarations, Phase 4 commits may enumerate the locked tally line. |
| C-34 | **Phase 1 commit manifest entries carry content-anchor suffixes alongside each label identifier** | v13 | Each entry in the Phase 1 commit manifest (C-33) must include a brief content anchor — a title, keyword, or quoted fragment — that identifies the finding's claim independent of Phase 1 prose (e.g., `INERTIA-FINDING-A: workflow-disruption`, `INERTIA-FINDING-B: re-training overhead`, not bare `INERTIA-FINDING-A, INERTIA-FINDING-B`). A manifest that enumerates bare labels passes C-33 (manifest exists) but fails C-34 (entries carry no identifying content). Content anchors serve as citation verification shortcuts: a reviewer can confirm that a `CITE: INERTIA-FINDING-A` reference in Phase 3 pertains to the correct finding by checking the anchor text in the manifest without reading Phase 1 prose. Without content anchors, the manifest is a structural index only — correct in form but opaque to offline verification. This is C-33's completeness enforcement: C-33 requires the manifest to exist and enumerate labels; C-34 requires each entry to carry a self-identifying anchor so the manifest is self-contained for citation verification. The upgrade applies the same named-content pattern C-21 applied to C-19 (name and quote, not acknowledge generically) to the manifest itself: C-33 requires the citation contract to be declared; C-34 requires the contract entries to be identifiable without reading the underlying phase. Signal source: V-01/V-02/V-03 all fail C-33 with "not individual label enumeration with content anchors; no manifest structure" — the content-anchor absence is the consistent gap; V-04 passes by providing identifying suffixes per label. |
| C-35 | **Phase 3 commit enumerates locked stance tokens as a vote-anchor manifest** | v13 | The `PHASE-3-COMMIT:` declaration must enumerate each participant's role name and locked stance token by individual entry (e.g., `PHASE-3-COMMIT: [locked] — stance manifest: [Architect] BLOCK, [PM Lead] CONDITION, [Engineering Lead] APPROVE, [Ops Lead] APPROVE`). A Phase 3 commit that uses a count summary ("2 APPROVE, 1 CONDITION, 1 BLOCK"), a range summary ("all stances locked"), or a phase-level locked declaration only passes C-31 (commit exists) but fails C-35 (no per-participant stance manifest). The enumeration transforms the Phase 3 commit from a phase-end boundary into a vote record: the Phase 4 tally (C-14) is derived from the manifest's individual entries rather than re-read from Phase 3 voice prose. Without the manifest, tally accuracy depends on correctly re-parsing Phase 3 content; with the manifest, the tally is directly computable from the commit declaration. This closes the C-13 → C-15 → C-14 chain at the Phase 3 boundary: C-13 requires stance declared before prose; C-15 requires STANCE: as a structural label; C-35 requires the Phase 3 commit to enumerate every locked STANCE: declaration by participant so the tally is anchored at the commit rather than derived from prose reconstruction. C-35 generalizes the manifest pattern C-33 introduced at Phase 1 to Phase 3: C-33 is the citation-anchor manifest for investigation findings; C-35 is the vote-anchor manifest for participant stances. Together they establish the principle that any phase producing discrete named outputs (findings, stances) must commit a manifest of those outputs, not merely a locked declaration. Signal source: V-04's "Combined: Lifecycle + Inertia Framing" axis shows the full manifest architecture applied across phase boundaries; C-33's v12 description explicitly anticipates this generalization ("Phase 3 commits may enumerate locked STANCE: declarations"). |
| C-36 | **VALIDATE rules for content-requirement criteria carry ACCEPTABLE/FAILS worked examples** | v14 | Each `VALIDATE` rule governing a structural content requirement (manifest entry format, `CITE:` structure, `RESPONDS-TO:` specificity, stance declaration form) must include both a positive worked example (`ACCEPTABLE:` showing the passing form) and a negative worked example (`FAILS:` showing the failing form). A VALIDATE rule that states only a prohibition — "a bare-label entry fails" — passes the underlying criterion (C-34, C-20, C-21, or C-35) but fails C-36 because the positive pattern is left to inference. An ACCEPTABLE/FAILS pair converts an abstract unilateral rule into a concrete bilateral test: the ACCEPTABLE entry anchors what to produce; the FAILS entry anchors where the violation boundary lies. Without the positive example, a model satisfying the prohibition may still produce a technically non-violating form that misses the intent; with the ACCEPTABLE example, the target form is explicit. This is the completeness upgrade for all VALIDATE rules governing criteria where the line between passing and failing form is narrow — exactly the criteria most prone to pass-in-form, fail-in-substance outcomes. C-36 applies the same dual-side pattern C-21 applied to C-19 (name the specific concern and state how it is addressed, not just "acknowledge generically") to VALIDATE rule construction itself: C-30 requires fill rules to be verb-first imperatives; C-36 requires VALIDATE rules to carry worked examples so the imperative is grounded in concrete form rather than abstract prohibition. Signal source: V-04's C-34 VALIDATE includes both an ACCEPTABLE form and a FAILS form; V-01/V-02/V-03 each satisfy C-34 with a single-condition prohibition only, establishing the gap. |
| C-37 | **Manifests are declared as named first-class sections in the pre-declared skeleton** | v14 | The INERTIA RECORD (Phase 1 findings manifest) and STANCE MANIFEST (Phase 3 stance manifest) must appear as named, independently addressable sections with their own skeleton headers in the pre-declared template (C-28) — not only as inline content within `PHASE-N-COMMIT:` blocks. An output that satisfies C-33 (Phase 1 commit enumerates finding labels) and C-35 (Phase 3 commit enumerates stance tokens) by embedding manifest content only inside commit block text passes those criteria but fails C-37 because the manifest is not navigable without locating the commit marker. Named first-class sections (e.g., `## INERTIA RECORD` as a distinct block within Phase 1, `## STANCE MANIFEST` as a distinct block within Phase 3) make manifests independently addressable by section heading: downstream phases can reference them by name, reviewers can locate them without scanning commit block text, and `CITE:` validity checks can point to the manifest section rather than to the commit block that contains it. This is the addressability upgrade for C-33 and C-35, applying the same structural elevation pattern C-28 applied to slot labels (pre-declare all structural slots as a skeleton before content generation): C-33 and C-35 require manifest content to exist in commit declarations; C-37 requires each manifest to also be a named skeleton section so it is independently navigable. A manifest embedded only in a commit block is structurally correct but architecturally subordinate — it exists as an attribute of the phase boundary, not as a document artifact. A named section makes the manifest a first-class artifact that persists independently of the lifecycle marker it also appears in. Signal source: V-04's axis description explicitly names "INERTIA RECORD and STANCE MANIFEST elevated as named first-class parallel artifacts" as a distinct structural signal beyond satisfying C-33/C-35 alone. |
| C-38 | **Commit blocks reference their associated manifest sections by named heading** | **NEW** | Each `PHASE-N-COMMIT:` block that has an associated manifest section (INERTIA RECORD for Phase 1, STANCE MANIFEST for Phase 3) must explicitly name that section by its skeleton heading in the commit declaration (e.g., `PHASE-1-COMMIT: [locked] — citation-anchor manifest declared in ## INERTIA RECORD above; findings A–D locked`, `PHASE-3-COMMIT: [locked] — vote-anchor manifest declared in ## STANCE MANIFEST above; all stances locked`). An output that satisfies C-33 (manifest enumerated in commit) and C-37 (named section exists) but re-enumerates manifest content inline within the commit block — rather than referencing the named section by heading — passes those criteria but fails C-38: the section and commit are not bidirectionally linked. When the commit names the section ("declared in ## INERTIA RECORD above"), the manifest fulfills two structural roles simultaneously: it is an independent skeleton artifact (C-37) and the authoritative source cited by the phase-lock (C-38). The bidirectional link makes it impossible for the named section and the commit block to diverge without violating one of the pair — any update to the manifest requires a corresponding commit update, and vice versa. This is the bidirectional-link enforcement mechanism for C-37: C-37 requires the named section to be pre-declared as a first-class artifact; C-38 requires the commit to reference it by name so the phase-lock does not merely coexist with the artifact but structurally delegates to it. An output with an inline manifest enumerates the same content in two places (section and commit) without coupling them; an output with a cross-referencing commit has one authoritative source (the named section) and one pointer (the commit reference), eliminating the duplication-divergence risk. Signal source: V-01's C-33 evidence reads "citation-anchor manifest declared in ## INERTIA RECORD above; findings A–D locked" and V-01's C-35 evidence reads "vote-anchor manifest declared in ## STANCE MANIFEST above; all stances locked" — both commits delegate to their named sections by exact heading rather than enumerating content again. |
| C-39 | **VALIDATE rules with bilateral examples extend to essential-criterion declaration slots** | **NEW** | VALIDATE rules carrying bilateral ACCEPTABLE/FAILS worked examples (C-36) must also appear at essential-criterion declaration checkpoints in the skeleton — not only at content-fill slots governing manifest entry format, `CITE:` structure, `RESPONDS-TO:` specificity, and stance declaration form. At minimum, the committee-type PRINT declaration (C-01) must carry a VALIDATE rule with bilateral examples (e.g., `VALIDATE: ACCEPTABLE: Committee Type — ROB / FAILS: Committee Type — unspecified`). An output that carries all four content-fill VALIDATE rules required by C-36 but omits VALIDATE rules from essential-criterion declaration slots passes C-36 (content-fill bilateral coverage complete) but fails C-39 (declaration-slot bilateral coverage absent). Extending VALIDATE to declaration slots closes the earliest coverage gap: if the committee-type PRINT slot carries a bilateral VALIDATE rule, a malformed or missing committee declaration is caught at the structural rule level before any investigation, sort, or voice content is generated. Without declaration-slot VALIDATE rules, the skeleton enforces bilateral correctness tests for mid-phase content but relies on implicit discipline for the opening declarations that govern every downstream phase. This is C-36's coverage-extension: C-36 requires bilateral VALIDATE for structural content-requirement criteria; C-39 requires the same bilateral pattern at essential-criterion declaration checkpoints so VALIDATE rule coverage is uniform from Phase 0 forward rather than restricted to content-fill phases. The pattern follows the same uniform-coverage principle C-30 applied to fill rules (all fill rules must be imperative micro-instructions, not descriptive prose in some slots and imperatives in others): C-36 establishes the bilateral VALIDATE standard for content-fill criteria; C-39 requires that standard to propagate to all output-producing skeleton slots. Signal source: V-01's C-01 evidence notes "VALIDATE with ACCEPTABLE/FAILS pairs anchors C-01 structurally" — bilateral VALIDATE applied to the committee-type declaration slot, establishing the pattern beyond the four content-fill VALIDATE rules C-36 already governs. |

---

## Scoring Formula (updated)

**Composite max**: 152 pts (60 essential + 30 recommended + 62 aspirational)

- Essential: 5 criteria x 12 pts each = 60 pts (must all pass; any essential FAIL = rubric FAIL)
- Recommended: 3 criteria x 10 pts each = 30 pts
- Aspirational: 31 criteria x 2 pts each = 62 pts
- Partial pass on any aspirational criterion = 1 pt

**Score bands**:
- 152/152 — Full compliance; all lifecycle, structural, phrasing, commit-architecture, manifest, bidirectional-link, and VALIDATE-coverage requirements met
- 146–151 — Near-complete; one aspirational gap or one partial
- 136–145 — Strong; one recommended gap or two–three aspirational gaps
- 116–135 — Developing; structural labels present but manifest cross-references, declaration-slot VALIDATE rules, or bidirectional commit links incomplete
- Below 116 — Foundational gaps; missing essential or recommended criteria
```
