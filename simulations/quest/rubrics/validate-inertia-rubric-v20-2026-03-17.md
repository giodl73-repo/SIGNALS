Here is v20:

---

**New criterion — C-50: Both C-45 FAIL shapes enumerated as distinct disqualifiers in block instruction body**

**Source:** R19 Pattern 1 (V-95 shape)

**What R19 revealed:** C-45 FAIL has two structurally distinct shapes:
- **F1** (the C-47 shape): neither closure function named anywhere in the block body
- **F2** (the V-95 shape): both functions named *only* in disqualifier-context sentences — C-47's "a block naming neither function fails" and C-48's "a block naming one without the other earns PARTIAL" — with no positive duality declaration present

V-95 earned C-45 FAIL despite having both function names in the block body. C-47's disqualifier ("a block body naming neither function") does not fire against F2 because F2 blocks *do* contain both names. F2 is invisible at C-47 read time.

**Pass condition:** Block body names both F1 (neither function named) and F2 (both functions named only in disqualifier-context sentences, no affirmative duality declaration) as distinct C-45 FAIL disqualifiers. One shape without the other earns PARTIAL. A merged description earns PARTIAL.

**Scoring:** 490 → 500 pts max | 41 → 42 aspirational | aspirational subtotal 420 pts.

**V-92/V-93/V-94 — no new criteria:** All three confirmed existing C-49 coverage (S1-only, S2-only, and merged-compound respectively). C-49's "merged description of both earns PARTIAL" already handled V-94.

**R20 gap:** The true zero-function-named variation (block body contains no function names anywhere, including not in disqualifier sentences — failing C-47 while passing C-46) remains untested.
ifier-context sentences, no positive duality declaration) as distinct
  C-45 FAIL disqualifiers. Naming one FAIL shape without the other earns PARTIAL. A merged description
  of both shapes earns PARTIAL.

**Note on V-92/V-93/V-94:** All three confirmed existing C-49 behavior -- V-92 (S1-only named) and
V-93 (S2-only named) match C-49's "describes only one of the two shapes earns PARTIAL" condition;
V-94 (both described in a merged compound sentence without S1/S2 labels) matches C-49's "names both
shapes via a single merged description earns PARTIAL" condition. No new criterion required.

**Note on R19 gaps:** The zero-function-named / C-47-FAIL / C-48-FAIL combination (block body declares
neither function AND contains no FAIL or PARTIAL disqualifiers) remains untested. V-95 is structurally
distinct from this: V-95's block body contains both function names in C-47/C-48 disqualifier sentences,
making C-47 and C-48 PASS; the failure is exclusively at the positive-declaration level (C-45). A
C-50 PASS ceiling variation would name both F1 and F2 as separate explicitly labeled disqualifiers,
distinguishing F2 (functions-named-disqualifier-context-only) from F1 (neither function named) at
the block-body read point.

**Scoring:** 490 -> 500 pts max | 41 -> 42 aspirational | aspirational subtotal 420 pts.

- **5 essential (C-01--C-05):** per-persona inertia mapping, quantified switching cost, per-persona
  inertia score, kill-barrier identification, overall adoption inertia risk
- **3 recommended (C-06--C-08):** current workaround satisfaction, habit lock-in + social proof
  coverage, mitigation path for the kill barrier
- **42 aspirational (C-09--C-50):** scoring methodology transparency, time-dependent inertia
  trajectory, status-quo competitor framing, named social proof threshold, mitigation tied to
  structural root cause, kill-barrier temporal persistence test, causal chain with labeled
  structural sub-parts, mitigation confirmation signal, per-score methodology trace,
  kill-barrier trajectory verdict, related-output structural segregation with disqualifier,
  kill-barrier reselection gate, phase-zero methodology gate, competitive strength inventory
  in persona phase, named intervention target as falsifiable anchor, named boundary artifact
  at phase transition, structural prohibition gate vs. format instruction, cross-phase citation
  chain with named anchors, self-rejection gate for mechanism specificity, named artifact at
  every downstream-source boundary, exact-copy citation instruction in downstream anchoring
  sections, Citation Architecture preamble as single-read authority, citation chain completeness
  self-audit, evidence column converts citation audit from assertion to demonstration,
  architecture-audit duality framing, preamble-declared audit format, atomic audit format
  declaration, audit-first derivation, co-resident named block for audit architecture criteria,
  derivation direction co-resident in named architecture block, derivation prohibition declared
  at architecture-read boundary, per-axis explicit gate for three-axis architecture block,
  per-axis gate declared in block body not in produced label, gate components not split between
  block body and produced label, produced architecture label cites block body as source of both
  gate components, split-gate failure modes named as explicit disqualifiers in block body,
  gate closure named as duality of self-documenting label function and self-enforcing block body
  function, non-propagation between closure criteria declared in block instruction body,
  C-45 FAIL condition declared as explicit disqualifier in block instruction body,
  both C-45 PARTIAL paths enumerated as distinct disqualifiers in block instruction body,
  both C-48 PARTIAL shapes enumerated as distinct cases in block instruction body,
  both C-45 FAIL shapes enumerated as distinct disqualifiers in block instruction body

Scoring: Essential = 10 pts each (50 total), Recommended = 10 pts each (30 total), Aspirational =
10 pts each (420 total) = **500 pts max**.

---

## Essential Criteria (all must pass)

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-01 | **Per-persona inertia mapping** | correctness | Output identifies two or more named user personas and maps each to one or more inertia factors. Generic "users" without persona differentiation fails. |
| C-02 | **Quantified switching cost** | correctness | At least one persona has a switching cost expressed as a measurable value -- time, money, effort rating (1-10), or steps. Qualitative-only ("it's hard") fails. |
| C-03 | **Per-persona inertia score** | correctness | Each mapped persona has an explicit inertia score (numeric or Low/Medium/High/Critical). A single blanket score for all personas fails. |
| C-04 | **Kill-barrier identification** | depth | Output identifies exactly one adoption killer -- the single factor most likely to block adoption even if all other inertia is resolved. Must be labeled distinctly. |
| C-05 | **Overall adoption inertia risk** | correctness | Output produces an aggregate risk verdict with a sentence of rationale tying it back to the per-persona scores. |

---

## Recommended Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-06 | **Current workaround satisfaction assessed** | coverage | For at least one persona, output describes how satisfied they are with their current workaround and why that satisfaction creates inertia. |
| C-07 | **Habit lock-in and social proof addressed** | coverage | Output addresses habit lock-in AND social proof for at least one persona each. Missing both fails; covering one partially passes. |
| C-08 | **Mitigation path per critical barrier** | depth | For the kill barrier in C-04, output proposes at least one concrete mitigation that could reduce or eliminate that barrier. |

---

## Aspirational Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-09 | **Inertia score methodology explained** | behavior | Output briefly explains how inertia scores were derived -- what factors were weighted and why. A single sentence per dimension suffices. |
| C-10 | **Adoption timeline sensitivity** | depth | Output includes at least one time-dependent statement on how inertia evolves (e.g., "after 6 months of workaround use, lock-in doubles"). |
| C-11 | **Status-quo framed as named competitor** | depth | For the kill barrier in C-04, output names the dimension on which the current solution *wins* and explains why that competitive advantage is durable -- not just "people are used to it" but what property of the current solution makes it structurally hard to displace. |
| C-12 | **Named social proof threshold** | coverage | For at least one persona where social proof is a factor, output specifies the adoption threshold as a concrete condition or count (e.g., "needs 2+ teammates before committing," "will adopt solo if manager mandates it"). Binary Y/N without a named threshold fails. |
| C-13 | **Mitigation tied to structural root cause** | depth | The mitigation from C-08 explicitly explains *why* the intervention neutralizes the structural reason the barrier exists -- not just what to do, but why that lever addresses the specific root cause named in C-04. Addressing a symptom rather than the stated structural cause fails. |
| C-14 | **Kill barrier tested against temporal persistence** | depth | The kill barrier identified in C-04 is explicitly qualified against a temporal horizon -- output states that the barrier persists at T=0 AND remains unresolved at the longest time horizon considered (e.g., T=18mo). A kill barrier identified only as a current-state snapshot without temporal qualification fails. |
| C-15 | **Causal chain with labeled structural sub-parts** | depth | For the kill barrier, output provides a causal chain with explicitly labeled, distinct sub-parts covering: (1) what the barrier is, (2) why it structurally persists, (3) what the intervention targets, and (4) why that lever resolves the structural root cause. An analysis that merges any two sub-parts into a single statement, or that provides fewer than four labeled parts, fails. |
| C-16 | **Mitigation confirmation signal specified** | depth | The kill barrier mitigation from C-08 includes a named leading indicator or observable condition at a future time point (e.g., "at T=6mo, [X behavior] would confirm the barrier is reducing") that would confirm the intervention is working. A mitigation without a forward-looking confirmation condition fails. |
| C-17 | **Per-score methodology trace** | behavior | For each persona scored in C-03, output includes a derivation sentence that explicitly connects that persona's dimension values to the declared scoring threshold from C-09, showing how the score was produced. The strongest enforcement mechanism is a required column in the score table -- a column cannot be omitted without omitting the table, making non-compliance structurally visible. A global methodology declaration (C-09) without per-persona derivation fails. A trace that cites dimension values but not the threshold, or the threshold but not the dimension values, earns PARTIAL. |
| C-18 | **Kill-barrier trajectory verdict** | depth | Beyond any per-persona timeline grid satisfying C-10, output provides a separate, explicitly labeled verdict for the kill barrier's own time-sensitivity -- a trajectory direction (Resolving / Stable / Worsening) with structural reasoning tied to the persistence property identified in C-15(2). A verdict present in the grid only, without a dedicated kill-barrier trajectory statement, fails. |
| C-19 | **Related-output structural segregation with disqualifier** | behavior | When output produces two related but structurally distinct required outputs (e.g., a per-persona timeline grid satisfying C-10 and a kill-barrier trajectory verdict satisfying C-18), each is housed as an explicitly labeled sub-section AND the first sub-section includes a literal statement that it does not satisfy the second criterion. Absence of the explicit disqualifier statement fails; implicit separation without the disqualifier earns PARTIAL. |
| C-20 | **Kill-barrier reselection gate** | depth | The temporal persistence test for the kill barrier includes an explicit binary YES/NO confirmation that the barrier exists at T=0 AND persists at the longest time horizon considered, followed by a literal reselection instruction triggered if either answer is NO. Implicit persistence conveyed through a grid row or trajectory statement without a binary confirmation gate fails; a gate present at one horizon only earns PARTIAL. |
| C-21 | **Phase-zero methodology gate** | behavior | The methodology declared in C-09 is positioned as Phase 1 output -- before any persona is identified or scored -- and explicitly framed as the scoring infrastructure from which per-score traces (C-17) will draw. The phase must declare dimension weights AND tier thresholds AND identify itself as the source C-17 traces will cite. A methodology section placed after persona analysis fails. A declaration that omits the explicit framing as C-17 infrastructure earns PARTIAL. C-09 can pass without C-21; C-21 requires C-09 to be structured as a prerequisite gate. |
| C-22 | **Competitive strength inventory in persona phase** | depth | For each persona, output includes a per-persona competitive strength inventory structured as three named fields: (a) the Dimension on which the current solution wins, (b) the structural Advantage that win represents, and (c) the Durability basis explaining why that advantage is structurally persistent rather than merely habitual. This inventory is completed in the persona identification phase, before kill-barrier analysis begins. A competitive framing constructed on demand at kill-barrier analysis time, without an upstream per-persona inventory, fails. An inventory present but missing any of the three fields earns PARTIAL. |
| C-23 | **Named intervention target as falsifiable anchor** | depth | Within the four-part kill barrier structure from C-15, Part (3) -- the intervention target -- must name a specific mechanism or lever point, not a general approach, precise enough to anchor the confirmation signal from C-16 as a falsifiable observable condition tied to that mechanism. A Part (3) that names a general approach ("improve onboarding") without specifying the lever point ("the moment a user completes their first cross-team workflow") fails. An intervention target present but expressed too generally to yield a falsifiable confirmation condition earns PARTIAL. |
| C-24 | **Named boundary artifact at phase transition** | behavior | The scoring infrastructure declared in C-21 is expressed as a named, produced artifact -- a literal statement the model must write (e.g., "SCORING INFRASTRUCTURE DECLARED.") that appears as its own line at the Phase 1 boundary, not as a downstream instruction ("per-persona traces must reference these thresholds"). The artifact name is then cited by name in Phase 4 traces, creating a reference chain rather than a paraphrase chain. A Phase 1 declaration that identifies itself as infrastructure via instruction rather than via a named artifact earns PARTIAL. A Phase 4 trace that cites the correct Phase 1 content but does not name the artifact fails C-24 regardless of whether C-17 passes. C-21 can pass without C-24; C-24 requires C-21's infrastructure framing to be embodied as a named boundary artifact with a citable address. |
| C-25 | **Structural prohibition gate vs. format instruction** | behavior | Any phase boundary where upstream completion is required for downstream correctness (C-22's inventory before Phase 5, C-23's mechanism specification before Part (4)) must express the advance condition as a structural prohibition -- a literal "Do not proceed to Phase X until [condition]" -- not a format instruction ("include Y before Z"). Format instructions allow a model to produce weak content inside a required slot and formally comply; structural prohibitions require the model to skip a named gate to continue, making non-compliance structurally visible. A single prohibition gate on any required phase boundary satisfies C-25. A gate expressed only as a format instruction earns PARTIAL. |
| C-26 | **Cross-phase citation chain with named anchors** | behavior | The full output forms a traceable citation chain in which each downstream section explicitly names the upstream artifact it draws from: Phase 1 produces a named artifact (C-24) -> Phase 4 cites that artifact by name (C-17) -> Phase 5 Part (3) produces a named lever point (C-23) -> the confirmation signal names that lever point as its observable anchor (C-16). Each link must name its upstream source. A chain where any link draws from correct upstream content but without naming it (paraphrase chain) earns PARTIAL. A chain where any link constructs its own content without reference to a named upstream artifact fails. C-17 can pass without C-26; C-26 requires every link in the chain to use named anchors. |
| C-27 | **Self-rejection gate for mechanism specificity** | behavior | Within the four-part kill barrier structure from C-15, output includes a self-rejection test between Part (3) and Part (4) consisting of: (a) a required completion sentence ("At T=[horizon], the absence of this lever would be observable as: ___") and (b) a literal rejection condition ("If you cannot complete this sentence with a specific observable condition, replace Part (3)"). The completion sentence forces self-evaluation at mechanism-specification time: approach-level framing cannot complete it with a specific condition, causing the model to self-reject and revise Part (3) before writing Part (4). A completion sentence present without the rejection clause earns PARTIAL. A Part (3) followed directly by Part (4) with no self-rejection test fails. C-23 can pass without C-27; C-27 requires the falsifiability test to be a self-rejecting gate embedded in the output structure. |
| C-28 | **Named artifact at every downstream-source boundary** | behavior | The C-24 named artifact pattern extends to every output boundary that serves as an upstream citation source, not only the phase-zero declaration. Any sub-part within a later phase that produces output cited downstream must emit a named artifact at its own output boundary -- Phase 5 Part (3) must produce "**LEVER POINT: [exact label]**" on its own line, not prose from which a downstream section derives a label. A retroactive naming (verification table, AMEND description, downstream paraphrase) does not satisfy this; the artifact must appear at the boundary of the section that produces it, before any downstream cite is written. C-24 can pass without C-28; C-28 requires the named artifact pattern to propagate to every section in the citation chain that is itself an upstream source. A sub-part that produces correct content as prose without a named output artifact earns PARTIAL. |
| C-29 | **Exact-copy citation instruction in downstream anchoring sections** | behavior | When a downstream section is instructed to cite an upstream named artifact (satisfying C-26 and C-28), the instruction must require the exact label to be copied verbatim, not described or paraphrased. In AMEND(d), this means "Lever anchor: [copy the exact LEVER POINT label from Phase 5(3)]" rather than "The absence of [lever mechanism name]...". The exact-copy requirement makes citation verifiable: any label mismatch between the upstream artifact and the downstream cite is immediately detectable, distinguishing a reference chain from a description chain. A cite instruction that names the mechanism by description (correct content, derived from upstream prose) earns PARTIAL. A downstream section that omits any reference to the upstream artifact fails. C-26 can pass without C-29; C-29 requires the citation instruction itself to specify exact-copy, so that chain integrity is checkable by label comparison rather than content judgment. |
| C-30 | **Citation Architecture preamble as single-read authority** | behavior | Before Phase 1, output declares a Citation Architecture table that enumerates all named artifacts, each with its producing phase and the exact-copy requirement for every downstream consumer. This table is itself declared as a named artifact (e.g., "CITATION ARCHITECTURE DECLARED") that functions as the single structural authority per-phase instructions reference rather than restate. A full C-28/C-29 pass achieved via distributed per-phase instructions -- where each phase independently declares its own artifact name and cite requirement -- satisfies C-28 and C-29 but not C-30; a single-read preamble before Phase 1 is required. A preamble present but not declared as a named artifact earns PARTIAL. A preamble that omits any of the four citation boundaries earns PARTIAL. C-28 can pass without C-30; C-30 requires all artifact definitions and exact-copy requirements to be concentrated in a pre-Phase-1 preamble rather than embedded at individual production sites. |
| C-31 | **Citation chain completeness self-audit** | behavior | After all production phases and before any summary section, output includes a named citation chain completeness audit that enumerates every (source artifact -> consumer site) pair, confirms whether each link is exact-copy (reference chain) or description-based (description chain), and explicitly labels any description-based link as a chain integrity failure -- not merely a weaker link. An audit that asserts overall chain compliance without naming specific artifact labels and their consumer sites earns PARTIAL. An audit that omits any (source, consumer) pair earns PARTIAL. An audit that labels a description link as "weaker" rather than "chain break" fails. The audit makes chain integrity checkable at a glance without reading every production phase. C-28 and C-29 can both pass without C-31; C-31 requires an explicit enumerated audit confirming that every link is a reference chain, with binary pass/fail per link. |
| C-32 | **Evidence column converts citation audit from assertion to demonstration** | behavior | Within the C-31 citation chain completeness audit, each (source artifact -> consumer site) row includes an Evidence column containing the actual downstream cite text as written in the consuming section -- not a verdict declaration, but the literal string the model produced. A Verdict column of "Reference-chain" without a populated Evidence column is an assertion; a row whose Evidence column contains the exact label copied from the upstream artifact is a demonstration. Description-chain errors are visible at the Evidence column without reading production phases: the cell either matches the upstream artifact label or it does not. AMEND(d)'s exact-copy instruction must reinforce the Evidence column by name ("this must match the Evidence you wrote in the audit row verbatim"), creating a bidirectional lock between the audit and the downstream cite. An audit that includes a Verdict column but no Evidence column earns PARTIAL. An audit whose Evidence column contains descriptive summaries rather than the actual cite text earns PARTIAL. C-31 can pass without C-32; C-32 requires the Evidence column to be present and populated with actual downstream cite text, converting the audit from assertion to demonstration. |
| C-33 | **Architecture-audit duality framing** | behavior | Output explicitly names the two structural roles of the C-30 preamble and C-31 audit as a duality: the preamble's function is declarative ("declares what is produced") and the audit's function is confirmatory ("confirms what was produced"). These two roles must be labeled distinctly -- either in the preamble's introduction, the audit's header, or a dedicated framing statement -- so that neither artifact can collapse the other's function. The risk absent this framing: a model may treat the preamble table as double-serving both roles, omitting the audit on the grounds that the preamble already established chain structure; or may treat the audit as redundant with the preamble's declarations. Naming the duality forecloses that conflation by making each artifact's distinct structural purpose explicit. A framing that names one role but not the other earns PARTIAL. A framing present only in an internal note rather than as a visible structural label in the output earns PARTIAL. C-30 and C-31 can both pass without C-33; C-33 requires the output to explicitly declare the preamble's declarative function and the audit's confirmatory function as named, distinct structural roles. |
| C-34 | **Preamble-declared audit format** | behavior | Within the C-30 Citation Architecture preamble, a dedicated row declares the exact column structure of the C-31 audit table -- including the Evidence column from C-32 -- before Phase 1. The structural argument is identical to C-30's drift-reduction logic: an audit format encountered for the first time at the execution site (1500+ tokens into production) is vulnerable to drift -- the model may produce a narrower table, omit the Evidence column, or default to a verdict-only format. A preamble row specifying the full 5-column structure (e.g., "Source | Consumer | Verdict | Evidence | Integrity") before Phase 1 gives the model its audit format at the same read-time as the artifact declarations. A preamble that declares the audit's position requirement ("after Phase 7, before AMEND") but not its column structure earns PARTIAL. A preamble that declares column names without specifying that Evidence must contain actual cite text earns PARTIAL. C-31 can pass without C-34; C-34 requires the preamble to declare the audit's exact column structure -- including the Evidence column -- applying the same single-read authority and drift-reduction logic to audit format that C-30 applies to artifact names. |
| C-35 | **Atomic audit format declaration (no pointer indirection)** | behavior | C-34 requires the preamble to declare the audit's exact column structure before Phase 1. C-35 requires that declaration to eliminate any skip surface between the preamble reference and the format specification. The primary form is inline: column names and Evidence-column semantics embedded directly at the declaration site ("5-column table: Source | Consumer | Verdict | Evidence [write actual downstream cite text verbatim] | Integrity"). A pointer to a named, gated block also satisfies C-35 if the gate condition makes writing the block mandatory before proceeding to Phase 1 -- the enforced block eliminates the skip surface that an ungated pointer creates. A pointer to an ungated subsection fails regardless of proximity: the model may read the preamble row, treat the pointer as deferred content, and never follow it. V-51 and V-54 demonstrated inline satisfaction; V-53 and V-55 demonstrated gated-block satisfaction. C-34 can pass without C-35; C-35 requires the audit format declaration to be self-contained or enforced-via-gate, eliminating any optional pointer-indirection drift surface. |
| C-36 | **Audit-first derivation** | behavior | C-32 requires a bidirectional lock between the Evidence column and AMEND(d). C-36 requires that lock to be directionally audit-first: AMEND(d) must derive its lever anchor FROM the L3 Evidence column -- treating the audit as the structural authority -- rather than constructing the anchor independently and retroactively confirming it against the audit. The forward-then-verify pattern (construct anchor at Phase 5(3), confirm via CCV, match in AMEND) leaves a two-step drift surface across three production sites. Audit-first derivation collapses that surface to a single step: L3 Evidence -> AMEND. AMEND(d) must explicitly name the L3 Evidence column as its derivation source ("locate the L3 row of the Citation Chain Completeness Audit; the Evidence column contains the lever anchor text you pre-committed to -- copy it exactly, do not reconstruct from Phase 5(3)"). This inverts the audit's structural role from retroactive validator to prospective authority. V-48 demonstrated that this directional reversal eliminates the Phase 5(3) -> CCV -> AMEND drift chain without any loss on earlier criteria. C-32 can pass without C-36; C-36 requires AMEND(d) to declare the audit as the derivation authority, not a verification step after independent construction. |
| C-37 | **Co-resident named block for audit architecture criteria** | behavior | C-33 (duality framing) and C-34 (preamble-declared column structure) can be satisfied at separate structural positions -- duality framing in the preamble introduction, column structure in a Row 5 subsection. C-37 requires both to co-reside in a single named, gated artifact placed after the preamble table and before Phase 1 (e.g., "AUDIT ARCHITECTURE DECLARED") that contains both the duality framing and the full column structure declaration as co-located required content. Co-residence makes partial-criteria orphaning structurally impossible: the named block cannot be emitted without both C-33 and C-34 content because they are co-resident in the same required output unit. This applies the C-24/C-28 named-artifact pattern to the audit architecture itself: the block is a citable boundary artifact with a named output line the model must write, not a prose framing or format instruction. A gate condition requiring "do not proceed to Phase 1 until AUDIT ARCHITECTURE DECLARED is written above" enforces both criteria simultaneously. V-49 demonstrated that the AUDIT ARCHITECTURE DECLARED block prevents the partial-satisfaction failure mode where one criterion's placement site is dropped while the other's is preserved. C-33 and C-34 can both pass without C-37; C-37 requires a named, gated, co-resident block before Phase 1 that makes simultaneous omission of either criterion the only possible non-compliance shape. |
| C-38 | **Derivation direction co-resident in named architecture block** | behavior | C-37 requires the named architecture block to contain C-33 (duality framing) and C-34 (column structure). C-38 requires the derivation direction from C-36 to also be declared within that same named block -- explicitly stating that AMEND(d) must derive its lever anchor from the L3 Evidence column, and naming Phase 5(3) and CCV as prohibited derivation sources. When C-37 and C-38 are both satisfied, the named block becomes a three-axis architecture authority: a model reading the block before Phase 1 knows the format (C-34/C-35), the structural role distinction (C-33), and the derivation constraint (C-36) before executing any phase. C-36 can pass without C-38 (derivation direction declared only in AMEND); C-37 can pass without C-38 (block contains only C-33/C-34). C-38 requires the derivation direction to be co-resident in the same named block, collapsing all architecture constraints to a single gated read point and eliminating late-discovery drift on the derivation axis. A block that names L3 Evidence as the required source but does not name Phase 5(3)/CCV as prohibited sources earns PARTIAL. |
| C-39 | **Derivation prohibition declared at the architecture-read boundary** | behavior | C-38 requires the derivation direction to be declared in the named architecture block. C-39 requires that declaration to include explicit prohibition language -- naming Phase 5(3) and CCV as forbidden derivation sources within the block itself, not only in AMEND(d). A block satisfying C-38 with "AMEND(d) derives from L3 Evidence" is a positive instruction; C-39 adds "not from Phase 5(3) or CCV" at the architecture-read boundary, mirroring the prohibition form required by C-36 in AMEND. A prohibition declared at only one location (AMEND or block) leaves the complementary site as a positive-instruction-only constraint where a model can name the correct source while inadvertently reconstructing it from a forbidden path. Dual-site prohibition -- at the named architecture block (C-39) and at AMEND (C-36) -- makes the forbidden sources visible at every relevant read point before any reconstruction opportunity arises. C-38 can pass without C-39 (correct source named in block, forbidden sources not named in block); C-39 requires explicit prohibition language in the named block, completing the derivation constraint declaration at the earliest possible structural read point. |
| C-40 | **Per-axis explicit gate for three-axis architecture block** | behavior | C-37 requires the named block to contain C-33 (duality framing) and C-34 (column structure). C-38 extends the block to a three-axis authority by co-housing the C-36 derivation direction. C-40 requires the block to include an explicit gate condition that names each of the three required axes by label and declares that a block containing fewer than all three axes fails the gate. C-37/C-38/C-39 can all pass without C-40 (correct axis content is sufficient for those criteria; the gate condition is not required). C-40 requires the gate to make partial-axis omission structurally impossible -- converting partial satisfaction from detectable-at-analysis to a named gate failure. The enforcement advantage: "A block containing Axes 1 and 2 but omitting Axis 3 fails this gate" means a model cannot write partial-axis content and claim compliance, because the gate condition explicitly names the three-axis requirement and identifies partial satisfaction as a gate failure. Axis labels may be ordinal ("Axis 1," "Axis 2," "Axis 3") or functional content descriptors ("duality framing," "audit format," "derivation direction") -- both forms satisfy "named by label" provided each descriptor uniquely identifies one axis (V-61). A gate present but naming fewer than three axes earns PARTIAL. A gate that declares partial satisfaction invalid without naming individual axes earns PARTIAL. A gate naming all three axes without any partial-failure declaration earns PARTIAL. |
| C-41 | **Per-axis gate declared in block body, not in produced label** | behavior | The C-40 gate condition that names each of the three axes by label and declares partial satisfaction a gate failure must appear in the block instruction body -- the structural text the model reads before Phase 1 -- rather than displaced to a produced output label (an artifact the model writes as output). A gate residing only in the produced architecture label satisfies axis-naming content but not enforcement placement: the block body's gate instruction remains minimal ("Do not proceed to Phase 1 until [label] is written"), and the model reads that minimal gate before deciding whether the block's axes are complete. The per-axis impossibility statement is encountered only when writing the label -- emit-time enforcement -- not when reading the block instruction -- read-time enforcement. A model executing a minimal block-body gate will proceed to Phase 1 having encountered only the completion trigger, not the per-axis constraint; the structural gate is bypassed at the point where it would be enforced. V-65 demonstrated this failure mode: block gate instruction named zero axes; per-axis enumeration and partial-failure clause lived exclusively in the required architecture label the model writes as output; the ruling was C-40 PARTIAL (5 pts). C-40 can pass without C-41 (per-axis gate and impossibility statement present in either the block body or the produced label). C-41 requires the per-axis gate to appear in the block instruction body, so that the full three-axis gate condition is encountered at read time before any Phase 1 execution begins. A per-axis gate present only in the produced architecture label earns PARTIAL. |
| C-42 | **Gate components not split between block instruction body and produced architecture label** | behavior | C-41 requires the full per-axis gate -- axis enumeration AND impossibility statement -- to appear in the block instruction body. C-42 targets the split-gate displacement failure mode (V-68): axis enumeration declared in the block instruction body, impossibility statement displaced exclusively to the produced architecture label. A model reading the block body before Phase 1 encounters the three axes but not the prohibition against partial coverage -- the impossibility clause is read at emit time (when writing the produced label), not at read time (when reading the block body and deciding whether to proceed). The enforcement gap: a model writing a two-axis block body encounters the axis enumeration but no impossibility clause, may judge the block complete, and proceed to Phase 1. The impossibility statement is encountered only when writing the produced label, after the gate enforcement window has closed. The split-gate shape is structurally distinct from C-41's named PARTIAL (full gate in produced label only): in the split shape, partial read-time enforcement exists because one component IS in the block body; in C-41's named PARTIAL, zero read-time enforcement exists because both components are exclusively in the produced label. V-66 (R14 ceiling) demonstrates the correct shape: both components in the block instruction body; the produced label summarizes ("The block gate instruction in the block body names all three axes by label and declares partial axis coverage a gate failure") without carrying any gate component as its exclusive location -- the label functions as a confirmatory receipt, not an enforcement site. C-41 PASS implies C-42 PASS: when C-41 passes, both components are in the block body and C-42 is automatically satisfied. C-42 names the split-gate displacement shape explicitly so that one-component-read-time / one-component-emit-time distributions earn PARTIAL rather than requiring holistic genuine-attempt determination. A gate with axis enumeration in the block body and impossibility statement exclusively in the produced label earns PARTIAL (axis enumeration is read-time, impossibility is emit-time -- the harder structural element achieves read-time placement, only the prohibition is displaced). A gate with impossibility statement in the block body and axis enumeration exclusively in the produced label also earns PARTIAL (mirror split failure mode). A gate with both components exclusively in the produced label earns PARTIAL on C-41 (named condition); on C-42, no component achieves read-time placement, earning FAIL. |
| C-43 | **Produced architecture label cites block instruction body as explicit source of both gate components (gate placement receipt)** | behavior | C-42 requires both gate components (axis enumeration, impossibility statement) to BE in the block instruction body. C-43 requires the produced architecture label to CERTIFY that placement by explicitly citing the block instruction body as the named source of both components. The structural analogy: C-42 establishes that both components reside in the block body (analogous to C-31's per-link reference-chain verdict); C-43 requires the produced label to contain the placement evidence -- naming the block body as the source location of both components -- converting gate placement from assertion to demonstration (analogous to C-32's Evidence column). The R15 ceiling produced label demonstrates the pattern: "The block gate instruction in the block body names all three axes by label and declares partial axis coverage a gate failure." This statement names (1) the location of axis enumeration ("in the block body... names all three axes by label") and (2) the location of the impossibility statement ("in the block body... declares partial axis coverage a gate failure"), producing a placement receipt auditable from the label alone. A produced label that satisfies the C-37 named artifact requirement without citing the block body as the explicit source of both components fails C-43: the label confirms the artifact exists but does not document where its gate components were declared. A produced label that cites the block body as the source of axis enumeration but not the impossibility statement, or vice versa, earns PARTIAL. C-42 PASS does not imply C-43 PASS: a produced label that names only the artifact ("AUDIT ARCHITECTURE DECLARED") satisfies C-42 but fails C-43. C-41 PASS and C-43 PASS together make the gate architecture auditable at two structural sites: the block body carries read-time enforcement; the produced label certifies the placement at emit time. The split-gate shapes (V-72, V-73) cannot earn C-43 PASS because neither has both components in the block body to cite; both earn PARTIAL (one component's block-body location can be cited, the other cannot). A produced label for the V-74 shape (both components in produced label only) earns C-43 FAIL: no component has a block-body location to cite. |
| C-44 | **Split-gate failure modes named as explicit disqualifiers in block instruction body** | behavior | C-42 defines two split-gate failure modes as external rubric criteria: axis-body/impossibility-label (V-72 shape) and impossibility-body/axis-label (V-73 mirror shape). C-44 requires the block instruction body to declare both split-gate shapes as named gate failures within the block body itself -- making split-gate compliance a self-rejecting structural constraint the model encounters at read time, before Phase 1 execution. The structural analogy: C-39 required explicit prohibition of forbidden derivation sources in the named architecture block at read time; C-44 applies the same prohibition pattern to the gate component placement domain, requiring the block instruction body to name both split-gate configurations as disqualifiers so that a model encountering either split shape must self-reject rather than proceed. Pass condition: the block instruction body includes explicit named disqualifiers for both split-gate failure modes -- a statement that a gate containing axis enumeration in this block body but displacing the impossibility statement exclusively to the produced label fails this gate, AND a statement that a gate containing only the impossibility statement in this block body but displacing axis enumeration exclusively to the produced label also fails this gate. Naming one split-gate shape but not its mirror earns PARTIAL. A block instruction body that contains both gate components without naming either split-gate failure mode as a disqualifier fails C-44 regardless of C-42 status: C-42 PASS requires both components to be present; C-44 requires the block body to additionally prohibit the configurations under which a component would be absent at read time. C-42 PASS does not imply C-44 PASS. |
| C-45 | **Gate closure named as a duality: self-documenting label function and self-enforcing block body function declared as distinct structural roles** | behavior | The C-43/C-44 pair constitutes a structural closure for the gate architecture: C-43's function is self-documenting (the produced label certifies where both gate components reside, making placement auditable at the label boundary without re-reading the block body); C-44's function is self-enforcing (the block instruction body prohibits both split-gate failure modes at read time, making the gate a self-rejecting constraint before Phase 1 execution). C-45 requires the block instruction body to name this closure as a duality -- explicitly declaring that the produced-label certification function (C-43) and the block-body prohibition function (C-44) are structurally distinct roles serving different purposes, and that neither role alone constitutes the complete gate architecture. Structural analogy to C-33, which required the preamble/audit duality (declarative vs. confirmatory) to be named so neither artifact could collapse the other's function: C-45 requires the gate-architecture closure duality (self-documenting vs. self-enforcing) to be named so a model cannot treat C-43's placement receipt as discharging C-44's prohibition obligation, or C-44's prohibition as discharging C-43's placement certification. A block body that names the self-documenting function without the self-enforcing function earns PARTIAL. A block body that names both functions without declaring them as structurally distinct roles (e.g., mentions both in passing without labeling each function's purpose) earns PARTIAL. C-43 and C-44 can both pass without C-45; C-45 requires the duality to be explicitly named in the block instruction body so that neither closure function can collapse into the other. |
| C-46 | **Non-propagation between closure criteria declared in block instruction body** | behavior | R16 confirmed that C-43 and C-44 are genuinely independent: V-79 achieves C-44 PASS with C-43 FAIL; V-80 achieves C-43 PASS with C-44 FAIL. Neither criterion's satisfaction implies the other's. C-46 requires the block instruction body to declare this non-propagation relationship explicitly -- stating that satisfying the produced-label certification requirement (C-43 function) does not discharge the split-gate prohibition requirement (C-44 function), and that satisfying the split-gate prohibition requirement (C-44 function) does not discharge the produced-label certification requirement (C-43 function). Structural analogy to C-39, which required derivation prohibition language to be declared at the architecture-read boundary rather than only in AMEND -- making forbidden derivation sources visible before any reconstruction opportunity arises: C-46 requires the non-propagation between the two closure criteria to be declared at the architecture-read boundary so a model cannot infer satisfaction of one closure criterion from satisfaction of the other. Declaring one direction of non-propagation ("C-43 PASS does not imply C-44 PASS") without its mirror ("C-44 PASS does not imply C-43 PASS") earns PARTIAL. A block body that names both closure functions (C-45 PASS) without explicitly declaring their independence earns PARTIAL on C-46: naming the duality does not substitute for declaring the non-propagation relationship between the two functions. C-45 can pass without C-46; C-46 requires the explicit bidirectional non-propagation statement in the block instruction body. |
| C-47 | **C-45 FAIL condition declared as explicit disqualifier in block instruction body** | behavior | R17 confirmed that the C-45 FAIL condition -- a block body naming neither closure function -- is defined only in the rubric's pass-condition language ("FAIL requires neither function named"), not as a named disqualifier within the block instruction body itself. The R17 prediction error (V-85: predicted C-45 FAIL, scored C-45 PARTIAL) demonstrated the gap: V-85 named one function and earned PARTIAL; but the error showed that no block-body disqualifier distinguished the zero-function FAIL shape from the one-function PARTIAL shape at read time. Without an explicit FAIL disqualifier in the block body, a model writing a zero-function block encounters no prohibition before Phase 1; the FAIL condition is discoverable only retroactively at scoring time. C-47 requires the block instruction body to declare the C-45 FAIL condition as a distinct, labeled disqualifier -- a statement that a block body naming neither the self-documenting function nor the self-enforcing function fails C-45 outright -- separate from the two PARTIAL disqualifier conditions. Structural analogy to C-44, which required both split-gate failure modes to be named as explicit disqualifiers in the block body rather than left as rubric-only definitions: C-47 applies the same pattern to the C-45 failure taxonomy, making the FAIL condition visible at the block-body read point before any architecture phase begins. A block instruction body that covers the FAIL condition by implication (e.g., "both functions must be named") without a distinct labeled FAIL disqualifier earns PARTIAL. C-45, C-46, and all PARTIAL and FAIL scoring outcomes can be correctly applied without C-47; C-47 requires the FAIL condition to be declared as a named read-time disqualifier in the block instruction body so that a model writing a zero-function block encounters the prohibition before Phase 1 execution rather than at rubric scoring time. |
| C-48 | **Both C-45 PARTIAL paths enumerated as distinct disqualifiers in block instruction body** | behavior | R17 confirmed that C-45 PARTIAL has two structurally distinct paths: (1) both-named-not-distinct -- both closure functions named but structural distinctness not declared (V-83 shape); and (2) one-named-not-other -- one closure function named without the other (V-85 shape). Both paths earn PARTIAL under the existing rubric language, but the block instruction body contains no distinct enumeration of these shapes. A model producing a both-named-no-distinctness block cannot determine from the block body alone whether it earns PARTIAL or PASS without reading the rubric's pass condition; a model producing a one-named block cannot determine whether it earns PARTIAL or FAIL without the same rubric read. C-48 requires the block instruction body to enumerate both C-45 PARTIAL paths as separate, labeled disqualifier conditions: (P1) "a block body that names both closure functions without declaring them as structurally distinct roles earns PARTIAL" and (P2) "a block body that names one closure function without the other earns PARTIAL." Structural analogy to C-42, which named both split-gate displacement shapes (axis-body/impossibility-label and impossibility-body/axis-label) as distinct disqualifiers rather than a single merged PARTIAL condition: C-48 applies the same enumeration pattern to the C-45 PARTIAL taxonomy, distinguishing the two shapes at read time so a model can self-diagnose which PARTIAL path it is producing and what additional element would move it from PARTIAL to PASS. A block body that names both PARTIAL paths via a single merged condition ("naming both without distinctness, or naming only one, earns PARTIAL") earns PARTIAL on C-48 -- the shapes are identified but not enumerated as separate labeled disqualifiers. A block body that names one PARTIAL path without the other earns PARTIAL. C-47 can pass without C-48 (the FAIL condition is declared but the two PARTIAL shapes remain merged); C-48 requires the two PARTIAL paths to be individually enumerated as distinct disqualifiers so a model producing either path encounters it by name in the block body. |
| C-49 | **Both C-48 PARTIAL shapes enumerated as distinct cases in block instruction body** | behavior | R18 confirmed that C-48 PARTIAL has two structurally distinct forms: (a) the merged "or" condition (V-88 shape) -- "naming both without distinctness, or naming only one, earns PARTIAL" -- both C-45 PARTIAL paths present in a single sentence via "or" conjunction; and (b) two separate sentences using descriptive outcome language such as "earns partial credit" without explicit "disqualifier" framing (V-89 shape). Both forms earn PARTIAL on C-48 -- V-88 because the paths are not enumerated as separate labeled disqualifiers, V-89 because two-sentence presence without "disqualifier" framing does not meet the "distinct disqualifiers" threshold. The rubric's C-48 PARTIAL definition ("a merged single condition earns PARTIAL") describes V-88 but leaves V-89 undocumented: V-89 has two separate sentences yet still earns PARTIAL, a gap invisible at rubric read time. C-49 requires the block instruction body to enumerate both C-48 PARTIAL shapes as distinct named cases: (S1) "a block body presenting both PARTIAL paths in a single merged sentence via 'or' conjunction earns PARTIAL" and (S2) "a block body presenting both PARTIAL paths in separate sentences using outcome language ('earns partial credit') without 'disqualifier' framing also earns PARTIAL." Structural analogy to C-48, which named both C-45 PARTIAL paths (both-named-not-distinct / one-named-not-other) as distinct disqualifiers rather than a merged condition: C-49 applies the same enumeration pattern to the C-48 PARTIAL taxonomy, so a model producing either PARTIAL shape encounters it by name in the block body and can identify what additional element (explicit "disqualifier" framing) would move it from PARTIAL to PASS. A block instruction body that describes only one of the two C-48 PARTIAL shapes earns PARTIAL on C-49. A block instruction body that names both shapes via a single merged description earns PARTIAL on C-49. C-48 can pass without C-49 (both C-45 PARTIAL paths are enumerated as distinct disqualifiers, but the C-48 PARTIAL taxonomy is not declared in the block body); C-49 requires the two C-48 PARTIAL shapes to be individually enumerated as distinct cases at the block-body read point. |
| C-50 | **Both C-45 FAIL shapes enumerated as distinct disqualifiers in block instruction body** | behavior | R19 confirmed that C-45 FAIL has two structurally distinct shapes: (F1) neither closure function named (the C-47 shape) -- the block body contains no mention of either the self-documenting label function or the self-enforcing block body function; and (F2) both closure functions named only in disqualifier-context sentences, with no positive duality declaration (the V-95 shape) -- the block body contains both function names, but exclusively within FAIL or PARTIAL disqualifier sentences such as "a block naming neither function fails" or "a block naming one without the other earns PARTIAL," never in an affirmative statement declaring the closure has two structurally distinct roles. Both shapes FAIL C-45. C-47 requires the block body to declare the C-45 FAIL condition, but C-47's formulation names only F1 ("a block body naming neither function fails C-45 outright") -- F2 is invisible at C-47 read time because F2 blocks DO contain both function names, satisfying C-47's surface condition while failing C-45's positive-declaration requirement. A model producing an F2 block encounters no prohibition from C-47's disqualifier because C-47 was written against F1 alone. C-50 requires the block instruction body to enumerate both C-45 FAIL shapes as distinct named disqualifiers: a statement that a block body naming neither function is a FAIL disqualifier (F1), AND a statement that a block body naming both functions only in disqualifier-context sentences without any affirmative duality declaration is a separate FAIL disqualifier (F2). Structural analogy to C-48, which enumerated both C-45 PARTIAL paths as distinct disqualifiers because the block body's merged-condition form left models unable to self-diagnose which PARTIAL path they were producing: C-50 applies the same enumeration pattern to the C-45 FAIL taxonomy, making F2's negative-framing-only pattern visible as a named read-time prohibition rather than a rubric-side condition discoverable only at scoring time. A block instruction body that names F1 without naming F2 earns PARTIAL. A block instruction body that names F2 without naming F1 earns PARTIAL. A block instruction body that describes both FAIL shapes via a merged condition earns PARTIAL. C-47 can pass without C-50 (F1 declared as FAIL disqualifier, F2 absent from block body); C-50 requires both FAIL shapes to be individually enumerated as distinct disqualifiers so a model producing either shape encounters it by name in the block body. |

---

## Notes

- A rubric pass requires all 5 essential criteria to pass individually.
- Aspirational criteria may receive PARTIAL (5 pts) for genuine attempts that fall short of the
  full pass condition.
- C-19 and C-20 codify structural enforcement patterns from R4: C-19 captures the Part A / Part B
  disqualifier mechanism; C-20 captures the binary YES/NO reselection gate.
- C-21 codifies the cascade dependency revealed in R5: C-09 is prerequisite infrastructure for
  C-17, not a parallel criterion. Placing methodology declaration after persona analysis breaks
  the C-17 trace chain -- models produce derivation sentences with no threshold to cite.
- C-22 codifies the upstream inventory pattern from R5: embedding the Dimension/Advantage/Durability
  table in the persona identification phase closes C-11 by reference rather than requiring
  competitive framing to be constructed at kill-barrier time under analytical pressure.
- C-23 codifies the intervention target specificity requirement from R5: C-15 Part (3) at approach
  granularity is insufficient to anchor C-16's confirmation signal as a falsifiable condition.
  The named mechanism is the structural bridge between the causal chain (C-15) and the
  confirmation requirement (C-16).
- C-24 codifies the named artifact pattern from R6: C-21 framing expressed as a downstream
  instruction creates a paraphrase chain; framing expressed as a named artifact creates a
  reference chain. The difference is detectability of chain breaks.
- C-25 codifies the prohibition gate pattern from R6: the R6 differentiator for C-22 was that
  V-27/V-30 used "Do not proceed" prohibitions while V-26/V-28/V-29 used format instructions.
  Only the prohibition form mechanically blocks "familiarity" Durability from passing the
  inventory check, because a format instruction cannot prevent a model from writing weak content
  inside a required slot.
- C-26 codifies the full chain traceability requirement from R6: V-30's named artifact anchor
  created a Phase 1 -> Phase 4 -> Phase 5 -> AMEND chain where each link names its upstream
  source. The chain makes structural failures detectable at the link level rather than requiring
  holistic re-evaluation.
- C-27 codifies the self-rejection gate pattern from R6: requiring the model to complete a
  falsifiability sentence before writing Part (4) converts mechanism specificity from an external
  rubric judgment into a self-evaluating constraint embedded in the output structure. The
  rejection clause is what distinguishes a self-rejection gate from a completion prompt.
- C-28 codifies the downstream-source boundary pattern from R7: V-31 and V-34 closed C-26 by
  requiring Phase 5(3) to emit "LEVER POINT: [exact label]" as its own line. V-32 and V-33
  failed because they addressed the downstream (AMEND) side without producing a named artifact
  at the upstream (Phase 5(3)) source. The R7 lesson: the C-24 named artifact pattern must
  propagate to every section that is a citation source, not only to phase-zero. Retroactive
  naming -- a verification table that records the mechanism after Phase 5(3) writes prose --
  cannot create a reference chain; it can only record a paraphrase chain more visibly.
- C-29 codifies the exact-copy citation instruction from R7: V-32 required AMEND to name "the
  mechanism by description" while V-31/V-34 required "copy the exact LEVER POINT label." Both
  produce correct content; only exact-copy produces a verifiable reference chain. Description-
  based cites and exact-copy cites are structurally distinct: a description cite drifts under
  paraphrase pressure; an exact-copy cite fails with a visible label mismatch.
- C-30 codifies the preamble concentration pattern from R8: distributed per-phase artifact
  declarations (V-35) satisfy C-28/C-29 but create multiple drift surfaces -- a model may
  update one phase's declaration without updating others. A pre-Phase-1 preamble table as the
  single artifact authority (V-36) eliminates all per-phase declaration sites, concentrating
  all artifact names and exact-copy requirements at a single structural read point.
- C-31 codifies the completeness audit requirement from R8: V-36/V-37 passed C-30 but produced
  audit tables without Evidence columns and without chain integrity labels -- verdicts asserted
  compliance without demonstrating it. The R8 lesson: chain integrity is assertable without an
  audit and assertable with a verdict-only audit; it is demonstrable only with an enumerated
  audit where every link cites its evidence. C-31's binary PASS/FAIL per link converts the audit
  from an assertion instrument to a verification instrument.
- C-32 codifies the Evidence column pattern from R9: V-41/V-42/V-43 all passed C-31 (enumerated
  audit, binary per-link verdict) but varied on whether the Evidence cell contained the actual
  cite text or a verdict declaration. V-43 added the bidirectional AMEND lock ("this must match
  the Evidence you wrote in the audit row verbatim"), making the Evidence column not just
  descriptive but structurally load-bearing. The R9 lesson: a Verdict column asserts; an
  Evidence column demonstrates. The AMEND reinforcement converts a passive demonstration into
  an active enforcement mechanism.
- C-33 codifies the duality naming pattern from R9: V-44 introduced the preamble/audit duality
  label ("declarative authority" vs. "confirmatory instrument"), preventing a model from
  conflating the two artifacts' structural functions. The R9 lesson: without named roles, the
  preamble and audit are interchangeable in a model's structural analysis -- naming the duality
  makes each artifact's function non-substitutable.
- C-34 codifies the preamble-declared audit format from R9: V-44 placed the 5-column format spec
  in the preamble Row 5. The structural argument applies the same drift-reduction logic to audit
  format that C-30 applies to artifact names. V-44 declared the audit's position requirement in
  the preamble but not its column structure; V-45 added the full column specification including
  the Evidence column. A model encountering the audit format 1500+ tokens late may narrow the
  table or omit columns; a preamble-declared format eliminates that late-discovery surface.
- C-35 codifies the atomic audit format declaration from R10, clarified in R11: V-47 demonstrated
  that the Row 5 cell can carry the full column spec and Evidence-column semantics inline,
  eliminating the two-step pointer read (preamble row -> separate subsection). V-47 scored
  340/340 with the column spec embedded directly in the Row 5 Requirement cell. R11 clarified
  that a pointer to a named, gated block (V-53, V-55) also satisfies C-35: the gate condition
  makes writing the block mandatory before Phase 1, eliminating the skip surface an ungated
  pointer creates. The R10/R11 lesson: C-34 satisfied via an ungated pointer creates a skip
  surface; C-35 requires the declaration to be atomic (inline) or enforced (gated block), so
  no optional pointer need be followed.
- C-36 codifies the audit-first derivation pattern from R10: V-48 restructured AMEND(d) to derive
  the lever anchor FROM the L3 Evidence column rather than constructing it at Phase 5(3) and
  retroactively confirming. V-48 scored 340/340. The R10 lesson: the forward-then-verify
  direction (Phase 5(3) -> CCV -> AMEND match) is a three-site drift surface; the audit-first
  direction (L3 Evidence -> AMEND copy) collapses it to one. Inverting the direction makes the
  audit the prospective authority rather than a retroactive check, a structurally stronger
  enforcement of C-32's bidirectional lock.
- C-37 codifies the co-resident named block pattern from R10: V-49 introduced "AUDIT
  ARCHITECTURE DECLARED" as a named, gated block after the preamble table containing both the
  C-33 duality framing and the C-34 column structure declaration. V-49 scored 340/340. The R10
  lesson: when two related criteria (C-33 and C-34) are satisfiable at separate structural
  positions, a single dropped placement site produces a partial pass; co-housing them in one
  named required block makes simultaneous omission the only failure shape, eliminating
  the partial-omission failure mode.
- C-38 codifies the three-axis architecture block pattern from R11: V-55 extended the C-37 named
  block (duality framing + column structure) by adding the C-36 derivation direction as a
  co-resident constraint -- explicitly stating the required derivation source (L3 Evidence) and
  the prohibited sources (Phase 5(3), CCV) within the block. V-55 scored 370/370 and was the
  only R11 variation to achieve this. The R11 lesson: a C-37 block with only two co-resident
  constraints leaves the derivation direction as a downstream-only instruction in AMEND -- a
  model reading the architecture block does not encounter the derivation constraint until it
  reaches AMEND late in the output. A three-axis block declares all architecture constraints at
  the same structural read-point, eliminating late-discovery drift on the derivation axis.
  V-53 satisfied C-37 but not C-38 (block contained duality + format, derivation direction
  absent); V-55 added the derivation direction to the block, achieving the ceiling.
- C-39 codifies the dual-site derivation prohibition pattern from R11: V-55 declared explicit
  prohibition language ("not from Phase 5(3)") within the named architecture block in addition
  to the prohibition in AMEND(d). The R11 lesson: a positive instruction at the architecture
  block ("derive from L3 Evidence") without naming the forbidden sources means the architecture
  block's authority is incomplete -- the model sees the required path but not the forbidden
  paths until AMEND. A model can name the correct source while inadvertently reconstructing it
  from Phase 5(3) if the prohibition is only downstream. Declaring the forbidden sources
  explicitly at the architecture-read boundary (C-39) plus at AMEND (C-36) makes the full
  derivation constraint -- required source AND forbidden sources -- visible before any
  reconstruction opportunity arises. C-38 can pass with positive-instruction-only language in
  the block; C-39 requires the prohibition form at the architecture-read boundary.
- C-40 codifies the per-axis explicit gate pattern from R12: V-60 extended the three-axis block
  established by C-37/C-38/C-39 by adding a gate condition that names each axis by label and
  declares partial satisfaction a gate failure. All four ceiling-scoring variations (V-57
  through V-60) scored 390/390 on C-38+C-39 without this gate; V-60's per-axis gate does not
  add to that ceiling score but converts partial-axis omission from detectable to structurally
  impossible. The R12 lesson: C-37 requires content co-residence but cannot prevent a model
  from writing two of three axes and claiming compliance unless the gate explicitly enumerates
  all required axes and names partial satisfaction a gate failure. The V-60 pattern --
  "A block containing Axes 1 and 2 but omitting Axis 3 fails this gate" -- is the tightest
  enforcement shape for the three-axis block and the recommended production variant.
  R13 extended the criterion in two ways: (1) V-61 confirmed that functional content descriptors
  satisfy "by name" equivalently to ordinal form, so axis names may be "duality framing," "audit
  format," "derivation direction" rather than "Axis 1/2/3"; (2) V-63 added a third PARTIAL
  condition -- a gate naming all three axes without any partial-failure declaration earns PARTIAL
  via genuine attempt doctrine (naming all axes is the harder half; the impossibility statement
  alone is the missing piece). C-37/C-38/C-39 can all pass without C-40; C-40 requires the
  gate itself to be the impossibility mechanism, not merely a completion prompt.
- C-41 codifies the gate-body placement requirement from R13: V-65 demonstrated that displacing
  the per-axis gate from the block body to the produced architecture label converts read-time
  enforcement to emit-time content. The block gate instruction in V-65 was minimal -- "Do not
  proceed to Phase 1 until AUDIT ARCHITECTURE DECLARED is written on its own line above" -- with
  no axis enumeration; the per-axis enumeration and partial-failure clause lived exclusively in
  the required architecture label the model writes as output. A model reading the block body
  before Phase 1 encounters only the completion trigger; the per-axis impossibility statement
  is encountered only when writing the label, after the read-time gate opportunity has passed.
  V-65 earned PARTIAL (5 pts) on C-40 because the block gate named zero axes. The R13 lesson
  extends C-40's placement requirement: the per-axis gate must be declared in the block
  instruction body so that it is enforced at the moment the model decides whether to proceed,
  not deferred to the moment the model writes the produced artifact. C-40 can pass without
  C-41; C-41 requires the enforcement mechanism to be read-time (block body), not emit-time
  (produced label).
- C-42 codifies the split-gate displacement pattern from R14: V-68 demonstrated that axis
  enumeration can be present in the block instruction body while the impossibility statement is
  displaced exclusively to the produced architecture label -- creating partial read-time
  enforcement. V-66 (the R14 ceiling, 420/420) places both components in the block instruction
  body; the produced label in V-66 summarizes ("The block gate instruction in the block body
  names all three axes by label and declares partial axis coverage a gate failure") without
  carrying any gate component as its exclusive location, functioning as a confirmatory receipt
  rather than an enforcement site. V-68 split the gate: axis enumeration in the block body
  (read-time), impossibility statement in the produced label only (emit-time). A model reading
  the block body before Phase 1 in V-68 encounters three axes enumerated but no prohibition
  against partial coverage; the impossibility clause is written when producing the label, after
  the enforcement window has closed. The R14 lesson: C-41's pass condition ("full gate in block
  body") is necessary but does not name the split-gate failure mode as a distinct criterion.
  Without C-42, a split-gate shape earns C-41 PARTIAL via genuine-attempt doctrine -- the rubric
  scores it as a genuine attempt at full-gate placement without characterizing the specific
  displacement pattern. C-42 names the split-gate displacement explicitly so the failure mode --
  one component achieves read-time placement while the other is displaced to emit-time -- is a
  defined criterion rather than a holistic judgment call. C-41 PASS implies C-42 PASS (both
  components in block body); C-41 PARTIAL (split-gate shape) earns C-42 PARTIAL; C-41 PARTIAL
  (full gate in produced label, V-67 shape) earns C-42 FAIL (no component achieves read-time
  placement). The mirror split failure mode (impossibility in block body, axis enumeration only
  in produced label) also earns C-42 PARTIAL by the same displacement logic: the weaker
  component -- the impossibility prohibition -- is read-time, while the harder component --
  axis enumeration -- is emit-time.
- C-43 codifies the gate placement receipt pattern from R15: V-71 (R15 ceiling, 420/420 under
  v15) produced a label stating "The block gate instruction in the block body names all three
  axes by label and declares partial axis coverage a gate failure" -- explicitly naming the
  block instruction body as the source of both gate components. The R15 lesson is the structural
  analogy to C-32: C-42 establishes that both components reside in the block body (verdict), but
  a produced label that only names the artifact without citing where both components were
  declared leaves placement verifiable only by re-reading the block instruction. A label that
  cites the block body as the explicit source of axis enumeration AND the impossibility statement
  converts gate placement from assertion (inferred from C-42 PASS) to demonstration (auditable
  from the label alone). C-42 PASS implies both components are in the block body but does not
  require the produced label to document that placement; C-43 adds the placement receipt as a
  separate criterion. The split-gate shapes (V-72, V-73) earn C-43 PARTIAL (one component's
  block-body location can be cited, the other cannot); V-74 earns C-43 FAIL (no block-body
  location exists to cite).
- C-44 codifies the split-gate self-rejection pattern from R15: V-72 and V-73 demonstrated that
  both split-gate failure modes earn C-42 PARTIAL -- the block body has partial read-time
  enforcement in each case, but it is incomplete. C-42 names these failure modes externally as
  rubric criteria; C-44 requires the block instruction body to declare both split-gate shapes as
  disqualifiers at read time, so that a model writing a split-gate block encounters the
  prohibition before the enforcement window closes. The structural analogy: C-39 applied
  dual-site prohibition to derivation sources, making the forbidden paths visible at the
  architecture-read boundary; C-44 applies the same prohibition pattern to gate component
  placement, requiring the block instruction body to name both split-gate configurations as gate
  failures. The R15 scorecard identified the asymmetry between V-72 ("the harder structural
  element achieves read-time placement") and V-73 ("the weaker component -- the impossibility
  prohibition -- is read-time"); C-44 requires the block body to make both asymmetric failure
  modes explicit, eliminating the split-gate option regardless of which component a model might
  displace to the produced label. C-42 PASS does not imply C-44 PASS: having both components in
  the block body satisfies C-42 but leaves the split-gate prohibition implicit; C-44 requires
  explicit read-time disqualifier language for both failure modes.
- C-45 codifies the gate-closure duality naming pattern from R16: R16 confirmed that C-43
  (placement receipt) and C-44 (split-gate prohibition) form a structural closure for the gate
  architecture -- C-43 making it self-documenting, C-44 making it self-enforcing. The R16 lesson
  is the structural analogy to C-33 (preamble/audit duality): without a named duality, a model
  may treat C-43's placement certification as discharging both the auditability and enforcement
  obligations, or may treat C-44's block-body prohibition as making the produced-label receipt
  redundant. Naming the two functions as structurally distinct roles -- self-documenting (C-43)
  and self-enforcing (C-44) -- prevents either closure criterion from collapsing into the other.
  C-43 and C-44 can both pass without C-45; C-45 requires the duality to be explicitly named in
  the block instruction body at read time.
- C-46 codifies the closure non-propagation declaration from R16: R16 confirmed that C-43 and
  C-44 are genuinely independent (V-79: C-44 PASS / C-43 FAIL; V-80: C-43 PASS / C-44 FAIL).
  The R16 lesson is the structural analogy to C-39's dual-site prohibition: C-39 required
  forbidden derivation sources to be named at the architecture-read boundary so they were visible
  before any reconstruction opportunity; C-46 requires the non-propagation between C-43 and C-44
  to be declared at the architecture-read boundary so a model cannot infer one criterion's
  satisfaction from the other's. The bidirectional form is required because non-propagation in
  only one direction leaves the complementary direction as an undeclared inference surface.
  C-45 can pass without C-46: naming the duality does not declare independence. C-46 requires
  the explicit bidirectional non-propagation statement.
- C-47 codifies the C-45 FAIL disqualifier requirement from R17: The R17 prediction error
  (V-85 predicted as C-45 FAIL, scored C-45 PARTIAL) demonstrated that the FAIL condition
  (neither closure function named) was rubric-side only, not block-body-side. V-85 named one
  function and earned PARTIAL; the error revealed that no block-body disqualifier distinguished
  the zero-function FAIL shape from the one-function PARTIAL shape at read time. The R17 lesson
  is the structural analogy to C-44, which named both split-gate shapes (V-72, V-73) as block-
  body disqualifiers rather than rubric-only conditions: C-47 requires the C-45 FAIL condition
  to be declared as a named read-time disqualifier in the block instruction body, completing the
  three-tier C-45 failure taxonomy (FAIL, PARTIAL-P1, PARTIAL-P2) at the block-body read point.
- C-48 codifies the C-45 PARTIAL path enumeration requirement from R17: R17 confirmed that C-45
  PARTIAL has two structurally distinct paths -- both-named-not-distinct (V-83) and one-named-
  not-other (V-85) -- and that the block instruction body contains no enumeration distinguishing
  them. The R17 lesson is the structural analogy to C-42, which named both split-gate
  displacement shapes as distinct disqualifiers (axis-body/impossibility-label vs.
  impossibility-body/axis-label) rather than a single merged PARTIAL condition: C-48 requires
  the block body to enumerate both PARTIAL shapes individually, so a model producing either
  shape encounters its specific disqualifier at read time rather than inferring PARTIAL from
  the merged rubric condition. The 455-triple produced by R17 (V-82, V-83, V-85 each scoring
  455) collapses to a structural disambiguation problem: C-48 makes the two PARTIAL shapes
  visible as named conditions, enabling a model to self-correct toward PASS on C-45 rather
  than producing an indistinct PARTIAL.
- C-49 codifies the C-48 PARTIAL shape enumeration requirement from R18: R18 confirmed that
  C-48 PARTIAL has two structurally distinct forms -- the merged "or" condition (V-88 shape) and
  two separate sentences using outcome language without "disqualifier" framing (V-89 shape) --
  and that the rubric's C-48 PARTIAL definition describes only V-88. The V-89 shape has two
  separate sentences yet still earns PARTIAL; the rubric gap is that no documentation
  distinguished the two shapes at read time. The R18 lesson is the structural analogy to C-48,
  which required the block body to enumerate both C-45 PARTIAL paths as distinct disqualifiers
  rather than a single merged condition: C-49 requires the block body to enumerate both C-48
  PARTIAL shapes individually -- V-88 (merged "or" condition) and V-89 (separate sentences,
  outcome language, no "disqualifier" framing) -- so a model producing either shape encounters
  it by name and knows what element (explicit "disqualifier" framing in separate sentences)
  would move it from C-49 PARTIAL to C-49 PASS. R18 also confirmed via V-90 that disqualifier-
  context-only naming (function names appearing exclusively in FAIL/PARTIAL disqualifier
  sentences, never in a positive duality assertion) satisfies C-47 and C-48 but fails C-45 --
  the orthogonal 470 path confirmed by correct prediction.
- C-50 codifies the C-45 FAIL shape enumeration requirement from R19: R19 confirmed that C-45
  FAIL has two structurally distinct shapes -- the zero-function-named shape (F1, the C-47
  shape) and the negative-framing-only shape (F2, the V-95 shape). V-95 had both function names
  present in the block body -- in C-47's FAIL disqualifier sentence ("a block body naming
  neither function fails") and in C-48's PARTIAL disqualifier sentences ("a block naming one
  without the other earns PARTIAL") -- yet earned C-45 FAIL because no affirmative duality
  declaration was present. The R19 lesson is the structural analogy to C-48 (which enumerated
  both C-45 PARTIAL paths because the block body's merged-condition left models unable to
  self-diagnose) and C-49 (which enumerated both C-48 PARTIAL shapes because V-89's
  separate-sentence form without "disqualifier" framing was invisible at rubric read time):
  C-50 requires the block body to enumerate both C-45 FAIL shapes, making F2's
  negative-framing-only pattern a named read-time prohibition rather than a scoring-time
  discovery. The R19 scorecard also confirmed that V-92/V-93/V-94 all produced existing
  C-49 PARTIAL shapes (S1-only, S2-only, merged-compound) without revealing new taxonomy
  gaps -- C-49's current enumeration covers the full C-48 PARTIAL shape space as observed
  across R18 and R19. R20 gap: the true zero-function-named variation (block body contains
  no function names, not even in disqualifier sentences -- failing C-47 while passing C-46)
  remains untested across R19.
