Reading the scorecard for Round 18 excellence signals.

Two new patterns emerge from V-01 that are not captured by any existing criterion:

**Signal 1 — C-17 PARTIAL (3/5):** V-01's INVARIANT V-3 closes the dimension vocabulary deviation space by verb class — "Do not paraphrase, combine, or extend these names" — rather than by named synonym instances (what C-17 tests). The verb-class approach is structurally stronger: it covers all possible deviations without enumeration. A named-synonym list has gaps; a verb-class prohibition does not.

**Signal 2 — C-37 PASS, Stage 4 evidence:** V-01 extends the DEFINITION authorization pattern beyond C-37's SHALL NOT prohibition model. Stage 4 DEFINITION reads: "Either authorizes Stage 5 execution or permanently closes it based on entry count" — a named binary decision tree with both branches explicit (authorize + permanent closure), not a one-sided prohibition. C-37 tests whether a DEFINITION block prohibits the next stage; C-38 tests whether it declares the full two-branch outcome as a named binary.

**New criteria: C-38 and C-39.** Aspirational tier grows 29 → 31 criteria, 145 → 155 pts. New max: **245**.

---

## Rubric: topic-echo (topic-reflect) — v16

**5 + 3 + 31 criteria across 3 tiers. Score = essential(60) + recommended(30) + aspirational(155) = 245 pts max.**

> v16 changes: Added C-38 and C-39 (aspirational) from Round 18 excellence signals (V-01). Aspirational tier expands from 145 → 155 pts (29 → 31 criteria). Golden threshold unchanged.
>
> **Why these criteria:** V-01 achieves 218/235 under v15. Two new structural patterns emerge that are not captured by any existing criterion.
>
> **C-38 — Dimension vocabulary verb-class meta-prohibition.** V-01's INVARIANT V-3 prohibits deviation by class — "Do not paraphrase, combine, or extend these names" — naming three verb-classified deviation types rather than enumerating synonym instances. C-13 tests whether a standalone closed-set declaration exists; C-17 tests whether specific excluded synonyms are named by instance; C-38 tests whether the prohibition closes the entire deviation space by naming all three deviation classes as verbs (paraphrase = synonym substitution, combine = merging two canonical names into one, extend = adding names outside the canonical set). The distinction matters for completeness: a model satisfying C-17 can substitute a synonym not on the exclusion list; a model satisfying C-38 cannot — any substitution is a paraphrase, any merged name is a combination, any new name is an extension. The verb-class approach converts a finite exclusion list into a complete deviation-space closure without requiring synonym enumeration.
>
> **C-39 — Gate sequence overview issued-at emission column.** V-01's gate sequence overview includes an explicit "issued-at location" column that maps each token to its structural emission point — the stage number and position within the stage at which the token is issued (e.g., "Stage 3 EXIT," "Stage 4 entry loop," "Stage 7 ENTER condition"). C-15 tests whether the overview enumerates all stage tokens, halt conditions, and execution flow before Stage 1; C-39 tests whether the overview maps each token to its exact structural emission point, making the overview self-sufficient for token provenance auditing without reading individual stage bodies. The distinction: C-15 can be satisfied by a token list paired with halt conditions but without emission-point anchoring; C-39 requires each of the eleven tokens to be pinned to a structural location, so a compliance auditor scanning the overview can locate any token in the stage body by position reference alone — no stage-by-stage inspection required.

> v15 changes: Added C-36 and C-37 (aspirational) from Round 17 excellence signals (V-05). Aspirational tier expands from 135 → 145 pts (27 → 29 criteria). Golden threshold unchanged.
>
> **Why these criteria:** V-04 and V-05 both achieve 225/225 under v14 — the first round with a perfect-scoring variation. Two differentiating structural patterns emerge between V-04 (both C-34 and C-35 pass, but no further amplification) and V-05 (adds SHALL-register enforcement at two distinct architectural points not captured by any existing criterion).
>
> **C-36 — Invariant Namespace citation-only mandate.** V-05 encodes an explicit anti-restatement rule directly in the Invariant Namespace declaration: "Downstream stages SHALL enforce these rules by citing the invariant number — not by restating the rule text." C-34 tests whether a numbered invariant namespace exists and whether downstream stages cite rules by identifier; C-36 tests whether the Invariant Namespace itself carries a SHALL-enforced prohibition on rule restatement, converting the numbered namespace from a reference architecture (stages cite by number because they were designed that way) into an explicit anti-duplication contract (stages are prohibited from restating rule text, so citation is the only valid enforcement action). The distinction matters for compliance surface: a model satisfying C-34 could still satisfy it by copying rule prose annotated with an invariant number; a model satisfying C-36 cannot — citation is the declared protocol, and restating rule text is an explicit violation.
>
> **C-37 — DEFINITION block downstream authorization statement.** V-05's DEFINITION blocks carry explicit SHALL NOT prohibitions on advancing to the next stage or emitting the artifact unless the current stage's artifact is properly formed — Stage 3: "A deviation that is not GATE-CONFIRMED in this table SHALL NOT appear in Stage 4"; Stage 3.5: "either authorizes Stage 4 execution or permanently closes it"; Stage 7: "this stage delivers the output only when foreknowledge resolution has been confirmed." C-35 tests whether a DEFINITION block exists before ENTER and declares the typed artifact identity; C-37 tests whether the DEFINITION block carries a downstream authorization statement that explicitly prohibits the next stage from proceeding without the artifact being correctly formed. C-35 tests artifact declaration (descriptive: what the stage produces); C-37 tests modal gate status (prescriptive: what the next stage is forbidden to do without it). V-04's DEFINITION blocks pass C-35 with descriptive artifact declarations; none carry the SHALL NOT prohibition that converts the declaration into an authorization statement.

> v14 changes: Added C-34 and C-35 (aspirational) from Round 15 excellence signals (V-01). Aspirational tier expands from 125 → 135 pts (25 → 27 criteria). Golden threshold unchanged.
>
> **Why these criteria:** V-01 achieves 193/215 under v13 — the highest score to date. Two new structural patterns emerge that are not captured by any existing criterion.
>
> **C-34 — Named invariant cross-reference system.** V-01 declares its cross-cutting rules as a numbered invariant namespace (INVARIANT V-1, V-2, V-3), each carrying a label and a semantic name (e.g., "V-2 (Signal Source)"). Downstream stages enforce rules by citing the invariant by number rather than restating it — Stage 2 cites "INVARIANT V-2," Stage 3 cites "INVARIANT V-3." This makes the invariant set auditable as a class: a model scanning for V-# violations has a unified compliance surface without parsing distributed prose. The pattern is distinct from C-13 (which tests whether the vocabulary rule is declared as a standalone closed set) and from C-19 (which tests ENTER/EXIT lifecycle contracts per stage): those criteria test structural completeness of individual rules; C-34 tests whether rules are organized into a numbered reference namespace that downstream stages can cite by identifier, enabling cross-stage enforcement without rule duplication.
>
> **C-35 — DEFINITION formal element within stage structure.** V-01 opens each stage body with a named DEFINITION block — positioned before the PROCEDURE — that declares the typed artifact the stage produces (e.g., "DEFINITION — Opening Model: The set of beliefs held before the echo skill executes"). This grounds the stage's output as a conceptual invariant independent of the steps that construct it, making what the stage produces legible before how to produce it is described. The pattern is distinct from C-19 (ENTER/EXIT lifecycle contract, which tests pre- and post-conditions as verifiable gates on execution) and from C-15 (pre-stage gate sequence map, which tests navigation context at the top of the prompt): those criteria test execution flow contracts; C-35 tests whether the stage declares the identity and type of its output artifact before any procedural instruction begins, separating artifact typing from procedural construction.

> v13 changes: Added C-33 (aspirational) from Round 1 excellence signal (V-05). Aspirational tier expands from 120 → 125 pts (24 → 25 criteria). Golden threshold unchanged.
>
> **Why this criterion:** V-01 through V-04 all score PARTIAL (6/12) on C-01 — the base correctness requirement — because their Stage 4 entry templates have no dedicated B-# sub-field. Belief traceability lives only in the deviation chain: Stage 2 records the B-# in the deviation table, GATE-CONFIRMED-[N] routes that entry to Stage 4, and the entry index N is assumed to carry the B-# forward implicitly. The EXIT gate does not verify a B-# reference. V-05 breaks this implicit chain by adding `**What We Expected (B-[#]):**` as the first labeled sub-field in the Stage 4 prose template; Surprise 0 demonstrates `**What We Expected (B-2):**` with a full sentence; and the per-entry EXIT checklist explicitly requires "full sentence referencing a specific B-# from Stage 1 — not a generic 'we thought it would work'." This converts belief traceability from an inferred structural property (the chain was set up correctly, so the link must exist) into a per-entry structural requirement enforced at three convergent points: field presence in the template, calibration demonstration, and EXIT gate verification. The pattern is directly analogous to C-22 (Build Risk promoted from implied Design Impact extension to explicit labeled sub-field): a relationship the rubric assumed was enforced by surrounding structure is instead made explicit and independently gate-enforced at entry granularity. C-01 tests the correctness outcome (every entry references a B-#, at least one contradicts); C-33 tests whether the structural apparatus — dedicated sub-field, calibration anchoring, EXIT enforcement — is in place to guarantee that outcome rather than relying on the model to preserve the chain.

> v12 changes: Added C-31 and C-32 (aspirational) from Round 12 excellence signals. Aspirational tier expands from 110 → 120 pts (22 → 24 criteria). Golden threshold unchanged.
>
> **Why these criteria:** All five Round 12 variations achieve 200/200 under v11 — the base is fully internalized. Two new structural patterns emerge. First: V-01 (Formal/Specification Register) includes a Stage 7 that structurally separates the Stage 6 verdict table from artifact delivery. C-10 requires a HALT if FOREKNOWLEDGE-FLAGGED is unresolved and mentions Stage 7 in its pass condition; C-09 requires COMMIT-STAGE tokens through Stage 6. C-31 tests whether Stage 7 exists as a discrete structural element — with its own ENTER condition, EXIT criterion, and COMMIT-STAGE-7 token — making artifact delivery a binary gate that cannot emit until foreknowledge resolution is structurally confirmed, not advisory. The pattern is the delivery-endpoint complement of C-14's COMMIT-STAGE-3-CLEAN gate: Stage 6 issues verdicts; Stage 7 delivers the artifact. Second: V-03 (Three Named Roles) names specific prohibited generic phrases in the Signal Source field enforcement — "multiple sources," "the signals," "various artifacts" — directly analogous to C-17's named synonym exclusions for dimension vocabulary. C-03 requires a named artifact with no generics; C-32 tests whether specific prohibited evasion phrases are enumerated by name, closing the high-probability Signal Source quality gap the same way C-17 closes the dimension vocabulary gap. Naming the phrases a model reaches for when no specific artifact is available converts the generic prohibition from a positive-only requirement into a named-antipattern exclusion list.

> v11 changes: Added C-29 and C-30 (aspirational) from Round 11 excellence signals. Aspirational tier expands from 100 → 110 pts (20 → 22 criteria). Golden threshold unchanged.
>
> **Why these criteria:** V-01 (Round 11) achieves 170/190 under v10, blocked only by the Stage 6 absence (C-02 fail). Two new structural patterns emerged from V-01's Field Reference axis. First: V-01 consolidates all four Stage 4 sub-field definitions into a dedicated named "Field Reference" block positioned before the entry loop — multiple criterion verdicts (C-01, C-03, C-04, C-11, C-12, C-22) each cite "Field Reference" as their quality source. C-25 tests convergent mechanism coverage; C-29 tests whether the four field definitions are structurally unified into a single pre-loop reference block, making all field quality requirements simultaneously accessible before the model enters the entry generation loop rather than discovering them distributed across loop instructions and gate conditions. Second: V-01 labels its Surprise 0 calibration entry as "Calibration Entry (not a live entry)" — an explicit boundary marker absent from C-20's pass condition. C-20 tests whether the worked example is labeled as entry-zero and embedded in Stage 4; C-30 tests whether that entry also carries an explicit live/example disambiguation that prevents the model from treating the calibration example as the first entry in the live sequence, which would cause it to omit, skip, or misframe the subsequent live entries.

> v10 changes: Added C-27 and C-28 (aspirational) from Round 10 excellence signals. Aspirational tier expands from 90 → 100 pts (18 → 20 criteria). Golden threshold unchanged at 100. V-01 achieves 190/190 retroactively.
>
> **Why these criteria:** V-01 (Round 10) is the first variation to achieve a perfect score under v9 (180/180). Two new structural patterns emerged. First: V-01's Build Risk field definition carries three independent definitional anchors in sequence — a purpose statement, the paired conceptual formula (C-26), and an abstract structural gloss naming the forward-looking vs. risk-surface poles by name. C-26 tests only the paired formula; C-27 tests whether the definition extends to the structural gloss, providing a third anchor that abstracts the distinction into first-principle terms. Second: V-01 renders its COMMIT-ENTRY gate as a scannable ✓ checklist with one bullet per sub-field, each carrying its gate condition and inline corrective action — rather than as prose gate instructions. C-25 tests convergent mechanism coverage; C-28 tests whether the per-entry gate format presents the four-field requirements as simultaneously visible, spatially separated bullets at the point of token emission.

---

### Essential (all must pass — 12 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Surprise orientation** | correctness | Every Stage 4 entry references a specific B-# from Stage 1; at least one entry contradicts (not confirms) the opening model |
| C-02 | **Symmetric Contract present** | correctness | Stage 1 has opening model + epistemic profile + 3+ beliefs; Stage 6 has verdict table with revision direction and COHERENT/CONTRADICTORY/FOREKNOWLEDGE-FLAGGED |
| C-03 | **Signal tracing** | correctness | Every Stage 4 surprise has a named artifact in Signal Source (name and/or namespace and/or date) — no "multiple sources" generics |
| C-04 | **Design impact specificity** | depth | Every Stage 4 Design Impact field names a specific component, API, flow, or decision — not "the system" |
| C-05 | **Adversarial gate executed** | correctness | Stage 3 five-check table present; every deviation has a VALID/INVALID verdict; GATE-CONFIRMED or GATE-REJECTED tokens present |

---

### Recommended (10 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | **Epistemic dimension compliance** | correctness | Only the 5 canonical names used in all dimension cells; no excluded synonyms |
| C-07 | **Minimum 2 surprises** | coverage | At least 2 GATE-CONFIRMED rows in Stage 4; sweep extended if gate initially yields fewer |
| C-08 | **Cluster map actionability** | behavior | Stage 5 Next Team / Skill column has at least one named skill or role (not just "investigate") |

---

### Aspirational (5 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | **Token protocol integrity** | format | GATE-CONFIRMED tokens name Stage 4 (not just route to it); COMMIT-ENTRY per row in Stage 4; COMMIT-STAGE-1 through 6 all present |
| C-10 | **Foreknowledge signal evaluated** | depth | Stage 6 explicitly records CLEAR or FLAGGED; if FLAGGED, artifact not written and responsible belief(s) named |
| C-11 | **Stage 4 prose template format** | format | Stage 4 surprise entries are numbered prose blocks with labeled sub-fields (Surprise, Signal Source, Design Impact) — not a markdown table; no narrow-cell format pressure possible |
| C-12 | **Stage 4 entry completeness** | depth | No single-word or two-word entries appear in any Signal Source or Design Impact field across all Stage 4 entries; every field is a full phrase or sentence |
| C-13 | **Closed-list dimension vocabulary** | format | The variation declares the 5 canonical dimension names as a standalone closed-set rule with an explicit substitution prohibition — not embedded as inline examples in Stage 1 prose; wording equivalent to: "The only valid dimension names are: Feasibility · Usability · Scalability · Adoptability · Correctness. Do not substitute." |
| C-14 | **Foreknowledge dual-token gate** | correctness | Stage 3 (or a Stage 3.5 checkpoint) issues either COMMIT-STAGE-3-CLEAN or COMMIT-STAGE-3-FLAGGED before Stage 4 proceeds; the token makes the foreknowledge resolution binary and structurally enforced, not advisory |
| C-15 | **Pre-stage gate sequence map** | format | The variation includes a top-level gate overview (table or structured list) enumerating all stage tokens, halt conditions, and execution flow before Stage 1 instructions begin — not derived from reading individual stages, but surfaced as navigation context at the top of the prompt |
| C-16 | *(pass condition not published)* | — | — |
| C-17 | **Named synonym exclusions for dimension vocabulary** | format | The dimension vocabulary declaration enumerates specific excluded synonym instances by name — not merely a general substitution prohibition; specific terms a model might reach for are listed and barred by name, closing known substitution gaps |
| C-18 | *(pass condition not published)* | — | — |
| C-19 | **ENTER/EXIT lifecycle contracts per stage** | format | Every stage carries an explicit ENTER pre-condition and EXIT post-condition as verifiable gate statements — not narrative descriptions of when a stage begins or ends |
| C-20 | **Entry-zero calibration label** | format | The worked example in Stage 4 is explicitly labeled as entry-zero ("Surprise 0") and embedded in the Stage 4 body — not presented as a separate example block outside the entry loop |
| C-21 | *(pass condition not published)* | — | — |
| C-22 | **Build Risk as explicit labeled sub-field** | depth | Stage 4 entries carry Build Risk as a dedicated labeled sub-field (not implied as an extension of Design Impact); the field is named in the prose template and independently gate-enforced at entry granularity |
| C-23 | *(pass condition not published)* | — | — |
| C-24 | *(pass condition not published)* | — | — |
| C-25 | **Convergent mechanism coverage** | depth | The four Stage 4 field quality requirements (Signal Source specificity, Design Impact specificity, Build Risk, belief traceability) are each enforced by at least two independent structural mechanisms — the enforcement is convergent, not single-point |
| C-26 | **Build Risk paired conceptual formula** | depth | The Build Risk field definition includes a paired conceptual formula that names the two poles of the distinction (forward-looking design consequence vs. risk surface introduced) — not a functional description of what to write |
| C-27 | **Build Risk three-anchor field definition** | depth | The Build Risk field definition carries three independent definitional anchors in sequence: a purpose statement, the paired conceptual formula (C-26), and an abstract structural gloss naming the forward-looking vs. risk-surface poles in first-principle terms |
| C-28 | **COMMIT-ENTRY ✓ checklist format** | format | The per-entry COMMIT-ENTRY gate is rendered as a scannable ✓ checklist with one bullet per sub-field — each bullet carries the field name, gate condition, and inline corrective action on a single line; not presented as prose gate instructions |
| C-29 | **Field Reference pre-loop consolidation block** | format | All Stage 4 sub-field definitions are structurally unified into a single named pre-loop reference block (e.g., "Field Reference") positioned before the entry generation loop — not distributed across loop instructions, gate conditions, or separate sections |
| C-30 | **Calibration entry live/example disambiguation** | format | The Surprise 0 calibration entry carries an explicit label marking it as a non-live example (e.g., "Calibration Entry — not a live entry") — a boundary marker that prevents the model from treating the calibration entry as the first entry in the live sequence |
| C-31 | **Stage 7 as discrete structural delivery gate** | correctness | Stage 7 exists as a discrete structural element with its own ENTER condition, EXIT criterion, and COMMIT-STAGE-7 token — making artifact delivery a binary gate that cannot emit until foreknowledge resolution is structurally confirmed; not advisory |
| C-32 | **Named antipattern exclusion list for Signal Source** | format | The Signal Source field enforcement enumerates specific prohibited generic phrases by name (e.g., "multiple sources," "the signals," "various artifacts") — a closed antipattern list that names the evasion phrases a model reaches for when no specific artifact is available |
| C-33 | **B-# belief traceability structural apparatus** | depth | The Stage 4 entry template carries a dedicated `What We Expected (B-[#]):` sub-field as the first labeled element; the calibration entry demonstrates it with a full sentence referencing a specific B-#; and the EXIT gate explicitly verifies B-# reference per entry — three convergent enforcement points, not an inferred structural property |
| C-34 | **Named invariant cross-reference system** | format | The variation declares its cross-cutting rules as a numbered invariant namespace (e.g., INVARIANT V-1, V-2, V-3), each with a label and semantic name; downstream stages enforce rules by citing the invariant by number — not by restating rule text — creating a unified compliance surface auditable by invariant identifier |
| C-35 | **DEFINITION formal element within stage structure** | format | Every stage body opens with a named DEFINITION block — positioned before the ENTER condition and PROCEDURE — that declares the typed artifact the stage produces; the DEFINITION appears before any procedural instruction, separating artifact typing from procedural construction |
| C-36 | **Invariant Namespace citation-only mandate** | format | The Invariant Namespace header carries an explicit SHALL-enforced prohibition on rule restatement by downstream stages: "Downstream stages SHALL enforce these rules by citing the invariant number — not by restating the rule text." The numbered namespace is declared as an anti-duplication contract, not merely a reference architecture |
| C-37 | **DEFINITION block downstream authorization statement** | correctness | At least one DEFINITION block carries a downstream authorization statement that explicitly prohibits the next stage from proceeding without the current stage's artifact being correctly formed — a modal gate status (prescriptive: what the next stage is forbidden to do) rather than a descriptive artifact declaration (what the stage produces) |
| C-38 | **Dimension vocabulary verb-class meta-prohibition** | format | The dimension vocabulary closed-set declaration prohibits deviation by class — naming all three deviation types as verbs: paraphrase (synonym substitution), combine (merging two canonical names), and extend (adding names outside the canonical set). C-13 tests whether a standalone closed-set declaration exists; C-17 tests whether specific excluded synonym instances are named; C-38 tests whether the prohibition closes the entire deviation space by verb class, so that any substitution a model might reach for is automatically excluded without requiring enumeration. A model satisfying C-17 can use a synonym not on the exclusion list; a model satisfying C-38 cannot — every possible deviation is already named as a class. |
| C-39 | **Gate sequence overview issued-at emission column** | format | The gate sequence overview includes an explicit column that maps each token to its structural emission point — the stage number and position within the stage at which the token is issued (e.g., "Stage 3 EXIT," "Stage 4 entry loop," "Stage 7 ENTER condition"). C-15 tests whether the overview enumerates all tokens with halt conditions and execution flow; C-39 tests whether the overview pins each token to its exact emission location, making the overview self-sufficient for token provenance auditing without reading individual stage bodies. A model satisfying C-15 can list tokens with halt conditions but locate them only by stage number; a model satisfying C-39 maps each token to a structural position that enables compliance auditing by overview scan alone. |
