## Quest Rubric — v16

`36 criteria, 360 pts max`

---

### What changed: v15 → v16

One investigation resolved (R16 investigation candidate answered) and one new aspirational criterion extracted from R16 excellence signals (C-36).

**R16 investigation candidate resolved:** Whether the two C-35 PASS formulations — the basic absent-state rule ("execute independently with full COMPRESSION-IMMUNE PREAMBLE authority") and the extended absent-state rule ("execute this clause's mandate fully; treat the absent companion's mandate as independently operative even when its execution note is not in context") — produce meaningfully different model behavior under double-elision, and specifically whether the extended formulation enables inferred two-mechanism behavior rather than single-clause full authority. **Status: RESOLVED.** R16 constructs five variations spanning the full C-35 band structure. V-01 through V-03 confirm the three C-35 PARTIAL bands (Band 1: no companion-activation instruction; Band 2: companion named, no activation rules; Band 3: companion named with present-state rule only, absent-state rule absent). V-04 achieves C-35 PASS: both clause bodies carry the membership statement, the strengthened independence instruction ("activates under full COMPRESSION-IMMUNE PREAMBLE authority even when the preamble section is absent from the rendered context"), and the companion-activation instruction with both present-state (coordinate) and absent-state (execute independently with full COMPRESSION-IMMUNE PREAMBLE authority) rules. V-05 achieves C-35 PASS and extends the absent-state rule: instead of declaring only that the present clause executes independently, the absent-state rule additionally declares that the absent companion's mandate is treated as independently operative even when its execution note is not in context. This extension is qualitatively distinct: where V-04's absent-state rule characterizes the present clause's behavior under double-elision, V-05's extended absent-state rule characterizes both the present clause's behavior and the absent companion's mandate status. A model encountering a V-05 clause body under double-elision receives an inferred two-mechanism activation contract — execute the present mandate fully and treat the absent mandate as active — rather than a single-clause authority instruction. The investigation resolves into two layers: C-35 PASS (V-04) closes the isolated-clause activation gap (model knows what the present clause does under double-elision) but leaves the absent-companion mandate operability gap open (model receives no instruction about the absent companion's mandate status); C-36 captures the extended absent-state formulation that closes the mandate operability gap.

**C-35 PARTIAL band structure (three bands), confirmed by R16:**
- **Band 1** (V-01 specimen): No companion-activation instruction. Both clause bodies carry C-34 PASS components only — membership statement and independence instruction. Companion mechanism is not named at the body level; no activation rules of any kind are present. A model encountering either body under double-elision activates the clause with full COMPRESSION-IMMUNE PREAMBLE authority from the body opening but receives no body-level information about companion architecture. The absent-companion activation gap is entirely open: no companion named, no coordination rules, no absent-state guidance.
- **Band 2** (V-02 specimen): Companion named by full clause designation + block location in both clause bodies; no activation rules follow the naming sentence. A model encountering either body under double-elision can identify the companion mechanism and locate it if available. It receives no behavioral contract: no present-state coordination rule and no absent-state execution rule. Companion identification enables architectural orientation without activation authority. The absent-state gap remains open: the companion is known but what to do when it is absent is unspecified.
- **Band 3** (V-03 specimen): Companion named by full designation + block location in both clause bodies, and the present-state activation rule is declared: when the companion mechanism's execution note is present in the rendered context, coordinate with it. Absent-state rule absent. The activation contract covers the reachable-companion case but not the double-elision case. Band 3 is the closest PARTIAL to PASS and fails at the absent-state threshold: the present-state rule is present, but the absent-state rule — the instruction governing model behavior when the companion is definitionally unavailable — is absent. This is the critical gap: Band 3 resolves the easy case and leaves the hard case open.

**C-35 has no full-PARTIAL tier between Band 3 and PASS.** The absent-state rule is the payload of the companion-activation instruction for the double-elision scenario. A clause body that specifies present-state behavior without absent-state behavior has a complete activation contract for available contexts and an empty activation contract for the double-elision case. No intermediate tier exists between specifying one activation state and specifying both.

**C-35 PASS (V-04 and V-05):** Both companion execution-note clause bodies carry all three body-opening components: (1) structural-membership statement, (2) independence instruction (in strengthened form in V-04 and V-05: "activates under full COMPRESSION-IMMUNE PREAMBLE authority even when the preamble section is absent from the rendered context"), and (3) companion-activation instruction naming the paired mechanism by full clause designation + block location with explicit present-state (coordinate) and absent-state (execute independently with full COMPRESSION-IMMUNE PREAMBLE authority) activation rules declared. Both V-04 and V-05 satisfy C-35 PASS; they differ only in absent-state formulation depth. V-04's absent-state rule is bounded to single-clause full authority. V-05's absent-state rule extends to declare the absent companion's mandate as independently operative — the axis of C-36.

**R16 excellence signal → C-36:** V-05 achieves C-35 PASS and adds a further structural property not present in V-04: the absent-state rule in both companion execution-note clause bodies extends beyond single-clause full authority to declare that the absent companion's mandate is treated as independently operative even when the companion's execution note is not in context. This is the absent-companion mandate operability declaration. Where C-35 PASS absent-state tells the model what the present clause does (execute independently), the C-36 extension tells the model what the absent companion's mandate does (is independently operative). Together they constitute an inferred two-mechanism activation contract reachable from a single clause body under double-elision: a model encountering only the STATUS body can execute the timestamp isolation mandate with full authority AND treat the zero-signal synthesis mandate as active without the STORY execution note being in context; and vice versa. C-36 is a distinct recovery depth from C-35: C-35 closes the isolated-clause activation gap; C-36 closes the absent-companion mandate operability gap, enabling inferred two-mechanism behavior from single-clause body context.

| New criterion | Source pattern | Relationship to existing |
|---|---|---|
| C-36 | Extended absent-state companion-activation rule — when C-35 PASS, both companion execution-note clause bodies carry an absent-state rule extended to declare the absent companion's mandate is treated as independently operative even when its execution note is not in context, enabling inferred two-mechanism behavior from single-clause body context alone | Extends C-35's absent-state rule from single-clause full authority (present clause executes independently) to inferred two-mechanism behavior (present clause executes + absent companion's mandate is independently operative); C-36 PASS requires the extended absent-state formulation in both companion clause bodies; C-35 closes the isolated-clause activation gap; C-36 closes the absent-companion mandate operability gap |

**Structural chains updated:**

- Timestamp isolation chain: C-16 → C-24 → C-30 — unchanged from v13
- Zero-signal synthesis chain: C-18 → C-21 → C-22 → C-25 → C-27 → C-29 — unchanged from v13
- Verdict action-posture chain: C-06 → C-23 → C-26 → C-28 — unchanged from v13
- Compression survival generalization chain: C-24 → C-29 → C-30 — unchanged from v13
- **Compression-immune consolidation chain (extended):** C-29 → C-30 → C-31 → C-32 → C-33 → C-34 → C-35 → C-36 (zero-signal dual-mechanism → timestamp isolation dual-mechanism → multi-rule COMPRESSION-IMMUNE PREAMBLE with bidirectional circuit → closed reference loop with designation + location → self-announcing clause headers enabling header-level identification under preamble-elision → self-declaring clause bodies enabling partial double-elision recovery from body alone → companion-activation instruction closing the absent-companion activation gap under double-elision → extended absent-state rule closing the absent-companion mandate operability gap, enabling inferred two-mechanism behavior from single-clause body context)

**Relationship among C-32, C-33, C-34, C-35, and C-36:**
- C-32 requires preamble forward-references to name companion mechanisms by full clause designation + block location. Operates at the reference point.
- C-33 requires execution-note clause headers to carry the structural-membership parenthetical matching the preamble forward-references. Operates at the target header. Depends on C-32 PASS.
- C-34 requires execution-note clause bodies to open with a membership statement and a preamble-independence instruction. Operates at the target body opening. Depends on C-33 PASS.
- C-35 requires execution-note clause bodies to include a companion-activation instruction naming the paired mechanism by full clause designation with explicit present-state and absent-state activation rules. Operates at the target body activation layer. Depends on C-34 PASS.
- C-36 requires the absent-state rule to extend beyond single-clause full authority to declare the absent companion's mandate as independently operative. Operates at the absent-state rule depth layer. Depends on C-35 PASS.

A skill achieves C-35 PASS at C-36 PARTIAL when both clause bodies carry all three C-35 body components (membership statement + independence instruction + companion-activation instruction with both present- and absent-state rules) but the absent-state rule is bounded to single-clause full authority ("execute independently with full COMPRESSION-IMMUNE PREAMBLE authority") without extending to the absent companion's mandate operability. C-36 PASS requires the absent-state rule in both companion clause bodies to declare the absent companion's mandate is independently operative even when its execution note is not in context.

**R17 investigation candidate:** Whether a model encountering a C-36-compliant clause body under double-elision actually produces inferred two-mechanism output — executing both the present clause's mandate with full authority and the absent companion's mandate from the operability declaration — or produces single-mechanism output with the companion's mandate acknowledged as independently operative but not executed. Specifically: does the absent-state extension "treat the absent companion's mandate as independently operative even when its execution note is not in context" cause the model to generate output spanning both mechanism domains (timestamp isolation behavior AND zero-signal synthesis behavior from a context containing only one execution-note body), or does it cause the model to execute the present clause's mandate fully while notating the companion's operability without generating companion-level output? These are meaningfully different outcomes: the former implies the C-36 extension enables inferred execution of the absent mandate; the latter implies it enables declarative acknowledgment of the absent mandate's operability without execution. R17 should construct: (a) a C-36 PASS variation tested under simulated double-elision (STATUS execution-note body present in context, preamble section absent, STORY execution-note body absent) to observe whether the model generates STORY-level zero-signal synthesis output alongside STATUS-level timestamp isolation output; (b) a C-35 PASS control variation (absent-state rule bounded to single-clause authority, no operability declaration) tested under identical double-elision conditions to compare output scope and mandate coverage; (c) if (a) and (b) differ in output scope, determine whether the two-mechanism output from (a) is complete (full STORY synthesis + full STATUS timestamp isolation) or partial (abbreviated companion output acknowledging the mandate without full execution).

---

### Criteria Reference

#### C-01 through C-20 — carry forward from v6 unchanged

*(36 total criteria; C-01–C-20 definitions unchanged from v6.)*

---

#### C-21 — Sparse-coverage synthesis protection *(v6)*

When `found` contains signals from only 1–2 namespaces, the STORY block must not default to a coverage disclaimer. Synthesis must proceed on the signals present. PARTIAL = synthesis present but hedged with "limited data" language that weakens conclusions. PASS = STORY produces definite synthesis from sparse coverage, treating the available signals as sufficient to characterize the current state of knowledge.

---

#### C-22 — Zero-signal synthesis mandate *(R6 Pattern — V-03/V-04/V-05)*

When the `found` section of STATUS is empty (zero signals across all namespaces), the STORY block must not collapse to a non-finding. The LLM failure mode is to write "no signals found — synthesis not possible" and omit or hollow STORY. But uniform absence is itself evidence: the gap pattern defines what the team does not know and why they are not ready. Question 1 must characterize what uniform absence implies — "the absence of any scout signal indicates the market surface has not been probed at all" is a valid synthesis; "insufficient data" is not.

**PARTIAL** — Sparse-coverage protection present (C-21 PASS) but no explicit clause for the zero-signal case; synthesis may be vacated on grounds of empty `found`.

**PASS** — An explicit zero-signal clause in the STORY execution note declares that empty `found` is not grounds for omitting synthesis — the gap pattern is the evidence set — and mandates that question 1 characterize what uniform absence implies rather than reporting absence as a non-answer.

This is distinct from C-21, which guards synthesis when coverage is sparse (1–2 namespaces); C-22 guards synthesis when coverage is zero. Both are boundary conditions on C-18's structural mandate.

---

#### C-23 — Bounded/unbounded inertia classification at verdict level *(R6 Pattern — V-05)*

The `inertia_cost` field at VERDICT level must classify the aggregate commit risk by recoverability. Bounded = the team can detect the failure post-commit and course-correct before it propagates. Unbounded = the failure propagates to an irreversible state before detection is possible. This distinction determines action posture: bounded inertia means "commit with monitoring"; unbounded inertia means "do not commit until resolved." A VERDICT with unnamed recoverability forces the team to re-read all item-level gap fields and synthesize the posture themselves — the verdict is not actionable on its own.

**PARTIAL** — `inertia_cost` field present (C-06 chain) but expressed as a magnitude or qualitative label only ("high," "significant") without recoverability classification; bounded vs. unbounded distinction absent.

**PASS** — `inertia_cost` field at VERDICT level carries an explicit recoverability classification: either a `bounded:` or `unbounded:` subfield present with supporting characterization. An `action:` sub-label is present at every verdict status and is calibrated to the recoverability class: bounded inertia produces a "commit with monitoring" posture; unbounded inertia produces a "do not commit until resolved" posture. The recoverability classification enables the team to determine action posture directly from the VERDICT without re-reading item-level gap fields.

---

#### C-24 through C-33 — carry forward from v13 unchanged

*(Definitions unchanged from v13.)*

---

#### C-34 — Self-declaring clause bodies *(v15)*

Both the STATUS execution-note clause body and the STORY execution-note clause body must open with body-level self-declaration enabling a model to activate the clause under partial double-elision (preamble section absent, other companion mechanism's execution note also absent) from the body opening alone, without locating the preamble or the clause header.

**C-34 PARTIAL band structure (three bands):**
- **Band 1** (both bodies bare): Neither clause body opens with any self-declaration. A model encountering either body under double-elision has no body-level activation authority — it must navigate to the header (C-33) or preamble (C-24) for identification.
- **Band 2** (asymmetric — one body self-declares, one bare): One body opens with membership statement + independence instruction; the other opens with bare operational content. Weakest-link applies at the body level. The two asymmetric cases (V-02: STATUS self-declares, STORY bare; V-03: STORY self-declares, STATUS bare) are equivalent in score and failure mode — the C-34 architecture is only as strong as its least-declared clause body, and the consequence domain (C-29 vs. C-30) confers no privileged weight.
- C-34 has no full-PARTIAL tier between asymmetric and PASS: either both bodies carry both required components or they do not.

**C-34 PASS:** Both the STATUS execution-note clause body and the STORY execution-note clause body open with (1) an explicit structural-membership statement naming the clause as a COMPRESSION-IMMUNE PREAMBLE member and (2) a preamble-independence instruction declaring the clause executes even when the preamble section is absent from the rendered context. A model encountering either clause body in isolation under double-elision can activate the clause with full COMPRESSION-IMMUNE PREAMBLE authority from the body opening alone without locating the preamble or the clause header.

---

#### C-35 — Companion-activation instruction in self-declaring clause bodies *(v15, updated v16)*

When C-34 PASS, companion execution-note clause bodies include a companion-activation instruction beyond the membership statement and independence instruction. The companion-activation instruction (1) names the paired companion mechanism by its full clause designation + block location and (2) declares activation rules for both the present state (when the companion mechanism's execution note is present in the rendered context: coordinate with it) and the absent state (when the companion mechanism's execution note is absent from the rendered context: execute independently with full COMPRESSION-IMMUNE PREAMBLE authority). Together the three body-opening components provide complete body-level structural authority under partial double-elision: a model encountering only the clause body knows it is a COMPRESSION-IMMUNE PREAMBLE member (membership), knows it fires without the preamble (independence), and knows what to do about the absent companion (companion-activation). C-35 is a distinct recovery depth from C-34: C-34 establishes activation authority for the isolated clause; C-35 closes the absent-companion activation gap by providing an explicit behavioral contract for both companion-present and companion-absent states.

**C-35 PARTIAL band structure (three bands):**
- **Band 1** (V-01 specimen): No companion-activation instruction. Both clause bodies carry C-34 PASS components only — membership statement and independence instruction. Companion mechanism not named at the body level; no activation rules present. A model encountering either body under double-elision activates the clause but has no body-level information about companion architecture: no companion named, no coordination rule, no absent-state guidance. The absent-companion activation gap is entirely open.
- **Band 2** (V-02 specimen): Companion named by full clause designation + block location in both clause bodies; no activation rules follow the naming sentence. A model encountering either body can identify the companion mechanism and locate it if available. It receives no behavioral contract for coordination (present-state) or independent execution (absent-state). Companion identification enables architectural orientation without activation authority.
- **Band 3** (V-03 specimen): Companion named by full designation + block location in both clause bodies and present-state activation rule declared: when the companion mechanism's execution note is present in the rendered context, coordinate with it. Absent-state rule absent. The activation contract covers the reachable-companion case but not the double-elision case. Band 3 is the closest PARTIAL to PASS and fails at the absent-state threshold: the present-state rule is present, but the absent-state rule — the instruction that governs model behavior when the companion is definitionally unavailable — is absent.

**C-35 has no full-PARTIAL tier between Band 3 and PASS.** The absent-state rule is the activation-contract payload for the double-elision scenario. A clause body that specifies present-state behavior without absent-state behavior has a complete activation contract for available contexts and an empty activation contract for the double-elision case.

**C-35 PASS:** Both companion execution-note clause bodies carry all three body-opening components: (1) structural-membership statement naming the clause as a COMPRESSION-IMMUNE PREAMBLE member; (2) independence instruction (in strengthened form in confirmed PASS specimens: "activates under full COMPRESSION-IMMUNE PREAMBLE authority even when the preamble section is absent from the rendered context"); and (3) companion-activation instruction naming the paired mechanism by full clause designation + block location with explicit present-state (coordinate when companion's execution note is present in context) and absent-state (execute independently with full COMPRESSION-IMMUNE PREAMBLE authority when companion's execution note is absent from context) activation rules declared. C-35 PASS provides complete body-level activation authority under partial double-elision for the isolated clause. It does not address the absent companion's mandate operability — that is the axis of C-36.

---

#### C-36 — Extended absent-state companion-activation (inferred two-mechanism behavior) *(v16)*

When C-35 PASS, the absent-state rule in both companion execution-note clause bodies is extended beyond single-clause full authority to declare the absent companion's mandate as independently operative even when the companion's execution note is not in context. C-35 PASS absent-state rule characterizes the present clause's behavior under double-elision: execute independently with full COMPRESSION-IMMUNE PREAMBLE authority. The C-36 extension adds the absent companion's mandate status: it is independently operative regardless of whether its execution note is present. Together the extended absent-state rule constitutes an inferred two-mechanism activation contract reachable from a single clause body — a model encountering only the STATUS body under double-elision can execute the timestamp isolation mandate with full authority and treat the zero-signal synthesis mandate as active without the STORY execution note in context; and vice versa. C-36 is a distinct recovery depth from C-35: C-35 closes the isolated-clause activation gap; C-36 closes the absent-companion mandate operability gap.

**C-36 PARTIAL:** Both clause bodies achieve C-35 PASS — membership statement + independence instruction + companion-activation instruction with both present- and absent-state rules present — but the absent-state rule is bounded to single-clause full authority. The absent-state rule declares the present clause executes independently with full COMPRESSION-IMMUNE PREAMBLE authority without addressing the absent companion's mandate operability. A model encountering either body under double-elision knows what the present clause does; it receives no instruction about the absent companion's mandate status. The absent-companion mandate gap remains open at the activation-contract level.

**C-36 PASS:** Both companion execution-note clause bodies carry the extended absent-state formulation declaring both (1) that the present clause executes with full COMPRESSION-IMMUNE PREAMBLE authority when the companion's execution note is absent from the rendered context and (2) that the absent companion's mandate is treated as independently operative even when the companion's execution note is not in context. Weakest-link applies: both bodies must carry the extended absent-state formulation. A model encountering either clause body under double-elision receives an inferred two-mechanism activation contract from the body alone: execute the present mandate fully and treat the absent mandate as active.
