Looking at the R12 scorecard, I need to identify what genuinely new structural patterns the evidence reveals beyond v11's C-01–C-39.

**New patterns from R12:**

**V-02 (C-36 fully expanded)** introduces two structural specifics not named in any existing criterion:
- `"Role B Gap Record [UNCONDITIONAL] with structured field template"` — C-32/C-33/C-34 mandate the Gap Record exists; none specify its internal field structure. Named fields make Gap Records machine-comparable across phases.
- `"Pass 1 with systematic cross-reference scan instructions per cell type"` — C-36 mandates Phase 3E exists; it doesn't require entity-type-stratified scanning within it. A sweep that finds S-ID undeclared references can still completely skip OP-ID or INV-ID checking.

These are **C-40** and **C-41**. Point value: 10 / 33 = 0.303 pts each (aspirational bucket grows from 31 to 33 criteria).

---

```markdown
# trace-state Rubric v12
**Updated**: 2026-03-15
**Change**: Added C-40, C-41 from R12 excellence signals (Gap Record structured
field template making Gap Records machine-comparable across phases; Phase 3E
entity-type-stratified scan requiring distinct sweep rows per reference class so
a complete absence of OP-ID or INV-ID checking cannot be masked by S-ID findings).
Aspirational point value updated: 10 / 33 = 0.303 pts each.

---

## Design Rationale

**Essential (60%)** — the five things that make a trace-state output useful at all:
- C-01: before/after state for every operation (the whole point of the skill)
- C-02: preconditions + postconditions per operation (not just "what happened" but "why it was allowed")
- C-03: invariants named (the thing a state machine analysis is really hunting for)
- C-04: at least one anomaly identified (the deliverable — no finding = no signal)
- C-05: domain grounding (Sales/CS/Finance vocabulary — otherwise it's a toy example)

**Recommended (30%)** — what separates a useful trace from a great one:
- C-06: negative path (attempted invalid transition + why it's rejected)
- C-07: numbered step-by-step format (reader can replay the trace mechanically)
- C-08: race condition scenario (the hardest anomaly type, worth its own criterion)

**Aspirational (10%)** — raises the bar once the basics are stable:
- C-09: all four anomaly types covered (complete signal, not just the easy ones)
- C-10: structured notation / transition table (enables mechanical verification, not just prose review)
- C-11 through C-41: see full list below (accumulated from R1–R12 excellence signals)

---

## Aspirational Criteria (Full List)

- **C-09**: all four anomaly types covered (complete signal, not just the easy ones)
- **C-10**: structured notation / transition table (enables mechanical verification, not just prose review)
- **C-11**: sweep table with mandatory row-level verdicts per anomaly type (R1 excellence signal)
- **C-12**: Op ID cross-referencing between declaration and trace (R1 excellence signal)
- **C-13**: Entry Condition column in state machine table (R1 excellence signal)
- **C-14**: minimum-found threshold on sweep table — converts C-11 from coverage gate to depth gate (R2 excellence signal)
- **C-15**: full ID ecosystem — S-IDs, OP-IDs, INV-IDs all declared and cross-referenced (R2 excellence signal)
- **C-16**: undeclared reference elevated to named fifth anomaly class with its own sweep row (R2 excellence signal)
- **C-17**: anomaly register with separate OP-ID / S-ID / INV-ID columns — blank cell in any found-verdict row is a detectable mechanical gap (R2 excellence signal)
- **C-18**: pre-sweep hypothesis with dual-pass ANOMALY FINDER — falsifiable prediction record (R3 excellence signal)
- **C-19**: evidence-strength quality gate on sweep table — 1/2/3 scale with ≥1 strength ≥2 requirement (R3 excellence signal)
- **C-20**: numeric/temporal invariant gate — domain expert must produce at least one falsifiable numeric or temporal invariant before analysis begins (R4 excellence signal)
- **C-21**: Surprise column in reconciliation table — each prediction outcome labeled expected/surprising, producing a calibration signal beyond binary match/mismatch (R4 excellence signal)
- **C-22**: score-at-point-of-discovery instruction — evidence strength must be assigned when the finding is first recorded, not retroactively after the full sweep is complete (R4 excellence signal)
- **C-23**: score-aloud verbal protocol — C-22 expressed as a named cognitive behavioral discipline with an explicit self-correction checkpoint, not a passive table constraint or end-of-sweep reminder (R5 excellence signal)
- **C-24**: phase exit gate checkboxes — C-20, C-21, and C-22 appear as explicit hard completion gates per phase; model must affirmatively check each gate before advancing, preventing optional-boilerplate treatment (R5 excellence signal)
- **C-25**: three-value surprise taxonomy with calibration score — reconciliation table uses C/FP/FN classification (Correct, False Positive, False Negative) instead of binary Expected/Surprising, plus a computed calibration score with a stated threshold that triggers structural diagnosis when accuracy falls below it (R5 excellence signal)
- **C-26**: anomaly-type-as-top-level-phase with sequential commitment — each of the four anomaly types becomes a numbered top-level phase (not a subsection within a shared sweep part), each with its own exit gate; sequential phase ordering forces linear commitment so the model cannot advance to the next type until the current phase is complete (R6 excellence signal)
- **C-27**: prejudice-dismissal naming protocol — acquittal ("no finding") requires explicitly naming the specific trace steps, state conditions, or evidence that would need to exist to produce the finding; transforms absence records into structural gap maps rather than bare "None" declarations or generic written acquittals (R6 excellence signal)
- **C-28**: dual-mode doc-vs-trace detection — the anomaly sweep runs in two independent passes: one against the declaration tables alone, and one diffing declared behavior against the traced step sequence; a claim appearing in the documentation that is not demonstrated in the trace is itself a finding; adds a second detection path to every anomaly type (R6 excellence signal)
- **C-29**: acquittal-advocate sub-role with activation gate — a dedicated named sub-role (e.g., "Role B: Acquittal Advocate") that activates only when an anomaly-type phase reaches a "no finding" verdict; the sub-role must produce a Gap Record before the phase exit gate opens and cannot be bypassed; converts C-27's naming protocol from a passive constraint into an active role-gated enforcement mechanism where the gate itself is the acquittal record (R7 excellence signal)
- **C-30**: parallel dual-mode verdict columns with inline gap records — C-28's two detection passes expressed as two separate named columns per anomaly-type row in the sweep table (e.g., `Declaration-Only Finding` and `Trace-Diff Finding`); a "None" entry in either column requires an inline gap record in that cell, not a separate section; makes both detection paths independently auditable and transforms a procedural instruction into a mechanical table constraint (R7 excellence signal)
- **C-31**: phase LOCKED/OPEN status with explicit COMPLETE declaration — each phase carries an explicit LOCKED/OPEN status label; Phase N remains locked until Phase N-1 emits a named "PHASE N-1: COMPLETE" declaration as the unlock signal; strengthens C-26's sequential commitment from a behavioral progression rule into a mechanical lock state where advancement requires a required named artifact, not merely finishing the work (R7 excellence signal)
- **C-32**: unconditional gap documentation per phase — Role B fires on every phase regardless of finding count; the conditional "activates on no-finding" trigger from C-29 is removed; even phases with findings must produce a Gap Record documenting what was not found; closes the loophole where a single weak finding bypasses gap documentation for everything else that was not detected (R8 excellence signal)
- **C-33**: symmetric phase output contract — every phase emits a structurally identical pair: `{findings list} + {Gap Record}`; the absence of either component is a detectable mechanical error, not a judgment call about whether one was warranted; enables machine-checkable verification across the full trace and removes phase-level structural variance (R8 excellence signal)
- **C-34**: Gap Record as mechanical unlock signal — the Gap Record is not optional documentation produced in advance of the exit gate opening; it is the exit gate unlock signal itself; "The Gap Record is the unlock signal for the phase exit gate" promotes the artifact from a precondition to the literal gate trigger, applying the same lock/unlock mechanic used by PHASE N: COMPLETE and creating a uniform artifact-driven gate model across both finding documentation and gap documentation (R8 excellence signal)
- **C-35**: pre-detection Role B evidence standard commitment — before either detection pass runs in a phase, Role B explicitly commits to the minimum evidence standard it will hold for that phase's anomaly type (e.g., "I require direct trace-step or state-condition evidence at strength ≥ 2"); this commitment is recorded as a named artifact in the phase header, separate from and prior to the post-detection Gap Record; makes the Gap Record's reasoning auditable against a pre-stated standard rather than post-hoc, and elevates Role B from reactive gap-documenter to proactive standard-setter whose commitment cannot be adjusted after findings are known (R9 excellence signal)
- **C-36**: undeclared reference as fifth peer phase — undeclared references receive a dedicated top-level numbered phase (e.g., Phase 3E) structurally identical to the four anomaly-type phases: its own sweep table with Declaration-Only and Trace-Diff columns, Role B pre-detection commitment, Role B Gap Record, LOCKED/OPEN status label, and PHASE 3E: COMPLETE unlock signal; not a verification gate item in exit certification or a subsection of another phase, but a full sequential peer in the lock chain; restores the Phase 5 pattern as first-class, ensuring the fifth anomaly class receives the same mechanical enforcement as the other four (R9 excellence signal)
- **C-37**: evidence strength threshold as hard phase exit gate — the evidence strength threshold (at least one finding at strength ≥ 2, or Role B Gap Record explicitly explains why no finding met the standard) is expressed as a named exit gate condition per phase — a checkbox item in the phase exit certification that blocks PHASE N: COMPLETE from being declared and thus keeps Phase N+1 LOCKED until satisfied; creates a three-gate model per phase exit (Findings List gate + Evidence Strength gate + Gap Record gate), making C-19's quality requirement mechanically parallel to C-34's Gap Record gate rather than a per-step reminder or aspirational note (R9 excellence signal)
- **C-38**: global standards registry with pre-game seal — all phase evidence standards are consolidated into a PART 0 Standards Registry that is sealed before PART 1 opens; each phase restates its exact registry row verbatim at phase entry before detection begins; the seal is explicitly declared (e.g., "REGISTRY SEALED — standards cannot be revised after this point"); elevates C-35's per-phase pre-commitment to a global pre-game commitment covering all phases simultaneously, so no standard can be set or adjusted after the first analysis artifact is produced; makes cross-phase standard consistency mechanically auditable from the registry entry alone (R11 excellence signal)
- **C-39**: rejection immutability as mandatory trace step constraint — negative-path steps must explicitly document that the entity remains in its before-state after rejection; the requirement appears as a named structural element in the trace step template (e.g., "The entity must remain in its before-state after rejection") rather than a general recommendation; closes the gap where a narrative rejection description omits the after-state entirely, and makes guard-condition state immutability mechanically verifiable from the trace record — a rejected operation that changes entity state is a detectable anomaly, not an inference (R11 excellence signal)
- **C-40**: Gap Record structured field template — the Gap Record uses a fixed named-field template (e.g., `Examined:`, `Evidence Standard Applied:`, `What Was Not Found:`, `What Would Constitute a Finding:`, `Verdict:`) rather than free-form prose; the field structure makes Gap Records machine-comparable across phases and transforms the absence record from a written acquittal into a structured diagnostic artifact; a Gap Record that omits a required field is a detectable mechanical error, not a prose judgment call; elevates C-34's artifact-driven gate from "a record exists" to "a structurally complete record exists" (R12 excellence signal)
- **C-41**: Phase 3E entity-type-stratified scan — Pass 1 in Phase 3E issues distinct scanning instructions per reference entity class (S-ID scan, OP-ID scan, INV-ID scan) rather than a single generic "scan for undeclared references"; each entity class receives its own sweep row in the Phase 3E finding table with Declaration-Only and Trace-Diff columns; a sweep verdict on one entity class cannot satisfy the sweep requirement for another; closes the loophole where detecting S-ID undeclared references masks a complete absence of OP-ID or INV-ID checking, and makes Phase 3E coverage gaps detectable by row rather than requiring full-trace inspection (R12 excellence signal)

---

## Essential Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-01 | **Before/after state per operation** — Every operation in the trace shows the full entity state immediately before and immediately after execution. No operation is described with only a narrative summary. | correctness | Every operation has a documented before-state and after-state. At least three operations are traced. Narrative-only descriptions do not pass. |
| C-02 | **Preconditions and postconditions** — For each operation, the trace states what must be true before the operation executes (preconditions) and what is guaranteed to be true after (postconditions). | correctness | Every operation has at least one precondition and one postcondition. Vague statements like "data is valid" do not count — they must be state-specific. |
| C-03 | **Named invariants** — The trace identifies at least one named system invariant: a property that must hold across all states regardless of which operations execute. | correctness | At least one invariant is named and defined. Invariants stated only in passing within operation descriptions do not count — they must be declared as invariants. |
| C-04 | **At least one anomaly identified** — The trace produces at least one concrete finding: a violation, inconsistency, race condition, or undeclared reference discovered during analysis. | completeness | At least one anomaly is named with an anomaly type label, affected operation or state IDs, and a description of why it is a violation. "No anomalies found" does not pass. |
| C-05 | **Domain grounding** — The trace uses vocabulary from a real business domain (Sales, Customer Service, Finance, or equivalent). Entities, operations, and states are named for domain concepts, not generic placeholders. | quality | Entity names, operation names, and state labels are domain-specific. Placeholder names like Entity1 or Op_A do not pass. |

---

## Recommended Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-06 | **Negative path** — The trace includes at least one attempted invalid state transition — an operation that should be rejected — and documents why the guard condition blocks it. | correctness | At least one rejected operation appears in the trace with its guard condition named and the rejection reason stated. If C-39 is also evaluated, the after-state immutability constraint must be documented per C-39's requirement. |
| C-07 | **Numbered step-by-step format** — Trace steps are numbered sequentially so the reader can replay the trace in order without ambiguity. | quality | Steps are numbered (e.g., Step 1, Step 2) and sequential. An unnumbered narrative does not pass even if the order is inferable. |
| C-08 | **Race condition scenario** — The trace includes at least one race condition: a scenario where two concurrent operations on the same entity produce an anomalous outcome depending on interleaving. | completeness | At least one race condition is named, with the two competing operations identified, the affected state field named, and the anomalous outcome described. |

---

## Aspirational Criteria

| ID | Text | Category | Pass Condition |
|----|------|----------|----------------|
| C-09 | **All four anomaly types covered** — The sweep produces at least one finding in each of the four canonical anomaly types: invariant violation, invalid transition, race condition, undeclared reference. | completeness | One named finding per anomaly type. A type with zero findings does not pass unless C-16/C-36 apply and Phase 3E is present. |
| C-10 | **Structured notation / transition table** — State machine behavior is expressed in a formal transition table or structured notation, not only in prose. | quality | A table with columns for at least: current state, operation, guard condition, next state. Prose-only descriptions do not pass. |
| C-11 | **Sweep table with row-level verdicts** — The anomaly sweep is structured as a table with a verdict column; every row has an explicit verdict (Finding / No Finding), not just a narrative summary. | structure | Every anomaly type has a row in a sweep table. Every row has a verdict cell with a labeled verdict. Blank verdict cells do not pass. |
| C-12 | **OP-ID cross-referencing** — Operations are assigned unique identifiers (e.g., OP-01) declared in an operation registry and referenced by those IDs throughout the trace and sweep. | structure | An operation registry table exists with OP-IDs assigned. OP-IDs are used in trace steps and sweep table rows. Prose-only operation names without IDs do not pass. |
| C-13 | **Entry Condition column in state machine table** — The state machine table includes an Entry Condition column stating what must be true for the entity to enter each state, in addition to the standard transition columns. | structure | Entry Condition column is present in the state machine or state registry table. At least one entry condition per state is stated. An absent column does not pass. |
| C-14 | **Minimum-found threshold** — The sweep table carries a stated minimum-found threshold; failing to meet it triggers a named depth-gate failure, not merely a coverage note. | completeness | A minimum-found threshold number is stated (e.g., "minimum 2 findings required"). Failure to meet the threshold is labeled as a gate failure. No threshold stated does not pass. |
| C-15 | **Full ID ecosystem** — All three reference entity classes have declared ID registries: S-IDs for states, OP-IDs for operations, INV-IDs for invariants. All IDs are cross-referenced in trace steps and sweep rows. | structure | Three separate registry tables exist (state registry, operation registry, invariant registry) with IDs assigned. IDs from all three classes appear in trace steps. Missing any registry does not pass. |
| C-16 | **Undeclared reference as fifth anomaly class** — Undeclared references are declared as a named fifth anomaly type with its own sweep row, not treated as a sub-case of another anomaly type or handled in a footnote. | structure | A sweep row labeled "Undeclared Reference" or equivalent exists as a peer to the four canonical anomaly type rows. Absence of such a row does not pass. |
| C-17 | **Separate OP-ID / S-ID / INV-ID columns in anomaly register** — The anomaly register or finding table uses three separate columns for OP-ID, S-ID, and INV-ID references rather than a combined "affected IDs" column; a blank cell in any of the three columns on a found-verdict row is a detectable structural gap. | structure | Three separate ID columns exist. At least one found-verdict row has populated cells. A combined ID column does not pass. |
| C-18 | **Pre-sweep hypothesis with dual-pass ANOMALY FINDER** — Before the sweep begins, the trace records explicit predictions per anomaly type about what will and will not be found; the sweep then runs in two passes; the reconciliation step compares predictions to findings. | process | A hypothesis table exists with at least one prediction per anomaly type prior to the sweep. Two named passes are labeled. A reconciliation step compares predictions to actuals. Missing any component does not pass. |
| C-19 | **Evidence-strength quality gate** — Each finding is assigned an evidence strength score on a 1/2/3 scale at the point of discovery; the sweep has a quality gate requiring at least one finding at strength ≥ 2, or a Gap Record explaining why none met the standard. | quality | A 1/2/3 evidence strength scale is defined. At least one finding has a strength score. The quality gate condition (≥1 at strength ≥2 or Gap Record explanation) is explicitly stated. |
| C-20 | **Numeric/temporal invariant gate** — The invariant registry must contain at least one falsifiable numeric or temporal invariant (e.g., a threshold, a timeout, a count bound) before analysis begins; discovery of zero such invariants blocks the sweep from opening. | correctness | At least one invariant in the registry has a numeric or temporal threshold (a quantity, a duration, a count). An invariant registry containing only qualitative assertions does not pass. |
| C-21 | **Surprise column in reconciliation table** — The reconciliation table includes a Surprise column; each prediction outcome is classified as Expected (E) or Surprising (S), producing a calibration signal beyond binary correct/incorrect. | quality | A Surprise or equivalent column exists in the reconciliation table. Every prediction row has an E or S label. Missing column or unlabeled rows do not pass. |
| C-22 | **Score at point of discovery** — Evidence strength scores are assigned when a finding is first recorded, not retroactively after the sweep is complete; this constraint is stated as an explicit instruction in the sweep template. | process | The sweep template or scoring protocol contains an explicit instruction to assign the strength score at the moment of first recording, not during review. A template with no such instruction does not pass. |
| C-23 | **Score-aloud verbal protocol** — C-22 is expressed as a named cognitive behavioral discipline: the scorer must produce a complete scoring sentence (e.g., "I am scoring this [1/2/3] because [reference]") before recording; an incomplete sentence is not recordable; an explicit self-correction checkpoint is named. | process | A named scoring protocol with a sentence template exists. A self-correction checkpoint is named. The "incomplete sentence = not recordable" rule is stated. Missing any element does not pass. |
| C-24 | **Phase exit gate checkboxes** — Each phase has explicit checkbox gate items for C-20, C-21, and C-22 requirements; the model must affirmatively check each before the phase is declared complete; unchecked boxes block advancement. | structure | At least three named checkbox gates appear in each phase's exit certification. The gates map to C-20/C-21/C-22 conditions. Narrative-only exit conditions without checkboxes do not pass. |
| C-25 | **Three-value surprise taxonomy with calibration score** — The reconciliation table uses C/FP/FN (Correct, False Positive, False Negative) classification; a computed calibration score is stated; a threshold below which structural diagnosis is triggered is named; the diagnosis process names at least three steps. | quality | C/FP/FN labels are defined and applied per row. A calibration score formula or computation is shown. A named threshold and a multi-step diagnosis process (at least 3 steps) are stated. Missing any element does not pass. |
| C-26 | **Anomaly-type-as-top-level-phase with sequential commitment** — Each of the four anomaly types is a numbered top-level phase with its own exit gate; phases are sequentially locked so the model cannot enter Phase N+1 until Phase N emits its COMPLETE declaration. | structure | Four numbered top-level phases exist for anomaly types. Sequential lock dependency is stated. A COMPLETE declaration or equivalent unlock signal is required per phase. Subsections within a shared sweep part do not pass. |
| C-27 | **Prejudice-dismissal naming protocol** — An acquittal verdict ("no finding") requires naming the specific trace steps, state conditions, or evidence examined and why each cleared the anomaly type; generic "None" or "No findings detected" declarations do not pass. | process | Every no-finding verdict names at least one specific OP-ID, S-ID, or trace step examined and the clearing reason. Bare "None" or unsubstantiated acquittals do not pass. |
| C-28 | **Dual-mode doc-vs-trace detection** — Each anomaly-type sweep runs two independent passes: Pass 1 against declaration tables alone; Pass 2 diffing declared behavior against the traced step sequence; a declared behavior not demonstrated in the trace is itself a finding. | process | Two named passes are labeled per phase. Pass 1 targets declaration tables; Pass 2 targets trace-vs-declaration diff. The cross-pass finding rule is stated. Single-pass sweeps do not pass. |
| C-29 | **Acquittal-advocate sub-role with activation gate** — A named Role B (or equivalent sub-role) is defined; it activates per phase and must produce a Gap Record before the phase exit gate opens; it cannot be bypassed regardless of finding count. | structure | Role B or equivalent is named. Activation per phase (unconditional by R12 — see C-32) is stated. Gap Record production is required before gate opens. A role that activates only on no-finding does not pass C-32. |
| C-30 | **Parallel dual-mode verdict columns with inline gap records** — The finding table has two named columns (Declaration-Only Finding, Trace-Diff Finding); a "None" entry in either column requires an inline gap record in that cell; both columns are independently auditable. | structure | Two named detection columns exist in the finding table. "None" entries have inline explanations in the same cell. A single combined finding column does not pass. |
| C-31 | **Phase LOCKED/OPEN status with explicit COMPLETE declaration** — Each phase carries a LOCKED/OPEN status label; Phase N remains LOCKED until Phase N-1 emits a named PHASE N-1: COMPLETE declaration as the unlock signal. | structure | LOCKED/OPEN labels appear per phase. A PHASE N: COMPLETE (or equivalent named) declaration is required to unlock Phase N+1. Phases without status labels do not pass. |
| C-32 | **Unconditional gap documentation per phase** — Role B fires on every phase regardless of finding count; phases with findings still require a Gap Record documenting what was not found; the conditional no-finding trigger from C-29 is removed. | process | Gap Records appear on all phases, including phases with findings. Absence of a Gap Record on any phase — regardless of finding count — does not pass. |
| C-33 | **Symmetric phase output contract** — Every phase emits exactly two artifacts: a Findings List and a Gap Record; absence of either is a detectable mechanical error; structural variance across phases is not permitted. | structure | Every phase has a Findings List artifact and a Gap Record artifact. A phase with only one artifact does not pass regardless of its content. |
| C-34 | **Gap Record as mechanical unlock signal** — The Gap Record is not a precondition to the exit gate — it is the exit gate unlock signal itself; "The Gap Record is the unlock signal for the phase exit gate" or equivalent language is explicitly stated. | structure | The Gap Record's role as the unlock signal (not merely a precondition) is stated explicitly. A template where the Gap Record is described as optional documentation or a pre-gate requirement does not pass. |
| C-35 | **Pre-detection Role B evidence standard commitment** — Before either detection pass runs in a phase, Role B commits to the minimum evidence standard for that phase's anomaly type; the commitment is a named artifact in the phase header, prior to and separate from the post-detection Gap Record. | process | A named pre-detection commitment artifact appears in each phase header. The commitment is recorded before detection begins. Commitment stated after findings are known does not pass. |
| C-36 | **Undeclared reference as fifth peer phase** — Phase 3E (or equivalent) is a top-level numbered phase structurally identical to the four anomaly-type phases: sweep table with Declaration-Only and Trace-Diff columns, Role B pre-detection commitment, Role B Gap Record, LOCKED/OPEN status label, PHASE 3E: COMPLETE unlock signal; not a subsection of another phase or a verification gate item. | structure | Phase 3E (or equivalent) exists as a numbered top-level phase with all five structural elements: sweep table, pre-detection commitment, Gap Record, LOCKED/OPEN label, COMPLETE declaration. A paragraph description or subsection does not pass. |
| C-37 | **Evidence strength threshold as hard phase exit gate** — The evidence strength threshold (≥1 finding at strength ≥2 or Gap Record explanation) appears as a named checkbox in the phase exit certification, blocking PHASE N: COMPLETE until satisfied; it is a gate, not a reminder. | structure | An evidence strength checkpoint appears as a named checkbox gate in every phase exit certification. A phase exit that omits this gate does not pass. |
| C-38 | **Global standards registry with pre-game seal** — All phase evidence standards are consolidated into a PART 0 Standards Registry sealed before PART 1 opens; each phase restates its exact registry row verbatim at phase entry before detection begins; a seal declaration (e.g., "REGISTRY SEALED") is explicit. | structure | A PART 0 Standards Registry exists with a row per phase. A seal declaration appears at the close of PART 0. Each phase's pre-detection commitment restates the exact registry row verbatim. Standards set after PART 0 is sealed do not pass. |
| C-39 | **Rejection immutability as mandatory trace step constraint** — Negative-path trace steps explicitly document that the entity remains in its before-state after rejection; this requirement appears as a named structural element in the trace step template (e.g., "The entity must remain in its before-state after rejection"); deviation from before-state is labeled a detectable anomaly. | structure | The trace step template contains a named rejection immutability field or instruction. At least one rejected operation trace step shows the entity's before-state as its after-state. A narrative rejection description without the immutability record does not pass. |
| C-40 | **Gap Record structured field template** — The Gap Record uses a fixed named-field template (e.g., `Examined:`, `Evidence Standard Applied:`, `What Was Not Found:`, `What Would Constitute a Finding:`, `Verdict:`) rather than free-form prose; the field structure makes Gap Records machine-comparable across phases and transforms the absence record from a written acquittal into a structured diagnostic artifact; omitting a required field is a detectable mechanical error; elevates C-34's artifact-driven gate from "a record exists" to "a structurally complete record exists". | structure | A named-field template for the Gap Record is defined with at least four required fields. All Gap Records across all phases use this template. A Gap Record that omits any required field does not pass, regardless of its prose content. |
| C-41 | **Phase 3E entity-type-stratified scan** — Pass 1 in Phase 3E issues distinct scanning instructions per reference entity class (S-ID scan, OP-ID scan, INV-ID scan) rather than a single generic "scan for undeclared references"; each entity class has its own sweep row in the Phase 3E finding table with Declaration-Only and Trace-Diff columns; a sweep verdict on one entity class does not satisfy the sweep requirement for another; closes the loophole where detecting S-ID undeclared references masks a complete absence of OP-ID or INV-ID checking. | structure | The Phase 3E finding table has at least three rows: one per reference entity class (S-ID, OP-ID, INV-ID). Each row has Declaration-Only and Trace-Diff verdict cells. A single "undeclared references" row covering all entity types does not pass. |
```
