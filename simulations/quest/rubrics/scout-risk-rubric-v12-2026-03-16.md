Three new criteria extracted and rubric written to `scout-risk-rubric-v12-2026-03-17.md`.

---

**New criteria added to v12:**

**C-35 — AMEND resolution isolated as a named Phase 0a sub-phase before any role activates**
Escalates C-34. V-05's structured AMEND header (C-34) is produced in a dedicated Phase 0a that completes before any role or register phase begins. Scope-narrowing becomes a closed precondition — every subsequent phase inherits a fully resolved scope. Without the phase-isolation, AMEND work co-located with role activation allows scoping errors to propagate into operative phases.

**C-36 — HIGH-seed sub-phase within Phase 1 precedes register expansion**
Escalates C-15. V-03/V-05 insert a named step after dimension setup but before any expansion rows are added, seeding one HIGH-severity anchor per active dimension. This converts C-15 from a retrospective audit outcome into a structural process invariant — the Phase 2b diversity audit confirms a seeded fact rather than discovering a gap and triggering repair.

**C-37 — Competitor anatomy schema declared as a named pre-population field list before any competitor row is written**
Escalates C-27. V-02/V-04/V-05's Phase 3 ✓+ sweep across all seven criteria (C-24–C-30) traces to one upstream cause: the anatomy field set is declared as a named schema before any competitor row — including Competitor-00 — is populated. Field parity becomes a structural precondition, not a retrospective check. Matches the closed-vocabulary pattern of C-22 and C-33.

**Max composite: 142 → 148.** Scoring bands shifted +6 at the top end. Escalation ladder populated with all three new rows.
