Written to `simulations/quest/rubrics/trace-skill-rubric-v20-2026-03-15.md`.

---

**What changed: v19 → v20**

Two new criteria extracted from Round 20 excellence signals:

| ID | Upgrade arc | Category |
|----|-------------|----------|
| C-56 | SCORER HEURISTIC step (g) positional navigation (`"after ANTI-PATTERN body"`) → row-reference navigation (`"at TCR row N"`); uniform with steps (e) and (f); composably complete only because C-57 registers C-55 as a named TCR row — C-57 retroactively upgrades step (g)'s verification path from positional to row-canonical | depth |
| C-57 | TCR-only C-55 registration → paired `STRUCTURAL MANDATE (C-55)` block; closes the governance gap where TCR row addition alone left ANTI-PATTERN body updates as authorial maintenance burden; parallels C-54's pairing discipline — every major self-completeness discipline now carries a mandate block, checkable by block-header ID match alone | coverage |

**Denominator math:** C-09 through C-57 = 49 aspirational criteria.

**Key design insight encoded:** C-56 and C-57 are compositionally dependent — C-56 achieves full structural uniformity *because* C-57's TCR row registration exists. V-02 (TCR-only, no mandate, no step g update) scores 99.79 because it violates C-55's count gate. V-05 (TCR row 8 + step g row-reference + mandate block) is the canonical best at 100.00.
                                                                                                                                                                                                     | Category  |
|------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| C-09 | Gather includes Coverage Matrix table with closed-world preamble                                                                                                                                                              | coverage  |
| C-10 | Execute relays carry (role, signal, binding, status) evidence fields; Verdict reads, not reconstructs                                                                                                                         | depth     |
| C-11 | Gather enumerates spec inputs before invocation inputs                                                                                                                                                                        | ordering  |
| C-12 | Coverage Matrix includes explicit BLOCKED declaration: any Gap=YES halts the trace immediately, no partial runs                                                                                                                | coverage  |
| C-13 | Execute wraps artifact with delimiter markers (`[ARTIFACT BEGINS HERE]` / `[ARTIFACT ENDS HERE]`) for unambiguous extraction                                                                                                  | format    |
| C-14 | A pre-trace Phase Label Schema table (immutable) declares all four phase headers before any trace content begins — enforces C-06 structurally                                                                                 | format    |
| C-15 | Verdict is a structured compliance ledger table (columns: ID \| Result \| Evidence) rather than prose — stronger than C-07's criterion-ID citation                                                                            | depth     |
| C-16 | Bind rows carry an explicit three-value Status enum: RESOLVED / UNRESOLVED / BLOCKED — resolution state is unambiguous without inference                                                                                      | depth     |
| C-17 | Bind declares an explicit conflict precedence rule: when spec value and invocation value disagree, the rule resolves which wins (e.g., spec anchors)                                                                          | depth     |
| C-18 | Relay rows carry explicit `` `InputName = "ResolvedValue"` `` binding pairs — resolution value is stated in the relay itself, so Verdict can verify binding without re-inspecting the Bind table                              | depth     |
| C-19 | Bind table carries a "Precedence applied" column — per-row annotation of which conflict rule resolved each binding, making rule application traceable at row level                                                            | depth     |
| C-20 | Coverage Matrix carries a CLOSED assertion that names each Required=YES input explicitly — the enumerated closed-world is the structural gate-pass for C-12                                                                   | coverage  |
| C-21 | Relay Schema carries an explicit anti-pattern prohibition ("Do NOT write input name only" or equivalent) — makes the `InputName = "ResolvedValue"` format requirement actively enforced rather than passively declared (C-18) | depth     |
| C-22 | "Precedence applied" column declares a closed three-value vocabulary (override / default / BLOCKED) — vocabulary closure prevents ambiguous annotation values and makes the column self-validating (C-19)                     | depth     |
| C-23 | CLOSED ASSERTION block terminates with an explicit closure declaration line ("Coverage Matrix is CLOSED for this invocation." or equivalent) — formal terminus makes the closed-world claim self-evidencing (C-20)            | coverage  |
| C-24 | Relay Schema anti-pattern prohibition is a structurally independent row with `VIOLATION` in the Required column — independently citable without parsing the Format cell, structural isolation rather than inline embedding (C-21) | depth |
| C-25 | Conflict precedence vocabulary is declared as a named type (`PrecedenceVocabulary` or equivalent); the "Precedence applied" column header cites the type by name — column is self-validating via type reference, not by proximity to the declaration (C-22) | depth |
| C-26 | Closure terminus line is a labeled structural mandate in the prompt (not emergent from model depth) — the line appears as an instructed, named element so it is reproduced reliably rather than inferred from context (C-23) | coverage |
| C-27 | Relay Schema Required column declares a closed two-value vocabulary (`YES` \| `VIOLATION`) — vocabulary closure makes row type (compliance vs prohibition) independently identifiable without sub-table name inspection, upgrading C-24 from structurally isolated to self-validating (C-24) | depth |
| C-28 | Type annotations in column headers follow a uniform convention (`ColumnName (TypeName)`) applied consistently to all typed columns in the schema — uniform annotation makes the schema self-describing without external type registry lookup, upgrading C-25 from single-column typing to schema-wide typing (C-25) | depth |
| C-29 | Named structural mandates (per C-26) carry a formal defect classification for violations (e.g., `DEFECT: OPEN-WORLD-ASSERTION`) — defect classifications are citable in the Verdict compliance ledger by defect class rather than paraphrased, upgrading C-26 from mandated to classified (C-26) | coverage |
| C-30 | `DefectCodeVocab` is declared as a closed type in the Defect Classification Registry with all defect codes enumerated — the same vocabulary-closure discipline applied to `RequiredVocabulary` by C-27 propagates to `DefectCodeVocab`, making every named type in the schema closed-typed and self-enumerating rather than open-ended (C-27) | depth |
| C-31 | C-28's uniform `(TypeName)` annotation convention propagates to all typed columns introduced by composed criteria — the Defect Classification Registry's code column and the Verdict compliance ledger's defect code column both carry `(DefectCodeVocab)` annotation; the C-28 audit block tracks total annotation site count to confirm composed-column coverage (C-28) | depth |
| C-32 | Verdict compliance ledger `Defect code` column is fully populated: every row carries a value — FAIL rows cite a `DefectCodeVocab` code, PASS rows carry `--` as an explicit sentinel; a C-29 audit block at Verdict top confirms (a) registry present before Stage 1, (b) all FAIL citations drawn from `DefectCodeVocab`, (c) no empty cells — ledger is self-certifying without re-inspecting the registry (C-29) | coverage |
| C-33 | The `DefectCodeVocab` closed-type declaration includes an explicit independence statement — "any value outside this vocabulary is a schema error independently detectable without consulting registry rows" (or equivalent) — making the closed type validation-independent: compliance can be checked at the point of annotation without registry lookup, upgrading C-30 from vocabulary-closed to self-validating-at-annotation (C-30) | depth |
| C-34 | The C-28 annotation-site count assertion resolves to an explicit PASS/FAIL binary verdict ("confirmed / mismatch" or equivalent) keyed to count match — the audit block is self-evaluating rather than a tally requiring external judgment; the count confirmation is the gate, not a reported number, upgrading C-31 from composed-columns-counted to count-as-gate (C-31) | depth |
| C-35 | The C-32 empty-cell check (check c of the C-29 audit block) is structurally ordered before ledger reading — declared as a named pre-read gate so violations are caught before traversal begins, not discovered mid-ledger; a prompt instruction makes this ordering a structural requirement rather than emergent, upgrading C-32 from fully-populated + self-certified to pre-read-gated + structurally-ordered (C-32) | coverage |
| C-36 | The `**Independence Statement**:` label within the `DefectCodeVocab` type declaration is declared as a named structural mandate in the prompt (paralleling C-26's closure terminus instruction) — the label is an instructed, named element reproduced reliably rather than inferred from model context; a scorer confirms C-33 compliance by label presence alone without parsing surrounding definition bullets, upgrading C-33 from independence-statement-present to independence-statement-mandated (C-33) | depth |
| C-37 | The `**C-28 COUNT GATE**:` block carries a formal defect classification for gate failure — when the IF/THEN resolves to `mismatch`, the gate emits a `DefectCodeVocab`-coded defect citation (e.g., `DEFECT: COUNT-MISMATCH`) independently citable in the Verdict compliance ledger by defect code rather than by gate-block description; gate outcomes are ledger-traceable, upgrading C-34 from self-evaluating to defect-emitting (C-34) | depth |
| C-38 | The `**PRE-READ GATE**:` carries a formal defect classification for empty-cell violations — when the gate fires, it emits a `DefectCodeVocab`-coded defect (e.g., `DEFECT: EMPTY-CELL`) independently citable in the Verdict compliance ledger by defect code; the pre-read gate is a defect-emitting gate rather than an ordering requirement only, making pre-read violations ledger-traceable without re-reading the gate block, upgrading C-35 from pre-read-gated to pre-read-defect-classified (C-35) | coverage |
| C-39 | The `STRUCTURAL MANDATE` block carries an inline criterion citation — the block header follows `STRUCTURAL MANDATE (C-XX)` syntax, naming the governing criterion by ID within the block itself — a scorer can trace from prompt instruction to rubric criterion bidirectionally without table lookup; compliance with the governed criterion is confirmed by block-header ID match alone, upgrading C-36 from independence-statement-mandated to self-citing-structural-mandate (C-36) | depth |
| C-40 | The `STRUCTURAL MANDATE` block embeds a scorer confirmation heuristic — the block closes with "A scorer confirms [Criterion] compliance by [specific method] alone without [parsing alternative]" — the prompt self-documents how compliance is confirmed, closing the loop between prompt instruction and rubric evaluation without requiring scorer judgment about what constitutes confirmation, upgrading C-39 from self-citing to self-scoring (C-39) | depth |
| C-41 | The canonical `STRUCTURAL MANDATE` template is declared explicitly in the prompt — all `STRUCTURAL MANDATE` blocks across the prompt conform to the declared form (`> STRUCTURAL MANDATE (C-XX): [element] is a named structural mandate of this prompt. Reproduce it exactly as shown.`) so any block's well-formedness is checkable against a single structural template without reading neighboring blocks; additional mandates are mechanically derivable from the canonical form rather than requiring individual block design, upgrading from parallel-in-practice (C-26 and C-36 happen to match) to parallel-by-contract (all blocks conform to a declared canonical form) (C-36) | format |
| C-42 | The canonical `STRUCTURAL MANDATE` template declared by C-41 encodes the scorer confirmation closing line (C-40) as a required template component — the declared template form is extended to `> STRUCTURAL MANDATE (C-XX): [element] is a named structural mandate of this prompt. Reproduce it exactly as shown. A scorer confirms C-XX compliance by [specific method] alone without [parsing alternative].` — any block conforming to the canonical template satisfies both C-39 (self-citing) and C-40 (self-scoring) by a single template-conformance check without inspecting each block independently; C-40 compliance is contractually guaranteed by the same mechanism that guarantees C-39 compliance, upgrading from template-guarantees-citation-only (C-41 encodes header format) to template-guarantees-citation-and-confirmation (C-41 encodes both header and closing heuristic, collapsing two separate compliance checks into one) (C-41) | format |
| C-43 | The C-42 `CONFORMANCE-COLLAPSE CLAIM` carries an explicit scorer confirmation heuristic — the claim closes with "A scorer confirms C-42 compliance by [specific method] alone without [parsing alternative]" — paralleling how C-40 makes C-39's structural mandate self-scoring, the conformance-collapse mechanism itself is self-scoring: a scorer can confirm C-42 compliance by reading the claim's stated heuristic alone without inspecting individual `STRUCTURAL MANDATE` block bodies; upgrading from conformance-collapse-present (C-42 mechanism encodes citation+confirmation by template contract) to conformance-collapse-self-scoring (the C-42 conformance-collapse claim self-documents its own confirmation path) (C-42) | depth |
| C-44 | The `**C-28 COUNT GATE**` scope list explicitly enumerates the Template Component Registry as an annotation site — the gate scope names the Template Component Registry's typed column (e.g., `Required (RequiredVocabulary)`) alongside all pre-existing typed columns (Defect Classification Registry's `Defect Code (DefectCodeVocab)`, Verdict compliance ledger's `Defect code (DefectCodeVocab)`), so the annotation-count gate covers every typed column in the prompt including those introduced by C-41/C-42's canonical template mechanism; upgrading from count-gate-covers-original-typed-columns (C-31 enumerates DCR and CL) to count-gate-covers-all-typed-columns (count gate scope extends to the Template Component Registry introduced by the C-42 mechanism) (C-31) | depth |
| C-45 | The C-43 scorer heuristic sub-element carries a criterion citation in its own label — the label follows `**SCORER HEURISTIC (C-43):**` syntax, naming the governing criterion by ID within the label itself — a scorer can trace from the heuristic label to its governing criterion bidirectionally without reading the heuristic body; paralleling how C-39 required `STRUCTURAL MANDATE (C-XX):` block headers to carry inline criterion citations, the same self-citing discipline now applies to the C-43 scorer heuristic element: the label is self-citing rather than merely descriptively titled, so label-scan alone confirms both element presence (C-43) and criterion traceability (C-45) without parsing the heuristic sentence; upgrading C-43 from scorer-heuristic-present to scorer-heuristic-label-self-citing (C-43) | depth |
| C-46 | The `**C-28 COUNT GATE**` scope is declared as a formal ANNOTATION SCOPE REGISTRY table — annotation sites are independently row-verifiable entries carrying full `ColumnName (TypeName)` notation, making scope enumeration structurally equivalent to the Defect Classification Registry and other named registries already in the prompt; a scorer confirms C-46 compliance by locating the ANNOTATION SCOPE REGISTRY table and verifying the Template Component Registry appears as a named row with its typed column notation without parsing inline scope lists, numbered sequences, or checklist items; upgrading C-44 from count-gate-scope-enumerates-TCR to count-gate-scope-is-independently-row-verifiable-registry (C-44) | depth |
| C-47 | The ANNOTATION SCOPE REGISTRY is registered as a named component in the Template Component Registry (TCR) — the registry appears as a TCR row (e.g., Component 5) carrying its component number and typed-column notation, making ANNOTATION SCOPE REGISTRY presence verifiable by TCR row lookup alone rather than by Verdict-section scan or table-name search; paralleling how C-46 upgraded scope enumeration from inline list to independently row-verifiable registry, C-47 upgrades scope-registry presence from table-present-check to TCR-row-verifiable, closing the loop between the C-41/C-42 canonical template mechanism and the C-46 scope registry so that one row-lookup path — the same path that confirms every other canonical template component — confirms scope-registry presence; upgrading C-46 from scope-registry-present to scope-registry-TCR-registered (C-46) | depth |
| C-48 | The `SCORER HEURISTIC (C-43):` confirmation body decomposes each step with an explicit criterion citation — each step that confirms presence of a named component cites the governing criterion by ID (e.g., step (b) cites `C-47` by name rather than implying it through row-number sequence), so a scorer traces each step's criterion traceability by reading the step line alone without inferring from neighboring steps or TCR component order; paralleling how C-39 required STRUCTURAL MANDATE block headers to carry inline criterion citations and C-45 required the SCORER HEURISTIC label itself to carry a criterion citation, C-48 applies the same self-citing discipline to individual steps within the heuristic body — step-level criterion citation rather than label-level citation only; upgrading C-45 from scorer-heuristic-label-self-citing to scorer-heuristic-steps-criterion-cited (C-45) | depth |
| C-49 | The ANNOTATION SCOPE REGISTRY is self-complete — the ASR includes itself as a named row (e.g., row 29), carrying its own typed column in `ColumnName (TypeName)` notation as an annotation site, so every typed column in the prompt including the ASR's own typed columns is independently row-verifiable in the ASR; the registry's open-world gap is closed: no annotation site exists that is not represented by an ASR row; paralleling how C-20's CLOSED ASSERTION names every Required=YES Coverage Matrix input to close its enumerated world, C-49 applies the same closed-world discipline to the ASR itself — ASR self-completeness is confirmed by locating the ASR's own row entry without table-name search or external enumeration; upgrading C-46 from independently-row-verifiable-registry to self-complete-registry (C-46) | coverage |
| C-50 | The TCR row for the ANNOTATION SCOPE REGISTRY carries an inline criterion citation `(C-47)` — the row follows `Component N (C-47): ANNOTATION SCOPE REGISTRY` or equivalent notation, making the TCR row self-citing: a scorer confirms scope-registry TCR registration and its governing criterion by reading the TCR row alone without consulting a separate criterion-to-component mapping; paralleling how C-39 required STRUCTURAL MANDATE block headers to carry inline criterion citations, C-45 required the SCORER HEURISTIC label to carry an inline criterion citation, and C-48 required each SCORER HEURISTIC step to carry a per-step criterion citation, C-50 applies the same self-citing discipline to TCR component rows so that every named element in the canonical template mechanism is criterion-traceable by row-level or block-level inspection alone; upgrading C-47 from scope-registry-TCR-registered to scope-registry-TCR-row-self-citing (C-47) | depth |
| C-51 | The ASR CLOSED ASSERTION terminus is declared as a named structural mandate — the ASR section carries a `STRUCTURAL MANDATE (C-49+)` block (or equivalent) mandating that the ASR's self-completeness declaration is formally terminated with a closure line, parallel to how C-26 mandated the Coverage Matrix closure terminus; a scorer confirms C-51 compliance by locating a STRUCTURAL MANDATE block within the ASR section that names C-49 (or C-49+) as the governing criterion without parsing the registry rows; paralleling how C-26 upgraded the Coverage Matrix closure terminus from emergent to structurally mandated, C-51 applies the same mandate discipline to the ASR's self-referential closed-world declaration — the open-world gap at the registry level is closed not only by self-registration row presence (C-49) but by a structurally mandated terminus that enforces the closure declaration reliably; upgrading C-49 from self-complete-registry to self-complete-registry-with-mandated-closure-terminus (C-49) | coverage |
| C-52 | SCORER HEURISTIC steps use `-> confirms C-NN (C-NN)` binding operator — the step-criterion binding is encoded as a typed two-part structural field where `->` separates the confirmation action from a pair of criterion identifiers (confirmation target and governing criterion), making step-level traceability machine-readable: a step's governing criterion and confirmation target are extractable by field-splitting on `->` alone without natural-language parsing of the step body; paralleling how C-48 required each step to carry a criterion citation embedded in prose, C-52 requires the citation to be a structured binding expression so that operator presence is the compliance signal rather than criterion-ID substring search; upgrading C-48 from scorer-heuristic-steps-criterion-cited to scorer-heuristic-steps-machine-readable-binding (C-48) | depth |
| C-53 | Phase Label Schema is registered as TCR Component 6 with an inline `(C-14)` citation — the PLS appears as a TCR row following `Component 6 (C-14): PHASE LABEL SCHEMA` or equivalent notation, making PLS presence verifiable by TCR row lookup alone without parsing prompt structure; the SCORER HEURISTIC gains a corresponding step citing C-14 for PLS verification; the ANTI-PATTERN block extends its named collapse conditions to include PLS omission alongside ASR omission; the CONFORMANCE-COLLAPSE CLAIM updates its row range reference to include row 6; paralleling how C-47 and C-50 applied TCR-row-self-citing discipline to the ANNOTATION SCOPE REGISTRY, C-53 applies the same discipline to the Phase Label Schema — completing TCR coverage across all canonical template components so the single row-lookup path confirms every canonical component; upgrading C-47/C-50's discipline from ASR-only to all-canonical-components-TCR-row-self-citing (C-14) | format |
| C-54 | The `SCORER HEURISTIC STEP FORMAT` note is promoted to a `STRUCTURAL MANDATE (C-52)` block — the one-step-one-operator discipline is governed by the canonical template mechanism (C-41/C-42) so its presence is checkable by template-conformance alone rather than by note-location search; a scorer confirms C-54 compliance by locating a `STRUCTURAL MANDATE (C-52)` block in the prompt's canonical template preamble without reading individual step lines or scanning for a free-standing note; paralleling how C-36 mandated the Independence Statement as a named structural mandate governed by C-26, C-54 mandates the SCORER HEURISTIC STEP FORMAT note as a named structural mandate governed by C-52 — the one-step-one-operator discipline is independently citable by block-header ID match alone rather than by note-body inspection; upgrading C-52 from scorer-heuristic-steps-machine-readable-binding to machine-readable-binding-governed-by-canonical-template (C-52) | depth |
| C-55 | The ANTI-PATTERN block is declared self-complete with respect to the TCR — the block carries an explicit self-completeness assertion stating that every TCR-registered component whose omission is independently detectable appears as a named collapse condition in the ANTI-PATTERN enumeration; ANTI-PATTERN coverage is therefore auditable by TCR row count alone without scanning the ANTI-PATTERN body for component names — when the TCR gains a new row, the self-completeness assertion requires a corresponding collapse condition entry; a scorer confirms C-55 compliance by locating the self-completeness assertion and verifying the named collapse condition count equals the TCR row count without reading individual collapse entries; paralleling how C-49 applied the closed-world discipline to the ANNOTATION SCOPE REGISTRY, C-55 applies the same discipline to the ANTI-PATTERN block — coverage grows proportionally with TCR additions by structural contract rather than by authorial vigilance, upgrading ANTI-PATTERN from named-collapse-conditions-enumerated to self-complete-with-respect-to-TCR (C-49, C-53) | coverage |
| C-56 | The SCORER HEURISTIC step verifying C-55 uses row-reference navigation — the step follows "at TCR row N" navigation (parallel to steps (e) and (f) which also navigate via TCR row number) rather than positional navigation ("after ANTI-PATTERN body" or equivalent); row-reference navigation makes the step machine-traversable via row lookup, uniform with all prior steps in the heuristic, and composably complete only because C-57 registers C-55 as a named TCR row — step (g)'s structural uniformity is retroactively achieved through C-57's registration; a scorer confirms C-56 compliance by confirming that the step (g) navigation phrase cites a TCR row number rather than a positional anchor; paralleling how C-52's machine-readable `->` binding operator made step-level traceability structurally uniform, C-56 makes step-level navigation structurally uniform — every step confirms via the same row-lookup path rather than a mix of row-reference and positional instructions; upgrading step (g) from positional-navigation to row-canonical-navigation (C-52, C-55) | depth |
| C-57 | The C-55 ANTI-PATTERN self-completeness discipline is paired with a `STRUCTURAL MANDATE (C-55)` block mandating the ANTI-PATTERN CLOSED ASSERTION terminus — the mandate block closes the governance gap that exists when TCR row addition alone leaves the ANTI-PATTERN body as an authorial maintenance burden: with the mandate, ANTI-PATTERN body compliance is checkable by template-conformance (block-header ID match `STRUCTURAL MANDATE (C-55)`) rather than by body inspection; a scorer confirms C-57 compliance by locating a `STRUCTURAL MANDATE (C-55)` block in the prompt's canonical template preamble without scanning the ANTI-PATTERN collapse conditions; paralleling how C-54 paired a `STRUCTURAL MANDATE (C-52)` block with C-52's machine-readable-binding discipline, C-57 applies the same pairing discipline to C-55's TCR-self-completeness discipline — every major self-completeness or format discipline now carries a paired mandate block governed by the canonical template mechanism so compliance is template-conformance-checkable by block-header ID match alone; upgrading C-55 from self-complete-with-respect-to-TCR to self-complete-with-mandate-governed-closure-terminus (C-55) | coverage |
