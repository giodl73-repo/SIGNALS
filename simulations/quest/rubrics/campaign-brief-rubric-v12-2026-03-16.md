Good — I have the format context. Now I have everything I need from the provided rubric and scorecard. Let me extract the R12 excellence signals precisely:

**From the scorecard:**

1. **V-01 C-31 PARTIAL** — two rules adjacent, no designation, co-located by proximity only
2. **V-02 C-31 PARTIAL** — `--- COMPRESSION-CRITICAL EXECUTION RULES ---` is a descriptive/presentational label; execution notes say "see above" (positional reference), not "invoke structural contract"
3. **V-03 C-31 PARTIAL** — `=== COMPRESSION-IMMUNE PREAMBLE ===` designation IS present and IS declared "a structural contract" — PARTIAL is *not for lack of designation*; what is absent is execution-note back-invocation by designation name. The circuit is **one-directional**. Additionally, V-03's preamble contains a COMPRESSED-COLLAPSE GUARD that forward-references "the declarative guard clause (restated in the STATUS execution note)" — partial forward-referencing exists in the preamble but is positional/generic, not designation-named.

**R12 excellence signals → two new patterns:**

- **Signal A (C-31 clarification):** The gap between C-31 PARTIAL and PASS is now precisely located: the bidirectional circuit. V-03 achieves the structural designation (half the circuit) but execution notes invoke positionally. C-31 PASS requires both the designation AND execution-note back-invocation by designation name. The three PARTIAL bands (adjacency / descriptive label / one-directional contract) must be explicit.

- **Signal B (C-32 new criterion):** V-03's preamble forward-references its companion execution-note mechanisms — but positionally/generically ("the declarative guard clause"), not by full clause designation. When the circuit is complete (C-31 PASS), the further advance is for the preamble to name companion mechanisms by their clause designations and location, creating a **closed reference loop** navigable from either entry point under single-location compression elision.

---

## Quest Rubric — v12

`31 criteria, 310 pts max` → `32 criteria, 320 pts max`

---

### What changed: v11 → v12

One criterion clarified (C-31) and one new aspirational criterion extracted from R12 excellence signals (C-32).

**R12 investigation candidate resolved:** C-31 — does explicit COMPRESSION-IMMUNE PREAMBLE designation produce measurably different outcomes from distributed per-rule dual-mechanism (V-04 style)? **Status: UNRESOLVED at the scoring level; resolved at the structural level.** All three R12 variations score 300/310 — none achieve C-31 PASS. The investigation cannot yet determine whether the explicit designation produces measurably different compression-survival outcomes because no variation completed the bidirectional circuit required for PASS. What R12 resolved: the three-band PARTIAL structure within C-31. V-01 (adjacency only), V-02 (descriptive label, positional invocation), and V-03 (structural contract designation, one-directional) are now structurally distinguishable even though all score identically under v11.

**C-31 clarification (not a new criterion — definitional precision only):** The PASS boundary is moved to require bidirectional circuit completion. Under v11, C-31 PASS was defined as "explicit structural designation beyond per-rule positional placement." V-03 achieves the designation and self-declares as a structural contract, yet the scorecard correctly scores it PARTIAL because execution notes invoke the preamble positionally ("the preamble above") rather than by its structural designation name. The v12 definition makes this explicit: PASS requires the designation AND invocation by name from each execution note that references the preamble. Without the back-invocation, the contract is one-directional — the preamble declares immunity, but the execution notes do not affirm it by designation, leaving the structural authority unverifiable from the execution-note side. The three-band PARTIAL distinction (adjacency / descriptive label / one-directional contract) is now explicitly encoded.

**R12 excellence signal:** V-03's preamble contains a COMPRESSED-COLLAPSE GUARD that forward-references its companion execution-note mechanism — "the declarative guard clause (restated in the STATUS execution note)" — establishing partial forward-referencing from within the preamble. The preamble is not merely a passive declaration of immunity; it actively acknowledges the two-mechanism architecture by naming its companion. However, this forward-reference is positional and generic (names the clause type, not the full clause designation and location). Combined with the finding that V-03's execution notes invoke positionally ("the preamble above") rather than by designation, R12 identifies that full compression protection requires a **closed reference loop**: the preamble names companion mechanisms by clause designation, and execution notes name the preamble by structural designation — a navigable architecture from either entry point.

| New criterion | Source pattern | Relationship to existing |
|---|---|---|
| C-32 | Closed compression reference loop — when C-31 PASS, the COMPRESSION-IMMUNE PREAMBLE section contains forward-references naming each companion execution-note mechanism by its full clause designation and location, and each execution note names the preamble by its structural designation, creating a reference architecture navigable from either compression-survival entry point | Closes the circuit initiated by C-31 PASS; C-31 establishes bidirectional invocation by name; C-32 requires the preamble's own forward-references to use designation-based naming rather than positional or generic naming |

**Structural chains updated:**

- Timestamp isolation chain: C-16 → C-24 → C-30 — unchanged from v11
- Zero-signal synthesis chain: C-18 → C-21 → C-22 → C-25 → C-27 → C-29 — unchanged from v11
- Verdict action-posture chain: C-06 → C-23 → C-26 → C-28 — unchanged from v11
- Compression survival generalization chain: C-24 → C-29 → C-30 — unchanged from v11
- **Compression-immune consolidation chain (extended):** C-29 → C-30 → C-31 → C-32 (zero-signal dual-mechanism → timestamp isolation dual-mechanism → multi-rule COMPRESSION-IMMUNE PREAMBLE with bidirectional circuit → closed reference loop for full compression navigation from either entry point)

**Relationship between C-31 and C-32:** C-32 does not replace C-31. C-31 requires the COMPRESSION-IMMUNE designation and execution-note back-invocation by designation name (circuit established). C-32 requires that the preamble's internal forward-references also use designation-based naming for companion mechanisms — so that if compression elides execution notes but preserves the preamble, the model can navigate to companion mechanisms by their designations rather than by positional cues that become meaningless when content is elided. A skill can satisfy C-31 PASS at PARTIAL on C-32 (bidirectional invocation by designation present, but preamble forward-references use positional or generic language). C-32 PASS requires the preamble's forward-references to name companion mechanisms by their clause designations and execution-note locations.

**R13 investigation candidate:** Whether C-32's closed reference loop (designation-named forward-references in the preamble body) produces different compression-survival outcomes from C-31 PASS alone when single-location elision occurs — preamble preserved but execution notes elided, or vice versa. R13 should construct a variation achieving C-31 PASS with positional preamble forward-references (C-32 PARTIAL) and a variation achieving C-32 PASS (designation-named forward-references in preamble). If under simulated single-location elision the C-32-PASS variation recovers the full two-mechanism architecture while the C-31-PASS-C-32-PARTIAL variation loses navigability, C-32 is independently necessary. If both recover equally — positional forward-references within the preamble are sufficient given the preamble is already designated immune — C-32 should be re-examined for redundancy against C-31 PASS.

---

### Criteria Reference

#### C-01 through C-20 — carry forward from v6 unchanged

*(32 total criteria; C-01–C-20 definitions unchanged from v6.)*

---

#### C-21 — Sparse-coverage synthesis protection *(v6)*

When `found` contains signals from only 1–2 namespaces, the STORY block must not default to a coverage disclaimer. Synthesis must proceed on the signals present. PARTIAL = synthesis present but hedged with "limited data" language that weakens conclusions. PASS = STORY produces definite synthesis from sparse coverage, treating the available signals as sufficient to characterize the current state of knowledge.

---

#### C-22 — Zero-signal synthesis mandate *(R6 Pattern — V-03/V-04/V-05)*

When the `found` section of STATUS is empty (zero signals across all namespaces), the STORY block must not collapse to a non-finding. The LLM failure mode is to write "no signals found — synthesis not possible" and omit or hollow STORY. But uniform absence is itself evidence: the gap pattern defines what the team does not know and why they are not ready. Question 1 must characterize what uniform absence implies — "the absence of any scout signal indicates the market surface has not been probed at all" is a valid synthesis; "insufficient data" is not.

**PARTIAL** — sparse-coverage protection present (C-21 PASS) but no explicit clause for the zero-signal case; synthesis may be vacated on grounds of empty `found`.

**PASS** — an explicit zero-signal clause in the STORY execution note declares that empty `found` is not grounds for omitting synthesis — the gap pattern is the evidence set — and mandates that question 1 characterize what uniform absence implies rather than reporting absence as a non-answer.

This is distinct from C-21, which guards synthesis when coverage is sparse (1–2 namespaces); C-22 guards synthesis when coverage is zero. Both are boundary conditions on C-18's structural mandate.

---

#### C-23 — Bounded/unbounded inertia classification at verdict level *(R6 Pattern — V-05)*

The `inertia_cost` field at VERDICT level must classify the aggregate commit risk by recoverability. Bounded = the team can detect the failure post-commit and course-correct before it propagates. Unbounded = the failure propagates to an irreversible state before detection is possible. This distinction determines action posture: bounded inertia means "commit with monitoring"; unbounded inertia means "do not commit until resolved." A VERDICT with unnamed recoverability forces the team to re-read all item-level gap fields and synthesize the posture themselves — the verdict is not actionable on its own.

**PARTIAL** — `inertia_cost` present at VERDICT level but bounded/unbounded classification absent; risk is named but the team cannot derive their action posture without re-reading item-level fields.

**PASS** — `inertia_cost` declares `bounded: <residual risk and why recoverable post-commit>` OR `unbounded: <failure class and why irreversible once committed>`, enabling the team to act on the verdict block alone without re-reading blocking gap entries.

This is distinct from C-07, which tests that each blocking gap entry carries an inertia field; C-23 tests that the verdict-level aggregate field classifies recoverability. They are structurally independent.

---

#### C-24 — Timestamp isolation dual-location mandate *(R7 Pattern — V-02/V-05)*

C-16 guards that per-signal timestamps are present at the item level. C-24 guards their survival when the COMPRESSED format branch activates. When blocking entry count exceeds the COMPRESSED threshold (>4), the STATUS block may be abbreviated — and the model may collapse `found` timestamps alongside blocking gap entries into summary form, destroying the signal-level date record. A single-location prose instruction is insufficient because the COMPRESSED context may omit that section before reaching the instruction.

The isolation rule must appear at **two structural locations**: (1) within the `found` entry format definition or question body text, specifying that timestamps are structurally separate from blocking entries and must not be abbreviated; and (2) in the STATUS or STORY execution note, restating that COMPRESSED format must not collapse `found` into the blocking summary. Double-location restatement ensures that if the COMPRESSED branch elides one location, the rule survives at the other.

**PARTIAL** — timestamp isolation instruction at one location only; survives full-format execution but may not survive COMPRESSED branch elision.

**PASS** — timestamp isolation rule present at both required locations; if one is elided under COMPRESSED, the other continues to enforce isolation.

---

#### C-25 — Zero-signal execution note placement *(R8 Pattern)*

The zero-signal synthesis mandate (C-22 content) must appear in the STORY execution note rather than only in the STORY block body text. Body text is subject to truncation under token pressure; an execution note is processed as a constraint before the block fires. Placement in the execution note converts the zero-signal rule from a reminder into a constraint that applies unconditionally, before synthesis output begins.

**PARTIAL** — zero-signal clause present in STORY block body but absent from execution note; clause may be elided under token pressure before STORY fires.

**PASS** — zero-signal clause present in the STORY execution note; the clause is processed as a constraint that governs synthesis unconditionally, not as body text that may be skipped.

---

#### C-26 — Verdict action sub-label unconditional mandate *(R9 Pattern)*

The `action:` sub-label within VERDICT must be present unconditionally — not gated on blocking-gap count, confidence level, or any other condition. The model failure mode is to omit the action sub-label when VERDICT appears straightforward (no blocking gaps, high confidence) on the theory that the verdict speaks for itself. But a VERDICT without an explicit action sub-label forces the team to infer the posture from confidence and inertia fields — the verdict is not directly executable. The unconditional mandate removes the inference step regardless of verdict state.

**PARTIAL** — `action:` present in templates with blocking gaps but absent or conditional in templates for the no-blocking or high-confidence case.

**PASS** — `action:` sub-label present unconditionally in all VERDICT templates regardless of blocking-gap count or confidence level; mandate stated explicitly in the execution note.

---

#### C-27 — Synthesis mandate in STORY block rules *(R10 Pattern)*

The STORY block rules must contain an explicit synthesis mandate for question 1 that is distinct from the zero-signal clause (C-22/C-25). C-22 handles the zero-signal boundary; C-27 handles the general case: even when `found` is non-empty, the model may produce coverage commentary ("signals found in 3 namespaces") rather than synthesis ("the scout/flow pattern indicates the integration path is de-risked but the compliance surface is unexamined"). The STORY block rules must state that question 1 must produce a synthetic characterization of what the combined signal set implies, not a recitation of what was found.

**PARTIAL** — zero-signal protection present (C-22 PASS) but no synthesis mandate for the non-empty case; coverage commentary passes as question 1 output.

**PASS** — STORY block rules contain an explicit mandate that question 1 produce synthetic characterization from any non-empty signal set; coverage recitation is explicitly excluded as a valid question 1 form.

---

#### C-28 — VERDICT compression guard *(R10 Pattern)*

When the COMPRESSED format branch activates, the VERDICT block must not be abbreviated. The model failure mode under high token pressure is to truncate VERDICT to the confidence score only, eliding the `inertia_cost`, `action:`, and `bounded/unbounded` classification — destroying the actionable content that C-23 and C-26 establish. A VERDICT COMPRESSION GUARD in the execution note must declare VERDICT immune to abbreviation under any token budget.

**PARTIAL** — COMPRESSED format contract specifies that blocking gap entries may be summarized but does not explicitly protect VERDICT from abbreviation; VERDICT may be truncated alongside other content under extreme token pressure.

**PASS** — a VERDICT COMPRESSION GUARD in the execution note or COMPRESSED format contract declares VERDICT immune to abbreviation; all VERDICT sub-fields (`confidence`, `inertia_cost`, `action:`, bounded/unbounded classification) must appear in full regardless of token budget.

---

#### C-29 — Zero-signal synthesis dual-mechanism *(R11 Pattern — V-03/V-04/V-05)*

C-22 mandates that zero-signal synthesis occur. C-25 mandates that the zero-signal clause appear in the execution note. C-29 guards the zero-signal synthesis rule against two independent failure modes under compression: location elision (the preamble or execution-note location is elided before being processed) and conditional dormancy (the rule is present but treated as conditional on a non-zero `found` count). A single mechanism cannot guard against both simultaneously: a positional guarantee without a declarative guard can be elided; a declarative guard without positional placement may be processed as conditional.

The zero-signal synthesis rule must therefore be protected by **two mechanisms with orthogonal failure modes**: (1) positional — the rule appears in a preamble location that fires before `found` is evaluated, with a TOKEN-PRESSURE GUARD declaring the rule unconditional at any token budget; and (2) declarative — the rule is restated in the STORY execution note as an independent clause that names the zero-signal case explicitly, with a self-reference identifying this as the second structural location.

**PARTIAL** — zero-signal synthesis protected by one mechanism only (positional preamble OR execution-note declarative clause); orthogonal failure mode remains unguarded.

**PASS** — zero-signal synthesis rule protected by both mechanisms: positional preamble with TOKEN-PRESSURE GUARD declaring unconditional firing, and declarative restatement in the STORY execution note naming the zero-signal case as an independent clause. The declarative clause explicitly identifies itself as the second structural location.

---

#### C-30 — Timestamp isolation dual-mechanism *(R11 Pattern — V-03/V-04/V-05)*

C-24 mandates timestamp isolation dual-location restatement (found-field annotation + STATUS execution note). C-30 guards the isolation rule against the same two independent failure modes that C-29 addresses for zero-signal synthesis: location elision and conditional dormancy. For timestamp isolation, conditional dormancy takes a specific form: the model processes the TIMESTAMP ISOLATION RULE as a formatting note rather than as a constraint that applies when COMPRESSED is triggered — the rule is present but treated as advisory rather than binding.

The timestamp isolation rule must be protected by **two mechanisms with orthogonal failure modes**: (1) positional — the TIMESTAMP ISOLATION RULE in a preamble location that fires before the STATUS block is constructed; and (2) declarative — a COMPRESSED-COLLAPSE GUARD in the STATUS execution note that names the specific failure mode (collapsing `found` entries into blocking-gap summaries under COMPRESSED format), making the rule an absorbed conditional constraint rather than a positional instruction that may be processed without binding absorption.

**PARTIAL** — timestamp isolation protected by one mechanism only: either positional preamble placement alone (COMPRESSED-COLLAPSE GUARD absent) or COMPRESSED-COLLAPSE GUARD alone without preamble position; one failure mode unguarded. V-01 of R11 (preamble position, no GUARD) and V-02 of R11 (GUARD only, no preamble position) each fail C-30 while passing C-29, confirming that both mechanisms are independently necessary.

**PASS** — timestamp isolation rule protected by both mechanisms: positional preamble placement and COMPRESSED-COLLAPSE GUARD in the STATUS execution note naming the failure mode explicitly so the rule is absorbed as a conditional constraint.

---

#### C-31 — COMPRESSION-IMMUNE PREAMBLE consolidation *(R11 Pattern — V-05; v12 clarification)*

When C-29 and C-30 both PASS, the two rules share a positional mechanism (both appear in a preamble location). C-31 guards against the failure mode of independent per-rule preamble placement: two adjacent rules with no structural relationship may be treated as separate positional instructions, each subject to independent elision, rather than as a single immune unit. Consolidating both rules under an explicitly designated COMPRESSION-IMMUNE structural section converts two independent positional bets into a single structural contract.

C-31 PASS requires two conditions simultaneously. First: the shared positional mechanism is unified under a section bearing an explicit COMPRESSION-IMMUNE designation (e.g., `=== COMPRESSION-IMMUNE PREAMBLE ===`) that self-declares the section as a structural contract — not merely a descriptive grouping header. Second: each execution note that references the preamble invokes it by its structural designation name ("the COMPRESSION-IMMUNE PREAMBLE") rather than by positional reference ("the preamble above", "the section above"). Bidirectional invocation by name completes the structural circuit: the preamble declares immunity by designation, and the execution notes affirm that authority by invoking the same designation. Without the second condition, the contract is one-directional — the preamble declares immunity, but the execution notes do not confirm it by name, leaving the designated authority unverifiable from the execution-note side.

**PARTIAL — three distinguishable bands:**

- *Band 1 (adjacency):* Two rules co-located by proximity in the preamble region, no section header, no designation. Co-location is structural only by inference.
- *Band 2 (descriptive label):* Two rules grouped under a descriptive header naming the grouping (e.g., "--- COMPRESSION-CRITICAL EXECUTION RULES ---") that identifies the rules as compression-critical but does not declare the section as structurally immune. Execution notes reference the section positionally ("see above"). The label is presentational, not contractual.
- *Band 3 (one-directional contract):* Explicit COMPRESSION-IMMUNE designation present; section self-declares as a structural contract. However, execution notes invoke the preamble positionally rather than by its structural designation name. The contract exists but is one-directional: the preamble declares immunity; the execution notes do not complete the circuit by affirming the designation.

**PASS** — explicit COMPRESSION-IMMUNE designation (e.g., `=== COMPRESSION-IMMUNE PREAMBLE ===`) with self-declaration as a structural contract, AND each execution note referencing the preamble invokes it by its structural designation name. Bidirectional designation invocation is present: the preamble declares immunity by name; the execution notes affirm it by invoking the same name. The structural contract is verifiable from either direction.

**Relationship to C-29/C-30:** C-31 does not replace C-29 and C-30. C-29 and C-30 each require dual-mechanism independence for their respective rules individually. C-31 requires that when both rules coexist, the shared positional mechanism is unified under an explicit COMPRESSION-IMMUNE designation with bidirectional invocation. A skill can satisfy C-29 PASS and C-30 PASS at PARTIAL on C-31 (per-rule dual-mechanism present, but no unified designation with circuit completion).

---

#### C-32 — Closed compression reference loop *(R12 Pattern — V-03 excellence signal)*

C-31 PASS establishes a bidirectional reference circuit: the COMPRESSION-IMMUNE PREAMBLE designates itself immune, and execution notes invoke it by designation. C-32 guards against the failure mode of asymmetric loop specification: the circuit exists (invocation is bidirectional by name) but the preamble's own internal forward-references to companion execution-note mechanisms use positional or generic language rather than designation-based naming. Under single-location compression elision — where the preamble is preserved but execution notes are elided, or vice versa — positional forward-references lose their navigational value while designation-based forward-references remain resolvable.

When C-31 PASS is achieved, the COMPRESSION-IMMUNE PREAMBLE section must also contain explicit forward-references naming each companion execution-note mechanism by its full clause designation and structural location (e.g., "see COMPRESSED-COLLAPSE GUARD in the STATUS execution note" rather than "see the execution note below" or "the declarative guard clause"). This creates a closed reference loop navigable from either entry point: if compression preserves the preamble but elides execution notes, the preamble's forward-references name each companion mechanism by designation, enabling the model to reconstruct the two-mechanism architecture from the preamble side alone. If compression preserves an execution note but elides the preamble, the execution note's back-invocation of the COMPRESSION-IMMUNE PREAMBLE by designation provides structural authority recovery. The full loop is: preamble → names companion execution-note mechanisms by clause designation → execution notes → name the preamble by structural designation → preamble.

**PARTIAL** — C-31 PASS achieved (designation present, bidirectional invocation by name). However, the preamble's internal forward-references use positional language ("the execution note below"), generic clause descriptions ("the declarative guard clause"), or clause-type naming without location specificity. The loop is present but asymmetrically specified: the back-reference from execution notes to preamble is designation-based; the forward-reference from preamble to companion mechanisms is not.

**PASS** — C-31 PASS achieved AND the COMPRESSION-IMMUNE PREAMBLE section contains forward-references that name each companion execution-note mechanism by its full clause designation and execution-note location. The reference architecture is fully closed and designation-specified in both directions. Single-location compression elision is survivable from either entry point: both preamble-first and execution-note-first reconstruction paths are navigable without positional assumptions.

**Prerequisite:** C-32 is only evaluable when C-31 is PASS. A C-31 PARTIAL skill has no structural contract to close a loop around; C-32 assessment is deferred.

**Relationship to C-31:** C-31 closes the circuit at the invocation level (execution notes name the preamble by designation). C-32 closes the circuit at the forward-reference level (preamble names companion mechanisms by designation). Together they create a fully specified closed loop. Independently, C-31 PASS establishes authority invocation; C-32 PASS establishes navigational completeness under partial elision.

---

### Chain summary (v12)

| Chain | Sequence |
|---|---|
| Timestamp isolation | C-16 → C-24 → C-30 |
| Zero-signal synthesis | C-18 → C-21 → C-22 → C-25 → C-27 → C-29 |
| Verdict action-posture | C-06 → C-23 → C-26 → C-28 |
| Compression survival generalization | C-24 → C-29 → C-30 |
| Compression-immune consolidation | C-29 → C-30 → C-31 → C-32 |
