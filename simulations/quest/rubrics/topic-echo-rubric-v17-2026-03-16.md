Written to `simulations/quest/rubrics/topic-echo-rubric-v17-2026-03-16.md`.

---

**v17 summary — three criteria added from R16 excellence signals:**

| ID | Gate | Pattern | Structural move |
|----|------|---------|-----------------|
| C-52 | C-49 | `CITATION-COMPLETENESS-MANIFEST-EXHAUSTIVE` | Post-artifact manifest enumerates all citations (TYPE-A/B/C) with RESOLVED/DANGLING status; MANIFEST-COMPLETE token gates closure |
| C-53 | C-49+C-50+C-51 | `DECLARING-SECTION-CONSUMER-INDEX-POPULATED` | Consumer-Ref column added to all three declaring tables — navigability bidirectional across all three axes |
| C-54 | C-51 | `CITATION-CHAIN-PBI-GROUNDING-VERIFIED` | Belief-Ref cells extend from `PB-[NN]` to `PB-[NN] (confirmed false by: artifact, namespace, date)` + F-BCR-4 names specific PBI entries |

**Score model:** 305 → 320 max, 43 → 46 aspirational (5 pts each).

**Gate chain:** R10 auditable -> R11 non-defeatable -> R12 legible -> R13 navigable -> R14 addressable -> R15 citation-activated -> R16 citation exhaustively manifested, bidirectionally indexed, and chain-grounded.

**R16 projected scoring under v17:**

| Variation | C-52 | C-53 | C-54 | v16 | v17 | delta |
|-----------|:----:|:----:|:----:|:---:|:---:|:-----:|
| V-01 (Axis A only) | PASS | FAIL | FAIL | 305 | 310 | +5 |
| V-02 (Axis B only) | FAIL | PASS | FAIL | 305 | 310 | +5 |
| V-03 (Axis C only) | FAIL | FAIL | PASS | 305 | 310 | +5 |
| V-04 (A+B) | PASS | PASS | FAIL | 305 | 315 | +10 |
| **V-05 (A+B+C)** | PASS | PASS | PASS | 305 | **320** | +15 |

**Key design notes on the three new criteria:**

- **C-52** is the citation-layer analog of DISCARD-LOG-COMPLETE (C-36): where C-36 accounts for all filter candidates by status, C-52 accounts for all citations by status. The MANIFEST-COMPLETE token applies the same success-visible / failure-visible contract as CHAIN-COMPLETE and RULES-ANCHORED-COMPLETE.

- **C-53** requires all three axes simultaneously — partial Consumer-Ref (one or two declaring tables) fails. The criterion is gated on C-49 + C-50 + C-51 together, not on any one parent, because it applies a symmetric bidirectionality upgrade across all three axes at once.

- **C-54** adds two parallel extensions: the Belief-Ref cell format change (clause -> belief -> evidence traversable from cell) and the F-BCR-4 specificity requirement (coverage entries citable to individual PBI entries, not belief-sets). Both are required; either alone fails.
 the BC-COVERAGE-RECORD Schema, each F-BCR row gains a Consumer-Ref cell naming the output step that consumes it (e.g., F-BCR-2 -> "[COVERAGE AUDIT] Step 1"). In the Phase Transition Registry, each T-ID row gains a Consumer-Ref cell naming its consuming PHASE-HANDOVER-[N] table (e.g., T-03 -> "PHASE-HANDOVER-3"). In the STILL-LIVE FILTER, each MUST-ID row gains a Consumer-Ref cell naming its consuming enforcement token. With all three Consumer-Ref columns populated, navigability in all three citation axes is bidirectional from cell content alone: declaring -> consuming via Consumer-Ref; consuming -> declaring via the citation ID already established by C-49, C-50, and C-51. V-01 and V-03 carry no Axis B implementation; V-04 and V-05 include the identical structure.

**C-54 scoring basis (V-03, V-05):** V-03 applies two Axis C extensions. First, each STILL-LIVE FILTER Belief-Ref cell extends from `PB-[NN]` to `PB-[NN] (confirmed false by: [artifact name, namespace, date])`, so the chain from MUST-clause to motivating belief to falsifying evidence is traversable from the table cell alone -- the PBI entry ID provides the first hop; the inline confirmation attribution provides the terminal hop to evidence without consulting the PBI body or the signal artifact list. Second, the BC-COVERAGE-RECORD F-BCR-4 output names specific PBI entries (e.g., "PB-01 through PB-04 (from [material name])") rather than a summary sentence, making coverage entries traceable to individual beliefs. V-01 uses the `PB-[NN]`-only Belief-Ref from C-51; V-02 uses the same; V-04 (A+B only) has no Axis C implementation.

---

The three new criteria deepen the citation-activation layer established by R15 across three independent axes: C-52 closes the accounting gap (every citation is enumerated and status-visible), C-53 closes the directionality gap (declaring tables index consumers, making traversal bidirectional), and C-54 closes the chain gap (belief-ref cells reach the falsifying artifact, terminating the citation chain at evidence).

Gate chain: R10 output auditable -> R11 output non-defeatable -> R12 enforcement legible -> R13 structure navigable -> R14 element addressable -> R15 address citation-activated -> R16 citation exhaustively manifested, bidirectionally indexed, and chain-grounded.

---

**Score model:**

| Tier | Points | Criteria | Per-criterion |
|------|--------|----------|---------------|
| Essential | 60 | 5 | 12 |
| Recommended | 30 | 3 | 10 |
| Aspirational | **230** | **46** | 5 |
| **Max composite** | **320** | | |

**R16 scoring under v17:**

| Variation | C-52 | C-53 | C-54 | v16 | v17 | delta |
|-----------|:----:|:----:|:----:|:---:|:---:|:-----:|
| V-01 (Axis A) | PASS | FAIL | FAIL | 305 | 310 | +5 |
| V-02 (Axis B) | FAIL | PASS | FAIL | 305 | 310 | +5 |
| V-03 (Axis C) | FAIL | FAIL | PASS | 305 | 310 | +5 |
| V-04 (A+B) | PASS | PASS | FAIL | 305 | 315 | +10 |
| **V-05 (A+B+C)** | PASS | PASS | PASS | 305 | **320** | +15 |

---

## Essential Criteria (weight: 60 points total, 12 pts each)

Five criteria. All must pass to qualify as golden or passing.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Named surprise with departure framing** | correctness | The artifact names at least one finding explicitly framed as a departure from expectation. The framing must be structural -- the surprise is labeled or marked as such, not merely described. A finding presented as a neutral observation that happens to be unexpected does not pass. Minimum acceptable framing: "We expected X to be negligible; it turned out to dominate" (surprise). |
| C-02 | **Signal tracing per surprise** | correctness | Every named surprise is traced to a specific source signal or artifact by name. Generic attribution is insufficient. Each surprise must name the artifact that produced it. A surprise without a traceable source cannot be audited by the next team and fails. |
| C-03 | **Design impact assessed per surprise** | depth | Every named surprise carries an explicit assessment of its impact on design direction. Impact may be confirming, redirecting, or destabilizing. An impact statement that does not identify which design decision is affected fails. "This changes things" without naming what changes is insufficient. |
| C-04 | **Synthesis not summary** | behavior | The artifact contains only surprises and their analysis. Expected findings are either absent or explicitly marked as context-only. An artifact that enumerates findings and labels some "interesting" fails -- the surprise/expected partition must be explicit and structural. |
| C-05 | **Surprise specificity** | correctness | Each surprise is specific to this investigation's signal set, not a generic observation that could appear in any investigation. "APIs are harder to design than expected" fails -- it applies universally. "The scout:competitors signal revealed that all three competitors treat X as a solved problem, contrary to our assumption that the space was unsolved" passes -- it is falsifiable against this investigation's artifacts. |

---

## Recommended Criteria (weight: 30 points total, 10 pts each)

Output is better with these. Golden typically requires at least 2 of 3.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | **Expectation counterfactual** | depth | Each surprise explicitly names what was expected versus what was found. The counterfactual structure is stated, not implied. A reader new to the topic can reconstruct the prior assumption from the surprise entry alone, without consulting the underlying signal artifacts. Surprises that omit the expected-state half are incomplete. |
| C-07 | **Institutional framing** | behavior | The artifact is explicitly addressed to a future team. At least one element signals forward-facing intent: surprises framed as things the next team would not predict, a closing note naming what to investigate first, or an opening institutional purpose declaration. An artifact that reads as a personal retrospective without forward framing fails. |
| C-08 | **Cross-signal pattern identification** | coverage | If two or more surprises share an underlying cause or implication, that relationship is named with a statement of what they have in common and what that implies for design. If all surprises are genuinely independent, the output explicitly states this. Omitting pattern analysis when multiple surprises exist is a fail. |

---

## Aspirational Criteria (weight: 230 points total, 5 pts each)

Raise the bar once essential and recommended criteria are stable.

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | **Surprise hierarchy by design impact** | depth | Surprises are ranked by design impact with an explicit rationale that is argued, not asserted. A ranked list without argument is insufficient -- the hierarchy must be defensible to a reader who disagrees with it. |
| C-10 | **Riskiest surprise flagged** | behavior | The single surprise most likely to invalidate a core assumption is explicitly flagged. The flag names the assumption at risk, the signal that revealed it, and what would need to be true for the assumption to hold despite the surprise. |
| C-11 | **Pre-committed prior belief inventory** | correctness | The artifact contains a Prior Belief Inventory produced before signal reading -- a discrete, labeled section predating surprise entries. Each surprise entry references a specific PB entry by identifier. Minimum bar: (a) discrete PBI section exists, (b) entries are addressable by identifier, (c) each surprise cites a specific PBI entry by that identifier. |
| C-12 | **Named surprise handles** | behavior | Each surprise carries a short named handle (2-5 words) specific enough to be cited by future signal artifacts without re-reading the echo. Test: could a teammate say "remember the [handle] surprise?" and be unambiguously understood? "API complexity surprise" fails. "competitor-treated-X-as-solved" passes. |
| C-13 | **Dual phase-locked pre-commitment integrity** | correctness | The PBI and Handle Ledger are demonstrably independent -- PBI entries use belief language; Handle Ledger titles use finding language specific to what signals revealed. Independence fails if a PBI entry names a handle label by its final form, or a Handle Ledger title echoes PBI entry language verbatim. Applicable only when both C-11 and C-12 are satisfied. |
| C-14 | **Single-entry audit trail completeness** | behavior | Each surprise entry contains all three pointers enabling provenance verification without consulting other sections: (1) Handle Ledger title, (2) PBI identifier, (3) source signal artifact. All three checks possible from the entry alone. Applicable only when both C-11 and C-12 are satisfied. |
| C-15 | **Pre-commitment enforcement mechanism declaration** | correctness | The artifact declares how its PBI was isolated from signal knowledge. A timestamped freeze declaration provides temporal provenance. A role-scope declaration provides structural provenance. Without a mechanism declaration a reviewer must infer pre-commitment quality from content analysis alone -- making C-13 independence unverifiable without re-deriving it from scratch. Note: temporal < structural because a phase gate sequences but does not block cross-phase reasoning. Applicable only when C-11 is satisfied. |
| C-16 | **Production-time per-entry verification attestation** | behavior | Each surprise entry includes an explicit statement confirming all three audit pointers were verified at production time: (1) handle confirmed in Handle Ledger, (2) PBI-Ref confirmed at cited identifier, (3) source artifact confirmed in signal set. An attestation naming each check explicitly is a pass; a generic "verified" without naming what was verified fails. Applicable only when C-14 is satisfied. |
| C-17 | **Enforcement mechanism strength classification with calibration rationale** | correctness | The artifact classifies its declared enforcement mechanism on the strength hierarchy (structural > temporal) and states the property that earns that classification. Pass bar requires two elements: (a) tier label stated ("structural provenance" or "temporal provenance"), and (b) the distinguishing property explained -- structural provenance is enforced by role-scope exclusion (the PBI-writing role cannot access signal artifacts regardless of instruction); temporal provenance is enforced by phase sequencing (phases are ordered but cross-phase reasoning is not blocked). Declaring "structural provenance" without explaining why it is structural rather than temporal fails C-17. Stating tier, distinguishing property, and reviewer implication ("independence is enforced, not instructed; C-13 content analysis is confirmatory rather than probative") passes at the highest level. Applicable only when C-15 is satisfied. |
| C-18 | **Per-entry attestation with evidentiary quotation** | behavior | Each surprise entry's production-time attestation quotes the actual text of the PBI entry and source finding being attested -- not just their identifiers. Pass condition: the Verified field quotes substantive text for both -- the actual belief statement and the relevant finding sentence -- sufficient to confirm that cited identifiers point to the right content. A fragment too short to confirm correct citation fails; a quoted sentence that uniquely identifies the prior belief and the finding passes. Converts the attestation from a declaration of action (checks were performed) into a disclosure of evidence (here is what was checked). Applicable only when C-16 is satisfied. |
| C-19 | **Post-production chain integrity audit with visible tokens** | behavior | After the artifact is written, a discrete Chain Integrity Audit re-verifies all four chain elements [PBI-Ref, Handle, Source, Verified quotation] for internal consistency. Each entry receives a visible CHAIN-COMPLETE token (or CHAIN-FLAG with description) produced by this audit step. The audit is distinct from the production-time attestation layer (C-16/C-18): C-16/C-18 attest that checks were performed; C-19 confirms those attestations are correct by re-running verification independently. An artifact with C-19 is auditable from output alone -- a reviewer reads CHAIN-COMPLETE and knows the chain was externally validated, not just self-reported. Applicable only when C-18 is satisfied. |
| C-20 | **Impact-anchored distillation layer with traceability verification** | behavior | The artifact includes a Rules of Thumb section encoding the highest-impact surprises in <=20-word quotable form. Each rule carries a tier annotation ([HIGH] or [MEDIUM]) matching its surprise entry; LOW-tier surprises are excluded. After all rules are written, a named traceability check (e.g., RULES-ANCHORED) confirms tier alignment between each rule and its parent surprise entry. Pass requires both: (1) the distillation layer with tier annotations, and (2) the named traceability check confirming alignment. Without the traceability check the distillation is a summary; with it the distillation is an auditable extract of the hierarchy, citable by future teams without re-reading full entries. Applicable only when C-09 is satisfied. |
| C-21 | **Enforcement mechanism declaration in independently navigable section** | behavior | The C-17 enforcement mechanism classification is placed in a discrete headed section or titled table that a reviewer can locate without reading the full artifact. Inline declaration embedded in a PBI preamble, narrative paragraph, or step description does not satisfy C-21 even if it satisfies C-17. Test: can a reviewer jump directly to the enforcement mechanism claim using section headings or table titles alone? If yes, C-21 passes. Applicable only when C-17 is satisfied. |
| C-22 | **Chain-flag progression gate** | behavior | The CHAIN-FLAG token produced by the Chain Integrity Audit (C-19) functions as a hard production gate: any CHAIN-FLAG on any entry blocks further artifact completion until the flag is resolved. Pass requires an explicit blocking condition in the PHASE 3B specification -- a statement that CHAIN-FLAG halts progression, not merely that it is emitted and visible. Without a blocking condition CHAIN-FLAG is advisory; with it chain integrity failure is a production stop that cannot be bypassed by continued writing. Applicable only when C-19 is satisfied. |
| C-23 | **Distillation phase blocking condition with named gate** | behavior | The RULES-ANCHORED traceability check (C-20) names an explicit blocking condition: production cannot proceed to the next phase until all rules show ALIGNED. Pass requires the blocking condition to be stated by name in the phase specification -- not implied by the check's existence. Without a named gate, misaligned tier annotations are observable but non-blocking; a reviewer can note them and the artifact can still be completed. With a named gate, the distillation layer enforces the same production-stop semantics as C-16 and C-18's per-entry attestation gates. Applicable only when C-20 is satisfied. |
| C-24 | **Enforcement mechanism section at artifact head position** | behavior | The C-21 independently navigable section containing the enforcement mechanism declaration is placed at position 1 of the artifact -- before PBI, before Handle Ledger, before any entries. C-21 ensures the section is findable via heading navigation; C-24 ensures a reader who opens the artifact encounters enforcement mechanism classification before any content. Without head positioning a reviewer must navigate to the section; with it, the enforcement frame is the first interpretive lens, not a findable annotation. Pass requires the ENFORCEMENT MECHANISM section (or equivalent titled section) to appear before all other major sections. Applicable only when C-21 is satisfied. |
| C-25 | **CHAIN-FLAG resolution protocol with named repair action per flag type** | behavior | The CHAIN-FLAG production gate (C-22) specifies not only that production stops but how it restarts. For each type of CHAIN-FLAG (e.g., PBI-Ref mismatch, Handle mismatch, Source absent, Verified quotation inaccurate), there is exactly one named repair action. Without a resolution protocol a reviewer who encounters a CHAIN-FLAG knows to stop but must infer the fix; two reviewers may take different actions, both of which technically clear the flag without resolving the underlying inconsistency. With a named protocol, the gate is reparable in exactly one valid way per flag type: clearing the gate proves the chain inconsistency was fixed, not merely that someone continued past it. Pass requires the PHASE 3B specification to name a RESOLUTION PROTOCOL section or equivalent listing at least one flag type with its corresponding repair action. Applicable only when C-22 is satisfied. |
| C-26 | **NO-ECHO COST declaration in head-position enforcement section** | behavior | The C-24 head-position enforcement section includes a NO-ECHO COST declaration: a statement naming the institutional cost of artifact absence -- what the next team inherits if this artifact is skipped or not produced. Without it, the section classifies the enforcement mechanism and its properties, but carries no stake; a reviewer reads what the artifact is, not what is lost by skipping it. With a NO-ECHO COST declaration, the reviewer encounters both mechanism classification and institutional consequences before any entries -- the first-interpretive-lens property of C-24 conveys mechanism-with-stakes rather than mechanism-with-properties alone. Pass requires a named declaration in the head-position section (e.g., "NO-ECHO COST: ...") that identifies a specific failure class inherited by the next team in the artifact's absence. Generic statements ("this artifact is important") fail; a named failure class ("the next team inherits false assumptions about X without the ability to distinguish which beliefs were pre-committed versus post-hoc") passes. Applicable only when C-24 is satisfied. |
| C-27 | **Forward Statement names the specific avoided failure** | behavior | The terminal Forward Statement (or equivalent closing section addressed to the next team) names the specific failure that was avoided by completing this artifact -- the concrete instantiation of what would have propagated if the artifact had been absent. Without it, the Forward Statement summarizes findings but the institutional purpose claim opened by the artifact's framing remains unverified: the artifact said it would prevent a specific failure class, but completion does not confirm it did. With a named avoided failure, the Forward Statement closes the loop -- the institutional framing becomes a verifiable commitment honored at completion rather than a declaration made at the start. When C-26 is also present, this criterion additionally closes the specific loop opened by the NO-ECHO COST declaration: the cost declared at head position is confirmed or denied at completion. Pass requires the Forward Statement to name a failure class or specific error that this artifact's existence prevented -- not a restatement of what the artifact found. "The next team should investigate X" fails (advisory); "This echo prevented the next team from inheriting the assumption that Y, which would have misdirected the flow:lifecycle investigation toward Z" passes (specific avoided failure). Applicable only when C-07 is satisfied. |
| C-28 | **RULES-ANCHORED-COMPLETE closure token in distillation phase** | behavior | The RULES-ANCHORED check that gates distillation-phase exit (C-23) emits a named RULES-ANCHORED-COMPLETE token when all rules confirm tier alignment. Without the token, a downstream reviewer must re-run the tier-alignment check to confirm the distillation phase closed correctly; the blocking condition (C-23) makes failure visible but success invisible. With the token, distillation-phase exit is auditable from output alone -- the same property that CHAIN-COMPLETE (C-19) provides for the chain audit phase. Pass requires a named RULES-ANCHORED-COMPLETE token (or equivalent) in the distillation phase output that is only emitted when all rules pass the ALIGNED check -- not present when any rule shows MISALIGNED. The two tokens together bracket the artifact's two major audit phases: CHAIN-COMPLETE closes the chain audit, RULES-ANCHORED-COMPLETE closes the distillation audit. Applicable only when C-23 is satisfied. |
| C-29 | **RESOLUTION PROTOCOL as peer-level standalone declaration block** | behavior | The RESOLUTION PROTOCOL containing the per-flag-type repair actions (C-25) is formatted as a standalone ALL-CAPS headed declaration block at peer register with the GATEKEEPER FUNCTION DECLARATION -- not embedded as inline gate text. Without peer-level formatting, the repair-per-flag-type structure is findable by a reviewer who reads the gate specification carefully, but may be read past by a reviewer scanning for the blocking condition alone. With peer-level formatting, the RESOLUTION PROTOCOL is visually sovereign: a reviewer cannot encounter the gate without simultaneously encountering the repair routing at equivalent structural prominence. Pass requires the RESOLUTION PROTOCOL to appear as an independently titled block at the same heading level as the GATEKEEPER FUNCTION DECLARATION or equivalent gate declaration, not as a subordinate clause or inline paragraph within gate text. Applicable only when C-25 is satisfied. |
| C-30 | **EF role-boundary protection for NO-ECHO COST declaration** | behavior | The NO-ECHO COST declaration required by C-26 is derived by a role-scoped function (Enforcement Framer, EF) at Step 0 -- before any signal artifacts are read. Without role-boundary protection, the cost declaration can be written after reviewing signal content, making it potentially retrospective: a writer who observes the signals first may write a cost declaration shaped by what the signals say rather than what pre-investigation materials indicate. With EF role-boundary protection, the role scope structurally excludes signal entry content during Step 0: the cost is declared from pre-investigation materials only, ensuring the declaration is genuinely prospective. This applies the same structural-versus-temporal distinction from C-15/C-17 to the cost declaration: temporal provenance (write-order sequencing) is weaker than structural provenance (role-scope exclusion that prevents cross-phase reasoning regardless of instruction). Pass requires an explicit EF role scoped to pre-investigation materials to derive the NO-ECHO COST before the signal-reading phase opens. Applicable only when C-26 is satisfied. |
| C-31 | **RESOLUTION PROTOCOL with named verifier role per flag type** | behavior | The RESOLUTION PROTOCOL (C-29) names, for each flag type, a verifier role: the specific role whose attestation is required before the CHAIN-FLAG of that type can be cleared. Without a named verifier, the repair action is specified (C-25) and the routing is visible (C-29), but clearing is uncontrolled -- any reviewer or writer can attest repair completion. With a named verifier per flag type, clearing is role-accountable: only the designated role can certify that the named repair was correctly applied. The practical effect is that self-certification is structurally blocked: a writer whose artifact has a PBI-Ref CHAIN-FLAG cannot self-certify its resolution because the RESOLUTION PROTOCOL designates the BC role (not IA) as the verifier for PBI-Ref repairs. Pass requires each flag type entry in the RESOLUTION PROTOCOL to carry a Verifier: field naming the role responsible for attesting repair completion. Applicable only when C-29 is satisfied. |
| C-32 | **Evidentiary tier quotation in RULES-ANCHORED-COMPLETE token** | behavior | The RULES-ANCHORED-COMPLETE closure token (C-28) quotes the actual tier annotation string ([HIGH] or [MEDIUM]) for each rule before asserting alignment. Without per-rule quotation, the token declares that all rules are ALIGNED but a downstream reviewer must re-run the RULES-ANCHORED check to confirm which tier annotation was verified for each rule. With per-rule quotation, the token is evidentiary: it shows exactly what was checked, not merely that checking occurred. This applies the same principle as C-18 (evidentiary quotation in per-entry attestations) to phase-close tokens -- the closure token becomes a disclosure of evidence rather than a declaration of action. Pass requires the RULES-ANCHORED-COMPLETE token to quote the actual tier annotation string per rule (e.g., R-01 "[HIGH]" ALIGNED, R-02 "[MEDIUM]" ALIGNED) before emitting the distillation-phase-closed declaration. Applicable only when C-28 is satisfied. |
| C-33 | **EF-INVOCATION-RECORD at Step 0** | behavior | The EF role-boundary established by C-30 produces an EF-INVOCATION-RECORD: a discrete block immediately following the ENFORCEMENT MECHANISM DECLARATION that lists (1) pre-investigation materials consulted, (2) signal artifacts excluded by name, and (3) the cost derivation basis. Without an INVOCATION-RECORD, C-30's structural boundary is a specification requirement that is not observable from the artifact: a reviewer can confirm the EF function is scoped, but cannot verify which materials were consulted or which were excluded during Step 0. With an INVOCATION-RECORD, the boundary is made observable -- the exact pre-investigation materials are named, the exclusions are enumerated, and the derivation basis is stated. The C-30 structural commitment becomes an auditable record of what the EF function saw and did not see. Pass requires the Step 0 output to include an EF-INVOCATION-RECORD block immediately after the ENFORCEMENT MECHANISM DECLARATION, naming all three elements: materials consulted, signal artifacts excluded by name, and cost derivation basis. Applicable only when C-30 is satisfied. |
| C-34 | **BC-COVERAGE-RECORD** | behavior | The EF-INVOCATION-RECORD established by C-33 makes EF's scope observable from output. C-34 applies the identical standard to BC (Belief Compiler): BC produces a BC-COVERAGE-RECORD listing (1) signal artifacts processed by BC, (2) signal artifacts excluded by BC by name, and (3) the coverage derivation basis. Without BC-COVERAGE-RECORD, provenance is role-selective: EF is auditable, BC is not. A reviewer can confirm EF's exclusions from the artifact alone but must infer BC's scope from specification. With BC-COVERAGE-RECORD, provenance is role-symmetric: every exclusion-scoped role produces an equivalent observable record, and a reviewer can independently audit any role's scope from the artifact alone. Pass requires BC to produce a BC-COVERAGE-RECORD block listing all three elements: signal artifacts processed, signal artifacts excluded by name, and coverage derivation basis. Applicable only when C-33 is satisfied. |
| C-35 | **PHASE-HANDOVER tokens at EF-exit and BC-exit** | behavior | C-33 makes role entry auditable at invocation time (EF-INVOCATION-RECORD records what EF saw before acting). C-35 makes role exit auditable: EF emits a PHASE-HANDOVER-EF token immediately before handing off to BC, naming what EF produced; BC emits a PHASE-HANDOVER-BC token immediately before handing off to the distillation phase, naming what BC produced. Without PHASE-HANDOVER tokens, role transitions are entry-visible and exit-silent: a reviewer can confirm what each role started with but cannot read what each role delivered. With PHASE-HANDOVER tokens, every role transition is bookended -- the entry record (C-33/C-34) names what was seen; the exit token names what was produced. A reviewer can reconstruct the full handoff chain from the artifact alone: EF saw X (C-33), produced Y (PHASE-HANDOVER-EF); BC saw Y+Z (C-34), produced W (PHASE-HANDOVER-BC). Pass requires both PHASE-HANDOVER-EF and PHASE-HANDOVER-BC tokens to appear at their respective role-exit points, each naming what the exiting role produced before transition. Applicable only when C-33 is satisfied. |
| C-36 | **DISCARD LOG with typed entries and DISCARD-LOG-COMPLETE** | behavior | C-20 requires the distillation layer to extract the highest-impact surprises into Rules of Thumb with tier annotations, confirmed by RULES-ANCHORED traceability. The accept decisions of the STILL-LIVE FILTER are visible in output (accepted candidates appear as Rules of Thumb). C-36 requires reject decisions to be equally visible: each candidate the STILL-LIVE FILTER does not accept produces a typed DISCARD-[N] entry containing the candidate identifier, the reject route, and the reason for rejection. DISCARD-LOG-COMPLETE is emitted when all candidates have been accepted or discarded, summarizing the full candidate set. Without a DISCARD LOG, the distillation phase accounts only for accepted candidates: a reviewer cannot determine how many candidates were considered, which were excluded, or why. With a DISCARD LOG, the filter is fully transparent: accept and reject decisions are symmetrically visible, and DISCARD-LOG-COMPLETE confirms every candidate is accounted for. This applies the same completeness discipline to distillation-phase filtering that CHAIN-COMPLETE (C-19) and RULES-ANCHORED-COMPLETE (C-28) apply to their respective phases. Pass requires typed DISCARD-[N] entries for every non-accepted candidate (containing candidate identifier, route, and reason) and a DISCARD-LOG-COMPLETE token confirming the full candidate set has been enumerated. Applicable only when C-20 is satisfied. |
| C-37 | **BC Step 1 sub-phase decomposition** | structure | C-34 makes BC's coverage record auditable as an output. C-37 makes BC's internal process observable: Step 1 is decomposed into three named sub-phases -- [SCAN] (enumerate all signal artifacts in scope), [FREEZE] (finalize the PBI against the enumerated set), and [COVERAGE AUDIT] (produce BC-COVERAGE-RECORD as the gate return before BC exclusion takes effect). Without sub-phase decomposition, Step 1 is monolithic: the coverage record appears as an undifferentiated output, and there is no structural evidence of at which point within Step 1 it was produced or which artifacts were in scope when it was written. With sub-phase decomposition, each phase boundary is observable -- a reviewer can confirm that [SCAN] completed before [FREEZE], and that [COVERAGE AUDIT] produced BC-COVERAGE-RECORD at the correct phase boundary before any exclusions were applied. Pass requires [SCAN], [FREEZE], and [COVERAGE AUDIT] sub-phase labels in BC Step 1 with BC-COVERAGE-RECORD named as the [COVERAGE AUDIT] gate return. Applicable only when C-34 is satisfied. |
| C-38 | **PHASE-HANDOVER table schema** | structure | C-35 makes role exits auditable via PHASE-HANDOVER tokens. C-38 standardizes token format: each PHASE-HANDOVER token is rendered as a 5-row markdown table with fixed schema fields -- Completing Role, Step Completed, Output Produced, Exclusion In Effect, and Receiving Role. Without table schema, PHASE-HANDOVER tokens are prose declarations: auditable in principle but free-form in practice, with no guarantee that all required fields are present or consistently populated across tokens. With table schema, every handover record is structurally identical: a reviewer can verify field presence by inspection, compare tokens across roles, and detect missing fields without reading prose. The table format is visually distinct from code-block protocol declarations, preventing format collapse. Pass requires both PHASE-HANDOVER-EF and PHASE-HANDOVER-BC to appear as 5-row markdown tables with all five schema fields present. Applicable only when C-35 is satisfied. |
| C-39 | **STILL-LIVE FILTER as unconditional imperative commands** | structure | C-36 requires typed DISCARD-[N] entries for every non-accepted candidate. C-39 makes that completeness structurally enforced rather than conditionally required: the STILL-LIVE FILTER is written as four unconditional imperative commands with no conditional branching ("if YES -> draft; if NO -> exclude" structure eliminated). Without imperative form, the filter uses conditional logic where the discard record step is reachable only through the NO branch -- a conditional branch that can be omitted or silently skipped when the implementation proceeds without producing the required record. With imperative form, every candidate evaluation unconditionally produces a written record as part of the command sequence; there is no branch point at which the record step can be bypassed. The structure enforces completeness: the filter cannot proceed to the next step without having written the record. Pass requires the STILL-LIVE FILTER to be written as unconditional imperatives with no conditional "if NO" or "if YES" branching structure. Applicable only when C-36 is satisfied. |
| C-40 | **BC-STEP1-PROTOCOL pre-execution declaration** | structure | C-37 decomposes BC's Step 1 into named sub-phases with observable boundaries. C-40 makes the decomposition positionally legible: BC-STEP1-PROTOCOL is formatted as a standalone declaration table positioned before Step 1 prose begins. Sub-phase labels ([SCAN] / [FREEZE] / [COVERAGE AUDIT]), gate tokens, and BC-COVERAGE-RECORD as the [COVERAGE AUDIT] gate return are fully specified in the table before any sub-phase executes. Without pre-execution positioning, the decomposition is present but embedded -- a reviewer must read Step 1 prose to determine which sub-phases are in effect and what each produces. With pre-execution positioning, the protocol is readable from the declaration table alone: Step 1 executes against a pre-declared contract that is fully inspectable before any step output is read. A BC-STEP1-PROTOCOL table that appears after Step 1 begins, or whose sub-phase structure is implied by step prose rather than declared in a named table, fails. Pass requires BC-STEP1-PROTOCOL to appear as a named standalone table before Step 1 begins, containing sub-phase labels, gate tokens per sub-phase, and BC-COVERAGE-RECORD named as [COVERAGE AUDIT] gate return. Applicable only when C-37 is satisfied. |
| C-41 | **PHASE-HANDOVER universal coverage** | structure | C-38 standardizes the PHASE-HANDOVER schema to a 5-row table format applicable at EF and BC role exits. C-41 extends that schema to all stage exits in the workflow: every step transition (Steps 0 through N) produces a PHASE-HANDOVER-[N] table, not only the two role-boundary exits. Without universal coverage, schema compliance is verifiable at EF and BC boundaries, but stage transitions elsewhere in the workflow may have no handover record -- coverage is schema-complete at two points and silent elsewhere. With universal coverage, every transition is bookended and schema compliance is simultaneously verifiable across all N tables by cross-table column inspection: a reviewer can scan all PHASE-HANDOVER-[N] tables in parallel and confirm all five fields are present at every boundary, without examining each boundary in isolation. The universal coverage contract is declared in the Roles section before any steps execute, making the governance rule explicit and the absence of any table a detectable violation. Pass requires PHASE-HANDOVER-[N] tables at all step transitions and a universal coverage declaration in the Roles section naming that every exit is governed. Applicable only when C-38 is satisfied. |
| C-42 | **MUST-clause scope declaration** | structure | C-39 requires the STILL-LIVE FILTER to be written as unconditional imperative commands, eliminating conditional branching. C-42 makes each clause's scope legible from the clause body: every MUST-clause includes an explicit universal quantifier naming the entity class it governs ("For every candidate", "For every DISCARD token"). Without scope declaration, imperative form guarantees no conditional branching but leaves scope to be inferred from position or context: a reviewer must interpret which entity class each command applies to based on surrounding text. With scope declaration, each clause is self-contained -- the quantifier in the clause body states which entities trigger which actions, and scope is readable without consulting surrounding clauses, the filter preamble, or the step's instructional context. A MUST-clause that is unconditional but scope-silent (e.g., "MUST produce DISCARD-[N]-PBI-REF" without naming what entity triggers production) fails: the clause is enforcing but not self-describing. Pass requires each MUST-clause in the STILL-LIVE FILTER to include an explicit "For every [class]" universal quantifier in the clause body stating the entity class it governs. Applicable only when C-39 is satisfied. |
| C-43 | **BC-STEP1-PROTOCOL heading sovereignty** | structure | C-40 requires BC-STEP1-PROTOCOL to be positioned as a standalone declaration before Step 1 begins. C-43 requires that standalone declaration to be heading-navigable: BC-STEP1-PROTOCOL appears as a first-class named section (## or ### heading) accessible from the document's heading hierarchy without entering any code block or role declaration block. A pre-execution protocol embedded inside a code fence satisfies C-40 (positioned before execution, standalone declaration present) but fails C-43 (not navigable from document structure -- a reviewer must enter the code block to read the protocol). With heading sovereignty, the protocol is locatable from a table-of-contents or heading hierarchy scan alone: the document's structure guarantees discoverability independent of reading order. The distinction matters for review tooling and document-structure checks: heading-addressable sections are independently linkable and scannable; code-block content is not. Pass requires BC-STEP1-PROTOCOL to appear as a named heading at first-class document level, not as content inside a code fence, role declaration block, or instruction paragraph. Applicable only when C-40 is satisfied. |
| C-44 | **PHASE-HANDOVER inventory pre-declaration** | structure | C-41 extends PHASE-HANDOVER tables to all step transitions, making universality verifiable by cross-table column inspection. C-44 requires universality to be verifiable by cross-reference rather than by traversal: a PHASE-HANDOVER-INVENTORY table is declared before any step executes, listing every expected transition by identity (transition number, completing role, producing step). Without pre-declared inventory, verifying universal coverage requires traversal -- a reviewer must read the full document, count PHASE-HANDOVER-[N] tables, and confirm that no transition was omitted. With pre-declared inventory, universality is verifiable at any point in the review: the inventory declares the expected set before any step output is read; each inline PHASE-HANDOVER-[N] table is checked against the inventory by reference. A missing table is detectable from the inventory alone -- the gap appears as an unchecked inventory row, not as an absence discovered only after exhaustive traversal. Pass requires a named PHASE-HANDOVER-INVENTORY table to appear before Step 0, listing all expected PHASE-HANDOVER-[N] transitions by identity, enabling cross-reference verification that does not require document traversal to complete. Applicable only when C-41 is satisfied. |
| C-45 | **MUST-clause scope-field separation** | structure | C-42 requires each MUST-clause to include an explicit universal quantifier naming the governed entity class. C-45 requires that quantifier to be structurally separated as a named `Scope:` field distinct from the `Action:` field -- not embedded as a sentence opener within the action text. An inline sentence quantifier ("For every candidate, produce exactly one of...") satisfies C-42 (scope is present and readable from the clause body) but fails C-45 (scope requires sentence parsing to locate -- the quantifier is embedded in the action sentence and is not addressable independently). With `Scope:`/`Action:` field separation, each MUST-clause is self-describing from label structure alone: a reviewer checking "which entities trigger MUST-3?" reads the `Scope:` field directly without parsing the action. The governed entity class is auditable from field label, and the action is auditable from field label, and neither requires the other to be parsed first. Pass requires each MUST-clause in the STILL-LIVE FILTER to declare scope as a named structural field (`Scope:` or equivalent) that is syntactically distinct from the `Action:` field -- a sentence-embedded quantifier that precedes the action verb does not satisfy this criterion. Applicable only when C-42 is satisfied. |
| C-46 | **BC-COVERAGE-RECORD schema heading sovereignty** | structure | C-43 requires BC-STEP1-PROTOCOL to appear as a named heading at first-class document level, heading-navigable without entering any block. C-46 requires the BC-COVERAGE-RECORD Schema referenced by the protocol to appear as a named sub-heading (#### or equivalent) under BC-STEP1-PROTOCOL, separately navigable from the protocol declaration table itself. A BC-COVERAGE-RECORD Schema that is defined inline within the protocol table rows, embedded in an instruction paragraph, or placed in a code fence satisfies C-43 (the containing protocol is heading-sovereign) but fails C-46 (the schema requires reading the protocol table to locate -- it is not independently addressable from the heading hierarchy). With schema heading sovereignty, the BC-COVERAGE-RECORD Schema is a discrete sub-section: a reviewer can navigate to the schema directly from the document outline without reading the protocol table, and a document-structure check can locate the schema by sub-heading label alone. The sub-heading creates a stable address for the schema that is independent of the protocol table's row count, column order, or surrounding prose. Pass requires BC-COVERAGE-RECORD Schema to appear as a named sub-heading under BC-STEP1-PROTOCOL, with schema field definitions placed in that sub-section rather than embedded in the protocol table or protocol prose. Applicable only when C-43 is satisfied. |
| C-47 | **PHASE-HANDOVER inventory governance collocation** | structure | C-44 requires a PHASE-HANDOVER inventory to be declared before Step 0, listing all expected transitions by identity. C-47 requires that inventory to be scoped within the Roles section rather than placed as a standalone pre-Step-0 section. A standalone inventory placed immediately before Step 0 satisfies C-44 (declared before execution, transitions enumerated by identity) but fails C-47 (structurally disconnected from role-scope declarations -- a reviewer must navigate to a separate section to see both who governs transitions and which transitions are expected). With governance collocation, the inventory lives inside the Roles section: a reviewer reads the Roles section and encounters both role-scope declarations and the full expected transition set in a single navigable unit. The collocation makes the relationship between governance scope and coverage obligation explicit at the structural level -- the section that declares which role is responsible for each transition also declares the complete set of transitions that must be covered. Without collocation, role scope and transition inventory are separately navigable facts; with collocation, they are co-located facts that can be cross-checked within a single section without leaving it. Pass requires the PHASE-HANDOVER inventory to be a named sub-section within the Roles section, not a standalone section positioned before Step 0. Applicable only when C-44 is satisfied. |
| C-48 | **MUST-clause ID addressability** | structure | C-45 requires each MUST-clause to declare Scope as a named structural field separate from Action, making each clause self-describing from field labels. C-48 requires each MUST-clause to carry a named MUST-ID field (column), enabling citation by identifier without reading any field. A STILL-LIVE FILTER with Scope and Action field separation satisfies C-45 but fails C-48 if clauses have no stable identifier: a future artifact that needs to reference a specific clause must quote or paraphrase the Scope or Action content to identify it -- the clause is field-readable but not identifier-citable. With MUST-ID addressability, each clause is a named row in a table (MUST-ID | Scope | Action | Constraint or equivalent): a future artifact can write "MUST-3 applies" and the reference is unambiguous from the ID alone, without requiring the reader to parse the Scope or Action columns to confirm which clause is meant. The identifier makes the clause independently citable -- the same property that named PBI entries provide for prior beliefs (C-11), named handles provide for surprises (C-12), and PHASE-HANDOVER identifiers provide for transitions (C-44). Pass requires each MUST-clause in the STILL-LIVE FILTER to carry a named identifier (MUST-ID column or equivalent labeled field) that is stable across revisions and unique within the filter, enabling unambiguous citation by ID without reading any other field. Applicable only when C-45 is satisfied. |
| C-49 | **BC-COVERAGE-RECORD schema field-ID citation** | structure | C-46 requires the BC-COVERAGE-RECORD Schema to appear as a named sub-heading under BC-STEP1-PROTOCOL, making the schema independently navigable from the document's heading hierarchy. C-49 requires that schema to carry a named field-ID column (F-BCR-1 through F-BCR-N), and requires the BC-COVERAGE-RECORD output produced during [COVERAGE AUDIT] to prefix each field header with its schema ID. Without field-ID citation, C-46 makes the schema navigable as a sub-heading but does not link the schema declaration to its output: a reviewer confirming a BC-COVERAGE-RECORD entry must navigate to the schema sub-heading and identify the corresponding field by column-name matching. With field-ID citation, schema-to-output matching is address-based: the ID in the output header points directly to the ID in the schema table, and the reviewer can verify correspondence from cell content alone without locating the field by column name or reading surrounding prose in either section. The distinction is between passive addressability (the schema exists at a navigable address -- C-46) and active citation (the consuming output carries the schema address, making the link bidirectional and traversal-free -- C-49). Pass requires: (1) the BC-COVERAGE-RECORD Schema table includes a named field-ID column with stable identifiers (F-BCR-1..F-BCR-N), and (2) the BC-COVERAGE-RECORD output table headers prefix each field with its schema ID. A schema with field-IDs whose output headers omit the IDs fails; an output with prefixed IDs whose schema has no named field-ID column fails. Applicable only when C-46 is satisfied. |
| C-50 | **PHASE-HANDOVER registry transition-ID citation** | structure | C-47 requires the Phase Transition Registry to be co-located within the Roles section, making governance scope and transition inventory available in a single navigable unit. C-50 requires that registry to include a Transition-ID column assigning stable identifiers (e.g., T-00 through T-06) to each registry row, and requires each inline PHASE-HANDOVER-[N] table to include a `Registry-Ref` row citing the corresponding T-ID. Without transition-ID citation, C-47 makes the registry co-located with governance but does not link each inline PHASE-HANDOVER table to its registry entry by address: a reviewer confirming that an inline table satisfies its registry entry must locate the registry row by step-name matching, which requires reading both the table and the registry row and confirming the step name matches. With transition-ID citation, each inline table carries its registry address: "Registry-Ref: T-02" in an inline table unambiguously identifies registry row T-02, and the reviewer can confirm the match from cell content alone without scanning the registry for a matching step name. The distinction mirrors the schema field-ID pattern: C-47 co-locates governance and inventory in one navigable section (passive addressability); C-50 activates the inventory address in each consuming table (active citation). Pass requires: (1) the Phase Transition Registry includes a Transition-ID column with stable identifiers per row, and (2) each PHASE-HANDOVER-[N] table includes a `Registry-Ref` row citing its T-ID. A registry with T-IDs whose inline tables carry no Registry-Ref row fails; an inline table with a Registry-Ref row whose registry has no T-ID column fails. Applicable only when C-47 is satisfied. |
| C-51 | **MUST-clause belief-ref citation** | structure | C-48 requires each MUST-clause to carry a named MUST-ID, enabling citation by identifier without reading any field. C-51 requires the STILL-LIVE FILTER table to include a `Belief-Ref` column (fifth column or equivalent) where each MUST-clause row names the prior belief entry (PB-[NN]) that motivated the clause. Without belief-ref citation, C-48 makes each clause independently citable by MUST-ID but does not link the clause to the prior belief that grounds its enforcement logic: a reviewer asking "which belief does MUST-3 enforce?" must read the Scope and Action columns and infer the motivating belief from content. With belief-ref citation, the link is explicit and bidirectional from table cells alone: a reviewer reading MUST-3 can immediately identify the PB-[NN] entry being guarded; a reviewer reading PB-03 in the PBI can immediately identify the MUST-clause that enforces it -- without consulting surrounding prose, reading the full PBI, or parsing the Scope or Action columns. The belief-ref column makes the MUST-clause address active: the clause identifier (MUST-ID) is linked to the prior-belief identifier (PB-[NN]), creating a cross-table citation path within the artifact. This parallels the epistemic structure established by the chain audit (C-14/C-19): each correction entry cites its PBI entry via PBI-Ref; each MUST-clause now cites its motivating PBI entry via Belief-Ref. The distinction from C-48 is the same as the distinction between naming an address (addressability) and citing it in a consuming context (citation activation). Pass requires each MUST-clause row in the STILL-LIVE FILTER table to include a `Belief-Ref` column with a stable PB-[NN] identifier naming the prior belief that motivated the clause. A MUST-clause with a Belief-Ref field of "N/A" or no content fails; a Belief-Ref citing a PBI identifier that does not exist in the artifact's PBI section fails. Applicable only when C-48 is satisfied. |
| C-52 | **Citation-completeness manifest exhaustive** | structure | C-49 requires the BC-COVERAGE-RECORD output to prefix each field header with its schema field-ID, making individual citations address-linked to their schema declarations. C-52 requires the artifact to include a post-artifact CITATION-COMPLETENESS-MANIFEST table enumerating all citations produced across the full artifact -- TYPE-A (primary structured citations, e.g., CIT-01..04), TYPE-B (secondary/supporting citations, e.g., CIT-05..11), and TYPE-C (supplementary citations, CIT-12+) -- with a RESOLVED or DANGLING status per row, and a MANIFEST-COMPLETE token emitted only when every row shows RESOLVED. Without the manifest, C-49 makes individual citations address-linked but provides no accounting of the complete citation set: a reviewer cannot determine from output alone how many citations exist, which were produced, or which could not be resolved -- completeness requires document traversal. With the manifest, citation accounting is exhaustive and status-visible: a reviewer auditing citation-completeness reads the manifest table alone without traversing the full artifact. The manifest functions as the citation-layer analog of DISCARD-LOG-COMPLETE (C-36): DISCARD-LOG-COMPLETE accounts for all filter candidates by status; the CITATION-COMPLETENESS-MANIFEST accounts for all citations by status. MANIFEST-COMPLETE is emitted only when every row is RESOLVED -- its absence is a detectable signal that citation accounting is incomplete, applying the same success-visible / failure-visible contract as CHAIN-COMPLETE (C-19) and RULES-ANCHORED-COMPLETE (C-28). Pass requires: (1) a CITATION-COMPLETENESS-MANIFEST table enumerating all citations by type (TYPE-A, TYPE-B, TYPE-C) with RESOLVED/DANGLING status per row, and (2) a MANIFEST-COMPLETE token emitted only when all rows show RESOLVED. Applicable only when C-49 is satisfied. |
| C-53 | **Declaring-section consumer index populated** | structure | C-49, C-50, and C-51 activate the citation layer in the consuming direction: BC-COVERAGE-RECORD output headers carry their schema field-IDs (C-49), inline PHASE-HANDOVER tables carry their registry T-IDs (C-50), and MUST-clause rows carry their motivating PBI identifiers (C-51). Each activation is unidirectional -- a reviewer at a consuming location can look up the declaring entry by ID, but a reviewer at a declaring entry cannot identify its consuming contexts without document traversal. C-53 adds the inverse direction to all three declaring tables simultaneously: the BC-COVERAGE-RECORD Schema gains a Consumer-Ref column where each F-BCR row names its consuming output step (e.g., F-BCR-2 -> "[COVERAGE AUDIT] Step 1 output header"); the Phase Transition Registry gains a Consumer-Ref column where each T-ID row names its consuming PHASE-HANDOVER-[N] table (e.g., T-03 -> "PHASE-HANDOVER-3"); the STILL-LIVE FILTER gains a Consumer-Ref column where each MUST-ID row names its consuming enforcement token. With Consumer-Ref populated in all three declaring tables, navigability is bidirectional in all three citation axes from cell content alone: declaring -> consuming via Consumer-Ref; consuming -> declaring via the citation ID established by C-49/C-50/C-51. Without Consumer-Ref, each citation axis is traversable in one direction only; with Consumer-Ref, every citation axis is traversable in both directions without document scan in either direction. Pass requires Consumer-Ref columns populated in all three declaring tables (BC-COVERAGE-RECORD Schema, Phase Transition Registry, and STILL-LIVE FILTER), each row naming its consuming context by identity. A partial implementation that populates Consumer-Ref in only one or two of the three declaring tables fails -- all three axes must be bidirectionally indexed. Applicable only when C-49, C-50, and C-51 are all satisfied. |
| C-54 | **Citation chain PBI grounding verified** | structure | C-51 requires each MUST-clause row in the STILL-LIVE FILTER to include a Belief-Ref column naming the PBI entry (PB-[NN]) that motivated the clause, making the clause-to-belief link bidirectional from table cells. However, the PBI entry asserts an expectation without naming the signal artifact that confirmed or falsified it: C-51 links clause to belief, but does not link belief to evidence. C-54 extends the Belief-Ref cell from a PBI identifier alone to a full citation chain entry: `PB-[NN] (confirmed false by: [artifact name, namespace, date])`. The extension makes the chain terminal at evidence: a reviewer reading MUST-3's Belief-Ref cell walks to PB-03 (via PBI identifier) and then immediately locates the falsifying signal artifact (via the inline attribution) -- the complete clause-to-belief-to-evidence chain is traversable from the table cell alone, without consulting the PBI section body, the signal artifact list, or any surrounding prose. C-54 additionally requires the BC-COVERAGE-RECORD F-BCR-4 output (Coverage field) to name specific PBI entries with material attribution (e.g., "PB-01 through PB-04 (from [material name])") rather than a summary sentence, making coverage entries traceable to individual belief entries rather than to belief-sets. Together the two extensions make the full citation chain traversable from table cell content alone across a five-hop path: MUST-ID -> Scope/Action (same row) -> Belief-Ref PBI identifier -> PBI entry (by ID lookup) -> confirming artifact (via inline attribution in Belief-Ref cell). No prose consultation is required at any hop. Pass requires: (1) each STILL-LIVE FILTER Belief-Ref cell to carry the form `PB-[NN] (confirmed false by: [artifact name, namespace, date])` rather than `PB-[NN]` alone, and (2) the BC-COVERAGE-RECORD F-BCR-4 output to name specific PBI entries with material attribution rather than a summary sentence. A Belief-Ref cell that names the PBI entry but omits the confirming artifact fails; a Belief-Ref that names a confirming artifact not present in the signal set fails. Applicable only when C-51 is satisfied. |
