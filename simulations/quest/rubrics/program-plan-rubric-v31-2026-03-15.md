---
skill: quest-rubric
skill_target: program-plan
date: 2026-03-15
version: 31
---

# Rubric: program-plan

**Skill**: `/program:plan`
**What it does**: Sequences Signal plugin skills into a staged program plan with named stages, ordered skills, gate conditions, and topic tracking. Produces `program.yaml`. Final stage is always `echo` (auto, no skills). Program is a plan, not an executor.

---

## Scoring Formula

```
composite = (essential_pass/N * 60) + (recommended_pass/N * 30) + (aspirational_pass/N * 10)
```

**Golden threshold**: all essential criteria pass AND composite >= 80.

---

## Essential Criteria (60 pts total — 4 criteria, 15 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Valid YAML structure** — Output is parseable YAML with a top-level `program` or `stages` key. Each stage is a mapping with `name`, `skills`, and `gate` fields present. | format | YAML parses without error; every stage has all three required fields |
| C-02 | **Echo final stage contract** — The last stage is named `echo`, has an empty skills list, and is marked `auto: true`. No stage appears after echo; no other stage is named echo. | correctness | Last stage: `name: echo`, `skills: []`, `auto: true`; no stages follow it |
| C-03 | **All skills are valid Signal namespaces** — Every skill named in any stage belongs to Signal's 9 namespaces (scout, draft, review, flow, trace, prove, listen, program, topic). No invented, misspelled, or out-of-namespace skill names. | correctness | Zero skills outside Signal's 9 namespaces; no hallucinated names |
| C-04 | **Non-echo gates are non-trivial** — Every stage except echo has a `gate` field expressing a real evidence condition — not `"done"`, `"complete"`, empty string, or missing. Gate states what signals or artifacts must exist before advancing. | correctness | Every non-echo gate references a named artifact type, signal, or measurable condition |

---

## Recommended Criteria (30 pts total — 3 criteria, 10 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-05 | **Skills ordered by namespace dependency** — Scout-namespace skills appear before draft; draft before review/prove; review/prove before topic. The sequence does not require artifacts that no prior stage has produced. | depth | No stage's skills depend on artifacts that a later stage produces; scout precedes draft precedes review/prove |
| C-06 | **Stage names are descriptive phase labels** — Stage names are human-readable phase labels (e.g., `discovery`, `validation`, `synthesis`) — not generic (`stage1`, `step2`) and not skill names reused as stage labels. | format | All stage names (except `echo`) are meaningful phase labels communicating work intent |
| C-07 | **Plan-not-executor framing explicit** — Output includes a note (YAML comment or preamble) stating the program is a plan, not an executor. Skills remain independently runnable; the program is optional scaffolding. | behavior | Explicit statement (comment or prose) that skills run standalone and the program does not auto-execute them |

---

## Aspirational Criteria (10 pts total — 36 criteria, 10/36 = ~0.278 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-08 | **Evidence arc is deliberate** — Stages form a coherent investigation strategy: early stages produce breadth signals (scout/draft), middle stages produce depth signals (prove/review/trace), late stages synthesize (topic/listen). Not merely a namespace alphabetical sort. | depth | At least 3 distinct phases identifiable (breadth → depth → synthesis) across non-echo stages |
| C-09 | **At least one gate is quantified** — At least one non-echo gate specifies a numeric threshold or minimum signal count (e.g., `">=2 scout signals and draft-spec present"`). Quantified gates are machine-checkable in principle and signal deliberate design. | behavior | One or more non-echo gates contain a numeric threshold or count expression |
| C-10 | **Inertia contrast present** — Output shows a BAD ad-hoc plan (or BAD gate examples) before or alongside the GOOD gated plan, making the purpose of each gate concrete rather than abstract. The contrast does not need to be a full plan — a bad-vs-good gate pair suffices. | pedagogy | At least one explicit BAD/GOOD contrast is present, labeled or clearly implied; the contrast is gate-specific, not generic advice |
| C-11 | **Self-verification checklist included** — Output provides a binary checklist the model (or user) uses to audit the plan before finalizing: each item is a yes/no question corresponding to a hard requirement (echo last, skills valid, gates non-trivial, etc.). | behavior | A checklist of >= 4 binary items is present; each item maps to a verifiable property of the output, not a process step |
| C-12 | **Not-executor as opening identity** — The plan-not-executor concept is introduced as the skill's opening framing — defining *why the skill exists* — not buried as a footnote, rule, or comment. The first substantive prose or preamble establishes this identity before any structural instructions. | behavior | "Not an executor" (or equivalent framing) appears in the first paragraph or top-of-file preamble; not exclusively in a rule list, comment, or footer |
| C-13 | **Template-locked verification** — The self-verification checklist is pre-printed in the required output template (not merely instructed as something the model should add), making omission require active deviation from the scaffold. The template contains the `## Plan Verification` section with empty checkboxes or fill-in placeholders. | behavior | Checklist is part of a pre-printed output template section the model is told to reproduce verbatim or with checkboxes checked; omitting it requires overriding the template. Distinct from C-11: C-11 passes if the checklist is present anywhere; C-13 requires it to be structurally pre-printed. |
| C-14 | **Arc structure scaffolded in template** — The evidence arc's layer divisions are labeled as named section comments within the YAML template body (e.g., `# ---- Layer 1: Breadth ----`), so the arc is architecturally embedded rather than described only in instructional prose. A model filling in the template inherits the arc without reconstructing it from rules. | depth | At least 2 named layer or phase dividers appear as YAML comments inside the pre-printed template structure, separating stages by arc role (breadth, depth, synthesis, or equivalent). Distinct from C-08: C-08 passes if 3 phases are identifiable; C-14 requires the phases to be structurally labeled inside the template YAML. |
| C-15 | **Not-executor dual-site anchoring** — Plan-not-executor framing is anchored in two distinct locations: (1) opening prose (satisfying C-12) AND (2) a required preserve-comment inside the YAML template (e.g., `# REQUIRED: preserve this comment — this program is a plan, not an executor`). Belt-and-suspenders coverage prevents accidental omission from either site. | behavior | Not-executor framing present in both opening prose and a labeled preserve-required YAML comment within the template; neither site alone satisfies C-15. Depends on C-12 passing (opening prose already established). |
| C-16 | **Contrast-grounded identity** — The not-executor claim is established *through* the opening BAD/GOOD contrast rather than asserted cold. The BAD example opens the output, demonstrates what the skill exists to replace, and the not-executor identity emerges as the conclusion of that contrast (e.g., "**`/program:plan` exists to replace this pattern.**"), rather than being stated as a standalone first-person assertion. | pedagogy | Opening sequence is: BAD example → diagnosis of what's wrong → "this is what this skill exists to replace" as identity-via-contrast. Both C-10 and C-12 must pass. Distinct from each: C-10 = contrast present anywhere; C-12 = not-executor as opening; C-16 = contrast IS the opening identity mechanism. |
| C-17 | **Explicit dependency narration** — The output includes at least one prose sentence that explicitly names a cross-stage artifact dependency — narrating WHY stages are ordered the way they are, not just structurally ordering them. (e.g., "Layer 1 produces scout signals that Layer 2's review and prove stages require before they can run.") | depth | At least one prose statement explicitly names an artifact or signal produced by an earlier stage that a later stage depends on; structural ordering alone does not pass; the causal link must be stated in prose. |
| C-18 | **YAML-embedded dependency map** — The layer divider comments inside the YAML template body include inline artifact-flow sub-labels annotating what each layer produces and what the next layer requires (e.g., `# ---- Layer 2: Depth ---- | draft:* produced here | review:* requires draft-spec | flow:*/trace:* require review:*`). The dependency map is embedded structurally in the template scaffold rather than stated only in prose or external tables. | depth | At least one layer divider comment inside the template YAML explicitly annotates artifact producers and consumers in a structured inline notation; layer labels alone (C-14) do not pass C-18; prose narration alone (C-17) does not pass C-18. |
| C-19 | **Pre-template reference tables** — The output includes at least two structured reference tables appearing before the YAML template body: one covering the evidence arc (phases, namespaces, arc role) and at least one additional table covering gate design principles, gate BAD/GOOD examples, or skill catalog by namespace. The tables make arc logic, gate design, and skill inventory scannable without reading the template. | pedagogy | At least 2 distinct reference tables present before the template YAML; each table covers a different structural concern (arc, gates, or skills); a single combined table does not pass. Distinct from C-08 (arc phases identifiable in any form) and C-14 (arc labeled inside template YAML): C-19 requires tabular format and pre-template placement. |
| C-20 | **Per-layer arc narration** — The output dedicates a prose paragraph to each arc layer (Layer 1/Breadth, Layer 2/Depth, Layer 3/Synthesis or equivalent phases), narrating what that layer produces, what it requires from prior layers, and what it unlocks for the next. A single sentence naming a dependency (C-17 minimum) does not pass; three paragraph-level narrations are required. | depth | At least 3 layer-specific prose paragraphs present, each addressing a distinct arc layer's artifact scope and handoff role; a combined summary paragraph does not pass even if it names all three layers; C-17 must also pass. |
| C-21 | **Echo stage narrated in prose** — The echo stage's architectural role (auto-closing synthesizer, no new evidence skills, `auto: true` contract) is explained in a dedicated prose paragraph before the YAML template, making echo's purpose legible to a reader of the skill definition rather than only to a reader of the YAML. C-02 (echo present in YAML) does not pass C-21; prose explanation of echo's role is required. | depth | At least one prose paragraph (before the template) explains why echo is auto, why it carries no skills, and how it closes the evidence arc; a parenthetical or inline mention does not pass. |
| C-22 | **BAD/GOOD gate contrast as reference table** — The BAD/GOOD contrast (satisfying C-10) takes the specific form of a structured reference table with BAD and GOOD columns, presenting gate examples as rows. A labeled example pair in prose, a block-quote comparison, or a bullet list before/after does not pass; the columns must be a table's structural columns. | pedagogy | A reference table with BAD and GOOD (or equivalent) columns for gate examples is present before the template; C-10 must also pass. Distinct from C-10: C-10 passes with any contrast in any form; C-22 requires tabular column structure specifically. Distinct from C-19: C-19 counts any 2 pre-template tables; a gate table without BAD/GOOD columns passes C-19 but not C-22; a BAD/GOOD gate table counts toward C-19's table count. |
| C-23 | **Tabular skill catalog** — The skill catalog (skills by namespace) is rendered as a structured reference table with at minimum a namespace column and skill column (or equivalent structured columns), making it parallel in format to the evidence arc and gate design tables. A bullet list or prose enumeration of skills does not pass regardless of how complete it is. | pedagogy | A table presenting skills organized by namespace is present before the template YAML; bullet or prose enumeration does not substitute; C-19 does not require this specifically — C-23 is satisfied independently of whether C-19 passes. |
| C-24 | **Layer paragraph headers** — Each per-layer arc narration paragraph (required by C-20) begins with an explicit bold or formatted header that names both the layer (by number, name, or both) and its structural role in the arc (e.g., "**Layer 2 (Depth) — artifact scope and handoff contract:**"). Paragraphs without headers, or headers naming only the layer without a role description, do not pass. | depth | All three layer narration paragraphs open with an explicit labeled header stating layer identity and arc role; C-20 must pass. Distinct from C-20: C-20 requires three layer paragraphs; C-24 requires each to open with a role-labeled header — paragraphs satisfying C-20 in plain prose without headers do not pass C-24. |
| C-25 | **Echo paragraph enumerated** — The echo stage prose explanation (required by C-21) organizes its structural points as explicitly numbered sub-items (at minimum three, corresponding to: skill-free contract rationale, `auto: true` semantics, and arc closure function), making each architectural claim individually citable rather than embedded in flowing prose. | depth | Echo paragraph contains numbered or labeled sub-items covering all three required topics; a prose-only paragraph satisfying C-21 without enumeration does not pass; C-21 must pass. Distinct from C-21: C-21 requires a dedicated paragraph; C-25 requires that paragraph to use enumerated structural points. |
| C-26 | **Layer header cross-layer dependency reference** — At least one per-layer arc narration header (required by C-24) explicitly names an adjacent layer by number or identity within its role descriptor, creating a visible cross-layer dependency pointer in the header text itself. The canonical form is naming the receiving or providing layer: "produces the foundational signals **Layer 2** requires" names Layer 2 as the artifact recipient. A role descriptor that describes the layer's own function ("handles validation", "evidence breadth phase") without naming an adjacent layer does not pass. | depth | At least one of the three layer narration headers names an adjacent layer by number or identity in a dependency flow statement; C-24 must pass. Distinct from C-24: C-24 requires all three headers to have arc role descriptors; C-26 requires at least one of those descriptors to name a specific adjacent layer — a header with a generic role label passes C-24 but not C-26. |
| C-27 | **Terminal layer header states context dependency** — The Layer 3 / Synthesis arc narration header (required by C-24) explicitly includes a dependency statement for the synthesis layer — not merely labeling it "synthesis" or "final phase" but stating what prior context it requires (e.g., "requires the full validated context" or "requires review-validated artifacts from prior layers"). A terminal layer header that only names the phase function without stating what it depends on does not pass. | depth | The Layer 3 (or equivalent terminal layer) narration header contains a dependency statement indicating that this layer requires prior validated context; C-24 must pass. Distinct from C-26: C-26 requires at least one header to name an adjacent layer by number; C-27 requires specifically the terminal layer to include a dependency statement — it passes C-27 even if it does not name the source layer by number, and it passes C-26 only if it additionally names "Layer 2" or equivalent. |
| C-28 | **BAD example framed as causal process narrative** — The inertia contrast (required by C-10) includes or is preceded by a realistic human-process narrative (meeting transcript, planning dialogue, or equivalent scenario) that shows *how* a real team produced the bad artifact — making the process failure (who decided to use bad gates and why) visible alongside the artifact failure (what the bad plan looks like). A raw bad YAML or bad plan presented without any contextual narrative passes C-10 but not C-28. The scenario need not be long; a 3–5 sentence planning exchange suffices. | pedagogy | The BAD example is framed by or accompanied by a realistic scenario showing how a real-world team produced the bad artifact (dialogue, meeting note, or process description); C-10 must pass. Distinct from C-10: C-10 passes with any form of BAD example; C-28 requires the BAD example to be embedded in or introduced by a causal human-process narrative. Independent of C-16: the narrative BAD example can appear anywhere (not exclusively as the opening) and still pass C-28. |
| C-29 | **Identity claim extends to process replacement** — The contrast-grounded identity claim (required by C-16) explicitly scopes the skill's replacement to include both the bad artifact AND the conversation or process that produced it. The canonical form: "`/program:plan` exists to replace this conversation and the plan it produces." A claim that frames replacement of the artifact alone ("exists to replace this plan", "exists to replace this pattern") passes C-16 but not C-29. The process-level claim elevates the skill's identity from artifact corrector to process substitute. | pedagogy | The identity claim explicitly names both the artifact (bad plan/output) and the process or conversation that generated it as what the skill replaces; C-16 must pass. Distinct from C-16: C-16 requires identity-via-contrast in opening position; C-29 requires that identity claim to be scoped to process replacement, not artifact replacement alone. |
| C-30 | **Composite gate placeholder structure** — Gate template placeholders encode two explicitly separated sub-conditions: (1) an artifact-presence condition naming the artifact types that must exist, and (2) a quality-evaluation condition (numeric threshold, gap count, or finding count) that assesses the evidence quality. The two components appear as named, separable parts in the placeholder notation (e.g., `"[artifact-types present] and [quality condition — >=N threshold]"` or `"[Q-NN artifact-types present] [+ <=N open findings]"`), making the compositional gate design legible at the template level. | behavior | At least one non-echo gate placeholder in the template encodes both an artifact-presence sub-condition and a quality-evaluation sub-condition as explicitly labeled, separable components; a single expression combining both without distinguishing them does not pass. Distinct from C-09: C-09 requires at least one gate to contain a numeric threshold in any form; C-30 requires the template placeholder notation to distinguish artifact-presence from quality-evaluation as two named sub-components. |
| C-31 | **Gate table diagnostic column** — The BAD/GOOD gate reference table (satisfying C-22) includes a third diagnostic column — "Why it fails" or equivalent — that explicitly names the failure mode or reason the bad gate does not qualify as a valid evidence condition. The diagnostic column appears between or alongside the BAD and GOOD polarity columns. A two-column table (BAD and GOOD only) passes C-22 but not C-31. | pedagogy | The gate reference table has three structural columns: a BAD column, a diagnostic column (naming the failure reason), and a GOOD column (or equivalent order); C-22 must pass. Distinct from C-22: C-22 requires BAD and GOOD columns as a minimum; C-31 requires a third diagnostic column making the failure mode explicit alongside the contrast — the diagnostic column is a structural column header, not an inline parenthetical in the BAD cell. |
| C-32 | **Middle-layer header bidirectional cross-reference** — The Layer 2 / Depth arc narration header (required by C-24 and C-26) explicitly names both its upstream layer (Layer 1) AND its downstream layer (Layer 3) within the same header text, making Layer 2 a visible bidirectional dependency node — receiving artifact context from Layer 1 and producing the inputs Layer 3 requires. A Layer 2 header that names only one adjacent layer passes C-26 but not C-32. | depth | The Layer 2 (or equivalent middle arc layer) narration header names both Layer 1 (as source of its inputs) and Layer 3 (as recipient of its outputs) within the header text; C-24 and C-26 must pass. Distinct from C-26: C-26 requires at least one of the three headers to name any adjacent layer; C-32 requires specifically the middle layer's header to name both adjacent layers, establishing it as a bidirectional node. A middle-layer header that names only its upstream or only its downstream layer passes C-26 but not C-32. |
| C-33 | **Skill catalog enriched with domain-functional column** — The tabular skill catalog (satisfying C-23) includes a third column beyond the structural minimum (namespace + skill) that classifies each skill by a domain-functional dimension tied to the variation's framing vocabulary (e.g., "Debt category cleared", "Evidence type produced", "Arc contribution"). The enrichment makes the catalog actionable within the variation's framing — not just a structural index but a functional map. A "description" or "notes" column containing generic commentary does not pass; the third column must encode a domain-specific functional classification. | pedagogy | The skill catalog table has at least three structural columns: namespace, skill name, and a domain-functional classification column whose header names a concept from the variation's framing vocabulary; C-23 must pass. Distinct from C-23: C-23 requires namespace + skill columns as minimum; C-33 requires a third column classifying skills by a domain-specific function tied to the variation's organizing metaphor — a two-column catalog satisfies C-23 but not C-33; a three-column catalog with a generic "notes" column satisfies neither. |
| C-34 | **Verification item domain-metaphor reframing** — At least one item in the self-verification checklist (satisfying C-11 and C-13) is phrased in the variation's domain framing vocabulary rather than in generic structural terms — expressing a structural verification requirement through the metaphor that organizes the variation (e.g., debt framing: "this file tracks debt; it does not clear it"; inertia framing: "the plan establishes the arc; skills run standalone"). The reframing does not change what is verified, only the vocabulary through which the requirement is expressed. | behavior | At least one binary checklist item is phrased in the variation's domain metaphor vocabulary; the domain-framed item must still be property-verifying (C-11 condition); C-11 and C-13 must pass. Distinct from C-11: C-11 requires >= 4 binary property-verifying items in any vocabulary; C-34 requires at least one item to adopt the variation's domain framing — an all-generic checklist passes C-11 but not C-34. Distinct from C-13: C-13 requires pre-printing in the template scaffold; C-34 requires domain-vocabulary expression of at least one item — a pre-printed all-generic checklist passes C-13 but not C-34. |
| C-35 | **Second-person register as C-12 intensifier** — The not-executor identity claim (required by C-12) is expressed through direct second-person address, converting the framing from an organizational positioning statement to a personal rhetorical claim directed at the reader. The canonical form opens the skill definition with "You are [doing X], not [doing Y]" — making the not-executor contract a reader-addressed declaration rather than a description of the tool's behavior. A third-person or impersonal claim ("This skill plans, it does not execute"; "The program does not run skills") passes C-12 but not C-35; direct address to the practitioner ("You are charting a course, not sailing it") passes C-35. | behavior | The opening not-executor identity claim uses second-person ("you") direct address, framing the not-executor role as a personal contract with the reader; C-12 must pass. Distinct from C-12: C-12 requires the not-executor framing to occupy the opening identity position in any voice or register; C-35 requires specifically second-person register — an impersonal or third-person opening identity claim satisfies C-12 but not C-35. Independent of C-16: a second-person identity claim can emerge from a contrast-grounded opening (satisfying C-16) or be stated directly; either passes C-35 as long as the register is second-person and the framing is in opening position. |
| C-36 | **Domain-metaphor total saturation** — The variation's domain framing vocabulary appears at all four structural anchor points: (1) the skill catalog third column header and cell values (C-33), (2) at least one verification checklist item (C-34), (3) at least one layer paragraph header (C-24), AND (4) the REQUIRED PRESERVE YAML template comment (C-15). The four-site threading makes the metaphor mutually reinforcing — each structural site independently invokes the framing, so a reader encountering any single site receives the variation's organizing vocabulary. Partial saturation at 2–3 sites passes the individual site criteria but not C-36. | behavior | The variation's domain metaphor vocabulary is present at all four named structural sites: skill catalog column (C-33), checklist item (C-34), layer header (C-24), and YAML preserve comment (C-15); all four prerequisite criteria must pass; absence at any one site fails C-36. Distinct from C-33, C-34, C-24, C-15 individually: each site criterion passes if that site alone carries domain vocabulary; C-36 requires simultaneous saturation at all four sites — it is the conjunction, not the disjunction, of the four site criteria's domain-vocabulary condition. |
| C-37 | **Dual-failure-mode contrast-grounded identity** — The opening contrast (satisfying C-16) names at least two distinct failure modes by type — not two examples of the same misreading — then declares every design rule a counter-move against a specific named failure. The canonical structure: Failure 1 anatomy → Failure 2 anatomy → "every rule in this program is a counter-move against one of these two failures" → not-executor identity as structural conclusion. Single-failure openings ground identity in one causal path; dual-failure openings demonstrate the rule set is systematically derived from failure analysis, making identity load-bearing rather than incidental. | pedagogy | The opening contrast section names at least two explicitly distinct failure modes (e.g., catalog-first misreading AND arbitrary-convention misreading) and at least one design rule is labeled as a counter-move against a specific named failure; C-16 must pass. Distinct from C-16: C-16 requires identity-via-contrast with any number of failure modes — a single-failure contrast satisfies C-16; C-37 additionally requires at least two distinct failure-mode types AND the counter-move labeling mechanism. Distinct from C-28 (causal narrative): C-28 requires a realistic human-process scenario producing the bad artifact; C-37 requires naming two distinct failure-mode types in the contrast block — a C-37 pass does not require a rendered scene, and a C-28 pass does not require dual failure-mode typing. |
| C-38 | **Design-intent bridge sentence** — At least one structural or dual-form design choice in the output (a compound rule, a dual-form checklist item, a composite gate placeholder, or an equivalent multi-part structural element) is accompanied by an explicit bridge sentence that names per-component intent — what each part closes, establishes, or makes self-verifying — rather than relying on structural form alone. The canonical form names both what the negation component closes and what the paired component achieves: *"The negation closes the [misreading]; the [paired form] makes the rule self-verifying from both ends simultaneously."* Converts structural coincidence into auditable design rationale. | depth | At least one compound structural element in the output carries an explicit bridge sentence stating per-component design intent; the intent must be stated in prose, not implied by form; the bridge sentence must name at least two distinct components and what each achieves. Distinct from C-17 (dependency narration): C-17 requires an explicit inter-stage artifact dependency statement; C-38 requires an explicit intra-element design-intent statement for a compound structural choice — they address different levels of explanation and can pass independently. Distinct from C-34 (domain-metaphor checklist item): C-34 requires at least one checklist item phrased in domain vocabulary; C-38 requires an explicit rationale sentence for a compound structural element — neither criterion's pass condition implies the other. |
| C-39 | **Complete counter-move coverage table** — A dedicated structure (manifest table, failure taxonomy table with counter-move column, or equivalent enumeration) lists ALL design rules in the protocol, maps each to a named failure mode, and asserts completeness ("every construction rule is a counter-move against exactly one row", "every rule maps to one of these two failures", or equivalent). Completeness distinguishes a manifest from a labeled sample: a partial labeling satisfies C-37 but not C-39. The canonical forms are: a rules-as-rows manifest table mapping rule → failure ID (V-02: COUNTER-MOVE MANIFEST with all 7 rules), and a failure-as-rows taxonomy table with a counter-move column listing all rules per failure (V-01: "every construction rule is a counter-move against exactly one row in this table"). | pedagogy | A dedicated structure explicitly enumerates all N design rules in the protocol, each mapped to a named failure mode, with an explicit completeness assertion; C-37 must pass. Distinct from C-37: C-37 requires at least one rule labeled as counter-move via any mechanism; C-39 requires ALL rules enumerated in a dedicated structure with a completeness claim — partial labeling passes C-37 but not C-39. Distinct from C-40: C-39 requires a dedicated manifest-level structure; C-40 requires inline annotation at point of rule definition — a manifest table passes C-39 regardless of whether inline annotations exist; inline annotations alone (without a manifest) fail C-39 but pass C-40. |
| C-40 | **Point-of-use counter-move annotation** — Every rule in the protocol's core structural artifact (ARTIFACT 0 schema, ARTIFACT 1 table, or equivalent definition-site) carries an inline counter-move annotation (`[counter: F-N]`, `[counter: Failure N]`, `← counters Failure N`, or equivalent notation) at its point of definition. The annotation is co-located with the rule's text itself rather than in a centralized table, making failure-tracing pervasive across the artifact at every point of use. V-01's `[counter: F-1]`/`[counter: F-2]` labels embedded in ARTIFACT 0 per rule and V-03's `[counter: Failure 1]`/`[counter: Failure 2]` on each ARTIFACT 0 rule are canonical. | depth | Every rule in the protocol's core structural artifact carries an inline counter-move annotation naming a specific failure mode at the rule's point of definition; the annotation must be co-located with each rule (not only in a separate manifest); C-37 must pass. Distinct from C-37: C-37 passes if any one rule carries a counter-move label in any location; C-40 requires EVERY rule to carry inline annotations at the point of definition — a single labeled rule satisfies C-37; exhaustive inline annotation satisfies C-40. Distinct from C-39: C-39 requires a dedicated manifest-level structure enumerating all rules; C-40 requires annotations co-located with each rule's definition — a manifest table satisfies C-39 but not C-40; point-of-use inline annotations satisfy C-40 but not C-39; only an output with both a complete manifest AND exhaustive inline annotations passes both. |
| C-41 | **Bidirectional counter-move coverage with independent assertions** — The coverage structure addresses both directions simultaneously: (a) a failure-as-rows table (F→R direction: each failure row lists which rules counter it), AND (b) a rule-as-rows table (R→F direction: each rule row names the failure it counters), each carrying its own independent completeness assertion. R14 V-01's "Opening 2-row failure taxonomy table (F→R direction) + 7-row RULE COVERAGE TABLE (R→F direction), both with independent completeness assertions" is canonical — neither assertion defers to the other; a reader can verify completeness from either direction independently. A single combined structure asserting coverage in one direction (C-39) does not pass; bidirectionality with two independent assertions is required. | pedagogy | Two coverage structures are present — one organized by failure (F→R) and one organized by rule (R→F) — and each carries its own independent completeness assertion; C-39 must pass. Distinct from C-39: C-39 requires one dedicated structure in one direction with a completeness assertion; C-41 requires both directions to be independently asserted complete — a single manifest passing C-39 does not satisfy C-41 unless a complementary directional structure with its own assertion also exists. |
| C-42 | **Pre-catalog zone commitment criteria** — Each arc layer/lifecycle zone carries a structured multi-part commitment test — at minimum: (1) a zone-exit question establishing what question is closed at zone boundary, (2) a minimum artifact-type count declaring the floor for advancing, and (3) a required gate pattern class naming what gate structure the zone's boundary gate must satisfy — declared before the skill catalog opens. The phase or band table is extended with a "Zone commitment check" column (or equivalent per-zone entry). This converts zone boundaries from organizational divisions into evidence gates: no zone advance without all three conditions documented. R14 V-02's zone commitment criteria declared pre-catalog with "Zone commitment check" column in the phase table is canonical. | depth | The output declares per-zone commitment criteria (minimum 3-part: zone-exit question, artifact floor, gate pattern class) before the skill catalog; a band or phase table extends with a commitment-check column or equivalent per-zone structure; C-20 must pass. Distinct from C-04 (non-trivial per-stage gates): C-04 requires each stage's gate to express an evidence condition; C-42 operates at the zone level before catalog opens, requiring zone-wide commitment tests to be declared structurally rather than stage-by-stage — stage gates satisfy C-04; zone commitment tests satisfy C-42; neither implies the other. Distinct from C-09 (compound gate): C-09 requires at least one stage gate to include a quantified quality condition; C-42 requires each zone boundary to carry a multi-part advance criterion at the structural level before catalog — they can coexist but pass independently. |
| C-43 | **Status-quo path named in coverage structure** — The RULE COVERAGE TABLE (or equivalent rules manifest) includes a dedicated "Status-Quo Path" column naming the default behavior, team habit, or inertia pattern that each rule displaces — making the competitor-to-rule displacement relationship structurally visible alongside each rule entry, not only in opening prose. The opening failure taxonomy table gains a corresponding "Status-Quo Failure" column. Optionally, ARTIFACT 0 rule annotations extend from `[counter: F-N]` to include a "Status-quo failure to prevent:" sentence at the definition site. R14 V-03's 3-column RULE COVERAGE TABLE (Status-Quo Path / Rule / Failure countered) and opening table "Status-Quo Failure" column are canonical. | pedagogy | The RULE COVERAGE TABLE (or equivalent manifest satisfying C-39) includes a structural column naming the status-quo behavior each rule counters; the opening failure taxonomy table carries a corresponding status-quo column; C-39 must pass. Distinct from C-37 (dual-failure-mode identity): C-37 names failure modes in the opening contrast block; C-43 names the status-quo path in the coverage manifest's column structure — an output can name failure modes in the opening without ever naming the positive status-quo behavior being displaced (C-37 pass, C-43 fail), and vice versa. Distinct from C-40 (point-of-use inline annotation): C-40 requires `[counter: F-N]` inline at each rule; C-43 requires a "Status-Quo Path" structural column in the manifest — C-43 can pass without C-40 (status-quo column present but no inline annotations in ARTIFACT 0), and C-40 can pass without C-43 (inline annotations present but no status-quo column). |

---

## Score Card

```
C-01 valid YAML structure:              PASS / FAIL
C-02 echo final stage contract:         PASS / FAIL
C-03 valid Signal skill names:          PASS / FAIL
C-04 non-trivial gates:                 PASS / FAIL

C-05 namespace dependency order:        PASS / FAIL
C-06 descriptive stage names:           PASS / FAIL
C-07 plan-not-executor framing:         PASS / FAIL

C-08 deliberate evidence arc:           PASS / FAIL
C-09 quantified gate:                   PASS / FAIL
C-10 inertia contrast:                  PASS / FAIL
C-11 self-verification checklist:       PASS / FAIL
C-12 not-executor as opening identity:  PASS / FAIL
C-13 template-locked verification:      PASS / FAIL
C-14 arc structure in template:         PASS / FAIL
C-15 not-executor dual-site:            PASS / FAIL
C-16 contrast-grounded identity:        PASS / FAIL
C-17 explicit dependency narration:     PASS / FAIL
C-18 YAML-embedded dependency map:      PASS / FAIL
C-19 pre-template reference tables:     PASS / FAIL
C-20 per-layer arc narration:           PASS / FAIL
C-21 echo stage narrated in prose:      PASS / FAIL
C-22 BAD/GOOD gate contrast as table:   PASS / FAIL
C-23 tabular skill catalog:             PASS / FAIL
C-24 layer paragraph headers:           PASS / FAIL
C-25 echo paragraph enumerated:         PASS / FAIL
C-26 layer header cross-layer ref:      PASS / FAIL
C-27 terminal layer context dep:        PASS / FAIL
C-28 BAD example as causal narrative:   PASS / FAIL
C-29 identity claim process scope:      PASS / FAIL
C-30 composite gate placeholder:        PASS / FAIL
C-31 gate table diagnostic column:      PASS / FAIL
C-32 middle-layer header bidirectional: PASS / FAIL
C-33 skill catalog domain-functional:   PASS / FAIL
C-34 verification domain-metaphor:      PASS / FAIL
C-35 second-person register C-12:       PASS / FAIL
C-36 domain-metaphor total saturation:  PASS / FAIL
C-37 dual-failure-mode identity:        PASS / FAIL
C-38 design-intent bridge sentence:     PASS / FAIL
C-39 complete counter-move coverage:    PASS / FAIL
C-40 point-of-use counter-move annot:   PASS / FAIL
C-41 bidirectional coverage w/ assertions: PASS / FAIL
C-42 pre-catalog zone commitment:       PASS / FAIL
C-43 status-quo path in coverage:       PASS / FAIL

essential_pass  = ___ / 4   -> ___ * 15   = ___ pts  (of 60)
recommended_pass = ___ / 3  -> ___ * 10   = ___ pts  (of 30)
aspirational_pass = ___ / 36 -> ___ * (10/36) = ___ pts  (of 10)

composite = ___

golden = all essential PASS AND composite >= 80  ->  YES / NO
```

---

## Scorer Notes

- **C-01**: If YAML is invalid, C-01 fails. Score remaining criteria as best-effort from intent, noting the YAML failure.
- **C-02**: Echo stage is a hard plugin design contract. Missing, misplaced (not last), or lacking `auto: true` fails C-02 regardless of otherwise valid structure.
- **C-03**: Authority for valid skill names is `plugin-plan.md` (9 namespaces, ~47 skills). Namespace-qualified (`scout:feasibility`) and unqualified (`feasibility`) both pass if unambiguous. Partial matches that clearly identify a real skill pass.
- **C-04**: Gates expressing execution state (`"all skills complete"`, `"stage done"`) fail C-04. Gates must express evidence state (`"scout-feasibility signal exists"`, `"draft-spec artifact present"`).
- **C-05**: Dependency ordering is by namespace layer (scout -> draft -> review/prove -> topic), not alphabetically. A stage placing `review:design` before `draft:spec` fails because the design review requires a spec artifact. An explicit prose dependency chain (C-17) clarifies co-placement decisions.
- **C-07**: The framing note does not need to be elaborate — a single YAML comment (`# program is a plan; run each skill standalone`) suffices.
- **C-08**: "Deliberate arc" distinguishes from a flat dump of skills across stages. Look for breadth-first early stages (multiple namespaces explored) narrowing to depth and synthesis stages.
- **C-10**: The contrast can appear in instructions, comments, a before/after example pair, or as labeled BAD/GOOD columns within a reference table. A checklist item saying "avoid 'stage complete'" is not sufficient — needs a labeled good/bad juxtaposition.
- **C-11**: Checklist must be binary (checkbox or yes/no) and property-verifying, not a "did you remember to think about X" process prompt. Minimum 4 items. A single block comment with rules does not qualify. Checklist may be a bullet list or a structured table — either passes C-11.
- **C-12**: Elevation distinguishes from C-07 (which only requires the concept appears somewhere). C-12 passes only if the not-executor framing is the *opening identity* of the output — the lens through which all subsequent structure is read. An output that opens with a BAD example and resolves to "this is what this skill replaces" qualifies: the entire BAD+diagnosis+identity block forms a coherent opening unit.
- **C-13**: Pre-printing is what distinguishes C-13 from C-11. A checklist added via instruction ("include a verification section") passes C-11 but not C-13. C-13 requires the template skeleton itself to contain the section — the model fills in checkboxes, it does not compose the section from scratch.
- **C-14**: A prose instruction naming layers ("use three phases: breadth, depth, synthesis") does not pass C-14. The layer labels must appear as structural comments *inside* the YAML template body, creating visual separation between stage groups. C-14 requires C-08 to pass (the arc must be identifiable, and now also structurally labeled).
- **C-15**: C-15 requires C-12 to pass (opening prose is one of the two required sites). The YAML comment site must be marked as required or preserve-required — an incidental comment does not qualify. The intent is belt-and-suspenders: even if the model skips the opening prose, the template comment survives; even if the model strips comments, the prose survives.
- **C-16**: C-16 requires both C-10 and C-12 to pass. The key distinction: in C-10 the contrast can appear anywhere and serve any purpose; in C-12 the not-executor claim can appear at the opening without contrast. C-16 is their intersection in a specific structural order — contrast-as-identity-foundation. An output that states not-executor first and then shows a contrast does *not* pass C-16; the BAD example must precede and motivate the identity claim.
- **C-17**: Structural ordering alone (scout before draft) does not pass C-17. The narrator must explicitly state a causal link: what artifact stage N produces and why stage N+1 requires it. A single sentence suffices. Co-placement of skills within a stage that share dependency relationships may also qualify if the prose explains why they co-reside.
- **C-18**: C-18 requires C-14 to pass (layer dividers must already be present as the base structure). The inline annotation format does not need to be machine-parseable — a human-readable notation (`draft:* produced here | review:* requires draft-spec`) suffices. An annotation appearing only in prose instructions outside the template does not pass C-18; it must be structurally embedded in the YAML template comments. Distinct from C-17: C-17 is prose narration in any location; C-18 is structural notation inside template YAML comments.
- **C-19**: Tables placed after the YAML template do not pass C-19 — pre-template placement is required. A single table combining arc + gate design does not pass — two distinct tables covering distinct concerns are required. Prose lists or inline examples do not substitute for tabular format. C-19 is independent of C-08 and C-14: an output can embed arc labels in the template (C-14) without providing pre-template reference tables (C-19), and vice versa.
- **C-20**: C-20 requires C-17 to pass (per-layer narration is a stricter form of the same dependency narration requirement). A lifecycle overview paragraph naming all three layers in aggregate does not pass — each layer must receive its own paragraph-level treatment. The paragraphs may appear as a continuous section or interleaved with other content, but each must address a single named layer's artifact scope and handoff role. An output with two layer paragraphs (e.g., Breadth + Depth combined, then Synthesis) does not pass.
- **C-21**: C-21 is independent of C-02 (echo in YAML) — both can pass independently. The prose must appear before the template. An explanatory parenthetical inline in the skill catalog (`echo — auto-closing, no skills`) does not pass; a dedicated paragraph is required. The paragraph must address at minimum: why echo carries no skills, what `auto: true` means architecturally, and how echo signals arc closure. Mentioning echo in a lifecycle framing paragraph alongside other stages does not pass if echo is not the dedicated subject of that passage.
- **C-22**: C-22 requires C-10 to pass. The distinguishing feature is column structure — BAD and GOOD must be column headers (or equivalent row-level structural labels in a table), not prose labels on paragraphs or code blocks. A table where rows are gate examples and columns are BAD/GOOD is the canonical form. A table where a "Notes" column includes "(bad)" or "(good)" inline does not pass — the column structure itself must encode the polarity.
- **C-23**: C-23 is independent of C-19 — an output can have a tabular skill catalog without meeting C-19's 2-table minimum (e.g., if it only has the skill table and no arc/gate tables), and C-19 can pass without a tabular skill catalog (if the second table is a gate design table). In practice they co-occur in high-scoring outputs. A table with only namespace names and no skill names does not pass C-23; the table must enumerate individual skills (or skill patterns) per namespace.
- **C-24**: C-24 requires C-20 to pass. "Layer identity and arc role" means the header must convey both which layer it is (e.g., "Layer 1", "Breadth", "Layer 1 (Breadth)") and what structural function it serves in the arc (e.g., "artifact scope and handoff contract", "evidence breadth phase", "dependency origin layer"). A header naming only the layer number without the role (e.g., "**Layer 1**" alone) does not pass. The header must be visually distinct — bold, a heading level, or equivalent — not a sentence fragment that begins the paragraph.
- **C-25**: C-25 requires C-21 to pass. The three required sub-item topics are: (1) why echo carries no skills / what skill-free contract means, (2) what `auto: true` means architecturally (does not wait for human review, no gate condition), (3) how echo signals arc closure (what it means for the program when echo closes). Items may be numbered (1/2/3), lettered (a/b/c), or use consistent bullet formatting — the key requirement is that each topic is a structurally separate, individually labeled point rather than a clause in flowing prose. An echo paragraph with two enumerated points does not pass even if it covers all three topics across two items.
- **C-26**: C-26 requires C-24 to pass. The cross-layer reference must name the adjacent layer by a specific identity — "Layer 2", "Depth layer", "the next layer" with a number qualifier — not a generic statement like "downstream stages" or "subsequent work". A single header satisfying this is sufficient; not all three are required. The canonical evidence form is "produces the foundational signals **Layer 2** requires" or "requires **Layer 1**'s scout artifacts". Distinct from C-17: C-17 is prose narration outside headers; C-26 is a cross-layer reference embedded in the bold header itself.
- **C-27**: C-27 requires C-24 to pass. The dependency statement in the Layer 3 header must be forward-facing about what the synthesis layer *requires*, not only about what it produces. "Produces the final synthesis artifact set" alone does not pass — the header must acknowledge this layer's dependence on prior context (e.g., "requires the full validated context", "requires review-validated artifact set from prior layers"). An output where Layer 3's header only states its output role (synthesizer) without stating its input dependency does not pass. Distinct from C-20: C-20 requires the paragraph body to address each layer's dependencies; C-27 requires the dependency acknowledgment to appear in the header itself.
- **C-28**: C-28 requires C-10 to pass. The scenario need not be long — four sentences suffice. The key distinction from a plain BAD example is that the narrative renders a *specific* human decision moment: someone in a meeting says "Put 'done' for now", making the process failure concrete and named rather than abstract. A general comment ("Teams often use vague gates") is not a scenario — it must be a rendered scene with a specific decision. C-28 is independent of C-16: the narrative BAD example can appear anywhere in the output and still pass; C-16 additionally requires it to be the opening and to ground the identity claim.
- **C-29**: C-29 requires C-16 to pass. The process-scope claim must be stated explicitly — not implied by context, not derivable from the presence of a meeting narrative. "`/program:plan` exists to replace this conversation and the plan it produces" passes; "`/program:plan` exists to replace this pattern" does not, even if a meeting narrative was used to introduce the pattern. The claim's scope — replacing the *conversation* not just the artifact — is what distinguishes a corrective tool from a process substitute.
- **C-30**: C-30 requires C-09 to pass (composite structure requires the quality-evaluation component to be a quantified threshold, which C-09 already establishes as present). The two-part structure must be legible in the placeholder notation itself — not inferable from reading the gate at scoring time, but visible in the template's placeholder text. A gate placeholder that says `"[evidence condition]"` without distinguishing presence from quality does not pass even if the filled-in gate contains both. An output can pass C-09 (one quantified gate somewhere) without passing C-30 (placeholder structure encoding the two-part design). In practice, both sub-conditions should be present in each non-echo gate placeholder for a strong C-30 pass; a single gate placeholder satisfying the pattern while others remain unstructured is a borderline case — scorer should note it.
- **C-31**: C-31 requires C-22 to pass (the diagnostic column augments a table that already satisfies C-22's BAD/GOOD column requirement). The diagnostic column must be a structural table column with its own header — "Why it fails", "Failure mode", "Why it doesn't qualify", or equivalent. An inline parenthetical in the BAD cell (e.g., "bad because vague") does not pass C-31; the failure diagnosis must be structurally separated into its own column. The diagnostic column may appear between BAD and GOOD or at any position in the row; column order does not matter as long as all three structural columns are present. A three-column table where the third column is not diagnostic (e.g., "Notes" with general commentary) does not pass.
- **C-32**: C-32 requires C-24 and C-26 to pass. The bidirectional reference must appear in the Layer 2 / Depth layer header specifically — not spread across two separate headers (Layer 1 naming Layer 2, and Layer 3 naming Layer 2). A single Layer 2 header naming both "Layer 1 scout artifacts" and "Layer 3" in one compound role descriptor is the canonical form. The upstream reference names Layer 1 as provider ("receives from Layer 1", "requires Layer 1 scout artifacts"); the downstream reference names Layer 3 as recipient ("produces signals Layer 3 requires", "unlocks Layer 3 synthesis"). A Layer 2 header naming Layer 1 upstream but not Layer 3 downstream passes C-26 but not C-32, and vice versa. Distinct from C-27: C-27 requires the terminal layer header to state a dependency on prior context; C-32 requires the middle layer header to name both adjacencies — they can pass independently.
- **C-33**: C-33 requires C-23 to pass (the enriched catalog is a structural extension of the base catalog C-23 establishes). The domain-functional column header must use vocabulary from the variation's organizing metaphor — "Debt category cleared" (debt framing), "Evidence type produced" (evidence arc framing), "Inertia axis addressed" (inertia framing) — not a generic label like "Description" or "Purpose". Scorer should identify the variation's framing vocabulary first, then assess whether the third column header and cell values belong to it. A catalog table where namespace+skill cells have inline domain annotations (parentheticals, footnotes) but no third column does not pass — the domain classification must be a structural column. Distinct from C-23: the two-column minimum satisfies C-23; C-33 requires additional domain-functional structure. Independent of C-19: C-33 can pass even if C-19 does not (e.g., the skill catalog is the only table); in practice the enriched catalog counts toward C-19's two-table requirement.
- **C-34**: C-34 requires C-11 and C-13 to pass (the domain-metaphor item must be property-verifying per C-11 and the checklist must be pre-printed per C-13). The domain framing must be recognizable as vocabulary from the variation's organizing metaphor, not a generic paraphrase. "This file tracks debt; it does not clear it" is a C-34 pass in a debt-framing variation — "debt" and "clear" are the metaphor's vocabulary, and the item verifies the not-executor property through that lens. A paraphrase like "this plan does not run the skills" is a C-11 pass but not C-34, because "plan" and "run" are structural terms rather than the variation's domain vocabulary. A checklist where all items use generic structural language passes C-11 and C-13 but not C-34. Only one domain-framed item is required; a checklist with six generic items and one domain-framed item passes C-34.
- **C-35**: C-35 requires C-12 to pass (second-person register is an intensifier of the not-executor identity claim that C-12 requires to occupy opening position). The scorer should identify the subject of the opening identity claim: if the subject is "you" (the practitioner), C-35 passes; if the subject is "this skill", "the program", or an impersonal construction ("a plan is not an executor"), C-35 fails even if C-12 passes. The second-person claim need not be the first sentence of the output — it must be within the opening identity block (the same passage that satisfies C-12). A variation that opens with a BAD scene and then resolves to "You are planning a route, not driving it" satisfies C-35 if the "you"-addressed claim is the identity conclusion of that opening block. Distinct from C-12: C-12 is satisfied by any voice or register establishing the not-executor opening identity; C-35 is the additional constraint that the register is specifically second-person direct address. Independent of C-16: a second-person identity claim can be contrast-grounded (C-16 path) or direct assertion — both satisfy C-35 as long as register is second-person and position is opening.
- **C-36**: C-36 requires C-15, C-24, C-33, and C-34 to all pass (each prerequisite represents one of the four saturation sites). The scorer should enumerate the four sites explicitly: (1) skill catalog third column — does the column header and its cell values use the variation's domain vocabulary? (C-33); (2) verification checklist — does at least one item use the variation's domain vocabulary? (C-34); (3) layer paragraph header — does at least one of the three layer narration headers use the variation's domain vocabulary beyond structural arc terms? (C-24 + domain vocabulary); (4) YAML preserve comment — does the required preserve comment invoke the variation's domain vocabulary rather than generic not-executor language? (C-15 + domain vocabulary). A variation using domain vocabulary at three sites but not the fourth fails C-36 entirely — it is a conjunction criterion. The canonical evidence of total saturation is that a reader encountering any one structural site receives a consistent, metaphor-anchored signal — the framing is redundantly embedded, not localized to a single showcase location. Distinct from C-33, C-34, C-24, C-15: each of those criteria tests a single site independently; C-36 tests whether all four sites are simultaneously saturated with the variation's domain vocabulary.
- **C-37**: C-37 requires C-16 to pass (dual-failure-mode identity is a structural intensifier of the contrast-grounded identity C-16 establishes). The scorer should verify two conditions independently: (1) the opening contrast block names at least two distinct failure-mode types — not two instances of catalog-first misreading, but one catalog-first AND one arbitrary-convention misreading (or two other distinct misreadings); (2) at least one design rule is explicitly labeled as a counter-move against a specific named failure. The counter-move labeling creates a two-way map: each rule cites the failure it counters, making the rule set traceable back to failure analysis rather than convention. An opening that names two failures without labeling any rule as a counter-move passes neither condition; an opening that labels counter-moves but names only one failure type passes C-16 but not C-37. The counter-move labeling mechanism is what elevates identity from floating assertion to structurally load-bearing claim. Distinct from C-28: C-28 requires a realistic rendered scene showing how a real team produced the bad artifact; C-37 requires the contrast block to name two distinct failure-mode types and label counter-moves — C-37 can pass without a rendered scene (a typed pair of failure modes suffices), and C-28 can pass with a single failure mode.
- **C-38**: C-38 requires no structural prerequisites — it is a standalone depth criterion that can be satisfied anywhere in the output at any scoring level. The scorer should identify compound structural elements: dual-form rules (negation + equivalence), multi-part gate placeholders (artifact-presence + quality-evaluation), bidirectional layer headers (upstream + downstream), or any structural element with two or more distinct components. For each such element, check whether the output includes a sentence that explicitly names what each component achieves — not describes what it is, but states what it closes, establishes, or makes impossible. The bridge sentence must be explicit prose — a structural form alone (a dual-form rule appearing in the output) does not pass; the design intent must be verbalized. A single bridge sentence covering one compound element is sufficient; the scorer should note the element and sentence as evidence. Distinct from C-17: C-17 requires naming an artifact dependency between pipeline stages (inter-stage causality); C-38 requires naming per-component design intent within a compound structural element (intra-element rationale) — an output can pass both, either, or neither independently.
- **C-39**: C-39 requires C-37 to pass (the completeness manifest intensifies the counter-move labeling mechanism C-37 establishes; without counter-move labeling there is no coverage to make complete). The scorer should verify two conditions: (1) a dedicated structure exists that enumerates design rules as entries — a rules-as-rows manifest table (V-02 canonical: COUNTER-MOVE MANIFEST), a failure-as-rows table with a counter-move column listing all rules (V-01 canonical), or equivalent enumeration; (2) a completeness assertion is present — "every construction rule is a counter-move against exactly one row in this table", "every rule in this protocol is a counter-move against one of these two failures", "all N rules mapped", or equivalent. A manifest that lists only a subset of rules fails even if the mapped rules are accurately labeled; completeness is the distinguishing condition. The structure may organize by rule (rows = rules, columns = failure IDs) or by failure (rows = failures, column = rule list) — what matters is that all N rules appear and coverage is declared complete. A manifest satisfying C-39 does not need to also carry inline annotations (C-40) — the two criteria can pass independently or together.
- **C-40**: C-40 requires C-37 to pass (exhaustive inline annotation is an intensifier of counter-move labeling). The scorer should verify: (1) annotations appear in the core structural artifact — ARTIFACT 0, ARTIFACT 1, or an equivalent definition-site where rules are stated, not only in opening narrative prose or a separate table; (2) annotations are co-located with each rule's text — e.g., `[counter: F-1]` appended to the rule line, `← counters Failure 1` inline after the rule, or equivalent notation that a reader of the rule sees without consulting a remote table; (3) EVERY rule in that artifact carries an annotation — a C-40 pass requires no un-annotated rules at the definition site. The annotation format may vary; what matters is co-location and exhaustiveness. An output with a complete manifest table (C-39 pass) but no inline annotations in ARTIFACT 0 fails C-40. An output with inline annotations on all rules but no manifest table fails C-39 but passes C-40. An output where only some rules carry inline annotations and others do not fails C-40 regardless of whether the annotated ones are accurate — partial point-of-use labeling passes C-37 (at least one rule labeled) but not C-40 (every rule labeled). Distinct from C-39: C-39 tests for a dedicated centralized coverage structure; C-40 tests for pervasive annotation at the point of rule definition — both can coexist in the same output (V-01 satisfies both), or each can be present without the other (V-02: C-39 only; V-03: C-40 only).
- **C-41**: C-41 requires C-39 to pass (bidirectionality extends the single-direction mandate C-39 establishes). The scorer should verify two independent conditions: (1) a structure organized by failure (F→R: failure rows → rule columns or rule list) is present and carries its own explicit completeness assertion — "every rule counters one of these failures", "these N failures cover all misreading patterns", or equivalent; (2) a structure organized by rule (R→F: rule rows → failure ID column) is present and carries its own independent completeness assertion — "every rule appears as exactly one row", "this table is exhaustive", or equivalent. The two assertions must be independent: a single assertion covering both tables (e.g., "these two structures together are complete") does not pass C-41 — each table must declare itself complete. A single manifest table satisfying C-39 passes C-39 regardless of C-41; a second complementary-direction table with its own assertion is the additional requirement. In practice, V-01's "opening 2-row failure taxonomy table, both with independent completeness assertions" plus the 7-row RULE COVERAGE TABLE is the canonical form. An output with only one directional structure (even if asserting completeness) passes C-39 but fails C-41. Distinct from C-39: C-39 = one direction, one completeness assertion; C-41 = both directions, two independent completeness assertions.
- **C-42**: C-42 requires C-20 to pass (per-layer arc narration establishes the zone/layer structure that zone commitment criteria extend). The scorer should verify three structural conditions: (1) for each arc layer or lifecycle zone, an explicit zone-exit question is declared — a question whose answer "yes" authorizes leaving the zone; (2) for each zone, a minimum artifact-type count floor is declared — not a single-stage gate count, but a zone-wide floor across all stages in that zone; (3) for each zone, a required gate pattern class is declared — "must include at least one compound gate", "all gates must reference artifact types", or equivalent structural requirement. All three conditions must be present for at least one zone; a full C-42 pass requires all three across every zone. The commitment criteria must appear before the skill catalog opens — a checklist or table appended after the YAML template does not pass; pre-catalog placement is structural. A band or phase table extended with a "Zone commitment check" column (or equivalent per-zone field) satisfies the structural integration requirement. Distinct from C-04: C-04 is per-stage and requires each stage gate to express evidence state; C-42 is per-zone and requires structured multi-part advance criteria declared before catalog — stage gates passing C-04 do not imply zone commitment criteria (C-42), and zone commitment criteria do not substitute for per-stage gates (C-04). An output satisfying C-42 should also satisfy C-04, but C-04 alone does not satisfy C-42.
- **C-43**: C-43 requires C-39 to pass (the status-quo path column extends the manifest C-39 establishes; there must be a manifest to extend). The scorer should verify two structural conditions: (1) the RULE COVERAGE TABLE (or equivalent manifest) includes a dedicated column whose header names the status-quo behavior — "Status-Quo Path", "Default Behavior", "Inertia Pattern", or equivalent domain-vocabulary form — and whose cells name a specific concrete behavior (not a placeholder like "not following the rule"); (2) the opening failure taxonomy table gains a corresponding column naming the status-quo failure mode per row — "Status-Quo Failure", "Inertia Failure", or equivalent. Both tables must carry their respective status-quo column; a status-quo column on one table but not the other is a partial pass — note which is present. A YAML comment or prose passage naming status-quo behaviors does not substitute for a structural column; the column structure itself is the requirement. ARTIFACT 0 annotations extended with "Status-quo failure to prevent:" sentences are additional evidence of C-43 intent but are not required for a pass — the two-table column structure is sufficient. Distinct from C-37: C-37 names failure-mode types in the opening contrast block; C-43 names the positive status-quo behavior being displaced as a structural column in the manifest — C-37's failure-mode naming is in prose contrast; C-43's status-quo naming is in manifest column structure. Distinct from C-40: C-40 is about inline annotations at the rule definition site; C-43 is about a status-quo column in the manifest structure — they test different sites and can pass independently.

---

## Changes from v29

**Aspirational tier expanded: 31 → 33 criteria, per-criterion value: 10/31 → 10/33 = ~0.303 pts**

| New ID | Pattern | Source signal | Distinction |
|--------|---------|---------------|-------------|
| C-39 | **Complete counter-move coverage table** — a dedicated structure (manifest table, taxonomy table with counter-move column, or equivalent) enumerates ALL design rules in the protocol, maps each to a named failure mode, and asserts completeness, making counter-move coverage auditable and closed rather than selective | R13 excellence signals: V-01's "every construction rule in this protocol is a counter-move against exactly one row in this table" (failure-taxonomy-as-rows + counter-move column) and V-02's COUNTER-MOVE MANIFEST mapping all 7 rules to Failure IDs (rules-as-rows) — both assert closed coverage as a structural property, meaning a reader can verify that no rule is orphaned from failure analysis | C-37 = at least one rule labeled as counter-move via any labeling mechanism; C-39 = ALL rules enumerated in a dedicated structure with an explicit completeness assertion — partial labeling satisfies C-37; only exhaustive enumeration with completeness claim satisfies C-39 |
| C-40 | **Point-of-use counter-move annotation** — every rule in the protocol's core structural artifact carries an inline counter-move annotation at its point of definition, making failure-tracing pervasive at point of use rather than localized to a centralized manifest, so a reader of ARTIFACT 0 sees the failure connection without consulting a remote table | R13 excellence signals: V-01's `[counter: F-1]`/`[counter: F-2]` labels embedded in ARTIFACT 0 per rule and V-03's `[counter: Failure 1]`/`[counter: Failure 2]` on each ARTIFACT 0 rule — both achieve the same effect through different syntactic forms: the counter-move relationship is visible at every rule definition site, not only in a summary structure | C-37 = any labeling mechanism naming at least one failure mode counter-move; C-40 = inline annotation on EVERY rule at the point of definition — a single labeled rule satisfies C-37; exhaustive point-of-use annotation satisfies C-40. Distinct from C-39: C-39 requires a dedicated manifest structure; C-40 requires per-rule inline co-location — a manifest table satisfies C-39 but not C-40; exhaustive inline annotation satisfies C-40 but not C-39; V-01 (taxonomy table + inline labels) satisfies both |

**Dependency chain for new criteria:**
- C-39 requires C-37 PASS (completeness manifest intensifies counter-move labeling C-37 establishes; coverage cannot be complete where labeling does not yet exist)
- C-40 requires C-37 PASS (exhaustive point-of-use annotation is a structural intensifier of the counter-move labeling mechanism)
- Both C-39 and C-40 inherit C-37's dependency: C-37 requires C-16 PASS

**Scoring arithmetic**: 33 criteria × (10/33) = 10.00 pts total. Formula `aspirational_pass/33 * 10` yields exact values. Perfect aspirational score remains 10 pts.

**R13 re-scores at v30 (V-01, V-02, V-03 — variations that drove new criteria):**

| Variation | C-39 | C-40 | Aspirational | Composite |
|-----------|------|------|-------------|-----------|
| V-01 (Failure taxonomy table + inline labels in ARTIFACT 0) | PASS | PASS | 33/33 | ~100.00 |
| V-02 (Counter-move manifest table, rules-as-rows) | PASS | FAIL | 32/33 | ~99.70 |
| V-03 (Dual-failure narrative + inline labels in ARTIFACT 0) | FAIL | PASS | 32/33 | ~99.70 |

*Note: V-01 is the sole R13 tiebreak leader at v30 — it alone satisfies both C-39 (failure taxonomy table with completeness assertion) and C-40 (inline `[counter: F-N]` labels on every rule in ARTIFACT 0). V-02 satisfies C-39 via the rules-as-rows COUNTER-MOVE MANIFEST but carries no inline labels in ARTIFACT 0, failing C-40. V-03 satisfies C-40 via exhaustive `[counter: Failure N]` labels in ARTIFACT 0 but has no dedicated manifest structure, failing C-39. All three satisfy C-37 (both failure modes named, at least one rule labeled as counter-move).*

---

## Changes from v30

**Aspirational tier expanded: 33 → 36 criteria, per-criterion value: 10/33 → 10/36 = ~0.278 pts**

| New ID | Pattern | Source signal | Distinction |
|--------|---------|---------------|-------------|
| C-41 | **Bidirectional counter-move coverage with independent assertions** — the coverage structure addresses both the F→R direction (failure-as-rows, mapping each failure to the rules that counter it) and the R→F direction (rule-as-rows, mapping each rule to the failure it counters), with each structure carrying its own independent completeness assertion so coverage is verifiable from either direction without reference to the other | R14 excellence signals: V-01's "Opening 2-row failure taxonomy table (F→R direction) + 7-row RULE COVERAGE TABLE (R→F direction), both with independent completeness assertions" — bidirectionality was implicit in R13 V-01 but R14 V-01 makes the independence of the two assertions explicit, establishing that each table is self-certifying rather than jointly covering the same claim | C-39 = one dedicated structure in one direction with a completeness assertion; C-41 = BOTH directions present, each independently asserting its own completeness — a single manifest satisfies C-39; only two complementary-direction structures with separate assertions satisfy C-41 |
| C-42 | **Pre-catalog zone commitment criteria** — each arc layer/lifecycle zone carries a structured multi-part commitment test (zone-exit question, minimum artifact-type count floor, required gate pattern class) declared before the skill catalog opens, converting zone transitions from organizational conveniences into evidence gates, and the phase/band table is extended with a zone commitment check column or equivalent per-zone field | R14 excellence signals: V-02's distinctive "each lifecycle zone carries 3-condition commitment criteria (zone-exit question, minimum artifact types, required gate pattern class) declared before catalog opens" and "Phase table extended with Zone commitment check column" — extends the per-layer arc narration framework (C-20) with pre-catalog structured commitment criteria that make zone boundaries load-bearing rather than cosmetic | C-04 = per-stage non-trivial gate; C-09 = compound gate with quantified quality condition; C-42 = per-zone multi-part commitment test declared pre-catalog at the structural level — stage gates satisfy C-04/C-09; only zone-level multi-part advance criteria declared before catalog satisfy C-42 |
| C-43 | **Status-quo path named in coverage structure** — the RULE COVERAGE TABLE (or equivalent rules manifest) includes a dedicated "Status-Quo Path" column naming the concrete inertia behavior that each rule displaces, and the opening failure taxonomy table gains a "Status-Quo Failure" column, making the positive status-quo behavior structurally visible alongside each rule entry rather than confined to opening prose; optionally, ARTIFACT 0 annotations extend to include per-rule "Status-quo failure to prevent:" sentences | R14 excellence signals: V-03's "3-column RULE COVERAGE TABLE adds 'Status-Quo Path' column", "Opening 'knowable information' table gains 'Status-Quo Failure' column", and "ARTIFACT 0 consumer-pull rule adds 'Status-quo failure to prevent' sentence" — makes the inertia being countered a named structural column rather than implicit context, so a reader encounters the displacement relationship at point of reference | C-37 = failure-mode types named in opening contrast prose; C-43 = status-quo behavior named in the coverage manifest's column structure — C-37 names failures narratively; C-43 names status-quo paths structurally. Distinct from C-40: C-40 is inline annotation at rule definition site; C-43 is a structural column in the manifest — they pass independently |

**Dependency chain for new criteria:**
- C-41 requires C-39 PASS (bidirectionality extends the single-direction mandate C-39 establishes; without an initial directional structure there is nothing to make bidirectional)
- C-42 requires C-20 PASS (per-layer arc narration establishes the zone/layer structure that zone commitment criteria extend)
- C-43 requires C-39 PASS (the status-quo path column extends the manifest C-39 establishes; without a manifest there is no column to add)
- C-41 and C-43 both inherit C-39's dependency: C-39 requires C-37 PASS, C-37 requires C-16 PASS

**Scoring arithmetic**: 36 criteria × (10/36) = 10.00 pts total. Formula `aspirational_pass/36 * 10` yields exact values. Perfect aspirational score remains 10 pts.

**R14 re-scores at v31 (V-01, V-02, V-03 — variations that drove new criteria):**

| Variation | C-41 | C-42 | C-43 | Aspirational | Composite |
|-----------|------|------|------|-------------|-----------|
| V-01 (Dual-Structure Coverage) | PASS | FAIL | FAIL | 34/36 | ~99.44 |
| V-02 (Zone Commitment Tests) | PASS | PASS | FAIL | 35/36 | ~99.72 |
| V-03 (Status-Quo Competitor Column) | PASS | FAIL | PASS | 35/36 | ~99.72 |

*Note: All three R14 variations pass C-41 — V-01 explicitly documents "both with independent completeness assertions"; V-02 inherits the same dual-structure from V-01; V-03 has both the opening taxonomy table (F→R, with completeness claim per C-37) and the 3-column RULE COVERAGE TABLE (R→F, "This table is exhaustive: every rule appears as exactly one row"). C-42 passes only for V-02, which is its source signal. C-43 passes only for V-03, which is its source signal. V-02 and V-03 tie at 35/36 aspirational (composite ~99.72). No single R14 variation achieves 36/36 — a variation combining zone commitment (C-42) and status-quo path column (C-43) would be the tiebreaker target for R15.*
