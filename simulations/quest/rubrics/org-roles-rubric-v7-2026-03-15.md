**`org-roles-rubric-v7-2026-03-16.md`** written.

Three new aspirational criteria extracted from the Round 6 excellence signals:

| ID | Source | Pattern | Distinguishes from |
|----|--------|---------|-------------------|
| **C-31** | R6 V-01 (4 locations), R6 V-02 (3 locations) | Placeholder name prohibition appears in a minimum of three independent checkpoints: contract block, derivation prose, gate condition | C-28 requires at least one explicit naming; C-31 requires redundant multi-site enforcement |
| **C-32** | R6 V-01 ("C-27 failure — named explicitly"), R6 V-02 ("Derivation from prior-phase plans is a C-27 failure named") | PREPARE sub-step names the specific bypass anti-pattern it prevents — C-28 principle applied to count integrity | C-27 requires the mechanism (named variable); C-32 requires the mechanism to also name its own failure class |
| **C-33** | R6 V-01 ("template annotations replicate both"), R6 V-02 ("template annotations replicate both labels") | CONTRACT VIOLATION labels from the FC block are mirrored in the template annotation at the write site | C-29 requires contract-block framing; C-33 requires that framing to also appear where the model authors output |

**Formula**: `/22` → `/25`. R6 V-01 and V-02 both score **100.00** under v7 (25/25 aspirational). R5 V-05 drops from 99.55 (v6) to **99.20** (v7) — it passes C-32 and C-33 but misses C-31 because it never named placeholder anti-patterns at all.
