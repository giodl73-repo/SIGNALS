**v20 written** to `simulations/quest/rubrics/flow-lifecycle-rubric-v20-2026-03-16.md`.

---

**Two new criteria extracted from Round 19 signals:**

**C-57 — Gate Defect D-ID Cross-Reference** (clarity)
Closes the gap between C-55's pass condition (category names appear inline in the gate text) and the stronger form where each category carries a parenthetical D-ID that row-specifically navigates to its scan-table entry. V-04 passes C-55 with plain prose names ("an unterminated path, an uncertified exception handler…"); V-01/V-02/V-03/V-05 go further with "…(D-01), …(D-02), …(D-03)" — making the gate-to-taxonomy linkage row-specific rather than requiring a category-name scan across the taxonomy.

**C-58 — Parenthetical Eligibility Binding Format** (clarity)
Closes the gap between C-56's pass condition (eligibility condition is co-located with the escape code) and the stronger form where code token + eligibility clause form a single typographic unit via immediate parenthetical adjacency. V-04 passes C-56 with "write SECONDARY -- but only when…" (instruction verb + dash separator); V-01/V-02/V-03/V-05 use "SECONDARY (use only when…)" — no separator, code token directly precedes open paren, unit is parseable in isolation.

**Counts:** Denominator 56 → 58. Aspirational count 48 → 50.
