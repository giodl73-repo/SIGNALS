The R18 scorecard surfaces two distinct excellence patterns not captured by any existing criterion:

1. **V-02's schema-constrained table format** — The 3-column table with a "Governing Criterion" column enforces criterion citation through schema structure, not author discipline. A prose block (V-01) produced a PARTIAL because the format allowed mechanism descriptions without criterion numbers; the table format makes an incomplete criterion reference structurally apparent.

2. **V-03's criterion-coupled artifact** — The annotated phase diagram simultaneously satisfies C-52 (workflow sequence) and C-53 (layer declaration) through a single structure, binding both requirements together and making divergence architecturally impossible.

These become **C-54** and **C-55**. Here is the complete v19 rubric:

---

```markdown
# topic-story Rubric — v19

**Version**: v19
**Date**: 2026-03-15
**Criteria count**: 55 (C-01–C-55)
**Aspirational count**: 48 (C-08–C-55), 10 pts / 48 = 0.2083 pts each
**R18 baseline under v19**: V-02 passes 30 of 48 aspirational criteria (1 PARTIAL) — 90 + (30 × 0.2083) + (1 × 0.1042) = 90 + 6.249 + 0.104 = **96.35**

---

## Design Rationale (v19)

Two aspirational criteria added in v19 (C-54, C-55), from Round 18 excellence signals:

**C-54** — From R18 Signal 1 (V-02 3-column table passing C-53 where V-01 prose block produced PARTIAL). V-01 satisfied C-53's spirit but failed the criterion-citation sub-requirement because a prose compliance architecture block is syntactically complete whether or not criterion numbers are present — the format provides no structural signal that criterion references are missing. V-02's 3-column table with a "Governing Criterion" column header changed this: the column schema requires a criterion-reference entry for each row, making an incomplete citation structurally apparent rather than semantically hidden in prose. The structural gap C-53 leaves open: C-53 requires criterion references in the layer declaration but does not specify a format that enforces this requirement structurally. A prose block satisfies C-53 if the author chose to include criterion numbers; the same prose format fails C-53 if the author used mechanism descriptions instead. The format itself does not distinguish between the two cases — an evaluator must verify criterion citation semantically. A schema-constrained format closes this gap: the "Governing Criterion" column exists as a mandatory schema component, and its presence or emptiness is a structural property of the completed declaration. C-54 fires on whether the declaration format's own schema enforces criterion citation — making it impossible to complete the declaration without providing criterion references — rather than relying on author discipline to include them.

**C-55** — From R18 Signal 2 (V-03 annotated phase diagram satisfying both C-52 and C-53 through a single artifact). C-52 and C-53 can each be satisfied through independent structures: a role-sequence instruction satisfies C-52 while a separate prose block or table satisfies C-53. When satisfied independently, two structures describe the same compliance architecture and can diverge — the workflow sequence may describe three phases while the layer declaration names two layers, or the two structures may use inconsistent terminology for the same mechanisms. A future rubric evolution may update one structure without updating the other. V-03 illustrated the alternative: the annotated phase diagram satisfies C-52 through the diagram's phase sequence (evaluator phase produces S2 verdicts; S4b authoring is a successor phase) and satisfies C-53 through the diagram's legend (each phase named as a compliance layer with criterion reference and inter-layer ordering stated through the sequence arrows and annotation text). The two compliance requirements are properties of a single artifact, not two independent structures. An evaluator auditing compliance for both C-52 and C-53 examines the same element; a rubric evolution that modifies the phase sequence necessarily modifies the layer declaration simultaneously, making divergence architecturally impossible. C-55 fires on whether C-52 and C-53 are satisfied through a single criterion-coupled artifact — one structure that provides both the workflow sequence constraint (C-52) and the layer declaration (C-53), co-locating and binding both requirements such that they cannot diverge.

---

*(full inherited rationale v16→v1, essential/recommended/aspirational criteria C-01–C-53 unchanged from v18)*

---

### C-51 — S4b Is Structurally Partitioned into a Verdict Inventory Stage and a Characterization Stage with Constrained Inputs

- **Weight**: aspirational
- **Category**: behavior
- **Text**: S4b is architecturally partitioned into two distinct stages: Part 1 (verdict inventory) and Part 2 (characterization), where Part 2 is explicitly constrained to draw only from Part 1's named signal inventory. C-49 fires on whether the S2→S4b chain is coherent and legible from the artifact — it is a content coherence requirement: an evaluator can verify the chain by checking whether named S4b signals match S2 YES/PARTIAL verdicts. C-51 fires on whether S4b has a structural partition that separates the signal inventory activity from the characterization activity and formally constrains Part 2's inputs to Part 1's outputs — it is an architectural partition requirement: an evaluator can verify compliance by checking whether the two-stage structure exists and whether Part 2 names Part 1 as its exclusive input. The structural gap between C-49 and C-51: an author satisfying C-49 without C-51 produces a coherent chain through narrative integration — the chain is traceable because the author chose to cross-reference S2 verdicts, but no structural mechanism prevents the author from drawing on additional signals outside the YES/PARTIAL set during the characterization activity. A two-part S4b partition closes this gap: Part 1 enumerates only YES/PARTIAL signals (no independently added signals permitted in Part 1); Part 2 draws characterization exclusively from Part 1 (no direct reference to the full signal set permitted in Part 2). The partition makes the selection and characterization activities structurally separate and the constraint on Part 2's inputs formally legible — an evaluator need not infer whether characterization was independently composed; the architecture expresses the constraint. The governance consequence: C-51 transforms S4b composition from a chain-legibility requirement (C-49) into a process-partition requirement, changing the audit question from "does the result trace back to S2?" to "does the structure enforce constrained inputs?" A rubric satisfying C-49 but not C-51 produces a legible chain; a rubric satisfying both produces a legible chain whose composition process is architecturally constrained. C-51 does not supersede C-49: C-49 passes when the chain is content-traceable; C-51 passes only when the two-part partition exists and Part 2 explicitly names Part 1 as its exclusive input source.
- **Pass condition**: S4b contains two explicitly labeled stages — a Part 1 (verdict inventory) that lists only YES/PARTIAL signals from S2 with their verdict labels (e.g., `(S2 verdict: YES/PARTIAL)` per entry), and a Part 2 (characterization) that explicitly states it draws only from Part 1 — using a constraint phrase, prohibited-form reminder, or explicit scope restriction co-located within Part 2's section prompt (e.g., "This section draws directly from Part 1 — not from an independent review of all signals"). A unified S4b section that integrates cross-references without a structural partition does not pass C-51, even if C-49 is satisfied. Part 2 must name Part 1 as its exclusive input within Part 2's authoring slot, not merely in a preamble or design rationale section.

---

### C-52 — The Authoring Workflow Specifies an Evaluator-First Production Sequence Making S2 Verdicts Named Artifacts Before S4b Authoring Begins

- **Weight**: aspirational
- **Category**: behavior
- **Text**: The authoring workflow specifies a distinct evaluator role or phase that must produce S2 verdicts as named outputs before an author role or phase begins S4b, creating temporal production-order enforcement of the S2→S4b chain through workflow structure rather than content gates or slot instructions. C-19 fires on whether a pre-artifact checklist exists as a gate before writing begins — a content mechanism that positions criteria before the writing context. C-50 fires on whether criteria are embedded as slot-label constraints at the point of authoring action — a content mechanism that positions criteria inside governed slots. C-52 fires on a third compliance architecture: a workflow-level temporal ordering constraint that makes S2 verdicts exist as discrete produced artifacts when S4b authoring begins, not as instructions the author should consult but as completed outputs of a prior role phase. The structural distinction between C-52 and C-19/C-50: a pre-artifact checklist (C-19) is present before writing begins but operates within a single-role workflow — the same author who reads the checklist proceeds to write S2 and S4b in sequence; a slot-label constraint (C-50) positions the criterion at the authoring point but within a single-role workflow — the same author who reads the slot label writes both S2 and S4b. Neither C-19 nor C-50 introduces a role boundary that separates the S2 evaluation activity from the S4b authoring activity. C-52 introduces a role boundary: an evaluator role produces S2 verdicts as complete named outputs, and an author role receives those outputs before beginning S4b. The evaluator's S2 outputs are not embedded instructions — they are artifacts that exist independently when S4b authoring begins. The compliance enforcement is temporal: the workflow sequence makes S4b authoring structurally downstream of evaluator verdict production. An author in a C-52-compliant workflow cannot begin S4b before S2 verdicts exist, not because a slot instruction directs them not to, but because the workflow sequence designates S4b as a successor activity. The governance consequence: C-52 makes S2 verdict completeness a prerequisite condition of S4b authoring entry, not a compliance obligation during S4b authoring. A rubric satisfying C-52 produces a compliance architecture orthogonal to C-19 and C-50: gate-level, slot-level, and workflow-level enforcement can coexist, each operating through a different mechanism and closable through a different failure mode. C-52 does not require C-19 or C-50; satisfying all three produces a rubric with three independent compliance enforcement layers. C-52 passes only when the rubric explicitly specifies an evaluator role or phase designated to complete S2 verdict production, with S4b authoring designated as a successor activity to that phase — the production sequence must be explicit in the rubric's workflow structure, not implied by narrative framing or the author's good intent.
- **Pass condition**: The rubric explicitly designates a workflow with at least two roles or phases: (1) an evaluator role or phase that produces S2 verdicts as named outputs (e.g., a `**EVALUATOR**` section or role label that encompasses S2), and (2) an author role or phase for which S4b is a designated activity, positioned as a successor to the evaluator phase. The production-order constraint must appear in the rubric's workflow structure — either as a role-sequence instruction, a phase diagram, or an explicit prerequisite statement (e.g., "S4b authoring begins after evaluator S2 verdicts are complete"). A rubric that encourages the author to consult S2 before writing S4b does not pass C-52; a rubric that uses a single undifferentiated author role does not pass C-52. The role or phase boundary must be explicit in the rubric's authoring instructions, making the sequence constraint a structural property of the workflow rather than an advisory.

---

### C-53 — The Rubric Explicitly Declares Its Multi-Layer Compliance Architecture, Naming Each Enforcement Layer, Its Governing Criterion, and Inter-Layer Ordering Relationships

- **Weight**: aspirational
- **Category**: behavior
- **Text**: The rubric explicitly declares the full set of compliance enforcement layers present in its authoring workflow — naming each layer, referencing its governing criterion, and stating the ordering relationships between layers where an ordering exists — making the multi-layer compliance architecture a formally declared and verifiable property of the rubric rather than an emergent consequence of satisfying individual criteria independently. C-51 fires on whether S4b is architecturally partitioned into verdict inventory and characterization stages (partition layer). C-52 fires on whether the workflow specifies an evaluator-first production sequence making S2 verdicts named artifacts before S4b begins (workflow/temporal layer). C-53 fires on a third orthogonal question: whether the rubric contains a meta-declaration that names these layers as a coordinated architecture and specifies how the layers are ordered. The structural distinction between satisfying C-51 and C-52 without C-53 and satisfying all three: a rubric implementing S4b partition and evaluator-first role sequence satisfies two requirements independently — an evaluator verifying the rubric confirms each mechanism is present, but the layers are not declared as a system. This is a compositional achievement: the layers coexist but are not named, counted, or ordered as a declared architecture. When a third mechanism is added (e.g., phase-sequence prerequisites as a second C-52 implementation path, as in V-05), the compositional approach scales without any mechanism requiring the rubric author to declare how the new layer interacts with existing layers or whether the total declared layer count is correct. V-05 illustrated this failure mode: three enforcement mechanisms were simultaneously present — named role labels, S4b structural partition, and phase-sequence prerequisites — each independently satisfying its governing criterion, but no declaration named the three layers or their ordering relationships. A future rubric evolution could silently remove or reorder a layer without breaking C-51 or C-52 individually, because no mechanism requires the layer count and ordering to be declared and verified as a system property. C-53 closes this gap: a rubric satisfying C-53 explicitly names each enforcement layer (e.g., "Layer 1: S4b structural partition [C-51]; Layer 2: evaluator-first production order [C-52]; Layer 3: lifecycle phase prerequisites [C-52 secondary path]"), states how the layers are ordered (e.g., "Layer 2 governs when S4b may begin — the evaluator-first phase must complete before S4b authoring enters; Layer 1 governs what S4b may contain once begun — Part 2 is constrained to Part 1's outputs; Layer 3 reinforces Layer 2 through phase-sequence gates"), and co-locates this declaration with the authoring workflow instructions rather than only in design rationale. The governance consequence: C-53 transforms multi-layer compliance from emergent architecture (layers present because criteria are independently satisfied) into declared architecture (layers named, ordered, and auditable as a system). An evaluator auditing a C-53-compliant rubric can verify not only whether each layer is present individually but whether the declared inter-layer ordering relationships are honored and whether the declared layer count matches the number of enforcement mechanisms implemented. C-53 does not require any particular number of layers or any specific ordering schema; it fires on whether the layers that exist are explicitly declared as a system in the authoring instructions. C-51 passes when the partition architecture exists; C-52 passes when the production-order workflow constraint exists; C-53 passes only when the rubric contains an explicit enforcement-layer declaration — with names, criterion references, and inter-layer ordering relationships — co-located with the authoring workflow, not merely implied by the coexistence of multiple enforcement mechanisms.
- **Pass condition**: The rubric contains an explicit enforcement-layer declaration naming each compliance enforcement mechanism present in the authoring workflow, referencing its governing criterion (e.g., C-51, C-52), and stating any ordering relationship between layers where an ordering exists. The declaration must appear in the rubric's authoring instructions or structural preamble — co-located with or directly referenced by the authoring workflow — not only in a design rationale or commentary section. The declaration may take the form of a numbered layer list, a phase-diagram legend with criterion annotations, or an explicit "compliance architecture" section, provided (a) each layer is named with its criterion reference, (b) inter-layer ordering relationships are explicitly stated where they exist, and (c) the total number of declared layers matches the number of enforcement mechanisms implemented in the rubric. A rubric that implements multiple enforcement mechanisms without an explicit layer declaration does not pass C-53, even if each mechanism individually satisfies its governing criterion. A declaration that names layers in design rationale but not in authoring instructions does not pass C-53 — the declaration must be present in the authoring workflow where an evaluator operating the rubric will encounter it.

---

### C-54 — The Compliance Architecture Declaration Uses a Schema-Constrained Format That Structurally Enforces Criterion Citation

- **Weight**: aspirational
- **Category**: behavior
- **Text**: The compliance architecture declaration required by C-53 uses a format whose schema structurally enforces criterion citation — the format designates a mandatory criterion-reference component for each layer entry (e.g., a dedicated "Governing Criterion" column in a table), making the presence or absence of criterion numbers a structural property of the completed declaration rather than a semantic property of the prose content. C-53 fires on whether the rubric contains an explicit enforcement-layer declaration naming each layer with its governing criterion and inter-layer ordering relationships. C-53 does not specify the format of this declaration — prose, table, diagram legend, or structured list each constitute valid declaration vehicles provided the required elements are present. C-54 fires on a second-order property of the declaration: whether the format's own schema enforces criterion citation structurally, making it impossible to complete the declaration without providing criterion references. The structural gap C-53 leaves open: a prose compliance architecture block (V-01) is syntactically complete whether or not criterion numbers are present — "Governing mechanism: structural partition" and "Governing criterion: C-51" are both valid prose entries, but only one satisfies C-53's criterion-citation sub-requirement. The format itself provides no structural signal that criterion references are missing; an evaluator must verify citation as a semantic step by reading the prose content. V-01 produced a C-53 PARTIAL precisely because prose format allows mechanism descriptions without criterion numbers, and the format provides no structural reminder that criterion numbers are the required form. A schema-constrained format closes this gap: V-02's 3-column table with a "Governing Criterion" column header creates a schema-level requirement for criterion references in column 3. The author cannot produce a complete table row without providing a column 3 entry, and the column header designates exactly what that entry must contain. An incomplete criterion citation — a mechanism description in column 3 rather than a criterion number — is structurally apparent because the column schema names the expected content type. An evaluator auditing C-54 compliance checks whether the declaration format contains a dedicated schema element (column, labeled field, or structured slot) whose header or label designates criterion citation — the format's structure is itself evidence of criterion-citation enforcement, independent of what any individual row contains. The governance consequence: C-54 transforms criterion citation from a content obligation enforced by author discipline (C-53) into a format obligation enforced by schema structure. A rubric satisfying C-53 but not C-54 declares layers with criterion references because the author chose to include them in open-ended prose or a list; a rubric satisfying both declares layers in a format whose schema demands criterion references regardless of author discipline. The failure mode C-54 prevents: a future rubric evolution that preserves the declaration's prose structure while substituting mechanism descriptions for criterion numbers satisfies C-53's intent but fails its criterion-citation sub-requirement — and the format provides no signal that the sub-requirement has been violated. A schema-constrained format makes this substitution structurally apparent. C-54 does not require a table specifically; a structured list template with explicit criterion-reference placeholders, or any format whose schema designates a mandatory criterion-citation field, satisfies C-54. The requirement is schema-enforcement of criterion citation, not a particular output form.
- **Pass condition**: The compliance architecture declaration uses a format whose schema designates a mandatory criterion-citation component for each layer entry — for example, a table with a column explicitly labeled "Governing Criterion" (or equivalent designation), or a structured list template where each layer entry includes an explicit criterion-reference placeholder labeled to indicate criterion numbers are required. The format must make incomplete criterion citation structurally apparent — either through a visibly empty or missing schema component — rather than relying on prose narrative to embed criterion numbers. A prose compliance architecture block that includes criterion numbers passes C-53 but does not pass C-54 unless the prose format contains a labeled schema element that specifically designates the criterion-reference field. A 3-column table with column 3 labeled "Governing Criterion" passes C-54 because the column schema requires criterion references for each row; a 2-column table with column 2 labeled "Mechanism Description" does not pass C-54 even if criterion numbers appear within the mechanism description column, because the column schema does not designate criterion citation as the required content type. A declaration in a format with no schema element dedicated to criterion citation does not pass C-54 regardless of whether criterion numbers are present in the content, because the schema does not enforce their presence.

---

### C-55 — C-52 and C-53 Compliance Is Satisfied Through a Single Criterion-Coupled Artifact That Co-Locates Workflow Sequence and Layer Declaration in One Structure

- **Weight**: aspirational
- **Category**: behavior
- **Text**: C-52 and C-53 compliance is satisfied through a single criterion-coupled artifact — a structure that simultaneously provides the workflow sequence constraint required by C-52 (evaluator-first production sequence making S2 verdicts named artifacts before S4b authoring begins) and the layer declaration required by C-53 (explicit enforcement-layer names, criterion references, and inter-layer ordering relationships), binding both requirements to one co-located structure and making the two declarations mutually consistent by design rather than by author discipline. C-52 fires on whether the authoring workflow specifies an evaluator-first production sequence — satisfied by a phase diagram, role-sequence instruction, or explicit prerequisite statement. C-53 fires on whether the rubric contains an explicit multi-layer compliance architecture declaration — satisfied by a prose block, table, or phase-diagram legend. Both criteria can be satisfied by independent structures: a role-sequence instruction satisfies C-52 while a separate prose block or table satisfies C-53. C-55 fires on a third property: whether the two requirements are satisfied by a single co-located artifact, creating criterion coupling between the workflow sequence and layer declaration. The structural gap between satisfying C-52 and C-53 through independent structures versus through a single coupled artifact: when satisfied independently, two structures describe the same compliance architecture and can diverge. The workflow sequence may describe three phases while the layer declaration names two layers; or the layer declaration may name Layer 2 as "evaluator-first production order" while the workflow sequence uses different terminology for the same constraint. An evaluator auditing compliance for both C-52 and C-53 must cross-reference two independent structures and verify their consistency — a step that is a maintenance discipline rather than an architectural guarantee. A future rubric evolution may update the workflow sequence (adding a phase) without updating the layer declaration (which still names the original layer count), producing a rubric that satisfies both criteria individually but describes the compliance architecture inconsistently across its two structures. A criterion-coupled artifact eliminates this gap: V-03's annotated phase diagram satisfies C-52 through the diagram's phase sequence arrows (which make S2 verdicts a named output of an evaluator phase and designate S4b authoring as a successor phase) and satisfies C-53 through the diagram's legend (which names each phase as a compliance layer, references its governing criterion as an annotation, and states inter-layer ordering through the sequence arrows and legend text). The workflow sequence and the layer declaration are not two structures — they are one structure read through two compliance lenses. An evaluator auditing C-52 and C-53 examines the same artifact for both. A rubric evolution that adds a phase to the diagram necessarily modifies the legend simultaneously; a layer declaration update necessarily modifies the sequence that produced it. Divergence between the workflow sequence and the layer declaration is architecturally impossible when both are properties of the same artifact. The governance consequence: C-55 transforms C-52 and C-53 from independently satisfiable requirements (each closeable through its own artifact, each maintainable separately) into a coupled requirement pair whose joint satisfaction is a structural property of a single co-located artifact. C-55 does not require a phase diagram specifically — any single structure that simultaneously satisfies C-52's pass condition (designated evaluator phase producing S2 verdicts as named outputs, S4b as successor activity) and C-53's pass condition (named layers with criterion references and inter-layer ordering, co-located with authoring workflow) satisfies C-55. The requirement is criterion coupling through co-location, not a particular artifact form. C-55 does not supersede C-52 or C-53; satisfying C-55 implies satisfying both C-52 and C-53, but C-52 and C-53 can each be satisfied without C-55.
- **Pass condition**: The rubric satisfies both C-52 and C-53 through a single structural artifact — one element of the authoring instructions that provides (a) the workflow sequence constraint satisfying C-52's pass condition: a designated evaluator role or phase that produces S2 verdicts as named outputs, with S4b authoring as an explicitly designated successor activity; and (b) the layer declaration satisfying C-53's pass condition: named enforcement layers with criterion references, inter-layer ordering relationships stated, and the declaration co-located with the authoring workflow. The artifact must explicitly serve both functions within its own structure — the sequence constraint and the layer declaration must both be derivable from reading the single artifact, without requiring cross-reference to a separate structure for either. A rubric that satisfies C-52 through a role-sequence instruction and C-53 through a separate prose block or table does not pass C-55 even if both C-52 and C-53 individually pass — the compliance requirements must be satisfied by the same artifact, not by two independently compliant structures. An annotated phase diagram satisfies C-55 when its phase sequence arrows define the evaluator-first production order (C-52 element) and its legend names each phase as a compliance layer with criterion annotations and inter-layer ordering statements (C-53 element), provided both elements appear within the diagram structure and either element read in isolation would satisfy its governing criterion's pass condition.

---

## Scoring Reference

| Band | Score | Meaning |
|------|-------|---------|
| Essential fail | 0–59 | One or more essential criteria failed |
| Passing | 60–79 | All essential pass; recommended weak |
| Good | 80–89 | All essential pass; recommended mostly pass |
| Golden | 90–100 | All essential pass; recommended strong; aspirational present |

**Composite formula**: `essential_score + recommended_score + aspirational_score`

- Essential: 15 pts per criterion × 4 = 60 pts. All-or-nothing per criterion (no partial).
- Recommended: 10 pts per criterion × 3 = 30 pts. Partial credit allowed (5 pts for PARTIAL).
- Aspirational: 10 pts total / 48 criteria = 0.2083 pts each (C-08–C-55). Partial credit allowed (0.1042 pts for PARTIAL).
- **Max score**: 100.0

**R18 baseline under v19 rubric**: V-02 passes 30 of 48 aspirational criteria (1 PARTIAL) — 90 + (30 × 0.2083) + (1 × 0.1042) = 90 + 6.249 + 0.104 = **96.35**

---

## What Changed from v18

| | v18 | v19 |
|---|---|---|
| Aspirational criteria | 46 (C-08–C-53) | 48 (C-08–C-55) |
| Per-criterion weight | 0.2174 pts | 0.2083 pts |
| PARTIAL weight | 0.1087 pts | 0.1042 pts |
| Baseline round | R17 | R18 |
| New criteria | C-53 | C-54, C-55 |

**C-54 source**: V-02 (3-column table) passing C-53 where V-01 (prose block) produced PARTIAL — the table's "Governing Criterion" column enforces criterion citation through schema structure, revealing that C-53 leaves the declaration format unspecified and therefore open to criterion-number-free prose.

**C-55 source**: V-03 (annotated phase diagram) simultaneously satisfying C-52 (workflow sequence) and C-53 (layer declaration) through a single artifact — the diagram's sequence arrows and legend serve both criteria, making the workflow sequence and layer declaration co-located and structurally bound to prevent divergence.
```

---

**Why these two signals and not others:**

V-01's PARTIAL was a *known failure mode* of C-53 (prose without criterion numbers) — it validates C-53's pass condition works correctly, no new criterion needed. V-02 and V-03 each revealed a structural property that C-53 doesn't reach:

- V-02 showed that the *format* can enforce the sub-requirement C-53 merely *requires* — a second-order compliance mechanism C-53 doesn't distinguish from prose
- V-03 showed that two criteria (C-52 + C-53) that are normally independently satisfiable can be architecturally coupled into one artifact, eliminating a divergence failure mode that exists when they are satisfied separately

The R18 baseline drops from 96.41 (V-02 under v18) to **96.35** (V-02 under v19) — V-02 gains C-54 but loses to dilution; V-03 gains C-55 but loses to dilution; both tie at 96.35, so V-02 is used as the named baseline.
