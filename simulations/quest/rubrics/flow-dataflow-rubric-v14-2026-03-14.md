Reading the scorecard carefully for R13 excellence signals before writing.

Two distinct patterns emerge from the visible scorecard data:

1. **V-01 C-15 PARTIAL + V-02 C-15 PARTIAL** — baseline-to-recovery/trade-off closure is consistently achieved through execution discipline but never enforced by a named SC with inline callbacks. The connection is structurally invisible to evaluation.

2. **V-02 C-28 FAIL / C-30 PARTIAL / C-32 PARTIAL + C-33 PASS** — non-tabular variations have no upfront criterion substitution map. Evaluators must infer which tabular criteria apply in modified form and what the prose substitute is. C-33 declares the register; nothing declares the criterion-level format substitution.

36 → 38 aspirational criteria; new weight `10/38 ≈ 0.263 pts` each.

---

# flow-dataflow Rubric — v14

## Change Log

| Version | Date | Change |
|---------|------|--------|
| v1 | 2026-03-14 | Initial rubric — 4 essential, 3 recommended, 2 aspirational |
| v2 | 2026-03-14 | Added C-10..C-12 from R1 excellence signals: pre-trace vocabulary gate, interleaved boundary gates, domain entity exposure per boundary |
| v3 | 2026-03-14 | Added C-13..C-15 from R2 excellence signals: pre-declared staleness contract, additive elapsed time calculation, incumbent baseline section |
| v4 | 2026-03-14 | Added C-16..C-18 from R3 excellence signals: cross-role reference chain, immutability declaration, incremental boundary elapsed computation |
| v5 | 2026-03-14 | Added C-19..C-21 from R4 excellence signals: machine-verifiable citation convention, stage-write progression gate, binary freshness verdict per boundary |
| v6 | 2026-03-14 | Added C-22..C-24 from R5 excellence signals: formal pre-declared registry table, phase gate self-verification checklists, upfront constraint section with inline callback syntax |
| v7 | 2026-03-14 | Added C-25..C-27 from R6 excellence signals: phase gate artifacts as first-class registry rows, non-natural role ordering as citation stress test, named recovery formula anchoring incumbent baseline |
| v8 | 2026-03-14 | Added C-28..C-30 from R7 excellence signals: named-column boundary block schema for column-level completeness verification, trade-off analysis as prompt-required section with baseline token citation, output register declaration with format-specific field-compliance markers |
| v9 | 2026-03-15 | Added C-31..C-33 from R8 excellence signals: pre-declared boundary block schema section, register-specific compliance marker mapping per structural field, register-invariant declaration for non-tabular output registers |
| v10 | 2026-03-14 | Added C-34..C-35 from R9 excellence signals: per-boundary incumbent equivalent column with token-cited baseline derivation, INCUMBENT TOTAL as required pre-trade-off closing artifact with additive breakdown and token citation in trade-off section |
| v11 | 2026-03-15 | Added C-36..C-38 from R10 excellence signals: baseline-first artifact ordering for citation-complete boundary cells, REGISTER DECLARATION Parts A/B as single-location C-34/C-35 authority, skip-level citation requirement in non-natural role ordering |
| v12 | 2026-03-15 | Added C-39..C-41 from R11 excellence signals: skip-level SC must name governed role in its body, skip-level SC must declare position distance in its body, Phase Gate 2 skip-level verification item must cite governing SC identifier by number |
| v13 | 2026-03-15 | Added C-42..C-43 from R12 excellence signals: C-37+C-41 closed verification chain as co-occurrence requirement, all three skip-level SC attributes co-present in a single SC block without distribution |
| v14 | 2026-03-15 | Added C-44..C-45 from R13 excellence signals: baseline-closure SC as named upfront constraint enforcing C-15's C-08/C-09 connection by token callback, format-mode declaration with criterion substitution map for non-tabular register variants |

## Purpose

Score a `flow-dataflow` skill output: a data lineage trace covering source, transformation stages,
and destination for a Finance, Operations, or Commerce pipeline scenario.

---

## Essential Criteria (weight: 60 pts total)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Data lineage chain** | correctness | Every in-scope item has an unbroken source => stage(s) => destination chain. No item appears at a destination without an identified origin. |
| C-02 | **Boundary error handling** | correctness | Each boundary is annotated: either the mechanism (retry, dead-letter, rollback) or an explicit "no handling" flag. Silence is not acceptable. |
| C-03 | **Data loss point identification** | coverage | At least one concrete loss point is named per pipeline stage. Generic "errors may occur" does not qualify. |
| C-04 | **Schema state at each stage** | correctness | Schema at every stage entry/exit is described or diffed. A stage that changes field names or types with no schema note fails. |

---

## Recommended Criteria (weight: 30 pts total)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-05 | **Latency characterization** | depth | Every stage has a latency annotation: value, range, or characterization (real-time / batch / daily). |
| C-06 | **Stale data windows** | depth | At least one stale window quantified. Failure-mode staleness addressed separately from normal-operation staleness. |
| C-07 | **Domain framing** | format | Domain-specific entity names appear in the lineage chain. Purely generic trace fails. |

---

## Aspirational Criteria (weight: 10 pts total) — **38 criteria in v14**

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-08 | **Recovery prescriptions** | depth | Every "no handling" annotation from C-02 and every loss point from C-03 has a paired, specific recovery suggestion. |
| C-09 | **Pattern trade-off analysis** | behavior | Named alternative pattern with at least two quantified or qualified trade-off dimensions. |
| C-10 | **Pre-trace domain context gate** | format | A DOMAIN CONTEXT section appears before the first stage block, locking entity names, downstream consumer, and business cadence. At least two of those terms reappear verbatim in subsequent lineage or stale-window content. |
| C-11 | **Interleaved boundary gates** | behavior | A boundary check block appears between every consecutive stage pair — inline, not post-hoc. A trailing BOUNDARY SUMMARY alone does not satisfy this criterion. |
| C-12 | **Domain entity exposure per boundary** | format | Every boundary check that satisfies C-11 names a domain entity from the scenario vocabulary. "data" or "records" alone does not pass. |
| C-13 | **Pre-declared staleness contract** | depth | The stale threshold for at least one entity is declared as an explicit contract in DOMAIN CONTEXT before Stage 1 is written. The stale analysis derives its rows by evaluating actual elapsed time against this threshold. A stale window asserted or synthesized after the stages does not pass. |
| C-14 | **Additive elapsed time calculation** | depth | Latency annotations across stages and boundaries are accumulated into an explicit end-to-end elapsed total. This total appears in or immediately before the stale analysis, enabling direct numeric comparison against C-13's threshold. Per-stage annotations alone with no cumulative total do not pass. |
| C-15 | **Incumbent or manual-process baseline** | behavior | A named baseline section describes the incumbent or manual process this pipeline replaces. The baseline must surface at least one vocabulary term that reappears in C-10 DOMAIN CONTEXT, and must be referenced in C-08 (recovery prescriptions) or C-09 (trade-off analysis) to close the comparison loop. A baseline not connected to C-08 or C-09 does not pass. |
| C-16 | **Cross-role reference chain** | behavior | Each role or section explicitly cites a named output from at least one prior role or section — not a generic back-reference ("as established above") but a named citation (e.g., "Role 1's DOMAIN CONTEXT", "INCUMBENT BASELINE from Role 2"). A section that introduces vocabulary or analysis without attributing it to a prior named source does not pass. |
| C-17 | **Immutability declaration on pre-committed values** | depth | At least one pre-committed contract value (such as the staleness threshold from C-13) is accompanied by an explicit prohibition on post-trace revision. The prompt text must state that the value cannot be changed after a specified point (e.g., "This threshold is fixed. You may not revise it after Stage 1."). A request for early declaration alone — as required by C-13 — does not satisfy this criterion. |
| C-18 | **Incremental boundary elapsed computation** | behavior | The cumulative elapsed total required by C-14 is computed as a named field within each BOUNDARY CHECK block, incrementally updated at every boundary as a structural side effect of writing that block. A cumulative total that appears only in a post-stage summary section does not pass, even if numerically correct. |
| C-19 | **Machine-verifiable citation target convention** | format | A formal citation convention — a consistent prefix, schema, or required field (e.g., `ARTIFACT:` prefix, `R-xx` IDs, `Citing:` row) — is declared and applied uniformly to every named handoff target in the variation. Every citation made by any subsequent role or section uses this convention, making references verifiable by token match rather than prose judgment. A variation that names handoff targets using inconsistent or ad hoc labels does not pass. C-16 checks whether citations are named; C-19 checks whether citation targets are created via a machine-parseable convention applied without exception. |
| C-20 | **Stage-write progression gate** | behavior | The prompt contains an explicit write-order prohibition: Stage N+1 may not be written until the boundary block between Stage N and Stage N+1 is fully complete with all required fields present. The prohibition must be stated as a sequencing constraint that names the gate condition (e.g., "Do not write Stage N+1 until this block is complete"). C-11 requires interleaved boundary block placement; C-20 requires an explicit deferral prohibition that makes the boundary block a structural prerequisite for stage advancement. A prompt that instructs boundary block placement without a write-order prohibition does not pass. |
| C-21 | **Binary freshness verdict per boundary** | depth | Each BOUNDARY CHECK block contains a required declarative verdict field (FRESH / STALE or equivalent binary) derived by comparing the cumulative elapsed total against the pre-declared threshold from C-13. C-14 requires the elapsed total to exist; C-18 requires it to be computed incrementally; C-21 requires that each boundary produce an explicit pass/fail verdict, converting the elapsed computation into a decision gate. A boundary block that records elapsed time without a verdict field does not pass. A verdict that appears only in the final stale analysis does not satisfy this criterion. |
| C-22 | **Formal pre-declared registry table** | format | A registry table — mapping every artifact to a machine-parseable token (e.g., `[A-xx]`) with at minimum an owner and description column — is produced before any role output begins. The table must enumerate all artifacts that will be cited downstream: a prefix declaration (C-19) or a flat label list satisfies token convention but does not satisfy this criterion. Every citation target named in any subsequent role must appear as a row in this table. A citation to a target not in the table does not pass. The table form makes citation targets first-class, enumerable, and column-verifiable independent of prose context. |
| C-23 | **Phase gate self-verification checklists** | behavior | At least one phase boundary contains a checklist the model must produce and explicitly tick (or mark as passed/failed) before the next phase may begin. Each checklist item must reference a specific named criterion or field required to be present (e.g., "Every boundary check has `Freshness verdict: FRESH or STALE`"). Non-compliance appears in the output as an unticked or failed item — readable by any evaluator without rubric access. An evaluator-facing summary or post-hoc audit does not satisfy this criterion; the checklist must be part of the model's required output at the phase boundary. |
| C-24 | **Upfront constraint section with inline callback syntax** | behavior | A named constraint section (e.g., STRUCTURAL CONSTRAINTS, PROTOCOL RULES) declares all structural requirements once, prominently, before any role output begins. Each constraint is given a short identifier (e.g., SC-1, R-09). Every role instruction that applies a constraint references it by that identifier at the point of application (e.g., "Apply SC-2:", "Per SC-2, Stage N+1 may not be written until…", "[SC-3: compare Elapsed…]"). C-19 requires a declared citation convention; C-20 requires a write-order prohibition; C-24 requires that these and other structural requirements be consolidated in one upfront section and back-referenced by name at every enforcement point, creating two-level enforcement: declaration and inline callback. A variation that declares constraints upfront without inline callbacks, or applies constraints inline without an upfront declaration section, does not pass. |
| C-25 | **Phase gate artifacts as first-class registry rows** | format | Every phase gate verification artifact (e.g., a post-role checklist) appears as a named row in the pre-role registry table required by C-22 — with the same token, owner, and description columns as data artifacts. C-22 requires all citation targets to be pre-registered; C-25 requires specifically that checklist and gate artifacts are enumerated in that table, making the verification structure itself auditable alongside the data lineage structure. A registry that lists only data or lineage artifacts while checklist artifacts are implied, declared inline, or absent from the table does not pass. |
| C-26 | **Non-natural role ordering as citation stress test** | behavior | The role sequence is designed so that at least one downstream role must cite artifacts produced by a role that is not its immediate predecessor in the natural domain order (e.g., Commerce-first forces Finance and Operations to cite Commerce's stale SLA rather than re-declare it). The prompt must require downstream roles to open with an explicit citation line (e.g., `Citing:`) that names specific tokens from non-adjacent roles. A role sequence that follows natural upstream-to-downstream domain order does not satisfy this criterion even if citations are present, because natural ordering permits context to be inferred rather than cited. C-16 requires that citations are named; C-26 requires that the role ordering is designed to make inference structurally insufficient — citation laziness becomes detectable as a missing token, not a prose gap. |
| C-27 | **Named recovery formula anchoring incumbent baseline** | depth | The variation spec names a recovery formula — a reusable prescription structure (e.g., "RECOVERY: [loss point] → [action] → [verification step]") — as an explicit design feature, and the INCUMBENT BASELINE section is the declared source for the baseline state that the recovery formula references. A variation that produces recovery prescriptions meeting C-08 through execution discipline, without naming the recovery formula as a spec-level design feature, does not pass. C-15 requires the baseline to be referenced in recovery; C-27 requires the formula that performs that reference to be named as a spec artifact, making the baseline-recovery coupling independently inspectable. |
| C-28 | **Named-column boundary block schema** | format | The prompt declares a fixed named-column schema for every BOUNDARY CHECK block (e.g., `Entity \| Elapsed \| Freshness Verdict \| Error Handling \| Domain Entity`). Every boundary check block produced by any role must contain all declared columns. An evaluator can verify completeness by column-name token match without reading prose. A boundary block that omits one or more declared column names does not pass, even if the information is present in surrounding prose. C-11 requires interleaved placement; C-28 requires that the block structure is column-verifiable independent of format. |
| C-29 | **Trade-off analysis as prompt-required section with baseline token citation** | behavior | The variation prompt explicitly requires a TRADE-OFF ANALYSIS section from at least one role, names the alternative pattern to be evaluated, and requires that section to cite the INCUMBENT BASELINE token (from C-15/C-27) at least once. A trade-off analysis that appears in output without a prompt-level requirement specifying the section name and citation obligation does not pass. C-09 requires the trade-off content to exist; C-29 requires the prompt to make the section and its baseline citation a structural obligation rather than a content suggestion. |
| C-30 | **Output register declaration with format-specific field-compliance markers** | format | The variation prompt declares an OUTPUT REGISTER for each role's primary artifact — naming the artifact token, the output format (tabular, prose, structured prose), and the required structural fields. For tabular registers, field compliance is verified by column-name presence. For prose registers, field compliance is verified by a named field-presence marker (e.g., a required label or heading) declared in the register specification. A role that produces output without meeting its declared register's field-compliance specification does not pass, regardless of content quality. C-22 requires artifacts to be pre-registered by token; C-30 requires that each registered artifact carry a format-specific compliance verification method. |
| C-31 | **Pre-declared boundary block schema section** | format | The prompt contains a dedicated BOUNDARY BLOCK SCHEMA section (or equivalent named section) that appears before any role instruction and declares the complete field list for every BOUNDARY CHECK block. This section is the single authoritative source for boundary block structure — role instructions reference it by name (e.g., "Per BOUNDARY BLOCK SCHEMA, include all declared fields") rather than redeclaring fields inline. C-28 requires named columns to exist; C-31 requires that the column declaration lives in one named pre-role section cited by reference, eliminating the possibility of per-role schema drift. A variation that redeclares or modifies the boundary block schema within individual role instructions does not pass. |
| C-32 | **Register-specific compliance marker mapping per structural field** | format | For each output register declared under C-30, the prompt specifies a per-field compliance marker — a token, label, or heading that must appear in the output at the location of that field — so that field presence can be verified by marker token match, not prose inference. The mapping must cover every required structural field in the register. A register declaration that lists required fields without specifying per-field markers does not pass. C-30 requires format-specific compliance verification to be declared; C-32 requires the verification to be field-granular and token-matchable across all fields, not a single format-level assertion. |
| C-33 | **Register-invariant declaration for non-tabular output registers** | format | When a variation includes a non-tabular output register (prose, narrative, or structured prose), the prompt contains a REGISTER-INVARIANT DECLARATION that names the fields required regardless of output format and specifies the prose-mode marker for each. This declaration acknowledges that tabular column-name matching (C-28, C-32) does not apply to prose registers and substitutes an explicit prose-mode compliance verification method. A non-tabular register that uses the same compliance specification as a tabular register, or that lacks any format-specific compliance declaration, does not pass. C-30 and C-32 apply to all registers; C-33 applies only when a non-tabular register is present and requires the prose-mode compliance method to be explicitly declared rather than inferred from tabular conventions. |
| C-34 | **Per-boundary incumbent equivalent column with token-cited baseline derivation** | depth | Each BOUNDARY CHECK block contains an INCUMBENT EQUIVALENT field that states what the analogous step in the incumbent or manual process cost in elapsed time, derived by citing the INCUMBENT BASELINE token from C-15/C-27. The derivation must be shown — not asserted — by naming the source token and the specific baseline value used. A boundary block that reports an incumbent equivalent without tracing it to the baseline token does not pass. C-15 requires the baseline to exist and be referenced in C-08/C-09; C-34 requires the baseline to be operationalized at every boundary as a per-block comparison field with a visible token citation. |
| C-35 | **INCUMBENT TOTAL as required pre-trade-off closing artifact** | depth | Before the TRADE-OFF ANALYSIS section, the variation output must contain an INCUMBENT TOTAL artifact that sums all per-boundary INCUMBENT EQUIVALENT values from C-34 into a single additive baseline total, shows the additive breakdown by boundary, and cites the INCUMBENT BASELINE token as the derivation source. The TRADE-OFF ANALYSIS section must cite this INCUMBENT TOTAL — not the original INCUMBENT BASELINE token — as its baseline reference. C-34 requires per-boundary incumbent values to exist; C-35 requires them to be aggregated into a named closing artifact that becomes the authoritative baseline input for trade-off scoring. A trade-off analysis that cites the baseline directly without an INCUMBENT TOTAL aggregation step does not pass. |
| C-36 | **Baseline-first artifact ordering for citation-complete boundary cells** | format | In every BOUNDARY CHECK block, the INCUMBENT EQUIVALENT field (C-34) appears before the FRESHNESS VERDICT field (C-21). This ordering is declared as a structural requirement in the BOUNDARY BLOCK SCHEMA section (C-31) and enforced by the write-order prohibition (C-20): the baseline comparison must be computed before the freshness verdict is issued. A boundary block that computes the freshness verdict before or without the incumbent equivalent does not pass. C-34 and C-21 require both fields to exist; C-36 requires their relative ordering to be declared as a schema rule and enforced as a write prerequisite, making the derivation chain directional and auditable. |
| C-37 | **REGISTER DECLARATION Parts A/B as single-location C-34/C-35 authority** | format | The prompt contains a REGISTER DECLARATION section with two named parts: Part A declares the per-boundary INCUMBENT EQUIVALENT field specification (governing C-34 compliance across all boundary blocks) and Part B declares the INCUMBENT TOTAL artifact specification (governing C-35 compliance before the trade-off section). Both parts appear in a single contiguous section before any role output. No role instruction may redeclare or modify these specifications inline. C-34 and C-35 require the fields to exist; C-37 requires that both specifications live at one authoritative location, making the per-boundary and aggregate incumbent tracking structure independently inspectable without reading role instructions. A variation that declares Part A and Part B in separate sections, or embeds either specification within a role instruction, does not pass. |
| C-38 | **Skip-level citation requirement in non-natural role ordering** | behavior | When the role sequence is non-natural (C-26 applies), the prompt contains a structural constraint (in the C-24 upfront section) that explicitly requires each downstream role to cite at least one artifact from a non-adjacent upstream role by token — not merely from its immediate predecessor. This constraint is back-referenced by identifier at each downstream role's opening instruction (e.g., "Per SC-X, open with a `Citing:` line naming one token from a non-adjacent role"). C-26 requires the non-natural ordering to be designed as a citation stress test; C-38 requires the skip-level citation obligation to be expressed as a named SC with inline callbacks, making citation laziness structurally detectable rather than visible only through output inspection. A variation that uses non-natural ordering (C-26) without a named skip-level citation SC does not pass. |
| C-39 | **Skip-level SC names governed role in its body** | format | The structural constraint governing skip-level citation (required by C-38) must include, within the body of the SC itself, the name or identifier of the specific downstream role it governs (e.g., "This constraint governs Role 3 — Operations"). The governed role may not be inferred from placement or from surrounding role instructions. C-38 requires the skip-level citation SC to exist with inline callbacks; C-39 requires the SC body to be self-describing with respect to scope, so that the constraint can be evaluated in isolation without reading the role sequence. An SC that governs skip-level citation without naming its governed role in the SC body does not pass. |
| C-40 | **Skip-level SC declares position distance in its body** | format | The structural constraint governing skip-level citation (required by C-38) must include, within the body of the SC itself, the position distance of the skip — the number of roles separating the governed role from the non-adjacent upstream source it must cite (e.g., "cite an artifact from a role at least 2 positions upstream"). The distance may not be inferred from role sequence ordering. C-39 requires the SC body to name the governed role; C-40 requires the SC body to also quantify the skip distance, making the structural intent of the non-natural ordering explicit within the constraint itself. An SC that names the governed role but omits the position distance does not pass. |
| C-41 | **Phase Gate 2 skip-level verification item cites governing SC identifier** | behavior | The Phase Gate 2 checklist (or equivalent phase gate at the boundary following the skip-level role) must contain a verification item that explicitly cites the governing SC identifier (e.g., "Per SC-12: Role 3 cited a token from a role ≥2 positions upstream — PASS/FAIL"). The verification item may not reference the skip-level requirement by description alone; the SC identifier must appear as a token in the checklist item text. C-23 requires phase gate checklists to exist with named criteria; C-41 requires specifically that the skip-level citation check cites its governing SC by identifier, closing the verification chain between C-38's SC declaration and C-23's checklist enforcement. A phase gate that verifies skip-level citation without citing the SC identifier does not pass. |
| C-42 | **C-37 + C-41 closed verification chain as co-occurrence requirement** | behavior | C-37 (REGISTER DECLARATION Parts A/B as single-location authority) and C-41 (Phase Gate 2 skip-level verification item citing governing SC identifier) must co-occur in the same variation: C-37's REGISTER DECLARATION header must cite the governing SC identifier established under C-38/C-39/C-40, and C-41's Phase Gate 2 checklist item must cite that same SC identifier. The two citations — one structural declaration, one verification gate — form a closed chain: every failure mode self-locates its governing SC at the point of failure. A variation that satisfies C-37 and C-41 independently, without the cross-citation linking them to the same SC identifier, does not pass. C-37 and C-41 require each structure to exist; C-42 requires their governing SC citations to be identical, making the chain mechanically verifiable by token match. |
| C-43 | **All three skip-level SC attributes co-present in a single SC block** | format | The skip-level structural constraint required by C-38 must contain all three attributes — governed role name (C-39), position distance (C-40), and inline callback cite-back syntax (C-38) — within a single contiguous SC block in the upfront constraint section. No attribute may be distributed to a separate SC, a role instruction, or an annotation outside the SC block. C-39 and C-40 require the attributes to exist; C-43 requires them to co-occur in one block, so that the complete specification of the skip-level obligation is readable and verifiable at a single location without assembling it from distributed sources. A variation where any of the three attributes appears outside the single SC block does not pass, even if all three attributes are present in the variation. |
| C-44 | **Baseline-closure SC in upfront constraint section** | behavior | The STRUCTURAL CONSTRAINTS section required by C-24 must contain a named, numbered constraint (e.g., SC-X: BASELINE CLOSURE) that explicitly states: (a) the INCUMBENT BASELINE token must appear as a citation in the RECOVERY PRESCRIPTIONS section, and (b) the INCUMBENT BASELINE token must appear as a citation in the TRADE-OFF ANALYSIS section. This SC must be back-referenced by identifier at both the RECOVERY PRESCRIPTIONS section header and the TRADE-OFF ANALYSIS section header in the role instructions (e.g., "Per SC-X, cite [INCUMBENT BASELINE]"). C-15 requires the baseline to be connected to C-08/C-09; C-44 requires this connection to be expressed as a named, numbered structural constraint with inline callbacks, making the closure verifiable by token match at both citation sites rather than auditable only through output inspection. A variation that achieves C-15's baseline-recovery/trade-off connection through execution discipline without a named SC and paired callbacks does not pass. |
| C-45 | **Format-mode declaration with criterion substitution map** | format | When the variation includes a non-tabular output register (C-33 applies), the prompt must contain a FORMAT MODE DECLARATION — either as a named section or as a named SC entry in the C-24 upfront constraint section — that: (a) declares the output format for each non-tabular register (e.g., `FORMAT: PROSE`), (b) lists each tabular-structural criterion identifier (e.g., C-28, C-32) that applies in modified form for that register, and (c) names the prose-mode substitute requirement for each listed criterion (e.g., "C-28 substitute: prose field-presence enumeration with per-field PRESENT/ABSENT verdict"). C-33 requires a register-invariant declaration for non-tabular registers; C-45 requires that the variation-level format mode and its per-criterion substitution map are declared upfront as a named artifact, enabling evaluators to verify prose compliance against a declared substitution schema rather than inferring format-mode applicability from output structure. A non-tabular variation that satisfies C-33 without an upfront criterion substitution map does not pass. |
