Reading the scorecard carefully for new patterns before writing.

The only differentiation across the five variations in the scorecard is **C-01**: V-01 through V-04 score PARTIAL (6/12) because their Stage 4 entry templates have no B-# sub-field — belief traceability lives only in the deviation chain (Stage 2 → GATE-CONFIRMED-[N] → entry N). V-05 scores full PASS by adding `**What We Expected (B-[#]):**` as a dedicated first sub-field, demonstrating it in Surprise 0, and enforcing it explicitly in the per-entry EXIT gate. That is the one new structural pattern.

C-02 through C-14: all five variations pass all remaining criteria — no new patterns there.

New criterion: **C-33 — Stage 4 B-# belief anchor sub-field** (aspirational).

---

## Rubric: topic-echo (topic-reflect) — v13

**5 + 3 + 25 criteria across 3 tiers. Score = essential(60) + recommended(30) + aspirational(125) = 215 pts max.**

> v13 changes: Added C-33 (aspirational) from Round 1 excellence signal (V-05). Aspirational tier expands from 120 → 125 pts (24 → 25 criteria). Golden threshold unchanged.
>
> **Why this criterion:** V-01 through V-04 all score PARTIAL (6/12) on C-01 — the base correctness requirement — because their Stage 4 entry templates have no dedicated B-# sub-field. Belief traceability lives only in the deviation chain: Stage 2 records the B-# in the deviation table, GATE-CONFIRMED-[N] routes that entry to Stage 4, and the entry index N is assumed to carry the B-# forward implicitly. The EXIT gate does not verify a B-# reference. V-05 breaks this implicit chain by adding `**What We Expected (B-[#]):**` as the first labeled sub-field in the Stage 4 prose template; Surprise 0 demonstrates `**What We Expected (B-2):**` with a full sentence; and the per-entry EXIT checklist explicitly requires "full sentence referencing a specific B-# from Stage 1 — not a generic 'we thought it would work'." This converts belief traceability from an inferred structural property (the chain was set up correctly, so the link must exist) into a per-entry structural requirement enforced at three convergent points: field presence in the template, calibration demonstration, and EXIT gate verification. The pattern is directly analogous to C-22 (Build Risk promoted from implied Design Impact extension to explicit labeled sub-field): a relationship the rubric assumed was enforced by surrounding structure is instead made explicit and independently gate-enforced at entry granularity. C-01 tests the correctness outcome (every entry references a B-#, at least one contradicts); C-33 tests whether the structural apparatus — dedicated sub-field, calibration anchoring, EXIT enforcement — is in place to guarantee that outcome rather than relying on the model to preserve the chain.

> v12 changes: Added C-31 and C-32 (aspirational) from Round 12 excellence signals. Aspirational tier expands from 110 → 120 pts (22 → 24 criteria). Golden threshold unchanged.
>
> **Why these criteria:** All five Round 12 variations achieve 200/200 under v11 — the base is fully internalized. Two new structural patterns emerge. First: V-01 (Formal/Specification Register) includes a Stage 7 that structurally separates the Stage 6 verdict table from artifact delivery. C-10 requires a HALT if FOREKNOWLEDGE-FLAGGED is unresolved and mentions Stage 7 in its pass condition; C-09 requires COMMIT-STAGE tokens through Stage 6. C-31 tests whether Stage 7 exists as a discrete structural element — with its own ENTER condition, EXIT criterion, and COMMIT-STAGE-7 token — making artifact delivery a binary gate that cannot emit until foreknowledge resolution is structurally confirmed, not advisory. The pattern is the delivery-endpoint complement of C-14's COMMIT-STAGE-3-CLEAN gate: Stage 6 issues verdicts; Stage 7 delivers the artifact. Second: V-03 (Three Named Roles) names specific prohibited generic phrases in the Signal Source field enforcement — "multiple sources," "the signals," "various artifacts" — directly analogous to C-17's named synonym exclusions for dimension vocabulary. C-03 requires a named artifact with no generics; C-32 tests whether specific prohibited evasion phrases are enumerated by name, closing the high-probability Signal Source quality gap the same way C-17 closes the dimension vocabulary gap. Naming the phrases a model reaches for when no specific artifact is available converts the generic prohibition from a positive-only requirement into a named-antipattern exclusion list.

> v11 changes: Added C-29 and C-30 (aspirational) from Round 11 excellence signals. Aspirational tier expands from 100 → 110 pts (20 → 22 criteria). Golden threshold unchanged.
>
> **Why these criteria:** V-01 (Round 11) achieves 170/190 under v10, blocked only by the Stage 6 absence (C-02 fail). Two new structural patterns emerged from V-01's Field Reference axis. First: V-01 consolidates all four Stage 4 sub-field definitions into a dedicated named "Field Reference" block positioned before the entry loop — multiple criterion verdicts (C-01, C-03, C-04, C-11, C-12, C-22) each cite "Field Reference" as their quality source. C-25 tests convergent mechanism coverage; C-29 tests whether the four field definitions are structurally unified into a single pre-loop reference block, making all field quality requirements simultaneously accessible before the model enters the entry generation loop rather than discovering them distributed across loop instructions and gate conditions. Second: V-01 labels its Surprise 0 calibration entry as "Calibration Entry (not a live entry)" — an explicit boundary marker absent from C-20's pass condition. C-20 tests whether the worked example is labeled as entry-zero and embedded in Stage 4; C-30 tests whether that entry also carries an explicit live/example disambiguation that prevents the model from treating the calibration example as the first entry in the live sequence, which would cause it to omit, skip, or misframe the subsequent live entries.

> v10 changes: Added C-27 and C-28 (aspirational) from Round 10 excellence signals. Aspirational tier expands from 90 → 100 pts (18 → 20 criteria). Golden threshold unchanged at 100. V-01 achieves 190/190 retroactively.
>
> **Why these criteria:** V-01 (Round 10) is the first variation to achieve a perfect score under v9 (180/180). Two new structural patterns emerged. First: V-01's Build Risk field definition carries three independent definitional anchors in sequence — a purpose statement, the paired conceptual formula (C-26), and an abstract structural gloss naming the forward-looking vs. risk-surface poles by name. C-26 tests only the paired formula; C-27 tests whether the definition extends to the structural gloss, providing a third anchor that abstracts the distinction into first-principle terms. Second: V-01 renders its COMMIT-ENTRY gate as a scannable ✓ checklist with one bullet per sub-field, each carrying its gate condition and inline corrective action — rather than as prose gate instructions. C-25 tests convergent mechanism coverage; C-28 tests whether the per-entry gate format presents the four-field requirements as simultaneously visible, spatially separated bullets at the point of token emission.

---

### Essential (all must pass — 12 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **Surprise orientation** | correctness | Every Stage 4 entry references a specific B-# from Stage 1; at least one entry contradicts (not confirms) the opening model |
| C-02 | **Symmetric Contract present** | correctness | Stage 1 has opening model + epistemic profile + 3+ beliefs; Stage 6 has verdict table with revision direction and COHERENT/CONTRADICTORY/FOREKNOWLEDGE-FLAGGED |
| C-03 | **Signal tracing** | correctness | Every Stage 4 surprise has a named artifact in Signal Source (name and/or namespace and/or date) — no "multiple sources" generics |
| C-04 | **Design impact specificity** | depth | Every Stage 4 Design Impact field names a specific component, API, flow, or decision — not "the system" |
| C-05 | **Adversarial gate executed** | correctness | Stage 3 five-check table present; every deviation has a VALID/INVALID verdict; GATE-CONFIRMED or GATE-REJECTED tokens present |

---

### Recommended (10 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | **Epistemic dimension compliance** | correctness | Only the 5 canonical names used in all dimension cells; no excluded synonyms |
| C-07 | **Minimum 2 surprises** | coverage | At least 2 GATE-CONFIRMED rows in Stage 4; sweep extended if gate initially yields fewer |
| C-08 | **Cluster map actionability** | behavior | Stage 5 Next Team / Skill column has at least one named skill or role (not just "investigate") |

---

### Aspirational (5 pts each)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | **Token protocol integrity** | format | GATE-CONFIRMED tokens name Stage 4 (not just route to it); COMMIT-ENTRY per row in Stage 4; COMMIT-STAGE-1 through 6 all present |
| C-10 | **Foreknowledge signal evaluated** | depth | Stage 6 explicitly records CLEAR or FLAGGED; if FLAGGED, artifact not written and responsible belief(s) named |
| C-11 | **Stage 4 prose template format** | format | Stage 4 surprise entries are numbered prose blocks with labeled sub-fields (Surprise, Signal Source, Design Impact) — not a markdown table; no narrow-cell format pressure possible |
| C-12 | **Stage 4 entry completeness** | depth | No single-word or two-word entries appear in any Signal Source or Design Impact field across all Stage 4 entries; every field is a full phrase or sentence |
| C-13 | **Closed-list dimension vocabulary** | format | The variation declares the 5 canonical dimension names as a standalone closed-set rule with an explicit substitution prohibition — not embedded as inline examples in Stage 1 prose; wording equivalent to: "The only valid dimension names are: Feasibility · Usability · Scalability · Adoptability · Correctness. Do not substitute." |
| C-14 | **Foreknowledge dual-token gate** | correctness | Stage 3 (or a Stage 3.5 checkpoint) issues either COMMIT-STAGE-3-CLEAN or COMMIT-STAGE-3-FLAGGED before Stage 4 proceeds; the token makes the foreknowledge resolution binary and structurally enforced, not advisory |
| C-15 | **Pre-stage gate sequence map** | format | The variation includes a top-level gate overview (table or structured list) enumerating all stage tokens, halt conditions, and execution flow before Stage 1 instructions begin — not derived from reading individual stages, but surfaced as navigation context at the top of the prompt |
| C-16 | **Worked calibration example** | depth | The variation includes at least one complete filled-in Stage 4 example (not a template) showing a realistic artifact reference in Signal Source (name, namespace, date) and a full-sentence Design Impact naming a specific component or contract |
| C-17 | **Named synonym exclusions** | format | The vocabulary rule names at least 2 specific prohibited synonyms (e.g., "Reliability," "Performance," "Complexity," "Maintainability") in addition to the general substitution prohibition — anticipating the model's highest-probability substitution errors by name |
| C-18 | **Synonym-to-canonical replacement mapping** | format | The vocabulary rule maps each named prohibited synonym to its canonical replacement (e.g., "Reliability → Correctness," "Performance → Scalability") — not only naming what is prohibited but specifying what to use instead; at least 2 such mappings present |
| C-19 | **Per-stage ENTER/EXIT lifecycle contract** | format | At least 3 stages include explicit ENTER conditions (what must be true to begin the stage) and EXIT criteria (what constitutes successful completion) — not just instructions for what to do within the stage, but verifiable pre- and post-conditions framed as a contract |
| C-20 | **Calibration example embedded as Stage 4 entry 0** | depth | The worked calibration example required by C-16 is labeled "Surprise 0" (or equivalent entry-zero notation) and formatted identically to the Stage 4 entry template — positioned within or immediately preceding the live Stage 4 entry block, not in a separate example or callout section; the model enters Stage 4 having already observed a complete realistic entry in the exact format it will produce |
| C-21 | **Vocabulary self-repair instruction at EXIT gate** | format | At least one stage EXIT criterion explicitly instructs the model to detect and correct any malformed dimension names by consulting the canonical synonym-to-canonical mapping table before emitting — prescribing the corrective action ("correct … using the mapping table"), not only stating the compliance requirement; converts the mapping table from a reference resource into an active runtime repair protocol |
| C-22 | **Stage 4 Build Risk sub-field** | depth | Each Stage 4 entry includes a labeled Build Risk sub-field (or equivalent) naming a specific component, decision, or risk surface implicated by the surprise — not a general risk statement; the field is a prose sub-field (not a table column) that extends Design Impact (what changes) with an explicit buildability consequence (what could break or require rework); the field is modeled in the calibration example |
| C-23 | **Surprise 0 dual-field specificity anchoring** | depth | Surprise 0 fills in BOTH Design Impact AND Build Risk with full-sentence, component-specific values that are conceptually distinct and non-redundant — Design Impact names what must change (a component, API, flow, or decision to be updated); Build Risk names a different component, contract, or assumption that could break or require rework; the two values are not paraphrases of each other; the worked instance makes the forward-looking (what changes) vs. risk-surface (what is implicated) distinction legible through concrete paired content that a live entry imitates simultaneously |
| C-24 | **COMMIT-ENTRY Build Risk specificity gate** | format | The Stage 4 per-entry EXIT criterion or COMMIT-ENTRY gate includes an explicit Build Risk specificity check — naming the failure condition (Build Risk is a general risk category or abstract statement → do not emit COMMIT-ENTRY) and the corrective action (rewrite Build Risk naming a specific component, decision, or risk surface before emitting); this converts the Build Risk sub-field from a structural slot requirement (presence enforced) into a runtime-enforced specificity constraint where an insufficiently specific value blocks entry token emission |
| C-25 | **Stage 4 four-field convergent mechanism coverage** | depth | All four Stage 4 sub-fields (Surprise, Signal Source, Design Impact, Build Risk) are each governed by at least two independent enforcement mechanisms drawn from: (a) Surprise 0 demonstrates a complete, specific value for the sub-field (imitation floor), (b) a per-entry EXIT criterion names the sub-field and its quality requirement (gate enforcement), or (c) a self-repair or correction instruction prescribes active remediation for the sub-field (repair protocol); no sub-field is covered by fewer than two mechanisms; the variation's Stage 4 design achieves convergent coverage across all four output fields simultaneously |
| C-26 | **Build Risk field definition paired conceptual formula** | depth | The Build Risk sub-field definition — within Stage 4 entry instructions, field documentation, or field label prose — includes an explicit paired formula that names both Design Impact and Build Risk in structural relation, stating the forward-looking change / risk-surface implication distinction as a property of the two fields (e.g., "Design Impact names what must change; Build Risk names what is implicated by that change — a different component, dependency, or contract that could fail"); the formula names both fields and their relationship in a single definitional statement; the distinction is encoded in the field definition itself and is legible before any calibration example is encountered |
| C-27 | **Build Risk field definition triple-anchor sequence** | depth | The Build Risk sub-field definition includes three independent definitional anchors in sequence: (a) a purpose statement establishing what the field captures (e.g., "names what is implicated by the change"), (b) the paired conceptual formula required by C-26 naming both fields in structural relation, and (c) an abstract structural gloss that names the conceptual poles by label (e.g., "one is forward-looking (what to update), the other is risk-surface (what could break or require rework)") — the gloss abstracts the distinction into first-principle terms independent of the specific formula phrasing; all three anchors appear in the field definition before any calibration example is encountered |
| C-28 | **COMMIT-ENTRY four-field visual checklist** | format | The Stage 4 per-entry EXIT criterion or COMMIT-ENTRY gate renders all four sub-field requirements as a scannable ✓ checklist — each sub-field on its own dedicated bullet or line, with its gate condition (what constitutes a pass) and its inline corrective action (what to do if the check fails) visible at that bullet — rather than as prose gate instructions; a model scanning the list sees all four field requirements and their corrective actions simultaneously at the point of token emission, without parsing them out of continuous prose |
| C-29 | **Stage 4 dedicated Field Reference block** | format | Stage 4 includes a dedicated, named Field Reference block (or equivalent labeled section) that consolidates all four sub-field definitions — Surprise, Signal Source, Design Impact, Build Risk — in a single scannable location before the entry loop instructions begin; the block is structurally separate from the COMMIT-ENTRY gate checklist and separate from inline per-loop instructions; the model encounters all four field quality requirements as a unified reference before entering the entry generation loop, rather than discovering them distributed across loop instructions, gate conditions, or the calibration example |
| C-30 | **Calibration entry live/example boundary marker** | format | The Surprise 0 calibration entry (required by C-20) carries an explicit label or inline marker that distinguishes it from live Stage 4 entries — using language such as "Calibration Entry (not a live entry)," "example only — do not continue this entry," or equivalent — preventing the model from treating the worked example as the first entry in the live sequence, from numbering live entries starting at 2, or from omitting a live entry because the calibration example is treated as having already satisfied the Stage 4 output requirement; the live/example boundary is made explicit at the entry itself, not only in surrounding prose |
| C-31 | **Stage 7 post-verdict artifact emission gate** | format | The variation includes a Stage 7 that structurally separates artifact delivery from the Stage 6 verdict table — Stage 6 issues COHERENT/CONTRADICTORY/FOREKNOWLEDGE-FLAGGED verdicts per belief; Stage 7 is a distinct gate that confirms all foreknowledge flags are resolved before the artifact is emitted; COMMIT-STAGE-7 token is present; Stage 7 includes an explicit ENTER condition (e.g., all beliefs carry a verdict and no FOREKNOWLEDGE-FLAGGED entries remain unresolved) and an EXIT criterion (artifact cleared for delivery); the verdict determination and artifact delivery are structurally distinct stages with separate gate tokens, so a model that reaches Stage 6 verdicts cannot drift into artifact emission before the foreknowledge gate is separately confirmed |
| C-32 | **Named Signal Source prohibited phrase enumeration** | format | The Signal Source field definition, Field Reference block, or COMMIT-ENTRY gate enforcement instructions name at least 2 specific prohibited generic phrases (e.g., "multiple sources," "the signals," "various artifacts," "recent work") in addition to the general requirement for a named artifact — anticipating the model's highest-probability Signal Source evasion phrases by name, in direct parallel to C-17's named synonym exclusions for dimension vocabulary; the named anti-patterns appear as explicit gate-failing examples rather than only being implied by the positive requirement to name a specific artifact; naming the high-probability evasion phrases converts the Signal Source prohibition from a positive-only requirement into a named-antipattern exclusion list that closes off the specific generic forms a model reaches for when no specific artifact is at hand |
| C-33 | **Stage 4 B-# belief anchor sub-field** | depth | The Stage 4 entry template includes a dedicated first sub-field — `What We Expected (B-[#])` or equivalent — that names the specific belief from Stage 1 being contradicted or updated; Surprise 0 demonstrates a concrete B-# reference (e.g., `**What We Expected (B-2):**`) with a full sentence; the per-entry EXIT criterion or COMMIT-ENTRY gate includes an explicit B-# reference check requiring a full sentence naming a specific B-# — not a generic formulation such as "we thought it would work"; this converts belief traceability from an implicit structural chain (Stage 2 deviation table → GATE-CONFIRMED-[N] → entry N) into a per-entry structurally enforced sub-field requirement, closing off the failure mode where a model that reconstructs the chain incorrectly — or omits the Stage 2 link — produces Stage 4 entries with no recoverable connection to the Stage 1 opening model |
