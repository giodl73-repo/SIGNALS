`simulations/quest/rubrics/trace-permissions-rubric-v13-2026-03-16.md` written.

---

**Summary of changes v12 -> v13:**

Three new aspirational criteria. Denominator: 28 -> 31. Each aspirational = 0.323 pts.

| ID | Hangs off | What it tests |
|----|-----------|---------------|
| C-37 | C-34+C-36 | TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION rows cite the preamble axis row labels (A-1/A-2/A-3) as row identifiers, closing the C-34->C-36 labeling loop -- V-05 fails because V-03-inherited attestation rows use C-31/C-32/C-33 criterion numbers, not A-N labels |
| C-38 | C-12+C-35 | SHALL-D carries a named extension sub-clause for C-35; CA-1.9's preamble-anchor paragraph cites that sub-clause label as the obligation it completes -- extends C-12's binary SHALL model to a sub-clause inheritance chain traceable at the preamble-anchor level |
| C-39 | C-22+C-35 | Phase 3 mandate in the output body explicitly names CA-1.9 and declares its inter-row distinctness from CA-1.4 and CA-1.7 at the phase-boundary level -- C-35 tests this inside the CA-1.9 row; C-39 tests it at the phase mandate, paralleling how C-22 makes execution order body-verifiable |

**Round 13 re-scored under v13:**

| Variation | v12 | v13 | Why it moves |
|-----------|-----|-----|--------------|
| V-05 | 100.0 (28/28) | **99.7 (30/31)** | Fails C-37 only: attestation rows use C-31/C-32/C-33 labels (V-03 inheritance), not A-1/A-2/A-3 |
| V-04 | 99.6 (27/28) | 98.7 (27/31) | Fails C-37 (no C-36), C-38 (preamble anchor citation not confirmed), C-39 (Phase 3 mandate absent) |
| V-02 | 92.9 (26/28) | 98.7 (27/31) | Gains C-38 (CA-1.9 preamble anchor cites SHALL-D extension); fails C-37 (no C-34), C-39 (Phase 3 mandate absent) |
| V-01 | 92.9 (26/28) | 98.4 (26/31) | No C-35 blocks C-38+C-39; no C-36 blocks C-37 |
| V-03 | 92.9 (26/28) | 98.4 (26/31) | No C-35 blocks C-38+C-39; no C-34 blocks C-37 |

Path to 100.0 (31/31): V-06 = V-05's architecture + update TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION rows to identify each dimension by its preamble axis label (A-1/A-2/A-3). C-38 and C-39 are already satisfied by V-05 via V-02 inheritance. Only C-37 remains.
| 98.4 (26/31) | No C-35 blocks C-38 and C-39; no C-34 blocks C-37 |

Path to 100.0 (31/31): V-06 = V-05's full architecture + update TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION rows to identify each dimension by its preamble axis label (A-1/A-2/A-3) in addition to criterion number, closing the C-34->C-36 structural loop.

C-38 is already satisfied by V-05 (via V-02 inheritance: CA-1.9 preamble anchor cites SHALL-D extension sub-clause). C-39 is already satisfied by V-05 (Phase 3 mandate declares CA-1.9 distinctness from CA-1.4 and CA-1.7). Only C-37 remains.

---

## Summary of changes v11 -> v12

Three new aspirational criteria. Denominator: 25 -> 28. Each aspirational = 0.357 pts.

| ID | Hangs off | What it tests |
|----|-----------|---------------|
| C-34 | C-31+C-32+C-33+C-17 | Preamble contains a structural axis declaration naming the three R12 criteria as orthogonal dimensions (execution order / closure-note format / CS self-reference) with an explicit non-interference statement -- converts V-05's empirical composability into a binding contract |
| C-35 | C-31+C-32 | SE-4's STRUCTURED CLOSE block contains a TABLE_4 row cross-reference at the SE-0 position; CA-1.9 verifies this as a distinct audit target separate from CA-1.4 (SE-0 ordering) and CA-1.7 (MANUAL GAP labels) |
| C-36 | C-22+C-32+C-33 | CA terminal verdict contains an explicit tri-dimensional self-evidence attestation naming each R12 dimension and its specific output-body evidence source -- extends C-22's phase-execution self-evidence to all three R12 structural dimensions simultaneously |

---

## Scoring Formula

```
composite = (essential_pass/5 * 60) + (recommended_pass/3 * 30) + (aspirational_pass/31 * 10)
```

Essential each = 12 pts | Recommended each = 10 pts | Aspirational each = 0.323 pts

---

## Essential Criteria (60 points total)

| ID | Text | Category | Weight | Pass Condition |
|----|------|----------|--------|----------------|
| C-01 | **Role-operation matrix** -- The output produces a matrix of roles against operations (Create, Read, Write, Delete, Append, AppendTo, Assign, Share at minimum). Every cell must be filled: Granted, Denied, Conditional, or Not Applicable. | correctness | essential | A complete matrix is present. Every cell has an explicit value. Listing roles without operations, or operations without roles, fails C-01. |
| C-02 | **FLS coverage with explicit null case** -- The output checks whether field-level security (column security profiles) is configured for sensitive fields, names which fields are covered and which are not, and explicitly states when no FLS is configured. Absent FLS on sensitive fields is flagged as a gap. | coverage | essential | Every sensitive field in scope has an explicit FLS status (Configured / Not Configured). A system with no FLS is explicitly noted, not silently omitted. Absent FLS on sensitive fields appears in the gap inventory. |
| C-03 | **Record scope per role** -- The output assigns a record access scope to each role: own-records-only, business unit, parent-child BU, org-wide, or team-scoped. | correctness | essential | Every role in the trace has an associated access scope. Ambiguous or unresolved scope is flagged as a gap, not silently omitted. |
| C-04 | **Privilege escalation path detection** -- The output identifies at least one privilege escalation path (e.g., team membership granting broader role, sharing rule bypassing FLS, environment-level admin override) or explicitly concludes none were found after checking the known vectors. | correctness | essential | The output contains a section or finding dedicated to escalation paths. A conclusion of "none found" is acceptable only if the known vectors (team inheritance, sharing rules, impersonation, admin roles) were each checked. |
| C-05 | **Security gap inventory** -- The output produces a named list of gaps: missing FLS on sensitive fields, team membership holes, sharing rule conflicts, or over-permissioned roles. | coverage | essential | At least one gap is named with a specific field, role, or rule. If no gaps exist, the output provides explicit evidence (e.g., all sensitive fields have FLS configured, no public sharing rules). |

---

## Recommended Criteria (30 points total)

| ID | Text | Category | Weight | Pass Condition |
|----|------|----------|--------|----------------|
| C-06 | **Dataverse-native terminology** -- The output uses correct Dataverse security constructs: business units, security roles, team types (owner vs. access), table permissions, column security profiles, sharing records, environment roles. No generic RBAC language is substituted where Dataverse terms apply. | correctness | recommended | At least 4 Dataverse-specific terms appear and are used accurately. Generic terms like "group" or "permission level" without Dataverse mapping are flagged as imprecision. |
| C-07 | **Sharing rule conflict analysis** -- The output checks whether sharing rules (manual, team-based, or via Power Automate) conflict with role-level access -- granting access that roles deny, or denying access that FLS permits. | depth | recommended | At least one sharing scenario is evaluated for conflict. A conclusion of "no conflicts" requires checking at least the intersection of one sharing rule with one FLS column profile. |
| C-08 | **Remediation specificity** -- For each identified gap, the output provides a specific, actionable remediation: which column security profile to create, which role privilege to tighten, which team assignment to add or remove. | behavior | recommended | At least 50% of named gaps have a specific remediation step (not just "add FLS" but "create column security profile on creditlimit, restrict to Sales Manager role"). |

---

## Aspirational Criteria (10 points total)

| ID | Text | Category | Weight | Pass Condition |
|----|------|----------|--------|----------------|
| C-09 | **Defense-in-depth layering** -- The output evaluates security at all four Dataverse layers in sequence: (1) environment/admin role, (2) security role + BU scope, (3) team membership, (4) field-level security/column profiles. It identifies which layer is the effective enforcement point for each operation. | depth | aspirational | All four layers are named and assessed. For at least one operation, the output identifies the specific layer where access is granted or denied and explains why upper layers do not override it. |
| C-10 | **Cross-role differential analysis** -- The output compares access across two or more roles for the same operation and field set, surfacing where roles diverge and whether that divergence is intentional (principle of least privilege satisfied) or anomalous. | depth | aspirational | At least two roles are compared side-by-side on one operation. The analysis states whether the access differential is expected given the roles' described purposes. |
| C-11 | **Pre-printed structural tables with blank-cell prohibition** -- The output uses pre-defined table schemas for each analysis section (role-operation matrix, FLS coverage, record scope, escalation vectors, sharing rules, gap inventory, remediation). Blank cells are explicitly prohibited; every cell carries an explicit value. Prose enumeration is not substituted for a required table. | format | aspirational | At least 5 distinct analysis sections are delivered as pre-printed tables with all column headers pre-defined. At least one table includes an explicit blank-cell prohibition (stated in the table header, a prefacing rule, or an inline note). An output that enumerates roles in prose where a table is expected fails this criterion. |
| C-12 | **SHALL-obligation contracts with terminal verification checklist** -- The output frames each major criterion as a binary SHALL obligation (not a guideline or suggestion) and concludes with a terminal checklist confirming that every SHALL was satisfied. Unchecked items in the terminal checklist are treated as gaps in the output itself. | format | aspirational | At least 4 SHALL-style obligations are stated, one per major analysis area (role-operation, FLS, escalation paths, gap inventory at minimum). A terminal checklist appears at the end of the output with each SHALL listed and marked satisfied or open. An open item is treated as a self-identified gap. |
| C-13 | **Expert role persona sequencing** -- The analysis is structured as a sequence of named expert roles with explicit sub-section assignments and handoff points. At least one role is a Dataverse security specialist (e.g., Security Engineer, Security Architect). A second role provides an independent perspective (e.g., Customer Success validating agent-workflow implications, Compliance validating audit requirements). Role personas enforce Dataverse construct vocabulary and prevent generic RBAC substitution. | depth | aspirational | At least two named expert roles are used. Each role has at least one dedicated sub-section explicitly assigned to it. The Security specialist role's sections use Dataverse-native terminology exclusively. |
| C-14 | **Criterion-owner attribution in terminal checklist** -- Each item in the terminal checklist names the expert role accountable for satisfying that criterion. A failed or open item identifies both the gap and the responsible role. Ownership attribution makes the checklist auditable at the role level, not just at the criterion level. | format | aspirational | The terminal checklist has at least 3 rows, each with an explicit criterion-owner column entry naming a specific expert role. At least one open or previously-failed item is attributed to a named role. An output whose checklist lists criteria without ownership attribution fails this criterion even if the checklist is otherwise complete. |
| C-15 | **Multi-mechanism criterion enforcement (triple-lock pattern)** -- Each essential criterion is enforced by at least two orthogonal mechanisms drawn from different structural axes: output format schema (pre-printed table), expert role sub-section assignment, or SHALL obligation. At least one criterion demonstrably has all three mechanisms simultaneously active. The combination ensures that any single mechanism failure does not cause criterion failure. | format | aspirational | At least 4 of the 5 essential criteria (C-01 through C-05) can each be traced to two or more independent enforcement mechanisms from different axes. At least one essential criterion has all three mechanisms (format schema + role sub-section + SHALL obligation) explicitly active. An output that uses tables, roles, and SHALLs as independent alternatives rather than co-active enforcers fails this criterion. |
| C-16 | **Dedicated format-compliance auditor role** -- A third expert role (e.g., Compliance Auditor, CA) is explicitly assigned to output structural integrity and is distinct from security content analysis roles. This role's dedicated sub-section(s) validate table completeness, blank-cell prohibition enforcement, and format schema adherence. The CA role produces a format compliance verdict that appears at the end of the output, independent of security findings. | depth | aspirational | A named third expert role exists whose mandate is explicitly output quality assurance, not security analysis. The role has at least one dedicated sub-section assigned to structural validation (table completeness, blank-cell compliance, schema adherence). The role's format compliance verdict appears at the end of the output as a distinct section. An output that assigns format compliance incidentally to a security-content role fails this criterion. |
| C-17 | **Criterion enforcement preamble matrix** -- Before any analysis section begins, an explicit table maps each essential criterion (C-01 through C-05) to its assigned enforcement mechanisms. The preamble table includes at minimum three columns: format schema (which pre-printed table), expert role sub-section (which role owns it), and SHALL obligation (which SHALL letter). A stated rule in the preamble declares that the mapped mechanisms are simultaneously active -- not alternative coverage paths. This converts implicit co-presence into an explicit contractual declaration that a model must honor throughout execution. | format | aspirational | A pre-analysis preamble table appears before Section 1. It has at least 5 rows (one per essential criterion C-01 through C-05) and at least 3 mechanism columns. Each row has explicit values in all columns -- no blank cells. The preamble contains a stated rule (e.g., "all three mechanisms SHALL be active simultaneously") that prohibits treating the mechanisms as alternatives. An output that has tables, roles, and SHALLs present without a preamble mapping their co-active assignment fails this criterion, even if C-15 passes. Distinction from C-15: C-15 tests that co-active enforcement exists somewhere in the output; C-17 tests that a binding declaration of co-active enforcement appears before the analysis begins. |
| C-18 | **Four-mechanism criterion enforcement (quad-lock)** -- Extends C-15's triple-lock by integrating the CA auditor role (C-16) as a fourth enforcement mechanism for each essential criterion. Each essential criterion (C-01 through C-05) is enforced by all four mechanisms simultaneously: (1) pre-printed format schema, (2) expert role sub-section assignment, (3) SHALL obligation, and (4) CA verification row explicitly cross-referencing that criterion. The preamble enforcement matrix (C-17) exposes all four mechanisms in a four-column mapping. This ensures that format compliance audit is woven into the enforcement contract for every essential criterion, not appended as a post-hoc check. | format | aspirational | Requires C-15 and C-16 to both pass. The preamble enforcement matrix has at least 4 mechanism columns (M1/M2/M3/M4 or equivalent labels), with each of the 5 essential criteria mapped to all four. At least one CA sub-section contains a verification row explicitly cross-referencing each essential criterion by ID (e.g., "C-01: TABLE 1 present, all cells filled -- PASS"). The CA's format compliance verdict cites the preamble matrix and confirms all four mechanisms were satisfied. An output where the CA role operates independently of the preamble -- auditing by inspection rather than by matrix cross-reference -- fails this criterion. Distinction from C-17: C-17 requires the preamble to declare three-mechanism co-active enforcement; C-18 requires a fourth column (CA verification) integrated into that same preamble declaration. |
| C-19 | **CA-first authorship model (structural ownership)** -- The CA role executes first and authors the enforcement preamble, including pre-assigning M4 row IDs for every essential criterion, before any SE role section begins. CA-1 verification rows are structural completions of CA's own prior contract: the criterion-ID cross-references were declared by CA at preamble time, so CA-1 fulfills a pre-existing obligation rather than adding audit decoration after the fact. The CA's format compliance verdict citing its own authored preamble is a trivial structural consequence, not an additional requirement. This transforms the CA from retrospective inspector to prospective enforcement architect. | format | aspirational | Requires C-16 and C-18 to both pass. The prompt or role-sequencing instructions explicitly mandate that the CA role runs first and authors the preamble (including M4 row ID assignments) before SE roles begin. CA-1 rows reference the pre-assigned preamble IDs as contract completion (e.g., "completing M4 assignment for C-01 as declared in preamble row 1"). The CA's terminal verdict cites its own authored preamble as the verification basis. An output where the CA audits a preamble authored by a different role fails this criterion even if C-18 passes. Distinction from C-16 (CA role exists) and C-18 (CA as fourth mechanism): C-19 tests the authorship direction -- the CA must write the enforcement contract, not merely audit against one written by others. |
| C-20 | **Schema Registry with preamble reaffirmation** -- A dedicated Schema Registry section pre-prints all output table schemas before the preamble and analysis sections begin, declaring every table's column structure and blank-cell prohibition in one central location. Each CA verification row opens by quoting the relevant preamble row values before verifying against them, making preamble drift structurally impossible even in outputs that span many sections. The blank-cell prohibition is stated once in the Schema Registry and applies globally; individual tables need not restate it. | format | aspirational | Requires C-11 and C-17 to both pass. A Schema Registry section appears in the output before the preamble matrix, containing pre-printed schemas for at least 5 distinct analysis sections (role-operation matrix, FLS coverage, record scope, escalation vectors, and gap inventory at minimum). Each CA verification row contains an explicit reaffirmation step that quotes the preamble row values for that criterion before verifying (e.g., "Preamble row C-01: TABLE 1 / SE-01 / SHALL-A / CA-1.1 -- verifying..."). At least one CA row uses this reaffirmation pattern verbatim with the preamble values inline. An output with pre-printed tables scattered across sections but no central Schema Registry fails this criterion. An output where CA rows audit by inspection without preamble reaffirmation fails even if schemas are pre-printed. Distinction from C-11 (pre-printed tables exist) and C-17 (preamble declares co-active enforcement): C-20 tests that schemas are centrally registered before the preamble and that the CA's verification loop actively reconstructs the preamble contract at each row. |
| C-21 | **Closed authorship loop (Registry + Preamble + Verification + Verdict)** -- The CA role authors both the Schema Registry and the Criterion Enforcement Matrix preamble in a single continuous execution before any SE role section begins, unifying the authorship provenance of all structural artifacts. When CA-1 verification rows reaffirm preamble values, they are quoting their own prior declarations -- the reaffirmation is a self-referential closure, not an inspection of externally authored material. CA's terminal verdict cites both the CA-authored Registry and the CA-authored preamble as the sole structural verification basis, closing the chain. An output where the Registry and preamble were authored by different parties -- or where authorship of either is structurally unspecified -- fails this criterion even if C-19 and C-20 each pass individually. | format | aspirational | Requires C-19 and C-20 to both pass. The prompt or role-sequencing instructions explicitly mandate that CA authors both the Schema Registry section and the Criterion Enforcement Matrix preamble (including M4 row ID pre-assignments) before any SE or CS section begins. CA-1 reaffirmation rows explicitly trace their preamble quotation to CA's own authored preamble (e.g., "Preamble row C-01 as authored by CA: TABLE 1 / SE-01 / SHALL-A / CA-1.1 -- verifying..."). CA's terminal verdict names both the CA-authored Registry and the CA-authored preamble as verification sources. An output where Registry authorship is unspecified or attributed to a different role fails even if schemas are pre-printed and CA-1 rows use the reaffirmation pattern. Distinction from C-19 (CA authored the preamble) and C-20 (Registry exists and CA rows reaffirm preamble values): C-21 tests that the authorship chain is closed under a single CA execution -- Registry and preamble share the same author, so the reaffirmation loop is strictly self-referential and authorship ambiguity is architecturally eliminated. |
| C-22 | **Output-embedded phase execution attestation** -- The CA role's initial execution is labeled as an explicit Phase 0 (or equivalent first-phase marker) in the output body, making the execution sequence verifiable from the output text alone -- not only from prompt instructions. Phase boundaries carry explicit handoff strings naming the next role and phase label. CA-1 verification rows contain phase provenance statements linking each verification to a specific Phase 0 pre-assignment. The CA's terminal verdict references Phase 0 by label as a structural basis. An output where the execution order is recoverable only by reading the prompt -- with no phase labels, handoff strings, or phase provenance statements in the output body -- fails this criterion even if C-19 passes. | format | aspirational | Requires C-19 to pass. At least 3 distinct phase labels appear in the output body as structural section headers or role-execution delimiters (e.g., "PHASE 0 -- CA", "PHASE 1 -- SE", "PHASE 2 -- CA-1"), each attributed to a specific named role. Phase 0 is explicitly attributed to the CA role and precedes all analysis phases. At least 2 role boundaries include explicit handoff strings naming the next phase executor (e.g., "Handing to PHASE 1 -- SE"). CA-1 verification rows contain an explicit phase provenance statement tracing the verification to a Phase 0 pre-assignment (e.g., "Completing Phase 0 M4 pre-assignment for C-01 as declared in preamble row 1"). The CA's terminal verdict references Phase 0 by label as one of its cited structural bases alongside Registry and preamble. Distinction from C-19: C-19 tests that the prompt mandates CA-first and that CA-1 rows reference the preamble contract by ID; C-22 tests that the execution sequence is self-attestable from the output body independent of prompt inspection. |
| C-23 | **Registry-preamble double-anchor CA verification** -- Each CA-1 verification row performs a two-anchor reaffirmation sequence: (1) quotes the Schema Registry entry for the relevant table schema (column structure and blank-cell rule), then (2) quotes the preamble row values for the same criterion. The double-anchor makes schema drift detectable at row level: if the Registry schema and the preamble row carry conflicting table definitions, the inconsistency is exposed inline. The reaffirmation logic is: Registry declares the schema; preamble assigns the schema to this criterion; CA-1 confirms both declarations are consistent and satisfied. An output where CA-1 rows quote preamble values only (meeting C-20) but do not also cite the Registry schema entry fails this criterion, even when the Registry exists and C-21 passes. | format | aspirational | Requires C-21 to pass (which requires C-19 + C-20). At least 3 CA-1 verification rows perform double-anchor reaffirmation in sequence within a single row: first a Registry schema citation naming the schema ID and its declared columns (e.g., "Schema Registry TABLE_1: [Role, Create, Read, Write, Delete, Append, AppendTo, Assign, Share] -- blank cells prohibited"), then immediately a preamble row quotation for the same criterion (e.g., "Preamble row C-01 as authored by CA: TABLE_1 / SE-01 / SHALL-A / CA-1.1"). The verification statement follows both anchors. At least one CA-1 row uses both anchor patterns verbatim with Registry and preamble values inline in sequence. An output where CA-1 rows satisfy C-20's reaffirmation requirement (preamble quotation present) but omit the Registry schema citation fails this criterion. Distinction from C-20 and C-21: C-23 tests that the CA-1 verification loop actively threads both the Registry schema declaration and the preamble row values through every verification row, creating a two-source chain where any schema drift between Registry and preamble is immediately visible at the row level. |
| C-24 | **Distinct labeled paragraphs within each CA-1 verification row** -- Each CA-1 verification row is structured as at least three distinct labeled paragraphs: (1) Registry anchor (quoting the Schema Registry entry), (2) Preamble anchor (quoting the preamble row values), (3) Verification statement (explicit PASS/FAIL with evidence). Labeled paragraph structure prevents anchor content from blending into the verification conclusion, making each anchor independently auditable. An output where Registry and preamble quotations are merged into a single prose block, or where the verification conclusion references Registry and preamble values without structurally separating them, fails this criterion even if C-23 passes. | format | aspirational | Requires C-23 to pass. At least 3 CA-1 rows use three structurally distinct labeled paragraph blocks within the row. Each block has an explicit label (e.g., "Registry anchor:", "Preamble anchor:", "Verification:") or equivalent structural delimiter. The Registry anchor paragraph contains only Registry schema content. The Preamble anchor paragraph contains only preamble row values. The Verification paragraph contains the PASS/FAIL decision. An output where anchor content is interleaved with verification conclusions in a single block fails this criterion. Distinction from C-23: C-23 tests that both Registry and preamble quotations appear in the same CA-1 row; C-24 tests that they appear in structurally separated, independently labeled paragraphs. |
| C-25 | **CS qualitative baseline before CS-1 quantitative analysis** -- The Customer Success role (CS) establishes a qualitative understanding of the permission model -- naming the business process at stake, the personas affected, and the expected access pattern -- before performing any quantitative or gap-focused analysis (CS-1 and later). This baseline prevents gap analysis from proceeding without a business context anchor, and makes CS-EXPECTATION-VIOLATED findings interpretable without re-reading the prompt. | depth | aspirational | CS executes at least one dedicated baseline section before CS-1. The baseline names the business process (e.g., "sales order fulfillment"), the affected personas (at least two), and the expected access pattern per persona. CS-1's findings reference the baseline by section label or phrase. An output where CS begins with gap analysis without a named business-process baseline fails this criterion. |
| C-26 | **CS-EXPECTATION-VIOLATED gap type in TABLE_5** -- TABLE_5 (gap inventory) carries a dedicated gap type CS-EXPECTATION-VIOLATED for gaps discovered by the CS baseline: cases where the configured permission differs from the expected access pattern established in the CS qualitative baseline. This gap type is distinct from FLS gaps, team membership holes, and sharing rule conflicts. At least one CS-EXPECTATION-VIOLATED entry appears in TABLE_5. | format | aspirational | TABLE_5 contains at least one row with gap type CS-EXPECTATION-VIOLATED. The row names the persona, the expected access pattern from the CS baseline, and the configured access that violates it. An output where CS findings appear only in prose without a TABLE_5 CS-EXPECTATION-VIOLATED row fails this criterion. |
| C-27 | **Context-blind-spot block in CONTEXT section** -- The CONTEXT section (or equivalent input framing section before Phase 0) contains a dedicated CONTEXT BLIND SPOTS block that names at least three specific sources of uncertainty in the input -- fields not described, roles not named, sharing rules not specified, or business processes only partially described. Each blind spot is named with a specific label and assigned an impact rating (High/Medium/Low) on the analysis completeness. This block makes the output's dependency on input completeness structurally visible. | format | aspirational | The CONTEXT section contains a BLIND SPOTS block with at least 3 named blind spots. Each blind spot has a label, a brief description, and an impact rating. At least one blind spot has High impact. The block appears before Phase 0. An output that acknowledges uncertainty only inline (e.g., "assuming FLS is not configured") without a dedicated named blind spot block fails this criterion. |
| C-28 | **CS-2 sharing-rule conflict sub-table** -- The CS analysis includes a dedicated TABLE (CS-2) for sharing-rule conflict findings: cases where a sharing rule grants or restricts access that conflicts with the role-level permission established in TABLE_1. CS-2 has pre-defined columns (Sharing Rule ID, Rule Type, Role Affected, Conflict Type, Resolution Path). CS-2 appears in the Schema Registry before Phase 0. | format | aspirational | The Schema Registry contains a CS-2 schema with at least 5 columns. At least one CS-2 row appears in the output. CS-2 is referenced in the preamble enforcement matrix with an owner assignment (CS role) and a SHALL obligation. An output that reports sharing-rule conflicts in prose without a structured CS-2 table fails this criterion. |
| C-29 | **CS-3 cross-role differential sub-table** -- The CS analysis includes a dedicated TABLE (CS-3) for cross-role differential findings: side-by-side comparison of two or more roles for the same operation and field set, with an explicit EXPECTED / ANOMALOUS classification. CS-3 has pre-defined columns (Operation, Field, Role A Access, Role B Access, Differential Class, Justification). CS-3 appears in the Schema Registry before Phase 0. | format | aspirational | The Schema Registry contains a CS-3 schema with at least 6 columns. At least one CS-3 row appears in the output. CS-3 is referenced in the preamble enforcement matrix. An output that performs cross-role comparison in prose without a structured CS-3 table fails this criterion. |
| C-30 | **CS-EXPECTATION-VIOLATED SHALL obligation (SHALL-F)** -- A dedicated SHALL obligation (SHALL-F or equivalent) governs CS-EXPECTATION-VIOLATED gap registration: "Each CS qualitative baseline violation SHALL be registered as a CS-EXPECTATION-VIOLATED row in TABLE_5 with persona, expected pattern, and configured pattern named." SHALL-F appears in the preamble enforcement matrix. CA-1.6 verifies TABLE_5 for CS-EXPECTATION-VIOLATED rows and SHALL-F compliance. | format | aspirational | The preamble SHALL list includes SHALL-F with the exact obligation: CS-EXPECTATION-VIOLATED registration covering persona, expected pattern, and configured pattern. CA-1.6 row appears in Phase 3, performs double-anchor verification, and returns PASS/FAIL for SHALL-F compliance. An output with CS-EXPECTATION-VIOLATED rows but no SHALL-F obligation governing them fails this criterion. |
| C-31 | **TABLE_4 escalation vectors at SE-0 before SE-1** -- The SE role fills TABLE_4 (escalation vectors) at SE-0 -- a dedicated sub-step that executes before SE-1 (role-operation matrix). TABLE_4 at SE-0 establishes the escalation ceiling for each role before any cell in TABLE_1 is populated, so TABLE_1 values are constrained by the escalation model from the first row. SE-0 appears as an explicit labeled sub-step in Phase 1 (SE phase) with a handoff string to SE-1. An output where TABLE_4 is populated after TABLE_1, or where SE-0 is not a distinct labeled sub-step, fails this criterion even if TABLE_4 exists. | format | aspirational | Phase 1 contains an explicit SE-0 sub-step labeled before SE-1. TABLE_4 (escalation vectors) is populated within SE-0. SE-0 ends with a handoff string to SE-1. TABLE_1 values must reference SE-0 TABLE_4 content (e.g., "role ceiling established at SE-0"). An output where TABLE_4 appears after TABLE_1 in the output body fails even if both tables are present. Distinction from C-04 (escalation paths exist) and C-18 (CA as fourth mechanism): C-31 tests the execution order of TABLE_4 relative to TABLE_1, not the existence of escalation analysis or the CA audit layer. |
| C-32 | **MANUAL GAP / STRUCTURED CLOSE two-part closure-note labels** -- Each SE analysis sub-section (SE-1 through SE-4 at minimum) closes with a two-part closure note using exact label format: "MANUAL GAP: [gap-statement-or-NONE]" followed by "STRUCTURED CLOSE: [summary-of-findings-and-ceiling]". Both labels are exact strings; "Gap note:" or "Summary:" substitutions fail. The two-part structure enforces that every sub-section produces both an actionable gap statement and a structured summary before proceeding to the next sub-section. | format | aspirational | SE-1, SE-2, SE-3, and SE-4 each end with a MANUAL GAP block and a STRUCTURED CLOSE block as distinct labeled paragraphs. The labels are verbatim ("MANUAL GAP:" and "STRUCTURED CLOSE:"). At least one MANUAL GAP block contains a non-NONE entry. An output that uses informal closure phrases (e.g., "no gaps found", "summary: ...") without the exact label format fails this criterion. Distinction from C-11 (tables required) and C-24 (CA-1 paragraph structure): C-32 governs the SE role's sub-section closure protocol, not table formatting or CA-1 row structure. |
| C-33 | **CS-0 cites Schema Registry IDs before CS-1** -- The CS role opens its analysis with CS-0: a dedicated sub-step that quotes the Schema Registry IDs for CS-2 and CS-3 (sharing-rule conflict and cross-role differential tables) by their exact Registry labels before any CS-1 qualitative baseline content is produced. CS-0's Registry citations make the CS analysis structurally anchored to the pre-registered schemas from Phase 0, so CS-2 and CS-3 instantiation in later steps is a Registry-ID-driven obligation, not an ad hoc table choice. An output where CS begins with CS-1 baseline content without a CS-0 Registry citation step fails this criterion even if CS-2 and CS-3 are later populated correctly. | format | aspirational | Phase 2 (CS phase) contains an explicit CS-0 sub-step before CS-1. CS-0 quotes at minimum the Schema Registry IDs for CS-2 and CS-3 by exact label (e.g., "Schema ID: CS-2 -- Sharing Rule Conflict Sub-Table" and "Schema ID: CS-3 -- Cross-Role Differential Sub-Table"). CS-0 ends with a handoff string to CS-1. CA-1.8 verifies CS-0 presence and Registry ID citation. An output where the CS role's first sub-section is the qualitative baseline (CS-1) without a preceding Registry-citation step fails this criterion. Distinction from C-28 and C-29 (CS-2 and CS-3 exist in Schema Registry): C-33 tests that the CS role's execution begins by explicitly citing those Registry IDs, creating a self-referential anchor between the CS execution sequence and the pre-registered schema declarations. |
| C-34 | **Structural axis non-interference declaration in preamble** -- The preamble (Phase 0, Step 0.2 or equivalent) contains a dedicated STRUCTURAL AXIS NON-INTERFERENCE DECLARATION block, distinct from the C-01--C-05 mechanism mapping rows. This block names the three R12 structural axes (C-31: execution order, C-32: closure-note format, C-33: CS self-reference) as orthogonal dimensions and states an explicit non-interference rule: satisfying one axis does not justify relaxing another. The block converts the empirical composability of C-31/C-32/C-33 (observed in R12-V-05) into a binding contractual declaration that the model must honor throughout execution. | format | aspirational | Requires C-17, C-31, C-32, and C-33 to pass. A STRUCTURAL AXIS NON-INTERFERENCE DECLARATION block appears in Step 0.2 (or the preamble section immediately following the C-01--C-05 mechanism rows) as a separate named block. The block names all three axes (C-31, C-32, C-33) explicitly, declares them orthogonal dimensions, and includes a non-interference statement (e.g., "satisfying one axis does not justify relaxing another" or equivalent). The block is structurally distinct from the preamble enforcement matrix rows -- it is not a row in the C-01--C-05 table. An output where non-interference language appears only incidentally in prose without a dedicated named declaration block fails this criterion. Distinction from C-17 (preamble declares three-mechanism co-active enforcement for C-01--C-05): C-34 tests that a separate, structurally labeled declaration establishes non-interference for the three R12 structural axes as orthogonal obligations, independent of the C-01--C-05 enforcement contract. |
| C-35 | **SE-4 STRUCTURED CLOSE TABLE_4 row cross-reference + CA-1.9** -- SE-4's STRUCTURED CLOSE block (required by C-32) contains an explicit TABLE_4 row cross-reference in the format: "Cite: TABLE_4 (filled at SE-0) row [ID] -- [role] privilege ceiling: [pattern]". CA-1.9 is a dedicated verification row in Phase 3 that audits this cross-reference as a distinct audit target separate from CA-1.4 (SE-0 ordering) and CA-1.7 (MANUAL GAP labels). The cross-reference anchors SE-4's closure note to the escalation ceiling established at SE-0, ensuring the STRUCTURED CLOSE is a ceiling-referenced summary, not a free-form note. | format | aspirational | Requires C-31 and C-32 to pass. SE-4's STRUCTURED CLOSE block contains the verbatim cross-reference format: "Cite: TABLE_4 (filled at SE-0) row [ID] -- [role] privilege ceiling: [pattern]". A CA-1.9 verification row appears in Phase 3 with its own Registry anchor, Preamble anchor, and Verification paragraphs (per C-24). CA-1.9 is not a sub-item of CA-1.4 or CA-1.7 -- it is a sequentially numbered, independently labeled row. An output where SE-4's STRUCTURED CLOSE references TABLE_4 informally (e.g., "citing the SE-0 escalation ceiling") without the exact row-reference format fails this criterion. An output where CA-1.9 does not exist as an independent row (e.g., TABLE_4 cross-reference validation is folded into CA-1.4) fails this criterion. Distinction from C-31 (TABLE_4 at SE-0) and C-32 (MANUAL GAP / STRUCTURED CLOSE labels): C-35 tests that SE-4's STRUCTURED CLOSE contains a structurally exact TABLE_4 row reference back to SE-0, and that this specific content has its own CA-1.9 verification row distinct from other CA-1 rows. |
| C-36 | **CA terminal verdict tri-dimensional self-evidence attestation** -- The CA Format Compliance Verdict (Phase 3 terminal section) contains an explicit TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION block with a table naming each R12 structural dimension (C-31: execution order, C-32: closure-note format, C-33: CS self-reference) and its specific output-body evidence source. Each entry is marked CONFIRMED. This block extends C-22's phase-execution self-evidence (which attests execution order from phase labels) to cover all three R12 structural axes simultaneously, making the axes independently verifiable from the output body without consulting prompt instructions. | format | aspirational | Requires C-22, C-32, and C-33 to pass. The CA Format Compliance Verdict section contains a TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION block with at least 3 rows, one per R12 dimension. Each row names the dimension (C-31, C-32, or C-33), names the specific output-body evidence source (e.g., "PHASE 2 section header SE-0 precedes SE-1; TABLE_4 at SE-0 before TABLE_1"), and carries a CONFIRMED marker. All three rows must be marked CONFIRMED for this criterion to pass. An output where the CA terminal verdict summarizes R12 compliance in prose without a structured attestation table fails this criterion. An output whose attestation table covers fewer than all three R12 dimensions fails this criterion. Distinction from C-22 (phase-execution attestation): C-22 tests that phase labels in the output body make execution order self-attestable; C-36 tests that a dedicated attestation block in the CA terminal verdict covers all three R12 axes with named output-body evidence sources for each. |
| C-37 | **Preamble axis-label citation in tri-dimensional attestation** -- When C-34 provides a STRUCTURAL AXIS NON-INTERFERENCE DECLARATION with labeled axis rows (A-1/A-2/A-3 or equivalent) and C-36 provides a TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION, the attestation rows identify each R12 dimension by its preamble axis row label (A-1/A-2/A-3) in addition to or in place of the criterion number. This closes the C-34->C-36 structural loop under a shared labeling scheme: the axis declaration in the preamble and the attestation in the CA terminal verdict reference each dimension by the same label, making the loop verifiable by label match rather than by semantic interpretation. An output where the TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION rows identify dimensions only by criterion number (C-31/C-32/C-33) or descriptive phrase -- without citing the corresponding preamble axis row labels -- fails this criterion even if C-34 and C-36 both pass. | format | aspirational | Requires C-34 and C-36 to pass. The TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION table uses the preamble axis row labels (A-1, A-2, A-3 or equivalent) as row identifiers or row-header prefixes for each R12 dimension. Each attestation row contains the axis label from the preamble axis declaration (e.g., "A-1: Execution order (C-31) -- [evidence] -- CONFIRMED"). At least one row uses the exact preamble axis label (A-1, A-2, or A-3) as a named identifier within the row. An output where attestation rows use only criterion numbers (C-31/C-32/C-33) or dimension descriptions without citing the A-N labels declared in the preamble axis table fails this criterion. Distinction from C-34 (axis declaration with A-N labels exists in preamble) and C-36 (attestation table exists in CA terminal verdict): C-37 tests that the A-N label scheme from C-34 is the identification mechanism used in C-36's attestation rows, creating a closed labeling loop between preamble declaration and terminal attestation. |
| C-38 | **SHALL extension sub-clause with named CA-1.N preamble-anchor citation** -- When C-35 requires CA-1.9 to verify SE-4's TABLE_4 row cross-reference, the responsible SHALL obligation (SHALL-D for escalation paths, which owns SE-4) carries a named extension sub-clause declaring the C-35 sub-criterion obligation (e.g., "SHALL-D-EXT" or "SHALL-D [C-35]"). CA-1.9's preamble-anchor paragraph explicitly cites this extension sub-clause label as the obligation it is completing. This extends C-12's binary SHALL model (obligation stated or not) to a sub-clause inheritance model: new sub-criteria extend existing SHALLs rather than adding independent SHALL-H/I obligations, and CA-1.N verifiers cite the specific sub-clause they fulfill -- making obligation inheritance structurally traceable at the preamble-anchor level. | format | aspirational | Requires C-12 and C-35 to pass. At least one existing SHALL obligation (SHALL-A through SHALL-G) carries a declared extension sub-clause with a named label targeting a new sub-criterion. CA-1.9's preamble-anchor paragraph (per C-24's labeled paragraph structure) cites this extension sub-clause label as the obligation source being completed (e.g., "Preamble anchor: SHALL-D extended with C-35 clause -- completing sub-clause obligation for SE-4 TABLE_4 row cross-reference"). The citation must appear in the preamble-anchor paragraph, not in the Registry anchor or the verification conclusion paragraph. An output where CA-1.9 cites only the base SHALL-D obligation without naming an extension sub-clause fails this criterion. An output where the SHALL-D extension sub-clause is declared in the preamble but CA-1.9's preamble anchor does not cite it by name also fails. Distinction from C-12 (SHALL obligations exist) and C-35 (CA-1.9 exists and is distinct from CA-1.4 and CA-1.7): C-38 tests that SHALL extension sub-clauses are declared for new sub-criteria and that CA-1.N verifiers explicitly name the extension sub-clause in their preamble-anchor paragraph, creating a traceable obligation inheritance chain rather than a new independent SHALL obligation. |
| C-39 | **Phase mandate CA-1.N inter-row distinctness declaration** -- The Phase 3 execution mandate (or equivalent phase-level instruction block in the output body governing the CA's verification pass) explicitly names CA-1.9 by row ID and declares its inter-row distinctness from CA-1.4 (SE-0 ordering) and CA-1.7 (MANUAL GAP labels) as non-overlapping audit targets. This makes CA-1.9's scope verifiable from the phase-boundary declarations in the output body alone, without entering the CA-1.9 row content. Extends C-22's phase-execution attestation (which makes execution order verifiable from phase labels) to cover the scope of individual CA-1.N verification rows. | format | aspirational | Requires C-22 (phase labels in output body) and C-35 (CA-1.9 exists and is distinct) to pass. The Phase 3 section header or Phase 3 mandate instruction block in the output body contains an explicit statement naming CA-1.9 by row ID and declaring its distinctness from CA-1.4 and CA-1.7 as separate audit targets (e.g., "CA-1.9 verifies SE-4 STRUCTURED CLOSE TABLE_4 row cross-reference at SE-0 position -- distinct from CA-1.4 and CA-1.7"). The distinctness declaration must appear at the phase-mandate level (a structural phase boundary in the output body), not only inside the CA-1.9 verification row. An output where inter-row distinctness is stated only inside CA-1.9 itself (meeting part of C-35) but not in the Phase 3 mandate block fails this criterion. Distinction from C-35 (CA-1.9 itself contains distinct-target language): C-39 tests that the same distinctness claim is echoed at the phase-mandate boundary, making it verifiable from the phase structure without reading the CA-1.9 row -- paralleling how C-22 makes execution order verifiable from phase labels without reading the CA role's authored preamble. |

---

## Tier Totals

| Tier | Criteria | Max pts |
|------|----------|---------|
| Essential | C-01 -- C-05 | 60 |
| Recommended | C-06 -- C-08 | 30 |
| Aspirational | C-09 -- C-39 | 10 |
| **Total** | | **100** |

Aspirational count: 31 criteria (C-09 through C-39). Each aspirational = 10/31 = 0.323 pts.

---

## Design Notes

**C-37 vs C-34 and C-36**: C-34 requires the preamble to contain a STRUCTURAL AXIS NON-INTERFERENCE DECLARATION with labeled axis rows (A-1/A-2/A-3). C-36 requires the CA terminal verdict to contain a TRI-DIMENSIONAL SELF-EVIDENCE ATTESTATION table. Both can coexist in an output (as in R13-V-05) without the attestation table referencing the axis labels -- the C-34 axis labels and the C-36 attestation rows use different identification schemes (A-N vs C-N). C-37 closes this loop: the attestation rows must use the preamble axis labels as their row identifiers. R13-V-05 fails C-37 because it inherits V-03's attestation rows (which identify dimensions as "Execution order (C-31)") and the preamble axis uses A-1/A-2/A-3 labels that are not carried through to the attestation.

**C-38 vs C-12 and C-35**: C-12 requires SHALL obligations to be stated. C-35 requires CA-1.9 to exist and be distinct from CA-1.4/CA-1.7. Neither tests whether the SHALL obligation that owns SE-4 (SHALL-D) carries a named sub-clause for the C-35 sub-criterion, or whether CA-1.9's preamble-anchor paragraph cites that sub-clause. C-38 tests both: the SHALL extension must be named, and CA-1.9 must cite it. R13-V-02 and R13-V-05 pass C-38 (inherited via V-02's CA-1.9 structure). R13-V-04 has SHALL-D extension in the preamble but its CA-1.9 preamble-anchor citation is not confirmed by the scorecard evidence.

**C-39 vs C-22 and C-35**: C-22 requires phase labels in the output body to make execution order self-attestable. C-35 requires CA-1.9 to contain distinctness language. C-39 requires the phase-mandate block itself (a structural phase boundary) to carry the CA-1.9 distinctness claim -- at a higher structural level than the CA-1.9 row. R13-V-05 satisfies C-39 (Phase 3 mandate explicitly names CA-1.9 and its distinctness). R13-V-04 satisfies C-35 via a NOTE in SE-4 naming CA-1.9, but the Phase 3 mandate for V-04 is not described with the same distinctness declaration, so V-04 fails C-39.

**C-37, C-38, C-39 are independent**: C-37 requires C-34+C-36 (axis label cross-link). C-38 requires C-12+C-35 (SHALL extension chain). C-39 requires C-22+C-35 (phase-mandate distinctness echo). A variation can satisfy any combination independently. R13-V-05 satisfies C-38 and C-39 but not C-37; R13-V-02 satisfies C-38 but not C-37 or C-39.
