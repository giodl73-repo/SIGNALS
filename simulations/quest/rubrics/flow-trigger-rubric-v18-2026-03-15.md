The v18 rubric is written to `flow-trigger-rubric-v18-2026-03-16.md`. Here is the complete summary of what was extracted and added:

---

## v18 — three new aspirational criteria extracted from R20

| ID | Source | What it captures | Extends | N_aspirational | Max (raw) |
|----|--------|-----------------|---------|----------------|-----------|
| C-48 | R20 V-05 hypothesis (explicit dual-axis articulation); V-04 demonstrates both axes structurally without the naming statement | **Cross-axis execution-sequence redundancy** — when C-46 (all phases carry dependency + violation mode) AND C-47 (registry rows in execution sequence) are both fully satisfied simultaneously, execution-sequence violations become independently detectable from two orthogonal axes: the phase-body level and the registry-row-position level. Neither axis covers the other's detection domain. PARTIAL = both C-46 and C-47 PASS but no explicit statement names the cross-axis redundancy (redundancy implicit, not stated). FAIL = either C-46 or C-47 FAIL or PARTIAL. | C-46 + C-47 (captures emergent redundancy property of the pair; neither alone provides it) | 39 → 40 | 162.5 → 165.0 |
| C-49 | R20 V-01/V-04/V-05 axis — registry header `*(Row order: Domain Expert obligations first — declaration-time artifact producers; Auditor obligations follow — verification-time enforcers.)*` present; absent in V-02/V-03 which use "standard Auditor-first row order" | **Registry row-ordering invariant as a named positional commitment** — the Phase 0 obligation registry carries an explicit header annotation stating the row-ordering invariant in prose, naming both (a) the ordering rule and (b) the role-time justification for each group, converting the positional ordering from a convention inferred from row arrangement into a stated structural contract. PARTIAL = C-47 PASS + partial annotation (ordering rule but not role-time justification, or one group only). FAIL = C-47 FAIL, OR C-47 PASS but no header annotation of any kind. | C-47 (same domain — C-49 adds explicit declaration requirement on top of correct ordering, as C-38 extends C-32 by requiring formal obligation text in addition to column presence) | 40 → 41 | 165.0 → 167.5 |
| C-50 | R20 V-04/V-05 excellence signal — DERIVATION TEST covers all 7 INERTIA CONTRAST mechanisms including DUAL-TIME ATTRIBUTION CHAIN; V-01/V-02/V-03 fail C-39 (only 6 mechanisms) so C-50 is unreachable for them | **Attribution chain DERIVATION TEST coverage** — the DERIVATION TEST (C-35) explicitly includes DUAL-TIME ATTRIBUTION CHAIN as a covered mechanism, providing a derivation row mapping the failure mode label ("anonymous detection-time attribution") to its absent structural property and derived minimum implementation. Closes the loop between C-35 (formal completeness proof) and C-39 (attribution chain self-documentation): makes the C-39 claim falsifiable by requiring derivability from the failure mode label alone. PARTIAL = C-39 PASS via INERTIA CONTRAST entry + C-35 PASS, but DERIVATION TEST table lacks an attribution chain row (mechanism declared but derivation not extended to it). FAIL = C-39 FAIL or C-35 FAIL. | C-35 (DERIVATION TEST domain) via C-39 (attribution chain as a mechanism) — raises the derivation coverage requirement to span the full mechanism set including the attribution chain | 41 → 42 | 167.5 → 170.0 |

**Structural relationships:**
- C-48 is an emergent-combination criterion: it captures a property of the C-46/C-47 pair that neither member alone provides, analogous to how C-32 captures a property that the per-row criteria (C-25–C-31) could each pass without delivering.
- C-49 extends C-47 by adding a declaration requirement to a correct-ordering requirement — the same move C-38 makes on C-32, converting per-element metadata into a table-level invariant.
- C-50 extends C-35 via C-39, requiring the DERIVATION TEST to cover the attribution chain mechanism specifically. It is the closing criterion for the C-39 thread: C-39 documents the chain, C-50 proves the documentation is derivable without rubric access.
