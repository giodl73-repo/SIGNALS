`signal-check-rubric-v11-2026-03-17.md` written. Here is the v10->v11 summary:

---

**v10 -> v11 summary:** 4 new criteria, /51 -> /55.

| ID | Criterion | Source |
|----|-----------|--------|
| C-60 | Extensibility rule body names the minimum syntactic boundary mechanism, distinguishing co-location embedding from period-separated alternatives | R10 EX-01: semicolon-embedding as minimum syntactic form |
| C-61 | Lookup-table form declaration includes an explicit form-over-content statement naming the scan-enabling form change as the gate | R10 EX-02: form change (not content) is the gate |
| C-62 | Co-requirement declaration includes an explicit partial-compliance detectability statement naming two-of-three satisfaction as identifiably incomplete | R10 EX-03: detectability as a declared design property |
| C-63 | Phrasing-agnosticism chain closure includes an explicit registry self-completeness certification naming all governed equivalence classes by criterion-pair mapping | R10 pattern 5: end-to-end self-describing registry |

**Additive cascade:** V-01 fails all four; V-02 fails C-61/C-62/C-63; V-03 fails C-62/C-63; V-04 fails C-63 only; V-05 passes all.

**New composite: 55/55 = 100.00. Prior A+ ceiling (51/55) = 92.73%.**

The four criteria deepen C-56 through C-59 by converting each from a structural requirement into a self-declaring contract: C-60 names the syntactic boundary mechanism (not just the positional property), C-61 declares the form/content gate explicitly (not just structurally), C-62 names partial-compliance detectability as a design property (not just a consequence), and C-63 requires a registry self-completeness certification (not just a locally-declared equivalence).
d-to-end self-describing property that V-05 achieves but does not explicitly declare |

**Additive cascade:** V-01 fails all four; V-02 fails C-61/C-62/C-63; V-03 fails C-62/C-63; V-04 fails C-63 only; V-05 passes all.

**Additive structure:** The four criteria deepen C-56 through C-59 by converting each from a structural requirement into a self-declaring contract. C-60 converts C-56's "embedded" from a positional property into a syntactically-specified constraint. C-61 converts C-57's form requirement from an auditable structural fact into an explicitly declared gate. C-62 converts C-58's co-requirement from a joint-requirement statement into a detectability contract. C-63 converts C-59's phrasing-agnosticism from a locally-declared equivalence into a registry-level self-completeness certification.

**Cascade logic:** C-60 enforces the minimum-syntactic-form requirement that names the boundary mechanism, not just the positional property. C-61 enforces the form-over-content statement that closes the false-inference gap where content improvements could be mistaken for structural compliance. C-62 enforces the partial-compliance detectability statement that elevates the co-requirement framing from "jointly required" to "partial compliance is identifiably incomplete." C-63 enforces the registry self-completeness certification that makes the equivalence registry auditable as a complete governed system rather than as a set of individually-declared classes.

**New composite: 55/55 aspirational = 100.00. Prior A+ ceiling (51/55) = 92.73.**

---

**v9 -> v10 summary:** 4 new criteria, /47 -> /51.

| ID | Criterion | Source |
|----|-----------|--------|
| C-56 | Extensibility rule embeds a violation condition within the rule body | R9 EX-01: "single-layer class is a locally visible violation" elevates rule from specification to self-contained enforcement point |
| C-57 | Active equivalence classes in labeled lookup-table form | R9 EX-02: criterion-ID pairs as labeled lookup entries enabling membership verification by scan |
| C-58 | Three-point completeness declared by explicit co-requirement statement | R9 EX-03: co-requirement declaration distinguishes complete extensibility package from three independently-passed criteria |
| C-59 | Three structural requirements declared phrasing-agnostic with two confirmed-equivalent phrasings | R9 invariance: V-04 and V-05 satisfy C-53/C-54/C-55 identically -- extends C-49/C-50 pattern into the new complex |

**Additive cascade:** V-01 fails all four; V-02 fails C-57/C-58/C-59; V-03 fails C-58/C-59; V-04 fails C-59 only; V-05 passes all.

**New composite: 51/51 aspirational = 100.00. Prior A+ ceiling (47/51) = 92.16.**

---

## Essential Criteria (50% of composite)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-01 | **All four signal-check dimensions are present** | structure | Output addresses all four dimensions: SEQUENCE, COHERENCE, CAUSAL GAP, and STALENESS. A report that omits any dimension or collapses two into one = fail. |
| C-02 | **SEQUENCE finding is grounded in artifact dates or ordering** | correctness | Cites actual artifact timestamps or creation order. A SEQUENCE verdict with no artifact reference = fail. |
| C-03 | **COHERENCE finding identifies specific agreements or contradictions between signals** | correctness | Names at least two signals and states whether they align or contradict on a specific claim. Vague statements with no named comparison = fail. |
| C-04 | **CAUSAL GAP verdict states whether the feature-to-outcome mechanism has evidence** | correctness | States explicitly whether artifact evidence exists for the causal pathway and names what is present or missing. A section that only restates the hypothesis = fail. |
| C-05 | **Output is coaching, not verdictive** | behavior | Surfaces issues as observations the team can decide to address -- not blocking failures or binary PASS/FAIL judgments. "You must fix X before proceeding" = fail. |

---

## Recommended Criteria (30% of composite)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-06 | **STALENESS finding applies a concrete threshold** | depth | Uses an explicit recency criterion (e.g., 30 days) rather than a subjective impression. No threshold stated = fail. |
| C-07 | **Each flagged issue includes a suggested next action** | behavior | Every flagged dimension gets at least one concrete action (e.g., "run discover-competitors again"). Flags without actions = fail. |
| C-08 | **Report opens with a readiness summary** | format | Output begins with a brief summary of clean vs. flagged dimensions and overall readiness. Diving straight into per-dimension detail = fail. |

---

## Aspirational Criteria (10% of composite)

| ID | Criterion | Category | Pass Condition |
|----|-----------|----------|----------------|
| C-09 | **Cross-dimension pattern is named when present** | depth | When multiple dimensions flag the same root weakness, the output names the pattern explicitly. Four separate flags with no root identification = fail when the pattern is obvious. |
| C-10 | **Missing signal types are identified by namespace** | coverage | Lists namespaces for which no artifact exists and notes whether those gaps are expected or concerning given topic stage. Only evaluating what exists = fail. |
| C-11 | **Readiness summary is drafted before dimension analysis and confirmed or revised after** | format | The readiness summary is positioned as an early draft, then confirmed or revised at the close of the dimension section. Summary assembled only after analysis, or contradicting findings without a revision note, = fail. *Source: V-05 R1 structural fix -- STEP 2 before STEP 3, paired with "draft now, finalize after."* |
| C-12 | **Staleness threshold is calibrated from inventory contents, not author judgment** | depth | Threshold is dynamically derived from inventory (e.g., tightens to 14 days when competitor signals exist, relaxes for early-stage topics). Fixed threshold regardless of inventory composition = fail. *Source: V-05 R1 inventory-triggered tightening -- "use 14 days if any competitor signals exist."* |
| C-13 | **Analysis phases are isolated via visual separators and scope budgets** | format | Each analysis phase is boxed with a visible border marker and annotated with a scope budget (word count or sentence limit). Phases that bleed into one another or carry no proportionality constraint = fail. *Source: V-04 R2 -- prevents analysis bleed, enforces proportionality across phases. Compatible with conversational register: `---STEP N--- (~X words) ---` markers satisfy the criterion without a template frame.* |
| C-14 | **Severity scale is embedded in analysis but protected from coaching register** | behavior | The output uses a structured internal scoring mechanism (e.g., severity table, ranked flags) and labels it as internal-only, keeping the team-facing prose advisory in tone. Severity exposed directly to team as a gate, or no internal scoring structure present, = fail. *Source: V-02 R2 -- separates analytical precision from coaching presentation without sacrificing either. Compatible with conversational register: inline `(internal: green/yellow/red)` notation satisfies the criterion.* |
| C-15 | **Status-quo alternative is anchored at Step 0 before inventory runs** | depth | The output front-loads an explicit "beats doing nothing?" framing before the dimension analysis begins -- establishing the inertia baseline so each dimension can be evaluated against the cost of not acting rather than in isolation. Dimension analysis that begins without a declared inertia baseline = fail when the topic has any detectable status-quo alternative. *Source: V-04 R3 -- propagates the beats-doing-nothing question through all four dimensions, making cross-dimension patterns more legible when they trace to a shared inertia-beating gap.* |
| C-16 | **Two-register separation is enforced by document shape, not instruction** | behavior | The output is divided into a named internal analytical record (e.g., PART 1) and a named team coaching report (e.g., PART 2) as distinct document sections. Register protection that depends solely on inline warnings or per-paragraph labels = fail; structural separation by section boundary is required. *Source: V-05 R3 -- makes C-14 register leakage architecturally impossible rather than instruction-dependent: the team coaching section cannot acquire severity labels because it is a separate document section with no severity rows.* |
| C-17 | **SEQUENCE is evaluated as mechanism-evidence audit, not temporal ordering alone** | depth | When CAUSAL GAP has been evaluated first (or its findings are available), SEQUENCE asks whether the discovery phase established the mechanism evidence -- not merely whether discovery preceded spec. A SEQUENCE section that only audits temporal order and ignores whether discovery built the right causal case = fail when mechanism evidence gaps have been identified. *Source: V-01 and V-04 R5 -- CAUSAL GAP-first framing qualitatively changes SEQUENCE: "did discovery precede spec?" becomes "did discovery establish the mechanism evidence you just evaluated?" Strengthens mechanism depth without degrading the C-02 artifact-citation requirement.* |
| C-18 | **PART 2 opens with an inertia case-strength question at summary level** | behavior | The team coaching summary (PART 2 or equivalent) includes a top-level readiness frame that explicitly asks whether the case that the feature beats doing nothing is clearly built, partially built, or still open -- before or alongside the dimension observations. Inertia framing that appears only within individual dimension coaching (never at summary level) = fail. *Source: V-02 R5 -- extends C-15's Step 0 inertia anchor into the coaching register at summary level, adding a distinct framing that is advisory ("clearly built / partially built / still open") and does not risk C-05. Complements C-15 (which anchors the analysis) and C-16 (which structures the registers) without duplicating either.* |
| C-19 | **Mechanism verdict from CAUSAL GAP is embedded as a required literal input to SEQUENCE** | depth | SEQUENCE does not run until the CAUSAL GAP mechanism verdict is explicitly quoted in-place (e.g., `Quoting mechanism verdict: "..."`) -- making architectural handoff impossible to skip rather than merely instructed. A SEQUENCE section that runs without the mechanism verdict as a named, quoted input = fail when mechanism evidence gaps are present. *Source: V-01 and V-04 R6 differentiation on C-17 -- the verbatim-quote requirement elevates C-17 from an instructional norm to an architectural constraint: SEQUENCE literally cannot proceed without consuming the CAUSAL GAP verdict. Strengthens mechanism depth without replacing the C-02 artifact-citation requirement.* |
| C-20 | **Inertia-relevance is a named column in the artifact inventory that CAUSAL GAP explicitly consumes** | depth | The Step 0 inertia anchor seeds a named "Inertia Relevant?" (or equivalent) column in the artifact inventory table, and CAUSAL GAP explicitly scores mechanism evidence against inertia-relevant signals only -- creating a traceable inertia thread across at least three phases (Step 0 declaration -> inventory tagging -> CAUSAL GAP scoring). An inertia anchor that declares the baseline at Step 0 but does not propagate as a named column through inventory = fail; CAUSAL GAP that scores all artifacts equally without filtering to inertia-relevant signals = fail. *Source: V-04 R6 differentiation on C-15 -- converts the static Step 0 declaration into a living artifact: the inertia-relevance tag flows from anchor through inventory into CAUSAL GAP, making the mechanism question operationally traceable rather than rhetorically present.* |
| C-21 | **Inertia case-strength occupies a dedicated named section in PART 2, not a combined or inline position** | behavior | The team coaching summary gives the inertia case-strength frame (C-18's "clearly built / partially built / still open") its own named section header at the same structural level as the readiness summary -- not merged into the readiness section, not introduced as a sub-question, not positioned inline. An inertia frame that shares a section boundary with readiness or appears as a paragraph within another section = fail. *Source: V-02 and V-04 R6 differentiation on C-18 -- dedicating a named section (e.g., `STEP A: INERTIA CASE STRENGTH`) before the readiness draft elevates the inertia question to a structural peer of readiness rather than an addendum, signaling to the team that the two questions are independently answerable and neither subordinates the other.* |
| C-22 | **PART 2 inertia case-strength section opens with mechanism verdict quoted verbatim** | behavior | The dedicated inertia case-strength section in PART 2 (C-21) opens by quoting the CAUSAL GAP mechanism verdict verbatim (e.g., `Quoting mechanism verdict: "..."`) before stating the coaching judgment -- making the inertia case architecturally grounded in the analytical record rather than independently re-inferred across the register boundary. A PART 2 inertia section that reaches a coaching conclusion without quoting the mechanism verdict explicitly = fail when mechanism evidence gaps have been identified. *Source: V-01/V-04/V-05 R7 EX-01 -- extends C-19's verbatim-quote constraint from SEQUENCE into PART 2: the same architectural guarantee that prevents SEQUENCE from re-inferring the mechanism verdict now prevents the coaching register from doing so. Divergence between coaching conclusion and mechanism verdict becomes detectable by inspection rather than by inference.* |
| C-23 | **Named binary outputs from dimension analysis are consumed by label in STEP 3 and PART 2** | depth | When a dimension analysis produces a named binary verdict (e.g., `Pre-specification gap: YES/NO`), STEP 3's cross-dimension pattern analysis and PART 2's coaching sections explicitly reference that binary by its label -- not by re-deriving the conclusion from prose. Cross-dimension synthesis that re-infers a timing or mechanism verdict from analytical prose rather than consuming a named output = fail; PART 2 coaching that references the conclusion without naming the source binary = fail. *Source: V-02/V-04/V-05 R7 EX-02 -- converts dimension analysis outputs from prose findings into named artifacts that downstream phases must consume structurally. Cross-dimension patterns become traceable: STEP 3 cannot construct a pattern narrative that contradicts the named binary without the contradiction being visible in the label reference.* |
| C-24 | **STEP 3 emits a named root-pattern label that PART 2 coaching consumes by name** | depth | When a cross-dimension pattern is present, STEP 3 emits a labeled output (e.g., `Root pattern: [SHORT NAME]`) that PART 2 coaching sections (e.g., STEP B, STEP C per-dim, STEP D) reference explicitly by that label -- not by re-inferring the cross-dimension shape from analytical prose. A STEP 3 that names the pattern only in prose without a labeled output = fail when a cross-dimension pattern is present; PART 2 coaching that characterizes the cross-dimension shape without citing the STEP 3 root-pattern label = fail. Extends C-23's named-binary chain from dimension analysis through synthesis into the coaching register, closing the prose-drift gap between PART 1 synthesis and PART 2 readiness. *Source: V-03/V-04/V-05 R8 EX-01 -- STEP 3 root-pattern label observed to prevent PART 2 from independently re-characterizing the cross-dimension pattern in advisory language, creating a detectable mismatch if the coaching register drifts from the synthesis verdict.* |
| C-25 | **Upfront ARCHITECTURE block declares all named outputs and their consuming steps before analysis runs** | format | The output opens with a named block (e.g., `ARCHITECTURE`) that lists every named output the analysis will produce and every downstream step that must consume it by label -- before any dimension analysis begins. An architecture declaration that is absent, or that appears after analysis has started, = fail; a block that lists outputs but omits the consuming steps = fail. Makes the dataflow contract auditable before execution rather than distributed across per-step instructions, so any consumption gap is visible without reading the full output. *Source: V-05 R8 EX-02 -- upfront ARCHITECTURE block observed to convert the named-output pipeline from an emergent property of per-step instructions into an explicit, front-loaded contract: a reader can verify C-23 and C-24 compliance by checking the ARCHITECTURE block against STEP 3 and PART 2 without tracing the full analytical chain.* |
| C-26 | **Consuming steps carry an explicit "Required input -- do not re-derive" prohibition on each named-input reference** | depth | Every step that consumes a named output (per C-23, C-24) annotates that input reference with an inline prohibition (e.g., `Required input -- do not re-derive`) rather than relying solely on instructions elsewhere to enforce non-re-derivation. A consuming step that references a named input without the prohibition marker = fail when an ARCHITECTURE block (C-25) has declared the consuming relationship. Converts label consumption from an instructional norm to an explicit anti-pattern constraint: re-derivation becomes a visible violation rather than an undetectable judgment call. *Source: V-05 R8 EX-03 -- inline prohibition notation observed to close the gap between C-23/C-24's structural requirement and execution: even when the ARCHITECTURE block and per-step instructions are present, re-derivation can occur silently; the prohibition marker makes the constraint local to the consumption site and independently enforceable.* |
| C-27 | **ARCHITECTURE block uses three-column table form (Named Output | Produced by | Consumed by) on the advisory register** | format | When the output is in the advisory register, the ARCHITECTURE block is structured as a three-column table with exactly the columns Named Output, Produced by, and Consumed by -- not as a schema enumeration or prose list. A schema or prose ARCHITECTURE block on the advisory register when table form is achievable with equal completeness = fail; a table that omits the Consumed by column = fail. Three-column table delivers C-25's pre-execution auditability contract with lower overhead than schema form: independently scannable without requiring readers to parse per-step consuming-step annotations. Imperative-register outputs that use schema form for precision do not fail this criterion -- C-27 applies specifically to the advisory register. *Source: V-01/V-04 R9 EX-01 -- table form observed to satisfy C-25 with significantly lower overhead: pipeline front-loaded, independently scannable, accurate to scope; schema form (V-05) is valid but exceeds minimum-overhead threshold on advisory register.* |
| C-28 | **C-26 prohibition is implemented per-input (separate annotation per named input reference), not per-step grouped** | depth | When a consuming step references multiple named inputs, each named-input reference carries its own independent prohibition annotation (e.g., `Required input -- do not re-derive: Pre-specification gap` and `Required input -- do not re-derive: Root pattern` as separate lines) rather than a single grouped annotation covering both (e.g., `Required inputs -- do not re-derive [Input A, Input B]`). A consuming step that groups two or more named-input prohibitions into a single annotation = fail when those inputs are referenced independently at the same step. Per-input form makes partial compliance impossible: a model cannot comply for one named input while re-deriving a second when each reference carries its own independent prohibition; the grouped form leaves that violation undetectable. *Source: V-05 R9 EX-02 -- per-input prohibition observed to close a compliance gap that per-step grouped notation leaves open: grouped annotation allows selective compliance without a locally visible violation; independent per-input annotations make each specific re-derivation immediately identifiable at the consumption site.* |
| C-29 | **STEP C per-dimension Root pattern contribution notes emit a named binary (`STEP C drift: CLOSED/OPEN`) that STEP D consumes by label** | depth | When STEP C includes per-dimension Root pattern contribution notes (C-24 extended), each dimension's contribution note emits a named binary verdict (e.g., `STEP C drift: CLOSED` when the dimension aligns with the STEP 3 root-pattern label; `STEP C drift: OPEN` when it diverges or cannot confirm the label). STEP D readiness confirmation explicitly consumes each STEP C drift binary by label before issuing a final verdict. A STEP C that includes per-dimension contribution notes but does not emit a named binary = fail when STEP D exists; a STEP D that confirms readiness without referencing the STEP C drift binaries by label = fail. Closes the final open gap in the C-23/C-24 named-binary chain: dimension coaching observations become consumable artifacts that STEP D references structurally rather than re-derives from prose, completing the chain from dimension-level coaching back to confirmed readiness. *Source: R9 EX-03 direction candidate -- per-dimension contribution notes observed to characterize cross-dimension relationships in advisory language without emitting a named binary; STEP D readiness confirmation cannot structurally consume them, leaving the coaching-to-readiness handoff as an inferential step rather than an architectural one.* |
| C-30 | **Each production step annotates its named output with a forward-declaration arrow to its primary consumer** | depth | When a step emits a named binary or labeled output (per C-23, C-24, C-29), that output line carries a forward-declaration annotation at the production site (e.g., `Pre-specification gap: YES -> STEP 3`) naming the consuming step -- not relying solely on the ARCHITECTURE block or the consuming step's back-reference to establish the handoff. A production step that emits a named output without a forward-declaration arrow = fail when an ARCHITECTURE block has declared the consuming relationship and the consuming step carries a C-26 prohibition. Creates a three-point declaration chain for every named output (ARCHITECTURE pre-declares -> production step forward-declares -> consuming step back-references via prohibition), making chain integrity locally verifiable at each handoff without cross-referencing the full output. *Source: R10 V-04 EX-01 -- production-site forward annotations observed to make the named-binary handoff self-declaring at both ends; chain verification reduced from a global scan to a local pairwise check.* |
| C-31 | **ARCHITECTURE block opens with a one-line meta-rule pre-declaring the per-input prohibition form as a binding contract** | format | The ARCHITECTURE block (C-25, C-27) carries an explicit meta-rule in its opening line before the three-column table -- stating that every consuming step must use per-input prohibition form, not grouped (e.g., "Each consuming step carries a separate Required input -- do not re-derive: [label] per named input -- not grouped") -- converting C-28 from an inferred constraint into a pre-declared contract. An ARCHITECTURE block whose table is accurate and complete (C-27) but whose opening omits the per-input meta-rule = fail when C-28 compliance is required. Pre-declaration at the contract level makes the per-input requirement auditable at a single read point without inspecting each step individually. *Source: R10 V-04 EX-02 -- ARCHITECTURE meta-rule header observed to elevate per-input prohibition from a locally-enforced execution norm to a pre-declared structural contract.* |
| C-32 | **STEP D emits a named confirmed-readiness verdict that PART 2's overall readiness summary consumes by label** | depth | STEP D does not only consume STEP C drift binaries (C-29) and issue a prose conclusion -- it emits a labeled final verdict (e.g., `Confirmed readiness: READY / CONDITIONALLY READY / NOT READY`) that the PART 2 overall readiness summary (STEP B or equivalent) consumes by label before stating the team-facing readiness position. A STEP D that issues a confirmed-readiness conclusion only in prose without a named output = fail when PART 2 exists; a PART 2 readiness summary that states a readiness position without referencing the STEP D confirmed-readiness label = fail. Closes the STEP D to team-facing handoff: PART 2's summary is structurally grounded in the analytical record rather than re-inferring readiness across the register boundary. *Source: R10 V-04 EX-03 -- STEP D confirmed-readiness prose observed to leave PART 2 without a named input to consume; a named verdict closes that gap symmetrically with C-29's closure of the STEP C to STEP D gap.* |
| C-33 | **STEP A (PART 2 inertia case-strength) emits a named verdict consumed by PART 2's readiness summary by label** | depth | The dedicated inertia case-strength section (STEP A or equivalent, per C-21) closes by emitting a named verdict (e.g., `Inertia case: BUILT / PARTIAL / OPEN`) and PART 2's readiness summary section explicitly consumes that label before stating the team-facing readiness position -- making the inertia-case judgment a structural input to readiness rather than a prose backdrop. A STEP A that reaches an inertia-case conclusion only in advisory prose without a named output = fail when a PART 2 readiness summary exists; a PART 2 readiness summary that states a position without referencing the STEP A inertia-case label = fail when mechanism evidence gaps have been identified. Closes the final open handoff in the PART 2 internal chain: STEP D confirmed-readiness (C-32) establishes the analytical grounding; STEP A inertia-case establishes the strategic grounding; the readiness summary must consume both to be jointly grounded. Without C-33, STEP B draws on two named inputs (C-32) and one prose judgment (STEP A), leaving the inertia dimension uniquely unarchitectured inside PART 2. *Source: R10 R11 direction -- after C-32 closes the STEP D to PART 2 handoff, the only remaining inferential gap in PART 2 is STEP A's inertia verdict flowing into STEP B as prose rather than as a named binary; this gap is structurally identical to the STEP D gap that C-32 just closed one handoff later in the same register.* |
| C-34 | **PART 2 readiness summary opens by consuming both the STEP D confirmed-readiness label (C-32) and the STEP A inertia-case label (C-33) as per-input prohibitions** | depth | The PART 2 readiness summary section (STEP B or equivalent) opens with exactly two per-input prohibition annotations -- one consuming the STEP D confirmed-readiness verdict by label (`Required input -- do not re-derive: Confirmed readiness`) and one consuming the STEP A inertia-case verdict by label (`Required input -- do not re-derive: Inertia case`) -- before stating the team-facing readiness position. A readiness summary that references only the STEP D label without a parallel STEP A label = fail when both sections exist; a readiness summary that groups both inputs under a single annotation = fail (per C-28 per-input form). Makes the team-facing readiness position jointly architecturally grounded in both analytical confirmation (STEP D) and strategic inertia-case assessment (STEP A): both inputs are required, independently prohibited from re-derivation, and visible as symmetric peers at the consumption site. Joint consumption converts readiness from a STEP D re-statement into a synthesized position across two independently grounded dimensions. *Source: R11 direction extension -- C-33 creates the STEP A named binary; C-34 closes the complementary consumption requirement: once two parallel named outputs exist (C-32 and C-33), C-28 per-input form requires them to be consumed independently, making joint grounding architecturally enforced rather than instructionally encouraged.* |
| C-35 | **ARCHITECTURE block extends to include PART 2 internal named outputs and their consuming steps, making full cross-register dataflow auditable before either part begins** | format | The ARCHITECTURE block (C-25, C-27, C-31) includes rows for PART 2 internal named outputs -- at minimum, the STEP A inertia-case verdict (C-33) and its consumer (PART 2 readiness summary) -- in addition to the PART 1 named outputs already declared. An ARCHITECTURE block that accurately declares all PART 1 outputs but omits PART 2 internal outputs = fail when PART 2 contains named outputs consumed by other PART 2 steps. Adding PART 2 rows to the ARCHITECTURE table extends C-25/C-27's pre-execution auditability from the analytical register to the full cross-register pipeline: a reader can verify C-33/C-34 joint-consumption compliance from the ARCHITECTURE block without reading through PART 2 section by section. Without C-35, the ARCHITECTURE block accurately contracts the PART 1 pipeline and the PART 1 to PART 2 handoffs (C-32 row), but the PART 2 internal chain is undeclared -- its structure is only visible by tracing through the coaching sections, defeating the front-loaded auditability goal that C-25/C-27 establish for PART 1. *Source: R11 direction extension -- C-33/C-34 create a new PART 2 internal dataflow contract (STEP A -> STEP B); C-35 requires that contract to appear in the ARCHITECTURE block before either part begins, applying the same auditability standard that C-25 established for PART 1 to the newly-architectured PART 2 chain.* |
| C-36 | **Per-input prohibitions independently self-standing at convergence** | depth | At any step where two or more named inputs converge (e.g., STEP E consuming both Confirmed readiness and Inertia case), each prohibition annotation is independently parseable without reference to the other -- meaning the label it names, the step it references, and the constraint it asserts are complete within the annotation itself. A prohibition annotation at a convergence step that relies on an adjacent annotation to supply its step context or label scope = fail. Independence-in-isolation is the property that makes per-input form (C-28) meaningful at convergence: when each annotation is self-standing, a reader can lift any single annotation out of the convergence step and verify its compliance without reading the other annotations present. *Source: carry-forward.* |
| C-37 | **ARCHITECTURE Consumed-by entries at step-level granularity** | format | Every Consumed-by entry in the ARCHITECTURE table names the consuming step at the step level (e.g., "PART 2 STEP E") rather than the section level (e.g., "PART 2"). A Consumed-by entry that identifies only the section without the step = fail when the section contains multiple steps and the consuming step is identifiable. Step-level granularity in the ARCHITECTURE table closes the ambiguity gap that section-level entries create: a reader verifying C-26 prohibition compliance at a specific step can locate the consuming step from the ARCHITECTURE table without scanning the full section. *Source: carry-forward.* |
| C-38 | **ARCHITECTURE table covers all named outputs including STEP C drift binaries** | format | The ARCHITECTURE block includes a row for each STEP C per-dimension drift binary (C-29) as a named output produced by STEP C and consumed by STEP D -- not just the higher-level named outputs from STEP 1/2/3. An ARCHITECTURE table that declares the STEP D confirmed-readiness row (C-32) but omits the STEP C drift-binary rows that feed it = fail. Complete coverage makes the full PART 1 chain auditable from the table alone. *Source: carry-forward.* |
| C-39 | **STEP C per-dimension coaching notes reference the STEP 3 root-pattern label as a Required input** | depth | Each STEP C per-dimension coaching note opens with a Required input prohibition referencing the STEP 3 root-pattern label (C-24), establishing that the per-dimension contribution framing consumes the synthesis verdict rather than re-deriving the cross-dimension pattern from individual observations. A STEP C coaching note that characterizes the dimension's contribution to the root pattern without referencing the STEP 3 label as a named input = fail when a root pattern has been identified. *Source: carry-forward.* |
| C-40 | **Forward-declaration arrows in STEP C drift binaries identify STEP D as the consuming step** | depth | Each STEP C drift binary (e.g., `STEP C drift: CLOSED -> STEP D`) carries a forward-declaration arrow (C-30) naming STEP D as the consumer -- not a generic "downstream" or section reference. A STEP C drift binary without a forward-declaration arrow to STEP D = fail when C-30 is otherwise satisfied. Consistent arrow notation at STEP C drift binaries extends the three-point declaration chain from higher-level outputs down to the per-dimension coaching level. *Source: carry-forward.* |
| C-41 | **ARCHITECTURE meta-rule explicitly names independence-in-isolation as a positive standard for per-input form** | format | The ARCHITECTURE meta-rule (C-31) includes an explicit positive framing of the per-input prohibition standard -- naming "independence-in-isolation" (or equivalent: "each annotation self-standing") as the design property that per-input form satisfies, not merely the anti-pattern it avoids. A meta-rule that states only the grouped-form prohibition without naming the positive independence property = fail when C-36 compliance is required. Naming the positive standard makes per-input compliance auditable as a design goal rather than as an absence of the prohibited form: a reader can verify independence by checking whether each annotation is self-standing, not only by checking whether grouping is absent. *Source: carry-forward.* |
| C-42 | **ARCHITECTURE block includes a pipeline-completeness count commitment** | format | The ARCHITECTURE block includes an explicit count of named outputs declared (e.g., "12 named outputs declared") as a pre-execution commitment that the subsequent analysis must satisfy -- making under-declaration detectable by count without scanning the full output. An ARCHITECTURE block without a named-output count = fail when the block contains 5 or more named outputs. The count commitment converts the ARCHITECTURE block from a declaration of intent into a verifiable completeness contract: a reader can confirm block completeness by counting the table rows against the declared total without checking each output against the full analysis. *Source: carry-forward.* |
| C-43 | **Per-input compliance notes at each N-input convergence step** | depth | At every step where N >= 2 named inputs converge, the step carries an explicit compliance note (e.g., `[C-43 PASS: N independent per-input annotations present at this convergence step -- independence-in-isolation verified]`) immediately after the per-input prohibitions -- confirming that all N inputs are independently annotated and each annotation is self-standing. A convergence step without this compliance note = fail when N >= 2 named inputs are consumed at the same step. The compliance note converts C-36 and C-28 from retroactively-verifiable properties into locally-declared confirmations: a reader can verify multi-input convergence compliance from the note alone without counting annotations or checking cross-annotation dependencies. *Source: carry-forward.* |
| C-44 | **Forward-declaration arrow inline on terminal verdict line** | depth | The terminal named output at the final PART 1 step (e.g., STEP E confirmed-readiness verdict) carries its forward-declaration arrow (C-30) inline on the verdict line itself -- not as a separate annotation or a trailing sentence. A terminal verdict that emits a named output and carries the forward-declaration arrow in a separate line or sentence rather than inline on the verdict line = fail. Inline placement on the terminal verdict line makes the three-point chain complete at the point of maximum visibility: the verdict, its name, and its consumer appear as a single atomic unit, making the handoff to PART 2 locally verifiable without parsing surrounding context. *Source: carry-forward.* |
| C-45 | **Independence-in-isolation named as positive standard in ARCHITECTURE meta-rule** | format | The ARCHITECTURE meta-rule (C-41 extension) names "independence-in-isolation" explicitly as the positive standard -- not as a description of C-36's property but as the design principle that the per-input form (C-28) is intended to enforce. A meta-rule that satisfies C-41 (names the positive property) but uses a paraphrase without naming the principle by its canonical label = fail when C-45 compliance has been established as the canonical phrasing. Naming the principle by canonical label makes the ARCHITECTURE meta-rule self-consistent with C-36's pass condition and the C-43 compliance note, creating a shared vocabulary across the three per-input enforcement layers (meta-rule -> compliance note -> convergence step annotation). *Source: carry-forward.* |
| C-46 | **C-46 coverage certification emitted after STEP E** | format | The output closes with an explicit coverage certification (e.g., `C-46 coverage: complete -- N/N criteria verified across K convergence steps`) that confirms how many of the named-binary-chain criteria (C-23, C-24, C-28, C-29, C-30, C-36, C-43, C-44) were satisfied and at which steps they were verified. An output that omits this certification = fail. The coverage certification converts the named-binary chain from a distributed structural property (verifiable only by reading the full output) into a front-loadable summary: a reader can assess chain completeness from the certification line without tracing each criterion across every step. *Source: carry-forward.* |
| C-47 | **Three enforcement nodes declared as co-equal peers in ARCHITECTURE meta-rule** | format | The ARCHITECTURE meta-rule explicitly declares the three enforcement nodes (ARCHITECTURE block, production-site forward-declaration, consumption-site prohibition) as co-equal peers forming a complete local-verifiability contract -- not as a primary mechanism with two supporting annotations. A meta-rule that positions one node as primary and the others as reinforcing = fail; a meta-rule that names two nodes without the third = fail. Declaring three co-equal peers converts the enforcement chain from a layered hierarchy into a flat contract where any single handoff is locally verifiable from the triple of annotations at that point alone. *Source: carry-forward.* |
| C-48 | **Pipeline-completeness count commitment stated before analysis begins** | format | The ARCHITECTURE block (C-42 extension) states the pipeline-completeness count commitment in the block's opening -- before the three-column table begins -- so that the declared count is visible before any rows are read. A count commitment embedded within or after the table = fail when the count can be stated in the block header without displacing the meta-rule or table structure. Front-loading the count makes the completeness contract auditable at first read: a reader knows how many outputs to expect before scanning the table, enabling immediate detection of a count mismatch without reaching the end of the table. *Source: carry-forward.* |
| C-49 | **ARCHITECTURE meta-rule declares form-equivalence for compliance-note implementations, naming at least two confirmed-equivalent forms** | format | The ARCHITECTURE meta-rule's named-binary chain section explicitly states that compliance-note implementations are form-equivalent and names at least two confirmed-equivalent forms (e.g., Form A: bracket notation `[C-43 PASS: ...]`; Form B: isolation comments `/* C-43 PASS: ... */`). A meta-rule that specifies exactly one compliance-note form without acknowledging form equivalence = fail when two or more passing forms have been confirmed in variation testing. Form-equivalence declaration closes the false-failure gap that a single-canonical-form requirement would create: a model using an equivalent form satisfies C-43 without matching the original notation; the named examples make that equivalence auditable from the ARCHITECTURE block. *Source: carry-forward.* |
| C-50 | **ARCHITECTURE meta-rule carries explicit phrasing-agnosticism for C-47, naming at least two confirmed-equivalent phrasings** | format | The ARCHITECTURE meta-rule's unified-contract declaration (C-47) explicitly states that the three-node contract can be expressed using multiple equivalent phrasings and names at least two confirmed-equivalent examples (e.g., "co-equal self-declaration peers" from V-04 R6 and "complete local-verifiability contract" from V-05 R6). A meta-rule that declares the unified contract without acknowledging phrasing equivalence = fail when two or more passing phrasings have been confirmed in variation testing. Explicit phrasing-agnosticism with named equivalents closes the false-failure gap that a single-canonical-phrasing requirement would create: a model that uses a confirmed-equivalent phrasing satisfies C-47 without using the original wording; the named examples make that equivalence auditable from the ARCHITECTURE block without requiring variation-history access. *Source: V-04/V-05 R6 pattern -- two distinct phrasings both confirmed to satisfy C-47; phrasing-agnosticism declaration with named equivalents observed to prevent false failures on semantically equivalent but textually different implementations.* |
| C-51 | **Form-equivalence declaration (C-49) names confirmed-equivalent form classes by label (Form A / Form B)** | format | The form-equivalence declaration in the ARCHITECTURE meta-rule (C-49) names the confirmed-equivalent implementation forms as labeled classes (e.g., Form A: bracket notation; Form B: isolation comments) rather than describing them in prose or listing them as unlabeled examples. A form-equivalence declaration that acknowledges multiple forms without labeling them as distinct named classes = fail when two or more passing forms have been confirmed. Labeled form classes make the equivalence registry enumerable and auditable: a model can confirm which form class its compliance notes belong to by checking the ARCHITECTURE block; an unlabeled declaration requires prose interpretation to determine whether a novel implementation qualifies, reintroducing the false-failure gap that C-49 closes. *Source: V-04/V-05 R8 pattern -- Form A (bracket notation) and Form B (isolation comments) labeled as named classes in ARCHITECTURE meta-rule; labeling observed to make form-class membership determinable from the block alone without inspecting each compliance note individually.* |
| C-52 | **ARCHITECTURE meta-rule explicitly names the two-level equivalence-registry design principle -- one property-declaration criterion and one enumeration criterion per equivalence class** | format | The ARCHITECTURE meta-rule explicitly states the two-level principle as a binding architectural rule: each equivalence class governed by the meta-rule must carry both a property-declaration criterion (C-47, C-49) and an enumeration criterion (C-50, C-51), and names both active equivalence classes by their criterion pairs to ground the contract in current instances. A meta-rule that notes the C-47+C-50 / C-49+C-51 symmetry as a retrospective observation without converting it into a binding principle = fail; a meta-rule that names the principle prospectively without declaring an extensibility contract for future classes = fail; a meta-rule with an extensibility contract that names only one active equivalence class when two exist = fail. Making the two-level pattern an explicit architectural principle converts a retrospectively verifiable design property into a front-loaded extensibility contract: single-layer additions to future equivalence classes are locally visible violations rather than gaps detectable only by cross-referencing the full criterion set. *Source: R7 finding 5 -- C-50/C-47 and C-51/C-49 symmetry confirmed complete; C-52 pre-declares the two-level pattern as an extensibility contract, closing the drift gap that C-50 and C-51 individually closed.* |
| C-53 | **ARCHITECTURE meta-rule two-level principle is declared prospectively, not as a retrospective observation of current symmetry** | format | The ARCHITECTURE meta-rule states the two-level pattern as a constraint on what future equivalence classes must do -- not as a characterization of what current equivalence classes happen to share. A meta-rule that describes the existing C-47+C-50 / C-49+C-51 symmetry as an observed structural property ("both active equivalence classes follow the same two-level structure") without converting it into a binding forward-facing principle ("each equivalence class must carry both layers") = fail. Observation describes what is; a prospective principle constrains what must be -- C-53 isolates the prospective-framing requirement as an independently verifiable property of the C-52 meta-rule: a reader can confirm prospective framing by checking whether the declaration contains an obligation ("must," "is required to," "cannot be added without") rather than a characterization ("follows," "exhibits," "shares"). *Source: V-01 R8 fail mode -- C-52 candidate noted the symmetry retrospectively rather than declaring the pattern as a binding principle with a forward-facing obligation. Retrospective framing satisfies neither the principle nor the extensibility-contract requirements of C-52 because it does not constrain future additions; C-53 isolates this failure axis so that the prospective-vs-retrospective distinction is auditable independently of whether an extensibility contract is present.* |
| C-54 | **Prospective two-level principle declaration includes an explicit extensibility contract for future equivalence classes** | format | The prospective two-level principle declaration (C-53) includes a forward-facing rule stating that any equivalence class added to the ARCHITECTURE in the future must carry both a property-declaration criterion and an enumeration criterion, making single-layer additions a locally visible violation. A two-level principle declared prospectively (satisfying C-53) but without a forward rule for future classes = fail. A principle without an extensibility contract leaves future classes governed only by precedent -- the observer must check whether a new class matches the existing pair structure -- rather than by a pre-declared constraint where a single-layer class produces an immediately detectable violation. The extensibility contract converts precedent-based compliance into rule-based compliance: single-layer future additions violate a stated rule, not merely deviate from an inferred pattern. *Source: V-02 R8 fail mode -- principle named prospectively with both active equivalence classes visible, but no extensibility contract declared.* |
| C-55 | **Extensibility contract names all currently active equivalence classes by their criterion pairs** | format | The extensibility contract (C-54) names all equivalence classes currently active in the ARCHITECTURE by their criterion pairs (e.g., phrasing-equivalence: C-47+C-50; form-equivalence: C-49+C-51), grounding the contract in current instances before extending it to future ones. An extensibility contract that names only one active equivalence class when two or more are present = fail. Naming all current instances makes the contract's scope verifiable before future classes are added: a reader can confirm the contract covers the full current architecture from the ARCHITECTURE block alone; a gap in the current-instance enumeration leaves one active class ungoverned at the point of declaration. *Source: V-03 R8 fail mode -- extensibility contract present and principle prospective, but only phrasing-equivalence class named; form-equivalence class an unnamed gap.* |
| C-56 | **Extensibility rule embeds a violation condition within the rule body, making future violations locally self-detectable without consulting surrounding context** | depth | The extensibility rule (C-54) includes not only a positive requirement statement but also an explicit violation condition within the same rule body -- naming what would constitute a violation for a future class (e.g., "any future equivalence class missing one of the two layers is a locally visible violation"). An extensibility rule that states the positive requirement but omits an explicit violation condition = fail; a violation condition that appears in surrounding prose rather than within the rule body itself = fail when the rule body is extractable as a self-contained statement. Embedding the violation condition locally converts the extensibility rule from a specification into a self-contained enforcement point: a future evaluator can assess a new equivalence class against the rule without consulting the ARCHITECTURE block or the broader analytical context. *Source: R9 V-04/V-05 EX-01.* |
| C-57 | **Active equivalence classes in the extensibility contract are presented in labeled lookup-table form, enabling membership verification by label without re-applying the property test** | format | The extensibility contract body (C-55) enumerates the active equivalence classes as a labeled list or table -- each entry naming one class by criterion-ID pair as a discrete labeled item -- rather than embedding the class names inline within the contract's continuous prose. An extensibility contract that satisfies C-55 (both classes named by criterion-ID pairs) but presents those pairs woven into prose sentences = fail when the pairs are not independently scannable as separate labeled entries. Labeled enumeration converts criterion-ID pairs from inline anchors into reference entries: evaluators verify active-class membership by scanning the labeled list rather than parsing the prose argument that established each pairing. *Source: R9 V-04/V-05 EX-02.* |
| C-58 | **Three-point completeness declared by an explicit co-requirement statement naming the three structural requirements as jointly required, none substituting for the others** | format | The extensibility contract section (C-53+C-54+C-55 complex) includes an explicit co-requirement statement declaring that all three structural requirements -- prospective principle (C-53), extensibility rule (C-54), and both-classes enumeration (C-55) -- are jointly required and each independently checkable, with none substituting for the others. An extensibility contract section that satisfies all three criteria without an explicit co-requirement statement = fail when the three properties are not self-evidently co-required from reading the section. The co-requirement statement converts the three-component structure from an implicit property into a front-loaded declared contract: partial satisfaction (two of three) becomes a visible violation of the declared package rather than an undetectable gap. *Source: R9 V-04/V-05 EX-03.* |
| C-59 | **Extensibility contract's three structural requirements are declared phrasing-agnostic, naming at least two confirmed-equivalent phrasings** | behavior | The extensibility contract section (or its immediate context) explicitly states that phrasing variants of the three structural requirements (C-53, C-54, C-55) are accepted as equivalent and names at least two confirmed-equivalent phrasings (e.g., "property-declaration criterion + enumeration criterion" (V-04 R9) and "property criterion + registry criterion" (V-05 R9)). An extensibility contract that uses exactly one phrasing for the three requirements without an explicit phrasing-agnosticism declaration = fail when at least one alternative phrasing has been confirmed equivalent in variation testing. Extends the C-49/C-50 pattern into the C-52/C-53/C-54/C-55 complex. *Source: R9 property-level invariance confirmation.* |
| C-60 | **Extensibility rule body names the minimum syntactic boundary mechanism that preserves co-location, distinguishing it from period-separated alternatives** | depth | The violation condition embedded in the rule body (C-56) includes an explicit naming of the syntactic boundary mechanism that maintains the consequence clause within the same statement as the obligation -- identifying the minimum syntactic form (e.g., semicolon joining, compound statement structure) and naming the structurally-separating alternative (period separation creating a standalone sentence) as the fail form. A rule body that embeds a violation condition (C-56) without naming the syntactic mechanism and its separation-preventing function = fail; a rule body that describes the violation condition's position without specifying what syntactic constraint enforces co-location = fail. Naming the mechanism converts "embedded" from a positional property (checkable by visual proximity) into a syntactically-specified constraint (checkable by boundary analysis): future variants can comply by using any mechanism that satisfies the named syntactic form, and future evaluators can identify violations by checking whether the boundary mechanism is present rather than inferring from example. Closes the gap between C-56's requirement (violation condition within rule body) and its enforcement: without the mechanism named, a period-separated restatement of the violation condition could appear to satisfy C-56 by proximity while structurally breaking the self-containment property. *Source: R10 EX-01 -- semicolon-embedding observed as the minimum syntactic form that prevents structural separation; period separation with identical content fails C-56 because the consequence clause becomes extractable as a standalone sentence; C-60 makes the mechanism a declared requirement rather than an observable implementation detail, so future evaluators apply a structural test rather than a visual proximity check.* |
| C-61 | **Lookup-table form declaration explicitly states the form-over-content distinction, naming the scan-enabling form change as the gate independent of the content it carries** | format | The lookup-table criterion (C-57) or its immediate context includes an explicit form-over-content statement -- naming the labeled column-keyed table structure (not the specific criterion IDs or class names it contains) as the property that enables membership verification by scan, and identifying prose-embedded IDs as the form that requires parsing and therefore fails C-57. A lookup-table criterion that requires the form without a form-over-content statement = fail when a content-only reading of the criterion could lead an evaluator to credit content improvements (e.g., adding more criterion IDs or clearer class names in prose) as satisfying the scan-enablement requirement. The form-over-content statement closes a false-inference gap: without it, an evaluator might believe that refining prose content satisfies C-57; with it, the gate is explicitly structural -- only a form change that enables label-scan satisfies the criterion, regardless of how precise the prose content is. Content improvements and form improvements are independent dimensions; C-61 makes this independence a declared principle rather than an inferred property of the criterion. *Source: R10 EX-02 -- "the form change, not the content, is the gate" observed as the key property distinguishing C-57 compliance from content-level improvement; without the form-over-content statement, the structural gate is present but undeclared -- future evaluators who read C-57 as a content requirement rather than a form requirement will apply the wrong test; C-61 makes the correct test explicit at the criterion level.* |
| C-62 | **Co-requirement declaration includes an explicit partial-compliance detectability statement naming two-of-three satisfaction as identifiably incomplete, not sub-optimal** | behavior | The co-requirement declaration (C-58) includes not only the joint-requirement statement ("all three required; none substitutes for another") but also an explicit detectability statement naming partial compliance -- two-of-three elements passing -- as an identifiably incomplete state rather than a sub-optimal one, and naming the joint-package framing as the mechanism by which partial failures are made visible. A co-requirement declaration that satisfies C-58 without the detectability statement = fail when the joint-package framing is the mechanism by which partial failures become identifiable. The detectability statement converts the co-requirement declaration from a requirement description into a self-announcing enforcement property: the semantic distinction between "identifiably incomplete" and "sub-optimal" is what makes the violation unambiguous -- sub-optimal admits degrees, incomplete does not. Without C-62, C-58's co-requirement declaration is structurally present but silent on what partial compliance means; C-62 requires the declaration to make that meaning explicit rather than leaving it to inference. *Source: R10 EX-03 -- co-requirement as package declaration observed to make partial compliance detectable; the excellence pattern is that the declaration explicitly names detectability as a design property and names partial compliance as identifiably incomplete -- converting the framing from an incidental benefit of the joint-package structure into a declared function that future evaluators can apply directly.* |
| C-63 | **Phrasing-agnosticism chain closure includes an explicit registry self-completeness certification naming all governed equivalence classes by criterion-pair mapping** | format | The phrasing-agnosticism declaration (C-59) includes an explicit self-completeness certification stating that the equivalence registry governs all active equivalence classes -- naming at minimum the phrasing-equivalence class (C-47+C-50), the form-equivalence class (C-49+C-51), and the structural-requirements complex (C-53+C-54+C-55) -- and asserting that all three are governed by the same two-layer extensibility contract. A phrasing-agnosticism declaration that covers the C-53/C-54/C-55 complex with two named phrasings (satisfying C-59) without asserting registry completeness = fail when two or more prior equivalence classes also exist in the registry. The self-completeness certification closes the coverage-gap question for the full equivalence registry: a reader can verify from the certification alone that no equivalence class is ungoverned, without tracing through the full criterion set to check whether the two-layer contract extends to all classes. Without C-63, a compliant C-59 declaration confirms that the structural-requirements complex is phrasing-agnostic but leaves open whether the registry as a whole is complete -- a gap that is only detectable by enumerating all equivalence classes and checking each against the two-layer contract; C-63 requires that enumeration to appear in the declaration itself, making registry completeness self-certifying rather than observer-inferred. *Source: R10 pattern 5 -- "equivalence registry now covers both form-/phrasing-equivalence classes and structural-requirements complex -- all governed by the same two-layer extensibility contract" observed as an end-to-end self-describing property that V-05 achieves but does not explicitly declare; C-63 makes the self-completeness certification a required explicit declaration rather than an inferred property of the full criterion set.* |
