```json
{"top_score": 94, "all_essential_pass": true, "new_patterns": ["explicit category enumeration in field rules (one of: correctness/depth/format/coverage/behavior) converts C-07 from partial to pass — only pattern that closes the enumeration at prompt authoring time", "Addresses: F-NN column makes failure-first derivation (C-12) a column-presence check rather than a prose-quality judgment — empty cell is a structural failure detectable without reading content"]}
```
