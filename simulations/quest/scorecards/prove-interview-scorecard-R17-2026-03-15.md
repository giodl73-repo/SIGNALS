# /quest:score — prove-interview Round 17 (Rubric v15)

> **INPUT NOTE**: V-01 is complete. V-02 is truncated (Schema E example row missing; no phase structure visible). V-03–V-05 are absent from the input. Scoring proceeds on V-01 (full) and V-02 (visible schema registry only). V-03–V-05 cannot be scored.

---

## V-01 — Full Scoring

**Axis**: Skeptic-first role sequence with structural enforcement of objection lifecycle tracking.

### Essential (E/5 × 60)

| ID | Score | Evidence |
|----|-------|----------|
| C-01 | **PASS** | Phase 0A card schema mandates Role & title; gate checks population |
| C-02 | **PASS** | Card schema has Prior Knowledge + Blind Spots; gate requires population |
| C-03 | **PASS** | Gate items in Phases 1–3 require vocabulary signature terms to appear in transcript |
| C-04 | **PASS** | Phases 1–3 gates: evidence table using 0.2 schema required after each transcript |
| C-05 | **PASS** | Phase 0C declares SURPRISING/CONFIRMING/INCONCLUSIVE; Phase 1 gate: ≥1 SURPRISING or INCONCLUSIVE |

**E = 5/5 → 60 pts**

---

### Recommended (R/3 × 30)

| ID | Score | Evidence |
|----|-------|----------|
| C-06 | **PASS** | Gate items mandate `[triggered by: "exact phrase"]` follow-up in every interview phase |
| C-07 | **PASS** | Three distinct subjects required: Skeptic, Neutral/Challenger, Advocate |
| C-08 | **PASS** | Schema 0.2 has explicit Signal Type column |

**R = 3/3 → 30 pts**

---

### Aspirational (A/38 × 10)

| ID | Score | Evidence |
|----|-------|----------|
| C-09 | **PASS** | Phase 4 mandates INTERVIEW SYNTHESIS GRID with named schema |
| C-10 | **PASS** | Phase 5 requires fidelity note distinguishing grounded vs. constructed, with named basis |
| C-11 | **PASS** | Phase 5 requires voice divergence acknowledgment with concrete vocabulary contrast |
| C-12 | **PASS** | Every interview gate: `[triggered by: "exact phrase"]` citation required |
| C-13 | **PASS** | Phase 0B requires register table before any transcript; gate enforces it |
| C-14 | **PASS** | Phase 0C declares INCONCLUSIVE with decision rule; Phase 1 gate explicitly requires ≥1 SURPRISING or INCONCLUSIVE |
| C-15 | **PASS** | Each phase scoped to single named concern; purposes unambiguous |
| C-16 | **PASS** | `[triggered by: "exact phrase"]` and 0.3 SURPRISING/CONFIRMING templates embedded in gate items |
| C-17 | **PASS** | Schema 0.2 column: `Strength (rationale)` — rating + sentence combined |
| C-18 | **PASS** | Schema 0.2 column: `Origin-of-Claim [verbatim / inferred / corroborated]` |
| C-19 | **PASS** | Card schema requires Sequence Position & Rationale; SEQUENCE RULE states order with explicit rationale |
| C-20 | **PASS** | Every phase closes with a named gate block |
| C-21 | **PASS** | Phase 4 gate: Objection Lifecycle Trace required, must name persist/transform/resolve and cite the specific Phase 2/3 moment |
| C-22 | **PASS** | Card schema: exactly 3 vocabulary terms per subject; gate: "terms named (not described)" |
| C-23 | **PASS** | Card schema has Evidential Function field; gate: "structural contribution, not role restatement" |
| C-24 | **PASS** | Phase 4 gate: "At least one evidence item's weight explicitly adjusted for experience-proximity difference" |
| C-25 | **PASS** | 0.3 format: `SURPRISING (expected: [Expectation Register → field], got: [emergence])` — register named explicitly |
| C-26 | **PASS** | Gates 1–3: "At least one vocabulary signature term appears in transcript (→ 0.4)" |
| C-27 | **PASS** | Schema 0.2 has separate Origin-of-Claim and Experience-Proximity columns; not merged |
| C-28 | **PASS** | Phase 0C declares all three labels with explicit decision rules before any transcript |
| C-29 | **PARTIAL** | Schemas 0.2 and 0.4 declared as column/field lists with no populated example rows; 0.3 format has an inline template but no instantiated evidence-table row |
| C-30 | **PASS** | PRE-INTERVIEW MASTER GATE confirms Phase 0A, 0B, 0C complete before Phase 1 begins |
| C-31 | **PASS** | PRE-INTERVIEW MASTER GATE names Phase 0A, 0B, 0C, and vocabulary signatures explicitly |
| C-32 | **PASS** | Named gate blocks throughout: `PHASE N GATE — PROHIBITED: [...] — REQUIRED: [ ] item` |
| C-33 | **PASS** | DOCUMENT CONTRACT at document head assigns numbered handles 0.1–0.4; gate items cite them throughout |
| C-34 | **PASS** | Master gate references "Phase 0A: COMPLETE", "Phase 0B: COMPLETE", "Phase 0C: COMPLETE" — named sub-gate cross-references, not reassertions |
| C-35 | **FAIL** | Point-of-use checks in Phase 1–3 gates cite schema handles (→ 0.2, → 0.4) but never cite PRE-INTERVIEW MASTER GATE as authorization authority |
| C-36 | **PASS** | Schema 0.2 includes Framework Dimension column |
| C-37 | **N/A** | Only one named structural source for moment labels (Expectation Register) — no second source declared |
| C-38 | **PASS** | Phase 4 INTERVIEW SYNTHESIS GRID required with four named schema columns |
| C-39 | **PARTIAL** | PROHIBITED clauses in Phase 0A, 0B, 0C, 1, 2 gates; absent from Phase 3 and Phase 4 gates |
| C-40 | **PARTIAL** | Interview phases 1–3 each cover transcript + extraction + labeling (multi-concern) without numbered sub-phase decomposition |
| C-41 | **PARTIAL** | Experience-Proximity vocabulary [GROUNDED/CONDITIONAL/INFERRED] declared at document head in 0.2, not in the exit gate of the introducing phase |
| C-42 | **PARTIAL** | Many gate items cite → 0.N handles correctly; several do not (Phase 0A: "Evidential function stated as structural contribution", "Sequence rationale named for each position"; Phase 4 gate: no handles) |
| C-43 | **PASS** | All three setup phases have artifact-declaring titles co-referential with master gate cross-references: "PHASE 0A — SUBJECT CARD SET", "PHASE 0B — EXPECTATION REGISTER", "PHASE 0C — MOMENT-LABEL DECISION TAXONOMY" |

**A tally:**
- PASS: C-09–C-28, C-30–C-36, C-38, C-43 = **28**
- PARTIAL × 0.5: C-29, C-39, C-40, C-41, C-42 = **2.5**
- FAIL: C-35 = **0**
- N/A: C-37 (excluded from numerator; denominator stays 38)

**A = 30.5 / 38 → 8.03 pts**

---

### V-01 Composite

| Component | Raw | Points |
|-----------|-----|--------|
| Essential (E/5 × 60) | 5/5 | 60.00 |
| Recommended (R/3 × 30) | 3/3 | 30.00 |
| Aspirational (A/38 × 10) | 30.5/38 | 8.03 |
| **Total** | | **98.03** |

---

## V-02 — Partial Scoring (Schema Registry Only)

V-02 visible content: FORMAT CONTRACT — SCHEMA REGISTRY (Schemas A–D complete with example rows; Schema E column headers only, example row = placeholder; no phase structure, no gates, no master gate).

**Scoreable from visible content:**

| ID | Score | Evidence |
|----|-------|----------|
| C-01 | **PASS** | Schema A has Role & Title column |
| C-02 | **PASS** | Schema A has Prior Knowledge + Blind Spots columns |
| C-08 | **PASS** | Schema C has Signal Type column |
| C-16 | **PASS** | Schema D embeds example instantiation: `LABEL-01: SURPRISING (expected: E-01 → ...)` |
| C-17 | **PASS** | Schema C has Strength + Strength Rationale columns |
| C-18 | **PASS** | Schema C has Origin-of-Claim column |
| C-22 | **PASS** | Schema A has Vocabulary Signature [Term 1, Term 2, Term 3] column |
| C-23 | **PASS** | Schema A has Evidential Function column |
| C-27 | **PASS** | Schema C keeps Origin-of-Claim and Experience-Proximity as separate columns |
| C-28 | **PASS** | Schema D declares all three labels with decision rules |
| C-29 | **PARTIAL** | Schemas A–D have instantiated example rows; Schema E example row = blank placeholder |
| C-33 | **PASS** | All schemas declared at document head before any content |

**Not scoreable** (phase structure absent): C-03, C-04, C-05, C-06, C-07, C-09–C-15, C-19–C-26, C-30–C-32, C-34–C-43.

> V-02 cannot produce a composite score. If the full prompt is structured similarly to V-01 with a phase + gate architecture, the schema-registry-first approach would likely equal or slightly exceed V-01 on C-29 (example rows for all schemas, if Schema E placeholder is filled). The key innovation is the named Schema IDs (A–E) vs. V-01's numeric handles (0.1–0.4), which may trade off against C-42 compatibility depending on how gate items cite these.

---

## Rankings

| Rank | Variation | Score | Notes |
|------|-----------|-------|-------|
| 1 | **V-01** | **98.03** | Complete — highest scoreable variation |
| — | V-02 | n/a | Truncated; strong schema section visible |
| — | V-03–V-05 | n/a | Not provided |

---

## Excellence Signals (V-01)

1. **Numbered sub-schema handles with inline gate citations** — document head assigns 0.1–0.4; every gate item with a format requirement cites its handle. Converts schema declarations into a citation-resolution system rather than a reference legend.

2. **Phase titles as artifact co-references** — Phase 0A/0B/0C titles encode the artifact name produced, making master gate cross-references semantically unambiguous without body inspection.

3. **Objection lifecycle trace as mandatory synthesis artifact** — Phase 4 gate requires explicit persist/transform/resolve verdict with citation of the specific Phase 2/3 moment that determined the outcome. Closes the skeptic arc structurally rather than leaving it as an authorial judgment.

4. **SEQUENCE RULE as enforced structural contract** — the skeptic-first ordering is declared with explicit rationale ("objections must surface before advocates can frame the conversation"), making sequence a governed rule rather than a suggestion.

---

```json
{"top_score": 98.03, "all_essential_pass": true, "new_patterns": ["phase-title artifact co-reference makes master gate evidenced closure semantically resoluble without body inspection (C-43)", "sub-schema handle system upgrades document-head contract from declaration-with-propagation to citation-resolution per gate item (C-42 partial)"]}
```
