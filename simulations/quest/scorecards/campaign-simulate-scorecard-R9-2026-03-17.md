sub-observables, and each maps to exactly one Compliance Checklist row with three sub-claims. The declaration is no longer boilerplate — it is a contract that predicts every structural section in the report, and the checklist verifies that contract was honored.

**Signal 3 — Full four-section empty-state guard (C-34) eliminates structural ambiguity.** V-01 through V-04 guard only the sections they happen to have. V-05 explicitly names all four section types in a dedicated axis row and verifies all four in the checklist. "Section absent" and "section executed with zero results" are now structurally distinguishable for every schema-bearing section.

**Signal 4 — Classification-to-verb constraint fuses C-31 and C-26 into a self-auditing remediation chain.** The GAP/CONTRADICTION/ASSUMPTION classification constrains the allowed remediation verb; the Remediation Quality Gate table makes the constraint checkable by column scan. An out-of-vocabulary verb signals mis-classification. This is the first variation where remediation is both structured (C-26) and typed (C-31) simultaneously.

---

```json
{"top_score": 96, "all_essential_pass": true, "new_patterns": ["trace-first execution order (trace-state > trace-contract > trace-permissions > flow-lifecycle > flow-conversation) pre-loads the DR-NN dependency map from the platform layer before domain-layer ambiguity enters", "nine-row self-extending Structural Axis Declaration creates one-to-one correspondence between targeted quality criteria and declared axes, making the declaration a verifiable contract rather than a depth-capped template"]}
```
