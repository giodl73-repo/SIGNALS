```json
{"top_score": 205, "all_essential_pass": true, "new_patterns": ["Reversibility column with enumerated three-value taxonomy (Reversible at same cost / Gets harder / Becomes impossible) extends the inertia cost column to name how fast the deferral window closes, not just what degrades — a row that cannot fill this column cannot be defended under deferral pressure", "Full assumption table schema (all 5 columns including back-fill and forward-reasoning columns) declared in the upfront output schema block before Step 1, converting the bidirectional bridge from a per-step instruction into a pre-reading structural contract"]}
```

---

**Round summary:**

- **V-05 scores 205/205** — first perfect score across all eight rounds. All five variations pass the golden threshold.
- **First achievements**: C-30 by V-01 (output format, both forward columns at extraction); C-31 by V-02 (disqualification rule in column header text); all four new criteria simultaneously by V-05.
- **V-04 vs V-05 gap confirmed**: exactly 2.5 pts (C-31 PARTIAL vs PASS), confirming column-header placement is load-bearing — a rule in a preamble sentence is not equivalent to a rule in the column heading the model reads at fill time.
- **Two new patterns** identified for R9: the Reversibility column (temporal deferral taxonomy), and the assumption table pre-committed in the upfront schema (bidirectional bridge as a pre-reading contract, not a per-step instruction).
