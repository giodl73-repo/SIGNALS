The JSON was cut. Complete block:

```json
{"top_score": 105, "all_essential_pass": true, "new_patterns": ["explicit re-read FORMAT CONTRACT schema N in HALT header at every governed PRODUCE step — recall action is part of the gate itself, not a pointer after the gate passes", "ROLE-NAME LOCK block embeds per-cell directive notes inline at sites 2 and 5 — lock declaration becomes co-located per-site instruction document rather than name list only"]}
```
