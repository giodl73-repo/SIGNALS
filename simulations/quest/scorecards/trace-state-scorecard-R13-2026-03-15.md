-05: if states and operations are not recognizable in the CS domain, C-05 fails]`. Prior rounds placed C-35 only at sub-section level (invariant registers, defect logs). Lifting it to the domain-pass heading makes domain plausibility enforcement visible before any column in the entire pass is read — the highest structural navigation node achievable without moving to the artifact-level hard rules.

---

### New Patterns

Two patterns from V-04 not demonstrated in prior rounds:

**Pattern 1 — C-34 on state-representation field labels**: Disqualification conditions applied to `C-01b` (description-not-state-name) and `C-01c` (noun-phrase-not-verb-phrase) extend the C-34 vocabulary from quantitative floors and identifier patterns to structural output format. Any criterion that has a named correct format has a corresponding disqualifying format — V-04 makes this explicit.

**Pattern 2 — C-35 at domain-pass heading tier (C-05 linkage)**: Placing a `[C-05: consequence]` annotation on the `### Pass N` heading itself means enforcement is encountered at the domain entry point — before section headings, before column headers, before any cell. This is a new structural tier for C-35 that prior rounds did not reach.

---

```json
{"top_score": 100, "all_essential_pass": true, "new_patterns": ["C-34 on state-representation field labels: disqualifying patterns for C-01b (description-not-state-name) and C-01c (noun-phrase-not-verb-phrase) extend C-34 vocabulary from value-content fields to structural output-format fields", "C-35 at domain-pass heading tier: [C-05: consequence] on the Pass-N heading makes domain plausibility enforcement visible at the highest structural navigation node, before any section or column in the pass is read"]}
```
