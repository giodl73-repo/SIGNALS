Here are the 5 complete R7 variations, written to `simulations/quest/golden/org-roles-variate-R7-2026-03-16.md`:

---

## Round 7 Variation Map

| V | Axis | Primary change | Hypothesis |
|---|------|---------------|------------|
| **V-01** | role-sequence | Inertia-advocate derived FIRST from context (not cold), domain experts explicitly respond to the inertia claim | Context-grounded inertia becomes a structural constraint; each domain expert must name the inertia gap they address |
| **V-02** | output-format | Per-role **Diagnosis Card** intermediate step before any YAML is written; cross-card uniqueness scan required | Forcing an explicit uniqueness argument ("no other role would prioritize this question first — closest overlap is X, which would ask Y") prevents duplicate verify_questions structurally |
| **V-03** | phrasing-register | **Failure-mode-first**: every phase opens with its anti-patterns before giving instructions | Model encounters prohibitions as the opening frame of each phase; C-31 sites appear as failure class names rather than inline parentheticals |
| **V-04** | role-sequence + output-format | V-01 + V-02: context-first inertia derivation AND Diagnosis Cards; inertia-advocate card written first, visible to all subsequent card authors | Domain expert uniqueness arguments are written while the inertia frame is on-screen, producing verify_questions more directly adversarial to the inertia position |
| **V-05** | all axes | R6 V-01 cold-hypothesis base + V-02 Diagnosis Cards + V-03 failure-mode-first + new **explicit C-31 site labels** (`C-31-SITE-1`, `C-31-SITE-2`, `C-31-SITE-3`) | Self-documenting three-site structure — the model sees the count (three) at each checkpoint, eliminating ambiguity about whether C-31 is satisfied |

**All five variations embed the three v7 mechanisms:**
- **C-31**: `[FC-1]` PROHIBITED NAMES → Phase 2 DO NOT instruction → Phase 5 GATE item (three independent checkpoints)
- **C-32**: Phase 6 PREPARE names "count-bypass failure" as the explicit anti-pattern class
- **C-33**: Phase 5 YAML template annotation mirrors `CONTRACT VIOLATION (type 1) — PHANTOM` and `CONTRACT VIOLATION (type 2) — UNIVERSALIST` verbatim from `[FC-10]`
