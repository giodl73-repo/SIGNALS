Written to `simulations/quest/golden/org-roles-variate-R11-2026-03-16.md`. Here's the R11 variation map:

---

## Round 11 — Variation Map

| V | Axis | New pattern | Hypothesis |
|---|------|-------------|------------|
| **V-01** | lifecycle emphasis | **SCAN ORDERING GATE** — 6-item explicit gate after Step 7 asserting each phase ran in canonical sequence, no merging, no reordering | C-48 is currently satisfied by structural position; this variate turns ordering into a formally *declared* and *verified* constraint — potential C-50 |
| **V-02** | output format | **Criterion ID propagation** — `[C-NN]` annotations on every step-end gate/criterion-check block throughout the pipeline, not just the per-file checklist | C-49 covers per-file checklist; propagating IDs to all gate checkpoints creates a fully cross-referenced pipeline — potential C-50 |
| **V-03** | inertia framing | **PROVENANCE-CHAIN DECLARATION** — named artifact in Step 6 PREPARE declaring all three artifact chains (A: inertia gap, B: orthogonality, C: status-quo→registry) with source/transit/destination/integrity rule before any Diagnosis Card is written | The three chains exist structurally but are never declared as a formal artifact; making them explicit and gated creates a new self-auditing mechanism — potential C-50 |
| **V-04** | V-01 + V-02 | SCAN ORDERING GATE + criterion-ID propagation to all gates (combined synthesis) | Full V-01+V-02 combination: canonical ordering asserted as gate condition AND full pipeline criterion cross-referencing at every checkpoint |
| **V-05** | V-01+V-02+V-03 + register | All three new patterns + **formal Phase-based register** (Phase 0–6 with GATE blocks, vs imperative Steps) | Tests whether all three new mechanisms survive the register change that caused R10 V-03/V-04 to score 99.76 (missing C-49); V-05 restores formal register WITH all mechanisms intact |

**Key structural decisions:**
- All 5 inherit R10 V-05 as base (100.00 under v11) — every variation must hold all 41 criteria
- Single-axis variations pick up one new mechanism each, leaving all other R10 V-05 structure intact
- V-05 is the stress test: formal register + all three mechanisms — falsifiable if register re-introduces C-29/C-33 regressions despite criterion-annotated per-file checklist
