# org:committee Variations — Round 5

Generated at `simulations/quest/golden/org-committee-variate-R5-2026-03-16.md`.

---

## What R5 targets

R4 added **C-16** (GATE field) and **C-17** (CITE: subfield). Both became structural through the labeled-declaration pattern. R5 applies the same pattern to the two new criteria:

- **C-18** — all-approve self-correction diagnostic: If the tier sort produces an all-APPROVE lineup, detect it, name the mis-sorted role, correct before proceeding. R4 had this only as a prose note. R5 elevates it to a named checkpoint (`SORT-CHECK:`) in every variation.
- **C-19** — Tier 3 addresses a named Tier 1/2 concern: Every advocate voice must explicitly reference and respond to a specific peer concern. R4 had this only as a voice instruction. R5 makes it a structural subfield (`RESPONDS-TO:`) parallel to `CITE:`.

---

## Variation summary

| # | Axis | C-18 mechanism | C-19 mechanism |
|---|------|----------------|----------------|
| **V-01** | Lifecycle emphasis | Named Step 4b + `SORT-CHECK:` output line | `RESPONDS-TO:` subfield in each Tier 3 voice block |
| **V-02** | Output format (template) | `SORT-CHECK:` template field between sort + voices | `RESPONDS-TO:` template subfield in Tier 3 block |
| **V-03** | Phrasing register (minimal) | Inline diagnostic command after sort instruction | Dual-requirement appended to Tier 3 voice line |
| **V-04** | Inertia framing | `REMAINING-SORT-CHECK:` on non-inertia participants | INERTIA-FINDING tags serve as the named Tier 1 anchors |
| **V-05** | Full combination | Named Step 4b with `SORT-CHECK:` required line | `RESPONDS-TO:` structural subfield + explicit fail rule |

**Key distinction V-04 tests**: whether C-19 can be satisfied architecturally through the inertia-finding tag mechanism (naming `INERTIA-FINDING-A/B` = naming the Tier 1 concern) rather than a separate `RESPONDS-TO:` subfield — and whether that produces richer passes because advocates face the strongest adversarial case rather than picking the most convenient Tier 2 condition.

**V-05 ceiling attempt**: Five labeled declarations the model cannot elide — `GATE` (C-16), `SORT-CHECK` (C-18), `STANCE:` (C-13/C-15), `CITE:` (C-17), `RESPONDS-TO:` (C-19) — targeting all 11 aspirational criteria for the 112-point composite max.
