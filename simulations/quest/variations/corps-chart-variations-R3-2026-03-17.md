Here are the five R3 variations, written to the golden file:

---

## Variation Summary

| ID | Axis | Core mechanism |
|----|------|----------------|
| **V-01** | C-13 placement: lock-as-active-contract | ROLE-NAME LOCK introduced as a named contract with explicit downstream invoke instructions ("ROLE-LOCK-CHECK" at each section heading). Per-section checks re-activate the constraint locally rather than relying on global prose. |
| **V-02** | C-14 form: header-driven four-field | The Rebuttal four fields are `#### section headers`, not labeled bullets. Headers are generated before content, so ROLE UNDER PRESSURE is committed before OBSERVABLE BREAKDOWN can be written. Eliminates the retrofit-role failure mode. |
| **V-03** | Lifecycle emphasis: per-section ROLE-LOCK-CHECK | Every post-classification section opens with a ROLE-LOCK-CHECK block naming which specific columns/fields are subject to the lock. Phase 2 through Phase 6 each have their own check. ROLE-NAME LOCK is minimal (just a list); enforcement is distributed. |
| **V-04** | Combined: C-13 contract + C-14 form (paired) | Opens with an explicit "TWO-PART ROLE CONTRACT" frame that presents C-13 and C-14 as a coherent unit. Lock names the permitted set; form forces selection before prose. The VERDICT re-assessment trigger quotes the form field rather than re-writing it. |
| **V-05** | Full integration: format schemas + C-13 + C-14 + per-section checks | Pre-output FORMAT CONTRACT declares all 6 table schemas + REBUTTAL FORM CONTRACT. ROLE-NAME LOCK includes a downstream-section checklist. Per-section ROLE-LOCK-CHECKs at every section boundary. Tests whether mechanical layering over-constrains or maximizes compliance. |

**What changed from R1** — R1 variations explored phrasing register, inertia framing, and role cross-reference (pre-dating C-13/C-14 entirely). R3 treats C-13 and C-14 as the primary variation axes, with each variation exploring a different enforcement strategy: passive list vs. active contract (V-01), header-driven form (V-02), distributed per-section checks (V-03), paired-mechanics framing (V-04), and full mechanical integration (V-05).
