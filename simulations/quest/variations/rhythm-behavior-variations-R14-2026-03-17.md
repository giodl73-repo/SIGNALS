Written to `simulations/quest/variations/rhythm-behavior-variations-R14-2026-03-17.md`.

---

## R14 Summary

**Strategy**: C-39 extension into higher N in descriptive register + C-37 structural maximum extension to the Synthesis body (N=6 total). All 5 predicted 33/33.

| V | Axis | Key move |
|---|------|----------|
| V-01 | Lifecycle emphasis | Baseline reconfirmation at new denominator 33/33 |
| V-02 | Register + lifecycle | Descriptive + N=4 quad bodies (positions 1,2,3,4) -- C-39 extension |
| V-03 | Register + lifecycle | Descriptive + N=5 all sub-skill bodies -- direct descriptive counterpart of R13 V-05 |
| V-04 | Lifecycle emphasis | Imperative + N=6 (sub-skill bodies 1-5 + Synthesis body) -- C-37 beyond sub-skill sequence |
| V-05 | Register + lifecycle | Descriptive + N=6 all six named positions -- absolute structural maximum |

**Key design decisions:**

- **V-02/V-03** close the gap C-39 opened: R13 confirmed descriptive + N=3; N=4 and N=5 in descriptive are untested. C-39's register-invariance predicts both pass; these are the sequential one-step and full-bound confirmations.
- **V-04/V-05** probe C-40's scope boundary. C-40 says "N=5, the structural maximum (all five sub-skill bodies)" -- but C-37 says "bounded only by available structural positions." The Synthesis body is a confirmed qualifying position (C-32) that C-40 excludes from its count. V-04 and V-05 test whether N=6 (sub-skills + Synthesis) is absorbed by C-37 without penalty, in imperative then descriptive.
- **No genuinely open uncertainty**: all four test moves are predicted YES by independent confirmed criteria. The R14 campaign is a systematic closure of the compositional space rather than an exploration of unknown territory.

  + N=5 (all five sub-skill bodies); V-03 = descriptive + N=5 (same five positions). C-40
  confirmed N=5 in imperative as the structural sub-skill maximum; V-03 tests the same
  structural maximum in descriptive register. C-39 (register-invariance of no-N-ceiling rule)
  predicts the same inertness. Never tested: descriptive + N=5.

- **V-04** probes the boundary of C-40's scope. C-40 states "N=5, the structural maximum of
  the rhythm-behavior prompt (all five sub-skill section bodies)." But C-37 says "bounded only
  by the number of available structural positions in the prompt." The Synthesis section body is
  a confirmed qualifying position (C-32, R9 V-04) that is not a sub-skill body. V-04 places
  the rule in all five sub-skill bodies PLUS the Synthesis body: N=6 total. The first qualifying
  occurrence (position 1, flow-lifecycle) satisfies C-22; all five subsequent occurrences are
  inert. C-37's no-ceiling principle, bounded by available structural positions, covers N=6
  without modification. Never tested: any variation placing the rule in the Synthesis body
  while also saturating all five sub-skill bodies.

- **V-05** combines V-03 (descriptive + N=5) and V-04 (imperative + N=6) into the absolute
  structural maximum: descriptive register + all six named positions. C-38 confirmed
  descriptive + dual body inertness; C-39 confirmed descriptive + no-N-ceiling. V-05 applies
  both to the largest N achievable in the prompt. Never tested: descriptive register at N=6.

**Open questions for R14:**

| Question | V | Hypothesis |
|----------|---|-----------|
| Does C-39's register-invariance extend to N=4 in descriptive? | V-02 | YES -- C-39 states no-N-ceiling is register-invariant; N=4 follows directly |
| Does C-39 confirm at N=5 (all sub-skill bodies) in descriptive? | V-03 | YES -- C-39 covers any N; N=5 descriptive is the direct counterpart of R13 V-05 |
| Does C-37's no-N-ceiling extend to the Synthesis body (N=6 total)? | V-04 | YES -- Synthesis body is a confirmed qualifying position; C-37 is bounded by available positions, not a sub-skill count |
| Does descriptive register + N=6 (absolute structural max) compose without interaction? | V-05 | YES -- C-38+C-39 confirm register invariance for any N; C-37 covers Synthesis body |

**Predicted scores under v14 rubric** (aspirational denominator = 33):

| Variation | C-38 | C-39 | C-40 | Composite | Golden |
|-----------|------|------|------|-----------|--------|
| V-01 | PASS (no section body; trivial) | PASS (no section body; trivial) | PASS (single placement; trivial) | **100** | YES |
| V-02 | PASS (descriptive + dual+ inert -- C-38 scope) | PASS (N=4 descriptive -- C-39 register-invariant no-ceiling) | PASS (N=4 follows from C-40 no-ceiling framing) | **100** | YES |
| V-03 | PASS (descriptive + dual+ inert) | PASS (N=5 descriptive -- C-39 at structural sub-skill max) | PASS (N=5 descriptive counterpart of R13 V-05) | **100** | YES |
| V-04 | PASS (imperative; trivial) | PASS (imperative; trivial) | PASS (N=5 sub-skill bodies + Synthesis; C-40 no-ceiling framing applies) | **100** | YES |
| V-05 | PASS (descriptive + dual+ inert) | PASS (N=5+ descriptive -- C-39 register invariant) | PASS (N=6 descriptive; C-40 no-ceiling framing applies) | **100** | YES |

---

## V-01 -- Production Baseline at 33/33 (Lifecycle Emphasis, Reconfirmation)

**Axis**: Lifecycle emphasis -- identical to R13 V-01 (= R12 V-01 = R11 V-01 = R10 V-01 =
R9 V-05): minimal form, labeled paragraph `**Axis-Header Rule**: Each gate header names its
axis.` in the confirmed inter-section peer slot between Synthesis and Consolidated Findings,
flanked by `---` dividers. Slot-name definition lines absent. Inline example retained. No
other changes from R13 V-01.

**Hypothesis**: R13 V-01 scored 30/30 under v13. Under v14 the three new criteria encode
R13 findings directly:
- C-38 passes: no named section body is involved; the rule is in an inter-section peer slot.
  C-38 fires only on dual section-body descriptive compositions -- trivially inapplicable.
- C-39 passes: no named section body involved and only one placement exists. C-39 fires only
  when register-invariance of no-N-ceiling is tested -- trivially inapplicable.
- C-40 passes: single placement; C-40 fires on N>=4 confirmations -- trivially inapplicable.
Expected: 33/33 = 100, golden.

**Novelty verification**: Exact re-run of R13 V-01. Purpose is denominator transition
verification (30->33) under the new rubric criteria. Not structurally new; new only as
reconfirmation at v14.

```
You are running `rhythm-behavior` for topic: {{topic}}.

Output: a single continuous simulation findings report saved as
`simulations/campaign/{{topic}}-simulate-{{date}}.md`.

Write everything as one document from start to finish. Do not promise to continue.

Sections in this exact order: flow-lifecycle, flow-conversation, trace-state,
trace-contract, trace-permissions, SYNTHESIS, Consolidated Findings.

---

### What to look for

**spec-gap** -- a requirement that is missing, ambiguous, or underspecified; name the
spec section that is silent or unclear.

**contract-violation** -- divergent assumptions between caller and callee at a named
boundary.

**Blast radius**:
- WIDE -- corrupts shared state or breaks multiple downstream callers
- MEDIUM -- two or more components affected, shared state not reached
- NARROW -- failure stays inside one component

**Blast Radius Rationale** -- 3-slot format required for every finding:
  "[LEVEL] because [boundary] propagates to [caller/component], [effect]."
  Example: "WIDE because session-sequence-number contract propagates to flow-conversation
  routing and trace-contract pre-check, producing stale-state errors at both."

Open this table before flow-lifecycle. Append rows as findings are discovered.
Blast Radius Rationale is required for every row.

| F-ID | Sub-Skill | Location | Finding Type | Blast Radius | Blast Radius Rationale | Impact | Remediation |
|------|-----------|----------|--------------|--------------|------------------------|--------|-------------|

---

### flow-lifecycle

Walk `{{topic}}` through four phases: INIT (preconditions and partial-init failures),
NOMINAL (component handoffs and silent failures), DEGRADED (error states and recovery),
TEARDOWN (cleanup and handoff completeness).

Add each finding immediately (Sub-Skill = flow-lifecycle, classify, assign blast radius).
If a phase has no findings, say so briefly.

---

### flow-conversation

Walk the conversation and intent flow for `{{topic}}`: intent routing paths, response
contracts, graph transitions, fallback branches, session state tracking, timeout handling.
Note anything the spec leaves unclear or where caller and callee would disagree.

Add each finding immediately (Sub-Skill = flow-conversation, classify, assign blast radius).
If none: say so explicitly.

---

### trace-state

Build the complete state model for `{{topic}}`: spec-defined states, valid transitions,
reachability from initial state, unauthorized crossings (contract-violation by default).

Add each finding immediately (Sub-Skill = trace-state, classify, assign blast radius).
If none: say so explicitly.

---

### trace-contract

Check every API and service boundary: pre/postcondition symmetry, data invariants,
migration contracts. Note anywhere callee behavior differs from what callers expect.

Add each finding immediately (Sub-Skill = trace-contract, classify, assign blast radius).
If none: say so explicitly.

---

### trace-permissions

Trace the permission model: role authorization rules, field-level security, team membership
effects, sharing rules, escalation paths. Note anything the spec omits or where access
control assumptions diverge.

Add each finding immediately (Sub-Skill = trace-permissions, classify, assign blast radius).
If none: say so explicitly.

---

### Synthesis

Review all findings together. For patterns spanning two or more sub-skills:
- Name contributing sub-skills (at least two) and each contributor's blast radius
- Describe the compounding effect
- CROSS-SKILL blast radius >= max(contributing sub-skills); no downgrade permitted
- Rationale: 3-slot format + provenance: "Inherited [LEVEL] from [sub-skill-X] ([F-ID])"
- Label: "CROSS-SKILL: [sub-skill-A + sub-skill-B]"
- Add to findings table with Sub-Skill = CROSS-SKILL

If none: "Synthesis: no cross-sub-skill patterns found."

---

**Axis-Header Rule**: Each gate header names its axis.

---

### Consolidated Findings

Title: Simulation Campaign Report -- {{topic}} -- {{date}}

Sort the findings table by blast radius (WIDE first, NARROW last).
Label: "Sorted by Blast Radius: WIDE -> MEDIUM -> NARROW"

Top finding: one concrete next step naming the exact spec section, boundary, or component
a developer must address before writing implementation code.

Three gates before closing. One axis per gate. Do not merge.

**Coverage Axis Gate**: spec-gap present? contract-violation present?
Missing: **add** one before Format Axis Gate.

**Format Axis Gate**: all Blast Radius Rationale cells use 3-slot format with all slots named?
Violation: **rewrite** those cells before Inheritance Axis Gate.

**Inheritance Axis Gate**: all CROSS-SKILL blast radius >= max(contributors), with
"Inherited [LEVEL] from [sub-skill-X] ([F-ID])" in rationale?
Violation: **fix** blast radius and annotation before closing.

Coverage summary:
| Sub-Skill | Findings | Types |
|-----------|---------|-------|
| flow-lifecycle | | |
| flow-conversation | | |
| trace-state | | |
| trace-contract | | |
| trace-permissions | | |
| CROSS-SKILL | | |
| TOTAL | | |
```

---

## V-02 -- Descriptive Register + N=4 Quad Section Bodies (Register + Lifecycle, C-39 Extension to N=4)

**Axis**: Phrasing register + lifecycle emphasis -- the entire prompt is written in descriptive
register AND the axis-header rule is placed inside FOUR named section bodies: at the end of
the flow-lifecycle body (position 1), at the end of the flow-conversation body (position 2),
at the end of the trace-state body (position 3), and at the end of the trace-contract body
(position 4). The rule appears in descriptive form at all four positions: "The closing
confirmation gates are each labeled with a header that names the axis they check." No rule
appears in the trace-permissions body, the Synthesis body, or any inter-section slot. All
other elements identical to R13 V-04's descriptive form.

**Hypothesis**: C-39 (R13) states that C-37's no-N-ceiling rule is register-invariant: the
first qualifying named section-body placement satisfies C-22; all subsequent placements are
inert regardless of register. R13 V-04 confirmed descriptive + N=3 (positions 1, 3, 5).
V-02 tests descriptive + N=4 (positions 1, 2, 3, 4). C-39's generalization covers N=4 without
modification: flow-lifecycle body in descriptive satisfies C-22 (C-36 confirmed, R12 V-04);
flow-conversation, trace-state, trace-contract bodies are inert in descriptive (C-39 confirmed
no ceiling, register-invariant). The four chosen positions are all individually confirmed under
C-35. No unconfirmed variable is introduced. Expected: 33/33 = 100, golden.

**Novelty verification**: R13 V-04 = descriptive + N=3 (positions 1, 3, 5). V-02 = descriptive
+ N=4 (positions 1, 2, 3, 4). No prior round has combined descriptive register with four
section-body placements. Genuinely new.

```
This is `rhythm-behavior` running for topic: {{topic}}.

The output is a single continuous simulation findings report written to
`simulations/campaign/{{topic}}-simulate-{{date}}.md`. The entire report is
produced as one document from start to finish, without any promise of continuation.

The report proceeds through these sections in sequence: flow-lifecycle,
flow-conversation, trace-state, trace-contract, trace-permissions, SYNTHESIS,
Consolidated Findings.

---

### What to look for

**spec-gap** -- a requirement that is missing, ambiguous, or underspecified. The specific
spec section that is silent or unclear is named in the finding.

**contract-violation** -- a divergence in assumptions between caller and callee at a
named boundary.

**Blast radius**:
- WIDE -- corrupts shared state or breaks multiple downstream callers
- MEDIUM -- two or more components affected, shared state not reached
- NARROW -- failure stays inside one component

**Blast Radius Rationale** -- the 3-slot format is used for every finding:
  "[LEVEL] because [boundary] propagates to [caller/component], [effect]."
  Example: "WIDE because session-sequence-number contract propagates to flow-conversation
  routing and trace-contract pre-check, producing stale-state errors at both."

The findings table is opened before flow-lifecycle begins. Rows are appended as each
finding is discovered. The Blast Radius Rationale column is populated for every row.

| F-ID | Sub-Skill | Location | Finding Type | Blast Radius | Blast Radius Rationale | Impact | Remediation |
|------|-----------|----------|--------------|--------------|------------------------|--------|-------------|

---

### flow-lifecycle

The flow-lifecycle section traces `{{topic}}` through its full operational lifecycle
across four phases: INIT (preconditions and partial-init failures), NOMINAL (component
handoffs and silent failures), DEGRADED (error states and recovery), and TEARDOWN
(cleanup and handoff completeness).

Findings are appended to the table as they are discovered (Sub-Skill = flow-lifecycle,
finding type classified, blast radius assigned). Any phase that produces no findings is
noted as such.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### flow-conversation

The flow-conversation section examines the conversation and intent flow for `{{topic}}`:
intent routing paths, response contracts, graph transitions, fallback branches, session
state tracking, and timeout handling. Anything the spec leaves unclear, or where caller
and callee assumptions would diverge, is recorded as a finding.

Findings are appended as discovered (Sub-Skill = flow-conversation). If this section
produces no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-state

The trace-state section constructs the complete state model for `{{topic}}`: spec-defined
states, valid transitions, reachability from the initial state, and unauthorized state
crossings (classified as contract-violation by default).

Findings are appended as discovered (Sub-Skill = trace-state). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-contract

The trace-contract section examines every API and service boundary: pre/postcondition
symmetry, data invariants, and migration contracts. Any location where callee behavior
differs from what callers expect is recorded as a finding.

Findings are appended as discovered (Sub-Skill = trace-contract). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-permissions

The trace-permissions section traces the permission model: role authorization rules,
field-level security, team membership effects, sharing rules, and escalation paths.
Anything the spec omits, or where access control assumptions diverge, is recorded
as a finding.

Findings are appended as discovered (Sub-Skill = trace-permissions). If this section
produces no findings, that is stated explicitly.

---

### Synthesis

The Synthesis section reviews all findings collectively. For patterns that span two or
more sub-skills:
- The contributing sub-skills are named (at least two) along with each contributor's
  blast radius
- The compounding effect is described
- The CROSS-SKILL blast radius is >= max(contributing sub-skills); no downgrade from
  the highest contributor is permitted
- The rationale uses the 3-slot format plus provenance: "Inherited [LEVEL] from
  [sub-skill-X] ([F-ID])"
- The finding is labeled "CROSS-SKILL: [sub-skill-A + sub-skill-B]"
- The finding is inserted into the findings table with Sub-Skill = CROSS-SKILL

If no cross-skill patterns are present: "Synthesis: no cross-sub-skill patterns found."

---

### Consolidated Findings

Title: Simulation Campaign Report -- {{topic}} -- {{date}}

The findings table is sorted by blast radius (WIDE first, NARROW last).
Label: "Sorted by Blast Radius: WIDE -> MEDIUM -> NARROW"

Top finding: a concrete next step naming the exact spec section, boundary, or component
a developer must address before writing any implementation code.

Three confirmation gates follow, one axis per gate, not merged.

**Coverage Axis Gate**: Confirms that at least one spec-gap finding and at least one
contract-violation finding are present in the report. If either category is absent,
add the missing finding before proceeding to the Format Axis Gate.

**Format Axis Gate**: Confirms that every Blast Radius Rationale cell uses the 3-slot
format with all three slots named. Any cell that does not conform is rewritten using
the [LEVEL] / [boundary] / [caller/component] / [effect] structure before proceeding
to the Inheritance Axis Gate.

**Inheritance Axis Gate**: Confirms that every CROSS-SKILL blast radius is >= max of
the contributing sub-skills, and that the rationale carries "Inherited [LEVEL] from
[sub-skill-X] ([F-ID])". Any finding that fails this check has its CROSS-SKILL blast
radius and provenance annotation corrected before closing.

Coverage summary:
| Sub-Skill | Findings | Types |
|-----------|---------|-------|
| flow-lifecycle | | |
| flow-conversation | | |
| trace-state | | |
| trace-contract | | |
| trace-permissions | | |
| CROSS-SKILL | | |
| TOTAL | | |
```

---

## V-03 -- Descriptive Register + N=5 All Sub-Skill Bodies (Register + Lifecycle, C-39 at Structural Sub-Skill Maximum)

**Axis**: Phrasing register + lifecycle emphasis -- the entire prompt is written in descriptive
register AND the axis-header rule is placed inside ALL FIVE named sub-skill section bodies:
at the end of the flow-lifecycle body (position 1), at the end of the flow-conversation body
(position 2), at the end of the trace-state body (position 3), at the end of the trace-contract
body (position 4), and at the end of the trace-permissions body (position 5). The rule appears
in descriptive form at all five positions: "The closing confirmation gates are each labeled
with a header that names the axis they check." No rule appears in the Synthesis body or any
inter-section slot. All other elements identical to R13 V-04's descriptive form.

**Hypothesis**: This is the direct descriptive-register counterpart of R13 V-05 (which used
imperative register at the same five positions). C-40 confirmed N=5 imperative as the
structural sub-skill maximum. C-39 states that C-37's no-N-ceiling rule is register-invariant.
Therefore: the flow-lifecycle body in descriptive satisfies C-22 (C-36 confirmed, R12 V-04);
positions 2, 3, 4, 5 are inert in descriptive register (C-39 confirmed no-N-ceiling is
register-invariant). The first qualifying occurrence satisfies; all subsequent occurrences
are inert regardless of count or register. All five positions are individually confirmed under
C-35. Expected: 33/33 = 100, golden.

**Novelty verification**: R13 V-05 = imperative + N=5 (all five sub-skill bodies). R13 V-04
= descriptive + N=3 (positions 1, 3, 5). V-03 = descriptive + N=5 (all five positions). No
prior round has combined descriptive register with five-body placement. Genuinely new: direct
descriptive-register counterpart of R13 V-05.

```
This is `rhythm-behavior` running for topic: {{topic}}.

The output is a single continuous simulation findings report written to
`simulations/campaign/{{topic}}-simulate-{{date}}.md`. The entire report is
produced as one document from start to finish, without any promise of continuation.

The report proceeds through these sections in sequence: flow-lifecycle,
flow-conversation, trace-state, trace-contract, trace-permissions, SYNTHESIS,
Consolidated Findings.

---

### What to look for

**spec-gap** -- a requirement that is missing, ambiguous, or underspecified. The specific
spec section that is silent or unclear is named in the finding.

**contract-violation** -- a divergence in assumptions between caller and callee at a
named boundary.

**Blast radius**:
- WIDE -- corrupts shared state or breaks multiple downstream callers
- MEDIUM -- two or more components affected, shared state not reached
- NARROW -- failure stays inside one component

**Blast Radius Rationale** -- the 3-slot format is used for every finding:
  "[LEVEL] because [boundary] propagates to [caller/component], [effect]."
  Example: "WIDE because session-sequence-number contract propagates to flow-conversation
  routing and trace-contract pre-check, producing stale-state errors at both."

The findings table is opened before flow-lifecycle begins. Rows are appended as each
finding is discovered. The Blast Radius Rationale column is populated for every row.

| F-ID | Sub-Skill | Location | Finding Type | Blast Radius | Blast Radius Rationale | Impact | Remediation |
|------|-----------|----------|--------------|--------------|------------------------|--------|-------------|

---

### flow-lifecycle

The flow-lifecycle section traces `{{topic}}` through its full operational lifecycle
across four phases: INIT (preconditions and partial-init failures), NOMINAL (component
handoffs and silent failures), DEGRADED (error states and recovery), and TEARDOWN
(cleanup and handoff completeness).

Findings are appended to the table as they are discovered (Sub-Skill = flow-lifecycle,
finding type classified, blast radius assigned). Any phase that produces no findings is
noted as such.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### flow-conversation

The flow-conversation section examines the conversation and intent flow for `{{topic}}`:
intent routing paths, response contracts, graph transitions, fallback branches, session
state tracking, and timeout handling. Anything the spec leaves unclear, or where caller
and callee assumptions would diverge, is recorded as a finding.

Findings are appended as discovered (Sub-Skill = flow-conversation). If this section
produces no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-state

The trace-state section constructs the complete state model for `{{topic}}`: spec-defined
states, valid transitions, reachability from the initial state, and unauthorized state
crossings (classified as contract-violation by default).

Findings are appended as discovered (Sub-Skill = trace-state). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-contract

The trace-contract section examines every API and service boundary: pre/postcondition
symmetry, data invariants, and migration contracts. Any location where callee behavior
differs from what callers expect is recorded as a finding.

Findings are appended as discovered (Sub-Skill = trace-contract). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-permissions

The trace-permissions section traces the permission model: role authorization rules,
field-level security, team membership effects, sharing rules, and escalation paths.
Anything the spec omits, or where access control assumptions diverge, is recorded
as a finding.

Findings are appended as discovered (Sub-Skill = trace-permissions). If this section
produces no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### Synthesis

The Synthesis section reviews all findings collectively. For patterns that span two or
more sub-skills:
- The contributing sub-skills are named (at least two) along with each contributor's
  blast radius
- The compounding effect is described
- The CROSS-SKILL blast radius is >= max(contributing sub-skills); no downgrade from
  the highest contributor is permitted
- The rationale uses the 3-slot format plus provenance: "Inherited [LEVEL] from
  [sub-skill-X] ([F-ID])"
- The finding is labeled "CROSS-SKILL: [sub-skill-A + sub-skill-B]"
- The finding is inserted into the findings table with Sub-Skill = CROSS-SKILL

If no cross-skill patterns are present: "Synthesis: no cross-sub-skill patterns found."

---

### Consolidated Findings

Title: Simulation Campaign Report -- {{topic}} -- {{date}}

The findings table is sorted by blast radius (WIDE first, NARROW last).
Label: "Sorted by Blast Radius: WIDE -> MEDIUM -> NARROW"

Top finding: a concrete next step naming the exact spec section, boundary, or component
a developer must address before writing any implementation code.

Three confirmation gates follow, one axis per gate, not merged.

**Coverage Axis Gate**: Confirms that at least one spec-gap finding and at least one
contract-violation finding are present in the report. If either category is absent,
add the missing finding before proceeding to the Format Axis Gate.

**Format Axis Gate**: Confirms that every Blast Radius Rationale cell uses the 3-slot
format with all three slots named. Any cell that does not conform is rewritten using
the [LEVEL] / [boundary] / [caller/component] / [effect] structure before proceeding
to the Inheritance Axis Gate.

**Inheritance Axis Gate**: Confirms that every CROSS-SKILL blast radius is >= max of
the contributing sub-skills, and that the rationale carries "Inherited [LEVEL] from
[sub-skill-X] ([F-ID])". Any finding that fails this check has its CROSS-SKILL blast
radius and provenance annotation corrected before closing.

Coverage summary:
| Sub-Skill | Findings | Types |
|-----------|---------|-------|
| flow-lifecycle | | |
| flow-conversation | | |
| trace-state | | |
| trace-contract | | |
| trace-permissions | | |
| CROSS-SKILL | | |
| TOTAL | | |
```

---

## V-04 -- Imperative + N=6 All Six Named Positions (Lifecycle Emphasis, C-37 Beyond Sub-Skill Sequence)

**Axis**: Lifecycle emphasis -- the Axis-Header Rule is placed as a standalone labeled
paragraph inside ALL FIVE named sub-skill section bodies AND the Synthesis section body:
at the end of the flow-lifecycle body (position 1), at the end of the flow-conversation
body (position 2), at the end of the trace-state body (position 3), at the end of the
trace-contract body (position 4), at the end of the trace-permissions body (position 5),
and at the end of the Synthesis body (position 6). No rule appears in any inter-section
slot. All other elements identical to V-01.

**Hypothesis**: C-40 states "N=5, the structural maximum of the rhythm-behavior prompt
(all five sub-skill section bodies)." However, C-37 states the inertness rule "is bounded
only by the number of available structural positions in the prompt, not by an N-count limit."
The Synthesis section body is a confirmed qualifying position under C-32 (R9 V-04) -- a named
section body that is not a sub-skill body. C-40's "structural maximum" language refers
specifically to the sub-skill sequence; it does not exclude the Synthesis body from C-37's
general principle. V-04 places the rule in all six confirmed named positions. The first
qualifying occurrence (flow-lifecycle body, position 1) satisfies C-22; positions 2-6 are
redundant but inert. No criterion penalizes N=6 any more than N=5. The Synthesis body
contributes one additional inert placement; C-37's boundedness principle absorbs it without
modification. Expected: 33/33 = 100, golden.

**Novelty verification**: R13 V-05 = N=5 (all five sub-skill bodies; Synthesis excluded).
V-04 = N=6 (all five sub-skill bodies + Synthesis body). No prior round has placed the rule
simultaneously in all five sub-skill bodies AND the Synthesis body. The Synthesis body as a
6th concurrent placement is genuinely new.

```
You are running `rhythm-behavior` for topic: {{topic}}.

Output: a single continuous simulation findings report saved as
`simulations/campaign/{{topic}}-simulate-{{date}}.md`.

Write everything as one document from start to finish. Do not promise to continue.

Sections in this exact order: flow-lifecycle, flow-conversation, trace-state,
trace-contract, trace-permissions, SYNTHESIS, Consolidated Findings.

---

### What to look for

**spec-gap** -- a requirement that is missing, ambiguous, or underspecified; name the
spec section that is silent or unclear.

**contract-violation** -- divergent assumptions between caller and callee at a named
boundary.

**Blast radius**:
- WIDE -- corrupts shared state or breaks multiple downstream callers
- MEDIUM -- two or more components affected, shared state not reached
- NARROW -- failure stays inside one component

**Blast Radius Rationale** -- 3-slot format required for every finding:
  "[LEVEL] because [boundary] propagates to [caller/component], [effect]."
  Example: "WIDE because session-sequence-number contract propagates to flow-conversation
  routing and trace-contract pre-check, producing stale-state errors at both."

Open this table before flow-lifecycle. Append rows as findings are discovered.
Blast Radius Rationale is required for every row.

| F-ID | Sub-Skill | Location | Finding Type | Blast Radius | Blast Radius Rationale | Impact | Remediation |
|------|-----------|----------|--------------|--------------|------------------------|--------|-------------|

---

### flow-lifecycle

Walk `{{topic}}` through four phases: INIT (preconditions and partial-init failures),
NOMINAL (component handoffs and silent failures), DEGRADED (error states and recovery),
TEARDOWN (cleanup and handoff completeness).

Add each finding immediately (Sub-Skill = flow-lifecycle, classify, assign blast radius).
If a phase has no findings, say so briefly.

**Axis-Header Rule**: Each gate header names its axis.

---

### flow-conversation

Walk the conversation and intent flow for `{{topic}}`: intent routing paths, response
contracts, graph transitions, fallback branches, session state tracking, timeout handling.
Note anything the spec leaves unclear or where caller and callee would disagree.

Add each finding immediately (Sub-Skill = flow-conversation, classify, assign blast radius).
If none: say so explicitly.

**Axis-Header Rule**: Each gate header names its axis.

---

### trace-state

Build the complete state model for `{{topic}}`: spec-defined states, valid transitions,
reachability from initial state, unauthorized crossings (contract-violation by default).

Add each finding immediately (Sub-Skill = trace-state, classify, assign blast radius).
If none: say so explicitly.

**Axis-Header Rule**: Each gate header names its axis.

---

### trace-contract

Check every API and service boundary: pre/postcondition symmetry, data invariants,
migration contracts. Note anywhere callee behavior differs from what callers expect.

Add each finding immediately (Sub-Skill = trace-contract, classify, assign blast radius).
If none: say so explicitly.

**Axis-Header Rule**: Each gate header names its axis.

---

### trace-permissions

Trace the permission model: role authorization rules, field-level security, team membership
effects, sharing rules, escalation paths. Note anything the spec omits or where access
control assumptions diverge.

Add each finding immediately (Sub-Skill = trace-permissions, classify, assign blast radius).
If none: say so explicitly.

**Axis-Header Rule**: Each gate header names its axis.

---

### Synthesis

Review all findings together. For patterns spanning two or more sub-skills:
- Name contributing sub-skills (at least two) and each contributor's blast radius
- Describe the compounding effect
- CROSS-SKILL blast radius >= max(contributing sub-skills); no downgrade permitted
- Rationale: 3-slot format + provenance: "Inherited [LEVEL] from [sub-skill-X] ([F-ID])"
- Label: "CROSS-SKILL: [sub-skill-A + sub-skill-B]"
- Add to findings table with Sub-Skill = CROSS-SKILL

If none: "Synthesis: no cross-sub-skill patterns found."

**Axis-Header Rule**: Each gate header names its axis.

---

### Consolidated Findings

Title: Simulation Campaign Report -- {{topic}} -- {{date}}

Sort the findings table by blast radius (WIDE first, NARROW last).
Label: "Sorted by Blast Radius: WIDE -> MEDIUM -> NARROW"

Top finding: one concrete next step naming the exact spec section, boundary, or component
a developer must address before writing implementation code.

Three gates before closing. One axis per gate. Do not merge.

**Coverage Axis Gate**: spec-gap present? contract-violation present?
Missing: **add** one before Format Axis Gate.

**Format Axis Gate**: all Blast Radius Rationale cells use 3-slot format with all slots named?
Violation: **rewrite** those cells before Inheritance Axis Gate.

**Inheritance Axis Gate**: all CROSS-SKILL blast radius >= max(contributors), with
"Inherited [LEVEL] from [sub-skill-X] ([F-ID])" in rationale?
Violation: **fix** blast radius and annotation before closing.

Coverage summary:
| Sub-Skill | Findings | Types |
|-----------|---------|-------|
| flow-lifecycle | | |
| flow-conversation | | |
| trace-state | | |
| trace-contract | | |
| trace-permissions | | |
| CROSS-SKILL | | |
| TOTAL | | |
```

---

## V-05 -- Descriptive Register + N=6 Absolute Structural Maximum (Register + Lifecycle, C-38+C-39 at All Six Named Positions)

**Axis**: Phrasing register + lifecycle emphasis -- the entire prompt is written in descriptive
register AND the axis-header rule is placed inside ALL FIVE named sub-skill section bodies
AND the Synthesis section body: at the end of the flow-lifecycle body (position 1), at the
end of the flow-conversation body (position 2), at the end of the trace-state body (position 3),
at the end of the trace-contract body (position 4), at the end of the trace-permissions body
(position 5), and at the end of the Synthesis body (position 6). The rule appears in
descriptive form at all six positions: "The closing confirmation gates are each labeled with
a header that names the axis they check." No rule appears in any inter-section slot. All
other elements identical to V-03's descriptive form for sub-skill sections and Synthesis.

**Hypothesis**: V-05 combines V-03 (descriptive + N=5 sub-skill bodies) and V-04 (imperative
+ N=6 including Synthesis body) into the absolute structural maximum: descriptive register +
all six named positions. C-38 confirmed descriptive + dual body inertness; C-39 confirmed
descriptive + no-N-ceiling. The Synthesis body is a confirmed qualifying position (C-32,
R9 V-04). C-34 states all aspirational criteria are register-invariant; C-37's principle that
inertness is bounded only by available structural positions applies to descriptive register
without modification. The first qualifying occurrence (flow-lifecycle body, position 1)
satisfies C-22 in descriptive register (C-36 confirmed); positions 2-6 are inert regardless
of count and register (C-39 and C-37 joint coverage). The Synthesis body in descriptive
register is a new structural position for this composition but follows directly from C-32's
confirmation and C-34's register-invariance. Expected: 33/33 = 100, golden.

**Novelty verification**: V-03 = descriptive + N=5 (sub-skill bodies only). V-04 = imperative
+ N=6 (sub-skill bodies + Synthesis). V-05 = descriptive + N=6 (all six named positions). No
prior round has combined descriptive register with six-body placement. The absolute structural
maximum in descriptive register is genuinely new.

```
This is `rhythm-behavior` running for topic: {{topic}}.

The output is a single continuous simulation findings report written to
`simulations/campaign/{{topic}}-simulate-{{date}}.md`. The entire report is
produced as one document from start to finish, without any promise of continuation.

The report proceeds through these sections in sequence: flow-lifecycle,
flow-conversation, trace-state, trace-contract, trace-permissions, SYNTHESIS,
Consolidated Findings.

---

### What to look for

**spec-gap** -- a requirement that is missing, ambiguous, or underspecified. The specific
spec section that is silent or unclear is named in the finding.

**contract-violation** -- a divergence in assumptions between caller and callee at a
named boundary.

**Blast radius**:
- WIDE -- corrupts shared state or breaks multiple downstream callers
- MEDIUM -- two or more components affected, shared state not reached
- NARROW -- failure stays inside one component

**Blast Radius Rationale** -- the 3-slot format is used for every finding:
  "[LEVEL] because [boundary] propagates to [caller/component], [effect]."
  Example: "WIDE because session-sequence-number contract propagates to flow-conversation
  routing and trace-contract pre-check, producing stale-state errors at both."

The findings table is opened before flow-lifecycle begins. Rows are appended as each
finding is discovered. The Blast Radius Rationale column is populated for every row.

| F-ID | Sub-Skill | Location | Finding Type | Blast Radius | Blast Radius Rationale | Impact | Remediation |
|------|-----------|----------|--------------|--------------|------------------------|--------|-------------|

---

### flow-lifecycle

The flow-lifecycle section traces `{{topic}}` through its full operational lifecycle
across four phases: INIT (preconditions and partial-init failures), NOMINAL (component
handoffs and silent failures), DEGRADED (error states and recovery), and TEARDOWN
(cleanup and handoff completeness).

Findings are appended to the table as they are discovered (Sub-Skill = flow-lifecycle,
finding type classified, blast radius assigned). Any phase that produces no findings is
noted as such.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### flow-conversation

The flow-conversation section examines the conversation and intent flow for `{{topic}}`:
intent routing paths, response contracts, graph transitions, fallback branches, session
state tracking, and timeout handling. Anything the spec leaves unclear, or where caller
and callee assumptions would diverge, is recorded as a finding.

Findings are appended as discovered (Sub-Skill = flow-conversation). If this section
produces no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-state

The trace-state section constructs the complete state model for `{{topic}}`: spec-defined
states, valid transitions, reachability from the initial state, and unauthorized state
crossings (classified as contract-violation by default).

Findings are appended as discovered (Sub-Skill = trace-state). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-contract

The trace-contract section examines every API and service boundary: pre/postcondition
symmetry, data invariants, and migration contracts. Any location where callee behavior
differs from what callers expect is recorded as a finding.

Findings are appended as discovered (Sub-Skill = trace-contract). If this section produces
no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### trace-permissions

The trace-permissions section traces the permission model: role authorization rules,
field-level security, team membership effects, sharing rules, and escalation paths.
Anything the spec omits, or where access control assumptions diverge, is recorded
as a finding.

Findings are appended as discovered (Sub-Skill = trace-permissions). If this section
produces no findings, that is stated explicitly.

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### Synthesis

The Synthesis section reviews all findings collectively. For patterns that span two or
more sub-skills:
- The contributing sub-skills are named (at least two) along with each contributor's
  blast radius
- The compounding effect is described
- The CROSS-SKILL blast radius is >= max(contributing sub-skills); no downgrade from
  the highest contributor is permitted
- The rationale uses the 3-slot format plus provenance: "Inherited [LEVEL] from
  [sub-skill-X] ([F-ID])"
- The finding is labeled "CROSS-SKILL: [sub-skill-A + sub-skill-B]"
- The finding is inserted into the findings table with Sub-Skill = CROSS-SKILL

If no cross-skill patterns are present: "Synthesis: no cross-sub-skill patterns found."

The closing confirmation gates are each labeled with a header that names the axis they check.

---

### Consolidated Findings

Title: Simulation Campaign Report -- {{topic}} -- {{date}}

The findings table is sorted by blast radius (WIDE first, NARROW last).
Label: "Sorted by Blast Radius: WIDE -> MEDIUM -> NARROW"

Top finding: a concrete next step naming the exact spec section, boundary, or component
a developer must address before writing any implementation code.

Three confirmation gates follow, one axis per gate, not merged.

**Coverage Axis Gate**: Confirms that at least one spec-gap finding and at least one
contract-violation finding are present in the report. If either category is absent,
add the missing finding before proceeding to the Format Axis Gate.

**Format Axis Gate**: Confirms that every Blast Radius Rationale cell uses the 3-slot
format with all three slots named. Any cell that does not conform is rewritten using
the [LEVEL] / [boundary] / [caller/component] / [effect] structure before proceeding
to the Inheritance Axis Gate.

**Inheritance Axis Gate**: Confirms that every CROSS-SKILL blast radius is >= max of
the contributing sub-skills, and that the rationale carries "Inherited [LEVEL] from
[sub-skill-X] ([F-ID])". Any finding that fails this check has its CROSS-SKILL blast
radius and provenance annotation corrected before closing.

Coverage summary:
| Sub-Skill | Findings | Types |
|-----------|---------|-------|
| flow-lifecycle | | |
| flow-conversation | | |
| trace-state | | |
| trace-contract | | |
| trace-permissions | | |
| CROSS-SKILL | | |
| TOTAL | | |
```
