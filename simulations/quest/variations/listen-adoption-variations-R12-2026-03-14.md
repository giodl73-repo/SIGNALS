Written to `simulations/quest/rubrics/listen-adoption-rubric-v12-variate-R12-2026-03-15.md`.

---

## listen-adoption R12 Variations Summary

**Baseline (all five carry from R11 V-05):** STRUCTURAL CONTRACT (C-35), double-anchored mechanism proof — per-gate footers in H/I/J + pre-verification declaration (C-34), TABLE 3 header declaring rows as typed structural slot keys (C-36), plus all R11/R10 carries.

**Three variation axes explored:**

| | Axis | Hypothesis |
|---|---|---|
| **V-01** | **Role sequence** — TABLE 1 adds `Incumbent Dependency` column; THE INCUMBENT named in TABLE 1 preamble rather than standalone SECTION B | Merging displacement grounding into archetype assignment prevents C-01 and C-26 from satisfying independently via disconnected sections |
| **V-02** | **Output format** — TABLE 3 row labels prefixed `SLOT-KEY:`, extending C-36's typed-header contract from header level down to each individual row | Row-level type contract closes the gap where a model reads the header once and ignores it during row generation |
| **V-03** | **Lifecycle emphasis** — PHASE 3 CHASM decomposed into `CHASM-A` (incumbent defense) / `CHASM-B` (bridge condition) / `CHASM-C` (crossing signal) subsections | Per-element structural slots prevent the most common C-03 failure mode: merging all three elements into a single prose block at uneven depth |
| **V-04** | **V-02 + V-03** | SLOT-KEY: row types + chasm decomposition are structurally orthogonal — both close independent generation-time gaps |
| **V-05** | **V-01 + V-02 + V-03** | Three axes enforce generation-time constraints at three structurally independent locations (TABLE 1 column header, TABLE 3 row labels, PHASE 3 section headers) with no downstream dependency between them |

**C-19 note:** PARTIAL persists across all five by paradox of strength — answer-form Q-BARRIER questions suppress gate failures; no CORRECTION BLOCK fires. This is the remaining open gap for R13 if resolution is desired.
