Written to `simulations/quest/golden/topic-plan-variate-R3-2026-03-15.md`.

---

## Round 3 — topic:plan Variations

Three single-axis, two combined. All target the three new v3 criteria.

### V-01 — Output Format: `??` sentinel tokens in pre-committed schemas (C-14, C-15)
Every schema cell pre-printed with `??`. The model must replace `??` or leave the sentinel — a remaining `??` is machine-scannable evidence of a gap, not an invisible blank. Delta summary sentence uses `??` slots that require exact integers.

### V-02 — Phrasing Register: conversational + named banned-phrase blacklist (C-16)
Conversational direct-address with an inline enumerated blacklist at the proposal gate: "compelling reason", "clearly warranted", "update is needed", etc. Named phrases close the escape hatch that "no generic phrases" leaves wide open. Single axis.

### V-03 — Lifecycle Emphasis: DELTA SUMMARY BLOCK as a standalone gated phase (C-15)
Delta detection split into its own phase with one sole deliverable: the templated sentence `Strategy was written on [DATE]. [N] artifacts are NEW. [M] are PRIOR.` The exit gate requires exact integers — the phase cannot be passed by producing a table. Tests whether isolated phase responsibility improves counted-total compliance.

### V-04 — Role Sequence + Output Format: defense register with sentinel tokens (C-14, C-15, C-16)
Defense-first sequence (commit to what the strategy would argue before reading challengers) combined with `??` sentinels in all tables. "vs. NO CHANGE" must name the Step 2 defense argument defeated — combining fair-fight framing with the C-16 named-reference requirement.

### V-05 — Full Combination: all three new v3 criteria + phase gates + inertia premise (C-14, C-15, C-16 + all essential + recommended)
All three new criteria have dedicated structural mechanisms: `??` sentinels (C-14), DELTA SUMMARY BLOCK as a gated phase (C-15), inline named banned-phrase blacklist (C-16). Wrapped in named phases with binary exit gates and inertia premise. Eight schemas pre-committed, conflict audit included.

**Risk ranking** (safest first): V-05 > V-03 > V-04 > V-01 > V-02
