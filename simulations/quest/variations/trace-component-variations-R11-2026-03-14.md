Written to `simulations/quest/rubrics/trace-component-rubric-v11-variations-R11-2026-03-15.md`.

---

## R11 Variations — Summary

**5 complete prompt bodies (V-01 through V-05)**

| V | Axis | C-36 Mechanism | C-37 Mechanism |
|---|------|---------------|---------------|
| V-01 | Inertia framing | Direction column **header** embeds `<-> inert (default)` — active codes are replacements; blank cell = structurally inert | MANIFEST-N/TABLE-N adjacency via "immediately below" text |
| V-02 | Lifecycle emphasis | Numbered permitted-values list; `<-> inert` first with "(default)" annotation | `↓ TABLE-N begins immediately below` **marker embedded in each MANIFEST close** |
| V-03 | Output format | **SCHEMA CHARTER artifact** declares inert-as-default as a design decision before any tables | SCHEMA CHARTER declares paired-block adjacency as Organizational Principle 1 |
| V-04 | Inertia + Lifecycle (combined) | V-01 column header embedding | V-02 adjacency marker — both present simultaneously |
| V-05 | Role sequence + Inertia (combined) | **Schema Architect role** produces TRAVERSAL-SCHEMA declaring `<-> inert` by design; analyst role violating it contradicts own artifact | TRAVERSAL-SCHEMA declares paired-block structure in role 1 |

**Predicted ranking: V-04 >= V-01 > V-02 >= V-03 > V-05**

The key R11 structural bet: V-04 combines non-redundant mechanisms — the column header closes the within-table escape path (blank Direction cell = inert by default) while the adjacency marker closes the block-boundary escape path (content between manifest and table). These target different failure modes and don't add coherence cost to each other.

V-05's two-role structure is the novel experiment: schema-as-design-decision vs. schema-as-instruction. The risk is drift between the TRAVERSAL-SCHEMA artifact and the execution tables.
