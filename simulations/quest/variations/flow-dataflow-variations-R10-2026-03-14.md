The 5 variations are written to `simulations/quest/rubrics/flow-dataflow-rubric-v10-R10-2026-03-15.md`.

---

## Summary

**Round 10 targets C-34 and C-35** — the only two criteria not scoreable in R9.

### Variation map

| Var | Axis | Hypothesis |
|-----|------|-----------|
| **V-01** | Lifecycle emphasis | Graft C-34/C-35 onto the R9 V-01 baseline (98 pts, natural order) by adding a 7th boundary column and registering INCUMBENT TOTAL as `[A-13]` with SC-9 + SC-10. Minimal blast radius; isolates the lifecycle axis. |
| **V-02** | Output format | Make REGISTER DECLARATION the *primary* enforcement vehicle for C-34/C-35 — explicit cell-form row for `Incumbent Equiv.` and section-format row for INCUMBENT TOTAL in Part B. SC-9/SC-10 become callbacks, not declarations. Evaluator can verify C-34/C-35 from one location. |
| **V-03** | Inertia framing | INCUMBENT BASELINE produced first (before DOMAIN CONTEXT). Operations leads. SLA is explicitly derived from `[A-01] total + headroom`. Every `Incumbent Equiv.` cell cites a baseline visible since line 1 — no forward-citation complexity. |
| **V-04** | Role sequence + Lifecycle | Commerce → Operations → Finance. Finance last. Commerce and Operations cite `[A-02]` *forward* in boundary cells — token pre-declared in registry, content not yet produced. Strongest C-34 citation stress: structural compliance is only detectable way to get it right. Finance aggregates INCUMBENT TOTAL from all upstream forward-citations. |
| **V-05** | Inertia framing + Dual-register | Finance (tabular) → Commerce (conversational) → Operations (tabular). REGISTER TRANSLATION TABLE extended to C-34/C-35 compliance markers in both registers. C-33 REGISTER INVARIANTS section explicitly names how C-34 (`**Incumbent equivalent:**` bold marker) and C-35 (tabular extraction from conversational markers) survive the register switch. |

### Key structural innovations

- **V-01/V-02/V-03**: 7-column BOUNDARY BLOCK SCHEMA with `Incumbent Equiv.` column; `[A-13] INCUMBENT TOTAL` as registered artifact with role-breakdown table
- **V-04**: Forward-citation protocol — `[A-02]` cited before produced; Finance verifies consistency retroactively
- **V-05**: REGISTER TRANSLATION TABLE covers C-34/C-35 for conversational register; `**Incumbent equivalent:**` bold marker as C-34's conversational compliance check; INCUMBENT TOTAL extracts from conversational markers
- **All variations**: SC-10 enforces "produce `[A-13]` immediately before `[A-14]`; cite `[A-13]` not `[A-02]` in TRADE-OFF" — the decisive C-35 structural gate
