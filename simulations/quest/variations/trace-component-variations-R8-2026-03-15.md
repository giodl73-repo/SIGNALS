`simulations/quest/golden/trace-component-variate-R8-2026-03-15.md` written — 5 complete variations.

---

**What's in R8 and why:**

**3 single-axis variations:**

- **V-01 (Role sequence)** — ECC as a dedicated first role before schemas exist. Schema fill rules embed OBL-IDs (`"Step values MUST appear in §5 Upstream Ref [OBL-02 coverage]"`), creating bidirectional binding between the contract and the schemas. The Architecture Gate (Role 4) emits TRACE ARCHITECTURE: SEALED only after inventorying all four pre-trace artifacts.

- **V-02 (Output format)** — ECC as a two-column compliance table inside the fused TRACE ARCHITECTURE artifact. The audit role fills the COMPLIANCE column against pre-declared ECC rows rather than constructing its own checklist — making evidence chain audit mechanical. 3-role structure (Architect → Trace Author → Auditor).

- **V-03 (Phrasing register)** — Flat pre-trace phase (no role machinery). Five named BLOCKS with DECLARED tokens. Imperative SHALL/MUST language with named violation types throughout. An inline ARCHITECTURE CHECKLIST ticks five conditions before emitting TRACE ARCHITECTURE: SEALED. One C-18 risk flagged: the criterion says "role-level gate" — if read strictly, a named blocking block (not a role transition) may score partial.

**2 combined variations:**

- **V-04 (Role sequence + Output format)** — ECC-first role + compliance table format + OBL-ID column annotations in schema headers. Audit fills the compliance table against the pre-declared Role 1 ECC. Seal token explicitly inventories artifact counts.

- **V-05 (All three)** — Full synthesis. ECC first, violation types named, OBL-ID brackets in section headers (`§4 Re-render List [OBL-01]`), PASS-THROUGH PART B includes a `Binds To` column referencing TABLE columns by name, and the TRACE ARCHITECTURE: SEALED token carries a falsifiable count inventory (`ECC (6 obligations: OBL-01–OBL-06), TABLE-01 SCHEMA (5 cols), …`) so the seal is a checkable claim, not just a marker.
