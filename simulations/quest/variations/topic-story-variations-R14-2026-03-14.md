Written to `simulations/quest/variations/topic-story-variations-R14-2026-03-15.md`.

---

## R14 Variations — Summary

**Axes selected**: Role sequence (V-01), Output format (V-02), Lifecycle emphasis (V-03), Role sequence + Inertia framing (V-04), Phrasing register + Lifecycle emphasis (V-05)

**C-45 and C-46** — all five variations pass both. Every variation inherits the V-05 R13 foundation (two verbatim hedge patterns, inertia comparator + S4b cross-reference in S4c Para 2).

**C-38 upgrade** — V-01, V-04, V-05 introduce cell-level (three-coordinate) entry conditions: `Signal inventory axis, S1 row, Status field — non-empty...`. This names axis + row + field-name rather than just "all fields complete," which should move C-38 from PARTIAL to PASS.

**Two excellence signal candidates** for R15:

1. **Three-element reversal condition** (V-04) — Para 3 of S4c requires `(trigger) + (option change) + (whether triggered option beats inertia at that trigger point)`. This was R13's uncaptured Signal 2. C-46 fires only on Para 2; no criterion fires on Para 3's inertia-at-trigger requirement.

2. **Class-labeled hedge prohibition** (V-05) — Three verbatim patterns + explicit class label ("aggregate-first hedging cluster") with an extension rule: "new instances of this class are prohibited even if not explicitly named." C-45 fires on ≥2 named patterns; no criterion fires on whether the class is named and made self-extending.
