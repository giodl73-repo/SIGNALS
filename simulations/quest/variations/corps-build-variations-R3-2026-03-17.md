**Variate R3 — corps-build** written to `simulations/quest/golden/corps-build-variate-R3-2026-03-17.md`.

---

## Summary

5 complete variations across 3 new axes, none of which appeared in R1 or R2:

| Variation | Axis | Hypothesis |
|-----------|------|-----------|
| **V-01** | Example-driven | Minimal worked example (2-team org.yaml → full output fragment) before instructions eliminates format ambiguity at source; strongest on C-02, C-05, C-07 |
| **V-02** | Constraint-first | 5 hard invariants declared at top before any steps; model treats them as active during writes, not as post-hoc criteria; strongest on C-01, C-03, C-04, C-05 |
| **V-03** | Minimal imperative | Strip all ceremony — no phases, no audit, no scaffold; tests whether R1-R2 structure is load-bearing or overhead |
| **V-04** | Constraint-first + Example-driven | Invariants tell the model *what must be true*; example tells it *what it should look like*; addresses the two most common failure modes together |
| **V-05** | Constraint-first + Schema-as-contract (R2) + Coverage audit (R2) | Tests whether R2's best structural axes are amplified by constraint-first framing, or whether three compounding axes produce diminishing returns from prompt length |

**R3's distinguishing bet**: V-03 is the control — if minimal passes, R1-R2 overhead was unnecessary. If V-03 fails on C-01/C-03 and V-02/V-05 pass, constraint-first framing is the load-bearing addition R1-R2's structure was approximating.
