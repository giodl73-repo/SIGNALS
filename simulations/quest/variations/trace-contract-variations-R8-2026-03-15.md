Here are the 5 complete R8 variations:

---

**Round 8 Variation Map**

| | Axis | Target | Hypothesis |
|---|---|---|---|
| V-01 | lifecycle-emphasis | C-23 isolation | Upgrades the C-13 bracket token to a clause-count + identity manifest. Tests whether the token format upgrade alone is achievable without structural overhead. |
| V-02 | output-format | C-24 isolation | Adds explicit zero/singleton/multi branch instructions to the Patterns block. Prior rounds satisfy C-10 (grouping when N>=2) but leave zero and singleton cases ambiguous. |
| V-03 | depth | C-25 isolation | Embeds a two-stage self-test protocol (draft → necessary-information test → mechanism) inside every finding template. Converts the C-05 named disqualifier into a decision procedure with a binary outcome. |
| V-04 | lifecycle-emphasis x output-format | C-23 x C-24 | Pre-declares both the structured gate token format and the three-branch Patterns template in the artifact skeleton before Phase 1 begins. Tests additive coverage at two structural layers. |
| V-05 | all three new + R7 platinum | C-23/24/25 + C-13/14/16/22 | Full R8 ceiling. Skeleton pre-declares all seven mechanisms — gate manifest, Patterns branches, self-test slots, severity-stratified templates, connector-impact on every tier. Tests whether all 25 criteria can be targeted simultaneously without regressing C-01–C-05. |

**Key structural innovations in R8:**

- **C-23** (V-01, V-04, V-05): `[EXPECTED-SEALED | clauses:N | date:YYYY-MM-DD | author:contract-expert | phase:1-complete]` with echoed `gate-clauses` in the closure token — clause count becomes a verifiable invariant
- **C-24** (V-02, V-04, V-05): Three named branches with distinct pass conditions — zero requires an affirmative statement (not silence), singleton requires per-finding attribution lines (not just "same cause"), multi requires grouped per-root-cause sections
- **C-25** (V-03, V-05): `hypothesis-draft:` → `self-test:` → `mechanism:` triple slot — the necessary-information question is embedded in the template, making the binary YES/NO verdict a structural requirement before the finding is considered complete
