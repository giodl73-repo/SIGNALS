Written to `simulations/quest/golden/prove-prototype-variate-R19-2026-03-15.md`.

---

## Round 19 — prove-prototype Variate Summary

**Target criteria:** C-49 (table-enforced thread co-equality) + C-50 (Structural Marker Inventory) — both applied on the R18 base.

| Variation | Axis | C-49 mechanism | C-50 mechanism | Key structural advance |
|-----------|------|---------------|---------------|----------------------|
| **V-01** | Role sequence | Two-row thread table with Scope \| Key structural markers \| Terminal discharge obligation; both rows carry equivalent multi-clause depth | Pre-execution inventory section with 4-column table + REQUIRED closure statement | BUILD split into IMPLEMENTER + MEASURER sub-roles, creating a fifth intra-container handoff symmetric with CLOSE's COMPARATOR/AUDITOR |
| **V-02** | Output format | Thread table visually continuous with all other tables; column schema evaluated by the same grammar the reader is already using for phase outputs | Inventory uses same 4-column visual convention as all phase tables — no register discontinuity | All phase outputs table-anchored; C-22 substantive values machine-scannable; no prose bullet lists |
| **V-03** | Phrasing register | Model addressed directly: "Write a two-row table with this schema — both rows MUST carry equivalent depth" | Model addressed directly: "Write this section now... After the table, write: REQUIRED..." | Conversational second-person imperative throughout; structural commands issued as direct-address directives, reducing metacognitive translation overhead |
| **V-04** | Role seq + Phrasing reg | Thread 2 Scope column names all four sub-roles and both handoff boundaries, depth-matched to Thread 1's four-state lifecycle | Inventory section written via direct-address commands; closure issued as explicit instruction to the model | IMPLEMENTER/MEASURER + COMPARATOR/AUDITOR both declared in the Thread 2 table row, creating symmetric intra-container role structure |
| **V-05** | Role seq + Format + Inertia | Both thread rows use multi-clause entries across all three columns; CLOSE COMPLETE sub-role discharge recorded as a two-row table rather than inline prose | Inventory placed after document title that names the competitor-under-investigation placeholder; five competitor surfaces enumerated in Thread 1 Key structural markers column | Three-axis ceiling design: BUILD sub-roles, all-table format, competitor framing across all five surfaces including competitor name in document preamble title line |

**Key structural bets for R19:**

- **V-01** is the cleanest single-axis test: does making BUILD's role structure mirror CLOSE's improve the document's overall role contamination defense symmetry? Thread 2 gains a second handoff chain in the table's Terminal discharge obligation cell, which must be depth-matched to Thread 1's four-state chain.
- **V-03** tests whether conversational direct-address produces better C-49 column symmetry than third-person structural specification — the hypothesis being that models need less inference to fill a column they are told to fill in direct imperative.
- **V-05** is the ceiling design: the CLOSE COMPLETE sub-role discharge is expressed as a two-row table itself, meaning Thread 2's terminal discharge is verified by the same tabular structure that enforces Thread 1's.
