Written to `simulations/quest/golden/quest-score-variate-R15-2026-03-15.md`.

---

## Round 15 — 5 Variations Summary

**Single axis (3 variations):**

| # | Axis | C-35 Prediction | Key mechanism |
|---|------|-----------------|---------------|
| V-01 | **Output format (DC)** | **PASS** | Splits Audit A into `Q1-TypeLevel` + `Q2-IntraRun` as separate labeled columns; ANCHOR REVIEW BLOCK pre-states both questions verbatim with `per Q1 above` / `per Q2 above` per-cell references |
| V-02 | **Lifecycle emphasis (PP)** | **PARTIAL** | SPECIFICITY PRE-PASS builds TYPE-LEVEL SCAN TABLE and INTRA-RUN SCAN TABLE as separate phase artifacts — but the per-cell VERIFIER schema collapses both back into one "Specificity" column; tests whether phase-level separation satisfies the per-cell-schema requirement |
| V-03 | **Phrasing register (OR)** | **FAIL** | Uses `TYPE-GENERICITY SCAN:` / `RUN-DUPLICATE SCAN:` directive labels inside a single "Specificity-Scan" field — tests whether operational clarity without structural column separation satisfies C-35 |

**Combinations (2 variations):**

| # | Axes | C-35 Prediction | Key mechanism |
|---|------|-----------------|---------------|
| V-04 | **DC + PP** | **PASS** | Pre-pass registers are authoritative; per-cell table has Q1-TypeLevel + Q2-IntraRun columns consistent with pre-pass; two non-redundant enforcement points |
| V-05 | **DC + PP + role** | **PASS + ES** | SPECIFICITY PRE-PASS elevated to named ROLE: SPECIFICITY AUDITOR with gate [SPECIFICITY AUDIT COMPLETE]; VERIFIER entry condition quotes gate verbatim — adds independent-audit property (candidate C-42: VERIFIER header alone confirms Q1/Q2 separation without reading SPECIFICITY AUDITOR body) |

**Ceiling path**: V-01 or V-04/V-05 all target C-35 PASS. V-05's VERIFIER-entry-gate citation is the excellence signal to watch for R16 criterion extraction.
