Here are the 5 Round 10 variations, written to `simulations/quest/golden/topic-plan-variate-R10-2026-03-15.md`:

---

## R10 Variations Summary

| Variation | Axis | C-35 Mechanism | Hypothesis |
|-----------|------|----------------|------------|
| **V-01** | Output format | Two-level: compact SCHEMA REGISTER table (4 rows, auditable by count) + expanded schema blocks | Separates completeness check (register scan) from correctness check (block read) — two distinct structural layers |
| **V-02** | Lifecycle emphasis | TABLE SCHEMAS as Phase −1 with Gate-S; schema completeness is the first gate in the chain Gate-S→Gate-0→…→Gate-4 | Gate-S gives schema completeness the same structural weight as defense register completeness (Gate-0); its absence breaks the chain visibly |
| **V-03** | Inertia framing | Schemas as "immutable output contracts" parallel to strategy; column drift = contract breach; Cal = "chain of custody" | When schema inertia matches strategy inertia, column changes require defeating a declared position — not a formatting choice |
| **V-04** | Combined (format + phrasing register) | Schema Contract Board with per-schema BREACH CONDITION; SHALL/MUST/PROHIBITED throughout | Every schema violation has a named breach condition; every obligation is binary-evaluable (SHALL met / violated) with no interpretive judgment |
| **V-05** | Combined (all axes) | Three-path audit: PATH 1 (schema spine count), PATH 2 (WS-N headers), PATH 3 (EXIT slots); Schema B's Cal dependency = cross-schema contract term inherited by Schema C | Three orthogonal failure modes; Cal constraint is architecturally upstream via contract inheritance, not just a phase instruction |

**Key C-35 discriminator across variations:** V-01 makes it auditable by register row count. V-02 makes it gateable (Gate-S). V-03 makes it inertia-governed. V-04 names its breach conditions. V-05 makes it PATH 1 of a three-path system.
