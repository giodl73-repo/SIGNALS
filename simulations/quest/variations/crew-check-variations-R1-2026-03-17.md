Written to `simulations/quest/golden/crew-check-variate-R1-2026-03-17.md`.

---

## crew-check — Variate R1 Summary

Five complete prompt bodies, single-axis first then combinations:

| Variation | Axis | Hypothesis |
|-----------|------|-----------|
| **V-01** | Role sequence (severity-anchored) | Running challenger → product → technical causes each row to calibrate against the severity landscape already established — C-08 becomes structural, not luck-dependent |
| **V-02** | Output format (labeled blocks, not table) | Labeled key-value blocks per reviewer make C-02 impossible to violate — a missing label is visually immediate; tables let empty cells hide |
| **V-03** | Lifecycle emphasis (phase-gated, minimal prose) | Four phases with a single exit condition each — model cannot produce output before role loading is verified and depth is declared; C-01 and C-04 become guaranteed |
| **V-04** | Output format + Phrasing register | Schema-first contract with formal SHALL language makes blank cells structurally impossible and forces explicit role-lens anchoring before each finding (C-03) |
| **V-05** | Role sequence + Inertia framing + Lifecycle emphasis | Full integration: inertia mandate as Phase 1 exit condition, archetype-sequenced queue, phase gates — each rubric tier has a dedicated structural guarantee |

**What differs from crew-review R1**: crew-review explored inertia framing (V-01), schema-first format (V-02), and conversational register (V-03). These variations explore severity-anchored sequencing (V-01), labeled-block format (V-02), phase-gated lifecycle as the primary axis (V-03), and a new combination that pairs formal register with schema contracts (V-04). The full-integration V-05 is parallel in ambition but builds the inertia mandate into Phase 1's exit condition rather than as a step-2 instruction.
