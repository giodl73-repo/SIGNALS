Written to `simulations/quest/golden/topic-plan-variate-R9-2026-03-15.md`.

---

## Round 9 Summary

**New criteria targeted**: C-31 (named phase lifecycle template), C-32 (artifact-to-register calibration column), C-33 (inverted artifact sequence with upfront calibration), C-34 (compound audit structure)

**Single-axis variations:**

| Variation | Axis | Distinguishing mechanism |
|-----------|------|--------------------------|
| **V-01** | Lifecycle emphasis | `ENTRY / JOB / EXIT` as bold sub-sections with upfront `PHASE LIFECYCLE TEMPLATE` declaration; EXIT is the architectural home for every stop gate |
| **V-02** | Output format | Each phase prefaced by a three-row `Schema D` phase-spec table; all table schemas (defense register, inventory, proposals) declared upfront in a `TABLE SCHEMAS` section before Phase 0 |
| **V-03** | Inertia framing | Defense register as document spine; calibration column = "Defenses Implicated"; all language frames change as defense-defeat; banned forms are "inadmissible rebuttals" |

**Combined variations:**

| Variation | Axes | Distinguishing mechanism |
|-----------|------|--------------------------|
| **V-04** | Lifecycle + Role sequence | `PHASE LIFECYCLE TEMPLATE` as a standalone referenced section; `CALIBRATION PROTOCOL` as an inter-phase section between Phase 1 and Phase 2 — a named boundary, not a sub-task |
| **V-05** | All axes | `ARCHITECTURE DECLARATION` names both audit paths explicitly; calibration-first enforced as "ARCHITECTURAL CONSTRAINT" with a named violation condition (`SEQUENCE VIOLATION`); "Neither path is redundant" stated explicitly |

**Predicted discriminators for Round 10**: named violation condition (V-05), explicit independence assertion on audit paths (V-05), upfront schema declaration for calibration column (V-02), phases citing the lifecycle template by name (V-04).
