Now I have the full format picture. Writing the five complete variations.

---

# flow-throttle Variate — Round 5 (rubric v5 — C-15 role-locked pipeline, C-16 phase-boundary prohibition, C-17 enumeration anchor)

**Date:** 2026-03-16
**Rubric:** v5 (C-01 through C-17; 5 essential / 3 recommended / 9 aspirational; max composite 135)
**New in v5:** C-16 (phase-boundary data-lock prohibition statement) and C-17 (consequence-phase exhaustive enumeration anchor) — first appearance. C-15 (role-locked sequencing pipeline) carried from v4 with tighter definition.
**Baseline ceiling:** R4 V-05 — C-01 through C-15 reliable under role-sequence with positive handoffs; C-16 and C-17 absent.

---

## Round 5 State Analysis: What R4 Established vs. What R5 Must Add

**R4 confirmed (carry forward under role-sequence structure):**

- C-01 (primary bottleneck): reliable when binding constraint requires T-NN + numeric limit + causal reason.
- C-02 (backpressure 2+ hops): reliable under explicit hop table with mechanism column.
- C-03 (2+ tiers with enforcing component): reliable under Tier Registry table schema.
- C-04 (UX per tier): reliable when UX catalog table requires one row per T-NN.
- C-05 (unprotected burst path): reliable under a dedicated gap table requiring structural explanation.
- C-06 (retry-after handling): reliable when binding tier row requires Retry-After verdict plus failure-mode description.
- C-07 (cascading failure): reliable when cascade section requires named T-NN chain with causal links.
- C-08 (quantified thresholds): reliable when registry table prohibits vague labels — number + unit or explicit not-documented declaration.
- C-09 (mitigations with tradeoffs): reliable when mitigation entries require Change + Tradeoff fields by schema.
- C-10 (comparative severity ranking): reliable when ranking section requires every T-NN with explicit blast-radius or frequency justification.
- C-11 (threshold sourcing provenance): reliable when registry Source column prohibits inference and requires named-document attribution or explicit not-documented flag.
- C-12 (coverage self-verification): reliable when completion block enumerates every downstream section by canonical name with a count-match check.
- C-13 (pre-analysis source commitment): reliable under ROLE 1 / ROLE 2 separation with source register appearing before the first tier row.
- C-14 (binding constraint exclusivity): reliable when bottleneck step requires exactly one T-NN designation plus contrastive reasoning for at least one non-binding tier.
- C-15 (role-locked sequencing pipeline): reliable under R4 role-sequence structure — named roles (ROLE 1 / ROLE 2), explicit "ROLE 1 complete" gate, one-directional data dependency enforced by phase boundary rules. R4 handoff form: "ROLE 1 complete" + "ROLE 2 opens / every tier reference must use ROLE 1 T-NN identifiers." Satisfies C-15's requirement for named phases with explicit handoff statements and one-directional analysis dependency.

**R4 gaps (what Round 5 must close):**

C-16 — Phase-boundary data-lock prohibition: R4 ROLE boundaries close with positive completion statements and positive references ("ROLE 2 draws from ROLE 1 registry"; "every tier reference must use ROLE 1 T-NN identifiers"). These satisfy C-15's handoff requirement but fail C-16's prohibition requirement. The distinction is auditing posture: "draws from ROLE 1" tells the analyst what to use; "no new tier name, limit value, or source reference may be introduced after this line" names the violation class and makes downstream compliance verifiable by scanning, not by reconstructing intent. An analysis satisfies C-15 and fails C-16 when all handoffs are positive-only. Round 5 must introduce prohibition statements at each phase boundary — statements that name the specific data class forbidden downstream, not general appeals to use prior artifacts.

C-17 — Consequence-phase exhaustive enumeration anchor: R4 UX catalog tables require "one row per T-NN from the ROLE 1 registry" and include a coverage check ("state the row count; must match ROLE 1 registry count"). The count-match check detects omission after the fact but does not make coverage verifiable by construction — a reader must compare two numbers. C-17 requires the consequence section to open with an explicit enumeration anchor naming the specific prior artifact ("For each tier in the ROLE 1 Tier Registry:" or "Expanding the ROLE 1 registry row by row:"), making completeness auditable by reference before any row is filled. An output can satisfy C-04 (every tier has UX, count matches) while failing C-17 (no anchor makes the completeness mechanism visible). Round 5 must embed named enumeration anchors in consequence phase headers — not in completion blocks, but at the opening of each consequence section.

**Three questions this round answers:**

Q1 (V-01): Does placing the prohibition statement as a formal closing declaration within the role boundary itself — rather than as a separate phase-lock banner — produce C-16 compliance, or does the model produce the role gate ("ROLE 1 complete") without the prohibition, because the prohibition is a secondary instruction adjacent to the gate?

Q2 (V-02): Does organizing the entire output as named table artifacts with sequential identifiers (TABLE-A, TABLE-B, TABLE-C) cause the consequence sections to naturally reference prior artifacts by name — satisfying C-17's anchor requirement — or does the model reference the tables positionally ("the table above") rather than by canonical name?

Q3 (V-03): Does framing the phase-lock prohibition as the prompt's primary structural organizing principle — stating the prohibition architecture before any role or table definitions — produce more reliable C-16 compliance than embedding prohibitions within role boundary declarations?

---

## Variation Map

| Variation | Axis | New criteria targeted | Predicted composite |
|-----------|------|-----------------------|---------------------|
| V-01 | Single: role sequence — prohibition statement embedded as formal role-boundary closing declaration | C-16 | ~130/135 |
| V-02 | Single: output format — named table artifacts with sequential identifiers; consequence sections anchor by canonical table name | C-17 | ~130/135 |
| V-03 | Single: lifecycle emphasis — phase-lock prohibition as primary structural principle declared before role and table definitions | C-16 (stronger form) | ~130/135 |
| V-04 | Combined: role sequence + lifecycle emphasis + basic enumeration anchor | C-16 + C-17 | ~133/135 |
| V-05 | Maximum density: V-04 + inertia framing that names the three failure modes C-16/C-17 existence corrects | C-16 + C-17 reinforced | ~135/135 |

---

## V-01 — Single Axis: Role Sequence — Prohibition as Formal Role-Boundary Declaration

**Axis:** Role sequence — each ROLE boundary closes with a two-part formal declaration: a positive completion statement ("ROLE 1 complete") followed immediately by a prohibition statement that names the specific data class forbidden in downstream roles. The prohibition is not a banner or a separate section — it is syntactically paired with the role gate as the second half of the same closing block. The hypothesis is that embedding the prohibition inside the gate structure, rather than appending it as an advisory note, prevents the model from producing the gate without the prohibition: the gate form is incomplete without both parts.

**Hypothesis:** R4 prompts close ROLE 1 with a standalone "ROLE 1 complete" statement followed by phase boundary rules that describe what downstream roles may reference. The prohibition concept is present in spirit ("you may NOT introduce a new tier that was not registered in ROLE 1") but located in the opening phase boundary rules block, not in the role's closing declaration. When the role closing and the prohibition are structurally separated, the model may treat the closing as the gate and the prohibition rules as advisory context — producing a complete-looking handoff without the prohibition statement that C-16 requires. Pairing the prohibition with the gate as a two-part closing form forces the model to produce both parts as a unit. Risk: the model writes a compliant prohibition form ("no new tier name or limit value may be introduced after this line") but then immediately introduces new source references in ROLE 2 because the prohibition only names tier names and limit values.

---

**THE INERTIA PROBLEM**

Throttle failures reach production undiscovered because teams skip pre-deploy analysis. The reasoning: "it passed dev." The first incident is the discovery mechanism. That is the status quo this skill exists to displace.

What inertia conceals:

- The burst path no one tested because dev ran at 10% of production volume
- The Retry-After handler that was never implemented because the dev throttle never fired
- The cascade that compounds three tiers when the first connection limit fires during a spike
- The post-incident review that cannot verify what evidence was available before the analysis: source references added after the bottleneck was named, tier names introduced mid-analysis, UX conclusions that cannot be traced to any registered tier

The analysis below locks the evidence base before any claim is made. Evidence-phase data cannot migrate into the claim phase. Claim-phase data cannot migrate into the consequence phase. Each lock is declared as a prohibition, not an appeal to refer to prior artifacts.

---

You are a Connectors / Power Automate throughput domain expert with two distinct analytic perspectives. Execute them in strict sequence. ROLE 1 completes entirely before ROLE 2 opens.

**SIGNAL**

*(Insert scenario: describe the Power Automate flow topology, connector stack, trigger type, parallelism degree, and target request volume. Example: "A scheduled cloud flow runs every 5 minutes, reads up to 500 rows from SharePoint Online, processes each row via an Apply to Each with concurrency 10, and writes results to Dataverse. Target: 6,000 rows/hour, mid-size enterprise tenant.")*

---

**PHASE BOUNDARY RULES — read before beginning:**

- ROLE 1 covers: source registration, tier enumeration, numeric limits, enforcing components, scope, threshold provenance, and binding constraint designation. ROLE 1 does not cover: caller behavior, retry responses, error codes visible to flow authors, UX consequences, backpressure propagation.
- ROLE 2 covers: backpressure trace, UX catalog, retry-after gap, burst path analysis, cascade scenario, severity ranking, mitigation prescriptions. ROLE 2 may not introduce any tier, source reference, or limit value that ROLE 1 did not produce. Every tier reference in ROLE 2 must use the T-NN identifier assigned in ROLE 1.

---

### ROLE 1 — DOMAIN EXPERT

#### Step 1A — Source Register

Before any tier name or threshold appears, list every documentation source this analysis will draw from.

| Source-ID | Document name | Provides | Applies to this signal because |
|-----------|--------------|----------|-------------------------------|
| S-01 |  |  |  |
| S-02 |  |  |  |

Column definitions:

- `Document name` — exact name of the Microsoft documentation page, connector reference article, or platform SLA document. A URL alone fails; the canonical document name is required.
- `Provides` — what specific limit values or behavioral descriptions this source supplies (e.g., "per-connection request rate for SharePoint connector", "tenant-level daily action count for Power Automate E3").
- Minimum two sources. If a tier has no documented limit, the tier still receives a T-NN in Step 1B and its Threshold cell reads `not documented [reason]` where reason is one of: undocumented, environment-specific, requires-runtime-measurement. Inference is prohibited.

#### Step 1B — Tier Registry

Enumerate every rate-limit tier relevant to the described flow. Assign a permanent T-NN identifier to each.

| T-NN | Enforcing component | Scope | Threshold (number + unit) | Window type | Source-ID |
|------|---------------------|-------|--------------------------|-------------|-----------|
| T-01 |  |  |  |  |  |
| T-02 |  |  |  |  |  |

Column definitions:

- `T-NN` — permanent identifier. All downstream ROLE 2 references must use T-NN; informal name references without T-NN anchor are untracked.
- `Threshold` — a number with unit (e.g., 600 req/min, 10 concurrent connections). Vague labels ("throttled," "limited") are not thresholds. If undocumented, state `not documented [reason]` — do not infer a value.
- `Source-ID` — must match a Step 1A Source Register entry. A threshold without a Source-ID is an unattributed claim.
- Minimum two tiers with distinct enforcing components.

State: "Tier Registry complete — N tiers registered (T-01 through T-NN)." Seal the count before Step 1C.

#### Step 1C — Binding Constraint Designation

From the Step 1B Tier Registry:

1. Designate the single T-NN that will throttle first at the given volume. State: the T-NN, the threshold value, and the causal mechanism (why this tier binds before all others — the structural reason it hits its ceiling first at this load and topology).
2. For at least one non-binding tier: state the mechanism that prevents it from binding first (contrastive reasoning — the specific limit, scope, or window property that keeps it below its threshold when the binding tier fires).

Exactly one binding tier. Designating two tiers as co-equal binding constraints is not permitted.

---

> **ROLE 1 COMPLETE. No new tier name, source reference, or limit value may be introduced after this declaration. ROLE 2 is restricted to the T-NN identifiers and Source-IDs produced in Steps 1A–1C. A component appearing in ROLE 2 without a T-NN from Step 1B is a scope violation.**

---

### ROLE 2 — CALLER PERSPECTIVE

Open with: "ROLE 2 operating on N-tier registry (T-01 through T-NN)." N must match the ROLE 1 registry count.

#### Step 2A — Backpressure Propagation (minimum 2 hops)

| Hop | From (T-NN) | To component | Mechanism | Observable effect |
|-----|-------------|--------------|-----------|-------------------|
| 1 |  |  |  |  |
| 2 |  |  |  |  |

- `From (T-NN)` — must be a T-NN from the ROLE 1 registry. If a source component has no T-NN, halt and declare a scope violation before proceeding.
- `Mechanism` — must be one of: queue-fill, connection-hold, retry-amplification, dependency-stall, timeout-cascade, quota-exhaustion. Generic labels ("degraded," "affected") fail.
- Minimum two hops.

#### Step 2B — UX Consequence Catalog

**For each tier in the ROLE 1 Tier Registry — expand every T-NN row, omit none:**

| T-NN | Error code or signal | Retry-After present? (Y/N) | Retry-After honored by caller? (Y/N) | Visible in run history? (Y/N/SILENT) | Failure mode if Retry-After absent or ignored |
|------|---------------------|---------------------------|-------------------------------------|--------------------------------------|---------------------------------------------|
| T-01 |  |  |  |  |  |
| T-02 |  |  |  |  |  |

Coverage check: row count must match ROLE 1 registry count. State the match explicitly.

#### Step 2C — Unprotected Burst Path

| Path-ID | Entry component | Structural gap reason | T-NNs bypassed or overwhelmed | Consequence at burst volume |
|---------|----------------|----------------------|-------------------------------|------------------------------|
| P-01 |  |  |  |  |

- `T-NNs bypassed` — ROLE 1 identifiers only. If no T-NN applies, halt and declare a scope violation.
- `Structural gap reason` — must be a structural explanation: missing concurrency cap, trigger type exempt from gateway throttle, path that bypasses queue, no per-instance limit on this connector invocation.

#### Step 2D — Cascade Scenario

Trace one cascade starting from the ROLE 1 binding constraint. Format: `T-NN (binding, limit: X) → T-NN (mechanism) → T-NN (mechanism)`. Minimum two downstream hops with explicit causal links. State the compounded throughput or error-rate effect. Use T-NN identifiers throughout; informal names without T-NN anchors are untracked.

#### Step 2E — Retry-After Gap Assessment

For the ROLE 1 binding constraint tier: is Retry-After handling implemented in the caller? If absent: name the failure mode precisely (retry storm, missing exponential backoff, silent quota exhaustion, immediate re-queue within the same throttle window). If present: state whether the Retry-After value is being read and respected or treated as a static delay.

#### Step 2F — Severity Ranking

Rank all ROLE 1 T-NN tiers by user-visible severity, highest to lowest. Every T-NN must appear. For each: T-NN, severity tier (High/Medium/Low), justification referencing blast radius or impact frequency. For the top-ranked tier: additionally state blast radius, failure visibility (silent vs. explicit), and estimated recovery time.

Coverage check: T-NN count must match ROLE 1 registry count. State explicitly.

#### Step 2G — Mitigation Prescriptions

For at least two gaps identified in Steps 2A–2F:

**GAP-01:**
- Gap: [T-NN or Path-ID, failure mode, which step surfaced it]
- Component to modify: [specific named component — not a layer or a system category]
- Change: [setting name + value, or retry policy with concrete parameters]
- Expected outcome: [quantified or behaviorally observable change]
- Tradeoff: [specific cost — latency added, throughput ceiling lowered, operational overhead]

**GAP-02:**
- Gap: [T-NN or Path-ID, failure mode, which step surfaced it]
- Component to modify: [specific named component]
- Change: [setting name + value]
- Expected outcome: [quantified or observable]
- Tradeoff: [specific cost]

---

> **ROLE 2 COMPLETE. No new tier names, source references, or limit values were introduced after the ROLE 1 declaration. Any scope violations encountered were declared before proceeding.**

---

**COMPLETION VERIFICATION**

```
ROLE 1 registry count:                                       [N tiers]
Step 2B UX catalog row count:                                [N rows — must match]
Step 2F severity ranking entry count:                        [N entries — must match]
ROLE 1 complete before ROLE 2 opened:                        [ ]
ROLE 2 complete before consequence verification:             [ ]
ROLE 1 prohibition statement present (not documentation):    [exact text of prohibition]
ROLE 2 prohibition statement present:                        [exact text of prohibition]
C-14: binding tier designated with contrastive reasoning:    [ ]
C-13: Source-ID in every Step 1B threshold:                  [ ]
Scope violations declared (0 = clean):                       [count]
```

---

## V-02 — Single Axis: Output Format — Named Table Artifacts with Sequential Identifiers

**Axis:** Output format — all output is organized as explicitly named, sequentially identified tables (TABLE-A through TABLE-F). Each table has a canonical name that later sections reference by that exact name. Consequence sections open with explicit cross-references to prior tables: "Expanding TABLE-A Tier Registry row by row:" and "For each entry in TABLE-A Tier Registry, mitigations follow from TABLE-E Gap Inventory." The prohibition at phase boundaries is positive-only (R4 form: "draws from TABLE-A"), not a prohibition statement — C-16 is not targeted. The anchor mechanism for C-17 comes from table-naming, not from a named prohibition architecture.

**Hypothesis:** C-17 requires the consequence section to name the specific prior artifact ("For each tier in the Phase 1 Tier Inventory") not a vague class ("for each tier"). When the entire output is organized as sequentially named tables, the natural language for referencing prior tiers is "TABLE-A row T-NN" — a named artifact reference by construction. The risk with R4's approach is that consequence sections open with "One row per T-NN from the ROLE 1 registry" which names a phase (ROLE 1) but not a specific artifact (the registry table itself). Naming the table (TABLE-A Tier Registry) makes the anchor concrete: a reader verifying coverage can locate TABLE-A, count its rows, and verify that the consequence section has one entry per row without reconstructing the analysis phases. Primary risk: the model references tables positionally ("the table above", "as shown in the previous section") rather than by canonical table name, preserving the structural form but failing the named-anchor requirement.

---

**THE INERTIA PROBLEM**

Throttle failures reach production undiscovered because the system passed at development and staging volumes. Teams reason: "it worked in dev" — and skip the analysis. The first production incident is the discovery mechanism. That is the status quo this skill exists to displace.

What inertia conceals:

- The burst path that was never rate-limited because no one tested at the volume where it fires
- The missing Retry-After handler that turns a 30-second throttle window into a 10-minute retry storm that exhausts quota across the tenant
- The cascade that takes down three dependent flows when a single connector limit fires
- The post-incident review that finds the UX analysis cannot be verified: no artifact name anchors the consequence table to the tier inventory; completeness requires reading both documents end-to-end rather than checking a named cross-reference

The analysis below produces six named tables. Every consequence section names the specific table it expands. Completeness is verifiable by table reference.

---

You are a Connectors / Power Automate throughput domain expert. Produce the following six tables in order. Each table has a canonical name — use it verbatim in any cross-reference. Consequence tables must open with an explicit named reference to the source table ("Expanding TABLE-A Tier Registry row by row" or "For each entry in TABLE-A Tier Registry"). Do not reference tables positionally ("the table above") — reference them by canonical name.

**SIGNAL**

*(Insert scenario.)*

---

### TABLE-A — Source Register

*Complete before TABLE-B. TABLE-B thresholds must cite a TABLE-A Source-ID or state `not documented [reason]`.*

| Source-ID | Document name | Provides | Applies to this signal because |
|-----------|--------------|----------|-------------------------------|
| S-01 |  |  |  |
| S-02 |  |  |  |

Minimum two entries. State: "TABLE-A Source Register closed — N sources registered." before opening TABLE-B. TABLE-B may not reference any source not in TABLE-A.

---

### TABLE-B — Tier Registry

*Complete before any analysis section. This is the authoritative tier enumeration. No tier may appear in TABLE-C through TABLE-F that does not have a row in TABLE-B.*

| T-NN | Enforcing component | Scope | Threshold (number + unit) | Window type | Source-ID (TABLE-A) | Binding? (at most one Y) |
|------|---------------------|-------|--------------------------|-------------|---------------------|--------------------------|
| T-01 |  |  |  |  |  |  |
| T-02 |  |  |  |  |  |  |

Column definitions:

- `Threshold` — a number with unit, or `not documented [reason]`. Inference prohibited.
- `Source-ID` — must match a TABLE-A entry.
- `Binding?` — at most one Y across all rows. The row with Y is the binding constraint. State the causal mechanism and contrastive reasoning for at least one non-binding tier in the row or immediately below the table.
- Minimum two tiers.

State: "TABLE-B Tier Registry closed — N tiers registered (T-01 through T-NN)." TABLE-C through TABLE-F may not introduce new T-NN identifiers.

---

### TABLE-C — Backpressure Propagation Chain

*Draws from TABLE-B binding tier designation. If a component appears here without a TABLE-B T-NN, halt and declare a scope violation before continuing.*

| Hop | From (TABLE-B T-NN) | To component | Mechanism | Observable effect |
|-----|---------------------|--------------|-----------|-------------------|

Minimum two rows. Mechanism vocabulary: queue-fill, connection-hold, retry-amplification, dependency-stall, timeout-cascade, quota-exhaustion. Generic entries fail.

---

### TABLE-D — UX Consequence Matrix

*Expanding TABLE-B Tier Registry row by row — for each entry in TABLE-B Tier Registry, one consequence row. No TABLE-B tier may be omitted. No tier absent from TABLE-B may appear here.*

| TABLE-B T-NN | Error code or signal | Retry-After present? (Y/N) | Retry-After honored? (Y/N) | Visible in run history? (Y/N/SILENT) | Failure mode if Retry-After absent |
|--------------|---------------------|---------------------------|---------------------------|--------------------------------------|-----------------------------------|
| T-01 |  |  |  |  |  |
| T-02 |  |  |  |  |  |

Coverage check: state row count. Must match TABLE-B tier count.

---

### TABLE-E — Gap Inventory

| Gap-ID | Gap type | T-NN or Path-ID (TABLE-B) | Structural description | Risk tier (H/M/L) | Blast radius or frequency justification |
|--------|----------|--------------------------|----------------------|-------------------|-----------------------------------------|
| G-01 | Unprotected burst path |  |  |  |  |
| G-02 | Retry-after handling failure |  |  |  |  |
| G-03 | Cascading throttle scenario |  |  |  |  |

Minimum G-01 (burst path with structural gap reason), G-02 (retry-after assessment for binding tier), G-03 (cascade naming at least two TABLE-B T-NN tiers with causal links). Add rows for additional gaps.

---

### TABLE-F — Mitigation Register

*For each entry in TABLE-E Gap Inventory — one mitigation row. No G-ID may be omitted. No gap absent from TABLE-E may appear here.*

| TABLE-E Gap-ID | Component to modify | Change (setting or API parameter + value) | Expected outcome | Tradeoff |
|----------------|--------------------|-----------------------------------------|------------------|----------|
| G-01 |  |  |  |  |
| G-02 |  |  |  |  |
| G-03 |  |  |  |  |

Tradeoff is required for every row — a mitigation without a named tradeoff is incomplete.

---

### Severity Ranking

*For each entry in TABLE-B Tier Registry, ranked highest to lowest user-visible severity:*

Rank: T-NN (severity) — justification referencing TABLE-E gap entries or TABLE-D failure modes.

Coverage check: T-NN count must match TABLE-B count.

---

**COMPLETION VERIFICATION**

```
TABLE-A source count:                                     [N]
TABLE-B tier count:                                       [N]
TABLE-D row count (must match TABLE-B):                   [N]
TABLE-F row count (must match TABLE-E):                   [N]
Severity ranking T-NN count (must match TABLE-B):         [N]
TABLE-B introduced no tiers absent from TABLE-A sources:  [ ]
TABLE-D anchor references "TABLE-B Tier Registry" by name:[exact text of opening line]
TABLE-F anchor references "TABLE-E Gap Inventory" by name:[exact text of opening line]
Severity ranking anchor references TABLE-B by name:       [exact text of opening line]
Scope violations (T-NN outside TABLE-B appearing in C–F): [count]
```

---

## V-03 — Single Axis: Lifecycle Emphasis — Phase-Lock Prohibition as Primary Structural Principle

**Axis:** Lifecycle emphasis — the prompt opens with a DATA LOCK ARCHITECTURE declaration before any role definition, table schema, or content instruction. The architecture section states: (a) why positive-reference handoffs ("draws from Phase 1") are insufficient for verifiable phase compliance; (b) what a prohibition statement is and why it differs from a completion statement; and (c) the specific prohibition form required at each phase boundary. The prompt then defines roles and tables as implementations of this architecture, not the other way around. C-17 named enumeration anchors appear as instructions but are not the structural centerpiece. The single-axis test: does framing the prohibition as the primary organizing principle — rather than as a closure decoration appended to role gates — produce more reliable C-16 compliance than V-01's paired-gate form?

**Hypothesis:** V-01 embeds the prohibition within the role-closing declaration as a formal two-part gate. The risk is that the role gate ("ROLE 1 complete") is the salient structural event and the prohibition is the appended second clause — the model may produce a well-formed gate without recognizing the prohibition as load-bearing. V-03 inverts the hierarchy: the prohibition architecture is the first substantive content the model encounters, and the roles are introduced as the mechanism for implementing that architecture. When the prohibition is the frame rather than the decoration, the model's structural commitment is to the lock, not to the role sequence. Primary risk: the model reads the architecture declaration as preamble and produces the same role-gate form as V-01 — generating the prohibition text because the architecture section described it, but not anchoring consequence sections by artifact name (C-17 partial).

---

**THE INERTIA PROBLEM**

Throttle failures reach production undiscovered because the system passed at development and staging volumes. Teams reason: "it worked in dev" — and skip the analysis. The first production incident is the discovery mechanism. That is the status quo this skill exists to displace.

What inertia conceals:

- The burst path that was never rate-limited because no one tested at the volume where it fires
- The missing Retry-After handler that turns a 30-second throttle window into a 10-minute retry storm that exhausts quota across the tenant
- The cascade that takes down three dependent flows when a single connector limit fires
- The analysis whose phase boundaries cannot be verified after the fact: the handoff said "draws from Phase 1" and the review team cannot determine whether any Phase 2 numbers were introduced before the Phase 1 evidence base was complete — because "draws from" is a direction, not a detectable constraint

This analysis is organized around a DATA LOCK ARCHITECTURE. The locks are prohibition statements. They are not completion statements.

---

**DATA LOCK ARCHITECTURE**

This analysis uses phase-locked evidence flow. Two phase boundaries exist. Each boundary is enforced by a prohibition statement.

**What a prohibition statement is:**

A prohibition statement names a specific class of data that may not appear after the boundary. Form: "No new [data class] may be introduced after this line." A completion statement ("Phase 1 complete") marks an event. A positive handoff ("Phase 2 draws from Phase 1") describes a direction. Neither is a prohibition. A prohibition makes violations detectable: a reader can scan all content after the boundary and determine compliance by checking whether the named data class appears — without reconstructing the analysis sequence.

**The two locks:**

> **DATA LOCK 1 — EVIDENCE BASE CLOSED:**
> No new source name, tier name, or limit value may be introduced after this boundary. This lock fires at the end of Phase 1. Any claim, derivation, or propagation trace in Phase 2 that references a source or tier not present in the Phase 1 output is a lock violation.

> **DATA LOCK 2 — ANALYTICAL CLAIMS CLOSED:**
> No new tier name, limit value, or gap description may be introduced after this boundary. This lock fires at the end of Phase 2. Phase 3 consequence entries must correspond to tiers and gaps produced in Phase 1 and Phase 2 respectively. A tier appearing in the Phase 3 UX catalog that was not registered in Phase 1 is a lock violation.

These locks are reproduced verbatim at their respective phase boundaries below.

---

You are a Connectors / Power Automate throughput domain expert. Execute the three phases below in strict order. Reproduce each DATA LOCK verbatim at its boundary. Do not paraphrase.

**SIGNAL**

*(Insert scenario.)*

---

### PHASE 1 — EVIDENCE COMMITMENT

*Scope: source registration, tier enumeration, numeric limits, enforcing components, threshold provenance, binding constraint designation. Does not cover: caller behavior, retry handling, UX consequences, backpressure propagation.*

#### Step 1A — Source Register

| Source-ID | Document name | Provides | Applies to this signal because |
|-----------|--------------|----------|-------------------------------|

Minimum two entries. No tier threshold in Step 1B may appear without a Source-ID that matches a row here. If undocumented: `not documented [reason]` — not an inferred value.

#### Step 1B — Tier Registry

| T-NN | Enforcing component | Scope | Threshold (number + unit) | Window type | Source-ID |
|------|---------------------|-------|--------------------------|-------------|-----------|

Minimum two tiers. `T-NN` identifiers are permanent — all Phase 2 and Phase 3 references must use them.

#### Step 1C — Binding Constraint

Designate exactly one T-NN as the binding constraint. State: causal mechanism (why this tier binds before all others). Provide contrastive reasoning for at least one non-binding tier (the mechanism that prevents it from binding first under the described load).

---

> **DATA LOCK 1 — EVIDENCE BASE CLOSED:**
> No new source name, tier name, or limit value may be introduced after this line. Phase 2 analysis and Phase 3 consequences are restricted to the T-NN identifiers and Source-IDs produced in Phase 1. Any component appearing in Phase 2 or Phase 3 without a Phase 1 T-NN is a lock violation.

---

### PHASE 2 — ANALYTICAL CLAIMS

*Scope: backpressure trace, burst paths, cascade scenario, retry-after gap. Does not cover: new tier introduction, new source citation. Every tier reference must use Phase 1 T-NN identifiers.*

#### Step 2A — Backpressure Propagation (minimum 2 hops)

| Hop | From (T-NN) | To component | Mechanism | Observable effect |
|-----|-------------|--------------|-----------|-------------------|

Mechanism vocabulary: queue-fill, connection-hold, retry-amplification, dependency-stall, timeout-cascade, quota-exhaustion.

#### Step 2B — Unprotected Burst Path

Identify at least one structural gap where burst traffic bypasses or overwhelms throttle controls. Name the entry component, the structural gap reason, the T-NNs affected, and the consequence at burst volume.

#### Step 2C — Retry-After Gap Assessment

For the binding constraint tier: is Retry-After handling implemented in the caller? If absent: name the failure mode precisely (retry storm, missing exponential backoff, silent quota exhaustion, immediate re-queue within same throttle window). If present: state whether Retry-After value is read and respected.

#### Step 2D — Cascade Scenario

Trace one cascade starting from the binding constraint. Minimum two downstream hops with T-NN identifiers and explicit causal links. State the compounded throughput or error-rate effect.

---

> **DATA LOCK 2 — ANALYTICAL CLAIMS CLOSED:**
> No new tier name, limit value, or gap description may be introduced after this line. Phase 3 consequence entries are restricted to the tiers registered in Phase 1 and the gaps identified in Phase 2. A tier appearing in Phase 3 without a Phase 1 T-NN is a lock violation.

---

### PHASE 3 — CONSEQUENCES

*Scope: UX consequences, mitigations, severity ranking. Every entry references Phase 1 tier identifiers or Phase 2 gap descriptions. No new evidence or analytical claims.*

#### Step 3A — UX Consequence Catalog

**For each tier in the Phase 1 Tier Registry — one row per T-NN, no omissions:**

| T-NN | Error code or signal | Retry-After present? (Y/N) | Honored by caller? (Y/N) | Visible in run history? | Failure mode if Retry-After absent |
|------|---------------------|---------------------------|-------------------------|-------------------------|-----------------------------------|

Coverage check: row count must match Phase 1 tier count. State explicitly.

#### Step 3B — Mitigation Prescriptions

For each gap identified in Phase 2 (Steps 2B, 2C, 2D at minimum):

- Gap: [T-NN or gap description from Phase 2]
- Component to modify: [named component]
- Change: [setting + value or policy with concrete parameters]
- Expected outcome: [quantified or observable]
- Tradeoff: [specific cost — required]

#### Step 3C — Severity Ranking

Rank all Phase 1 T-NN tiers by user-visible severity. Every T-NN must appear. Justify each rank with reference to blast radius or impact frequency. Coverage check: T-NN count must match Phase 1 count.

---

**COMPLETION VERIFICATION**

```
Phase 1 tier count:                                          [N]
Phase 3 UX catalog row count (must match):                   [N]
Phase 3 severity ranking count (must match):                 [N]
DATA LOCK 1 reproduced verbatim:                             [ ]
DATA LOCK 2 reproduced verbatim:                             [ ]
Phase 2 introduced no new T-NN (0 violations):               [count]
Phase 3 introduced no new T-NN (0 violations):               [count]
Phase 3 UX anchor names "Phase 1 Tier Registry":             [exact opening line]
Phase 3 mitigation gap citations all trace to Phase 2:       [ ]
```

---

## V-04 — Combined: Role Sequence + Lifecycle Emphasis + Basic Enumeration Anchor

**Axis:** Combines V-01's prohibition-embedded-in-role-gate form with V-03's upfront DATA LOCK ARCHITECTURE declaration, and adds explicit named enumeration anchors in consequence section headers. This is the first variation to target all three: C-15 (role-locked pipeline, already reliable from prior rounds), C-16 (prohibition statements from V-01 + V-03 mechanisms), and C-17 (named enumeration anchors). The inertia framing is not the structural centerpiece — that is reserved for V-05.

**Hypothesis:** V-01 places the prohibition inside the role gate; V-03 declares the prohibition architecture before the roles. Each single-axis variation tests one mechanism in isolation. V-04 combines both: the architecture declaration primes the model to treat prohibitions as primary, and the embedded gate form ensures the prohibition appears even if the architecture section is treated as preamble. Adding explicit "For each tier in [named artifact]" anchor text in Step 3 headers closes C-17 without requiring the table-naming infrastructure of V-02. The combined form is expected to achieve PASS on C-16 and C-17 simultaneously. Risk: the two prohibition mechanisms reinforce each other strongly enough that the enumeration anchor becomes the weakest link — the model produces the "for each tier" text but omits the specific artifact name, satisfying the form without the verifiability requirement C-17 actually tests.

---

**THE INERTIA PROBLEM**

Throttle failures reach production undiscovered because the system passed at development and staging volumes. Teams reason: "it worked in dev" — and skip the analysis. The first production incident is the discovery mechanism. That is the status quo this skill exists to displace.

What inertia conceals:

- The burst path that was never rate-limited because no one tested at the volume where it fires
- The missing Retry-After handler that turns a 30-second throttle window into a 10-minute retry storm
- The cascade that compounds three tiers when the first connection limit fires
- The post-incident audit that cannot verify phase compliance because the handoff was a completion statement ("ROLE 1 complete"), not a prohibition — and coverage verification required reconstructing the full analysis rather than checking a named artifact reference

The analysis below uses a phase-lock architecture with prohibition statements at every boundary and named artifact anchors at every consequence section opening.

---

**PHASE-LOCK ARCHITECTURE — READ BEFORE BEGINNING**

This analysis enforces two data locks. Each lock is a prohibition statement, not a completion statement. The distinction: a prohibition names the data class that may not appear downstream, making compliance verifiable by scanning. A completion statement marks an event; it cannot be verified by inspection.

**Lock form required at each phase boundary:**

> "No new [specific data class] may be introduced after this boundary. All [phase] content must trace to [named prior artifact]."

General statements ("no new information") fail because they do not name a specific verifiable data class. The prohibition must name at least one of: tier name, limit value, source reference, gap description.

---

You are a Connectors / Power Automate throughput domain expert with three analytic phases. Execute in strict sequence. ROLE 1 completes before ROLE 2 opens. ROLE 2 completes before ROLE 3 opens.

**SIGNAL**

*(Insert scenario.)*

---

### ROLE 1 — DOMAIN EXPERT: Evidence Commitment

*Covers: source registration, tier enumeration, limit values, provenance, binding constraint. Does not cover: caller behavior, retry signals, UX, backpressure.*

#### 1.1 Source Register

| Source-ID | Document name | Provides | Applies to this signal because |
|-----------|--------------|----------|-------------------------------|

Minimum two entries. Every threshold in the Tier Registry must cite a Source-ID here, or declare `not documented [reason]`. No inference.

#### 1.2 Tier Registry

| T-NN | Enforcing component | Scope | Threshold (number + unit) | Window type | Source-ID |
|------|---------------------|-------|--------------------------|-------------|-----------|

T-NN identifiers are permanent. Minimum two tiers. No tier introduced in later roles without a T-NN here is in scope.

#### 1.3 Binding Constraint

Designate exactly one T-NN as binding. State: causal mechanism. Contrastive reasoning for at least one non-binding tier: the specific mechanism that prevents it from binding first.

---

> **ROLE 1 COMPLETE. No new tier name, source reference, or limit value may be introduced after this line. Roles 2 and 3 are restricted to the Source-IDs and T-NN identifiers produced in Sections 1.1–1.3.**

---

### ROLE 2 — ARCHITECT: Analytical Claims

*Draws exclusively from ROLE 1 artifacts. Does not cover: UX consequences, mitigations, severity ranking. Every tier reference uses ROLE 1 T-NN identifiers.*

Open with: "ROLE 2 operating on N-tier ROLE 1 registry (T-01 through T-NN)."

#### 2.1 Backpressure Propagation (minimum 2 hops)

| Hop | From (T-NN) | To component | Mechanism | Observable effect |
|-----|-------------|--------------|-----------|-------------------|

Mechanism vocabulary: queue-fill, connection-hold, retry-amplification, dependency-stall, timeout-cascade, quota-exhaustion. `From (T-NN)` must match a ROLE 1 tier; if absent, halt and declare scope violation.

#### 2.2 Unprotected Burst Path

At least one structural gap: entry component, gap reason (structural — not volumetric), T-NNs affected, consequence at burst volume.

#### 2.3 Retry-After Gap

Binding constraint tier: is Retry-After implemented in the caller? If absent: precise failure mode. If present: does the caller read the Retry-After value or apply a static delay?

#### 2.4 Cascade Scenario

One cascade from binding constraint. Format: `T-NN → T-NN (mechanism) → T-NN (mechanism)`. Minimum two downstream hops with explicit causal links. State compounded effect.

---

> **ROLE 2 COMPLETE. No new tier name, limit value, source reference, or gap type may be introduced after this line. ROLE 3 is restricted to tiers registered in ROLE 1 and gaps identified in ROLE 2. A tier in ROLE 3 without a ROLE 1 T-NN is a scope violation.**

---

### ROLE 3 — UX ADVOCATE: Consequence Derivation

*Draws from ROLE 1 tier registry and ROLE 2 analytical claims only.*

#### 3.1 UX Consequence Matrix

**For each tier in the ROLE 1 Tier Registry (Sections 1.1–1.2) — one row per T-NN, expanding the registry in registration order, omitting none:**

| ROLE 1 T-NN | Error code or signal | Retry-After present? (Y/N) | Honored by caller? (Y/N) | Visible in run history? (Y/N/SILENT) | Failure mode if Retry-After absent |
|-------------|---------------------|---------------------------|-------------------------|--------------------------------------|-----------------------------------|

Coverage check: row count must match ROLE 1 registry count. State the match.

#### 3.2 Mitigation Register

**For each gap identified in ROLE 2 (Sections 2.2–2.4) — one mitigation per gap, referencing the ROLE 2 section that surfaced it:**

- ROLE 2 gap source: [section + gap description]
- Component to modify: [named component]
- Change: [setting + value or policy with concrete parameters]
- Expected outcome: [quantified or observable]
- Tradeoff: [specific cost — required]

#### 3.3 Severity Ranking

**For each tier in the ROLE 1 Tier Registry — ranked highest to lowest user-visible severity:**

Rank: T-NN (severity H/M/L) — justification referencing blast radius or impact frequency, citing ROLE 2 gap entries where applicable.

Coverage check: T-NN count must match ROLE 1 registry count.

---

**COMPLETION VERIFICATION**

```
ROLE 1 registry count:                                           [N]
ROLE 3 UX matrix row count (must match):                         [N]
ROLE 3 severity ranking count (must match):                      [N]
ROLE 1 prohibition statement — text:                             [exact text]
ROLE 2 prohibition statement — text:                             [exact text]
ROLE 3 UX anchor names "ROLE 1 Tier Registry" specifically:      [exact opening line]
ROLE 3 mitigation anchor names "ROLE 2" sections specifically:   [exact opening line]
ROLE 3 severity anchor names "ROLE 1 Tier Registry" specifically:[exact opening line]
Scope violations (T-NN introduced outside ROLE 1):               [count]
C-14: binding designation + contrastive reasoning present:       [ ]
C-13: all thresholds attributed to Source-IDs:                   [ ]
```

---

## V-05 — Maximum Density: Role Sequence + Prohibition Architecture + Named Anchors + Inertia Framing for Three Structural Failures

**Axis:** Combines V-04's full structure (three-role pipeline, prohibition-in-gate + architecture-declaration, named enumeration anchors) with targeted inertia framing that names three specific structural failures: *retroactive citation* (the evidence base was not locked before claims were built — C-13/C-16 failure mode), *assertion without elimination* (a binding tier was named without explaining why alternatives were not binding — C-14 failure mode), and *coverage by coincidence* (every tier happened to receive a UX entry but no mechanism made completeness auditable — C-17 failure mode). The inertia framing activates C-14's contrastive reasoning requirement organically: the model enters the binding constraint step already primed to understand that naming a tier without eliminating alternatives is the exact failure the skill exists to prevent. V-05 tests whether the three-failure inertia frame produces higher reliability on C-14, C-16, and C-17 together than V-04's structure alone.

**Hypothesis:** V-04 achieves C-16 and C-17 compliance through structural enforcement alone — prohibition statements embedded in role gates and explicit "for each tier in [named artifact]" anchors in consequence headers. The binding constraint step in V-04 achieves C-14 by explicit instruction ("contrastive reasoning required"). V-05's inertia framing converts these three instructions from "things to do" into "failures this analysis corrects" — each structural element is introduced as the specific counter to a named failure mode. The hypothesis: when the model understands that *not* producing a prohibition statement produces "retroactive citation" (a named failure), and that *not* naming a specific artifact produces "coverage by coincidence" (a named failure), compliance is driven by failure-avoidance framing rather than instruction-following alone. This framing is more robust to structural drift because the failure modes are named at the top and the structural requirements are introduced as the remedy. Risk: the three-failure framing front-loads conceptual content that the model treats as preamble — reading past it to reach the role definitions — producing the same result as V-04.

---

**THE INERTIA PROBLEM**

Throttle failures reach production undiscovered because the system passed at development and staging volumes. Teams reason: "it worked in dev" — and skip the analysis. The first production incident is the discovery mechanism. That is the status quo this skill exists to displace.

What inertia conceals:

- The burst path that was never rate-limited because no one tested at the volume where it fires
- The missing Retry-After handler that turns a 30-second throttle window into a 10-minute retry storm that exhausts quota across the tenant
- The cascade that takes down three dependent flows when a single connector limit fires

But there is a second-order inertia specific to analysis itself: the failure of the analysis, not the failure of the flow. Three structural failures produce analyses that cannot be verified and whose conclusions cannot be trusted:

**Failure 1 — Retroactive citation.** A number appears; a source is found afterward. The evidence base was assembled during the analysis, not before it. The reader cannot determine whether the source was consulted before the claim was formed or located to justify it after. Counter: lock the evidence base before any tier name or limit value appears. The lock must be a prohibition — not a completion statement, not a positive reference. "No new source reference may appear after this line" is verifiable; "draws from the above register" is not.

**Failure 2 — Assertion without elimination.** A tier is called primary. No competing tier is eliminated. The designation is a choice, not an argument — the reader cannot verify whether any other tier was considered. Counter: for the binding constraint, name the causal mechanism that makes it primary *and* name the mechanism that prevents at least one alternative from being primary first. Two tiers, two different reasons — then the designation is an argument.

**Failure 3 — Coverage by coincidence.** Every tier received a UX entry. But the analyst reached this result without any mechanism that would have caught an omission. A different analyst, on a different day, might have omitted a tier and the count-match check would not have fired because the total was still N. Counter: the consequence section opens with an explicit anchor to the specific prior artifact: "For each tier in [named prior registry]." The anchor makes omission detectable before any row is filled.

This analysis is structured to correct all three failures. The structural corrections are not optional: they are the three locks the evidence-lock architecture enforces.

---

**EVIDENCE-LOCK ARCHITECTURE**

Three phases. Two data locks. Each lock is a prohibition statement with a named data class. Consequence sections open with named artifact anchors.

The model produces this architecture in the output. The locks are reproduced verbatim at their phase boundaries. The anchors appear as the first line of each consequence section.

---

You are a Connectors / Power Automate throughput domain expert. Execute the three roles in strict sequence. Reproduce the DATA LOCK declarations verbatim at their phase boundaries. Open each ROLE 3 section with the anchor text as specified.

**SIGNAL**

*(Insert scenario.)*

---

### ROLE 1 — DOMAIN EXPERT: Evidence Commitment

*Corrects Failure 1 (retroactive citation). The evidence base is committed here, before any analytical claim. Every source is registered before any tier appears. Every tier is registered before any backpressure trace begins.*

*Scope: source registration, tier enumeration, numeric limits, enforcing components, threshold provenance, binding constraint. Does not cover: caller behavior, Retry-After signals, UX consequences, backpressure propagation, cascade scenarios.*

#### 1.1 Source Register

| Source-ID | Document name | Provides | Applies to this signal because |
|-----------|--------------|----------|-------------------------------|

Minimum two entries. Every threshold in the Tier Registry must cite a Source-ID registered here. If no source exists for a tier: `not documented [reason]` — where reason is one of: undocumented, environment-specific, requires-runtime-measurement. Inference is prohibited. A number without a Source-ID is a retroactive-citation failure.

#### 1.2 Tier Registry

| T-NN | Enforcing component | Scope | Threshold (number + unit) | Window type | Source-ID |
|------|---------------------|-------|--------------------------|-------------|-----------|

Column definitions:

- `T-NN` — permanent identifier (T-01, T-02, …). Every downstream reference to a throttle tier in Roles 2 and 3 must use this T-NN. A tier name without a T-NN is an untracked reference.
- `Threshold` — number with unit, or `not documented [reason]`. No vague labels. No inferred values.
- `Source-ID` — must match a 1.1 Source Register entry.
- Minimum two tiers with distinct enforcing components.

State: "Tier Registry complete — N tiers registered (T-01 through T-NN)." before Section 1.3.

#### 1.3 Binding Constraint

*Corrects Failure 2 (assertion without elimination).*

Designate exactly one T-NN as the binding constraint:

1. **Designation**: which T-NN, the threshold value exceeded, and the causal mechanism — the structural reason this tier reaches its ceiling before all others at the given volume and topology.
2. **Elimination**: for at least one non-binding tier, name the mechanism that prevents it from binding first. This is not optional. A designation without elimination is an assertion; an assertion cannot be verified.

Exactly one binding tier. Two co-equal designations is not permitted.

---

> **DATA LOCK 1 — EVIDENCE BASE CLOSED: No new source name, tier name, or limit value may be introduced after this line. Roles 2 and 3 are restricted to the Source-IDs and T-NN identifiers produced in Sections 1.1–1.3. A source or tier appearing in Roles 2 or 3 without a Role 1 registration is a retroactive-citation violation.**

---

### ROLE 2 — ARCHITECT: Analytical Claims

*Draws exclusively from Role 1 artifacts. Does not introduce new source registrations, tier names, or limit values. Every tier reference uses Role 1 T-NN identifiers.*

Open with: "ROLE 2 operating on N-tier Role 1 registry (T-01 through T-NN)." N must match Role 1 registry count.

#### 2.1 Backpressure Propagation (minimum 2 hops)

| Hop | From (T-NN) | To component | Mechanism | Observable effect |
|-----|-------------|--------------|-----------|-------------------|

- `From (T-NN)` — must be a Role 1 T-NN. If a component has no T-NN, halt and declare: "SCOPE VIOLATION — [component name] not in Role 1 registry. Analysis halted pending Role 1 correction."
- `Mechanism` — queue-fill, connection-hold, retry-amplification, dependency-stall, timeout-cascade, quota-exhaustion.
- Minimum two hops with mechanism and observable effect per hop.

#### 2.2 Unprotected Burst Path

At least one structural gap where burst traffic bypasses or overwhelms throttle controls. State: entry component, structural gap reason (must be structural — not volumetric: missing cap, trigger type exempt, path bypasses gateway, no per-instance limit), Role 1 T-NNs affected, consequence at burst volume.

#### 2.3 Retry-After Gap Assessment

For the Role 1 binding constraint tier: is Retry-After handling present in the caller? If absent: name the failure mode precisely (retry storm, missing exponential backoff, silent quota exhaustion, immediate re-queue within same throttle window). If present: is the Retry-After value read and used as the delay, or is a static delay applied regardless?

#### 2.4 Cascade Scenario

One cascade starting from the binding constraint. Format: `T-NN (binding, limit: X) → T-NN (mechanism) → T-NN (mechanism)`. Minimum two downstream hops with T-NN identifiers and explicit causal links. State the compounded throughput or error-rate effect.

---

> **DATA LOCK 2 — ANALYTICAL CLAIMS CLOSED: No new tier name, limit value, source reference, or gap type may be introduced after this line. Role 3 consequences must correspond exclusively to tiers registered in Role 1 and gaps identified in Role 2. A tier in Role 3 without a Role 1 T-NN is a coverage-by-coincidence risk — if it appears, it is a scope violation.**

---

### ROLE 3 — UX ADVOCATE: Consequence Derivation

*Corrects Failure 3 (coverage by coincidence). Each consequence section opens with a named artifact anchor that makes completeness auditable before any row is filled.*

*Draws from Role 1 tier registry and Role 2 analytical claims only. Does not introduce new tiers, sources, or gap types.*

#### 3.1 UX Consequence Matrix

**For each tier in the Role 1 Tier Registry (Section 1.2) — one row per T-NN, expanding the registry in registration order (T-01 first), omitting none:**

| Role 1 T-NN | Error code or signal | Retry-After present? (Y/N) | Honored by caller? (Y/N) | Visible in run history? (Y/N/SILENT) | Failure mode if Retry-After absent |
|-------------|---------------------|---------------------------|-------------------------|--------------------------------------|-----------------------------------|
| T-01 |  |  |  |  |  |
| T-02 |  |  |  |  |  |

Coverage check: state row count. Must match Role 1 registry count. The opening anchor ("For each tier in the Role 1 Tier Registry") makes omission detectable before the count check fires.

#### 3.2 Mitigation Register

**For each gap identified in Role 2 (Sections 2.2–2.4 of the Role 2 Tier Registry) — one mitigation per gap, referencing the specific Role 2 section that surfaced it:**

- Role 2 gap source: [section number + gap description]
- Component to modify: [named component — not a layer or system category]
- Change: [setting name + value, or retry policy with concrete parameters; e.g., "set Apply-to-each concurrency to 5; add Delay action reading Retry-After header value when HTTP 429 is received"]
- Expected outcome: [quantified or behaviorally observable]
- Tradeoff: [specific cost — latency added, throughput ceiling lowered, operational overhead — required; a mitigation without a tradeoff is incomplete]

Coverage check: gap count must match Role 2 gap count (Sections 2.2, 2.3, 2.4 minimum). State match.

#### 3.3 Severity Ranking

**For each tier in the Role 1 Tier Registry — ranked highest to lowest user-visible severity:**

Rank: T-NN (H/M/L) — justification referencing blast radius or impact frequency. For the top-ranked tier: additionally state estimated blast radius, failure visibility (silent vs. explicit), estimated recovery time. For each lower-ranked tier: cite the Role 2 gap entries or Role 1 scope property that determines the rank order.

Coverage check: T-NN count must match Role 1 registry count.

---

**COMPLETION VERIFICATION**

```
Role 1 tier registry count:                                         [N]
Role 3 UX matrix row count (must match):                            [N]
Role 3 severity ranking count (must match):                         [N]
Role 3 mitigation count ≥ Role 2 gap sections:                      [ ]

DATA LOCK 1 reproduced verbatim:                                    [ ]
DATA LOCK 2 reproduced verbatim:                                    [ ]

Role 1 prohibition text (specific data class named):                [exact text]
Role 2 prohibition text (specific data class named):                [exact text]

Failure 1 (retroactive citation) — all thresholds have Source-IDs:  [ ]
Failure 2 (assertion without elimination) — contrastive reasoning:  [ ]
Failure 3 (coverage by coincidence) — anchors name specific artifact:
  3.1 UX opening line:                                              [exact text]
  3.2 Mitigation opening line:                                      [exact text]
  3.3 Severity opening line:                                        [exact text]

Scope violations (T-NN outside Role 1 appearing in Roles 2–3):     [count]
Scope violations declared before continuing:                        [Y if count > 0]
```
