R16 variations written to `simulations/quest/golden/prove-interview-variate-R16-2026-03-15.md`.

---

## R16 Axis Map

| Variation | Axis | Key innovation |
|-----------|------|----------------|
| **V-01** | Single — document-head contract | `SECTION 0` declares 4 numbered sub-schemas (0.1–0.4); no downstream citation yet; tests C-33 in isolation |
| **V-02** | Single — downstream sub-schema citation | No SECTION 0, but phase gates close checklist items with `— per GATE FORMAT CONTRACT`, `— per EVIDENCE SCHEMA CONTRACT` etc.; tests whether citation pattern alone approaches C-35 |
| **V-03** | Single — C-36 framework dimension column | `INERTIA PROFILE TABLE` elevated to named framework with 4 labeled dimensions; Phase 3 adds explicit `FRAMEWORK DIMENSION` column mapping each item to a framework dimension; no head contract |
| **V-04** | Combined — head + downstream citation | SECTION 0 with 0.1–0.4 AND gates cite `— per 0.1` / `— per 0.2`; the two-layer chain makes C-33 satisfiable and approaches C-35 |
| **V-05** | Combined — full apparatus | All R16 innovations: numbered sub-schemas (C-33), `— per 0.N` citations (C-35 approach), framework dimension column (C-36), dual-source `0.3` template naming both Expectation Register and INERTIA PROFILE FRAMEWORK (C-37), vocabulary gate-embedded in Phase 0A gate block as a checklist item rather than body prose (C-41) |

**The V-05 structural pattern from the rubric analysis** — sub-schemas numbered as navigable citation targets (`per 0.2 schema`, `per 0.3 contract`) — is the organizing principle of V-04 and V-05. V-01/V-02/V-03 isolate each axis to measure its independent contribution before combining.
