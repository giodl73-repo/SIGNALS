**flow-dataflow R15 — 5 variations written** to `simulations/quest/golden/flow-dataflow-variate-R15-2026-03-15.md`.

---

## Variation Summary

| Var | Axis | Key Mechanism | C-27 | C-28 |
|-----|------|---------------|------|------|
| **V-01** | Lifecycle emphasis | 6 sequential phase gates with completion guards | T-07 pre-declared in STEP 0 scaffold with purpose text | Inline acknowledgment block per stage, explicit deferral naming T-07 |
| **V-02** | Output format (max tables) | Running "Inline Acknowledgment Log" (T-07) accumulates rows across all stages | T-07 scaffold row names "FM-NN lifecycle resolution audit" with T-02 dependency | Rows appended to T-07 log after each stage boundary table |
| **V-03** | Role sequence (Commerce→Operations→Finance) | Stage assignment map in STEP 0; three role sections with hand-off boundaries | T-07 declared before Commerce's first trace | Each role's stage block contains FM-NN inline acknowledgments with T-07 deferral |
| **V-04** | Inertia framing + lifecycle | Incumbent baseline is PHASE 1 (before pipeline); T-04 Stage Enumeration has "Replaces T-01 Step(s)" column | T-07 declared in scaffold as "FM-NN lifecycle resolution audit" | FM-NN check per stage with explicit deferral to T-07 (PHASE 4) |
| **V-05** | Conversational + list-heavy + compressed | Single numbered checklist, bullet-list stage traces, no phase labels | T-07 in Step 1 table list with "produced after all traces" | "FM-NN check" bullet per stage with deferral instruction woven into prose |

**Structural differentiations from R14:**
- All 5 variations pre-declare T-07 as the FM-NN lifecycle resolution audit (C-27 baseline)
- All 5 incorporate inline FM-NN acknowledgments with explicit deferral language (C-28 baseline)
- V-02 tests whether a *running log table* structure for acknowledgments outperforms per-stage prose notes on C-28 completeness
- V-04 adds a "T-01 Safeguard at Risk" column to the FM prediction register — directly linking predicted failure modes to the manual safeguard they bypass
