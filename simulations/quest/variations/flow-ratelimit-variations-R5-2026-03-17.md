The file is written. Here's a summary of what was produced:

---

**flow-ratelimit — Variate R5** (5 complete prompt variations)

**R5 target gaps from R4 scorecard:**
- R4 V-01 scored 120/120 on 11 aspirational criteria — the R5 rubric adds C-20, C-21, C-22 extracted from those R4 excellence signals
- R4 V-01 FAILS C-22: used terminal reconciliation (satisfies C-18), not gate-checkpoint currency
- C-20 requires the derivation chain to be *in the cell itself*, not just derivable

**Variation axes and hypotheses:**

| Var | Axis | Primary Criteria Targeted |
|-----|------|--------------------------|
| V-01 | Role sequence — full gate chain (all 9 analysis-body transitions gated) + arithmetic mandate in Format Contract Rejection Clause B | C-21, C-20 |
| V-02 | Output format — DERIVATION CHAIN as a mandatory 4th column in the Format Contract schema; rejection clause catches missing derivation at any table | C-20 |
| V-03 | Lifecycle emphasis — gate-checkpoint Verdict currency: every phase exit condition includes a mandatory "insert revision marker NOW before proceeding" check | C-22 |
| V-04 | Combined: full gate chain (C-21) + gate-checkpoint currency at every gate (C-22); gate has two parts: deliverable check + currency check, both required | C-21, C-22 |
| V-05 | Combined: all three + INERTIA framing + four-field UX template; INERTIA/PROTECTED/DERIVATION CHAIN as the Format Contract schema, gate-checkpoint currency at all 10 transitions | C-20, C-21, C-22, C-12, C-14, C-19 |

**Key architectural decisions:**
- V-01 retains terminal reconciliation (C-18) without gate-checkpoint currency — isolates the gate-chain axis cleanly
- V-02 isolates the arithmetic trace axis via column compliance vs. per-role advisory
- V-03 uses phase lifecycle with partial gate chain (C-17 but not C-21) to isolate the currency axis
- V-04 is the minimum combination that achieves both C-21 and C-22 simultaneously
- V-05 integrates all five structural disciplines with INERTIA naming the status-quo competitor throughout
