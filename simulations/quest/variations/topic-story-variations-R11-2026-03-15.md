Here are the five variations for `topic-story` Round 11, targeting the two v10 rubric additions:

---

## V-01 — Output Format: VERDICT block on R10 V-01 base
**Axis**: Add a pre-gather `VERDICT` block (verb + one-sentence basis) before Step 1. Zero other changes to R10 V-01's four-element derivation chain. Tests whether the minimal insertion satisfies C-28 without touching namespace or role structure. C-29 is deliberately left unaddressed — single-axis isolation.

## V-02 — Role Sequence: Full tri-role EXTRACTOR/ANALYST/AUTHOR
**Axis**: EXTRACTOR's first required output is namespace enumeration (minimum 3, structural gate, not an authorial option). ANALYST reads EXTRACTOR output only and produces pattern block, delta, posture, and derivation pre-work. AUTHOR reads ANALYST output only and writes beats. VERDICT precedes all roles. Explicit ROLE COMPLETE markers enforce one-way flow. Satisfies C-24, C-26, C-28, C-29 together.

## V-03 — Inertia Framing: VERDICT as the entering recommendation
**Axis**: The VERDICT is framed as the *inertia recommendation* — the verb the team would commit to before reading any signal. The ledger tracks how each signal confirms or challenges this stance. Beat 5 includes an explicit inertia-to-final comparison: did the verb hold? If yes, what did signals add? If no, what changed? This makes C-22 (anti-stagnation) mechanically checkable and C-21 (falsifiable baseline) directly tied to the verdict structure.

## V-04 — Combination: VERDICT + namespace enumeration on R10 V-04 base
**Axis**: R10 V-04 (reconciliation table + 5-element derivation chain) upgraded with VERDICT as a pre-gather block and namespace enumeration as a named Step 1 sub-task. Tests whether C-28 and C-29 can be satisfied as minimal additions to the proven R10 V-04 structure without full tri-role. Diagnostic: if C-29 requires C-26, this variation will show PARTIAL.

## V-05 — Full combination: tri-role + VERDICT + reconciliation + 5-element derivation + terminal checklist
**Axis**: EXTRACTOR (namespace enum + digests) → ANALYST (mutation chain + reconciliation table + pattern block + delta + 5-element derivation pre-work) → AUTHOR (5 beats + independent bridge in Beat 5) → 24-item terminal checklist gate. Explicit non-substitution rule for Beat 5 bridge. Targets C-20, C-22, C-23, C-24, C-25, C-26, C-27, C-28, C-29 simultaneously.
