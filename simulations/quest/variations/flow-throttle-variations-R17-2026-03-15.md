Written to `simulations/quest/golden/flow-throttle-variate-R17-2026-03-16.md`.

---

## Round 17 — V-01 through V-05

**Variation map:**

| # | Axis | C-41 mechanism | C-43 mechanism | C-44 mechanism |
|---|------|----------------|----------------|----------------|
| V-01 | Lifecycle emphasis (single) | SIGNAL CHECKPOINT with two milestone records | Two-row milestone table: Signal 1 = section-presence, Signal 2 = count-complete | Bypass-failure prohibition at checkpoint |
| V-02 | Output format (single) | PHASE 1 ACTIVATION SEQUENCE with signal distinction table + count table | "Confirms" / "Does NOT confirm" columns make the distinction column-readable | Bypass-attempt rejection table naming "looks complete" as a typed row |
| V-03 | Role sequence (single) | Role 1.5 (INVENTORY VERIFIER) — separate role, single responsibility | Two roles produce two signals; separation is architectural | C-44 inertia-frame rejection as first content of Role 1.5, including the C-26 parallel proof |
| V-04 | Phrasing register + lifecycle | TWO-SIGNAL MANDATORY GATEWAY — numbered steps G-1 through G-4 | Two explicit SHALL-NOT-conflate declarations, each signal in its own numbered obligation | PROHIBITED bypass clause with SHALL NOT at every decision branch |
| V-05 | Role sequence + output format + inertia framing | Role 1.5 + verification table + role activation chain | Signal distinction table (SIG-01/SIG-02) with role attribution column | Named failure mode + structural proof + inertia-frame rejection field that must be filled before verification table runs |

**C-42/C-43/C-44 coverage strategy:**

Every variation carries all three new criteria. C-43 was the gap in V-01/V-02/V-04 under v17 — each now either introduces two named signals with explicit distinction (V-01: milestone table; V-02: signal distinction table; V-04: non-conflation SHALL statements) or keeps the two-role architecture (V-03/V-05). C-44 was missing from V-01 through V-04 — each now includes a named "looks complete" bypass rejection with the structural proof argument that FORMAT CONTRACT COMPLETE ≠ count completeness. All Field 5 audit entries explicitly require citing body evidence rather than inferring from output quality, satisfying C-42's "not inferred from body quality" constraint.
