**R6 variate written** to `simulations/quest/golden/corps-pr-variate-R6-2026-03-16.md`.

---

## Variation Axes

| Variation | Axis | C-19/C-20 Approach |
|-----------|------|--------------------|
| **V-01** | Inertia framing | C-19: terminal fields (Budget verdict, Budget strength) declared below a separator — not as table rows. Adds Net direction column (WORSE/BETTER/NEUTRAL) per row so verdict is derivable from the table. |
| **V-02** | Lifecycle emphasis | C-20: Phase C entry split into two named sub-conditions — **C1** (Phase B exit checklist, B1–B5) and **C2** (per-role IA read prerequisite verified by Net direction citation). Each is independently auditable. |
| **V-03** | Role sequence | C-20: per-role **IA READ RECEIPT block** as first structural element of every technical role section; receipt must restate Phase B Net direction for a named row — Net citation is the read proof artifact. No pipeline declaration needed. |
| **V-04** | Inertia + Lifecycle (combination) | C-19 terminal fields + C-20 dual-clause pipeline. Chain: Phase B fills Net direction per row → Phase C C1 checklist verifies those fields → Phase C C2 requires citing a Net direction value (only exists if Phase B was read). C-19 compliance is the C-20 proof mechanism. |
| **V-05** | Inertia + Role sequence + Phrasing (combination) | C-19 terminal fields + per-role read receipts (C-20 per-role) + ban-to-fix table (C-13). Tests whether per-role materialization produces equivalent audit properties to a pipeline declaration without the declaration overhead. |

**Key R6 insight**: C-19 and C-20 can be wired together — if Phase B requires a Net direction per row (C-19), then Phase C C2 compliance can be verified by asking whether the role cited a Net direction value. Citing it proves they read Phase B; the Net column is the proof token.
