Here are the 5 complete R9 variations:

---

## V-01 — Output format axis (Finance → Operations → Commerce)

**Hypothesis**: Adding a standalone `BOUNDARY BLOCK SCHEMA` section before any role begins (C-31), plus a `REGISTER DECLARATION` enumerating column-name compliance markers per field (C-32), closes those gaps without touching the proven R7 V-01 role sequence. SC-7 adds `Stage Latency` as a required stage table column, closing the C-05 regression opened by R8's boundary-column innovations. SC-8 structurally mandates `[A-13] TRADE-OFF ANALYSIS` with both `[A-01]` and `[A-02]` token citations (C-29).

---

## V-02 — Role sequence axis (Operations → Commerce → Finance)

**Hypothesis**: Non-natural Finance-last ordering is the single variation; the C-31/C-32/C-05/C-29 fixes from V-01 apply identically. Any score difference isolates role-sequence effects on non-adjacent citation compliance.

---

## V-03 — Phrasing register axis (Commerce → Finance → Operations, conversational)

**Hypothesis**: Conversational register achieves full verifiability when the `REGISTER INVARIANT DECLARATION` section maps every required boundary field to a sentence-opener or bold-keyword compliance marker, and explicitly names which criteria apply in non-tabular form (C-33 + C-32). Evaluator scans for declared markers; no holistic prose reading required.

---

## V-04 — Combined: role sequence (Commerce → Operations → Finance) + lifecycle emphasis

**Hypothesis**: Maximally non-natural ordering (Finance last) combined with a 7-column boundary schema adding `Incumbent Equiv.` per boundary (SC-9) plus a named `[A-12] INCUMBENT TOTAL` artifact (SC-10) required before `TRADE-OFF ANALYSIS` closes the R8 V-04 gap: pipeline-vs-manual comparison becomes a numeric, per-boundary audit trail with a quantitative closing endpoint.

---

## V-05 — Combined: dual-register (Finance/Operations tabular, Commerce conversational) + REGISTER TRANSLATION TABLE

**Hypothesis**: A named `REGISTER TRANSLATION TABLE` section simultaneously satisfies C-31 (pre-declared schema), C-32 (per-field markers for both registers), and C-33 (register-invariant declaration with marker mapping for conversational Commerce) via a single lookup structure. Role sequence Finance → Commerce → Operations maximizes non-adjacent citation distance (Operations must cite Finance across two hops). Register transition mid-sequence (tabular→conversational→tabular) stress-tests whether the translation table survives format switching.

---

**Key structural innovations across R9:**
- `BOUNDARY BLOCK SCHEMA` as a pre-role named section (every variation except V-03, which uses `REGISTER INVARIANT DECLARATION` equivalently)
- `REGISTER DECLARATION` with per-field compliance marker enumeration (all five)
- `Stage Latency` as required stage table column / `Stage latency:` marker in conversational (all five — C-05 fix)
- SC-8 mandating `[A-13]` as structurally required section with dual token citations (all five — C-29 fix)
- `REGISTER INVARIANT DECLARATION` naming criteria survival in conversational register (V-03, V-05 — C-33)
- `INCUMBENT TOTAL` as a closing artifact before `TRADE-OFF ANALYSIS` (V-04)
- `REGISTER TRANSLATION TABLE` as single unified compliance declaration (V-05)
