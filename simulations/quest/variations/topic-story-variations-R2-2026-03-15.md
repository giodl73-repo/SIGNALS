Here are the five Round 2 variations, written to the file. Summary:

---

### What was generated

**V-01 — Output format (template slots)**
Provides explicit labeled blocks — `The pattern:`, `This pattern produces:`, uncertainty table with direction columns — as format slots to fill before prose. Tests whether structural prescription alone produces C-10/C-11/C-12 without behavioral instruction.

**V-02 — Lifecycle emphasis (process gate)**
Two mandatory passes: Pass 1 produces Output 1.1 (findings register), Output 1.2 (pattern statement), Output 1.3 (decision-cost uncertainty register) before Pass 2 may begin. The gate enforces C-10's "pre-composition" qualifier structurally.

**V-03 — Phrasing register (conditional framing)**
Establishes the conditional register for uncertainty via instruction + example: "do not write X, write instead Y (with if/then direction)." Tests whether phrasing discipline alone reliably elicits C-12 without structural overhead.

**V-04 — Combination: V-01 template + V-02 gate + R1 audience pressure**
Doubles down on C-10 (slot AND gate), strongest combined C-11 (slot + required step), most explicit C-12 (table with direction columns). Inherits R1's best mechanism. Predicted highest probability golden candidate.

**V-05 — Combination: V-03 register + R1 audience pressure + C-11 bridge instruction with revision pressure**
Behavioral approach: three rules instead of structural templates. Rule 4 adds "if you cannot write this sentence, your pattern and recommendation are not actually connected — revise one of them." Tests whether revision pressure substitutes for template slots at lower format cost.

---

**Key design choices**:
- C-10 root cause: model discovers pattern during prose composition, embedding it rather than externalizing it. Fix: either a labeled slot (V-01) or a process gate (V-02) or both (V-04).
- C-11 root cause: co-presence without derivation. Fix: explicit bridge sentence + revision pressure (V-05 Rule 4).
- C-12 root cause: descriptive hedges without directional conditionals. Fix: table columns (V-01) or conditional example with omit-if-no-consequence rule (V-03/V-05).
