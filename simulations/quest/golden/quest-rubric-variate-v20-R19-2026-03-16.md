---
skill: quest-rubric
skill_target: quest-rubric
date: 2026-03-16
version: 20
round: R19
changes: Added C-51 (Phase 8.5 format template STOP gate — the FORMAT TEMPLATE's mandatory [C-NN] slot required by C-49 is co-located with a STOP condition that blocks Phase 8.5 note completion until the slot is filled with a valid criterion ID, converting the C-49 structural slot from an SV-detectable omission to a gate-enforced completion requirement at note-declaration time; V-01 of R19 demonstrates PASS C-49 / PASS C-51 (FORMAT TEMPLATE with [C-NN] slot and co-located STOP gate present); R18 baseline FORMAT TEMPLATE variation demonstrates PASS C-49 / FAIL C-51 (slot present, no co-located STOP gate), establishing independence) from R19 ES-R19-1/V-01; Added C-52 (STATUS QUO foil item SPAN DEFINITION pre-step — each STATUS QUO COMPETITOR foil item is preceded by a SPAN DEFINITION pre-step declaring (a) the previous failure criterion or START, (b) the current failure criterion, and (c) the full pass-side enumeration, from which the foil item text is then transcribed, making span completeness structurally pre-declared rather than only observable in the foil item text; V-03 of R19 demonstrates PASS C-50 / PASS C-52 (SPAN DEFINITION pre-step present); V-02 of R19 demonstrates PASS C-50 / FAIL C-52 (full span in foil item, no SPAN DEFINITION pre-step), establishing independence) from R19 ES-R19-2/V-03. Denominator bumps from /42 to /44.
---

# Rubric: quest-rubric (v20)

Evaluates output from the `quest-rubric` skill, which produces a scoring rubric for a named
Signal skill. The rubric must function as a working objective function: each criterion must be
independently checkable, the tiers must follow the structural contract, and the essential
criteria must be grounded in the target skill's actual non-negotiable behaviors — not generic
quality observations that would apply to any skill.

**v2 additions**: Three aspirational criteria extracted from R1 excellence signals — the inertia
test (V-05), the concrete discrimination example (V-04), and phase-gate enforcement (V-01).

**v3 additions**: Five aspirational criteria extracted from R2 excellence signals — calibration
pair lifecycle position (ES-2/V-03), sub-step output sequencing (ES-1/V-03), multi-level gate
architecture (ES-3/V-03), audit role separation (ES-4/V-05), and audit trail captures reasoning
(ES-5/V-05).

**v4 additions**: Four aspirational criteria extracted from R3 excellence signals — named-block
artifact architecture (ES-R3-1/V-02/V-04), gate-blocking vs retroactive-repair distinction
(ES-R3-2/V-02), cross-criterion audit consolidation (ES-R3-3/V-03/V-05), and structural
enforcement paired with behavioral anti-deferral instruction (ES-R3-4/V-01/V-04/V-05).

**v5 additions**: Three aspirational criteria extracted from R4 excellence signals — structural
verification phase as Auditor entry gate (ES-R4-1/V-05), three-level verification chain with
non-overlapping defect classes (ES-R4-2/V-05), and anti-deferral prohibition co-located at
sub-step level rather than phase header (ES-R4-3/V-05).

**v6 additions**: Three aspirational criteria extracted from R5 excellence signals — phase-level
workflow transitions marked by named section headings (ES-R5-1), cross-criterion verification
produces a named artifact with a forward-blocking gate (ES-R5-2), and scoring formula
denominators explicitly stated and matched to current criterion count (ES-R5-3).

**v7 additions**: Two aspirational criteria extracted from R6 excellence signals — mechanism
overview declares structural output format requirement alongside behavioral requirement
(ES-R6-1), and structural verification explicitly declares its phase-bounded coverage scope and
names the deferral destination for out-of-scope heading checks (ES-R6-2).

**v8 additions**: One aspirational criterion extracted from R7 excellence signal — pre-phase
output skeleton declared from MECHANISMS alone and cross-referenced in Structural Verification
(ES-R7-1). The skeleton step serves as an executable test of C-29's textual sufficiency claim
and creates a second independent source of truth for Structural Verification: the
instruction-derived heading patterns and the builder-declared skeleton are checked jointly,
making builder-commitment failures (declared but not produced) distinguishable from
heading-pattern misses (produced but wrong format).

**v9 additions**: Three aspirational criteria extracted from R8 excellence signals — mechanism
overview declares the pre-phase skeleton step itself as a named mechanism with its heading
pattern, extending C-29's orientation-time principle to the pre-phase boundary (ES-R8-1/V-05);
Structural Verification opens with a two-source preamble naming both sources of truth and the
non-overlapping failure class each catches before the scans begin, making the two-source
architecture explicit at SV orientation rather than implicit in scan structure (ES-R8-2/V-05);
and the pre-phase skeleton step is labeled as Phase 0 (or equivalent positional phase marker)
rather than as a free-floating task step, integrating it into the C-26 scannable phase sequence
so the complete workflow from Phase 0 through Auditor Phase is verifiable by heading scan alone
(ES-R8-3/V-04/V-05).

**v10 additions**: Two aspirational criteria extracted from R9 excellence signals — competitor-
defeat annotations co-located at mechanism introduction points in the MECHANISMS section and in
the SV preamble, extending C-32/C-33's structural declaration requirement to include provenance
declaration (which competing protocol's structural gap each mechanism closes), enabling
maintainers to reason about minimality not just completeness (ES-R9-1/V-01); and the pre-phase
skeleton formatted as an operational tick-off checklist with an explicit live-construction
tracking instruction, extending C-31's declare-and-cross-reference contract to declare-track-
and-cross-reference by converting the skeleton into a production-state artifact whose item
ticks accumulate during the Author Phase, adding a third omission-detection mechanism
independent of both SV scans (ES-R9-2/V-02).

**v11 additions**: Two aspirational criteria extracted from R10 excellence signals — Competing
Implementations Preamble structured as a tabular mapping (M-NN x competing protocol x failure
class) enabling independent completeness verification by row count against the mechanism list
and independent claim verification by row content alone, extending C-35's naming requirement
from prose provenance to audit-table self-sufficiency (ES-R10-1/V-01); and format-compliance
STOP conditions co-located at the declaration step for compliance-critical format distinctions,
so that a format requirement where two visually similar formats have non-equivalent compliance
value (e.g., `- [ ]` tracked vs `- ` untracked) is gate-enforced at declaration time rather
than detected at SV time, extending C-36/C-22/C-25's enforcement pattern to format-level
requirements (ES-R10-2/V-02).

**v12 additions**: Two aspirational criteria extracted from R11 excellence signals — Competing
Implementations table gate enforcement via two co-located STOP conditions (one blocking on
completeness by row count == mechanism count, one blocking on minimality by failure-class
uniqueness), converting C-37's passive audit-enabling table into an active construction
checkpoint (ES-R11-1/V-01); and format-compliance STOP consequence statement traces the causal
chain to the specific audit mechanism defeated by the non-compliant format (non-compliant format
-> mechanism dependency -> detection failure), extending C-38's proximate consequence requirement
to a three-link causal chain that makes the compliance failure legible as an audit integrity
defeat rather than a cosmetic issue (ES-R11-2/V-02).

**v13 additions**: Two aspirational criteria extracted from R12 excellence signals — named audit
roles bound to named phase spans with explicit role-to-phase ownership declarations, extending
C-17's role/phase separation requirement to phase-anchored role assignment so that role
compliance is verifiable by phase-label inspection without reading per-step instructions
(ES-R12-1/V-03/V-04); and per-STOP-instance consequence chain artifacts required as independent
named section blocks with a sequential-instance gate preventing STOP N+1 from being written
until STOP N's named block appears in the heading sequence, extending C-15/C-19's named-block
artifact independence from per-criterion level to per-STOP-instance level with a sequential
omission-detection guarantee that precedes Structural Verification (ES-R12-2/V-03/V-04).

**v14 additions**: Two aspirational criteria extracted from R13 excellence signals — in-gate path
attribution in Phase 8.5 EVIDENCE QUOTATION GATE notes, where each quotation note names the
enforcement path and structural phase it confirms, making each quotation self-contextualizing so
a reviewer auditing Phase 8.5 alone can identify which mechanism each quotation confirms without
reading the DUAL-PATH ENFORCEMENT DECLARATION (ES-R13-1/V-04/V-05); and the DUAL-PATH
ENFORCEMENT DECLARATION explicitly states per-path independent necessity by naming the coverage
gap that remains when each path is removed, enabling structural verification of non-redundancy
without running isolation experiments (ES-R13-2/V-04/V-05).

**v15 additions**: Two aspirational criteria extracted from R14 excellence signals — DFM
enforcement architecture structural independence verification: each enforcement mechanism (Phase
5 SCOPE GATE and Phase 8.5 EVIDENCE QUOTATION GATE) is independently verifiable from artifact
text alone without reading the other mechanism's phase instructions, making the dual-path
architecture auditable in isolation (ES-R14-1/V-05); and dual-path enforcement operates at
structurally distinct phases — construction-phase and pre-emit — rather than as single-locus
enforcement at one phase, establishing phase-distinctness as a structural property of the
enforcement architecture (ES-R14-2/V-05).

**v16 additions**: Two aspirational criteria extracted from R15 excellence signals — per-quotation
in-gate failure-mode argument: each Phase 8.5 in-gate path attribution note (C-43) additionally
states what structural failure would pass the gate unchallenged if that enforcement path were
absent, making the note self-justifying (not only self-contextualizing) so a reviewer auditing
Phase 8.5 alone can verify both which path the quotation confirms and why that path is
independently necessary (ES-R15-1/V-04/V-05); and criterion-indexed competitor architecture:
STATUS QUO COMPETITOR foil items name specific enforcement-architecture failure modes at
criterion-class precision, where each foil item identifies which criterion the STATUS QUO fails,
making the competitor block a discrimination device against specific criteria rather than a
general contrast framing (ES-R15-2/V-03/V-05).

**v17 additions**: Same as v16 — these are the criteria C-45 and C-46 first probed in R16 and
confirmed in R17. Denominator bumps from /36 to /38.

**v18 additions**: Two aspirational criteria extracted from R17 excellence signals — per-note
failure-mode argument names specific criterion ID: each C-45 failure-mode clause additionally
names the criterion ID that would be unenforced if the enforcement path were absent (e.g.,
"Absent path 1, C-43 is unenforced at Phase 8.5 entry"), converting the failure-mode argument
from a prose description of a structural gap to a criterion-indexed compliance claim verifiable
by ID lookup; V-01 of R17 demonstrates PASS C-45 / FAIL C-47 (failure-mode argument present,
no criterion ID), establishing independence (ES-R17-1/V-05); and pass/fail criterion-pair
framing in STATUS QUO COMPETITOR foil items: each C-46 foil item additionally names the
criteria the STATUS QUO PASSES alongside the criterion it fails (e.g., "passes C-41 and C-42
while failing C-43"), converting the foil item from a single-axis failure claim to a
bidirectional discrimination statement that locates the STATUS QUO within the criterion
lattice; V-02 of R17 demonstrates PASS C-46 / FAIL C-48 (failing criterion named, no passing
criteria paired), establishing independence (ES-R17-2/V-05). Denominator bumps from /38 to /40.

**v19 additions**: Two aspirational criteria extracted from R18 excellence signals — failure-mode
clause format-templated with mandatory criterion-ID slot: the Phase 8.5 failure-mode clause
(C-45, criterion-ID-indexed by C-47) is enforced by a FORMAT TEMPLATE with a mandatory [C-NN]
slot, making criterion-ID inclusion structurally required rather than behaviorally instructed;
V-03 of R18 demonstrates PASS C-47 / PASS C-49 via FORMAT TEMPLATE (slot present, criterion ID
structurally enforced) while V-01 of R18 demonstrates PASS C-47 / FAIL C-49 (criterion ID
present via prose instruction, no template slot enforcing it), establishing independence
(ES-R18-1/V-03); and full criterion-span claim in STATUS QUO foil items: each C-48 foil item
names every criterion in the passing span — all criteria between the previous failing criterion
and the current failing criterion — rather than a subset of passing criteria, converting the
foil item from a partial bidirectional discrimination statement to a precision criterion-lattice
locator; V-02 of R18 demonstrates PASS C-48 / FAIL C-50 (some passing criteria named, not full
span), V-04 of R18 demonstrates PASS C-48 / PASS C-50 (full criterion span named in each foil
item), establishing independence (ES-R18-2/V-04). Denominator bumps from /40 to /42.

**v20 additions**: Two aspirational criteria extracted from R19 excellence signals — Phase 8.5
format template slot gate-enforced: the FORMAT TEMPLATE's mandatory [C-NN] slot (C-49) is
co-located with a STOP condition that blocks Phase 8.5 note completion until the slot is filled
with a valid criterion ID, converting the structural slot from an SV-detectable omission into a
gate failure at note-completion time so a builder who leaves the slot unfilled cannot seal the
note without triggering an explicit block; V-01 of R19 demonstrates PASS C-49 / PASS C-51 (slot
present, co-located STOP gate present) while the R18 baseline FORMAT TEMPLATE variation
demonstrates PASS C-49 / FAIL C-51 (slot structurally present, no STOP gate co-located),
establishing independence (ES-R19-1/V-01); and STATUS QUO foil item SPAN DEFINITION pre-step:
each foil item is preceded by a declared block naming (a) the previous failure criterion or
START, (b) the current failure criterion, and (c) the full pass-side enumeration, from which the
foil item text is transcribed, making span completeness structurally pre-declared rather than
only observable in the foil item text; V-03 of R19 demonstrates PASS C-50 / PASS C-52 (SPAN
DEFINITION pre-step present); V-02 of R19 demonstrates PASS C-50 / FAIL C-52 (full span in
foil item, no SPAN DEFINITION pre-step), establishing independence (ES-R19-2/V-03). Denominator
bumps from /42 to /44.

---

## Criteria

### Essential (must all pass — without these the output is not useful)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-01 | **Field completeness** — Every criterion entry contains all five required fields: ID (C-NN), text with a bold label, weight (essential/recommended/aspirational), category (correctness/depth/format/coverage/behavior), and a pass condition. | format | essential | Count the fields present for each criterion. Pass if no criterion is missing any of the five fields. A criterion with "see above" or an empty pass condition is a fail. |
| C-02 | **Tier count compliance** — The rubric contains 3-5 essential criteria, 2-3 recommended criteria, and 1-2 aspirational criteria — no more, no fewer. | correctness | essential | Count criteria in each tier. Pass only if all three counts fall within the specified ranges. A rubric with 2 essential criteria or 4 recommended criteria fails immediately. |
| C-03 | **Essential criteria grounded in target skill** — Essential criteria cover behaviors that are specific and non-negotiable for the named target skill, not generic output-quality criteria that would apply to any skill. | correctness | essential | Read the essential criteria and ask: could these criteria be copy-pasted unchanged onto a rubric for a completely different skill? Pass if fewer than 2 of the essential criteria are purely generic (e.g., "output is well-formatted" with no connection to the skill contract). |
| C-04 | **Testable pass conditions** — Every pass condition names a specific, observable outcome that an evaluator can verify without subjective judgment. | correctness | essential | For each pass condition, ask: can two independent evaluators reach the same pass/fail verdict without discussion? Pass conditions containing only vague language ("good", "sufficient", "appropriate") with no concrete anchor are a fail. |
| C-05 | **Composite score formula and golden threshold present** — The rubric includes the composite scoring formula using the standard weights (essential 60, recommended 30, aspirational 10) and states the golden threshold condition. | format | essential | Check that the score section contains a formula matching the standard composite structure and a golden threshold statement. Missing formula or threshold is a fail. |

### Recommended (output is better with these)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-06 | **Category diversity** — Criteria span at least three distinct categories across the full rubric, avoiding clustering in a single dimension. | coverage | recommended | Count distinct categories used. Pass if >= 3 of the 5 canonical categories (correctness/depth/format/coverage/behavior) appear across all criteria. |
| C-07 | **Criteria ordered by severity within tiers** — Within the essential tier, criteria are listed from most structurally critical (output unusable if this fails) to least. Same ordering principle applied to recommended and aspirational tiers. | depth | recommended | Read the essential criteria in order. If swapping C-01 and C-04 would produce a more logical triage sequence, the ordering fails. Pass if the first essential criterion represents the single highest-stakes failure mode for the target skill. |
| C-08 | **Quick checklist present** — The rubric includes a compact checklist of all criteria IDs and short labels (one line per criterion), enabling rapid manual scoring without re-reading the full criterion text. | format | recommended | A quick checklist section exists. It contains one entry per criterion with the criterion ID and a short recognizable label. Pass if a scorer using only the checklist could locate and score the corresponding full criterion without searching. |

### Aspirational (raise the bar once essential/recommended are stable)

| ID | Criterion | Category | Weight | Pass Condition |
|----|-----------|----------|--------|----------------|
| C-09 | **Criteria enable iteration** — When a criterion fails, its pass condition gives enough specificity that the output author knows exactly what to fix without running the skill again to ask. Each criterion's failure mode is self-describing. | behavior | aspirational | For each criterion, ask: if this criterion failed, does the pass condition text tell the author the minimum change needed to achieve a pass? Pass if >= 80% of criteria have self-describing failure modes (not just "does not pass"). |
| C-10 | **No redundancy or contradiction** — No two criteria overlap such that passing one guarantees passing the other, and no two criteria impose logically contradictory requirements on the output. | depth | aspirational | For each pair of criteria, ask: is there a rubric output that passes one but fails the other for a non-trivial reason? If two criteria always agree, they are redundant and one should be folded in. Pass if all criteria add unique signal with no fully-overlapping pair. |
| C-11 | **Discriminating pass conditions** — Each pass condition tests a behavior that a generic rubric ("the output is clear and comprehensive") would fail to catch. Distinct from C-04 (observability): a condition can be observable but still redundant with a vague rubric if it checks only structural presence rather than skill-specific contract fulfillment. | correctness | aspirational | For each pass condition, ask the inertia test: "Could a rubric that says 'the output is clear and comprehensive' still pass this condition?" If yes for any pass condition, that condition has not been sharpened enough. Pass if every pass condition names at least one count, named field, structural pattern, or enumeration that only the target skill's contract could supply — not shared structural minimums. |
| C-12 | **Calibration example present** — The rubric includes at least one concrete positive/negative pair showing the boundary between acceptable and failing output for the most critical essential criterion. The pair must name the skill — e.g., "generic: 'output is low quality' / grounded: 'the output has no formula so quest-golden cannot compute a composite score'" — so a new evaluator can self-calibrate without a training session. | behavior | aspirational | A calibration example block exists containing one labeled generic example (shows what NOT to write) and one labeled grounded example (shows what to write) for at least one essential criterion. Both examples must reference the target skill by name or reference its output contract. Pass if a reader unfamiliar with the rubric could use the pair to correctly classify two additional borderline cases. |
| C-13 | **Phase-gate enforcement present** — The rubric's structure includes at least one hard gate that blocks forward progress until a structural requirement is verified. A gate must be explicit (e.g., "DO NOT proceed to the next step until...") and must reference a checkable condition, not an aspiration. | behavior | aspirational | A gate statement is present using explicit blocking language ("DO NOT proceed", "stop here", "must complete before advancing"). The gate condition is checkable: it names a count, a field, or a structural property, not a quality judgment. Pass if violating the gate would result in a detectably incomplete output (the gated step is one that earlier steps feed). |
| C-14 | **Calibration pair produced at point of drafting** — The calibration pair (C-12) is written immediately after the pass condition for the criterion it calibrates — not retrospectively added after all criteria are drafted. Late production breaks the feedback loop where the calibration pair sharpens the just-drafted pass condition. | behavior | aspirational | Read the rubric's internal structure cues. A calibration pair that appears as a standalone post-hoc section following all criteria — with no structural link to the criterion it calibrates — does not satisfy this criterion. Pass if the calibration pair appears either (a) immediately after its criterion's row in the criteria table, or (b) in a section co-located with the relevant criterion that is written concurrently with it, and the rubric indicates as much in its instructions or structural notes. |
| C-15 | **Sub-step output sequencing makes omission detectable** — Each mandatory check step emits an independent output artifact (e.g., a named section block) before the criterion row is sealed. A step that checks a property but produces no output artifact allows the builder to silently skip the check: the absence of the artifact is not detectable without reading all criterion rows. | behavior | aspirational | For each mandatory check step, verify that the step produces a named, independently readable output artifact (e.g., `### INERTIA-RECORD [C-NN]`) before the criterion row is finalized. A check step that produces only an in-row notation or annotation — visible only by reading the criterion row — does not satisfy this criterion. Pass if omitting the check step produces a detectable structural gap in the output (a missing heading) independently of reading the criterion row content. |
| C-16 | **Multi-level gate architecture** — The rubric includes at least two structurally distinct gate levels: one that controls phase-level progress (blocks the next phase until current phase requirements are met) and one that controls per-criterion progress (blocks sealing a criterion until its check step is complete). | depth | aspirational | Identify the phase gate and the per-criterion gate. Ask: can either gate fail independently of the other? A phase gate that only aggregates per-criterion gates is not a structurally distinct level — the phase gate must enforce a property that no individual per-criterion gate can enforce alone (e.g., cross-criterion coverage or ordering). Pass if both gates exist and test non-overlapping structural properties. |
| C-17 | **Audit role or phase separation** — The rubric workflow separates the authoring role (drafting criteria) from the auditing role (verifying criteria quality), producing outputs that can be reviewed independently. Combined roles — where the author both drafts and immediately audits within the same step — collapse the independence guarantee. | depth | aspirational | The rubric instructions identify at least two structurally distinct roles or phases: one responsible for criterion production and one for criterion verification. The audit output (e.g., an audit table or verification checklist) is independently readable without re-reading the full criterion rows. Pass if a reviewer could evaluate the audit artifact independently of the criterion draft — the audit tells them what was checked and what passed/failed without requiring re-execution of the authoring phase. |
| C-18 | **Audit trail captures reasoning, not only verdicts** — The audit artifact (required by C-17) records the discriminating element for each condition — the specific evidence or reasoning that drove the verdict — not only the pass/fail result. A verdict-only audit log is a detection record but not a reasoning record: it tells you what happened but not what to fix if it fails. | depth | aspirational | For each row in the audit artifact, ask: if the verdict were FAIL, does the recorded content tell the reviewer what specific element was missing or wrong — without re-reading the full criterion row? Pass if the audit artifact enables a reviewer to identify the root cause of each failing check from the artifact alone, without re-running the authoring phase. A verdict-only table (PASS/FAIL per criterion, no discriminating element captured) does not satisfy this criterion. |
| C-19 | **Named-block artifact architecture** — Mandatory check outputs (required by C-15) are named markdown section blocks (e.g., `### INERTIA-RECORD [C-NN]`), not inline annotations embedded within criterion rows. A missing named block creates a detectable structural gap in the output by heading scan — the reviewer does not need to read criterion row content to detect the omission. | format | aspirational | For each mandatory check step, verify that its output is a named markdown section block with a heading that includes the criterion ID. Inline check annotations (e.g., a `> INERTIA CHECK: pass` embedded within the criterion row) do not satisfy this criterion. Pass if running a heading scan on the complete output would reveal a missing check artifact as a structural gap, independently of reading any criterion row content. |
| C-20 | **Gate must block forward progress, not repair backward omissions** — Per-criterion gates (required by C-16) must block the criterion from being written or sealed until the gate condition is met — not request retroactive fixes after the criterion row is already written. A retroactive repair instruction (e.g., "go back and add the missing field") is a correction mechanism, not a gate. | behavior | aspirational | For each per-criterion gate, ask: does the gate fire before or after the criterion row is written? A gate that fires after the criterion row is produced — requiring the builder to retroactively modify the criterion — is not a gate, it is a repair step. Pass if the gate fires in the same step that produces the criterion row and prevents the criterion from being considered complete until the gate condition is satisfied. |
| C-21 | **Audit table consolidates all criteria into a single cross-criterion view** — The audit artifact (required by C-17) presents all criteria as rows in a single table, enabling cross-criterion pattern analysis (e.g., clustering in one category, redundancy detection) without manual aggregation. An audit artifact that records criteria in separate sections requires the reviewer to reconstruct the cross-criterion view manually. | depth | aspirational | The audit artifact contains a single table with one row per criterion. The table includes at minimum: criterion ID, a discriminating element for the most recent check, and the verdict. Cross-criterion patterns (e.g., all essential criteria cover the same category) must be detectable by reading the table alone, without reading individual criterion rows or separate audit sections. Pass if a reviewer could identify category clustering or redundancy from the table alone, without navigating the full rubric. |
| C-22 | **Structural sequencing enforced by both ordering constraint and explicit anti-deferral instruction** — Calibration-pair production (C-12, C-14) must be governed by both a structural ordering constraint (calibration pair must be produced before the criterion row is finalized) and an explicit behavioral instruction prohibiting deferral ("do not write all criteria first and add calibration pairs later"). Neither alone is sufficient: the ordering constraint is gameable by producing an empty pair; the anti-deferral instruction is ignorable without a structural enforcement mechanism. | behavior | aspirational | The calibration-pair instruction includes two co-located elements: (a) a structural ordering constraint (language specifying the production order — "write the calibration pair before finalizing the criterion row"), and (b) an explicit anti-deferral behavioral instruction ("do not batch calibration pairs at the end" or equivalent prohibition). A calibration-pair instruction that includes only one of these elements fails this criterion. Pass if a builder who reads only the calibration-pair instruction would encounter both a sequencing requirement and a deferral prohibition. |
| C-23 | **Structural verification phase gates Auditor entry** — C-20 requires per-criterion gates to block forward progress rather than repair backward omissions. C-23 extends this principle one level up: the structural heading-scan (or equivalent structural completeness check) must itself be a hard gate that blocks Auditor Phase entry. A structural scan that allows the Auditor phase to begin despite detected heading-pattern violations is a detection step, not a gate. | behavior | aspirational | The Structural Verification step includes explicit blocking language that prevents the Auditor Phase from beginning until all structural checks pass. Language such as "if any scan fails, do not proceed to the Auditor Phase" is required. A structural scan that reports findings as observations — allowing the Auditor Phase to proceed regardless of result — does not satisfy this criterion. Pass if violating the structural check produces a hard stop that the workflow cannot route around without ignoring the gate language. |
| C-24 | **Three-level verification chain covers non-overlapping defect classes** — C-16 requires two gate levels (phase and per-criterion). C-24 requires the full verification architecture to cover three distinct defect classes in sequence, each addressed by a mechanism the prior level cannot handle: (1) per-criterion omission prevention at write time, (2) structural pattern bypass detection post-phase, and (3) cross-criterion specificity analysis at audit time. | depth | aspirational | Identify the three verification mechanisms and the defect class each addresses. The defect classes must be non-overlapping: a defect caught by mechanism 2 must be structurally undetectable by mechanism 1 (because it requires a completed phase to scan), and a defect caught by mechanism 3 must require cross-criterion comparison (not per-criterion checking). Pass if the three mechanisms address: (1) builder skipping a required check mid-criterion, (2) builder completing all per-criterion checks but producing the wrong structural pattern, and (3) builder producing correct patterns but with redundant or non-discriminating pass conditions across criteria. |
| C-25 | **Anti-deferral prohibition co-located at the operative sub-step, not only in phase header** — C-22 requires an explicit anti-deferral behavioral instruction paired with structural sequencing. C-25 specifies where that instruction must appear: inside the sub-step body it governs (e.g., directly inside the calibration-pair sub-step instruction), not only in a phase preamble or section header that a builder could skip while following the sub-steps. | behavior | aspirational | Locate the anti-deferral instruction required by C-22. Verify that it appears inside the sub-step body where the gated action is performed (not only in a phase header or preamble above the sub-step). A rubric where the anti-deferral instruction appears only in a phase introduction paragraph — with no restatement inside the operative sub-step instruction — does not satisfy this criterion. Pass if a builder following only the sub-step instructions (skipping all preamble text) would still encounter the anti-deferral prohibition at the point where deferral is possible. |
| C-26 | **Phase-level workflow transitions marked by named section headings** — C-19 requires per-criterion artifact outputs to appear as named markdown section blocks, making their absence detectable by heading scan without reading content. C-26 extends this principle one level up: each workflow phase transition is marked by a named section heading (e.g., `### AUTHOR PHASE`, `### STRUCTURAL VERIFICATION`, `### AUDITOR PHASE`), making the position of structural verification between Author and Auditor phases verifiable by heading scan. | format | aspirational | The workflow's phase transitions are marked by named section headings that appear in the output. A reviewer scanning only section headings (not reading prose) can confirm: (a) the Author Phase precedes Structural Verification, (b) Structural Verification precedes the Auditor Phase, and (c) the Auditor Phase is the final phase. A workflow where phase transitions are conveyed only through numbered steps or prose instructions — with no heading-level markers in the output — does not satisfy this criterion. Pass if the phase sequence is verifiable by heading scan without reading any instruction or prose content. |
| C-27 | **Cross-criterion verification produces a named artifact with a forward-blocking gate** — C-15 requires each mandatory per-criterion check to emit an independent output artifact before the criterion is sealed, making omission detectable by structural gap. C-19 requires those artifacts to be named markdown section blocks. C-27 extends both to cross-criterion verification: any mechanism that checks properties across multiple criteria simultaneously (e.g., redundancy check, category coverage check) must produce a named section block and carry a forward-blocking gate that prevents subsequent steps from beginning until the cross-criterion check is complete. | behavior | aspirational | The rubric includes at least one cross-criterion verification mechanism (operating on the set of criteria together, not on individual criteria). This mechanism produces a named section block (e.g., `### REDUNDANCY-CHECK-TABLE`) and carries explicit blocking language preventing forward progress (e.g., "DO NOT proceed to Structural Verification until this table is complete and all pairs are confirmed non-redundant"). A cross-criterion check that produces only a prose summary embedded in a phase header, without a named block and a gate, does not satisfy this criterion. Pass if the cross-criterion check artifact is detectable by heading scan and the gate is explicit. |
| C-28 | **Scoring formula denominators explicitly annotated with the criterion ID range they cover** — The scoring formula uses a fixed denominator for each tier (5 for essential, 3 for recommended, N for aspirational). As aspirational criteria are added across rubric versions, the aspirational denominator changes. Without an annotation, a scorer must count the aspirational criteria in the table to verify the denominator is current — a manual step that is skipped in practice, allowing stale denominators to persist undetected. | format | aspirational | The scoring formula in the rubric includes an inline comment or annotation (e.g., `/* C-09 through C-52 */`) stating the criterion ID range covered by the aspirational denominator. The annotation must be co-located with the formula (not in a separate note), so a scorer verifying the denominator can do so by reading the formula line alone. A formula with a numeric denominator and no ID-range annotation does not satisfy this criterion. Pass if a scorer could verify the aspirational denominator's correctness without counting rows in the criteria table. |
| C-29 | **Mechanism overview declares structural output format requirement alongside behavioral requirement** — C-27 requires cross-criterion verification mechanisms to produce named section blocks; C-19 requires per-criterion check mechanisms to produce named section blocks. Both criteria govern what must be produced. C-29 requires the mechanism overview (or equivalent orientation section) to declare the structural format (named `###` heading) each mechanism will produce, so that a builder reading the orientation section can derive the complete expected heading structure before any phase begins. Without this declaration, the structural contract is discoverable only by reading the operative phase instructions — the builder cannot build an output skeleton from the orientation section alone. | behavior | aspirational | The rubric's MECHANISMS section (or equivalent named orientation block) lists each mechanism governed by C-19 and C-27 and names: (a) the mechanism's function (what it checks), and (b) the structural output format it produces (the named `###` heading it will emit, e.g., `### INERTIA-RECORD [C-NN]`). A MECHANISMS section that lists mechanism names and functions without naming the output heading pattern does not satisfy this criterion. Pass if a builder reading only the MECHANISMS section could construct the complete expected section heading sequence for a full rubric output, without reading any phase-level instructions. |
| C-30 | **Structural Verification explicitly declares its phase-bounded coverage scope and names the deferral destination for out-of-scope checks** — C-24's three-level verification chain assigns each level a distinct defect class. C-23 requires Structural Verification to gate Auditor entry. Neither criterion requires SV to declare what it does not check. Without an explicit scope declaration, a builder using SV may believe it verifies heading patterns from all phases — including Auditor Phase headings — when it covers only Author Phase output. | behavior | aspirational | The Structural Verification step's instructions include an explicit scope statement identifying: (a) the phase whose output it covers (Author Phase only, or equivalent), (b) at least one named category of heading check that is out of scope (e.g., Auditor Phase heading patterns), and (c) the mechanism or phase where the deferred check will be performed. A Structural Verification step with no scope declaration — leaving coverage extent implicit — does not satisfy this criterion. Pass if a builder reading the SV scope statement could enumerate both what SV checks and what it explicitly defers, with the deferral destination named. |
| C-31 | **Pre-phase output skeleton declared from MECHANISMS alone and cross-referenced in Structural Verification** — C-29 requires the mechanism overview to declare the structural format (named headings) each mechanism will produce, making the structural contract visible at orientation time. C-31 extends that declared contract into an executable artifact: the workflow includes a pre-phase step where the builder constructs the complete heading skeleton of the expected output from the mechanism overview alone — before any phase begins. This skeleton step serves as an executable test of C-29's textual sufficiency claim. The skeleton also creates a second independent source of truth alongside the instruction-derived heading patterns: Structural Verification cross-references the skeleton against actual output, catching headings the builder declared but failed to produce — a builder-commitment failure structurally distinct from a heading-pattern miss. | behavior | aspirational | The rubric's workflow includes a named pre-phase step (positioned before the Author Phase begins) that instructs the builder to construct the complete output heading skeleton from the MECHANISMS section alone. Structural Verification cross-references this builder-declared skeleton against actual output in a dedicated scan step, distinct from the instruction-derived heading-pattern scan. Pass if both (a) the pre-phase skeleton step exists with an instruction to derive the heading list from MECHANISMS alone, and (b) Structural Verification includes a dedicated scan step that compares actual output against the builder-declared skeleton (not just against the instruction-derived patterns). |
| C-32 | **Mechanism overview declares the pre-phase skeleton step as a named mechanism with its heading pattern** — C-29 requires the mechanism overview to declare the structural output format (named `###` heading) for every mechanism governed by C-19 and C-27. C-31 requires the workflow to include a pre-phase skeleton step. C-32 extends C-29 to cover the skeleton step itself: the mechanism overview must include an entry for the pre-phase skeleton step naming its section heading (e.g., `### PHASE 0 -- OUTPUT SKELETON`) and describing its role, so that a builder reading only the orientation section can derive the complete output heading skeleton — including the pre-phase section — before any phase begins. | behavior | aspirational | The rubric's mechanism overview (or equivalent opening orientation section) contains an entry for the pre-phase skeleton step naming: (a) its section heading pattern (e.g., `### PHASE 0 -- OUTPUT SKELETON`), and (b) its role in the workflow. A mechanism overview that lists only the inertia-record, calibration-pair, criterion, and redundancy-check mechanisms — without an entry for the skeleton step — does not satisfy this criterion, even if the skeleton step is fully specified in its operative phase-instruction section. Pass if a builder reading only the mechanism overview — without reading any phase instructions — could include the pre-phase skeleton heading in their output structure plan and derive a complete heading skeleton. |
| C-33 | **Structural Verification opens with a two-source preamble naming both sources of truth and the non-overlapping failure classes before the scans begin** — C-31 requires Structural Verification to cross-reference both the instruction-derived heading patterns and the builder-declared skeleton. C-33 requires SV to open with an explicit preamble that (1) names both sources of truth, (2) names the distinct failure class each source catches — heading-pattern miss (instruction scan) and builder-commitment failure (skeleton comparison) — and (3) states that these failure classes are non-overlapping, positioned before any scan step begins. | behavior | aspirational | The Structural Verification step's instructions include a preamble (positioned before any scan step or scan-heading) that explicitly states: (a) the names of both sources of truth being checked, (b) the distinct failure class each source catches, and (c) that these failure classes are non-overlapping. A Structural Verification step whose first content is a scan step or scan heading — with no architectural preamble — does not satisfy this criterion, even if both scans are present and correctly cross-reference both sources. Pass if a builder reading only the SV preamble — without reading the scan step instructions — could state why two sources are needed, which failure class each catches, and why collapsing to a single scan would leave one failure class undetected. |
| C-34 | **Pre-phase skeleton step labeled as Phase 0 (or equivalent positional phase marker), integrating it into the scannable phase sequence** — C-26 requires each workflow phase transition to be marked by a named section heading. C-31 requires a named pre-phase skeleton step. C-34 requires the skeleton step's heading to use a positional phase label (e.g., `### PHASE 0 -- OUTPUT SKELETON`), integrating it into the C-26 scannable phase sequence so that the complete workflow sequence Phase 0 -> Author Phase -> Structural Verification -> Auditor Phase is readable from headings alone. | format | aspirational | The pre-phase skeleton step (required by C-31) uses a section heading that includes a positional phase label (e.g., `### PHASE 0 -- OUTPUT SKELETON`, `### PRE-PHASE -- OUTPUT SKELETON`, or equivalent). A skeleton step named only by task (e.g., `### OUTPUT SKELETON`) without a phase-positional prefix does not satisfy this criterion. Pass if a reviewer scanning only section headings can identify the skeleton step as a distinct phase and confirm the complete workflow sequence Phase 0 -> Author Phase -> SV -> Auditor Phase. |
| C-35 | **Competitor-defeat annotations co-located at mechanism introduction points and in the Structural Verification preamble** — C-32 requires the mechanism overview to declare each mechanism's heading pattern and role. C-33 requires the SV preamble to declare both sources of truth and their non-overlapping failure classes. C-35 extends both: each MECHANISMS entry includes a named annotation identifying which structural gap in a known competing protocol the mechanism closes; and the SV preamble names at least one competing SV design by label and identifies the failure class that alternative would miss. Without provenance annotations, the mechanism set satisfies completeness but is silent on minimality. | depth | aspirational | (a) Each entry in the MECHANISMS section includes a statement identifying the competing protocol or design approach whose structural gap this mechanism closes. (b) The Structural Verification preamble names at least one competing SV architecture by label and identifies the specific failure class that architecture would miss. A MECHANISMS section with structural declarations (satisfying C-32) but no competitor-gap annotations does not satisfy condition (a). An SV preamble with failure-class declarations (satisfying C-33) but no named competing design does not satisfy condition (b). Pass if a maintainer reading only the MECHANISMS section and the SV preamble could construct the competitor landscape — named alternatives and their failure modes — without consulting any external source. |
| C-36 | **Pre-phase skeleton formatted as operational tick-off checklist with live-construction tracking instruction** — C-31 requires the pre-phase skeleton step to construct the complete expected heading structure from the mechanism overview and be cross-referenced in Structural Verification. C-36 extends C-31 by requiring the skeleton to function as a live production-tracking artifact during the Author Phase: the skeleton is formatted as a `- [ ]` checklist where each item corresponds to a required output heading, and the skeleton step includes an explicit instruction to tick each item as the corresponding output block is produced during the Author Phase. | behavior | aspirational | The pre-phase skeleton step (a) formats the expected heading list as a `- [ ]` checklist (one checkbox per required heading), and (b) includes an explicit instruction directing the builder to tick each checkbox as the corresponding output block is produced during the Author Phase (not retrospectively). A skeleton that uses `- [ ]` formatting but omits an instruction to tick during construction — leaving open the interpretation that ticking is a post-construction review activity — does not satisfy condition (b). Pass if the skeleton's format and instructions create a live production-tracking artifact whose checkbox state at the end of the Author Phase is directly interpretable as an omission record independently of Structural Verification. |
| C-37 | **Competing Implementations Preamble structured as a tabular mapping enabling independent completeness and minimality verification** — C-35 requires the SV preamble to name at least one competing SV architecture by label and identify the failure class it would miss. C-37 extends C-35(b) from prose naming to tabular mapping: the Competing Implementations Preamble is structured as a table with one row per mechanism, where each row independently carries the mechanism ID (M-NN), the competing protocol name, and the failure class that protocol misses. This structure enables two independent verifications: (1) completeness — row count is checkable against the mechanism list, and (2) minimality — each row's failure class is readable in isolation. | depth | aspirational | The Structural Verification preamble includes a table — not prose alone — with one row per mechanism in the MECHANISMS section. Each row contains at minimum: (a) mechanism ID (M-NN or equivalent), (b) the name of the competing protocol or design whose structural gap this mechanism closes, and (c) the failure class that competing protocol would miss. Pass if: (1) row count matches mechanism count without reading the MECHANISMS section; (2) each row is self-contained; and (3) failure classes across rows are distinct enough to make redundancy detectable from the table alone. |
| C-38 | **Format-compliance STOP conditions co-located at the declaration step for compliance-critical format distinctions** — When two visually similar formats have non-equivalent compliance value (e.g., `- [ ]` tracked vs `- ` untracked), a STOP condition must appear at the declaration step identifying the non-compliant variant by name and blocking forward progress if it is used. The STOP condition must be co-located at the declaration step (not only in an SV scan), so a builder who does not read SV encounters the enforcement before any phase begins. | behavior | aspirational | For each format requirement where two visually similar formats have non-equivalent compliance value, the declaration step includes a STOP condition that: (a) names the non-compliant format variant explicitly, (b) states the compliance consequence of using the non-compliant variant, and (c) blocks forward progress until the non-compliant entry is corrected. The STOP condition must appear in the step that declares the format requirement (not only in the Structural Verification scan). Pass if a builder following only the declaration step's STOP condition — without reading any other phase instruction — could identify which format variant is non-compliant, understand why using it is a compliance failure, and know that they must correct it before proceeding. |
| C-39 | **Competing Implementations table STOP conditions enforce completeness and minimality as active gates at the declaration point** — C-37 requires the SV preamble to include a tabular Competing Implementations Preamble enabling passive completeness and minimality verification. C-39 extends C-37 by requiring that both verification properties be gate-enforced at the declaration point via two co-located STOP conditions: (1) a completeness STOP blocking forward progress if row count does not equal mechanism count, and (2) a minimality STOP blocking forward progress if any two rows share a failure class. | depth | aspirational | The Competing Implementations table is accompanied by two co-located STOP conditions: (1) a completeness STOP that blocks forward progress if the table row count does not equal the MECHANISMS section mechanism count, naming both the verification step and the blocking action; (2) a minimality STOP that blocks forward progress if any two rows share a failure class, naming the verification step and blocking action. The STOPs must be co-located with the table declaration — not deferred to an SV scan step. Pass if a builder following only the table declaration and its co-located instructions — without reading any SV scan steps — would be blocked from proceeding if completeness fails or minimality fails. |
| C-40 | **Format-compliance STOP consequence statement traces the causal chain to the specific audit mechanism that the non-compliant format defeats** — C-38 requires a format-compliance STOP condition to include a consequence statement identifying the compliance failure when the non-compliant format variant is used. C-40 extends C-38's consequence statement requirement: the consequence must trace the causal chain from the non-compliant format to the specific downstream audit mechanism that is defeated, through three links: (1) the proximate non-compliance, (2) the audit mechanism dependency, and (3) the detection failure. | behavior | aspirational | For each format-compliance STOP condition (required by C-38), the consequence statement names not only the proximate non-compliance but also traces forward through at least three causal links: (a) the non-compliant format property, (b) the audit mechanism that depends on the compliant format property, and (c) the detection failure produced when the non-compliant variant is used. Pass if a maintainer reading only the STOP condition — without reading the SV scan steps — can identify: (a) the non-compliant format variant, (b) the named audit mechanism that depends on the compliant format, and (c) the specific defect class that escapes detection. |
| C-41 | **Named audit roles bound to named phase spans with role-to-phase ownership declarations** — C-17 requires the workflow to include two structurally distinct phases or roles. C-41 extends C-17: each named audit role must be explicitly coupled to a named phase span it owns, so that role compliance is verifiable by phase-label inspection alone without reading per-step instructions. With phase-binding, every phase has a declared owner, and every role has a declared phase. | depth | aspirational | The rubric's workflow (a) assigns each named role to a named phase span via an explicit ownership declaration, and (b) co-locates the ownership declaration with the phase heading (not only in a prose preamble separate from the phase structure). A workflow that names roles in a preamble but does not bind each role to a named phase heading does not satisfy condition (b). Pass if a reviewer scanning only section headings — without reading per-step instructions — can state which named role owns each phase and that each named role has exactly one owning phase. |
| C-42 | **Per-STOP-instance consequence chain artifacts are independent named section blocks with sequential-instance gate** — C-15 requires mandatory check outputs to be independent artifacts whose absence creates a detectable structural gap. C-19 requires those artifacts to be named markdown section blocks. C-42 extends the same requirement to the per-STOP-instance level: each STOP's consequence chain verification must produce an independent named section block, and the block for STOP N must appear before STOP N+1 is written — creating a sequential-instance gate. Without per-STOP named blocks, a builder can silently skip consequence chain verification for a STOP and proceed to STOP N+1 without a visible omission record. | behavior | aspirational | For each STOP condition in the rubric that requires a consequence chain, the workflow instructions require: (a) an independent named section block for each STOP instance (e.g., `### STOP-CONSEQUENCE-RECORD [STOP-NN]`), not an inline annotation; and (b) a sequential-instance gate instruction stating that the STOP N consequence chain artifact must be produced and its named block written before writing STOP N+1. Pass if a reviewer scanning section headings alone — without reading any STOP declaration content — can confirm that every STOP declaration is followed by a consequence-chain named block before the next STOP declaration appears, and that any STOP whose consequence block is absent creates a visible heading-scan gap. |
| C-43 | **Per-quotation path attribution in Phase 8.5 EVIDENCE QUOTATION GATE notes** — Each quotation in the Phase 8.5 EVIDENCE QUOTATION GATE carries a co-located note that names: (a) which enforcement path the quotation confirms (path 1: construction-phase mechanism; path 2: pre-emit mechanism, or equivalent labels), and (b) the structural phase at which that path operates (construction-phase or pre-emit). Without path attribution, a reviewer auditing Phase 8.5 cannot determine from the quotation block which enforcement mechanism is confirmed by which quotation without reading the DUAL-PATH ENFORCEMENT DECLARATION. With per-path + per-phase attribution co-located in the note, each quotation becomes self-contextualizing: its enforcement role is auditable from Phase 8.5 alone. Distinct from C-45 (which requires each note to additionally justify why its enforcement path is independently necessary by stating the failure-mode consequence): C-43 requires only self-contextualizing attribution (path + phase), not self-justifying argument. | behavior | aspirational | Each Phase 8.5 quotation note names (a) the enforcement path by label (e.g., "enforcement path 1", "path 2") and (b) the structural phase at which it operates (e.g., "construction-phase SCOPE GATE", "pre-emit STOP CONDITION"). A note that reads only a path label (no phase) or only a phase name (no path label) fails C-43. A note of the form "Enforcement path 1 -- construction-phase SCOPE GATE (Phase 5)" (path + phase + named mechanism) passes C-43. A note that confirms path and phase but adds no failure-mode argument passes C-43 but fails C-45. |
| C-44 | **DUAL-PATH ENFORCEMENT DECLARATION explicitly states per-path independent necessity** — The DUAL-PATH ENFORCEMENT DECLARATION (or equivalent non-redundancy section co-located before Phase 8.5) states, for each enforcement path, the specific coverage gap that would remain if only the other path were present. A declaration that lists both paths without per-path necessity arguments names the paths but does not enable a reviewer to verify that the two-path architecture is minimal rather than redundant. With per-path necessity arguments, the non-redundancy is structurally declared: removing path 1 reveals a coverage gap distinct from the gap revealed by removing path 2, and both gaps are nameable from the declaration alone. A declaration listing paths without per-path gap statements fails C-44 even if both paths are named correctly. Distinct from C-43 (which requires path attribution in per-quotation notes): C-44 requires the DUAL-PATH ENFORCEMENT DECLARATION itself to carry per-path independence arguments naming the coverage gap each removal creates. | depth | aspirational | The DUAL-PATH ENFORCEMENT DECLARATION (or equivalent) includes, for each enforcement path, a statement naming the coverage gap that results from removing that path — the gap must be distinct from the gap named for the other path, and must identify a structural failure mode (not a behavioral preference). A declaration listing paths with names but without coverage gap arguments fails C-44. Pass if a reviewer reading only the DUAL-PATH ENFORCEMENT DECLARATION — without running isolation experiments or reading prior scorecard results — can state: (a) which coverage gap path 1 prevents, (b) which coverage gap path 2 prevents, and (c) that the two gaps are structurally distinct such that each path is independently necessary. |
| C-45 | **Per-quotation in-gate failure-mode argument in Phase 8.5 EVIDENCE QUOTATION GATE notes** — Each Phase 8.5 in-gate note already attributes path + phase (C-43). C-45 requires each note to additionally state the specific structural failure that would pass Phase 8.5 unchallenged if that enforcement path were absent — making the note self-justifying rather than merely self-contextualizing. The failure-mode argument converts the note from a label ("this quotation confirms path 1") to a justification ("this quotation confirms path 1; absent path 1, [specific structural failure] would pass Phase 8.5 unchallenged because [mechanism]"). A reviewer auditing only the Phase 8.5 gate block can then verify both which path the quotation confirms (C-43) and why that path is independently necessary (C-45). V-04 of R16 demonstrates PASS C-43 / FAIL C-45: notes name the enforcement path and structural phase but add no failure-mode argument. V-04 of R17 demonstrates PASS C-43 / PASS C-45: notes carry both path attribution and failure-mode argument. Independence established: C-43 note structure (path + phase) is present without C-45 failure-mode extension. Distinct from C-47 (which extends C-45 by requiring the failure-mode clause to name the specific criterion ID that would be unenforced): C-45 requires a failure-mode argument (structural failure named), not a criterion-ID-indexed argument. | behavior | aspirational | Each Phase 8.5 in-gate path attribution note contains both the path + phase attribution required by C-43 AND a failure-mode clause stating what specific structural failure would pass Phase 8.5 unchallenged if that enforcement path were absent. A note that reads "Enforcement path 1 -- construction-phase SCOPE GATE (Phase 5). This quotation confirms path 1." (path + phase, no failure-mode argument) passes C-43 but fails C-45. A note that reads "Enforcement path 1 -- construction-phase SCOPE GATE (Phase 5). If this path were absent, a rubric with prose-only prohibition text would pass Phase 8.5 unchallenged." passes both C-43 and C-45. Pass if both Quotation A and Quotation B notes contain a failure-mode clause describing a specific structural failure that the absent path would leave unchallenged. |
| C-46 | **Criterion-indexed competitor architecture in STATUS QUO COMPETITOR foil items** — STATUS QUO COMPETITOR foil items name specific enforcement-architecture failure modes at criterion-class precision: each foil item identifies which criterion class the STATUS QUO fails (e.g., "absent per-quotation path attribution -> C-43 failure" rather than "fails to include per-quotation path attribution notes"). This makes the competitor block a discrimination device: it specifies exactly which criteria the STATUS QUO cannot satisfy, enabling a reviewer to determine at criterion-class resolution what a rubric built from the STATUS QUO prompt would score. Criterion-class precision distinguishes C-46 from generic foil items (prose description without criterion-class anchoring) and from structural-mechanism items (absent mechanisms without criterion-class mapping). V-03 of R16 demonstrates PASS C-46 / FAIL C-43 / FAIL C-44: criterion-indexed foil items present but structural mechanisms absent. V-04 of R16 demonstrates PASS C-43 / PASS C-44 / FAIL C-46: structural mechanisms present but foil items generic. Independence established from both directions. Distinct from C-48 (which extends C-46 by requiring foil items to name both passing and failing criteria as a pair): C-46 requires foil items to name failing criteria at criterion-class precision; passing criteria pairing is not required. | behavior | aspirational | Each STATUS QUO COMPETITOR foil item names at least one criterion ID (e.g., "C-43", "C-44") as the criterion class the STATUS QUO fails. A foil item that describes the structural gap in prose without citing a criterion ID (e.g., "fails to include per-quotation path attribution" without "-> C-43 failure") fails C-46 even if the structural gap is accurately described. Pass if the STATUS QUO COMPETITOR block contains at least two criterion-indexed foil items and an evaluator reading only the STATUS QUO block can determine at criterion-ID precision which criteria a rubric built from that prompt would fail. |
| C-47 | **Per-note failure-mode argument names specific criterion ID** — C-45 requires each Phase 8.5 in-gate note to state what structural failure would pass the gate unchallenged if the enforcement path were absent. C-47 requires that failure-mode clause to additionally name the specific criterion (by ID) that would be unenforced if the enforcement path were absent — e.g., "Absent path 1, C-43 is unenforced at Phase 8.5 entry" rather than "Absent path 1, the per-quotation path attribution requirement would pass unchallenged." The criterion-ID reference converts the failure-mode argument from a prose description of a structural gap to a criterion-indexed compliance claim: an auditor tracing only criterion IDs can locate the criterion being protected without reading surrounding context. V-01 of R17 demonstrates PASS C-45 / FAIL C-47: failure-mode arguments present in both Quotation A and B notes (satisfying C-45), but no criterion IDs named in the failure-mode clauses. V-04 of R17 demonstrates PASS C-45 / PASS C-47: failure-mode arguments present and each names the criterion ID that the absent path would leave unenforced. Independence established: C-45 failure-mode argument structure is present without criterion-ID extension. Distinct from C-45 (which requires a failure-mode argument naming the structural failure in prose): C-47 requires the argument to additionally name the criterion ID, making the claim criterion-indexed rather than prose-described. Distinct from C-49 (which extends C-47 by requiring the criterion-ID inclusion to be enforced by a FORMAT TEMPLATE slot rather than prose instruction): C-47 requires criterion-ID presence, C-49 requires that presence to be structurally mandated. | behavior | aspirational | Each Phase 8.5 in-gate note's failure-mode clause (satisfying C-45) names at least one criterion ID as the criterion that would be unenforced if the enforcement path were absent. A failure-mode clause that describes the structural gap without citing a criterion ID (e.g., "if this path were absent, path attribution in notes would be unverified") fails C-47 even if the clause satisfies C-45. A failure-mode clause of the form "Absent this path, C-43 is unenforced at Phase 8.5 entry" (names criterion ID) passes C-47. Pass if both Quotation A and Quotation B notes contain a failure-mode clause that cites at least one criterion ID as the criterion left unenforced by the absent enforcement path. |
| C-48 | **Pass/fail criterion-pair framing in STATUS QUO COMPETITOR foil items** — C-46 requires STATUS QUO COMPETITOR foil items to name the criterion class the STATUS QUO fails at criterion-class precision. C-48 requires each foil item to additionally name the criteria the STATUS QUO PASSES in the same item — e.g., "passes C-41 and C-42 while failing C-43" rather than "fails C-43." The pass/fail criterion pair converts the foil item from a single-axis failure claim to a bidirectional discrimination statement: it specifies both what the STATUS QUO achieves and what it fails, locating the STATUS QUO within the criterion lattice at paired-criterion resolution. This discrimination device enables a reviewer to determine — without consulting the scorecard — where a rubric built from the STATUS QUO prompt sits in the criterion space: which criteria it achieves and which it fails. V-02 of R17 demonstrates PASS C-46 / FAIL C-48: failing criteria cited by criterion ID (C-43 failure and C-44 failure named), but no passing criteria named alongside the failing criterion. V-04 of R17 demonstrates PASS C-46 / PASS C-48: both failing and passing criteria named in each foil item. Independence established: C-46 criterion-indexed foil item structure is present without pass-criteria pairing. Distinct from C-46 (which requires foil items to name the failing criterion at criterion-class precision): C-48 requires the foil item to additionally name at least one criterion the STATUS QUO passes, converting the foil item from a failure-only claim to a bidirectional discrimination statement. Distinct from C-50 (which extends C-48 by requiring the pass-side to enumerate a full criterion span rather than a subset): C-48 requires at least one passing criterion named; full-span coverage is not required. | behavior | aspirational | Each STATUS QUO COMPETITOR foil item names at least one criterion ID that the STATUS QUO FAILS (satisfying C-46) AND at least one criterion ID that the STATUS QUO PASSES in the same item. A foil item of the form "fails C-43" (failing criterion named, no passing criteria) fails C-48 even if it satisfies C-46. A foil item of the form "passes C-41 and C-42 while failing C-43" (passing and failing criteria both named in the same item) passes C-48. Pass if the STATUS QUO COMPETITOR block contains at least two foil items each with bidirectional criterion-pair framing, and an evaluator reading only the STATUS QUO block can determine both which criteria the STATUS QUO achieves and which it fails at criterion-ID precision. |
| C-49 | **Failure-mode clause format-templated with mandatory criterion-ID slot** — C-47 requires each Phase 8.5 in-gate note's failure-mode clause to name the criterion ID that would be unenforced if the enforcement path were absent. C-49 requires that criterion-ID inclusion to be enforced by a FORMAT TEMPLATE with a mandatory `[C-NN]` slot — e.g., the note template reads `"Absent this path, [C-NN] is unenforced at Phase 8.5 entry"` — rather than by a prose instruction that the builder must follow voluntarily. The FORMAT TEMPLATE converts C-47's behavioral requirement (the failure-mode clause must name a criterion ID) into a structural requirement: a builder following the template encounters a slot that can only be filled by a criterion ID, making omission visible as an unfilled `[C-NN]` rather than a missing prose element. A rubric that achieves C-47 via a prose instruction of the form "include the criterion ID in the failure-mode clause" satisfies C-47 but fails C-49 — the criterion-ID is present but not slot-enforced. V-03 of R18 demonstrates PASS C-47 / PASS C-49: FORMAT TEMPLATE with mandatory `[C-NN]` slot is present in the Phase 8.5 note instructions. V-01 of R18 demonstrates PASS C-47 / FAIL C-49: criterion IDs appear in failure-mode clauses via prose instruction, but no template slot mandates the slot. Independence established: C-47 criterion-ID presence is achievable without C-49 template enforcement. Distinct from C-47 (which requires criterion-ID naming in the failure-mode clause): C-49 requires that naming to be structurally mandated by a template slot rather than behaviorally instructed by prose. | behavior | aspirational | The Phase 8.5 note instructions include a FORMAT TEMPLATE for the failure-mode clause that contains a mandatory `[C-NN]` slot (or equivalent structural placeholder for a criterion ID). A rubric whose Phase 8.5 instructions state "each note must include the criterion ID that would be unenforced" (prose requirement) without providing a fill-in template does not satisfy C-49. A rubric whose Phase 8.5 note instructions include a template of the form `"Absent this path, [C-NN] is unenforced at Phase 8.5 entry"` with an explicit instruction that `[C-NN]` must be replaced by the criterion ID satisfies C-49. Pass if a builder following only the FORMAT TEMPLATE — without reading the surrounding prose instructions — would produce a failure-mode clause containing a criterion ID by filling in the slot, and if omitting the criterion ID would be visible as an unfilled placeholder rather than a silent omission. |
| C-50 | **Full criterion-span claim in STATUS QUO COMPETITOR foil items** — C-48 requires each STATUS QUO COMPETITOR foil item to name at least one criterion the STATUS QUO passes alongside the criterion it fails. C-50 requires the pass-side of each foil item to enumerate a full criterion span: every criterion between the first criterion in the span and the last criterion in the span that the STATUS QUO passes, inclusive — e.g., "passes C-41, C-42, C-43, C-44, and C-45 while failing C-46" rather than "passes C-41 and C-45 while failing C-46." The full-span claim converts the foil item from a partial bidirectional discrimination statement (C-48) to a precision criterion-lattice locator: a reviewer reading only the STATUS QUO COMPETITOR block can determine — at criterion-ID resolution and without gaps — exactly where in the criterion sequence the STATUS QUO achieves compliance and where it fails, without consulting the scorecard or criteria table. A foil item that names some but not all passing criteria in the span satisfies C-48 but fails C-50. V-02 of R18 demonstrates PASS C-48 / FAIL C-50: passing criteria named alongside failing criterion (satisfying C-48), but not all passing criteria in the span are named. V-04 of R18 demonstrates PASS C-48 / PASS C-50: every passing criterion in the span is named in each foil item. Independence established: C-48 bidirectional framing is achievable without C-50 full-span coverage. Distinct from C-48 (which requires at least one passing criterion named): C-50 requires all passing criteria in the span to be named, making the foil item a complete lattice-position statement rather than a partial one. Distinct from C-52 (which extends C-50 by requiring a SPAN DEFINITION pre-step to structurally pre-declare the span before the foil item is written): C-50 requires full-span enumeration to be present in the foil item text; C-52 requires the enumeration to be derived from a pre-declared SPAN DEFINITION block. | behavior | aspirational | Each STATUS QUO COMPETITOR foil item names every criterion in the passing span — all criteria between the span start and the failing criterion — on the pass side. A foil item of the form "passes C-41 and C-42 while failing C-46" (criteria C-43, C-44, C-45 omitted from pass side) fails C-50 even if it satisfies C-48. A foil item of the form "passes C-41, C-42, C-43, C-44, and C-45 while failing C-46" (every passing criterion in the span named) passes C-50. Pass if the STATUS QUO COMPETITOR block contains at least two foil items each with full-span pass-side enumeration, and an evaluator reading only the STATUS QUO block can determine — without gaps and without consulting the criteria table — at which criterion-ID boundary the STATUS QUO transitions from passing to failing. |
| C-51 | **Phase 8.5 FORMAT TEMPLATE slot gate-enforced by co-located STOP condition** — C-49 requires Phase 8.5 failure-mode clauses to be governed by a FORMAT TEMPLATE with a mandatory `[C-NN]` slot, making criterion-ID inclusion structurally required rather than behaviorally instructed. C-51 extends C-49 by requiring that the FORMAT TEMPLATE's `[C-NN]` slot be further enforced by a STOP condition co-located with the template that blocks Phase 8.5 note completion until the slot is filled with a valid criterion ID. The STOP converts the C-49 structural slot from an omission detectable at SV time (unfilled placeholder is visible in the artifact) to a gate failure at note-completion time: a builder who leaves `[C-NN]` unfilled encounters an explicit block before the note is sealed, not a deferred SV miss. A FORMAT TEMPLATE with a `[C-NN]` slot but no co-located STOP satisfies C-49 but fails C-51 — the slot is structurally present but not gate-enforced at completion time. V-01 of R19 demonstrates PASS C-49 / PASS C-51: FORMAT TEMPLATE with `[C-NN]` slot is present AND a co-located STOP blocks note completion when the slot is unfilled. R18 baseline FORMAT TEMPLATE variation demonstrates PASS C-49 / FAIL C-51: slot present, no co-located STOP gate, establishing independence. Distinct from C-49 (which requires a FORMAT TEMPLATE with a mandatory `[C-NN]` slot): C-51 requires that slot to be gate-enforced at note-completion time by a co-located STOP condition, not only SV-detectable as an unfilled placeholder. | format | aspirational | The Phase 8.5 note instructions include a FORMAT TEMPLATE with a mandatory `[C-NN]` slot (satisfying C-49) AND a STOP condition co-located with that template that blocks Phase 8.5 note completion if the `[C-NN]` slot is unfilled. A FORMAT TEMPLATE without a co-located STOP fails C-51 even if the `[C-NN]` slot is structurally present. The STOP must be co-located with the template (not only in an SV scan or a preamble), so a builder following only the template instructions encounters the STOP before the note is sealed. Pass if a builder following only the Phase 8.5 note template — without reading SV instructions — is blocked from completing the note when `[C-NN]` is left unfilled, making slot omission a gate failure at completion time rather than an SV-time miss. |
| C-52 | **STATUS QUO foil items preceded by SPAN DEFINITION pre-step** — C-50 requires each STATUS QUO COMPETITOR foil item to enumerate every passing criterion in the span on the pass side. C-52 requires each foil item to be preceded by a SPAN DEFINITION pre-step that structurally declares the span before the foil item is written: the pre-step names (a) the previous failure criterion or START, (b) the current failure criterion, and (c) the full pass-side enumeration; the foil item text is then transcribed from the SPAN DEFINITION's pass-side list. The SPAN DEFINITION converts C-50's observed completeness property (the foil item happens to name all passing criteria) into a structurally pre-declared computation: a reviewer auditing the STATUS QUO block can verify span completeness by checking the SPAN DEFINITION's pass-side list against the criteria table without counting criteria in the foil item text. A foil item with full-span enumeration but no SPAN DEFINITION pre-step satisfies C-50 but fails C-52 — the span is enumerated but not structurally pre-declared. V-03 of R19 demonstrates PASS C-50 / PASS C-52: each foil item is preceded by a SPAN DEFINITION block with three declared fields (previous failure, current failure, pass-side enumeration); V-02 of R19 demonstrates PASS C-50 / FAIL C-52: full-span enumeration present in foil items, no SPAN DEFINITION pre-steps. Independence established: C-50 full-span coverage is achievable without C-52 SPAN DEFINITION pre-step structure. Distinct from C-50 (which requires full-span enumeration to be present in the foil item text): C-52 requires that enumeration to be derived from an explicit SPAN DEFINITION pre-step declared before the foil item is written, making span completeness structurally pre-declared rather than only post-hoc observable. | format | aspirational | Each STATUS QUO COMPETITOR foil item is preceded by a SPAN DEFINITION block declaring all three fields: (a) previous failure criterion (e.g., "Previous failure: C-39" or "Previous failure: START"), (b) current failure criterion (e.g., "Current failure: C-46"), and (c) pass-side enumeration listing every criterion in the span (e.g., "Pass side: C-40, C-41, C-42, C-43, C-44, C-45"). A foil item with full-span enumeration but no SPAN DEFINITION pre-step fails C-52. A foil item preceded by a SPAN DEFINITION block with only two of the three fields fails C-52. Pass if every foil item is preceded by a SPAN DEFINITION block with all three declared fields, and the foil item's pass-side list matches the SPAN DEFINITION's pass-side enumeration, making span completeness structurally pre-declared and independently verifiable from the SPAN DEFINITION alone. |

---

## Scoring

```
composite = (essential_pass / 5 * 60)
          + (recommended_pass / 3 * 30)
          + (aspirational_pass / 44 * 10)  /* C-09 through C-52 */
```

**Note**: aspirational denominator is 44 (C-09 through C-52).

**Golden threshold**: all 5 essential criteria pass AND composite >= 80.

| Band | Score | Interpretation |
|------|-------|----------------|
| Golden | all essential + >= 80 | Ship-ready rubric -- use as objective function |
| Acceptable | all essential + 70-79 | Minor gaps, usable with reviewer awareness |
| Marginal | all essential + < 70 | Coverage or ordering gaps, iterate before use |
| Fail | any essential fails | Not a valid objective function regardless of total score |

---

## Baseline for R20

A variation satisfying all R19 criteria (C-49 PASS, C-50 PASS) but lacking a co-located STOP
gate on the FORMAT TEMPLATE slot (C-51 FAIL) or lacking SPAN DEFINITION pre-steps on STATUS QUO
foil items (C-52 FAIL) scores 42/44 x 10 = 9.55, composite <= 99.55. The new discrimination
targets for R20 are C-51 (FORMAT TEMPLATE slot gate-enforced by a co-located STOP condition
blocking Phase 8.5 note completion until [C-NN] is filled -- converts C-49's structural slot
from an SV-detectable omission to a gate failure at note-completion time; V-01 of R19
demonstrates PASS C-49 / PASS C-51 via co-located STOP gate, isolation confirmed; ceiling
variation would add an explicit consequence clause to the STOP naming which audit mechanism is
defeated when the slot is left unfilled, extending C-40's three-link causal chain to the
format-template domain) and C-52 (SPAN DEFINITION pre-step precedes each STATUS QUO foil item,
declaring previous failure criterion, current failure criterion, and pass-side enumeration from
which the foil item is transcribed -- converts C-50's enumeration requirement from an observed
property of the foil item text to a structurally pre-declared computation; V-03 of R19
demonstrates PASS C-50 / PASS C-52 via SPAN DEFINITION pre-step, V-02 of R19 demonstrates
PASS C-50 / FAIL C-52, isolation confirmed; ceiling variation would add a sequential-instance
gate blocking foil item N+1 until SPAN DEFINITION N is written, extending C-42's
sequential-instance pattern to the STATUS QUO domain).

---

## Quick Checklist (for manual scoring)

- [ ] C-01: Every criterion has all five required fields (ID, text, weight, category, pass condition)
- [ ] C-02: Exactly 3-5 essential, 2-3 recommended, 1-2 aspirational
- [ ] C-03: Essential criteria are specific to the target skill, not generic quality observations
- [ ] C-04: Every pass condition is testable by two independent evaluators without discussion
- [ ] C-05: Composite score formula and golden threshold are present
- [ ] C-06: At least 3 distinct categories represented across all criteria
- [ ] C-07: Essential criteria ordered from most to least structurally critical
- [ ] C-08: Quick checklist present with all criterion IDs
- [ ] C-09: Each failing criterion tells the author what to fix without re-running the skill
- [ ] C-10: No two criteria are redundant or contradictory
- [ ] C-11: Every pass condition fails the inertia test (a "clear and comprehensive" rubric would miss it)
- [ ] C-12: At least one concrete generic/grounded example pair present for an essential criterion
- [ ] C-13: At least one hard phase gate with explicit blocking language and a checkable condition
- [ ] C-14: Calibration pair is written immediately after pass-condition drafting, not retrospectively
- [ ] C-15: Each mandatory check step emits an independent output artifact before the criterion is sealed -- skipping leaves a detectable gap
- [ ] C-16: Two gate levels present -- one phase gate and at least one per-criterion gate
- [ ] C-17: Authoring phase and auditing phase are separated, producing independently readable outputs
- [ ] C-18: Audit artifact captures the discriminating element for each condition, not only pass/fail verdicts
- [ ] C-19: Mandatory check outputs are named markdown section blocks -- a missing block = a missing heading, detectable by section scan without reading content
- [ ] C-20: Per-criterion gates block forward progress before the criterion row is written -- not retroactive repair steps that request missing content after the fact
- [ ] C-21: Audit phase produces a single consolidated table (all criteria as rows) enabling cross-criterion pattern analysis without manual aggregation
- [ ] C-22: Calibration pair instruction includes both a structural ordering constraint AND an explicit anti-deferral behavioral instruction -- neither alone is sufficient
- [ ] C-23: Structural verification step (heading-scan) carries blocking language that prevents the Auditor phase from starting until all required patterns are confirmed present
- [ ] C-24: Workflow contains three verification mechanisms targeting non-overlapping defect classes: omission prevention at write time, structural bypass detection post-phase, cross-criterion specificity analysis at audit time
- [ ] C-25: Anti-deferral prohibition appears inside the sub-step body it governs -- not only in a phase header or preamble -- so a builder skipping preamble still encounters it
- [ ] C-26: Phase transitions are marked by named section headings -- the position of structural verification between Author and Auditor phases is verifiable by heading scan, not by reading instruction prose
- [ ] C-27: Any cross-criterion verification mechanism produces a named section block and carries a forward-blocking gate -- its omission is detectable by heading scan
- [ ] C-28: Scoring formula's aspirational denominator is annotated with the criterion ID range it covers (e.g., "C-09 through C-52") -- correctness verifiable without counting criteria in the table
- [ ] C-29: Mechanism overview names the structural output format (e.g., named `###` heading) required for each mechanism governed by C-19/C-27 -- structural contract visible at orientation time, not only at execution time
- [ ] C-30: Structural Verification step explicitly states its phase-bounded coverage scope, names out-of-scope heading patterns, and identifies where each deferred check will be performed
- [ ] C-31: Workflow includes a named pre-phase OUTPUT SKELETON step (built from MECHANISMS alone, before any phase) and Structural Verification cross-references it -- catching builder-declared-but-not-produced headings as a failure mode distinct from heading-pattern misses
- [ ] C-32: Mechanism overview includes an entry for the pre-phase skeleton step naming its heading pattern and role -- so a builder reading only the overview can derive a complete heading skeleton without reading phase instructions
- [ ] C-33: Structural Verification opens with a preamble (before any scan step) naming both sources of truth, the distinct failure class each catches, and that the failure classes are non-overlapping -- two-source architecture declared as design principle, not inferred from scan count
- [ ] C-34: Pre-phase skeleton step heading uses a phase-positional label (e.g., `### PHASE 0 -- OUTPUT SKELETON`) that anchors it as a distinct phase in the scannable sequence -- complete workflow Phase 0 -> Author -> SV -> Auditor verifiable from headings alone
- [ ] C-35: Each MECHANISMS entry names the competing protocol whose structural gap the mechanism closes; SV preamble names at least one competing SV design and the failure class it would miss -- provenance argument embedded in the rubric, not inferred externally
- [ ] C-36: Pre-phase skeleton uses `- [ ]` checklist format with an explicit instruction to tick each item during Author Phase construction -- skeleton is a live production tracker, not only a pre-construction declaration audited at SV time
- [ ] C-37: Competing Implementations Preamble is a table (M-NN | competing protocol | failure class) with one row per mechanism -- row count verifies completeness without reading MECHANISMS, row content verifies each claim without reading aspirational criteria, distinct failure classes across rows enable redundancy detection
- [ ] C-38: For each compliance-critical format distinction, a STOP condition at the declaration step names the non-compliant variant, states the compliance consequence, and blocks forward progress -- format enforcement is gate-backed at declaration time, not only detectable at SV time
- [ ] C-39: Competing Implementations table has two co-located STOP conditions -- one blocking on completeness (row count != mechanism count) and one blocking on minimality (duplicate failure class) -- co-located at the table declaration, not deferred to SV scan
- [ ] C-40: Each format-compliance STOP consequence traces three causal links: non-compliant format -> named audit mechanism dependency -> specific defect class that escapes detection
- [ ] C-41: Each named audit role is bound to a named phase span via an explicit ownership declaration co-located with the phase heading -- role compliance verifiable by heading scan without reading per-step instructions
- [ ] C-42: Each STOP's consequence chain produces an independent named section block (not inline annotations) with a sequential-instance gate -- STOP N named block must appear before STOP N+1 is written, absence detectable as a heading-scan gap during Author Phase construction
- [ ] C-43: Each Phase 8.5 EVIDENCE QUOTATION GATE quotation note names (a) the enforcement path by label and (b) the structural phase at which that path operates -- each quotation is self-contextualizing without reading the DUAL-PATH ENFORCEMENT DECLARATION
- [ ] C-44: DUAL-PATH ENFORCEMENT DECLARATION states, for each enforcement path, the specific coverage gap that remains when that path is removed -- per-path gap statements are distinct and name a structural failure mode, enabling non-redundancy verification without isolation experiments
- [ ] C-45: Each Phase 8.5 in-gate note contains a failure-mode clause stating what specific structural failure would pass Phase 8.5 unchallenged if the enforcement path were absent -- note is self-justifying (not only self-contextualizing), auditable from Phase 8.5 alone
- [ ] C-46: Each STATUS QUO COMPETITOR foil item names at least one criterion ID as the criterion class the STATUS QUO fails (criterion-class precision) -- competitor block is a discrimination device against specific criteria, not a general contrast framing
- [ ] C-47: Each Phase 8.5 in-gate failure-mode clause names at least one criterion ID as the criterion that would be unenforced if the enforcement path were absent -- failure-mode argument is criterion-indexed (e.g., "Absent path 1, C-43 is unenforced at Phase 8.5 entry"), not only prose-described
- [ ] C-48: Each STATUS QUO COMPETITOR foil item names both the criteria the STATUS QUO PASSES and the criterion it FAILS in the same item (pass/fail criterion-pair framing) -- foil item is a bidirectional discrimination statement locating the STATUS QUO within the criterion lattice (e.g., "passes C-41 and C-42 while failing C-43")
- [ ] C-49: Phase 8.5 failure-mode clause is governed by a FORMAT TEMPLATE with a mandatory [C-NN] slot -- criterion-ID inclusion is structurally enforced by the template slot, not only behaviorally instructed by prose, so omission is visible as an unfilled placeholder rather than a silent omission
- [ ] C-50: Each STATUS QUO COMPETITOR foil item enumerates every passing criterion in the span on the pass side -- full-span enumeration makes the foil item a precision criterion-lattice locator (e.g., "passes C-41, C-42, C-43, C-44, and C-45 while failing C-46"), not a partial bidirectional statement that leaves gaps in the pass-side coverage
- [ ] C-51: The FORMAT TEMPLATE's mandatory [C-NN] slot (C-49) is co-located with a STOP condition that blocks Phase 8.5 note completion until the slot is filled with a valid criterion ID -- slot omission is a gate failure at note-completion time, not only an SV-detectable unfilled placeholder
- [ ] C-52: Each STATUS QUO COMPETITOR foil item is preceded by a SPAN DEFINITION pre-step declaring (a) previous failure criterion or START, (b) current failure criterion, and (c) full pass-side enumeration -- foil item text is transcribed from the SPAN DEFINITION, making span completeness structurally pre-declared rather than only post-hoc observable
